module MLF.Elab.Phi.TestSupport (
    VSpine,
    mkVSpine,
    assertSpineSync,
    vSpineBinderAt,
    vSpineNameAt,
) where

import MLF.Elab.Phi.VSpine
    ( VSpine
    , assertSpineSync
    , mkVSpine
    , vSpineBinderAt
    , vSpineNameAt
    )
