# Findings

- `src/MLF/Elab/Run/Scope.hs` still exports `preferGenScope`, and the local Def. 15.3.2 note explicitly describes it as re-running the same lookup that `bindingScopeRef` already performed on the same original constraint, calling the step "redundant but harmless."
- `resolveCanonicalScope` remains the only production caller: it computes `scope0 <- bindingScopeRef constraint scopeRoot`, then immediately calls `scopeBase <- preferGenScope constraint scope0` before canonicalization.
- `preferGenScope` is operationally identity on `GenRef`; on `TypeRef nid` it calls `Binding.bindingPathToRoot constraint (typeRef nid)` again and returns the first `GenRef` from `drop 1 path` or the same `TypeRef nid` fallback, so successful results appear observationally identical to the original `bindingScopeRef` outcome.
- The current test surface is bounded: `test/ScopeSpec.hs` imports `preferGenScope` directly and has one dedicated regression asserting it propagates binding-tree cycle errors, while `resolveCanonicalScope` has its own cycle-propagation regression.
- `test/PipelineSpec.hs` includes source guards for `bindingScopeRefCanonical` / `resolveCanonicalScope` naming but no guard that requires `preferGenScope` to exist, so the expected test fallout is localized to `ScopeSpec` plus any documentation/source-string guards that mention the redundant step.
- `papers/these-finale-english.txt:13631` still anchors Definition 15.3.2 (`Typ(a′)` / `Typ(g a′)` for a subterm). While the OCR'd excerpt is noisy, the nearby scope note in `Scope.hs` still explicitly ties `ga′` to the nearest gen ancestor in the original constraint, and nothing in the thesis/docs inspected here suggests a second original-constraint lookup step is semantically required.
- `TODO.md`'s current rolling priorities focus on other simplifications (Task 43 edge-propagation uniformity and Task 36 generalize/reify alias cleanup), while the completed Task 41 entry already records the last semantic ga-scope repair as error-propagation only. That suggests `preferGenScope` is now cleanup/backlog work rather than an active semantic blocker.
- `implementation_notes.md` currently classifies remaining non-thesis-exact scope as non-semantic backlog. `CHANGELOG.md` records recent ga-scope/view-native cleanup and successful `ga scope` / `resolveCanonicalScope` verification, but neither doc gives `preferGenScope` a positive semantic justification.
- `Bugs.md` ties the recent ga-scope fix to removing `Left _ -> ref` error swallowing in the preference path; the resolved bug does not require keeping a separate redundant helper once error propagation is preserved.
- Fresh verification is currently blocked by unrelated workspace state: `cabal test mlf2-test --test-show-details=direct --test-options='--match "ga scope"'` failed during test-suite compilation because `test/Parity/FrozenArtifacts.hs` and `test/SpecUtil.hs` still reference `Solved.fromPreRewriteState`, which `MLF.Constraint.Solved` no longer exports. This blocker sits outside the proposed `MLF.Elab.Run.Scope` simplification.
