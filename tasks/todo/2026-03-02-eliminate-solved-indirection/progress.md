# Progress Log: Eliminate Solved Indirection

- 2026-03-02: Created isolated worktree `~/.config/superpowers/worktrees/mlf4/codex/eliminate-solved-indirection-plan` on branch `codex/eliminate-solved-indirection-plan`.
- 2026-03-02: Ran baseline verification in worktree: `cabal build all && cabal test` (PASS, 895 examples).
- 2026-03-02: Dispatched parallel explorer agents for cluster audits (Phi/Omega, Reify/Generalize, Run/Pipeline, Plan/Solved).
- 2026-03-02: Recovered from interrupted/stalled agent threads; collected completed reports and backfilled missing scope with direct grep/source inspection.
- 2026-03-02: Collected final Presolution/Plan + Solved cleanup report (closure signature and `canonicalConstraint` dependency map).
- 2026-03-02: Authored detailed agent-team execution plan at `docs/plans/2026-03-02-eliminate-solved-indirection-agent-team-implementation-plan.md`.
- 2026-03-02: Started Team D Wave 1 Tasks 3+6 execution in `src/MLF/Elab/Run/*` + `src/MLF/Elab/Elaborate.hs`.
- 2026-03-02: Confirmed red-state markers in owned files (`eeSolved`, solved-builder path, `rtcSolved`) before guard-test updates.
- 2026-03-02: Added Team D guard tests in `test/ElaborationSpec.hs` + `test/PipelineSpec.hs` and verified intended red failures for each guard using per-test `--match` runs.
- 2026-03-02: Implemented Team D refactor:
  - `ElabEnv` boundary switched to `eePresolutionView`
  - solved-builder pipeline path removed
  - result-type inputs switched from `rtcSolved` to `rtcPresolutionView`
- 2026-03-02: Ran requested verification commands; both exact OR-pattern commands returned `0 examples` in this suite.
- 2026-03-02: Ran equivalent sequential verification slices:
  - guards (`ElabEnv uses PresolutionView boundary`, `pipeline no longer defines buildSolvedFromPresolutionSnapshot`, `result-type context no longer stores rtcSolved`) all PASS
  - `Phase 6 — Elaborate` PASS (`163 examples`)
  - `D2: elaboration path is read-only on Solved` PASS (`4 examples`)
  - `Pipeline` slice PASS (`61 examples`)
- 2026-03-02: Started Team C Wave 1 Task 5 implementation in owned modules (`Reify/*`, `Elab/Run/Generalize*`, `Plan/ReifyPlan`).
- 2026-03-02: Mapped remaining solved-boundary usage in Task 5 scope; prepared TDD guard `generalize env derives canonical constraint from PresolutionView`.
- 2026-03-02: Added guard test `generalize env derives canonical constraint from PresolutionView` in `test/PipelineSpec.hs`; verified RED state with:
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "generalize env derives canonical constraint from PresolutionView"'` (FAIL at missing `pvCanonicalConstraint`/`pvCanonical` markers in `Generalize.hs`).
- 2026-03-02: Implemented Team C migration core:
  - `constraintForGeneralization` now accepts `PresolutionView` and derives canonical data via `pvCanonical` / `pvCanonicalConstraint`.
  - `instantiationCopyNodes` now accepts `PresolutionView` and canonicalizes through `pvCanonical`.
  - Pipeline call-sites now pass `presolutionViewClean` into both functions.
- 2026-03-02: Attempted Reify-side `PresolutionView` import migration; reverted after detecting module cycle through `Presolution.Base -> Plan -> ReifyPlan -> Reify.TypeOps`.
- 2026-03-02: Post-change verification is currently blocked by unrelated in-progress Team B compile errors in `src/MLF/Elab/Phi/Omega.hs`; both requested verification commands fail before executing tests.
- 2026-03-02: Team B Task 4 started; added guard test `Phi modules no longer import MLF.Constraint.Solved directly` in `test/ElaborationSpec.hs`.
- 2026-03-02: Ran required red command for guard slice; build failed before test execution due pre-existing module cycle in `MLF.Constraint.Presolution.Plan.BinderPlan.Build` <-> `MLF.Reify.Core`.
- 2026-03-02: Team B Task 4 implementation complete in owned Phi files:
  - `Context` migrated to `PresolutionView` (`pvConstraint`/`pvCanonical`) and order-key derivation via `orderKeysFromConstraintWith`.
  - `IdentityBridge` migrated from `Solved` handle to `PresolutionView` handle.
  - `OmegaContext` now carries `PresolutionView` + injected reify closures; omega graph/binding lookups now use view queries (`pvLookupNode`, `pvLookupVarBound`, `pvLookupBindParent`, `pvBindParents`, `pvConstraint`, `pvCanonical`).
  - `Translate` now builds/threads `PresolutionView` for omega/context/identity operations and computes named-node set from view-backed constraint data.
- 2026-03-02: Added migration guard test in `test/ElaborationSpec.hs`: `Phi modules no longer import MLF.Constraint.Solved directly` (Team B-owned production Phi modules).
- 2026-03-02: Updated `test/Phi/IdentityBridgeSpec.hs` bridge constructors to pass `PresolutionView` adapters.
- 2026-03-02: Updated context tests in `test/ElaborationSpec.hs` to call `contextToNodeBound` with `PresolutionView` adapter from solved fixtures.
- 2026-03-02: Required verification commands executed:
  - guard match (`Phi modules no longer import...`) PASS (1 example)
  - requested OR-pattern slices run but matched 0 examples
  - equivalent concrete slices PASS: `Phi alignment` (7), `IdentityBridge` (23), `Phi soundness` (3), `OpRaise` (22), `OpWeaken` (15), `OpGraft` (3).
- 2026-03-02: Team E Wave 2 Task 7 started with TDD guards:
  - added `PresolutionPlanBuilder closes over PresolutionView, not Solved` (`test/PresolutionSpec.hs`) and verified RED.
- 2026-03-02: Team E Wave 2 Task 8 guard added:
  - added `Solved production-only builders are absent` (`test/Constraint/SolvedSpec.hs`) and verified RED.
- 2026-03-02: Team E Wave 2 Task 9 hygiene guard added:
  - added `production src tree has no MLF.Constraint.Solved imports in elaboration path` (`test/PipelineSpec.hs`, entrypoint module scope) and verified GREEN.
- 2026-03-02: Migrated `PresolutionPlanBuilder` boundary:
  - `Base` signature switched from `Solved -> ...` to `PresolutionView -> ...`.
  - `Plan.buildGeneralizePlans` now consumes `PresolutionView`.
  - `Elab.Run.Generalize.generalizeAtWithBuilder` now adapts runtime `Solved` to `PresolutionView` before builder invocation.
  - generalized `Presolution.View.fromPresolutionResult` to `PresolutionSnapshot a => a -> PresolutionView` to avoid module-cycle on `Base`.
- 2026-03-02: Removed `Solved.fromPresolutionResult` from `src/MLF/Constraint/Solved.hs`; updated solved parity test to use `fromPreRewriteState` with `snapshotUnionFind`/`snapshotConstraint`.
- 2026-03-02: Regression/fix cycle:
  - initial Task 7 implementation caused `Paper alignment baselines` failures (`InvalidBindingTree ... node ... not in constraint`) due solved-compat reconstruction mismatch.
  - fixed by constructing planner compatibility solved state via `Solved.mkTestSolved` on `pvCanonicalConstraint` with live-node sanitized canonical map.
  - re-ran failing slice: `Paper alignment baselines` PASS (`25 examples`).
- 2026-03-02: Wave 2 targeted verification slices executed and green:
  - `Phase 4 — Principal Presolution` (`154 examples`)
  - `Presolution witness ops` (`1 example`)
  - `Paper alignment baselines` (`25 examples`)
  - `MLF.Constraint.Solved` (`40 examples`)
  - `Frozen parity artifact baseline` (`1 example`)
- 2026-03-02: Final Gate C executed:
  - `cabal build all && cabal test` PASS (`905 examples, 0 failures`).
- 2026-03-02: Cleaned `MLF.Constraint.Solved` local name-shadowing (`canonicalMap` -> `canonMap`) in touched constructors and re-ran Gate C:
  - `cabal build all && cabal test` PASS (`905 examples, 0 failures`).
