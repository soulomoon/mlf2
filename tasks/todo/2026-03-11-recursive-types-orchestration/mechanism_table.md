# Goal Mechanism Table (Current vs Target)

Last updated (UTC): 2026-03-13
Goal: Complete the recursive-types roadmap through a 10-round non-coding orchestration campaign with fresh planner/researcher/implementer/reviewer/QA/verifier/integrator roles.
Source of truth: `docs/plans/2026-03-11-recursive-types-roadmap.md`, `tasks/todo/2026-03-11-recursive-types-design/`, `tasks/todo/2026-03-11-recursive-types-orchestration/orchestrator_prompt.md`, and current `master`.

| Mechanism | Current codebase behavior | Target behavior | Gap summary | Evidence (spec/code/tests) | Gate (YES/NO) | Next action |
| --- | --- | --- | --- | --- | --- | --- |
| M0 — Freeze semantics and acceptance criteria | `M0` is landed and verifier-green on `master`: Round 1 Attempt 4 merged commit `32fa898388df5c43556da880562a63de6aa26d91` as merge `b2282743a3f60e32c04e280b647188dc016194c7`, and the verifier marked the milestone gate `YES` on `2026-03-11T12:59:59Z`. | The semantic freeze stays explicitly documented, accepted, and closed on `master`. | Closed for this campaign; keep the merged acceptance note and verifier-owned YES gate as the reference baseline for later milestones. | `docs/plans/2026-03-11-recursive-types-roadmap.md`; `tasks/todo/2026-03-11-recursive-types-orchestration/orchestrator-log.jsonl` | YES | Closed on `master` at merge `b2282743a3f60e32c04e280b647188dc016194c7`; no further round work unless a later verifier finds regression. |
| M1 — Explicit xMLF/core recursive types | `M1` is landed and verifier-green on `master`: Round 2 Attempt 3 merged commit `a20538aca3fa7097135a79541b3e987d3db46773` as merge `697d7876b2bf11e84fbec2e966a04071b9c698d3`, and the verifier marked the milestone gate `YES` on `2026-03-11T15:28:24Z`. | Explicit/core recursive-type AST support remains compiled, structurally roundtrippable, and isolated from inference. | Closed for this campaign; later rounds may build on the landed constructors but should not reopen the structural M1 scope. | `docs/plans/2026-03-11-recursive-types-roadmap.md`; `tasks/todo/2026-03-11-recursive-types-orchestration/orchestrator-log.jsonl` | YES | Closed on `master` at merge `697d7876b2bf11e84fbec2e966a04071b9c698d3`; continue from this anchor into `M2`. |
| M2 — Runtime/typechecker/reduction support | `M2` is landed and verifier-green on `master`: Round 3 Attempt 4 merged commit `418c914ebb4c8b84a59a2acd110fa6af5688278d` as merge `5618ed60c3e54611e927019242d1b36a991d0d1b`, and the verifier marked the milestone gate `YES` on `2026-03-11T18:15:20Z`. | Explicit internal `roll`/`unroll` runtime forms remain typechecking/reducing deterministically with focused theorem-obligation coverage while staying strictly internal to `M2`. | Closed for this campaign; later milestones may build on the merged internal runtime support but should not reopen the M2-only boundary unless a later verifier finds regression. | `docs/plans/2026-03-11-recursive-types-roadmap.md`; `tasks/todo/2026-03-11-recursive-types-orchestration/orchestrator-log.jsonl`; `implementation_notes.md`; `test/TypeSoundnessSpec.hs`; `src/MLF/Elab/Types.hs` | YES | Closed on `master` at merge `5618ed60c3e54611e927019242d1b36a991d0d1b`; continue from this anchor into `M3`. |
| M3 — Public XMLF syntax + roundtrip tooling | `M3` is landed and verifier-green on `master`: Round 4 Attempt 1 merged commit `6fb16a22caec325a7971f12878c53bbf7f04406c` as merge `9e1214b485527b8064dbe640c3350725c1085d15`, and the verifier marked the milestone gate `YES` on `2026-03-11T18:41:42Z`. | Public XMLF syntax now exposes explicit recursive types/terms with stable roundtrip behavior and malformed-syntax rejections. | Closed for this campaign; later milestones may build on the public XMLF surface, but should not reopen M3 unless a later verifier finds regression. | `docs/plans/2026-03-11-recursive-types-roadmap.md`; `src-public/MLF/XMLF.hs`; `src/MLF/XMLF/Parse.hs`; `src/MLF/XMLF/Pretty.hs`; `tasks/todo/2026-03-11-recursive-types-orchestration/orchestrator-log.jsonl` | YES | Closed on `master` at merge `9e1214b485527b8064dbe640c3350725c1085d15`; continue from this anchor into `M4`. |
| M4 — Add contractiveness validation | `M4` is landed and verifier-green on `master`: Round 5 Attempt 2 merged commit `0d38bb1b6c88903a44c5b051ab96d57eabb88d23` as merge `0d38bb1b6c88903a44c5b051ab96d57eabb88d23`, and the verifier marked the milestone gate `YES` on `2026-03-12T10:51:33Z`. | Contractiveness remains enforced consistently for the explicit recursive-type layer with deterministic diagnostics while keeping the public `MLF.Pipeline` facade abstract. | Closed for this campaign; later milestones may build on the landed explicit-layer validator and conservative `forall` policy, but should not reopen `M4` unless a later verifier finds regression. | `docs/plans/2026-03-11-recursive-types-roadmap.md`; `src/MLF/Reify/TypeOps.hs`; `src/MLF/Elab/TypeCheck.hs`; `src/MLF/Elab/Types.hs`; `test/TypeCheckSpec.hs`; `test/ElaborationSpec.hs`; `implementation_notes.md`; `tasks/todo/2026-03-11-recursive-types-orchestration/orchestrator-log.jsonl` | YES | Closed on `master` at merge `0d38bb1b6c88903a44c5b051ab96d57eabb88d23`; continue from this anchor into `M5`. |
| M5 — Surface eMLF syntax exposure | Current `master` is now `8a7e437af3f1e4287673c19536966a92c2333a7b`, and the latest authority resync records that the packet had drifted behind the live repo after the verifier-owned Round 6 Attempt 1 outcome on `0d38bb1b6c88903a44c5b051ab96d57eabb88d23`. The approved public-surface `STMu` slice is still preserved as branch artifact `4fccce32d382f06a645eb6a19bc48c3a22c5f3e8` on `codex/rt-r06-m5-surface-mu` in `/Volumes/src/mlf4-worktrees/rt-r06-m5-surface-mu-a1`, but `.git` lockfile probes in `/Volumes/src/mlf4/.git`, `/Volumes/src/mlf4/.git/logs`, and `/Volumes/src/mlf4/.git/refs/heads` still fail with `Operation not permitted`. | Source annotations can carry recursive types without introducing inference of recursive types. | The selected first slice is implemented, committed, review-green, and QA-green, but still not landed on current `master`: the packet had to be resynchronized to the live repo first, and environment-level git metadata write denial still blocks any safe integration retry. `STMu` surface syntax, canonical `μ` pretty-printing, normalization through `STMu`, and explicit Phase 1 rejection remain absent from current `master` until a fresh external-recovery authority check from `8a7e437af3f1e4287673c19536966a92c2333a7b` returns `YES`. | `docs/plans/2026-03-11-recursive-types-roadmap.md`; `tasks/todo/2026-03-11-recursive-types-design/findings.md`; `tasks/todo/2026-03-11-recursive-types-orchestration/orchestrator-log.jsonl` | NO | External unblock only: attempt `.git` metadata recovery, then rerun a fresh external-recovery `authority_check` from current `master` `8a7e437af3f1e4287673c19536966a92c2333a7b`; retry integration and verifier only after that check returns `YES`, and do not advance to `M6` first. |
| M6 — Pipeline acceptance for explicit annotations only | The pipeline/constraint graph does not yet have a verified low-risk path for carrying explicit recursive annotations, and acyclicity assumptions remain thesis-critical. | A verifier-backed spike decides whether `TyMu`-style support is safe enough or whether the feature must remain explicit-layer only. | The campaign still lacks a tractable, paper-aware bridge from annotated syntax into the pipeline. | `docs/plans/2026-03-11-recursive-types-roadmap.md`; `src/MLF/Constraint/Acyclicity.hs`; `tasks/todo/2026-03-11-recursive-types-design/findings.md` | NO | Planner selects a spike slice only after `M5` is green and enough evidence exists to answer the roadmap questions. |
| M7 — Optional inference implementation | No recursive-type graph or inference support exists, and the roadmap treats this as optional/high-risk work after the feasibility spike. | Explicit recursive annotations typecheck through the full pipeline only if the earlier spike proves a sound enough representation. | High-risk solver/graph work is intentionally untouched and may become a separate research track. | `docs/plans/2026-03-11-recursive-types-roadmap.md`; current absence of recursive-type graph nodes/unification rules | NO | Attempt only if `M6` concludes that a safe implementation path exists; otherwise stop with verifier-backed blockage. |

Rules:
- Keep milestone order fixed as `M0`, `M1`, `M2`, `M3`, `M4`, `M5`, `M6`, `M7`.
- Use only `YES` or `NO`.
- Flip `NO -> YES` only with passing review/QA/verifier evidence recorded in `orchestrator-log.jsonl`.


_Update 2026-03-13_: Round 5 Attempt 2 landed `M4` on `master` as commit/merge `0d38bb1b6c88903a44c5b051ab96d57eabb88d23` after external authority recovery, clean-master replanning, review `YES`, QA `YES`, and fresh merged-master verification. Round 6 Attempt 1 then implemented, reviewed, QA-cleared, and committed the first `M5` slice on `codex/rt-r06-m5-surface-mu` as `4fccce32d382f06a645eb6a19bc48c3a22c5f3e8`, but verifier-owned closure marked the run terminal `FAILED` with `milestone_gate = NO` and `blockage_gate = YES` because environment-level git metadata write denial prevented advancing `master`. A later authority resync recorded that live `master` has since advanced to `8a7e437af3f1e4287673c19536966a92c2333a7b` through prose-only changes while the preserved M5 artifact still exists and `.git` metadata writability remains `NO`. `M5` therefore remains `NO` and stays the next anchor milestone for a future resumed run only after another fresh external-recovery authority check from current `master` returns `YES`.
