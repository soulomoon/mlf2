# Goal Mechanism Table (Current vs Target)

Last updated (UTC): 2026-03-11
Goal: Complete the recursive-types roadmap through a 10-round non-coding orchestration campaign with fresh planner/researcher/implementer/reviewer/QA/verifier/integrator roles.
Source of truth: `docs/plans/2026-03-11-recursive-types-roadmap.md`, `tasks/todo/2026-03-11-recursive-types-design/`, `tasks/todo/2026-03-11-recursive-types-orchestration/orchestrator_prompt.md`, and current `master`.

| Mechanism | Current codebase behavior | Target behavior | Gap summary | Evidence (spec/code/tests) | Gate (YES/NO) | Next action |
| --- | --- | --- | --- | --- | --- | --- |
| M0 — Freeze semantics and acceptance criteria | The roadmap and design task already describe iso-recursive `μ`, explicit `roll`/`unroll`, no implicit unfolding in equality, contractiveness in v1, and no cyclic graph support in v1, but this campaign has not yet recorded a verifier-owned gate on that semantic freeze. | The verifier explicitly accepts the written semantic freeze for this campaign, or the planner lands the smallest doc-only closure slice that makes the gate decision-complete. | Missing verifier decision and campaign-local audit trail for the semantic baseline. | `docs/plans/2026-03-11-recursive-types-roadmap.md`; `tasks/todo/2026-03-11-recursive-types-design/findings.md` | NO | Ask the verifier whether the existing roadmap + design packet satisfy `M0`; if `NO`, have the planner scope a doc-only closure slice. |
| M1 — Explicit xMLF/core recursive types | No recursive-type constructors such as `TMu` or `XTMu` exist in the explicit/core ASTs or XMLF type syntax. | Explicit/core recursive-type AST support exists, compiles, and roundtrips structurally without changing inference. | The low-risk explicit/core type layer has not started. | `docs/plans/2026-03-11-recursive-types-roadmap.md`; code search showing no `TMu`/`XTMu` | NO | Planner selects the smallest mergeable `M1` slice from the roadmap task breakdown. |
| M2 — Runtime/typechecker/reduction support | Elaborated/runtime terms have no `roll`/`unroll` forms and the typechecker/reducer have no recursive-type rules. | Explicit `roll`/`unroll` runtime forms typecheck and reduce deterministically with regression coverage. | Runtime semantics for explicit recursive terms are entirely absent. | `docs/plans/2026-03-11-recursive-types-roadmap.md`; code search showing no `ERoll`/`EUnroll` | NO | Planner scopes the next smallest runtime/typechecker/reduction slice after `M1` is green. |
| M3 — Public XMLF syntax + roundtrip tooling | Public XMLF syntax does not parse or pretty-print `μ`, `roll[...]`, or `unroll`. | Public XMLF syntax exposes explicit recursive types/terms with stable roundtrip behavior and malformed-syntax rejections. | The public xMLF surface is still paper-faithful only. | `docs/plans/2026-03-11-recursive-types-roadmap.md`; `src-public/MLF/XMLF.hs`; XMLF parser/pretty modules | NO | Planner selects the smallest XMLF-facing slice once `M2` is green. |
| M4 — Add contractiveness validation | No reusable contractiveness checker currently rejects ill-founded recursive types such as `μa. a`. | Contractiveness is enforced consistently for the explicit recursive-type layer with deterministic diagnostics. | The semantic safety gate for explicit recursive types is not implemented. | `docs/plans/2026-03-11-recursive-types-roadmap.md`; current absence of recursive-type validators | NO | Planner chooses the smallest contractiveness-validation slice after XMLF/runtime support is stable enough to use it. |
| M5 — Surface eMLF syntax exposure | Frontend source annotations cannot express `μ` types today. | Source annotations can carry recursive types without introducing inference of recursive types. | Source/frontend syntax remains closed over the non-recursive thesis language. | `docs/plans/2026-03-11-recursive-types-roadmap.md`; `tasks/todo/2026-03-11-recursive-types-design/findings.md` | NO | Planner scopes the smallest parser/pretty/normalization slice once explicit/core semantics are stable. |
| M6 — Pipeline acceptance for explicit annotations only | The pipeline/constraint graph does not yet have a verified low-risk path for carrying explicit recursive annotations, and acyclicity assumptions remain thesis-critical. | A verifier-backed spike decides whether `TyMu`-style support is safe enough or whether the feature must remain explicit-layer only. | The campaign still lacks a tractable, paper-aware bridge from annotated syntax into the pipeline. | `docs/plans/2026-03-11-recursive-types-roadmap.md`; `src/MLF/Constraint/Acyclicity.hs`; `tasks/todo/2026-03-11-recursive-types-design/findings.md` | NO | Planner selects a spike slice only after `M5` is green and enough evidence exists to answer the roadmap questions. |
| M7 — Optional inference implementation | No recursive-type graph or inference support exists, and the roadmap treats this as optional/high-risk work after the feasibility spike. | Explicit recursive annotations typecheck through the full pipeline only if the earlier spike proves a sound enough representation. | High-risk solver/graph work is intentionally untouched and may become a separate research track. | `docs/plans/2026-03-11-recursive-types-roadmap.md`; current absence of recursive-type graph nodes/unification rules | NO | Attempt only if `M6` concludes that a safe implementation path exists; otherwise stop with verifier-backed blockage. |

Rules:
- Keep milestone order fixed as `M0`, `M1`, `M2`, `M3`, `M4`, `M5`, `M6`, `M7`.
- Use only `YES` or `NO`.
- Flip `NO -> YES` only with passing review/QA/verifier evidence recorded in `orchestrator-log.jsonl`.
