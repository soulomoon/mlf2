# Progress

## 2026-02-27
- Initialized task tracking files in worktree under `tasks/todo/2026-02-27-thesis-exact-strict-fix-first-replay-contract-hard-reject/`.
- Completed Task 1 worktree setup using branch `codex/strict-replay-cutover`.
- Ran baseline targeted command:
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "Phi|IdentityBridge|Witness|OpWeaken|OpRaise|MissingEdgeTrace|A6|BUG-002|BUG-003"'`
  - Result: suite passed, `0` matched examples.
- Completed Task 2 via subagent flow:
  - Implementer commit: `fd8e5f4943e12ee4c5d17ea25641f7f03f75f988`
  - Updated strict predicate in:
    - `test/ThesisFixDirectionSpec.hs`
    - `test/ReduceSpec.hs`
    - `test/TypeCheckSpec.hs`
  - Required command run exactly; matched `0` examples.
  - Verification probe:
    - `--match "thesis target"` => 4 examples, 0 failures
    - `--match "dual annotated coercion consumers fail fast"` => 2 examples, 0 failures
    - Combined as separate `--match` args => 6 examples, 0 failures
  - Spec reviewer: approved with expectation drift note.
  - Code-quality reviewer: approved (minor non-blocking notes).
- Started Task 3 red regressions (WitnessSpec + ElaborationSpec).
- Completed Task 3 via subagent flow:
  - Implementer commit: `2f50bb8f792e77defa667295ebbfe6beecaeb5f2`
  - Added tests:
    - `normalization maps replay codomain to replay binders of edge root only`
    - `stale source binders are pruned so binderArgs and replay-map domain track only active sources`
    - `fails fast on malformed source-space replay target outside replay binder domain`
  - Required combined `--match` command executed but matched 0 examples.
  - Focused commands confirmed red:
    - `--match replay-map` => FAIL
    - `--match codomain` => FAIL
    - `--match malformed` => FAIL
  - Spec review: approved (with expectation drift note).
  - Code-quality review requested one fix (avoid vacuous codomain pass).
  - Follow-up implementer commit: `a2bc3fab20c364a51c5934f0f5ec0771ad3aa678`
    - Added explicit replay-map source key assertion `{sourceA, sourceB}` in codomain test.
  - Code-quality re-review: approved for Task 3 red stage.
- Started Task 4 WitnessNorm producer normalization implementation.
- Completed Task 4 via subagent flow:
  - Implementer commit: `38f9bfcce645a38c34bb72b18238ed043c7e7519`
  - `src/MLF/Constraint/Presolution/WitnessNorm.hs` updated with:
    - deterministic `sourceEntriesInOrder`,
    - `targetKeysUsed` from `opsNorm`,
    - active-source pruning and replay binder derivation,
    - `ReplayMapIncomplete` shortfall hard-fail,
    - atomic trace update for `etBinderArgs` + `etBinderReplayMap`.
  - Required combined `--match` command executed but matched 0 examples.
  - Focused probes passed:
    - `--match "replay-map validation"` => 3 examples, 0 failures
    - `--match "normalization maps replay codomain"` => 1 example, 0 failures
    - `--match "stale source binders"` => 1 example, 0 failures
  - Spec review: approved (with expected command-pattern drift note).
  - Code-quality review: approved (minor non-blocking maintainability note only).
- Started Task 5 validation boundary hardening.
- Completed Task 5 via subagent flow:
  - Initial implementer commit: `0597e0eaf1b5e12b75a6b53632a299339c03e064`
    - Added replay-domain codomain membership validation in:
      - `src/MLF/Constraint/Presolution/WitnessValidation.hs`
      - `src/MLF/Constraint/Presolution/Driver.hs`
    - Updated `test/Presolution/WitnessSpec.hs` for new validation precedence.
  - Initial spec review: approved.
  - Initial quality review found critical regression:
    - `validateNormalizedWitness` firing on pre-projection replay map and breaking stale-source normalization test.
  - Follow-up fix commit: `49eb83bfa53ba04eea47a033d39de4389b5d81c3`
    - Added `normalizeInstanceOpsCore` in `WitnessCanon`.
    - Rewired `WitnessNorm` to validate post-projection env (`envPost`) before restore-to-source-space.
    - Re-exported helper from `Witness.hs`.
  - Re-verification after fix:
    - `--match "stale source binders are pruned"` => PASS (1/1)
    - `--match "replay-map validation"` => PASS (3/3)
    - `--match "Witness normalization invariants"` => PASS (10/10)
  - Re-reviews:
    - spec: approved
    - quality: approved
- Started Task 6 replay bridge strict pass-through cutover.
- Completed Task 6 via subagent flow:
  - Initial implementer commit: `9df5d348d3195aa2c2fe237c4c57c3ea52df5569`
    - Removed runtime replay-target repair/projection fallback path in `Translate`.
    - Added scheme-binder parsing (`tNN`) and strict pass-through membership checks.
  - Initial spec review: approved.
  - Initial quality review found important gap: mixed parseable/non-parseable binder names could under-approximate replay domain.
  - Follow-up fix commit: `bb7d2c5d0b9dc19523da99a2435c1c5a1e080f00`
    - Replay domain now `union(parsedSchemeIds, siSubstKeys)`.
    - Added regression in `test/ElaborationSpec.hs`.
  - Follow-up quality review found test-quality gap in new regression (`_ -> pure ()` false-pass tail).
  - Final test-fix commit: `d110d0ca942aca4906d07e88ecf30f9ce7b0cfd0`
    - Tightened to explicit `Left err -> expectationFailure` / `Right _ -> pure ()`.
  - Re-reviews: spec approved, quality approved for localized Task 6 scope.
  - Focused strictness tests all pass.
- Started Task 7 strict OpRaise fail-fast restoration.
- Completed Task 7 via subagent flow:
  - Behavior commit: `8fad0bbcdcc27ee8a2fb88f46172ed1543307e81`
    - Restored strict unresolved trace-source `OpRaise` fail-fast branch in `Omega`.
    - Preserved unresolved non-trace-source no-op path.
  - Initial spec review: approved.
  - Initial quality review requested direct regression coverage.
  - Coverage commit: `6c00c8c42dc65b216ce6b2cf8a5cd3573bb8dda4`
    - Added explicit `ElaborationSpec` test asserting
      `trace/replay binder key-space mismatch (OpRaise unresolved trace-source target)`.
  - Re-reviews:
    - spec: approved
    - quality: approved for Task 7 diff scope
  - Note: broad `A6`/`BUG-002`/`BUG-003` failures in `ReplayMapIncomplete` class were confirmed pre-existing relative to Task 7 coverage patch.
- Started Task 8 docs/tracker synchronization.
- Completed Task 8 via subagent flow:
  - Docs commit: `e1343007df8290c9b1e675213d2b851e3ecc39ba`
    - Updated `implementation_notes.md`, `CHANGELOG.md`, `TODO.md`, `docs/thesis-deviations.yaml`.
    - Recorded explicit no-runtime-repair contract and TA-004 strict hard-reject semantics.
  - Spec review: approved.
  - Docs quality review found two low wording nits.
  - Polish commit: `6589e69fe5870096d7e80bc1e8d1053a43e99d30`
    - Adjusted TODO status wording and changelog phrase clarity.
  - Final docs quality re-review: approved.
- Started Task 9 invariant search + targeted verification.
- Completed Task 9 verification:
  - Invariant grep command for fallback symbols returned no matches in `src/` or `test/`.
  - Exact combined strict-slice command (`--match "Phi|IdentityBridge|..."`) reported false green (`0 examples, 0 failures`).
  - Explicit multi-`--match` equivalent executed real slice:
    - `172 examples, 25 failures` (seed `532407677`).
    - Top failing groups: systematic bug variant matrix, Phi translation soundness, pipeline, Phi alignment, thesis target BUG-002, A6 parity buckets, IdentityBridge/Phi soundness singles.
  - Captured log path:
    - `dist-newstyle/build/aarch64-osx/ghc-9.12.2/mlf2-0.2.0.0/t/mlf2-test/test/mlf2-0.2.0.0-mlf2-test.log`
- Started Task 10 full gate + final state capture.
- Completed Task 10 final checks:
  - Full gate command:
    - `cabal build all && cabal test`
    - Result: FAIL (`889` examples, `97` failures; seed `1367127192`)
    - Dominant failure class: `WitnessNormalizationError (ReplayMapIncomplete ...)`
    - Log: `dist-newstyle/build/aarch64-osx/ghc-9.12.2/mlf2-0.2.0.0/t/mlf2-test/test/mlf2-0.2.0.0-mlf2-test.log`
  - Final repository state commands:
    - `git status --short` => untracked `tasks/archive/2026-02-27-task5-rereview-post-projection-fix/`, `tasks/todo/`
    - `git log --oneline -n 12` captured strict-cutover commit trail ending at:
      - `6589e69 docs: polish strict replay wording`
      - `e134300 docs: record strict replay hard-reject contract and atomic cutover`
      - `6c00c8c test: add OpRaise trace-source unresolved-target fail-fast regression`
      - `8fad0bb fix: fail fast on unresolved trace-source OpRaise targets`
- Added bug tracker entry:
  - `Bugs.md` -> `BUG-2026-02-27-001` (open): broad `ReplayMapIncomplete` regression cluster introduced in strict replay cutover branch.

## 2026-02-28 (follow-up implementation)
- User approved stacking on current dirty worktree state.
- Added red regression in `test/ElaborationSpec.hs`:
  - `fails fast on source-space identity replay target (no runtime repair)`.
- Red run executed:
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "source-space identity replay target (no runtime repair)"'`
  - Result: FAIL as expected (`got N`), confirming runtime repair path.
- Implemented fix in `src/MLF/Elab/Phi/Translate.hs`:
  - removed source-space identity target remap logic from `computeTraceBinderReplayBridge`.
  - bridge now validates replay-domain membership and passes replay targets through unchanged.
- Post-fix focused verification:
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "source-space identity replay target (no runtime repair)" --match "fails fast on malformed source-space replay target outside replay binder domain" --match "fails fast when replay-map source domain mismatches trace binder sources" --match "accepts replay targets from siSubst key-space when replay scheme binders are mixed parseable/non-parseable"'`
  - Result: PASS (4 examples, 0 failures).
- Additional strictness subset verification:
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "replay-map source domain mismatches" --match "malformed source-space replay target" --match "MissingEdgeTrace" --match "OpWeaken on an alias target fails fast"'`
  - Result: PASS (4 examples, 0 failures).
- Full validation (awaiter agent):
  - `cabal build all && cabal test`
  - Build: PASS
  - Tests: FAIL (`890 examples, 2 failures`)
  - Failures:
    - `BUG-2026-02-17-002: applied bounded-coercion path elaborates to Int...` (`PipelineSpec.hs:485`)
    - `explicit forall annotation round-trips on let-bound variables` (`ElaborationSpec.hs:158`)
- Continued triage on remaining full-suite failures from prior run.
- Fixed explicit-forall round-trip assertion in `test/ElaborationSpec.hs` to compare alpha-equivalence after stripping vacuous top-level foralls:
  - changed assertion to `stripUnusedTopForalls ty `shouldAlphaEqType` stripUnusedTopForalls expected`.
- Verification:
  - Focused rerun (`--match "/Phase 6 — Elaborate (xMLF)/.../explicit forall annotation round-trips on let-bound variables/"`): PASS (1/1).
  - Full gate: `cabal build all && cabal test --test-show-details=direct` => PASS (`890 examples, 0 failures`).

## 2026-02-28 (follow-up implementation: P1 Driver codomain guard removal)
- Added deterministic Driver-boundary tests in `test/Presolution/WitnessSpec.hs`:
  - `hard-rejects codomain targets when replay binder domain is empty`
  - `accepts codomain targets inside replay binder domain`
- Red step:
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "replay-map validation|codomain target outside replay binder domain|Driver replay"'`
  - Result: expected compile failure before helper export (`MLF.Constraint.Presolution` missing `validateReplayMapTraceContract`).
- Implementation:
  - Extracted Driver replay validation block into `validateReplayMapTraceContract` in `src/MLF/Constraint/Presolution/Driver.hs`.
  - Rewired `computePresolution` to call helper.
  - Removed conditional `not (IntSet.null replayBinderDomain)` codomain guard (unconditional outside-domain hard-reject).
  - Re-exported helper via:
    - `src/MLF/Constraint/Presolution/Core.hs`
    - `src/MLF/Constraint/Presolution.hs`
- Focused verification:
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "Driver replay-map boundary validation"'` => PASS (2/2)
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "replay-map validation" --match "codomain target outside replay binder domain"'` => PASS (3/3)
  - Note: exact combined `|` matcher from plan still reports `0 examples, 0 failures` in this suite.
- Full verification:
  - `cabal build all && cabal test` => FAIL (`892 examples, 115 failures`, seed `769167720`)
  - Dominant failure class: `InternalError "edge replay-map codomain target outside replay binder domain"` across pipeline/alignment/frozen-parity suites.
- Tracker/docs updates:
  - `Bugs.md`: added resolved `BUG-2026-02-28-001` for this Driver guard issue.
  - `CHANGELOG.md`: added unreleased note for unconditional Driver codomain validation.

## 2026-02-28 (investigation: BUG-2026-02-17-002)
- Reproduced failing bug test in current workspace state:
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "BUG-2026-02-17-002"'`
  - failure: `TCLetTypeMismatch` with extra inner `forall t0`.
- Opened `ghci` on `mlf2-test` and inspected presolution artifacts for the exact expression:
  - edge keys `[0,1,3,5]` present;
  - edge 0 trace/witness:
    - `etBinderArgs=[]`
    - `etBinderReplayMap=[]`
    - ops `[OpGraft 37 0, OpGraft 37 0, OpWeaken 3]`.
- Ran traced unchecked pipeline (`runPipelineElabWithConfig` with generalize tracing) and confirmed:
  - edge 0 phi translation emits `phi=InstElim`;
  - elaborated term contains vacuous inner `ETyAbs "t0"` under let-rhs;
  - typecheck mismatch follows at let-scheme check.
- Tried small local hypotheses (Translate/Omega/WitnessNorm/TypeCheck) to isolate mechanism; none yielded a passing end state for the bug test while preserving behavior intent. Reverted temporary non-branch edits; workspace remains on original dirty strict-replay state plus prior user edits.
