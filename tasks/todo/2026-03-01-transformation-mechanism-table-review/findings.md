# Findings

## Summary
- All table rows remain `Alignment = Aligned` after re-audit.
- Added new `Thesis-exact` column.
- All rows are currently `Thesis-exact = No` because each mechanism includes at least one runtime bridge/contract/scaffolding layer beyond thesis-direct presentation.

## Row Groups
- Rows 1-4: snapshot solved replay/env wiring, result-type runtime bundle, staged boundary-check pipeline, synthesized-wrapper propagation branch.
- Rows 5-7: staged Ω execution with weaken queueing/flush, strict producer replay-map contracts, strict consumer replay bridge + alias recovery.
- Rows 8-11: explicit rigidify/validate translatability pass, raw-key replay bridge/canonical split, IdentityBridge runtime reconciliation object, Ω non-root alias recovery with fail-fast contracts.
- Rows 12-14: snapshot replay in solved reconstruction, single-path runtime verification scaffolding, campaign classification as repo governance metadata.
