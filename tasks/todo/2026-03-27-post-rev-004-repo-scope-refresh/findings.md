# Findings

## Starting Posture

- The live controller is still parked at accepted `rev-004`, with
  `orchestrator/state.json` showing `stage: "done"` and no successor
  revision yet.
- The March 26 global `keep` vs `reopen` gate consumed the pre-amendment
  representative matrix and the old same-lane public-collapse read.
- Accepted `rev-003` / `rev-004` repaired that exact same-lane `C2` / `C5` /
  `C7` pocket on bounded authoritative public surfaces, but did not publish a
  new repo-scope matrix or repo-scope gate.

## Open Question

- After carrying forward the repaired exact-pocket read, does the repo-scope
  evidence still support `reopen the non-cyclic-graph revision question`, or
  does it instead narrow back to an unresolved / continue-bounded posture
  focused on `C1` and `P5`?

## Current Read

- The old March 26 reopen gate is historically valid but strategically stale
  on the post-`rev-004` read, because its decisive `zero stable visible
  persistence rows` premise is no longer true once the repaired same-lane
  `C2` / `C5` / `C7` pocket is carried forward.
- The refreshed task-local matrix now reads:
  `stable visible persistence` on exact-pocket rows `C2`, `C5`, and `C7`;
  blocker debt on `P1-row` and `C1`;
  fail-closed rejection on `C3`, `C4`, and `C6`.
- The strongest current repo-scope draft posture is therefore not `keep` and
  not `reopen`, but `narrowed unresolved / continue within the current
  architecture`.
- The remaining live blocker set narrows to:
  `P1` local unannotated automatic success,
  `P2` non-local visibility through `C1`, and
  `P5` nested-`forall` interaction through `C3`.
