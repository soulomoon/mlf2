# Findings

## 2026-03-04
- TMT row status is currently: boundary aligned but internal elaboration path still materializes a solved-style view (`ChiQuery.chiSolved`) for helper signatures.
- Current code anchors:
  - `MLF.Elab.Run.Pipeline` builds `ElabEnv` from `PresolutionView` + edge artifacts.
  - `MLF.Elab.Elaborate` still binds `solved = ChiQuery.chiSolved presolutionView` internally.
- Existing regression slices (`row2 closeout guard`, `checked-authoritative`, `Dual-path verification`, `Phase 6 — Elaborate`) provide a strong baseline for migration safety.
- Wave 0 established closeout guards:
  - `elab-input thesis-exact guard: Elaborate active input path does not materialize chiSolved`
  - `elab-input thesis-exact guard: active Elaborate/Phi callback aliases avoid solved-typed generalize-at input`
- Gate A evidence (integration head): guard slice RED (`2 examples, 2 failures`) while checked-authoritative baseline remained PASS (`4 examples, 0 failures`).
- Wave 1 achieved migration target:
  - Team B introduced chi-native helper surfaces for generalize/scope/typeops/reify.
  - Team C removed active-path `chiSolved` reliance and made active callback path chi-native.
  - Gate B PASS:
    - `Phase 6 — Elaborate`: `181 examples, 0 failures`
    - `elab-input thesis-exact guard`: `2 examples, 0 failures`
- Wave 2 alignment/cleanup:
  - Task 4 required no additional Team D source delta (already aligned); explicit no-op commit recorded per plan.
  - Task 5 Gate C PASS with non-empty matchers:
    - `Pipeline (Phases 1-5)`: `69 examples, 0 failures`
    - `Dual-path verification`: `4 examples, 0 failures`
    - `checked-authoritative`: `8 examples, 0 failures`
- Wave 3 closeout:
  - Updated TMT row to thesis-exact `Yes` for elaboration input active path.
  - Final Task 6 verification in Team E worktree:
    - `elab-input thesis-exact guard`: `2 examples, 0 failures`
    - `checked-authoritative`: `8 examples, 0 failures`
    - `Dual-path verification`: `4 examples, 0 failures`
    - `cabal build all && cabal test`: `931 examples, 0 failures`
- No concrete new implementation defect was discovered during this run; `Bugs.md` was not updated.
