# Findings

## 2026-03-08
- User scope includes all four fallback families from the audit, not only the previously high-confidence pair.
- The execution workflow requires an isolated worktree; implementation is happening on `codex/remove-live-fallbacks-thesis-exact`.
- The repo already had audit/campaign task folders in the original workspace, but this worktree started clean and needed fresh task artifacts.
- `test/PipelineSpec.hs` already contained part of the guard-first surface for generalization fallback removal; the missing coverage was reifyInst trace helpers, planner wrapper-root owner fallback, and instantiation inference fallback recovery.
- `test/Presolution/EdgePlannerSpec.hs` was already wired indirectly through `test/PresolutionSpec.hs`; adding it to `test/Main.hs` duplicated the planner examples and had to be reverted.
- A focused red run confirmed the current live behavior:
  - `generalizeWithPlan` still falls back from GA to no-GA and then to `reifyType`.
  - `inferInstAppArgsFromScheme` still recovers `Just [Int]` from a bounded `a`/`Int` mismatch via fallback recovery.
  - planner owner resolution still succeeds when a synthesized wrapper only has wrapper-root ownership.
  - `MLF.Elab.Elaborate` still contains `allowFallbackFromTrace`, `fallbackProvenanceArityOk`, and `resolveFallbackArgNodes`.
- Generalization fallback ladder touchpoints:
  - `src/MLF/Elab/Elaborate.hs`: `generalizeNeedsFallback`, `generalizeAtWith`, `generalizeAtNode`, plus coherence-based fallback preference.
  - `src/MLF/Elab/Run/Pipeline.hs`: local `generalizeNeedsFallback` ladder around `Generalize.generalizeType'`.
  - `src/MLF/Elab/Run/ResultType/Util.hs`: `generalizeNeedsFallback` and `fallbackToReify` ladder.
  - `src/MLF/Elab/Generalize.hs`: `fallbackSchemeType` fallback branches in scheme selection.
- Reify-inst fallback touchpoints:
  - `src/MLF/Elab/Elaborate.hs`: `allowFallbackFromTrace`, `resolveFallbackArgNodes`, and trace/expansion-driven synthesis inside `reifyInst`.
- Planner/inference fallback touchpoints:
  - `src/MLF/Constraint/Presolution/EdgeProcessing/Planner.hs`: `resolveSchemeOwnerGen` still falls back from body-root to wrapper-root for synthesized wrappers.
  - `src/MLF/Elab/Run/Instantiation.hs`: `inferInstAppArgsFromScheme` retains a generic `fallback` branch used both as the main path and as retry from bound-driven matching.
- Existing behavioral tests likely to update or complement:
  - `test/ElaborationSpec.hs`: GA fallback specs at `generalizeWithPlan falls back ...`, result-type fallback specs for same-domain/missing roots, and strict `OpWeaken`/`OpGraft` fail-fast slices near the `reifyInst` code paths.
  - `test/Presolution/EdgePlannerSpec.hs`: planner fail-fast coverage already exists for invalid left nodes and normal synthesized wrapper plans, but no direct guard yet for wrapper-root owner fallback removal.
  - `test/PipelineSpec.hs`: existing source-guard pattern (`readFile` + `isInfixOf`) is the natural home for direct marker-removal guards.
- The live worktree already includes strict planner-owner and instantiation-inference changes outside the initial manual patching in this session:
  - `src/MLF/Constraint/Presolution/EdgeProcessing/Planner.hs` resolves scheme owners from the wrapped body only.
  - `src/MLF/Elab/Run/Instantiation.hs` no longer uses the older generic `fallback` branch.
- Directly removing the `reifyInst` trace-local fallback cluster causes broad breakage across parity/alignment suites (`FrozenParitySpec`, `Phi.AlignmentSpec`, `AlignmentInvariantSpec`, and explicit-forall regression slices). Per the user plan’s stop condition, this is currently evidence for a provisional keeper rather than a safely removable family.
- Removing the GA/no-GA/reify ladders does compile and flips the new direct guards green, but `cabal test` still reports strict-generalization regressions (currently centered on `SchemeFreeVars`-based baseline/sentinel failures plus explicit-forall/elaboration mismatches).
- The current branch state goes further than the earlier stop point:
  - planner wrapper-root fallback is removed and its direct guard/specs are green;
  - `inferInstAppArgsFromScheme` is strict and its direct guard/specs are green;
  - `Phi.Omega` now emits authoritative inferred instantiations for empty-Ω let-polymorphic cases, which restores the `let id = \x. x in id id` alignment baseline without reviving trace-search fallback helpers.
- The remaining red full-suite cluster is now narrower and concentrated in thesis-facing elaboration baselines:
  - BUG-004 annotated-consumer variants,
  - explicit-forall coercion / redirect-stability baselines,
  - BUG-003 `SchemeFreeVars` regressions under the stricter generalization path.

- Full campaign closeout: `cabal build all && cabal test` now passes after repairing the strict-regression cluster in `Phi`/annotation elaboration, term closure, and generalization mapping sanitization.
