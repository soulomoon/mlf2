# Findings

## 2026-03-18

- The live top-level controller is on `contract_version: 2` and uses the repo-local retry subloop in `orchestrator/retry-subloop.md`.
- The current machine state is idle at `stage: "select-task"` with no active round, so the controller must start a new round rather than resume an interrupted one.
- The current roadmap is the continue-bounded unannotated iso-recursive follow-on cycle with four pending items: `C1` through `C4`.
- The active live subject remains repaired `URI-R2-C1`, and the inherited explicit-only / non-equi-recursive / non-cyclic-graph boundary remains in force unless an accepted roadmap update explicitly changes it.
- The follow-on cycle begins after accepted `round-033`, so the next live round number must be `round-034`.
- The worktree contract requires one `codex/` branch and one `.worktrees/<round-id>` worktree per round, reused across retries in the same round.
- The accepted `U6` predecessor decision is `continue-bounded`, not `widen-approved` and not `stop-blocked`, so the next cycle must stay non-widening unless a later accepted decision gate says otherwise.
- The delegated guider selected `C1` for `round-034`; the round is now bound to the continue-bounded target-bind step before any new bounded implementation slice.
- The active round is now bound to branch `codex/round-034-c1-continue-bounded-target-bind` and worktree `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-034`.
- The delegated planner authored `orchestrator/rounds/round-034/plan.md` and fixed the `C2` target to a bounded `ResultType.Fallback` / `PipelineSpec` local-binding-only retention-hardening slice, but the planner subagent did not terminate cleanly afterward.
- The planner subagent later completed and the authored `round-034` state was intact, so the temporary `resume_error` was cleared and the controller can continue from `stage: "plan"`.
- The next delegated stage is `implement` for `round-034`, but no usable fresh real subagent was made available for that handoff and no `C1` implementation artifacts were created in `.worktrees/round-034`.
- A narrower re-dispatch without inherited controller context succeeded: the `C1` implementer authored the canonical bind artifact and froze exactly one future `C2` target bounded to `src/MLF/Elab/Run/ResultType/Fallback.hs` plus `test/PipelineSpec.hs`.
- The delegated reviewer accepted `round-034` / `C1` attempt 1 with `stage_action: finalize` and wrote the authoritative review snapshot plus `review-record.json`.
- The delegated merger marked `round-034` ready for squash merge with proposed commit title `docs(c1): bind continue-bounded URI-R2-C1 C2 target`.
- `round-034` is now squash-merged into `codex/automatic-recursive-type-inference`, and the controller has advanced to `stage: "update-roadmap"` with `last_completed_round: "round-034"`.
- After the accepted `C1` roadmap update, the next lawful item is `C2`: the bounded local-binding-only `ResultType.Fallback` / `PipelineSpec` retention-hardening slice frozen by `C1`.
- The delegated guider selected `C2` for `round-035`; the active round is now bound to branch `codex/round-035-c2-retention-hardening` and worktree `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-035`.
- The delegated planner authored `orchestrator/rounds/round-035/plan.md`, keeping `C2` bounded to `Fallback.hs`, `PipelineSpec.hs`, and the canonical `C2` implementation artifact with a mandatory full repo gate.
- The delegated `C2` implementer followed TDD, landed the bounded `Fallback.hs` + `PipelineSpec.hs` slice, wrote the canonical `C2` artifact, and reported both the targeted recursive-characterization test block and the full `cabal build all && cabal test` gate passing.
- The delegated reviewer rejected `round-035` / `C2` attempt 1 with `stage_action: retry` because the specific non-local proxy wrapper case still lacked explicit bounded coverage through both pipeline entrypoints, even though the implementation behavior and full gate were green.
- The retry implementer resolved the review defect without widening: attempt 2 changed only `PipelineSpec.hs`, the canonical `C2` artifact, and round notes; the new same-case entrypoint coverage passed immediately, so `Fallback.hs` stayed unchanged while the targeted block and full gate remained green.
- The delegated reviewer accepted `round-035` / `C2` attempt 2 with `stage_action: finalize`; retry state is now cleared and the round is ready for merge preparation.
- `round-035` is now squash-merged into `codex/automatic-recursive-type-inference`, and the controller has advanced to `stage: "update-roadmap"` with `last_completed_round: "round-035"`.
- After the accepted `C2` roadmap update, the next lawful item is `C3`: bounded verification and evidence consolidation for the accepted local-binding-only fail-closed retention slice.
- The delegated guider selected `C3` for `round-036`; the active round is now bound to branch `codex/round-036-c3-verification-gate` and worktree `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-036`.
- The delegated planner authored `orchestrator/rounds/round-036/plan.md`, keeping `C3` docs-only and requiring anchor evidence from `Fallback.hs`, `PipelineSpec.hs`, the accepted `C2` record, the focused `ARI-C1` block, the full repo gate, and predecessor continuity.
- The delegated `C3` implementer wrote the canonical bounded verification artifact and round notes, recording read-only anchor evidence plus green focused and full-gate verification without widening scope or editing code/test surfaces.
- The delegated reviewer accepted `round-036` / `C3` attempt 1 with `stage_action: finalize`, confirming the docs-only verification gate and predecessor continuity without needing retry.
- `round-036` is now squash-merged into `codex/automatic-recursive-type-inference`, and the controller has advanced to `stage: "update-roadmap"` with `last_completed_round: "round-036"`.
- After the accepted `C3` roadmap update, the next lawful item is `C4`: the bounded next-cycle decision gate for the verified repaired `URI-R2-C1` local-binding-only fail-closed lane.
- The delegated guider selected `C4` for `round-037`; the active round is now bound to branch `codex/round-037-c4-next-cycle-decision` and worktree `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-037`.
- The delegated planner authored `orchestrator/rounds/round-037/plan.md`, keeping `C4` aggregate-only and docs-only while requiring exactly one bounded result token based on the accepted `C1` / `C2` / `C3` evidence chain.
- The delegated `C4` implementer recorded the aggregate decision token `continue-bounded` in the canonical `C4` artifact and documented why `widen-approved` and `stop-blocked` were not lawful under the accepted evidence chain.
- The delegated reviewer accepted `round-037` / `C4` attempt 1 with `stage_action: finalize`, confirming the aggregate-only decision gate recorded exactly one lawful token and preserved the inherited boundary.
- `round-037` is now squash-merged into `codex/automatic-recursive-type-inference`, and the controller has advanced to `stage: "update-roadmap"` with `last_completed_round: "round-037"`.
- The accepted `C4 = continue-bounded` roadmap update did not close the control plane; it queued a fresh bounded `E1` through `E4` cycle, so the live roadmap remains open even though the `C1` through `C4` cycle is complete.
- The controller is now resuming from the queued next cycle with `round-038` parked at `stage: "select-task"` for the new bounded `E1` through `E4` lane.
- The delegated guider selected `E1` for `round-038`; the active round is now bound to branch `codex/round-038-e1-next-target-bind` and worktree `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-038`.
- The delegated planner authored `orchestrator/rounds/round-038/plan.md`, keeping `E1` docs-only while freezing the next slice to local-binding-only `boundVarTarget` / nested-`forall` retained-child hardening in `Fallback.hs` with focused `PipelineSpec` coverage.
- The delegated `E1` implementer wrote the canonical bind artifact and round notes, freezing exactly one next slice in the retained-child `Fallback.hs` branch while preserving repaired `URI-R2-C1` and the inherited boundary.
- The delegated reviewer accepted `round-038` / `E1` attempt 1 with `stage_action: finalize`, confirming the new bounded cycle is now lawfully bound to exactly one `E2` slice without widening.
- `round-038` is now squash-merged into `codex/automatic-recursive-type-inference`, and the controller has advanced to `stage: "update-roadmap"` with `last_completed_round: "round-038"`.
- After the accepted `E1` roadmap update, the next lawful item is `E2`: the bounded retained-child `boundVarTarget` / nested-`forall` implementation slice in `Fallback.hs` with focused `PipelineSpec` coverage.
- The delegated guider selected `E2` for `round-039`; the active round is now bound to branch `codex/round-039-e2-retained-child-hardening` and worktree `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-039`.
- The delegated planner authored `orchestrator/rounds/round-039/plan.md`, keeping `E2` bounded to the retained-child `Fallback.hs:530-674` lane with focused `ARI-C1` coverage in `PipelineSpec.hs` and the mandatory full repo gate.
- The delegated `E2` implementer followed TDD, landed the bounded retained-child `Fallback.hs` + `PipelineSpec.hs` slice, wrote the canonical `E2` artifact, and reported both the targeted ARI-C1 block (`8 examples, 0 failures`) and the full gate (`1129 examples, 0 failures`) passing.
- The delegated reviewer rejected `round-039` / `E2` attempt 1 with `stage_action: retry` because the new evidence set still lacked a fresh retained-child same-lane success case in the bounded ARI-C1 block, even though the bounded implementation and full gate were green.
- The retry implementer resolved the review defect without widening: attempt 2 changed only `PipelineSpec.hs`, the canonical `E2` artifact, and round notes; the targeted block and full gate remained green while the prior bounded `Fallback.hs` change stayed intact.
- The delegated reviewer accepted `round-039` / `E2` attempt 2 with `stage_action: finalize`; retry state is now cleared and the round is ready for merge preparation.
- `round-039` is now squash-merged into `codex/automatic-recursive-type-inference`, and the controller has advanced to `stage: "update-roadmap"` with `last_completed_round: "round-039"`.
- After the accepted `E2` roadmap update, the next lawful item is `E3`: the bounded verification and evidence consolidation gate for the accepted retained-child same-lane `Fallback.hs` / `PipelineSpec.hs` slice.
- The delegated `E3` implementer and reviewer completed `round-040` cleanly as a docs-only verification gate with fresh focused and full verification evidence over the accepted `E2` slice.
- The accepted `E4 = continue-bounded` roadmap update again extended the live control plane; `F1` has now been selected and the controller is parked at the next lawful plan handoff for the new bounded cycle.
- The delegated `F1` round completed after two docs-only retries and now binds the next cycle to one bounded `F2` slice around the local-binding `rootIsSchemeAlias && rootBoundIsBaseLike` `keepTargetFinal` / `targetC` branch in `Fallback.hs`.
- `F2` is now selected for `round-043`; the controller is parked at the next lawful planner handoff on branch `codex/round-043-f2-scheme-alias-hardening` and worktree `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-043`.
- The `round-043` worktree and branch already exist, `selection.md` is synced into `.worktrees/round-043/orchestrator/rounds/round-043/`, and no round-local `plan.md` or later-stage artifacts exist yet, so the next exact lawful action is a fresh delegated planner handoff for `F2`.
- The delegated `round-043` planner completed cleanly and wrote the initial non-retry `F2` plan, freezing the canonical artifact path `docs/plans/2026-03-19-uri-r2-c1-f2-bounded-implementation-slice.md`, the bounded `Fallback.hs` / `PipelineSpec.hs` ownership, and the mandatory focused plus full-gate verification contract.
- The delegated `round-043` implementer completed `F2` attempt 1 in the round worktree without widening scope: `Fallback.hs` now makes the selected local-binding scheme-alias/base-like lane explicit via `rootLocalSchemeAliasBaseLike`, `PipelineSpec.hs` adds exactly one positive same-lane example plus one matched fail-closed contrast inside the existing `ARI-C1` block, `boundVarTarget` stayed absent for the slice, and the full repo gate passed at `1132 examples, 0 failures`.
- The delegated `round-043` reviewer accepted `F2` attempt 1 with `stage_action: finalize`; fresh focused verification passed at `11 examples, 0 failures`, the fresh full gate passed at `1132 examples, 0 failures`, and the authoritative `review-record.json` confirms the diff stayed bounded to the frozen `F2` lane with no controller-state, roadmap, bug-tracker, or predecessor-history drift.
- The delegated merger marked `round-043` ready for squash merge with title `elab(f2): harden local scheme-alias/base-like fallback lane`; the controller committed the round branch at `89f0813`, squash-merged it into `codex/automatic-recursive-type-inference` as `d4d4bc2`, and advanced `orchestrator/state.json` to `stage: "update-roadmap"` with `last_completed_round: "round-043"`.
- The delegated roadmap update marked `F2` done, added the accepted `F2` artifact to roadmap continuity context, and tightened `F3` / `F4` around re-verifying and deciding exactly the accepted local scheme-alias/base-like slice without widening. The roadmap update is now committed as `e5af113`, and the controller is parked at `round-044` / `stage: "select-task"` for the next lawful bounded round.
- The delegated `round-044` cycle completed cleanly as docs-only verification: `F3` reverified the accepted `F2` local `rootLocalSchemeAliasBaseLike` / `targetC -> rootFinal` lane under read-only `Fallback.hs` / `PipelineSpec.hs` anchor checks, a fresh focused `ARI-C1` rerun (`11 examples, 0 failures`), a fresh full `cabal build all && cabal test` gate (`1132 examples, 0 failures`), and predecessor continuity; the round finalized on attempt 1, merged as `89768b3`, and the roadmap update committed as `db9f027`.
- The delegated `round-045` cycle completed cleanly as docs-only aggregate decision: `F4` finalized the next-cycle decision token `continue-bounded` on attempt 1 without reopening `F1` / `F2` / `F3`, the round merged as `3e492ba`, and the roadmap update committed as `aacb8be`.
- Accepted `F4 = continue-bounded` lawfully appended a new bounded `G1` through `G4` cycle. The delegated guider has already selected `G1` for `round-046`, and the controller is now parked at the next lawful planner handoff on branch `codex/round-046-g1-next-target-bind` and worktree `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-046`.
- The user-requested archived `run-orchestrator-loop` skill path is absent, but the installed skill at `/Users/ares/src/orchestratorpattern/skills/public/run-orchestrator-loop/SKILL.md` matches the repo-local controller contract and remains usable as the authoritative fallback.
- `round-046` remains a docs-only `G1` bind stage: it must freeze exactly one next bounded `G2` slice drawn from at most one remaining `keepTargetFinal` trigger family (`rootHasMultiInst` or `instArgRootMultiBase`) without reopening accepted `E2` / `E3` or `F1` / `F2` / `F3` / `F4` history.
- `G1` finalized authoritatively after a same-round docs-only retry and froze `G2` to the local-binding `rootHasMultiInst` family while leaving `instArgRootMultiBase` explicitly unselected.
- `G2` finalized authoritatively and merged the bounded `rootLocalMultiInst = rootBindingIsLocalType && rootHasMultiInst` / `targetC -> rootFinal` lane in `Fallback.hs` plus focused `PipelineSpec` coverage; fresh verification passed at `13 examples, 0 failures` for the focused `ARI-C1` block and `1134 examples, 0 failures` for the full repo gate.
- `round-048` / `G3` implement-stage evidence is authored and no blocker was found in the focused rerun or full gate, but the loop is currently blocked at the fresh reviewer handoff because the platform reported subagent usage-limit exhaustion before any reviewer-owned artifacts were written.
- The temporary `G3` reviewer-availability blockage was controller-only, not evidence-related: once a fresh reviewer subagent became available again, `G3` finalized on attempt 1 without any content changes to the authored verification packet.
- `G4` finalized authoritatively on attempt 1 with result token `continue-bounded`, while still preserving the repaired `URI-R2-C1` boundary and refusing `widen-approved` and `stop-blocked` as unlawful on the accepted `G3` evidence chain.
- The continue-bounded follow-on roadmap is now fully complete through item 16; `orchestrator/state.json` can lawfully rest at `stage: "done"` with no active round.
- The authored `G1` packet freezes the next `G2` slice to the local-binding `rootHasMultiInst` trigger family, explicitly leaving `instArgRootMultiBase` unselected for this cycle.
- `round-046` / `G1` attempt 1 failed review for a docs-only control-plane defect, not for the frozen `G2` target: `selection.md` still described stale repository facts (`BUG-2026-03-16-001` open, `orchestrator/state.json` modified, task packet untracked) that no longer hold.
- `round-046` / `G1` attempt 2 repaired only the stale selection facts, then finalized cleanly. The accepted `G1` authoritative record keeps the canonical `docs/plans/2026-03-19-uri-r2-c1-g1-next-target-bind.md` artifact, freezes exactly the `rootHasMultiInst` family for future `G2`, and leaves `instArgRootMultiBase` for a later bounded cycle if ever selected.
- The accepted `G1` roadmap update now makes `G2` the lowest-numbered unfinished item and tightens items 14 through 16 around the exact selected local-binding `rootHasMultiInst` `keepTargetFinal` / `targetC` lane.
- `round-047` is now prepared on branch `codex/round-047-g2-multi-inst-hardening` and worktree `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-047`, with `selection.md` and `plan.md` already authored for the bounded `G2` implementation slice.
- The delegated `round-046` planner chose `rootHasMultiInst` as the single bounded `G2` family, treating it as narrower and more reviewer-auditable than `instArgRootMultiBase`, and authored `orchestrator/rounds/round-046/plan.md` with the canonical `G1` artifact path `docs/plans/2026-03-19-uri-r2-c1-g1-next-target-bind.md`.
- The delegated `round-046` implementer authored the canonical docs-only `G1` bind artifact plus `implementation-notes.md`, recorded the full docs-only verification bundle, and preserved the explicit Cabal-gate skip because no code or test files changed.
- The implementer wrapper failed to terminate cleanly after producing the expected files, but the authored worktree artifacts were complete and syncable, so the controller preserved them and moved only to the reviewer handoff boundary instead of inventing any missing content.
