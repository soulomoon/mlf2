# Findings: Task 29 Solved Follow-up Implementation

## Code Mapping
- Phase 5/6 closeout documentation sync:
  - `implementation_notes.md`: added `2026-03-03 Task 29 solved follow-up closure` summary covering adapter consolidation, constructor migration, typed solved/base fallback handling, guard expansion, and review-fix closure.
  - `TODO.md`: updated Task 29 section to completed status with verification result and archive location.

- Phase 4 review-fix follow-up (new agent team):
  - `test/Constraint/SolvedSpec.hs`:
    - Replaced tautological constructor alias test with a semantic guard:
      - verifies `fromConstraintAndUf` canonical chasing (`canonical 0 = 1`),
      - canonicalized `lookupNode`,
      - preserved `originalNode` pre-merge data,
      - preserved `classMembers`, `instEdges`, and `originalConstraint`.
  - `test/ElaborationSpec.hs`:
    - Strengthened result-type fixture construction in `resultTypeInputsForArtifacts`:
      - derives non-empty `gaBaseToSolved` by canonicalizing base-domain keys,
      - builds deterministic reverse `gaSolvedToBase`.
    - Tightened guard test name/claims:
      - renamed to `AAnn root: primary annotation result type matches fallback facade with populated GA mappings`.
      - asserts non-empty GA maps and explicit `resolveGaSolvedToBase` mapped resolution before equivalence check.
  - `test/PipelineSpec.hs`:
    - Renamed adapter guard to avoid overclaiming:
      - `shared solved-to-presolution adapter matches selected solved queries on representative corpus`.

- Phase 3 typed solved/base resolution:
  - `src/MLF/Constraint/Presolution/Plan/Context.hs`:
    - Added `SolvedToBaseResolution` (`SolvedToBaseMapped | SolvedToBaseSameDomain | SolvedToBaseMissing`).
    - Added `resolveGaSolvedToBase :: GaBindParents -> NodeId -> SolvedToBaseResolution`.
    - Replaced ad hoc `gaSolvedToBase` fallback logic in `resolveTargetBase`, `resolveScopeRoot`, `resolveScopeGen`, and `resolveFirstGenAncestor` with explicit pattern-matched handling over `resolveGaSolvedToBase`.
    - Kept prior behavior for non-mapped cases: `resolveScopeRoot` remains mapping-only for scope-root remap; `resolveScopeGen` and `resolveTargetBase` still fall back to solved-domain behavior when no explicit mapping exists.
  - `src/MLF/Constraint/Presolution.hs`:
    - Phase 3 temporarily re-exported `SolvedToBaseResolution(..)` and `resolveGaSolvedToBase`; Phase 4 removed those re-exports and moved test access to `MLF.Constraint.Presolution.Plan.Context`.
- Phase 3 guard tests:
  - `test/ElaborationSpec.hs`:
    - Added guard test `gaSolvedToBase resolution classifies mapped, same-domain, and missing outcomes`.
    - Imports `SolvedToBaseResolution(..)` and `resolveGaSolvedToBase` from `MLF.Constraint.Presolution`.
- Phase 2 runtime-constructor migration:
  - `src/MLF/Constraint/Solved.hs`: added `fromConstraintAndUf :: Constraint -> IntMap NodeId -> Solved`; retained `mkTestSolved` as a compatibility alias.
  - `src/MLF/Constraint/Presolution/Plan.hs`: `buildSolvedFromPresolutionView` now calls `Solved.fromConstraintAndUf`.
  - `src/MLF/Reify/Core.hs`: `reify*OnConstraint` helpers now call `Solved.fromConstraintAndUf` (identity canonical via empty map).
  - `src/MLF/Elab/Generalize.hs`: updated identity-canonical note to reference `fromConstraintAndUf`.
  - `test/Constraint/SolvedSpec.hs`: added constructor parity guard `fromConstraintAndUf matches mkTestSolved on equivalent inputs`.
- Added shared adapter:
  - `src/MLF/Constraint/Presolution/View.hs`: new `fromSolved :: Solved.Solved -> PresolutionView`
- API boundary tightening:
  - `src/MLF/Constraint/Presolution.hs`: removed `fromSolved` re-export (call sites now import from `MLF.Constraint.Presolution.View` directly).
- Replaced duplicated runtime adapters:
  - `src/MLF/Elab/Elaborate.hs`: removed local `presolutionViewFromSolved`; now uses `fromSolved`.
  - `src/MLF/Elab/Phi/Translate.hs`: removed local `presolutionViewFromSolved`; now uses `fromSolved`.
  - `src/MLF/Elab/Run/Generalize.hs`: replaced inline `PresolutionView` record builder with `fromSolved`.
  - `src/MLF/Elab/Run/Pipeline.hs`: removed local `solvedToPresolutionView`; now uses `fromSolved`.
- Replaced migration guard shape:
  - `test/PipelineSpec.hs`: semantic test (`shared solved-to-presolution adapter preserves solved query semantics`) comparing `fromSolved` view queries against `Solved` queries over a representative corpus.
- Test visibility plumbing:
  - `mlf2.cabal`: exposed `MLF.Constraint.Presolution.View` from `mlf2-internal` so `mlf2-test` can import the boundary adapter without broadening `MLF.Constraint.Presolution`.
- Phase 4 solved-boundary guard tests:
  - `test/Constraint/SolvedSpec.hs`:
    - Added direct invariant guards:
      - `validateOriginalCanonicalAgreement` reports expected mismatch on merged-domain fixture.
      - `validateOriginalCanonicalAgreement` returns empty when domains align.
      - `canonicalizedBindParents` rewrites non-canonical child/parent refs into canonical-domain refs.
  - `test/ElaborationSpec.hs`:
    - Added `O15-TR-SEQ-EMPTY-IDENTITY: Trχ(ε)=ε when Σ(g)=ε` to isolate empty Ω translation from reorder coupling.
    - Added result-type guard `primary annotation result type matches fallback facade on replay-relevant annotated roots`.
- Phase 4 boundary hygiene follow-up:
  - `test/ElaborationSpec.hs` now imports `SolvedToBaseResolution(..)` / `resolveGaSolvedToBase` from `MLF.Constraint.Presolution.Plan.Context` (internal boundary) instead of top-level `MLF.Constraint.Presolution`.
  - `src/MLF/Constraint/Presolution.hs` no longer exports `SolvedToBaseResolution(..)` / `resolveGaSolvedToBase`.
  - `mlf2.cabal` now exposes `MLF.Constraint.Presolution.Plan.Context` and `MLF.Elab.Run.ResultType` from private `mlf2-internal` for test-only access.

## Test/Verification Evidence
- Review-fix targeted + full validation (2026-03-03):
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "MLF.Constraint.Solved"'` passed (`44 examples, 0 failures`).
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "AAnn root: primary annotation result type matches fallback facade with populated GA mappings"'` passed (`1 example, 0 failures`).
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "shared solved-to-presolution adapter matches selected solved queries on representative corpus"'` passed (`1 example, 0 failures`).
  - `cabal build all && cabal test` passed (`913 examples, 0 failures`).

- Phase 4 TDD red step:
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "primary annotation result type matches fallback facade"'` failed first with hidden-module errors:
    - `Could not load module ‘MLF.Constraint.Presolution.Plan.Context’`
    - `Could not load module ‘MLF.Elab.Run.ResultType’`
- Phase 4 targeted + full validation:
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "validateOriginalCanonicalAgreement reports original/canonical tag mismatches"'` passed (1 example).
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "canonicalizedBindParents rewrites non-canonical child/parent refs into canonical-domain refs"'` passed (1 example).
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "O15-TR-SEQ-EMPTY-IDENTITY: Trχ(ε)=ε when Σ(g)=ε"'` passed (1 example).
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "primary annotation result type matches fallback facade on replay-relevant annotated roots"'` passed (1 example).
  - `cabal test` passed (`913 examples, 0 failures`).
  - `cabal build all && cabal test` passed (`913 examples, 0 failures`).
- Phase 3 TDD red step:
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "gaSolvedToBase resolution classifies"'` failed first with missing exports:
    - `Module ‘MLF.Constraint.Presolution’ does not export ‘SolvedToBaseResolution(..)’`
    - `Module ‘MLF.Constraint.Presolution’ does not export ‘resolveGaSolvedToBase’`
- Phase 3 targeted + full validation:
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "gaSolvedToBase resolution classifies"'` passed (1 example).
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "generalizeAt fallback reifies from solved root even when base mapping points elsewhere"'` passed (1 example).
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "ga-invariant: validateCrossGenMapping"'` passed (2 examples).
  - `cabal test` passed (`908 examples, 0 failures`).
- Phase 2 TDD red step:
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "fromConstraintAndUf matches mkTestSolved on equivalent inputs"'` failed first with `Variable not in scope: fromConstraintAndUf`.
- Phase 2 targeted + full validation:
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "fromConstraintAndUf matches mkTestSolved on equivalent inputs"'` passed (1 example).
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "/Pipeline (Phases 1-5)/Integration Tests/"'` passed (18 examples).
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "/Phase 6 — Elaborate (xMLF)/Migration guards/"'` passed (2 examples).
  - `cabal test` passed (`907 examples, 0 failures`).
- Initial TDD red step (Phase 1 baseline):
  - `cabal test mlf2-test --test-show-details=direct --test-options="--match \"runtime elaboration modules share solved-to-presolution view adapter\""` failed before dedup refactor.
- Review-fix targeted checks:
  - `cabal test mlf2-test --test-show-details=direct --test-options="--match \"shared solved-to-presolution adapter preserves solved query semantics\""` passed (1 example).
  - `cabal test mlf2-test --test-show-details=direct --test-options="--match \"/Pipeline (Phases 1-5)/Integration Tests/\""` passed (18 examples).
  - `cabal test mlf2-test --test-show-details=direct --test-options="--match \"/Phase 6 — Elaborate (xMLF)/Migration guards/\""` passed (2 examples).
  - `cabal test mlf2-test --test-show-details=direct --test-options="--match \"PresolutionView mirrors solved canonical/node/bound queries\""` passed (1 example).
- Full confidence run:
  - `cabal test` passed (`906 examples, 0 failures`).

## Thesis Alignment Notes
- Refactor-only boundary consolidation: no algorithmic behavior changes; centralizes a representational adapter (`Solved` projection into `PresolutionView`) at the existing presolution boundary module.
- Keeps runtime elaboration code on the same read-only query surface while reducing duplication risk.

## Initial Hotspots (from static scan)
- Duplicated `Solved -> PresolutionView` adapters in:
  - `src/MLF/Elab/Elaborate.hs` (`presolutionViewFromSolved`)
  - `src/MLF/Elab/Phi/Translate.hs` (`presolutionViewFromSolved`)
  - `src/MLF/Elab/Run/Generalize.hs` (inline record in `generalizeAtWithBuilder`)
  - `src/MLF/Elab/Run/Pipeline.hs` (`solvedToPresolutionView`)
- Runtime `mkTestSolved` usage in:
  - `src/MLF/Constraint/Presolution/Plan.hs` (`buildSolvedFromPresolutionView`)
  - `src/MLF/Reify/Core.hs` (`reify*OnConstraint` helpers)
- `gaSolvedToBase` fallback handling concentrated in:
  - `src/MLF/Constraint/Presolution/Plan/Context.hs` (`resolveTargetBase`, `resolveScopeRoot`, `resolveScopeGen`, `resolveFirstGenAncestor`).
