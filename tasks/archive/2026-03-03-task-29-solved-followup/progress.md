# Progress Log: Task 29 Solved Follow-up Implementation

- 2026-03-03: Initialized task folder and planning files at `tasks/todo/2026-03-03-task-29-solved-followup/`.
- 2026-03-03: Starting subagent-driven implementation loops for Task 29 follow-up items.
- 2026-03-03: Phase 1 implementation dispatched to worker agent `019cafcf-e98b-7cd3-9546-e776a75efb33` (shared solved->presolution adapter + call-site migration).
- 2026-03-03: Added Phase 1 migration guard in `test/PipelineSpec.hs` and ran targeted red test (`--match "runtime elaboration modules share solved-to-presolution view adapter"`); observed expected failure before code changes.
- 2026-03-03: Implemented shared `fromSolved` adapter in `MLF.Constraint.Presolution.View`, re-exported from `MLF.Constraint.Presolution`, and migrated runtime call sites in Elaborate/Phi.Translate/Run.Generalize/Run.Pipeline.
- 2026-03-03: Removed redundant local solved->presolution adapters from runtime modules and resolved one compile issue (`pvCanonical` import scope in `Pipeline.hs`).
- 2026-03-03: Ran targeted validation (`Pipeline` integration guards, `Elaboration` migration guards, `PresolutionView` parity guard) and full suite (`cabal test`); all green (`906 examples, 0 failures`).
- 2026-03-03: Applied review-fix iteration: removed `fromSolved` re-export from `MLF.Constraint.Presolution`; runtime modules now import `MLF.Constraint.Presolution.View` directly.
- 2026-03-03: Replaced brittle source-text adapter guard in `PipelineSpec` with semantic solved-query parity assertions using `fromSolved`.
- 2026-03-03: Exposed `MLF.Constraint.Presolution.View` in `mlf2-internal` for test access; initial compile failed due hidden-module import, then passed after cabal exposure update.
- 2026-03-03: Re-ran relevant tests (new semantic adapter test, `Pipeline` integration slice, elaboration migration guards, solved-view parity guard) and full `cabal test`; all green (`906 examples, 0 failures`).
- 2026-03-03: Phase 1 approved after spec + quality re-review; proceeding to Phase 2 (`mkTestSolved` runtime dependency removal).
- 2026-03-03: Phase 2 RED step: added `Constraint.SolvedSpec` parity test for a new non-test constructor (`fromConstraintAndUf`) and confirmed expected failure (`Variable not in scope: fromConstraintAndUf`).
- 2026-03-03: Phase 2 GREEN step: added `Solved.fromConstraintAndUf`, kept `mkTestSolved` as compatibility alias, and migrated runtime call sites in `Presolution/Plan.hs` and `Reify/Core.hs`.
- 2026-03-03: Updated related runtime note in `MLF.Elab.Generalize` to reference `fromConstraintAndUf` identity-canonical path.
- 2026-03-03: Phase 2 validation passed: targeted constructor guard, pipeline integration slice, elaboration migration guards, and full suite (`907 examples, 0 failures`).
- 2026-03-03: Phase 2 approved after spec + quality review; starting Phase 3 (`gaSolvedToBase` explicit typed fallback handling + guards).
- 2026-03-03: Phase 3 RED step: added `ElaborationSpec` guard test for typed solved/base resolution outcomes and confirmed expected failure due missing exports (`SolvedToBaseResolution`, `resolveGaSolvedToBase`).
- 2026-03-03: Phase 3 GREEN step: introduced `SolvedToBaseResolution` + `resolveGaSolvedToBase` in `Plan/Context`, replaced ad hoc fallback paths in `resolveTargetBase`, `resolveScopeRoot`, `resolveScopeGen`, and `resolveFirstGenAncestor`, and re-exported the helper via `MLF.Constraint.Presolution`.
- 2026-03-03: Phase 3 validation passed: targeted new guard, existing fallback and ga-invariant slices, and full `cabal test` (`908 examples, 0 failures`).
- 2026-03-03: Phase 3 approved after spec + quality review; proceeding to Phase 4 (guard-test expansion).
- 2026-03-03: Phase 4 RED step: added solved-boundary guard tests in `test/Constraint/SolvedSpec.hs` and `test/ElaborationSpec.hs`; first targeted run failed as expected because `MLF.Constraint.Presolution.Plan.Context` and `MLF.Elab.Run.ResultType` were hidden from `mlf2-test`.
- 2026-03-03: Phase 4 GREEN step: exposed `MLF.Constraint.Presolution.Plan.Context` and `MLF.Elab.Run.ResultType` from `mlf2-internal` (`mlf2.cabal`), moved solved/base guard imports to `Plan.Context`, and removed temporary top-level `MLF.Constraint.Presolution` re-exports for `SolvedToBaseResolution`/`resolveGaSolvedToBase`.
- 2026-03-03: Phase 4 targeted validations passed:
  - `--match "validateOriginalCanonicalAgreement reports original/canonical tag mismatches"` (1 example)
  - `--match "canonicalizedBindParents rewrites non-canonical child/parent refs into canonical-domain refs"` (1 example)
  - `--match "O15-TR-SEQ-EMPTY-IDENTITY: Trχ(ε)=ε when Σ(g)=ε"` (1 example)
  - `--match "primary annotation result type matches fallback facade on replay-relevant annotated roots"` (1 example)
- 2026-03-03: Full suite passed after Phase 4 updates: `cabal test` (`913 examples, 0 failures`).
- 2026-03-03: Repository validation command passed: `cabal build all && cabal test` (`913 examples, 0 failures`).
- 2026-03-03: Started post-Phase-4 review-fix pass with a new parallel agent team:
  - worker A (`019cb008-5dab-7210-bca2-9a4c8d932a8a`) for `Constraint.SolvedSpec` constructor guard quality,
  - worker B (`019cb008-5de4-7b32-848d-ec855b26b1d7`) for `ElaborationSpec` result-type GA mapping realism,
  - reviewer (`019cb008-5dd0-7dd3-82e8-ece24f5ebec1`) for API-hygiene guidance.
- 2026-03-03: Applied worker A change: replaced tautological `fromConstraintAndUf` alias parity check with semantic constructor invariants in `test/Constraint/SolvedSpec.hs`.
- 2026-03-03: Applied worker B change: strengthened `resultTypeInputsForArtifacts` with populated GA maps and renamed/strengthened AAnn result-type guard in `test/ElaborationSpec.hs`.
- 2026-03-03: Fixed minor merge warning (`foldl'` redundant import) in `test/ElaborationSpec.hs`.
- 2026-03-03: Renamed adapter parity test wording in `test/PipelineSpec.hs` to avoid overclaiming query coverage.
- 2026-03-03: Review-fix validations:
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "MLF.Constraint.Solved"'` → pass (`44 examples, 0 failures`).
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "AAnn root: primary annotation result type matches fallback facade with populated GA mappings"'` → pass (`1 example, 0 failures`).
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "shared solved-to-presolution adapter matches selected solved queries on representative corpus"'` → pass (`1 example, 0 failures`).
  - `cabal build all && cabal test` → pass (`913 examples, 0 failures`).
- 2026-03-03: Phase 5 docs sync completed:
  - updated `implementation_notes.md` with Task 29 closure summary and runtime/thesis-alignment mapping.
  - updated `TODO.md` Task 29 section to completed status with validation + archive details.
- 2026-03-03: Phase 6 closeout completed:
  - marked all phases completed in `task_plan.md`.
  - moved task folder from `tasks/todo/2026-03-03-task-29-solved-followup` to `tasks/archive/2026-03-03-task-29-solved-followup`.
