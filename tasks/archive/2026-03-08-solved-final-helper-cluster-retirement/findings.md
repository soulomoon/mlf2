# Findings

- After the previous cleanup rounds, the only remaining non-must-stay public `Solved` helpers are the Reify/view-facing cluster: `lookupVarBound`, `genNodes`, `weakenedVars`, `isEliminatedVar`, and `canonicalizedBindParents`.
