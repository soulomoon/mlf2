You are the Planner agent for round 1.
Use only the evidence in this prompt. Do not use shell commands. Do not spawn subagents. Do not edit files.
Produce a thesis-exact implementation plan for the accepted simplification.
Primary source: `papers/these-finale-english.txt`; use `papers/xmlf.txt` only if the thesis is silent.

Accepted idea JSON:
{"title":"Extract the shared snapshot-canonicalization prelude between `Finalize` and `Presolution.View`","problem":"Candidate 2 is the best remaining simplification. `src/MLF/Constraint/Finalize.hs` and `src/MLF/Constraint/Presolution/View.hs` both still own the same live-node snapshot-UF sanitization logic, and `Finalize.presolutionViewFromSnapshot` then rebuilds a `PresolutionView` only to override its canonical constraint. By contrast, candidate 1 appears stale: `TODO.md` still mentions retiring `allNodes`/`instEdges`, but the current public `src/MLF/Constraint/Solved.hs` facade no longer exports them. Candidate 3 is also weaker: `computeResultTypeFromAnnLocal` in `Fallback.hs` is not a real duplicate of `Ann.computeResultTypeFromAnn`; it is a tiny cycle-breaking fallback helper with intentionally different behavior.","why_this_is_simpler":"A small extraction can centralize the shared snapshot prelude: sanitize the UF once, derive canonical-map/canonical-query data once, and let callers choose only the final canonical-constraint policy. That removes duplicated liveness filtering, avoids the current build-then-override pattern in `presolutionViewFromSnapshot`, and makes future maintenance less error-prone without widening the abstraction. The refactor stays bounded because it can stop at shared snapshot preparation rather than trying to merge the distinct finalized-vs-raw canonical-constraint paths.","why_thesis_exactness_should_be_preserved":"This seam sits in constraint canonicalization/finalization plumbing, which must remain semantically identical to the thesis-driven solving pipeline. The simplification should preserve the exact current sequence: drop dead/self UF entries, compute canonical representatives from the sanitized UF, and keep the existing distinction between raw snapshot rewriting in `fromPresolutionResult` and repaired/finalized constraint construction in `Finalize`. In other words, share the preparation, not the semantics.","likely_files":["src/MLF/Constraint/Finalize.hs","src/MLF/Constraint/Presolution/View.hs","implementation_notes.md","CHANGELOG.md"],"expected_benefits":["Removes one live copy of `sanitizeSnapshotUf`-style logic.","Eliminates the awkward `fromPresolutionResult` then `pvCanonicalConstraint` override path.","Reduces divergence risk between presolution-view and finalize code paths.","Keeps the simplification local to two constraint modules."],"risks":["Accidentally collapsing the intentional difference between snapshot rewriting and finalized/repaired canonical constraints.","Changing which UF map is used to build `pvCanonicalMap` could regress binding-tree validity or view parity.","Over-extracting into a too-general helper could obscure the explicit finalization stages."],"required_validation":["Keep a focused regression that dead/self UF entries are sanitized identically for both presolution-view and finalize entrypoints.","Re-run the presolution-view parity coverage referenced in `TODO.md` (`PresolutionView mirrors solved canonical/node/bound queries`).","Run targeted finalize/solved strict-validation tests, then `cabal build all && cabal test` before claiming completion."]}
Initial verifier gate JSON:
{
  "gate": "YES",
  "reason": "This simplification is still needed because the duplicate snapshot-UF sanitization/canonicalization prelude is still live in both `Finalize` and `Presolution.View`, and `presolutionViewFromSnapshot` currently has an awkward build-then-override shape that directly signals extractable shared setup. The competing simplifications are either already stale or intentionally non-duplicative, so this remains the strongest bounded cleanup. The extraction should stay narrow—share only the snapshot preparation, not the final canonical-constraint semantics—to preserve the current thesis-sensitive distinction between presolution-view rewriting and finalized/repaired constraint construction.",
  "evidence": [
    "`src/MLF/Constraint/Finalize.hs` exports `stepSanitizeSnapshotUf`, `stepCanonicalizeConstraint`, and `presolutionViewFromSnapshot`, while `src/MLF/Constraint/Presolution/View.hs` still defines its own `sanitizeSnapshotUf` and `fromPresolutionResult` path.",
    "`Finalize.presolutionViewFromSnapshot` currently calls `fromPresolutionResult` and then overrides `pvCanonicalConstraint`, which shows a shared snapshot-prelude is being recomputed while the final canonical-constraint policy intentionally differs.",
    "Candidate 1 is stale in the current codebase: `TODO.md` now focuses on the restricted `Solved` facade, `Bugs.md` has no open bugs, and `src/MLF/Constraint/Solved.hs` no longer exports `allNodes` or `instEdges`.",
    "Candidate 3 is weaker because `ResultType/Fallback.hs` explicitly describes `computeResultTypeFromAnnLocal` as a local simplified helper to avoid circular imports, not a drop-in duplicate of `Ann.computeResultTypeFromAnn`.",
    "The evidence packet explicitly flags the thesis-exactness constraint: any extraction must preserve the current distinction between raw snapshot rewriting and finalized/repaired canonical constraints."
  ]
}

Evidence packet:
- `src/MLF/Constraint/Finalize.hs`
  - `stepSanitizeSnapshotUf` drops dead/self UF entries.
  - `stepCanonicalizeConstraint` sanitizes then calls `Solve.repairNonUpperParents (Solve.rewriteConstraintWithUF ufSanitized constraint)`.
  - `presolutionViewFromSnapshot` sanitizes the UF, calls `fromPresolutionResult`, then overrides `pvCanonicalConstraint` with `stepCanonicalizeConstraint constraint ufSanitized`.
  - `finalizePresolutionViewFromSnapshot` finalizes via `Solve.finalizeConstraintWithUF`, then overrides `pvCanonicalConstraint` again with the finalized canonical constraint.
- `src/MLF/Constraint/Presolution/View.hs`
  - `fromPresolutionResult` sanitizes the UF, builds `pvCanonicalMap`/`pvCanonical`, and sets `pvCanonicalConstraint = rewriteConstraintWithUF uf constraint`.
  - `sanitizeSnapshotUf` is a second copy of the same live-node/self-edge filtering logic.
- Existing validation hooks already relevant to this seam include:
  - `PresolutionView mirrors solved canonical/node/bound queries`
  - `fromSolveOutput matches explicit pre-rewrite snapshot construction`
  - targeted `MLF.Constraint.Solved` tests
  - full gate `cabal build all && cabal test`
- `TODO.md` and `implementation_notes.md` both treat facade cleanup as largely complete; this snapshot-prelude duplication remains a bounded local cleanup.
- Thesis constraint: preserve the distinction between raw snapshot rewriting (`rewriteConstraintWithUF`) and finalized/repaired canonical constraint construction (`repairNonUpperParents`, `finalizeConstraintWithUF`). Helper ownership may change, but semantics must not.

Return JSON only matching the planner plan schema.
