You are the Planner agent for round 1.
Use only the evidence in this prompt. Do not use shell commands.
Decide whether attempt 2 is good enough relative to the plan.

Current plan JSON:
{"problem_statement":"`src/MLF/Constraint/Finalize.hs` and `src/MLF/Constraint/Presolution/View.hs` both own the same snapshot-UF sanitization/live-node filtering prelude, and `Finalize.presolutionViewFromSnapshot` currently rebuilds a `PresolutionView` through `fromPresolutionResult` only to replace `pvCanonicalConstraint`. That leaves one real duplicate and one awkward build-then-override path in thesis-sensitive canonicalization plumbing.","thesis_alignment":"Treat this as a refactor of shared preparation only, not a semantic merge. Preserve the current sequence described in the evidence packet: sanitize dead/self UF entries, derive canonical representatives from that sanitized UF, then keep the existing entrypoint-specific canonical-constraint policy unchanged. `fromPresolutionResult` must continue using raw `rewriteConstraintWithUF`, while finalize paths must continue using `repairNonUpperParents` and `finalizeConstraintWithUF` where they do today. If any ambiguity appears, resolve it against `papers/these-finale-english.txt`, using `papers/xmlf.txt` only if the thesis is silent.","root_cause":"The shared snapshot preparation concerns are currently embedded inside two different view-construction paths instead of being factored as one reusable prelude. Because canonical-map/query derivation and final canonical-constraint construction are mixed together, `Presolution.View` duplicated `sanitizeSnapshotUf`, and `Finalize` compensates by constructing a full `PresolutionView` and overriding only `pvCanonicalConstraint`.","exact_files_to_modify":["src/MLF/Constraint/Finalize.hs","src/MLF/Constraint/Presolution/View.hs","implementation_notes.md","CHANGELOG.md"],"step_by_step_tasks":["Define the semantic boundary first: share only sanitized-UF and canonical-map/query preparation, while leaving raw snapshot rewriting and finalized/repaired canonical-constraint construction distinct.","In `src/MLF/Constraint/Presolution/View.hs`, split `fromPresolutionResult` into a shared snapshot-preparation helper plus a small `PresolutionView` builder that accepts the caller-chosen canonical constraint.","Have that shared helper produce the sanitized UF and the canonical-map/query data from the sanitized UF, so `fromPresolutionResult` preserves its current `rewriteConstraintWithUF` behavior without owning duplicate sanitization logic.","In `src/MLF/Constraint/Finalize.hs`, rewire `stepSanitizeSnapshotUf` and `presolutionViewFromSnapshot` to consume the shared preparation path, keeping `stepCanonicalizeConstraint` exactly as the finalized/repaired policy and removing the build-then-override shape.","Apply the same shared-prelude usage to `finalizePresolutionViewFromSnapshot`, so it still uses finalized canonical-constraint construction but no longer recomputes or overrides a separately built raw view.","Record the refactor as a bounded no-semantics-change cleanup in `implementation_notes.md` and `CHANGELOG.md`, explicitly noting that the raw-vs-finalized canonical-constraint distinction remains intentional and thesis-sensitive.","Validate with the existing presolution-view parity hooks, solved/finalize strict-validation coverage, and then the full Cabal gate before claiming completion."],"tests_and_commands":["Run or preserve a focused regression that dead/self UF entries are sanitized identically for both the presolution-view and finalize entrypoints.","Re-run the existing parity coverage named `PresolutionView mirrors solved canonical/node/bound queries`.","Re-run the existing snapshot-construction parity coverage named `fromSolveOutput matches explicit pre-rewrite snapshot construction`.","Run targeted finalize/solved validation with `cabal test --test-show-details=direct`.","Run the full verification gate: `cabal build all && cabal test`."],"acceptance_criteria":["There is exactly one shared implementation of the snapshot-UF sanitization/canonicalization prelude for this seam, or a single shared implementation plus a thin delegating compatibility wrapper if `Finalize` must keep its current surface.","`fromPresolutionResult` still derives `pvCanonicalMap` and `pvCanonical` from the sanitized UF and still sets `pvCanonicalConstraint` with raw `rewriteConstraintWithUF`.","`presolutionViewFromSnapshot` no longer constructs a `PresolutionView` only to replace `pvCanonicalConstraint`, but its resulting canonical constraint remains semantically identical to the current `stepCanonicalizeConstraint` path.","`finalizePresolutionViewFromSnapshot` also reuses the shared preparation while preserving its current finalized canonical-constraint behavior.","The refactor stays local to the two constraint modules plus the noted docs, with no broadened abstraction that obscures the explicit finalization stages.","The named parity/validation hooks pass, followed by `cabal build all && cabal test`.","`implementation_notes.md` and `CHANGELOG.md` explain that the cleanup shares preparation only and does not change thesis-facing semantics."],"abort_criteria":["Abort if the extraction would collapse the intentional difference between raw snapshot rewriting and repaired/finalized canonical-constraint construction.","Abort if the shared helper changes which UF data feeds `pvCanonicalMap` or `pvCanonical`, or otherwise alters canonical/node/bound query answers.","Abort if preserving behavior requires a broader new abstraction or module split beyond this bounded two-module cleanup.","Abort if targeted parity or solved/finalize validation shows any behavioral drift in canonical constraints, binding-tree validity, or snapshot-view parity.","Abort if a thesis check is needed and indicates that even the proposed shared prelude would hide or reorder a semantically meaningful phase boundary."],"good_enough_definition":["A small shared helper or record prepares sanitized snapshot data once and is used by both `Presolution.View` and `Finalize`.","The duplicated live-node/self-edge sanitization logic is removed from the two-module seam, and the current override-heavy `presolutionViewFromSnapshot` construction is eliminated.","No thesis-facing behavior changes are introduced: only the shared preparation is centralized, while the raw and finalized canonical-constraint policies stay separate.","The existing named parity checks, targeted validation, and full Cabal gate all pass, and the architectural cleanup is documented in `implementation_notes.md` and `CHANGELOG.md`."]}

Implementer attempt 2 JSON:
{
  "blockers": [],
  "change_summary": [
    "Extracted a shared `SnapshotPreparation` path in `src/MLF/Constraint/Presolution/View.hs` that sanitizes snapshot UF data once and derives canonical map/query data from the sanitized UF.",
    "Rewired `src/MLF/Constraint/Finalize.hs` to reuse the shared preparation for `stepSanitizeSnapshotUf`, `presolutionViewFromSnapshot`, and `finalizePresolutionViewFromSnapshot`, removing the old build-then-override `PresolutionView` path.",
    "Preserved thesis-sensitive semantics: `fromPresolutionResult` still uses raw `rewriteConstraintWithUF`, while finalize paths still use repaired/finalized canonical-constraint construction.",
    "Updated `implementation_notes.md` and `CHANGELOG.md` to document the bounded no-semantics-change cleanup and recorded the validation runs.",
    "Validated with the named parity checks, `cabal test --test-show-details=direct`, and `cabal build all && cabal test`."
  ],
  "feasible": "YES",
  "files_changed": [
    "/Users/ares/.config/superpowers/worktrees/mlf4/round-1-snapshot-canonicalization-prelude-attempt2-scratch/src/MLF/Constraint/Presolution/View.hs",
    "/Users/ares/.config/superpowers/worktrees/mlf4/round-1-snapshot-canonicalization-prelude-attempt2-scratch/src/MLF/Constraint/Finalize.hs",
    "/Users/ares/.config/superpowers/worktrees/mlf4/round-1-snapshot-canonicalization-prelude-attempt2-scratch/implementation_notes.md",
    "/Users/ares/.config/superpowers/worktrees/mlf4/round-1-snapshot-canonicalization-prelude-attempt2-scratch/CHANGELOG.md"
  ],
  "meaningful_diff": "YES"
}

QA attempt 2 JSON:
{"gate":"YES","commands_run":["cabal test mlf2-test --test-show-details=direct --test-options='--match \"PresolutionView mirrors solved canonical/node/bound queries\"'","cabal test mlf2-test --test-show-details=direct --test-options='--match \"fromSolveOutput matches explicit pre-rewrite snapshot construction\"'","cabal build all && cabal test"],"failures_if_any":[]}

Observed notes:
- Worktree status contains modifications to exactly these files: `CHANGELOG.md`, `implementation_notes.md`, `src/MLF/Constraint/Finalize.hs`, `src/MLF/Constraint/Presolution/View.hs`.
- `MLF.Constraint.Presolution.View` now defines `SnapshotPreparation`, `prepareSnapshotPreparation`, `prepareSnapshotPreparationFromParts`, and `buildPresolutionView`.
- `MLF.Constraint.Finalize` now reuses that shared preparation for `stepSanitizeSnapshotUf`, `presolutionViewFromSnapshot`, and `finalizePresolutionViewFromSnapshot` while keeping `stepCanonicalizeConstraint` separate.
- QA passed the two named parity checks and `cabal build all && cabal test` (`1005 examples, 0 failures`).

Return JSON only matching the schema.
