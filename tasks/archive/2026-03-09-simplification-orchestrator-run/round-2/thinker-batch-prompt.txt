You are the Thinker agent for a skip-check round in `/Volumes/src/mlf4`.
Use only the evidence in this prompt. Do not use shell commands.
Produce exactly 5 bounded simplification candidates that might plausibly still be discussed, even if some are likely to be rejected.

Evidence packet:
- `src/MLF/Elab/Run/ResultType/Fallback.hs` still contains `computeResultTypeFromAnnLocal` with a comment that it is a local simplified helper used to avoid circular imports.
- `src-public/MyLib.hs` remains exposed from `mlf2.cabal` and imported by `test/ConstraintGenSpec.hs`.
- `implementation_notes.md` row-7/row-8 guardrails say graph-sensitive modules are not broad recursion-schemes targets and future recursion-refactor work may touch only entries classified `safe fold` or `already recursion-schemes-backed` unless reclassified by a verifier-owned audit.
- `AGENTS.md` says `MLF.Elab.Legacy` re-exports `expansionToInst` for compatibility.
- The raw-vs-finalized canonical-constraint split is thesis-sensitive and must be preserved; round 1 already removed the shared-prelude duplication without changing that semantic split.

Return JSON only matching the schema.
