# Findings — 2026-03-08 fallback plan audit

## Key discoveries
- Pending.
- Existing archived campaign folder already present at `tasks/archive/2026-03-08-fallback-removal-campaign/`.
- Current worktree has uncommitted modifications across elaboration/generalization/planner/instantiation/docs/tests related to fallback removal.
- Archived campaign task claims all six phases complete and a final green gate (`cabal build all && cabal test` = `992 examples, 0 failures`).
- Archived findings also record an earlier stop on direct `reifyInst` fallback removal, later followed by authority-preserving repairs in `Phi`/annotation/closure code rather than simple heuristic fallback retention.
- `test/PipelineSpec.hs` now contains source guards asserting absence of `generalizeNeedsFallback`, `fallbackToReify`, `fallbackSchemeType`, `allowFallbackFromTrace`, and `resolveFallbackArgNodes`.
- `test/Presolution/EdgePlannerSpec.hs` now contains synthesized-wrapper strict-failure tests plus a source guard for removal of wrapper-root owner fallback.
- `src/MLF/Constraint/Presolution/EdgeProcessing/Planner.hs` includes comments explicitly stating synthesized wrappers no longer have a wrapper-root recovery path.
- The worktree still has broad uncommitted changes, so archived-task claims need verification against current code and test results rather than trusting task metadata.
- Archived campaign claims all six phases complete and records a final `cabal build all && cabal test` pass (`992 examples, 0 failures`), but this must be checked against current repo state.
- Current source no longer contains the exact helper names `generalizeNeedsFallback`, `fallbackToReify`, `fallbackSchemeType`, `allowFallbackFromTrace`, or `resolveFallbackArgNodes`; source-guard tests for their absence exist in `test/PipelineSpec.hs`.
- Guard tests also exist for planner wrapper-root fallback markers and generic fallback recovery markers inside `inferInstAppArgsFromScheme`.
- `src/MLF/Elab/Run/ResultType/Util.hs` now runs only `generalizeAtWithBuilder ... (Just bindParentsGa)` with no GA→no-GA→reify retry ladder.
- `src/MLF/Elab/Run/Instantiation.hs` no longer contains a named generic `fallback` branch; it only returns `inferFromBound` / `inferFromBody` / `Nothing`.
- `src/MLF/Constraint/Presolution/EdgeProcessing/Planner.hs` now documents strict owner resolution with no wrapper-root recovery path.
- `src/MLF/Elab/Elaborate.hs` contains a fail-fast `PhiTranslatabilityError` when authoritative instantiation translation is missing for `ExpInstantiate`, instead of a trace helper retry ladder.
- `src/MLF/Elab/Generalize.hs` still contains local variables/comments named `fallbackTy` and `scheme-type fallback`, so phase-2 wording is only partially cleaned up even though the specific forbidden helper name is gone.
- Fresh verification in the current worktree: `cabal build all && cabal test` passes now (`992 examples, 0 failures`).
- Despite the green gate, `src/MLF/Elab/Generalize.hs` still has live `MissingNode` retry logic and rigid-bound substitution fallback (`fallbackTy`) that are broader than the plan's explicit fail-fast goal.
- `src/MLF/Elab/Elaborate.hs` still contains a substantial let-generalization fallback chooser (`fallbackChoiceFromVar`, `fallbackChoiceFromApp`, `fallbackChoiceFromLam`, coherence filtering, and `selected` candidate preference), which contradicts the plan’s removal of coherence-scored fallback preference.
- `src/MLF/Elab/Generalize.hs` still contains recursive scheme-type fallback behavior via `generalizeAtForScheme schemeScope typeRootC`, only skipping it when `schemeScope == scopeRootC`; the old marker name is gone, but the fallback behavior remains.
- `src/MLF/Elab/Elaborate.hs` no longer defines `allowFallbackFromTrace` / `resolveFallbackArgNodes`, but `reifyInst` still tries `targetArgs <|> expansionArgs`, so there is still a secondary expansion-based recovery path rather than a single fail-fast authoritative source.
- `src/MLF/Constraint/Presolution/EdgeProcessing/Planner.hs` appears strict for synthesized wrapper ownership; the source comment and tests align with body-root-only resolution.
- `src/MLF/Elab/Run/Instantiation.hs` appears strict for `inferInstAppArgsFromScheme`; the named generic fallback branch markers are gone.
- Planner fallback removal has direct behavioral fail-fast coverage in `test/Presolution/EdgePlannerSpec.hs` plus a source guard for the old wrapper-root lookup marker.
- Instantiation inference strictness has a direct regression in `test/GeneralizeSpec.hs` asserting `Nothing` for the former fallback-recovery case.
- Phase-6 closeout docs are present (`docs/architecture.md`, `implementation_notes.md`, `CHANGELOG.md`), but the archived progress file records a baseline gate, intermediate focused slices, one failing full gate, and a final passing full gate—not a full `cabal build all && cabal test` after each family.
- `implementation_notes.md` intentionally keeps historical notes about earlier fallback-era behavior; the latest closeout section summarizes the new strict path.
- Fresh verification on 2026-03-08: `cabal build all && cabal test` passes in the current worktree (`992 examples, 0 failures`).
- `test/PipelineSpec.hs` guards are mostly source-marker checks; they prove old names were removed, but they do not catch semantically equivalent fallback logic that remains under new names/structure.
- `TODO.md`, `docs/architecture.md`, and `implementation_notes.md` currently describe the fallback-removal campaign as fully complete, but that overstates the live state relative to the stricter user-written plan.
