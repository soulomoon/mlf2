# Findings

- `TODO.md` currently prioritizes keeping recent thesis-exact/fail-fast cleanup guards green; it does not list a competing higher-priority witness/trace refactor, but it emphasizes avoiding new fallback behavior.
- Recent `CHANGELOG.md` / `Bugs.md` entries are dominated by thesis-exact cleanup and guardrail work, indicating that replay/canonicalization-sensitive paths remain active and regression-tested.
- No deeper `AGENTS.md` exists under the repo root, so the root guidance governs this verifier pass.
- `TODO.md` still carries a witness/trace/expansion canonicalization workstream history (`Task 11`) and documents replay-sensitive source-domain handling around `etInterior`/`etCopyMap`.
- `implementation_notes.md` already records one recent successful canonicalization dedup extraction (`MLF.Constraint.Solved` + `MLF.Constraint.Presolution.View` into `MLF.Constraint.Canonicalization.Shared`), which makes another bounded canonicalization dedup technically and culturally plausible.
- `CHANGELOG.md` and `Bugs.md` show replay-contract/canonicalization bugs were fixed recently and guarded by tests, so duplicate canonicalization logic in this area is a live drift risk rather than dead code.
- `papers/these-finale-english.txt` constrains witness meaning and instantiation reconstruction (not helper ownership), so a pure extraction is thesis-safe as long as it preserves the existing source-domain replay/witness fields and only canonicalizes structural lookup fields.
- `test/CanonicalizerSpec.hs` already treats witness/trace canonicalization as a thesis-sensitive contract: canonicalize structural lookup fields while preserving source-domain provenance fields.
- `src/MLF/Constraint/Presolution/Rewrite.hs` and `src/MLF/Elab/Run/Util.hs` still each define live `canonicalizeWitness` / `canonicalizeTrace` helpers with the same semantics: canonicalize only structural lookup fields (`ewLeft`, `ewRight`, `ewRoot`, `etRoot`) while preserving source-domain provenance/replay fields.
- The duplication is live on both production paths: presolution rewrite (`Driver`) imports the rewrite copy, while runtime pipeline/tests import the `MLF.Elab.Run.Util` copy.
- The surrounding `canonicalizeExpansion` helpers are *not* identical today: presolution rewrite leaves `ExpForall` payloads untouched, while runtime canonicalizes `BoundNode` entries inside `ForallSpec`. That keeps the proposed simplification safely bounded to witness/trace helpers only.
- `MLF.Constraint.Canonicalization.Shared` is the best owner: it already hosts closely related internal canonicalization utilities and can depend on `MLF.Constraint.Presolution.Base` + `MLF.Constraint.Types.Witness` without creating an obvious import cycle.
- A focused dedup guard can reuse the pattern already present in `test/Constraint/SolvedSpec.hs`: read the consumer modules as source files and assert the witness/trace helper type signatures only live in `MLF.Constraint.Canonicalization.Shared`.
- The requested targeted test run is currently blocked by unrelated workspace changes: `cabal test ... --match "MLF.Constraint.Canonicalizer|migration guardrail: thesis-core boundary matches legacy outcome|checked-authoritative|Dual-path verification"` fails to build because `Solved.fromPreRewriteState` is no longer exported from `MLF.Constraint.Solved` while existing tests still reference it (`test/Parity/FrozenArtifacts.hs`, `test/SpecUtil.hs`).
- Reconfirmed on 2026-03-09 that the narrow `MLF.Constraint.Canonicalizer` test command is still blocked before execution by the unrelated `Solved.fromPreRewriteState` export mismatch in `test/Parity/FrozenArtifacts.hs` and `test/SpecUtil.hs`.
