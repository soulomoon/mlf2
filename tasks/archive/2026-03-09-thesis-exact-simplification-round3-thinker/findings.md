# Findings

- Session catchup reported a prior skip-check thinker prompt, but no unsynced planning-file updates needed recovery.
- The active simplification campaign already has round-specific artifacts for round 1, the orchestrator run, and a round-2 thinker; round 3 needs its own fresh proposal rather than reusing those accepted themes.
- The working tree currently contains unrelated tracked edits in `src/MLF/Constraint/Solved.hs` and `test/SolvedFacadeTestUtil.hs`; those files are off-limits for this thinker pass.
- `TODO.md` records recent simplification work as completed through tasks 58–70, so the next proposal should target a still-live, bounded simplification rather than reopening fallback-removal or solved-helper themes.
- Orchestrator notes confirm two accepted simplifications already landed in this campaign: the snapshot-preparation dedup and the `Presolution.Core` compatibility-facade retirement; user-provided accepted titles further exclude the solved-helper quarantine and the `MLF.Reify.Core` `solvedFromView` removal idea.
- `CHANGELOG.md` already records the dead flush-all delayed-weaken entrypoint as retired on 2026-03-09, so that no longer qualifies as a new simplification candidate.
- `TODO.md`/`CHANGELOG.md` show the `geRes` narrowing, compat-builder relocation, and dead solved-mutation-hook removal are all already complete; those follow-ups should not be proposed again.
- The live presolution code still exposes a thin alias layer in `MLF.Constraint.Presolution.EdgeUnify` for pending-weaken owner queries (`pendingWeakenOwnerForNode`, `pendingWeakenOwnerForEdge`, `pendingWeakenOwners`) that simply forwards into `StateAccess`/owner-local helpers and is consumed by `Driver` and `EdgeProcessing`.
- `MLF.Constraint.Presolution.Base` still carries `PendingWeakenOwnerUnknown` as a compatibility-preserving bucket; any simplification in the pending-weaken area must preserve that explicit unknown-owner semantics unless the thesis and current guards show it is dead.
- `MLF.Constraint.Presolution.EdgeUnify` still exports three pending-weaken owner query helpers (`pendingWeakenOwnerForNode`, `pendingWeakenOwnerForEdge`, `pendingWeakenOwners`) even though its module contract is edge-local unification / ω execution and the actual owner lookup primitives already live in `MLF.Constraint.Presolution.StateAccess`.
- Those helpers are thin aliases or state aggregators used only by `MLF.Constraint.Presolution.EdgeProcessing` and `MLF.Constraint.Presolution.Driver`; moving them beside `StateAccess` (or into a dedicated pending-weaken ownership helper module) would narrow `EdgeUnify` to execution-only responsibilities without changing row3 owner-boundary scheduling semantics.
- This candidate avoids the current off-limits `Solved` files and does not reuse the already accepted solved-helper or `Reify.Core solvedFromView` themes.
- Thesis impact is low and should be preserved by keeping the existing owner-boundary flush behavior, enqueue-time owner stamping, and `PendingWeakenOwnerUnknown` handling unchanged; the paper constrains χe/ω effects, not which internal module computes diagnostic owner buckets.
- The pending-weaken owner-query move is now explicitly rejected for this round and should not be proposed again from this task folder.
- After the round-5 result-type overlay cleanup, `MLF.Elab.Run.ResultType.View` still exports a dead `rtvSchemeBodyTarget` wrapper that simply forwards to `MLF.Elab.Run.Scope.schemeBodyTarget`; `rg -n '\brtvSchemeBodyTarget\b' src test` finds no external callers beyond its own definition/export.
- That wrapper is the last remaining `schemeBodyTarget` re-owner outside `MLF.Elab.Run.Scope`, which conflicts with the completed Task 58/implementation-notes rule that `Scope` should remain the single owner of scheme-target selection helpers.
- Removing that dead wrapper is bounded to one internal result-type module plus a small source guard/doc sync, and it tightens the current row-2/result-type boundary without touching any thesis-facing algorithm.
