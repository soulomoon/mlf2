# Findings

## Guidance issues found

- `AGENTS.md` lacked an explicit instruction-precedence/workspace-safety section even though this repo frequently has active uncommitted work.
- The task-management section referenced `codex-tmux-team`, but that skill/path is not present in the current session; the active parallel-work skills are `dispatching-parallel-agents` and `subagent-driven-development`.
- `tasks/readme` was too terse to act as a reliable companion guide and contained a typo (`foler`).

## Intended updates

- Make `AGENTS.md` more explicit about maintenance responsibility, precedence, workspace hygiene, and when to sync adjacent docs.
- Refresh task-management guidance to point at current skills without rewriting archival history.
- Expand `tasks/readme` so it mirrors the required task-folder layout described in `AGENTS.md`.
