# Findings

## 2026-03-26

- `orchestrator/state.json` is currently `stage: "done"` on roadmap family
  `2026-03-25-01-same-lane-retained-child-stable-visible-persistence-successor-orchestrator-roadmap`
  with `last_completed_round: "round-093"`.
- Accepted `round-083` finalized the architecture audit with
  `non-cyclic-graph = unknown`, not `keep` or `revise`.
- Accepted `round-088` selected `continue within the current architecture`
  as the strongest lawful architecture posture.
- Accepted `round-093` finalized the bounded successor decision
  `blocker debt remains within the current architecture` for the exact same
  same-lane retained-child pocket, while explicitly rejecting a
  `non-cyclic-graph` reopen on current evidence.
- The sharpest unresolved technical pressure is the exact-pocket split
  between helper-visible/internal recursive structure and authoritative public
  output collapsing to `TForall "a" Nothing (TVar "a")`.
- `.worktrees/` is already ignored, and repo-local `.codex/agents/` files are
  already tracked, so no ignore-file carve-out is required for the new
  scaffold.
- The current top-level orchestrator pointer stubs still point at the
  completed `2026-03-25-01-.../rev-003` bundle and will need to be advanced
  to the new roadmap family.
- The existing orchestrator role agents are hard-wired to the finished
  same-lane retained-child stable-visible-persistence loop and must be
  retuned for the new public-output-vs-`non-cyclic-graph` question.
- The new live roadmap family should stay narrower than a general
  automatic-iso-recursive-inference claim: it only asks whether the exact
  same-pocket public-output collapse can be repaired inside the inherited
  acyclic model or whether exact accepted evidence now specifically reopens
  `non-cyclic-graph`.
- Repo guidance requires the controller to keep `stage: "done"` while a new
  roadmap bundle still contains pending items; the decisive state transition
  for the fresh campaign is therefore the `roadmap_*` pointer advance, not a
  stage change.
- The scaffold verification contract is satisfied by state/pointer integrity
  and roadmap syntax checks; the first verification run failed only on one
  trailing-space defect in `.codex/agents/orchestrator-guider.toml`, which
  was repaired locally.
