# Round 091 Plan (`item-3` Exact Phase-6 Elaboration Resolution For The Frozen Same-Lane Retained-Child Pocket)

## Objective

Execute only roadmap item `3` and drive one bounded item-3 artifact at:
`/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/docs/plans/2026-03-25-same-lane-retained-child-stable-visible-persistence-phase-6-elaboration-resolution.md`.

This is the initial `item-3` plan for `attempt-1` with `retry: null`. The
round must resolve only the exact item-2 breakpoint for the frozen packet:

`ELet "k" (ELamAnn "x" recursiveAnn (EVar "x")) (ELet "u" (EApp (ELam "y" (EVar "y")) (EVar "k")) (EVar "u"))`

where `recursiveAnn = STMu "a" (STArrow (STVar "a") (STBase "Int"))`.

The only lawful item-3 outcomes are:

- land the smallest safe docs/code/test slice that makes this exact packet
  stop failing in `Phase 6 (elaboration)` on both `runPipelineElab` and
  `runPipelineElabChecked`, without changing family, anchor, owner-local
  frame, route, or quantified-boundary status; or
- record bounded proof that the exact elaboration gap remains blocker debt
  under the unchanged architecture and exact frozen tuple.

Current planning read: the first exact-pocket failure is emitted from
`src/MLF/Elab/Elaborate/Annotation.hs` inside `reifyInst`, where edge `3`
already has witness/trace/expansion authority but still throws
`PhiTranslatabilityError ["reifyInst: missing authoritative instantiation translation for edge 3","expansion args=[NodeId {getNodeId = 31}]]`.
The lawful next move is therefore to pin that exact edge in focused tests and
either consume the already-recorded exact-pocket authority locally or prove
that the current authority is still insufficient. This round is not a new
pipeline audit, not item-4 revalidation, and not a broader family search.

This round must not silently widen into:

- item `4` persistence revalidation or item `5` successor decision work;
- the alias-bound / base-like family;
- neighboring consumer routes;
- nested-`forall` or quantified-crossing success;
- `MLF.Elab.Inst`, `InstBot`, or `BUG-2026-03-16-001` replay repair;
- equi-recursive reasoning, cyclic structural graphs, multi-SCC search,
  second interfaces, or fallback-like recovery; or
- any architecture revision or reopened `non-cyclic-graph` argument.

## Locked Round Context

- Round id: `round-091`
- Roadmap item: `item-3`
- Stage: `plan`
- Active attempt: `attempt-1`
- Retry state: `null`
- Active branch: `codex/round-091`
- Active worktree:
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091`
- Fixed live subject: one exact same-lane retained-child pocket only:
  `boundVarTargetRoot`, one owner-local retained-child frame, route
  `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC`,
  clear-boundary only, and the exact item-2 `Phase 6` breakpoint above
- Stage mode: one bounded implementation-or-proof slice only
- Current round review feedback: none yet; no `review.md`,
  `reviews/attempt-<n>.md`, or `review-record.json` exists for `round-091`,
  so this is a full `attempt-1` plan rather than a retry delta

Current worktree state is already non-pristine. Respect existing edits and do
not revert unrelated work. In particular, leave the current modifications to
`orchestrator/state.json`, `orchestrator/roadmap.md`,
`orchestrator/retry-subloop.md`, `orchestrator/verification.md`, repo-local
guidance/docs, and the untracked roadmap/round scaffolding untouched.

## Accepted Continuity That Remains Binding

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/orchestrator/rounds/round-091/selection.md`
  is the stage authority for this round. It fixes the live subject to
  roadmap item `3` only, preserves the exact frozen tuple, and limits the
  lawful output to clearing or confirming the exact Phase-6 breakpoint.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/orchestrator/state.json`
  fixes the live controller state at `active_round_id = round-091`,
  `stage = plan`, `current_task = item-3`, `retry = null`,
  `roadmap_id = 2026-03-25-01-same-lane-retained-child-stable-visible-persistence-successor-orchestrator-roadmap`,
  `roadmap_revision = rev-003`, and the authoritative
  `roadmap_dir = orchestrator/roadmaps/2026-03-25-01-same-lane-retained-child-stable-visible-persistence-successor-orchestrator-roadmap/rev-003`.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/orchestrator/roadmaps/2026-03-25-01-same-lane-retained-child-stable-visible-persistence-successor-orchestrator-roadmap/rev-003/roadmap.md`
  makes item `3` the first unfinished dependency-satisfied item and defines
  its completion notes as one minimum bounded docs/code/test slice or one
  bounded blocker proof for the exact Phase-6 elaboration gap only.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/docs/plans/2026-03-25-same-lane-retained-child-stable-visible-persistence-case-and-review-ledger.md`
  freezes the exact tuple that must remain unchanged throughout this round:
  same-lane retained-child family, `boundVarTargetRoot`, one owner-local
  retained-child frame, route
  `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC`,
  clear-boundary only, and output-surface visibility obligations.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/docs/plans/2026-03-25-same-lane-retained-child-stable-visible-persistence-breakpoint-audit.md`
  localizes the first exact-pocket break to `Phase 6 (elaboration)` and
  names the precise failure text for edge `3`. Item `3` must resolve or
  confirm that exact break, not re-audit earlier solver evidence and not
  credit later rows.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  preserves the inherited boundary: explicit-only recursive behavior,
  iso-recursive meaning only, no equi-recursive reasoning, no cyclic
  structural graphs, no second interface, and no fallback widening.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/docs/plans/2026-03-24-automatic-iso-recursive-bound-var-target-same-lane-retained-child-next-cycle-decision-gate.md`
  remains the exact predecessor packet for this bounded lane. Neighboring
  routes and nested-`forall` success remain out of scope.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md`,
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`,
  and
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architecture-decision-and-successor-plan-choice.md`
  keep the same-lane retained-child pocket bounded as the strongest admitted
  candidate but still below accepted `stable visible persistence`. Item `3`
  may only clear the exact Phase-6 gate for this pocket; it may not classify
  later phases or decide the successor.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/orchestrator/roadmaps/2026-03-25-01-same-lane-retained-child-stable-visible-persistence-successor-orchestrator-roadmap/rev-003/verification.md`
  requires a focused exact-pocket check plus the `ARI-C1` rerun and full repo
  gate whenever this round lands code/test changes on the selected path.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/Bugs.md`
  still lists open `BUG-2026-03-16-001`, but that replay / `InstBot` defect
  remains predecessor context only and does not authorize widening this round
  into replay repair or a different live subject.

## File Map

### Create

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/docs/plans/2026-03-25-same-lane-retained-child-stable-visible-persistence-phase-6-elaboration-resolution.md`
  - Responsibility: canonical item-3 artifact recording exactly one bounded
    read for the frozen packet: either the exact code/test/doc slice that
    clears the Phase-6 breakpoint or the exact blocker proof that confirms
    the breakpoint remains debt under the unchanged architecture.

### Modify Only If The Exact Bounded Repair Exists

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/src/MLF/Elab/Elaborate/Annotation.hs`
  - Responsibility: keep `reifyInst` fail-closed, but allow the exact
    edge-`3` authoritative translation to come from already-recorded
    same-pocket authority if that authority is sufficient without widening.

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/src/MLF/Elab/Legacy.hs`
  - Responsibility: factor or expose the smallest view-native no-fallback
    `Expansion -> Instantiation` helper needed by `reifyInst`; do not turn
    this into a general compatibility or fallback surface.

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/test/ElaborationSpec.hs`
  - Responsibility: add one direct exact-edge regression proving what
    authoritative translation edge `3` does or does not support for this
    exact packet.

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/test/PipelineSpec.hs`
  - Responsibility: add or refresh the exact frozen-packet Phase-6 gate so
    the public pipeline proves that this exact packet either now survives
    elaboration or still fails for the same exact bounded reason.

### Read-Only Evidence

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/orchestrator/rounds/round-091/selection.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/orchestrator/verification.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/orchestrator/retry-subloop.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/orchestrator/state.json`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/orchestrator/roadmaps/2026-03-25-01-same-lane-retained-child-stable-visible-persistence-successor-orchestrator-roadmap/rev-003/roadmap.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/orchestrator/roadmaps/2026-03-25-01-same-lane-retained-child-stable-visible-persistence-successor-orchestrator-roadmap/rev-003/verification.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/orchestrator/roadmaps/2026-03-25-01-same-lane-retained-child-stable-visible-persistence-successor-orchestrator-roadmap/rev-003/retry-subloop.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/docs/plans/2026-03-24-automatic-iso-recursive-bound-var-target-same-lane-retained-child-next-cycle-decision-gate.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architecture-decision-and-successor-plan-choice.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/docs/plans/2026-03-25-same-lane-retained-child-stable-visible-persistence-case-and-review-ledger.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/docs/plans/2026-03-25-same-lane-retained-child-stable-visible-persistence-breakpoint-audit.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/src/MLF/Elab/Run/Pipeline.hs`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/Bugs.md`

### Preserve Unchanged

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/orchestrator/state.json`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/orchestrator/roadmap.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/orchestrator/retry-subloop.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/orchestrator/verification.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/Bugs.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/src/MLF/Elab/Run/ResultType/Fallback.hs`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/src/MLF/Elab/Inst.hs`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/src/MLF/Elab/Phi/Omega/Interpret.hs`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/src-public/`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/app/`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-091/mlf2.cabal`
- reviewer-owned history under earlier round directories

## Exact Selected Slice (Exactly One)

The only selected item-3 slice is:

resolve the exact edge-`3` Phase-6 elaboration translation gap for the frozen
same-lane retained-child packet, and nothing else.

This slice is allowed to:

- inspect the exact annotation-edge authority already carried by the frozen
  packet;
- add one exact-pocket public-pipeline regression and one exact-pocket
  elaboration-level regression;
- land one local no-fallback translation path if the current same-pocket
  `Expansion`, witness, trace, and scheme information are already sufficient;
  or
- fail closed with blocker proof if that local path is not lawful.

This slice is not allowed to:

- rerun or reinterpret item-2 as a broader audit;
- claim item-4 persistence classification or item-5 successor choice;
- change candidate search, owner/binder interpretation, route selection, or
  quantified-boundary status;
- move into alias-bound, neighboring-route, or nested-`forall` families;
- reopen replay repair, `InstBot`, or `BUG-2026-03-16-001`;
- introduce fallback-like rescue, second interfaces, cyclic reasoning, or
  multi-SCC search; or
- keep partial runtime edits that still do not clear the exact Phase-6 gate.

## Sequential Tasks

### Task 1 - Freeze the exact edge-3 item-3 contract before changing behavior

- Create the canonical item-3 artifact at the path above.
- State explicitly that this round is `attempt-1` with `retry: null` and that
  the frozen tuple from item `1` and item `2` remains unchanged.
- Copy the exact frozen packet, the exact item-2 error text, and the exact
  success criterion for this round:
  the same packet must stop failing in `Phase 6 (elaboration)` on both
  `runPipelineElab` and `runPipelineElabChecked`, without family/anchor/frame/
  route/boundary drift.
- Copy the exact fail-closed criterion for this round:
  if the current same-pocket authority still cannot produce a lawful
  authoritative translation for edge `3`, record blocker proof and stop.
- State explicitly that item `3` ends once the exact Phase-6 question is
  resolved one way or the other; it does not continue into item-4 ledger
  classification.

### Task 2 - Add exact red tests for the frozen packet only

- In `test/PipelineSpec.hs`, add one focused spec for the exact frozen packet
  that expresses the item-3 target directly:
  both `runPipelineElab` and `runPipelineElabChecked` should survive the
  exact packet without the item-2 `Phase 6 (elaboration)` failure.
- Keep the existing helper-visible `containsMu True` retained-child fallback
  test intact as bounded context only. Do not treat that helper-only row as a
  substitute for the public pipeline gate.
- In `test/ElaborationSpec.hs`, add one direct exact-edge spec that
  reconstructs the frozen packet through `runPipelineArtifactsDefault`,
  looks up the exact annotation edge `3`, and pins the same-pocket authority
  under review:
  the edge has the exact `ExpInstantiate [NodeId 31]` expansion and its
  authoritative translation must either become concrete or remain explicitly
  non-translatable for this exact packet.
- Write these as red tests first. Do not add any second packet, neighboring
  route, or alias-bound comparison as part of the item-3 slice.

### Task 3 - Land only the smallest lawful code path that consumes existing same-pocket authority

- Preferred code path:
  keep the repair local to
  `src/MLF/Elab/Elaborate/Annotation.hs` and
  `src/MLF/Elab/Legacy.hs`.
- First, factor the smallest view-native no-fallback helper out of
  `expansionToInst` so `reifyInst` can translate a canonicalized
  `ExpInstantiate` using the current `PresolutionView` / named-set authority
  already carried by the exact pocket, without threading new pipeline state
  or introducing a fallback surface.
- Then, in `reifyInst`, keep the existing hard-fail behavior by default, but
  when the exact authoritative-refinement branch has:
  `mExpansion = Just (ExpInstantiate args)`,
  `shouldRefine = True`,
  `schemeArity > 0`, and no inferred/trace authoritative args, try the new
  exact-pocket expansion translation before raising
  `PhiTranslatabilityError`.
- Only accept that translation if it stays within the same exact edge story:
  no guessed binders, no neighboring route lookup, no replay repair, and no
  family/anchor/frame/boundary drift.
- If the smallest local translation still cannot make the exact red tests go
  green, stop. Do not escalate into `MLF.Elab.Inst`, replay-map surgery,
  `Fallback.hs`, candidate-search rewrites, or pipeline-stage changes.

### Task 4 - Resolve the round in one bounded branch only

- Clearance branch:
  - keep the final diff limited to the canonical item-3 artifact plus the
    exact code/test files named above;
  - update the item-3 artifact with the exact edge-`3` authority consumed,
    the exact tests/commands that now pass, and the bounded statement that
    item `3` cleared the exact Phase-6 breakpoint for this frozen packet
    only; and
  - stop without item-4 persistence classification.
- Blocker-proof branch:
  - if the exact packet still cannot clear `Phase 6 (elaboration)` without
    widening beyond the files/scope above, do not keep partial runtime/test
    edits;
  - finalize the canonical item-3 artifact as blocker proof only, recording
    the exact authority inspected, the exact failing command/output, and why
    the unchanged architecture still leaves the exact edge-`3` gap as debt;
    and
  - state explicitly that item `4` remains blocked on the same frozen pocket
    and that no alias-bound or architecture-revision work became authorized.

### Task 5 - Verification

If the bounded repair lands code/test changes, run:

- `cabal test mlf2-test --test-show-details=direct --test-options='--match "same-lane retained-child exact packet clears Phase 6 elaboration"'`
- `cabal test mlf2-test --test-show-details=direct --test-options='--match "same-lane retained-child exact edge 3 authoritative instantiation"'`
- `cabal test mlf2-test --test-show-details=direct --test-options='--match "ARI-C1 feasibility characterization (bounded prototype-only)"'`
- `cabal build all && cabal test`
- `git diff --name-only -- src test src-public app mlf2.cabal`
  and confirm the only code/test paths listed are:
  `src/MLF/Elab/Elaborate/Annotation.hs`,
  `src/MLF/Elab/Legacy.hs`,
  `test/ElaborationSpec.hs`,
  and `test/PipelineSpec.hs`
- `git diff --name-only -- orchestrator/state.json orchestrator/roadmap.md orchestrator/retry-subloop.md orchestrator/verification.md Bugs.md`
  and confirm it is empty
- `rg -n 'Phase 6 \\(elaboration\\)|edge 3|NodeId 31|cleared the exact Phase-6 breakpoint|item 4 remains later work only' docs/plans/2026-03-25-same-lane-retained-child-stable-visible-persistence-phase-6-elaboration-resolution.md`

If the round finalizes as blocker proof only, run:

- `git diff --check`
- `python3 -m json.tool orchestrator/state.json >/dev/null`
- `roadmap_dir="$(jq -r '.roadmap_dir' orchestrator/state.json)" && test -f "$roadmap_dir/roadmap.md" && test -f "$roadmap_dir/retry-subloop.md" && test -f "$roadmap_dir/verification.md"`
- the exact frozen-packet replay command already frozen in the item-2 audit,
  and confirm it still reproduces:
  `Phase 6 (elaboration): PhiTranslatabilityError ["reifyInst: missing authoritative instantiation translation for edge 3","expansion args=[NodeId {getNodeId = 31}]]`
- `git diff --name-only -- src test src-public app mlf2.cabal`
  and confirm it is empty
- `git diff --name-only -- orchestrator/state.json orchestrator/roadmap.md orchestrator/retry-subloop.md orchestrator/verification.md Bugs.md`
  and confirm it is empty
- `rg -n 'Phase 6 \\(elaboration\\)|edge 3|NodeId 31|blocker debt|item 4 remains blocked' docs/plans/2026-03-25-same-lane-retained-child-stable-visible-persistence-phase-6-elaboration-resolution.md`

In both branches, reviewer-facing evidence must stay bounded to the exact
frozen packet and must not claim reconstruction/public persistence, nested
`forall` success, alias-bound progress, or architecture change.
