# Round 071 Plan (`N4` Exact Bounded Target Bind)

## Objective

Execute only roadmap item `N4` and prepare one docs-only exact-target-bind
artifact at:
`/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/docs/plans/2026-03-22-automatic-iso-recursive-base-target-exact-target-bind.md`.

This is the initial `N4` plan for `attempt-1` with `retry: null`. The round
must treat accepted `L1`, `L2`, `N1`, `N2`, and `N3` as binding predecessor
continuity, stay inside the selected preserved generic scheme-alias /
base-like `baseTarget` planning subject, and bind exactly one docs-only
target that satisfies the accepted `N3` contract without authorizing `N5`,
`N6`, or `N7`.

Current planning read: the only lawful `N4` target is the preserved generic
non-local scheme-root alias-bound / base-like `baseTarget -> baseC` packet
already exercised by `schemeAliasBaseLikeFallback False`, together with its
downstream generic `targetC` selection use
`Just baseC | rootIsSchemeAlias && rootBoundIsBaseLike -> baseC`. The docs
artifact must bind that one packet only:

- owner binder: the scheme-root alias binder behind the extracted `AVar` body
  in `schemeAliasBaseLikeFallback`;
- owned bound: the canonical base-like bound reattached to that root via
  `rebindRootTo`, currently realized by the `Int` base node in the focused
  fixture;
- exact translation step under study: the existing generic scheme-alias /
  base-like `baseTarget` computation and the same-lane `targetC` consumption
  that selects `baseC`;
- inverse-translation-safe bound-inlining story: this one packet only, using
  thesis-backed alias/inert-bound inlining constraints rather than a broader
  solver-wide claim; and
- structural / variance evidence for binding-flag reconstruction: reviewer-
  visible reasoning for this one packet only, not a general reopening of
  recursive inference.

This round must not reopen the accepted local
`rootLocalSchemeAliasBaseLike` `keepTargetFinal` / `targetC -> rootFinal`
lane, the accepted local empty-candidate
`rootLocalEmptyCandidateSchemeAliasBaseLike` `baseTarget -> baseC` lane, the
accepted local single-base or inst-arg-only lanes, any multi-inst family,
`boundVarTarget`, `boundTarget`, `schemeBodyTarget`, repaired `URI-R2-C1`,
replay reopen, `MLF.Elab.Inst`, `InstBot`, or any other blocked route.

## Locked Round Context

- Round id: `round-071`
- Roadmap item: `N4`
- Stage: `plan`
- Active attempt: `attempt-1`
- Retry state: `null`
- Fixed live subject: preserved generic scheme-alias / base-like `baseTarget`
  planning subject
- Active branch: `codex/round-071-n4-exact-target-bind`
- Active worktree:
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071`
- Stage mode: docs-only exact-target bind only
- Current round review feedback: none yet; this is a full `attempt-1` plan,
  not a retry delta

Current round worktree state is already non-pristine. Respect existing edits
and do not revert unrelated work:

- `?? orchestrator/rounds/round-071/selection.md`

## Accepted Continuity That Remains Binding

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/orchestrator/rounds/round-071/selection.md`
  already fixes this round to roadmap item `N4` only and explicitly forbids
  using this round for `N5` through `N7`, implementation, verification,
  roadmap/state edits, bug-tracker edits, or predecessor-history rewrites.
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/state.json` fixes the
  live controller state at `active_round_id: "round-071"`, `stage: "plan"`,
  `current_task: "N4"`, `branch:
  "codex/round-071-n4-exact-target-bind"`, and `retry: null`.
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/roadmap.md` makes
  `N4` the first pending item after accepted `N3`; every later item depends on
  the exact target that `N4` freezes and therefore cannot run first.
- `/Users/ares/.codex/worktrees/d432/mlf4/tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
  still records `N4 = NO` because the reopened loop has not yet frozen one
  exact bounded target inside the selected `baseTarget` subject.
- `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md`
  remains binding predecessor evidence that repaired `URI-R2-C1` had no fresh
  lawful exact successor slice inside the inherited boundary, and that the
  remaining generic scheme-alias / base-like `baseTarget` route was preserved
  only as broader future context.
- `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-21-uri-r2-c1-l2-post-l1-fail-closed-successor-decision-gate.md`
  remains binding predecessor evidence that repaired `URI-R2-C1` finalized as
  `stop-blocked` and that any later work on the preserved generic
  scheme-alias / base-like `baseTarget` route first required a separate
  roadmap amendment plus a fresh selection.
- `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-22-automatic-iso-recursive-post-l2-roadmap-amendment-authority-gate.md`
  remains binding predecessor evidence that the only lawful post-`L2`
  authority outcome is `reopen-planning-only`, preserving the inherited
  explicit-only / non-equi-recursive / non-cyclic-graph /
  no-second-interface / no-fallback boundary unchanged and keeping
  implementation plus verification blocked.
- `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-22-automatic-iso-recursive-next-live-subject-selection.md`
  remains binding predecessor evidence that the only selected live subject is
  the preserved generic scheme-alias / base-like `baseTarget` route and that
  this subject remains planning-only until `N4` binds one exact bounded
  target.
- `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-22-automatic-iso-recursive-base-target-safety-acceptance-contract.md`
  remains binding predecessor evidence that any later `N4` target is lawful
  only if it names one bounded owner-binder / owned-bound packet, one exact
  edge or translation step, one inverse-translation-safe bound-inlining
  story, one structural / variance explanation for binding-flag
  reconstruction, and one explicit out-of-scope boundary.
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/retry-subloop.md`
  allows retries for `N4`, but it also makes prior attempts immutable. This
  `attempt-1` plan must therefore create one new bounded bind artifact without
  rewriting any prior attempt or review artifact.
- `/Users/ares/.codex/worktrees/d432/mlf4/Bugs.md` currently carries open
  `BUG-2026-03-16-001`, but that replay / `InstBot` defect remains read-only
  predecessor context only. It does not reopen replay, `MLF.Elab.Inst`, or
  any repaired-queue family in this round.

## File Map

### Modify

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/docs/plans/2026-03-22-automatic-iso-recursive-base-target-exact-target-bind.md`
  - Responsibility: record the `N4` docs-only exact-target bind for the
    selected preserved generic scheme-alias / base-like `baseTarget` planning
    subject, freezing one bounded owner-binder / owned-bound packet and its
    same-lane `targetC` use while keeping every other route blocked.

### Read-Only Evidence

- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/roadmap.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/state.json`
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/verification.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/retry-subloop.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-21-uri-r2-c1-l2-post-l1-fail-closed-successor-decision-gate.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-22-automatic-iso-recursive-post-l2-roadmap-amendment-authority-gate.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-22-automatic-iso-recursive-next-live-subject-selection.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-22-automatic-iso-recursive-base-target-safety-acceptance-contract.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/Bugs.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/papers/these-finale-english.txt`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/orchestrator/rounds/round-071/selection.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/src/MLF/Elab/Run/ResultType/Fallback.hs`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/test/PipelineSpec.hs`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/src/MLF/Frontend/Normalize.hs`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/src/MLF/Elab/Run/Scope.hs`

### Preserve Unchanged

- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/roadmap.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/state.json`
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/retry-subloop.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/verification.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/orchestrator/rounds/round-071/selection.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/Bugs.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/src/`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/src-public/`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/app/`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/test/`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/mlf2.cabal`

## Exact Frozen `N4` Target (Exactly One)

The only frozen `N4` target is:

non-local preserved generic scheme-root alias-bound / base-like
`baseTarget -> baseC` bind in
`/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/src/MLF/Elab/Run/ResultType/Fallback.hs`,
limited to the existing generic scheme-alias / base-like `baseTarget`
computation at `Fallback.hs:367-408` and the downstream same-lane `targetC`
consumption at `Fallback.hs:700-710`, as exercised by
`schemeAliasBaseLikeFallback False` in
`/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/test/PipelineSpec.hs:1244-1269`
and
`/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/test/PipelineSpec.hs:1622-1625`.

Required interpretation of that one bounded target:

- owner binder: the scheme-root alias binder behind the extracted `AVar` body
  in `schemeAliasBaseLikeFallback`, not the already-accepted local
  `rootLocalSchemeAliasBaseLike` retained-final lane;
- owned bound: the canonical base-like bound reattached to `rootNid` via
  `rebindRootTo`, currently materialized by the focused `Int` base node in the
  existing non-local helper packet;
- same-lane selection use: only the downstream generic
  `Just baseC | rootIsSchemeAlias && rootBoundIsBaseLike -> baseC` `targetC`
  arm, not `keepTargetFinal`, not `rootFinal`, and not any
  `schemeBodyTarget` fallback;
- inverse-translation-safe bound-inlining story: only the thesis-backed
  alias/inert-bound packet for this one scheme-root owner / bound pair, using
  `8.3.1 Inlining bounds` plus `15.6.2 Expressivity of alias bounds` as the
  admissibility language; and
- structural / variance evidence for binding-flag reconstruction: only the
  reviewer-visible reasoning needed to show that this one packet can be
  narrated without heuristic repair, replay fallback, or any broader
  recursive-inference claim.

Everything else remains out of scope, including:

- the accepted local `rootLocalSchemeAliasBaseLike`
  `keepTargetFinal` / `targetC -> rootFinal` lane exercised by
  `schemeAliasBaseLikeFallback True`;
- the accepted local empty-candidate
  `rootLocalEmptyCandidateSchemeAliasBaseLike` `baseTarget -> baseC` lane
  exercised by `localEmptyCandidateSchemeAliasBaseLikeFallback True`;
- the accepted local single-base and inst-arg-only singleton-base
  `baseTarget` lanes;
- `rootLocalMultiInst`, `rootLocalInstArgMultiBase`, `boundVarTarget`,
  `boundTarget`, and `schemeBodyTarget`;
- repaired `URI-R2-C1`, replay reopen, `MLF.Elab.Inst`, `InstBot`, every
  other fallback family, every different solver/pipeline subject,
  equi-recursive reasoning, implicit unfolding, cyclic graphs, graph-cycle
  exceptions, multi-SCC support, second-interface work, and fallback
  widening.

## Sequential Tasks

### Task 1 - Re-establish exact `N4` authority and docs-only bind boundary

- Write the future canonical `N4` artifact as `attempt-1` with `retry: null`.
- State explicitly that `N4` is docs-only exact-target-bind work inside
  accepted `N1 = reopen-planning-only`, accepted
  `N2 = baseTarget-planning-subject-selected`, and accepted `N3` safety
  contract.
- Preserve the inherited explicit-only / non-equi-recursive /
  non-cyclic-graph / no-second-interface / no-fallback boundary unchanged.
- State explicitly that `N4` does not authorize `N5`, `N6`, or `N7`,
  implementation, verification, merge action, roadmap edits,
  controller-state edits, bug-tracker edits, or predecessor-history rewrites.

### Task 2 - Reconstruct the exact admissible packet and freeze the one lawful target

- Use `selection.md`, `roadmap.md`, `mechanism_table.md`, accepted `L1`,
  accepted `L2`, accepted `N1`, accepted `N2`, accepted `N3`, and
  `Bugs.md` to restate the exact packet that now constrains `N4`.
- Reconstruct the read-only code/test anchors that distinguish the selected
  packet from already-accepted same-family lanes:
  - `Fallback.hs:367-408` for the generic `baseTarget` computation;
  - `Fallback.hs:521-548` for the named local proof cluster that already owns
    the accepted local lanes;
  - `Fallback.hs:693-728` for the `keepTargetFinal` and `targetC` ordering;
  - `PipelineSpec.hs:1244-1269` for the `schemeAliasBaseLikeFallback` helper;
  - `PipelineSpec.hs:1594-1606` for the accepted local empty-candidate and
    accepted local retained-final contrasts; and
  - `PipelineSpec.hs:1622-1625` for the preserved non-local generic
    scheme-alias / base-like `baseTarget` packet.
- Use those anchors to prove one exact narrowing only:
  - the selected target is the preserved non-local generic scheme-root
    alias-bound / base-like `baseTarget -> baseC` packet exercised by
    `schemeAliasBaseLikeFallback False`;
  - the same-lane selection use is the downstream generic `targetC` arm that
    consumes that `baseTarget` packet; and
  - the accepted local `rootLocalSchemeAliasBaseLike`,
    `rootLocalEmptyCandidateSchemeAliasBaseLike`,
    `rootLocalSingleBase`, and `rootLocalInstArgSingleBase` packets remain
    immutable predecessor evidence only.

### Task 3 - Write the canonical `N4` artifact as one bounded bind

- Create
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/docs/plans/2026-03-22-automatic-iso-recursive-base-target-exact-target-bind.md`.
- The artifact must include:
  - the stage contract freeze for `round-071` / `N4` / `attempt-1` /
    `retry: null`;
  - accepted `L1` / `L2` / `N1` / `N2` / `N3` continuity as binding input;
  - the exact one-target freeze from the section above;
  - a reviewer-visible owner-binder / owned-bound statement;
  - the exact edge or translation step under study;
  - the inverse-translation-safe bound-inlining story for this packet only;
  - the structural / variance explanation for binding-flag reconstruction for
    this packet only; and
  - an explicit out-of-scope section that keeps every blocked route blocked.
- Current planning read for the artifact:
  - bind the non-local scheme-root alias-bound / base-like packet exercised by
    `schemeAliasBaseLikeFallback False`, not the already-accepted local
    `rootFinal` continuity lane;
  - describe the owned bound as the canonical base-like bound reattached by
    `rebindRootTo`, currently realized by the `Int` base node in the focused
    helper packet;
  - describe the exact translation step as the existing generic
    `rootIsSchemeAlias && rootBoundIsBaseLike` `baseTarget` / `targetC`
    selection, not any neighboring local proof arm; and
  - keep the narrative docs-only: admissibility is bound only for this target,
    not as implementation readiness and not as verification clearance.

### Task 4 - Keep non-authorization and same-family exclusions explicit

- State explicitly that `N4` does not authorize:
  - `N5`, `N6`, or `N7`;
  - implementation or verification;
  - merge action, roadmap edits, controller-state edits, or bug-tracker edits;
  - replay reopen, `MLF.Elab.Inst`, or `InstBot`; or
  - predecessor-history rewrites.
- State explicitly that `N4` does not reopen or select:
  - repaired `URI-R2-C1` as live work;
  - the accepted local `rootLocalSchemeAliasBaseLike`
    `keepTargetFinal` / `targetC -> rootFinal` lane;
  - the accepted local
    `rootLocalEmptyCandidateSchemeAliasBaseLike` `baseTarget -> baseC` lane;
  - the accepted local single-base and inst-arg-only singleton-base lanes;
  - `rootLocalMultiInst`, `rootLocalInstArgMultiBase`, `boundVarTarget`,
    `boundTarget`, `schemeBodyTarget`, or
    `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/src/MLF/Elab/Run/ResultType/View.hs`;
  - any other fallback family or any other solver/pipeline subject;
  - equi-recursive reasoning, implicit unfolding, cyclic structural graph
    encoding, graph-cycle exceptions, multi-SCC reasoning, cross-family
    search, second-interface work, or fallback widening.
- State explicitly that prior attempts and reviewer-owned history remain
  immutable under `orchestrator/retry-subloop.md`.

### Task 5 - Run the docs-only verification required for `N4`

Run the baseline docs/state checks required by
`/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/verification.md`:

- `git diff --check`
- `python3 -m json.tool orchestrator/state.json >/dev/null`
- `rg -n '"contract_version": 2|"retry": null|"retry": \{' orchestrator/state.json`
- `rg -n '^\d+\. \[(pending|in-progress|done)\]' orchestrator/roadmap.md`
- `test -f docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
- `test -f docs/plans/2026-03-14-unannotated-iso-recursive-r5-research-stop-decision.md`
- `test -f docs/plans/2026-03-17-uri-r2-c1-r4-repair-decision-gate.md`
- `test -f docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md`
- `test -f docs/plans/2026-03-21-uri-r2-c1-l2-post-l1-fail-closed-successor-decision-gate.md`
- `test -f tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
- `test -f orchestrator/retry-subloop.md`

Run `N4`-specific continuity, thesis, and anchor checks:

- `rg -n '^4\\. \\[pending\\] Execute the `N4` exact bounded target bind for the reopened loop' orchestrator/roadmap.md`
- `rg -n 'N4 — Exact bounded target bind for the reopened loop|bind one exact bounded slice before any implementation begins' tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
- `rg -n 'no fresh lawful exact successor slice|generic scheme-alias / base-like `baseTarget` route' docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md`
- `rg -n 'stop-blocked|blocked future work only|separate roadmap amendment|fresh selection' docs/plans/2026-03-21-uri-r2-c1-l2-post-l1-fail-closed-successor-decision-gate.md`
- `rg -n 'reopen-planning-only|implementation and verification blocked|baseTarget route becomes admissible' docs/plans/2026-03-22-automatic-iso-recursive-post-l2-roadmap-amendment-authority-gate.md`
- `rg -n 'baseTarget route|docs-first alias-bound / bound-inlining / binding-structure planning subject|N4 must still bind one exact bounded target' docs/plans/2026-03-22-automatic-iso-recursive-next-live-subject-selection.md`
- `rg -n 'Acceptance criteria for any later `N4` target|owner binder|owned bound|binding-flag reconstruction|No-go And Blocked-Boundary Record' docs/plans/2026-03-22-automatic-iso-recursive-base-target-safety-acceptance-contract.md`
- `rg -n '8\\.3\\.1 Inlining bounds|we inline the bounds which can be rebuilt unambiguously|reconstruct the binder and the binding flag|usedtwice|structuralancestor' papers/these-finale-english.txt`
- `rg -n '15\\.6\\.2 Expressivity of alias bounds|special care must be taken|entirely inline all inert bounds|forbid alias bounds entirely|cannot be expressed at all' papers/these-finale-english.txt`
- `rg -n '17\\.3 Perspectives|recursive types and second-order polymorphism alone is already tricky|Allowing cyclic term-graphs|main difficulties likely lie in the treatment of recursion in the binding structure' papers/these-finale-english.txt`
- `nl -ba src/MLF/Elab/Run/ResultType/Fallback.hs | sed -n '367,408p'`
- `nl -ba src/MLF/Elab/Run/ResultType/Fallback.hs | sed -n '521,548p'`
- `nl -ba src/MLF/Elab/Run/ResultType/Fallback.hs | sed -n '693,728p'`
- `nl -ba test/PipelineSpec.hs | sed -n '1244,1269p'`
- `nl -ba test/PipelineSpec.hs | sed -n '1594,1625p'`
- `nl -ba test/PipelineSpec.hs | sed -n '1698,1762p'`
- `nl -ba src/MLF/Frontend/Normalize.hs | sed -n '20,36p'`
- `nl -ba src/MLF/Elab/Run/Scope.hs | sed -n '127,160p'`
- `rg -n 'Attempt: `attempt-1`|Retry state: `null`|schemeAliasBaseLikeFallback|owner binder|owned bound|rootIsSchemeAlias && rootBoundIsBaseLike|rootLocalSchemeAliasBaseLike|rootLocalEmptyCandidateSchemeAliasBaseLike|binding-flag reconstruction|out-of-scope' docs/plans/2026-03-22-automatic-iso-recursive-base-target-exact-target-bind.md`

- `cabal build all && cabal test` is intentionally out of scope for this round
  if the diff remains docs-only and does not touch `src/`, `src-public/`,
  `app/`, `test/`, or `mlf2.cabal`. The implementation notes and reviewer
  record must state that skip reason explicitly rather than implying future
  code clearance.

## Completion Criteria

This plan is complete only if the implementation stage stays inside all of the
following limits:

1. The canonical `N4` artifact is created at the planned path and records
   `attempt-1` as a docs-only exact-target-bind stage.
2. The artifact preserves accepted `L1`, `L2`, `N1`, `N2`, and `N3` as
   binding predecessor continuity and does not relitigate closed repaired-queue
   history.
3. The artifact freezes exactly one bounded target:
   the preserved non-local generic scheme-root alias-bound / base-like
   `baseTarget -> baseC` packet exercised by
   `schemeAliasBaseLikeFallback False`.
4. The artifact freezes exactly one same-lane selection use for that target:
   the downstream generic `rootIsSchemeAlias && rootBoundIsBaseLike`
   `targetC` arm that consumes `baseC`.
5. The artifact names the owner binder, owned bound, exact translation step,
   inverse-translation-safe bound-inlining story, and structural / variance
   evidence for binding-flag reconstruction for that one packet only.
6. The artifact keeps the accepted local scheme-alias / base-like lanes,
   repaired `URI-R2-C1`, replay reopen, `MLF.Elab.Inst`, `InstBot`,
   `boundVarTarget`, `boundTarget`, `schemeBodyTarget`, every other fallback
   family, every different solver/pipeline subject, and every broader
   recursive-inference route explicitly out of scope.
7. No production/test/public-API/executable/Cabal/controller-state/roadmap/
   bug-tracker edit occurs, and no implementation or verification readiness is
   claimed.

## Reviewer Handoff

Reviewer should confirm that the implemented `N4` artifact:

- is explicitly framed as `round-071` / `N4` / `attempt-1` / `retry: null`;
- treats accepted `L1`, `L2`, `N1`, `N2`, and `N3` as binding predecessor
  continuity;
- freezes exactly one bounded target and one same-lane selection use;
- binds the preserved non-local generic scheme-root alias-bound / base-like
  `baseTarget -> baseC` packet rather than any already-accepted local lane;
- names the owner binder, owned bound, exact translation step,
  inverse-translation-safe bound-inlining story, and structural / variance
  evidence for binding-flag reconstruction for that exact packet only;
- explicitly keeps repaired `URI-R2-C1`, replay reopen, `MLF.Elab.Inst`,
  `InstBot`, `boundVarTarget`, `boundTarget`, `schemeBodyTarget`, every other
  fallback family, every different solver/pipeline subject, equi-recursive
  reasoning, implicit unfolding, cyclic graphs, second-interface work, and
  fallback widening blocked; and
- leaves `orchestrator/roadmap.md`, `orchestrator/state.json`, `Bugs.md`, and
  prior attempt/review history unchanged.
