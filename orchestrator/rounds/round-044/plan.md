# Round 044 Plan (`F3` Bounded Verification Gate)

## Objective

Execute only roadmap item `F3` and produce one accepted docs/evidence artifact at:
`/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-19-uri-r2-c1-f3-bounded-verification-gate.md`.

This is the initial `F3` plan for `attempt-1` with `retry: null`. The round must
freeze exactly one docs-only verification/evidence slice over the accepted `F2`
local-binding scheme-alias/base-like `keepTargetFinal` / `targetC`
implementation under repaired `URI-R2-C1`.

`F3` must verify that accepted `F2` slice against all three required evidence
surfaces:

1. a fresh rerun of the focused
   `ARI-C1 feasibility characterization (bounded prototype-only)` block in
   `/Users/ares/.codex/worktrees/d432/mlf4/test/PipelineSpec.hs`, including the
   local scheme-alias/base-like local-`TypeRef` success example, the matched
   non-local fail-closed contrast, the source guard for the local-binding gate,
   and the inherited bounded controls that remain in scope;
2. a fresh full repo gate via `cabal build all && cabal test`;
3. predecessor continuity checks anchored to the accepted `F2` record:
   `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-19-uri-r2-c1-f2-bounded-implementation-slice.md`
   plus
   `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/rounds/round-043/review-record.json`.

`attempt-1` remains docs-only. No production, test, public API, executable,
Cabal, roadmap, bug-tracker, or controller-state edits are planned or
authorized in this round. If any verification step fails, capture the blocker in
the canonical `F3` artifact and optional round notes; do not patch
`Fallback.hs`, `PipelineSpec.hs`, or any other code/test file inside this
verification gate.

## Locked Round Context

- Round id: `round-044`
- Roadmap item: `F3`
- Stage: `plan`
- Active attempt: `attempt-1`
- Retry state: `null`
- Fixed live subject: repaired `URI-R2-C1`
- Fixed inherited boundary:
  `explicit-only / non-equi-recursive / non-cyclic-graph / no second interface / no fallback widening`
- Stage mode: docs-only bounded verification/evidence consolidation only

Accepted carry-forward facts that must remain binding throughout `F3`:

- `E3` remains the verification model for this cycle: focused bounded-block
  rerun, fresh full-repo gate, predecessor continuity recheck, and docs-only
  diff discipline.
- `E4` authoritative `attempt-1` finalized `continue-bounded`, so the next
  lawful action after the accepted `E3` record was one more bounded non-widening
  cycle rather than replay reopen or broader widening.
- `F1` authoritative `round-042` froze exactly one future `F2` target: the
  adjacent local-binding scheme-alias/base-like `keepTargetFinal` / `targetC`
  lane in `Fallback.hs`, with future ownership limited to `Fallback.hs` and
  `PipelineSpec.hs`.
- `F2` authoritative `round-043` `attempt-1` landed only that bounded slice,
  introduced `rootLocalSchemeAliasBaseLike =
  rootBindingIsLocalType && rootIsSchemeAlias && rootBoundIsBaseLike`, routed
  the selected `targetC` lane through `rootFinal`, kept `boundVarTarget` absent
  for the selected slice, and left `rootHasMultiInst` /
  `instArgRootMultiBase` unchanged and out of scope.
- Accepted negative findings remain binding:
  `U2 = authority-narrowed`,
  `U3 = uniqueness-owner-stable-refuted`,
  `U4 = constructor-acyclic-termination-refuted`.
- `/Users/ares/.codex/worktrees/d432/mlf4/Bugs.md` remains replay-lane
  continuity context only. Open replay bug `BUG-2026-03-16-001` does not
  authorize replay reopen, `MLF.Elab.Inst`, `MLF.Elab.Inst.applyInstantiation`,
  or `InstBot` work in this round.

Current round control files already exist under
`/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/rounds/round-044/`.
Respect those existing edits. Do not revert or "clean up" unrelated work while
collecting `F3` evidence.

## Authoritative Inputs To Preserve

- `/Users/ares/.codex/worktrees/d432/mlf4/AGENTS.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/roles/planner.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/state.json`
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/rounds/round-044/selection.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/roadmap.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/verification.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/retry-subloop.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/docs/superpowers/specs/2026-03-18-unannotated-iso-recursive-continue-bounded-cycle-design.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-17-uri-r2-c1-u6-next-widening-decision-gate.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-17-uri-r2-c1-u5-bounded-unannotated-implementation-slice.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-19-uri-r2-c1-f2-bounded-implementation-slice.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/rounds/round-043/review-record.json`
- `/Users/ares/.codex/worktrees/d432/mlf4/src/MLF/Elab/Run/ResultType/Fallback.hs`
- `/Users/ares/.codex/worktrees/d432/mlf4/test/PipelineSpec.hs`
- `/Users/ares/.codex/worktrees/d432/mlf4/Bugs.md`

## Files Expected In Scope

Primary writable verification artifact:

1. `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-19-uri-r2-c1-f3-bounded-verification-gate.md`
   - canonical `F3` verification/evidence record.

Optional bounded note file:

1. `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/rounds/round-044/implementation-notes.md`
   - optional command transcript / blocker capture file if long outputs need a
     bounded home.

Read-only evidence anchors:

1. `/Users/ares/.codex/worktrees/d432/mlf4/src/MLF/Elab/Run/ResultType/Fallback.hs`
2. `/Users/ares/.codex/worktrees/d432/mlf4/test/PipelineSpec.hs`
3. `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-19-uri-r2-c1-f2-bounded-implementation-slice.md`
4. `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/rounds/round-043/review-record.json`
5. `/Users/ares/.codex/worktrees/d432/mlf4/Bugs.md`

Files that must remain untouched by `F3` `attempt-1`:

- `/Users/ares/.codex/worktrees/d432/mlf4/src/`
- `/Users/ares/.codex/worktrees/d432/mlf4/src-public/`
- `/Users/ares/.codex/worktrees/d432/mlf4/app/`
- `/Users/ares/.codex/worktrees/d432/mlf4/test/`
- `/Users/ares/.codex/worktrees/d432/mlf4/mlf2.cabal`
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/state.json`
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/roadmap.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/Bugs.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/rounds/round-044/selection.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/rounds/round-044/plan.md`
- reviewer-owned history under
  `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/rounds/round-001/`
  through
  `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/rounds/round-043/`

No edit to `Fallback.hs` or `PipelineSpec.hs` is authorized during `F3`
`attempt-1`. If the accepted `F2` slice fails reverification, capture the
blocker with evidence and stop; do not repair the slice inside this docs-only
gate.

## Sequential Tasks

### Task 1 - Freeze the `F3` `attempt-1` contract as a docs-only verification gate

- In the canonical `F3` artifact, state explicitly:
  - `Round: round-044`
  - `Roadmap item: F3`
  - `Stage: implement`
  - `Attempt: attempt-1`
  - `Retry state: null`
  - `Live subject: repaired URI-R2-C1`
- Reassert the inherited boundary unchanged:
  - explicit-only recursive baseline;
  - non-equi-recursive semantics;
  - non-cyclic structural graph encoding;
  - no second executable interface;
  - no compatibility / convenience / default-path widening.
- State that `F3` verifies only the accepted `F2` local-binding
  scheme-alias/base-like `keepTargetFinal` / `targetC` slice.
- State that `F3` does not reopen `F1` selection, does not reopen `F2`
  implementation, does not preempt `F4`, and does not authorize production or
  test edits during this attempt.
- State that any verification failure is a blocker to record, not permission to
  patch `Fallback.hs`, `PipelineSpec.hs`, or any other production/test file
  during this attempt.

### Task 2 - Reconstruct the accepted `F2` evidence chain without widening it

- Use
  `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/rounds/round-043/review-record.json`
  as the authoritative acceptance proof that `F2` finalized as
  `attempt=1`, `attempt_verdict=accepted`, `stage_action=finalize`,
  `status=authoritative`, `authoritative_attempt=1`, with the canonical
  artifact path
  `docs/plans/2026-03-19-uri-r2-c1-f2-bounded-implementation-slice.md`.
- Carry forward the exact bounded `F2` slice recorded in
  `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-19-uri-r2-c1-f2-bounded-implementation-slice.md`:
  - `rootLocalSchemeAliasBaseLike` is the reviewer-auditable proof for the
    selected local-binding lane;
  - `keepTargetFinal` keeps that one local scheme-alias/base-like trigger;
  - `targetC` selects `rootFinal` only for that local lane;
  - the focused `ARI-C1` block contains exactly one local positive example and
    one matched non-local fail-closed contrast for the selected lane.
- State explicitly that the accepted `F2` exclusions remain binding in `F3`:
  - `boundVarTarget` is absent for the selected slice;
  - `rootHasMultiInst` and `instArgRootMultiBase` remain unchanged and out of
    scope;
  - no replay reopen;
  - no `MLF.Elab.Inst`;
  - no `InstBot`;
  - no non-local widening;
  - no equi-recursive reasoning;
  - no cyclic structural encoding;
  - no second executable interface; and
  - no compatibility / convenience / default-path widening.
- Treat `/Users/ares/.codex/worktrees/d432/mlf4/Bugs.md` only as continuity
  context. Do not reinterpret replay-path bug history as current repair
  authority.
- State explicitly that `F3` answers only one bounded question:
  whether the accepted `F2` local-binding scheme-alias/base-like slice still
  looks stable under the focused `ARI-C1` block, the fresh full repo gate, and
  predecessor continuity rechecks.

### Task 3 - Collect read-only evidence from the bounded code/test anchors

- Inspect the existing selected-lane logic in
  `/Users/ares/.codex/worktrees/d432/mlf4/src/MLF/Elab/Run/ResultType/Fallback.hs`
  without editing it.
- Capture line-referenced evidence for all of the following:
  - `rootLocalSchemeAliasBaseLike`;
  - `keepTargetFinal`, including the required
    `rootBindingIsLocalType` gate and the unchanged out-of-scope trigger
    families `rootHasMultiInst`, `instArgRootMultiBase`, and the inherited
    `boundVarTarget` branch;
  - the final `targetC` selection branch that chooses `rootFinal` only when
    `rootLocalSchemeAliasBaseLike` holds and otherwise falls back to the
    inherited fail-closed branches;
  - the fact that `boundVarTarget` machinery still exists as inherited context,
    but the selected `F2` proof and examples do not depend on it.
- Inspect the focused
  `describe "ARI-C1 feasibility characterization (bounded prototype-only)"`
  block in
  `/Users/ares/.codex/worktrees/d432/mlf4/test/PipelineSpec.hs`
  without editing it.
- Capture line-referenced evidence showing the accepted bounded block still
  includes:
  - the retained-child same-lane baseline examples inherited from `E2` / `E3`;
  - the local scheme-alias/base-like local-`TypeRef` success example;
  - the matched non-local fail-closed contrast for the same wrapper;
  - the local-binding gate source guard that names
    `rootLocalSchemeAliasBaseLike`;
  - the inherited bounded controls that stay fail-closed outside the selected
    local lane.
- Reconfirm from
  `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/rounds/round-043/review-record.json`
  that `F2` finalized as an authoritative accepted attempt with the expected
  artifact path and no retry still pending.

Recommended evidence commands for this step:

- `nl -ba src/MLF/Elab/Run/ResultType/Fallback.hs | sed -n '521,689p'`
- `nl -ba test/PipelineSpec.hs | sed -n '1298,1365p'`
- `python3 -m json.tool orchestrator/rounds/round-043/review-record.json`

### Task 4 - Re-run the fresh verification suite required for `F3`

- Run the baseline checks required by
  `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/verification.md`:
  - `git diff --check`
  - `python3 -m json.tool orchestrator/state.json >/dev/null`
  - `rg -n '"contract_version": 2|"retry": null|"retry": \{' orchestrator/state.json`
  - `rg -n '^\d+\. \[(pending|in-progress|done)\]' orchestrator/roadmap.md`
  - `test -f docs/superpowers/specs/2026-03-18-unannotated-iso-recursive-continue-bounded-cycle-design.md`
  - `test -f docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  - `test -f docs/plans/2026-03-14-unannotated-iso-recursive-r5-research-stop-decision.md`
  - `test -f docs/plans/2026-03-17-uri-r2-c1-r4-repair-decision-gate.md`
  - `test -f docs/plans/2026-03-17-uri-r2-c1-u6-next-widening-decision-gate.md`
  - `test -f orchestrator/retry-subloop.md`
- Run the focused bounded test block exactly:
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "ARI-C1 feasibility characterization (bounded prototype-only)"'`
- Run the mandatory fresh full repo gate exactly:
  - `cabal build all && cabal test`

Although `F3` is docs-only, the fresh full repo gate remains mandatory for this
stage because the roadmap and selection require current verification evidence
for the already-accepted `F2` slice, not merely a restatement of `round-043`.

### Task 5 - Re-run accepted-`F2` continuity checks and docs-only diff checks

- Re-run the accepted-`F2` continuity checks against both the authoritative
  implementation artifact and the authoritative review record:
  - `python3 -m json.tool orchestrator/rounds/round-043/review-record.json >/dev/null`
  - `rg -n 'rootLocalSchemeAliasBaseLike|keepTargetFinal|targetC|boundVarTarget|rootHasMultiInst|instArgRootMultiBase' docs/plans/2026-03-19-uri-r2-c1-f2-bounded-implementation-slice.md`
  - a short reviewer-readable continuity summary over the `round-043`
    review-record fields and `checks` map.
- Re-run docs-only diff checks so the reviewer can confirm that `F3` did not
  patch code while reverifying code:
  - `git diff --name-only`
  - `git diff --name-only -- . ':(exclude)docs/**' ':(exclude)orchestrator/**'`
  - `git status --short --untracked-files=all`
- Treat
  `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/rounds/round-044/selection.md`
  and
  `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/rounds/round-044/plan.md`
  as existing round-control context only. Do not edit them during implement
  stage, and do not misclassify them as new `F3` implementation scope.
- If any non-docs diff appears under `src/`, `test/`, `src-public/`, `app/`, or
  `mlf2.cabal`, stop and report a blocker instead of treating the round as a
  clean docs-only verification gate.

### Task 6 - Author the canonical `F3` artifact and reviewer handoff

- Write the canonical verification artifact at:
  `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-19-uri-r2-c1-f3-bounded-verification-gate.md`.
- Structure the artifact so the reviewer can audit, in order:
  - stage contract freeze for `F3` `attempt-1`;
  - accepted evidence chain carried forward without widening;
  - bounded read-only anchor evidence from `Fallback.hs` and `PipelineSpec.hs`;
  - fresh baseline / focused-block / full-gate results;
  - accepted-`F2` continuity recheck;
  - docs-only diff evidence;
  - stability conclusion or blocker section; and
  - non-authorization statement preserving the bounded exclusions.
- Update
  `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/rounds/round-044/implementation-notes.md`
  only if needed to hold long command output or blocker details. Do not create
  any other round artifacts during implement stage.
- Ensure the reviewer can see, without inference, that:
  - this is the initial `F3` plan for `attempt-1` with `retry: null`;
  - the canonical `F3` artifact path is exact;
  - the `ARI-C1` rerun and `cabal build all && cabal test` gate are both fresh;
  - predecessor continuity was checked against the accepted `F2` artifact and
    `round-043` review record;
  - `boundVarTarget` remained absent for the selected slice; and
  - `rootHasMultiInst` / `instArgRootMultiBase` remained unchanged and out of
    scope.
- Ensure reviewer outcomes remain the lawful `C3`-style set for this
  verification gate:
  - `accepted + finalize`
  - `accepted + retry`
  - `rejected + retry`

## Non-Goals

- No reopening of `F1` target selection.
- No reopening of `F2` implementation.
- No production or test edits.
- No replay reopen, `MLF.Elab.Inst`, `MLF.Elab.Inst.applyInstantiation`, or
  `InstBot` work.
- No non-local widening.
- No redesign of the inherited retained-child `E2` / `E3` lane.
- No new use of `boundVarTarget` as clearance for the selected `F2` slice.
- No widening of `rootHasMultiInst` or `instArgRootMultiBase`.
- No equi-recursive reasoning or implicit unfolding.
- No cyclic structural graph encoding.
- No multi-SCC widening, cross-family widening, or broader search.
- No second executable interface.
- No compatibility fallback, convenience fallback, or default-on widening path.
- No edits to `orchestrator/state.json`, `orchestrator/roadmap.md`, `Bugs.md`,
  `src-public/`, `app/`, `mlf2.cabal`, or prior-round attempt artifacts.
- No rewrite of roadmap history or prior review history.

## Reviewer Checks

Baseline checks from
`/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/verification.md` still
apply.

Round-specific checks:

1. `plan.md` frames this as the initial `F3` plan for `attempt-1` with
   `retry: null`, not as a retry delta plan.
2. The canonical verification artifact path is exactly
   `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-19-uri-r2-c1-f3-bounded-verification-gate.md`.
3. Ownership stays bounded to the canonical `F3` artifact and optional
   `round-044` notes only; no production/test edits are authorized.
4. The live subject remains repaired `URI-R2-C1`, and the inherited boundary
   remains explicit-only / non-equi-recursive / non-cyclic-graph / no second
   interface / no fallback widening.
5. The plan requires a fresh rerun of the focused
   `ARI-C1 feasibility characterization (bounded prototype-only)` block and
   names the selected local scheme-alias/base-like positive example plus the
   matched non-local fail-closed contrast.
6. The plan requires a fresh full `cabal build all && cabal test` gate even
   though `F3` is docs-only.
7. The plan requires predecessor continuity checks against the accepted `F2`
   artifact and `round-043` review record.
8. The plan keeps the accepted `F2` exclusions explicit:
   `boundVarTarget` absent for the selected slice,
   `rootHasMultiInst` / `instArgRootMultiBase` out of scope,
   no replay reopen,
   no `MLF.Elab.Inst`,
   no `InstBot`,
   no non-local widening,
   no equi-recursive reasoning,
   no cyclic encoding,
   no second interface, and
   no convenience/default widening.
9. Any verification failure is treated as a blocker to record, not permission
   to patch code or rewrite accepted artifacts.
10. The plan does not authorize roadmap-history edits, prior-attempt rewrites,
    or implementation beyond the docs-only verification gate.
