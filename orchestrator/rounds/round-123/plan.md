# Round 123 Plan (`item-4` Successor Gate)

## Objective

Execute roadmap item `4` only:
record exactly one successor gate outcome and exactly one immediate handoff
after the bounded `C1` / `P2` lane.

This round is `attempt-1` with `retry: null`.

## Writable Slice

This round may write only:

- `docs/plans/2026-03-28-post-c1-p2-successor-gate-and-immediate-handoff-decision.md`
- `orchestrator/rounds/round-123/*`

## Task List

1. Bind the accepted baseline, predecessor gate, item-1 freeze, and item-3
   settlement surface.
2. Select exactly one lawful current outcome for the bounded `C1` / `P2`
   family.
3. Select exactly one lawful immediate handoff.
4. Record why the other lawful outcomes and handoffs are not selected.

## Exit Criteria

This round is complete only when the gate is exact, singular, and non-
widening.
