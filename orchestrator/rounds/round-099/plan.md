# Round 099 Plan (`item-1` Global Non-Cyclic-Graph Settlement Contract Freeze And Unresolved Family Evidence Ledger)

## Objective

Execute only roadmap item `1` and produce one docs-first artifact at:
`docs/plans/2026-03-26-global-non-cyclic-graph-settlement-contract-and-unresolved-family-evidence-ledger.md`.

This is the initial `item-1` plan for `attempt-1` with `retry: null`. The
round must freeze exactly one repo-level settlement contract and one
representative-family evidence ledger for the current
`non-cyclic-graph` question.

The artifact must:

- freeze the exact acceptance bar for later roadmap item `5` to record
  either `non-cyclic-graph = keep` or
  `reopen the non-cyclic-graph revision question`;
- freeze the current family ledger across positive families `P1` through
  `P6` and negative or bounded families `N1` through `N6` without
  promoting bounded pockets into broad accepted success;
- freeze the accepted predecessor evidence that later rounds may consume,
  including the baseline contract, the accepted capability contract,
  the accepted architectural audit, the accepted full-pipeline validation
  contract, the accepted representative campaign, the accepted strategic
  architecture decision, and accepted exact-pocket rounds `094` through
  `098`;
- make explicit that accepted rounds `094` through `098` are bounded exact-
  pocket predecessor evidence only, not repo-level settlement by
  themselves; and
- preserve the inherited explicit-only / iso-recursive /
  non-equi-recursive / non-cyclic-graph / no-second-interface /
  no-fallback boundary unchanged while later rounds gather production-surface
  evidence.

This round is docs-only and contract-freeze-only. It must not edit
production code, tests, public surfaces, executables, `mlf2.cabal`,
controller-owned machine state, roadmap machine metadata, retry contracts,
verification contracts, `Bugs.md`, or completed roadmap history.

This round must not silently widen into:

- roadmap items `2`, `3`, or `4` proof work;
- roadmap item `5` global settlement itself;
- roadmap items `6`, `7`, or `8`;
- production implementation, hardening, or capability-claim work;
- equi-recursive reasoning, cyclic structural graphs, multi-SCC search,
  second interfaces, or fallback behavior; or
- any claim that one bounded same-lane retained-child pocket already settles
  the repo-level architecture question.

Current planning read: item `1` is a docs-only settlement-contract-freeze
round. If the accepted record does not justify a stronger statement, the
artifact must fail closed by freezing only predecessor-established truth and
by naming later proof obligations rather than widening scope.

## Locked Round Context

- Round id: `round-099`
- Roadmap item: `item-1`
- Stage: `plan`
- Active attempt: `attempt-1`
- Retry state: `null`
- Fixed live subject: one docs-only global settlement-contract freeze and
  unresolved family evidence ledger for the refreshed
  `non-cyclic-graph` roadmap family
- Active branch: `codex/round-099`
- Active worktree:
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-099`
- Current round review feedback: none yet; no `review.md`,
  `reviews/attempt-<n>.md`, or `review-record.json` exists for `round-099`,
  so this is a full `attempt-1` plan rather than a retry delta

Current round worktree state is already non-pristine. Respect existing edits
and preserve the existing round artifacts unchanged:

- `M orchestrator/state.json`
- `?? orchestrator/rounds/round-099/controller-recovery-note.md`
- `?? orchestrator/rounds/round-099/selection.md`
- `orchestrator/rounds/round-099/reviews/` exists but contains no review
  files yet

## Accepted Continuity That Remains Binding

- `orchestrator/rounds/round-099/selection.md`
  is the stage authority for this round. It fixes the live subject to
  roadmap item `1` only, names the exact global settlement-contract freeze
  and unresolved-family-ledger scope, and forbids widening into proof slices,
  global settlement, or production implementation.
- `orchestrator/state.json`
  fixes the live controller state at `active_round_id = round-099`,
  `stage = plan`, `current_task = item-1`, `retry = null`,
  `roadmap_id = 2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap`,
  `roadmap_revision = rev-001`, and the authoritative
  `roadmap_dir = orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-001`.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-001/roadmap.md`
  makes item `1` the first unfinished item and defines its completion notes
  as one exact global settlement contract freeze plus one unresolved family
  ledger.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-001/retry-subloop.md`
  confirms that item `1` may retry in principle, but `retry: null` means
  this plan is the full first-attempt plan.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-001/verification.md`
  requires settlement-contract-freeze checks proving that item `1` freezes
  the exact global `keep` versus `reopen` bar, the unresolved family ledger,
  and the lawful predecessor evidence chain without overclaiming settlement.
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  preserves the inherited boundary:
  explicit recursive annotations remain the production baseline;
  recursive meaning remains iso-recursive only; and non-equi-recursive,
  non-cyclic-graph, no-second-interface, and no-fallback boundaries remain
  binding.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-capability-contract-and-evaluation-corpus.md`
  defines the representative family matrix and later success criteria:
  positive families `P1` through `P6`; negative or bounded families
  `N1` through `N6`; production-surface evidence required for any later
  repo-level claim; and explicit out-of-scope reads for `N3`, `N4`, and
  `N5` under the unchanged baseline.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md`
  keeps `iso-recursive = keep`, `non-equi-recursive = keep`,
  `non-cyclic-graph = unknown`, and `no-fallback = keep`. Item `1` must
  freeze that as the current repo-level strategic posture rather than
  reinterpret it into `keep` or `reopen`.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md`
  supplies the controlling production-surface evidence bar and the lawful
  outcome vocabulary
  `stable visible persistence`,
  `admitted but not reconstruction-visible / blocker debt`, and
  `fail-closed rejection`.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`
  keeps the accepted record at bounded-subset-only feasibility:
  zero `stable visible persistence` rows;
  blocker-debt rows `C1`, `C2`, `C5`, and `C7`;
  fail-closed rows `C3`, `C4`, and `C6`.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architecture-decision-and-successor-plan-choice.md`
  records `continue within the current architecture` as the strongest lawful
  accepted strategic read on the earlier bounded evidence, while still
  keeping `non-cyclic-graph` as the only inherited boundary classified
  `unknown`.
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md`
  through
  `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md`
  plus `orchestrator/rounds/round-094/review-record.json` through
  `orchestrator/rounds/round-098/review-record.json`
  remain the accepted exact-pocket predecessor chain for one same-lane
  retained-child pressure point only. Item `1` must preserve them as bounded
  exact-pocket evidence and must explicitly deny that they globally settle
  `non-cyclic-graph`.
- `Bugs.md`
  still lists open `BUG-2026-03-16-001`, but that replay / `InstBot` defect
  remains predecessor replay context only. It does not authorize roadmap
  reordering, implementation work, fallback widening, or architecture
  revision in item `1`.

## File Map

### Create

- `docs/plans/2026-03-26-global-non-cyclic-graph-settlement-contract-and-unresolved-family-evidence-ledger.md`
  - Responsibility: canonical docs-only item-1 artifact freezing the exact
    repo-level `keep` versus `reopen` acceptance bar, the unresolved family
    ledger across `P1` through `P6` and `N1` through `N6`, and the accepted
    predecessor evidence chain that later rounds must preserve.

### Read-Only Evidence

- `orchestrator/rounds/round-099/selection.md`
- `orchestrator/rounds/round-099/controller-recovery-note.md`
- `orchestrator/state.json`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-001/roadmap.md`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-001/verification.md`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-001/retry-subloop.md`
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-capability-contract-and-evaluation-corpus.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architecture-decision-and-successor-plan-choice.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-path-audit.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-collapse-clear-or-confirm.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md`
- `orchestrator/rounds/round-094/review-record.json`
- `orchestrator/rounds/round-095/review-record.json`
- `orchestrator/rounds/round-096/review-record.json`
- `orchestrator/rounds/round-097/review-record.json`
- `orchestrator/rounds/round-098/review-record.json`
- `Bugs.md`

### Preserve Unchanged

- `orchestrator/state.json`
- `orchestrator/rounds/round-099/controller-recovery-note.md`
- `orchestrator/rounds/round-099/selection.md`
- `orchestrator/rounds/round-099/reviews/`
- `orchestrator/roadmap.md`
- `orchestrator/retry-subloop.md`
- `orchestrator/verification.md`
- `Bugs.md`
- `src/`
- `src-public/`
- `app/`
- `test/`
- `mlf2.cabal`
- accepted predecessor docs and review records
- completed roadmap history

## Exact Selected Slice (Exactly One)

The only selected slice is:

freeze the global `non-cyclic-graph` settlement contract and unresolved
family evidence ledger.

This slice is allowed to:

- restate the exact later acceptance bar for
  `non-cyclic-graph = keep` versus
  `reopen the non-cyclic-graph revision question`;
- freeze the current family-by-family ledger for `P1` through `P6` and
  `N1` through `N6`;
- bind each ledger row to accepted predecessor evidence and current bounded
  posture;
- restate that accepted rounds `094` through `098` are bounded exact-pocket
  evidence only and do not settle the repo-level gate by themselves; and
- name the next missing work as later proof slices and the later global
  settlement gate, without performing them.

This slice is not allowed to:

- select the global settlement outcome now;
- propose or land production code, test, or public-surface changes;
- reopen or relitigate accepted predecessor truth;
- reinterpret one bounded retained-child pocket into repo-level capability or
  architecture settlement;
- authorize equi-recursive reasoning, cyclic graphs, multi-SCC search,
  second interfaces, or fallback widening; or
- plan production implementation before accepted item `5` records
  `non-cyclic-graph = keep`.

If the artifact starts depending on new code-path theory, fresh runtime
evidence, or causal claims not already accepted in the predecessor record to
justify a ledger entry, it must fail closed by preserving the existing
accepted read or by naming the missing proof as later-round work.

## Sequential Tasks

### Task 1 - Freeze the docs-only item-1 frame and inherited boundary

- Create the canonical item-1 artifact at the path above.
- State explicitly that item `1` is docs-only, contract-freeze-only, and
  `attempt-1` with `retry: null`.
- Reassert the inherited boundary unchanged:
  - explicit recursive annotations remain the production baseline;
  - recursive meaning remains iso-recursive only;
  - no equi-recursive equality or implicit unfolding is authorized;
  - no cyclic structural graph encoding or multi-SCC search is authorized;
  - no second interface is authorized; and
  - no compatibility, convenience, or default-path fallback widening is
    authorized.
- State explicitly that no production implementation, hardening, or final
  capability-claim planning is lawful before accepted item `5` records
  `non-cyclic-graph = keep`.
- State explicitly that accepted rounds `094` through `098` contribute
  bounded exact-pocket predecessor evidence only.

### Task 2 - Freeze the exact global settlement gate contract

- Add one explicit outcome-evaluation table with exactly two lawful later
  item-5 outcomes:
  - `non-cyclic-graph = keep`
  - `reopen the non-cyclic-graph revision question`
- Freeze the `keep` acceptance bar as requiring accepted production-surface
  evidence across the representative matrix:
  - credible support for positive families `P1` through `P6`;
  - fail-closed, bounded, or explicitly out-of-scope behavior across
    `N1` through `N6` consistent with the inherited baseline; and
  - reviewable solver -> elaboration -> reconstruction -> internal/public
    output continuity on the existing pipeline surfaces rather than
    solver-only wins or packet-specific folklore.
- Freeze the `reopen` acceptance bar as requiring accepted evidence that at
  least one required positive family cannot be carried inside the inherited
  acyclic model without crossing the current architecture boundary.
- State explicitly that item `1` itself selects neither outcome.
- State explicitly that the following are insufficient by themselves:
  - one bounded admitted pocket;
  - one blocker-debt pocket;
  - one `continue within the current architecture` strategic read; or
  - one unresolved `unknown` pressure axis in the audit.

### Task 3 - Freeze the unresolved family evidence ledger

- Present one family ledger covering every positive family `P1` through `P6`
  and every negative or bounded family `N1` through `N6`.
- Freeze the current read for the positive side without overclaiming:
  - `P1`: bounded predecessor support exists inside the inherited acyclic
    model, but one local-shape family alone does not settle the repo-level
    gate;
  - `P2`: still unresolved at repo scope, with non-local admitted evidence
    remaining blocker debt rather than accepted production-surface success;
  - `P3`: still unresolved at repo scope, with retained-child and alias-bound
    owner-sensitive pressure only partially admitted;
  - `P4`: still unresolved at repo scope, with same-lane retained-child
    evidence bounded to one exact pocket rather than general binder-sensitive
    placement success;
  - `P5`: still unresolved and reject-side only on accepted evidence;
  - `P6`: still unresolved because zero accepted rows currently reach
    `stable visible persistence` on both internal and public output surfaces.
- Freeze the current read for the negative and bounded side:
  - `N1`: fail-closed ambiguity rejection remains required and has accepted
    bounded support;
  - `N2`: fail-closed soundness and binder-discipline rejection remains
    required and has accepted bounded support;
  - `N3`: remains out of scope under the unchanged iso-recursive baseline;
  - `N4`: remains out of scope under the unchanged non-cyclic-graph /
    no-multi-SCC baseline unless a later accepted roadmap decision reopens
    it;
  - `N5`: remains out of scope under the unchanged no-second-interface /
    no-fallback baseline; and
  - `N6`: bounded or fail-closed handling remains required, with accepted
    evidence preserving bounded rejection but not yet clearing the
    representative high-pressure positive families.
- Tie the ledger explicitly to the accepted campaign vocabulary:
  zero `stable visible persistence` rows;
  blocker-debt rows `C1`, `C2`, `C5`, and `C7`;
  fail-closed rows `C3`, `C4`, and `C6`.
- State explicitly that the same-lane retained-child chain from rounds
  `094` through `098` feeds only the bounded reads for `P3`, `P4`, and `P6`
  and does not clear those families globally.

### Task 4 - Freeze the predecessor evidence chain and honest handoff

- Record the accepted predecessor chain that later rounds must preserve:
  - the inherited baseline contract;
  - the accepted capability contract and family matrix;
  - the accepted architectural audit keeping
    `non-cyclic-graph = unknown`;
  - the accepted full-pipeline production-surface contract;
  - the accepted representative campaign keeping the posture at
    bounded-subset-only feasibility;
  - the accepted strategic architecture decision
    `continue within the current architecture`; and
  - accepted exact-pocket rounds `094` through `098`.
- Add one explicit note that `BUG-2026-03-16-001` remains open predecessor
  replay context only and does not authorize roadmap reordering or a wider
  architecture claim here.
- State the one honest starting posture for this refreshed family:
  repo-level `non-cyclic-graph` settlement remains unresolved; the current
  record contains bounded admitted pockets and fail-closed rejection
  discipline, but it does not yet justify `keep` or `reopen`.
- Add one explicit handoff section pointing only to the later bounded proof
  work that item `1` enables, with the next immediate consumer being roadmap
  item `2`.

## Verification Expectations

Run the minimal docs-only verification needed for this item-1 freeze:

- `git diff --check`
- `python3 -m json.tool orchestrator/state.json >/dev/null`
- `rg -n '"contract_version": 2|"roadmap_id":|"roadmap_revision":|"roadmap_dir":|"retry": null' orchestrator/state.json`
- `roadmap_dir="$(jq -r '.roadmap_dir' orchestrator/state.json)" && test -f "$roadmap_dir/roadmap.md" && test -f "$roadmap_dir/retry-subloop.md" && test -f "$roadmap_dir/verification.md"`
- `rg -n 'non-cyclic-graph = keep|reopen the non-cyclic-graph revision question|P1|P2|P3|P4|P5|P6|N1|N2|N3|N4|N5|N6|stable visible persistence|admitted but not reconstruction-visible / blocker debt|fail-closed rejection|round-094|round-098|bounded exact-pocket predecessor evidence only|continue within the current architecture' docs/plans/2026-03-26-global-non-cyclic-graph-settlement-contract-and-unresolved-family-evidence-ledger.md`
- `git diff --name-only -- src src-public app test mlf2.cabal Bugs.md orchestrator/state.json orchestrator/rounds/round-099/controller-recovery-note.md orchestrator/rounds/round-099/selection.md orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-001/roadmap.md orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-001/retry-subloop.md orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-001/verification.md`

Do not run `cabal build all && cabal test` unless the diff escapes the
authorized docs-only surface. If the round remains docs-only, the reviewer
should record that code-path gates are out of scope for this item-1 freeze.

## Completion Criteria

This plan is complete when the implementer can land one docs-only canonical
artifact that:

- freezes the exact later acceptance bar for
  `non-cyclic-graph = keep` versus
  `reopen the non-cyclic-graph revision question`;
- freezes one family-by-family ledger across `P1` through `P6` and
  `N1` through `N6` without promoting bounded evidence into global success;
- preserves the inherited explicit-only / iso-recursive /
  non-equi-recursive / non-cyclic-graph / no-second-interface /
  no-fallback boundary unchanged;
- preserves accepted predecessor continuity, including the rule that accepted
  rounds `094` through `098` are bounded exact-pocket evidence only;
- leaves code, tests, controller state, roadmap machine metadata, and
  completed roadmap history untouched; and
- hands off only to later bounded proof work, with roadmap item `2` as the
  next immediate consumer, without silently widening into global settlement
  or production implementation.
