# Round 119 Plan (`item-3` Narrowed Repo-Scope Successor Gate)

## Objective

Execute roadmap item `3` only:
record one narrowed repo-scope successor gate from the accepted refreshed
representative family-matrix.

This round is `attempt-1` with `retry: null`. No current-round review
feedback exists yet beyond the planner contract, selection, and accepted
predecessor record.

The plan stays inside one actionable slice only:
the `posture decision` slice. The required immediate handoff is recorded only
as the one bounded consequence of the selected posture. This round does not
open or execute that successor work.

No parallel lane split is authorized. The round is aggregate-only and not
lane-parallelizable.

## Binding Boundary Carried Forward

The following accepted truth remains fixed and must be carried forward
without widening:

- `docs/plans/2026-03-27-post-rev-004-repo-scope-successor-authority-evidence-inputs-and-non-widening-boundary-freeze.md`
  remains the live repo-scope authority freeze for this roadmap family.
- `docs/plans/2026-03-27-post-rev-004-repo-scope-refreshed-representative-family-matrix-settlement-surface-and-provenance-validation.md`
  remains the live repo-scope matrix input for this round.
- explicit recursive annotations remain the production baseline;
  `iso-recursive = keep`,
  `non-equi-recursive = keep`,
  the inherited non-cyclic structural boundary,
  `no-fallback = keep`,
  and one-interface-only all remain binding unless the item-3 artifact says
  otherwise explicitly.
- the same-lane `C2` / `C5` / `C7` pocket remains settled predecessor truth
  only. It may be cited as carried-forward repo-scope evidence, but it must
  not be reopened as live same-pocket debt or rerun under a new name.
- the March 26 global
  `docs/plans/2026-03-26-global-non-cyclic-graph-keep-vs-reopen-decision-gate.md`
  remains immutable historical aggregate evidence only. This round must not
  reuse that old `keep` vs `reopen` framing unchanged as if it were still the
  live repo-scope answer.
- `Bugs.md`, local task packets, and unrepublished harness context remain
  planning context only. They may explain pressure or provenance, but they are
  not lawful controller truth for the item-3 decision.
- no equi-recursive reasoning, cyclic structural graphs, multi-SCC search,
  second interfaces, fallback widening, production implementation, hardening,
  rollout, or broad capability claim is authorized here.

## Authoritative Output Set

The implementer may write only the following round-owned outputs:

1. `docs/plans/2026-03-27-post-rev-004-repo-scope-narrowed-successor-gate-and-immediate-handoff-decision.md`
   - owner: aggregate implementer only;
   - role: the only authoritative item-3 artifact.
2. `orchestrator/rounds/round-119/implementation-notes.md`
   - owner: aggregate implementer only;
   - role: non-authoritative companion notes and verification trace for the
     same docs-only gate artifact.

Single-writer rule:

- planner owns `orchestrator/rounds/round-119/plan.md` only;
- reviewer remains the only lawful writer for `review.md`,
  `reviews/attempt-<n>.md`, and `review-record.json`;
- no stage in this round may edit `selection.md`, `orchestrator/state.json`,
  the roadmap bundle, `Bugs.md`, prior accepted docs, or any code/test file.

## Required Item-3 Outcome Vocabulary

The authoritative artifact must evaluate exactly three posture tokens and
select exactly one:

1. `non-cyclic-graph = keep`
2. `narrowed unresolved / continue within the current architecture`
3. `reopen the non-cyclic-graph revision question`

The same artifact must then record exactly one immediate handoff token:

1. `stop`
2. `open one bounded current-architecture successor lane`
3. `open one explicit boundary-revision family`

Forbidden outcome shapes:

- mixed posture;
- mixed handoff;
- `unknown`;
- reusing the March 26 global reopen result as the live answer without
  re-evaluating the refreshed matrix; or
- any handoff that silently jumps to production implementation, hardening, or
  a broad capability claim.

## Sequential Task List

### Task 1: Freeze the decision inputs and the live-vs-historical distinction

- Re-state in the canonical artifact that the live decision inputs are:
  the March 14 baseline contract, the March 25 capability and full-pipeline
  contracts, the March 25 architecture decision as predecessor context only,
  the accepted rev-004 same-lane settlement chain as settled predecessor
  truth only, the round-117 freeze artifact, and the accepted round-118
  refreshed matrix artifact.
- State explicitly that the March 26 global gate and earlier same-lane case
  chain remain immutable historical evidence only.
- State explicitly that this round consumes the refreshed matrix as already
  accepted repo-scope truth and does not rerun `C1`, `P5`, or the settled
  same-lane `C2` / `C5` / `C7` pocket.

### Task 2: Evaluate the three lawful posture tokens against the refreshed matrix

- Compare the refreshed round-118 matrix against the keep bar, the narrowed
  current-architecture continuation bar, and the reopen bar.
- The evaluation must account for the current refreshed read exactly as
  published:
  `C1` remains visibly non-recursive on authoritative surfaces;
  `C3` remains reject-side only once the nested `forall` boundary is crossed;
  `C2` / `C5` / `C7` now survive only as settled exact-pocket predecessor
  truth with repaired public output continuity for that one exact packet; and
  `P1-row`, `C4`, and `C6` remain historical carry-forward rows.
- The canonical artifact must explain why `non-cyclic-graph = keep` is either
  supported or blocked on the refreshed matrix rather than on the superseded
  March 26 aggregate read.
- The canonical artifact must explain why
  `reopen the non-cyclic-graph revision question` is either supported or
  blocked on the refreshed matrix rather than on the old public-collapse read
  that rev-004 repaired.
- If neither `keep` nor `reopen` is supportable on the refreshed matrix
  without weakening the accepted bars, the artifact must select
  `narrowed unresolved / continue within the current architecture`.

### Task 3: Map the selected posture to exactly one lawful immediate handoff

- The canonical artifact must record exactly one next move and must make the
  mapping explicit.
- If the selected posture is `non-cyclic-graph = keep`, the only lawful
  immediate handoff is `stop`.
- If the selected posture is
  `narrowed unresolved / continue within the current architecture`, the only
  lawful immediate handoff is
  `open one bounded current-architecture successor lane`, and the artifact
  must narrow that lane to the remaining `C1` / `P2 non-local-propagation`
  authoritative-surface blocker family only.
- If the selected posture is
  `reopen the non-cyclic-graph revision question`, the only lawful immediate
  handoff is `open one explicit boundary-revision family`, limited to the
  `non-cyclic-graph` question only.
- `P5` may be cited as surviving boundary pressure context, but it may not be
  promoted into a second live lane or used to reopen the settled same-lane
  pocket indirectly.

### Task 4: Write the one authoritative narrowed successor-gate artifact

- Write
  `docs/plans/2026-03-27-post-rev-004-repo-scope-narrowed-successor-gate-and-immediate-handoff-decision.md`
  as the only authoritative item-3 artifact.
- The canonical artifact must:
  - identify item `3`, `round-119`, and the docs-only aggregate gate scope;
  - preserve the inherited baseline and no-widening boundary;
  - distinguish live refreshed-matrix input from historical March 26
    aggregate evidence;
  - evaluate all three lawful posture tokens and select exactly one;
  - record exactly one immediate handoff token consistent with the selected
    posture;
  - make clear that any bounded current-architecture continuation, if chosen,
    targets the remaining `C1` blocker family rather than the closed
    same-lane `C2` / `C5` / `C7` pocket; and
  - stop at the narrowed decision record only, without creating a new roadmap
    revision, implementation slice, or hardening plan inside this round.

### Task 5: Write round notes and verification trace

- Write `orchestrator/rounds/round-119/implementation-notes.md` after the
  canonical artifact is complete.
- The notes file must include:
  - a concise change summary;
  - an explicit statement that the round remained aggregate-only and used no
    parallel lanes;
  - the baseline verification commands from `verification.md`;
  - successor-gate-specific checks proving that the artifact records exactly
    one live posture and exactly one next move, keeps the same-lane pocket
    closed, and treats the March 26 global gate as historical evidence only;
  - an explicit docs-only skip note for the full
    `cabal build all && cabal test` gate unless the diff escapes the allowed
    docs/orchestrator surface.

## Failure Discipline

- If the implementer cannot distinguish the refreshed round-118 matrix from
  the older March 26 global gate cleanly, the round must fail closed in
  review rather than reuse the old reopen decision as if nothing changed.
- If the chosen handoff would require reopening the settled same-lane
  `C2` / `C5` / `C7` pocket, adding a second live lane, or inferring new
  authority from `Bugs.md`, local drafts, or unrerun harness context, the
  round must fail closed instead.
- If the refreshed matrix still does not justify a stronger posture than
  bounded unresolved continuation, the artifact must say so explicitly rather
  than silently upgrading to `keep` or silently widening to implementation.
