# Round 077 Plan (`N10` Reopened-Loop Safety And Acceptance Contract For Selected `boundVarTarget`)

## Objective

Execute only roadmap item `N10` and produce one docs-only contract artifact at:
`docs/plans/2026-03-23-automatic-iso-recursive-bound-var-target-safety-acceptance-contract.md`.

This is the initial `N10` plan for `attempt-1` with `retry: null`. The round
must keep `N10` docs-only, preserve accepted `L1` / `L2` / `N1` through `N9`
continuity, and establish exactly one verifier-checkable safety and
acceptance contract for the selected retained-child / nested-`forall` /
binding-structure `boundVarTarget` planning subject.

Current planning read: the only lawful `N10` outcome is:

- `boundVarTarget-safety-acceptance-contract-established`

That outcome must mean one thing only: a future exact target bind is allowed
to proceed only if it satisfies a bounded invariant audit and acceptance gate
for the `boundVarTargetRoot` / `boundHasForallFrom` / `boundVarTarget` /
`keepTargetFinal` / `targetC` route, while preserving the inherited
`explicit-only / non-equi-recursive / non-cyclic-graph / no-second-interface / no-fallback`
boundary.

This round must not:

- treat the earlier `N2` selection, the earlier `N3` safety contract, the
  exact accepted non-local `baseTarget -> baseC` packet, or the accepted
  repaired-queue retained-child packet as automatic carry-forward authority;
- bind an exact target;
- authorize implementation;
- authorize verification; or
- edit roadmap/state/bug-tracker/review/merge artifacts or code.

## Locked Round Context

- Round id: `round-077`
- Roadmap item: `N10`
- Stage: `plan`
- Active attempt: `attempt-1`
- Retry state: `null`
- Fixed live subject: selected retained-child / nested-`forall` /
  binding-structure `boundVarTarget` planning subject only
- Active branch: `codex/round-077-n10-safety-acceptance-contract`
- Active worktree:
  `.worktrees/round-077`
- Stage mode: docs-only safety/acceptance contract only
- Current round review feedback: none yet; this is a full `attempt-1` plan,
  not a retry delta

Current round worktree state is already non-pristine. Respect existing edits
and do not revert unrelated work:

- `?? orchestrator/rounds/round-077/selection.md`

## Accepted Continuity That Remains Binding

- `orchestrator/rounds/round-077/selection.md`
  already fixes this round to roadmap item `N10` only, keeps the round
  docs-only and contract-only, preserves accepted
  `N9 = boundVarTarget-planning-subject-selected`, and forbids exact target
  binding, implementation, verification, roadmap/state edits, and silent
  widening.
- `orchestrator/rounds/round-077/state-snapshot.json` fixes the
  live controller state at `active_round_id: "round-077"`, `stage: "plan"`,
  `current_task: "N10"`, branch
  `codex/round-077-n10-safety-acceptance-contract`, and `retry: null`.
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-010/roadmap.md` makes item
  `10` the first pending item after accepted `N9`; later work cannot begin
  until this round freezes one fresh subject-specific safety/acceptance
  contract.
- `tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
  records that the earlier reopened lane already has predecessor-only
  `N3` / `N4` / `N5` / `N6` evidence, while the fresh post-`N8` successor
  lane selected by accepted `N9` still requires its own accepted contract
  before any new exact target or slice can begin.
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  remains the inherited baseline contract: explicit recursive annotations are
  supported, automatic recursive-type inference remains unresolved and
  disabled, and the inherited boundary remains mandatory until an accepted
  roadmap amendment changes it.
- `docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md`
  remains the authoritative `L1` fail-closed record. The repaired
  `URI-R2-C1` queue had no fresh lawful exact successor slice inside the
  inherited boundary.
- `docs/plans/2026-03-21-uri-r2-c1-l2-post-l1-fail-closed-successor-decision-gate.md`
  remains the authoritative `L2` closeout. The repaired queue finalized as
  `stop-blocked`, and any broader preserved route required separate later
  authority before it could become live work.
- `orchestrator/rounds/round-068/review-record.json`
  through
  `orchestrator/rounds/round-076/review-record.json`
  remain the accepted continuity chain for `N1` through `N9`.
- `docs/plans/2026-03-22-automatic-iso-recursive-next-live-subject-selection.md`
  and
  `docs/plans/2026-03-22-automatic-iso-recursive-base-target-safety-acceptance-contract.md`
  remain predecessor evidence for the earlier `baseTarget` lane only. They do
  not authorize reuse of the earlier subject or of the earlier `N3` contract
  as fresh-lane clearance here.
- `docs/plans/2026-03-22-automatic-iso-recursive-base-target-exact-target-bind.md`,
  `docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-bounded-implementation-slice.md`,
  `docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-bounded-verification-gate.md`,
  and
  `docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-next-cycle-decision-gate.md`
  remain predecessor evidence for the exact accepted non-local
  `baseTarget -> baseC` packet only. They do not authorize exact-target,
  implementation, or verification carry-forward for the selected
  `boundVarTarget` lane.
- `docs/plans/2026-03-18-uri-r2-c1-e1-next-target-bind.md`
  plus the accepted `E2` / `E3` / `E4` repaired-queue retained-child packet
  remain predecessor evidence for boundedness and fail-closed behavior only.
  They do not automatically authorize this fresh post-`N8` lane.
- `docs/plans/2026-03-23-automatic-iso-recursive-bound-var-target-next-live-subject-selection.md`
  remains the authoritative `N9` record. It selects exactly one planning
  subject only, fixes that subject to the retained-child / nested-`forall` /
  binding-structure `boundVarTarget` route, and explicitly leaves later
  safety-contract, exact-bind, implementation, and verification work blocked.
- `orchestrator/rounds/round-076/review-record.json`
  remains the authoritative acceptance proof that `N9` finalized as
  `accepted + finalize` with final outcome
  `boundVarTarget-planning-subject-selected`.
- `src/MLF/Elab/Run/ResultType/Fallback.hs`,
  `src/MLF/Elab/Run/Scope.hs`, and
  `test/PipelineSpec.hs` remain the
  read-only code/test anchors that identify the selected subject precisely.
- `Bugs.md` still carries open
  `BUG-2026-03-16-001`, but that replay / `InstBot` defect remains read-only
  predecessor context only and does not authorize replay reopen,
  `MLF.Elab.Inst`, `InstBot`, or a different live subject in this round.
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-010/retry-subloop.md`
  keeps prior attempts immutable. This `attempt-1` plan must therefore create
  one new bounded contract artifact without rewriting any prior attempt or
  review artifact.

## File Map

### Modify

- `docs/plans/2026-03-23-automatic-iso-recursive-bound-var-target-safety-acceptance-contract.md`
  - Responsibility: canonical `N10` docs-only invariant-audit / acceptance
    contract artifact for the selected `boundVarTarget` planning subject only.

### Read-Only Evidence

- `orchestrator/rounds/round-077/selection.md`
- `orchestrator/rounds/round-077/state-snapshot.json`
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-010/roadmap.md`
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-010/verification.md`
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-010/retry-subloop.md`
- `tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
- `docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md`
- `docs/plans/2026-03-21-uri-r2-c1-l2-post-l1-fail-closed-successor-decision-gate.md`
- `docs/plans/2026-03-22-automatic-iso-recursive-next-live-subject-selection.md`
- `docs/plans/2026-03-22-automatic-iso-recursive-base-target-safety-acceptance-contract.md`
- `docs/plans/2026-03-22-automatic-iso-recursive-base-target-exact-target-bind.md`
- `docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-bounded-implementation-slice.md`
- `docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-bounded-verification-gate.md`
- `docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-next-cycle-decision-gate.md`
- `docs/plans/2026-03-23-automatic-iso-recursive-bound-var-target-next-live-subject-selection.md`
- `orchestrator/rounds/round-076/review-record.json`
- `docs/plans/2026-03-18-uri-r2-c1-e1-next-target-bind.md`
- `src/MLF/Elab/Run/ResultType/Fallback.hs`
- `src/MLF/Elab/Run/Scope.hs`
- `test/PipelineSpec.hs`
- `papers/these-finale-english.txt`
- `Bugs.md`

### Preserve Unchanged

- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-010/roadmap.md`
- `orchestrator/rounds/round-077/state-snapshot.json`
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-010/retry-subloop.md`
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-010/verification.md`
- `Bugs.md`
- `src/`
- `test/`
- `src-public/`
- `app/`
- `mlf2.cabal`
- reviewer-owned history under
  `orchestrator/rounds/round-001/`
  through
  `orchestrator/rounds/round-076/`

## Exact Selected `N10` Slice (Exactly One)

The only selected `N10` slice is:

write exactly one docs-only subject-specific safety/acceptance contract for
the selected retained-child / nested-`forall` / binding-structure
`boundVarTarget` route, with authoritative outcome
`boundVarTarget-safety-acceptance-contract-established`.

That selected outcome must mean all of the following, and nothing wider:

- the contract is about the route centered on `boundVarTargetRoot`,
  `boundHasForallFrom`, `boundVarTarget`, `keepTargetFinal`, and the
  `targetC` branch in `MLF.Elab.Run.ResultType.Fallback`;
- `schemeBodyTarget` remains neighboring owner-boundary context only, not the
  selected subject itself and not a silently widened alternate target;
- the earlier `N2` selection, the earlier `N3` contract, the exact accepted
  non-local `baseTarget -> baseC` packet, and the repaired-queue retained-child
  packet remain predecessor evidence only, not live authorization;
- the contract may freeze invariant axes, acceptance criteria, and no-go
  conditions for a later exact target bind, but it may not itself bind that
  later exact target; and
- replay reopen, `MLF.Elab.Inst`, `InstBot`, `boundTarget`,
  `schemeBodyTarget` as a live subject, `src/MLF/Elab/Run/ResultType/View.hs`,
  every other fallback family, every different solver/pipeline subject,
  cross-family search, equi-recursive reasoning, implicit unfolding, cyclic
  structural encoding, multi-SCC support, second-interface work, and fallback
  widening remain blocked unless a later accepted roadmap item explicitly
  reopens them.

## Sequential Tasks

### Task 1 - Re-establish the exact `N10` stage contract and docs-only boundary

- Write the future canonical `N10` artifact as `round-077` / `N10` /
  `attempt-1` / `retry: null`.
- State explicitly that `N10` is docs-only safety/acceptance-contract work
  inside accepted `N9 = boundVarTarget-planning-subject-selected`.
- State explicitly that this round records one verifier-checkable contract
  only and does not bind the follow-on exact target, implementation slice, or
  verification slice for this lane.
- Preserve accepted `L1` / `L2` / `N1` through `N9` continuity as binding
  predecessor input.
- Preserve the inherited
  `explicit-only / non-equi-recursive / non-cyclic-graph / no-second-interface / no-fallback`
  boundary unchanged.

### Task 2 - Reconstruct the exact continuity packet and predecessor-only boundary

- Use `selection.md`, `state.json`, `roadmap.md`, `retry-subloop.md`, the
  mechanism table, accepted `L1`, accepted `L2`, accepted `N1` through `N9`,
  the earlier `baseTarget` docs chain, the repaired-queue retained-child
  packet, and `Bugs.md` to restate the exact packet that now constrains
  `N10`.
- Carry forward these fixed inputs:
  - the repaired `URI-R2-C1` queue remains closed predecessor evidence only;
  - the earlier reopened `baseTarget` lane, including its earlier `N2` and
    earlier `N3` artifacts, is predecessor evidence only after accepted `N8`
    and `N9`;
  - the exact accepted non-local `baseTarget -> baseC` packet is predecessor
    evidence only and not fresh-lane clearance;
  - the accepted repaired-queue retained-child packet is predecessor evidence
    only and not automatic carry-forward authority; and
  - open replay / `InstBot` bug pressure remains blocked context only.
- Do not reinterpret accepted predecessor evidence as permission to skip a
  fresh subject-specific contract for the selected `boundVarTarget` lane.

### Task 3 - Ground the contract in thesis-backed binding-structure constraints

- Extract only the thesis evidence needed to justify the contract axes for the
  selected `boundVarTarget` subject:
  - `3.3.3.1 Grafting` for the requirement that transformations over graphic
    types track changes in binding structure;
  - `5.1` for the fact that the `MLF` instance relation is adapted to the
    richer binding structure of graphic types;
  - `8.3.1 Inlining bounds` for the requirement that binder and binding-flag
    reconstruction stay unambiguous and fail closed when shared use or
    non-ancestor binding appears; and
  - `17.3 Perspectives` for the thesis claim that the core difficulty lies in
    recursion in the binding structure rather than in cyclic-graph
    experimentation.
- Use those anchors to justify only contract obligations, not a code path:
  - retained-child ownership and target choice must remain bounded to one
    binding-structure route;
  - nested-`forall` or nested scheme-root crossings must remain fail-closed;
  - recursive meaning must stay explicit-only, iso-recursive, and structurally
    acyclic; and
  - the selected subject must remain one bounded family rather than widening
    into fallback search.

### Task 4 - Freeze the exact code/test subject packet and threatened properties

- Use current read-only source/test anchors to name the selected planning
  subject precisely:
  - `Fallback.hs` lines covering `boundVarTargetRoot`,
    `boundHasForallFrom`, `boundVarTarget`, `sameLocalTypeLane`,
    `keepTargetFinal`, and the `targetC` branch that either selects the
    retained child or falls back through `schemeBodyTarget`;
  - `PipelineSpec.hs` lines covering the retained-child same-lane positive
    case, the retained-child same-lane source guard, and the retained-child
    nested-`forall` fail-closed contrast; and
  - `Scope.hs` lines showing `schemeBodyTarget` remains neighboring
    owner-local context rather than the selected subject itself.
- Translate those anchors into the threatened properties this contract must
  audit before any later exact target bind is lawful:
  - retained-child ownership and parent/child scope discipline;
  - nested-`forall` / nested-owner fail-closed behavior;
  - target-selection confinement between retained child, `schemeBodyTarget`,
    and `rootFinal`;
  - inherited explicit-only / non-equi-recursive / non-cyclic-graph
    boundaries; and
  - single-family confinement with no fallback, replay, or cross-family
    widening.

### Task 5 - Write the canonical `N10` artifact as one bounded safety/acceptance contract

- Create
  `docs/plans/2026-03-23-automatic-iso-recursive-bound-var-target-safety-acceptance-contract.md`.
- The artifact must include:
  - the stage contract freeze for `round-077` / `N10` / `attempt-1` /
    `retry: null`;
  - accepted `L1` / `L2` / `N1` through `N9` continuity as binding input;
  - the thesis basis from the selected sections of
    `papers/these-finale-english.txt`;
  - the exact code/test anchor packet for the selected subject;
  - a bounded invariant-audit section for the selected `boundVarTarget`
    subject;
  - verifier-checkable acceptance criteria for any later exact target bind;
    and
  - an explicit no-go / blocked-boundary section.
- Current planning read: record exactly one accepted outcome:
  `boundVarTarget-safety-acceptance-contract-established`.
- Current planning read: the invariant audit should name, at minimum, these
  axes:
  - retained-child ownership must identify the kept child, its parent/owner
    relation, and why the route remains inside the selected binding-structure
    lane;
  - nested-`forall` / nested scheme-root crossings must stay fail-closed via
    `boundHasForallFrom` rather than being reinterpreted as admissible;
  - target selection must explain when a retained child is chosen and when the
    code stays on `schemeBodyTarget` / `rootFinal` instead, without silently
    widening to another family;
  - recursive meaning must remain explicit-only and iso-recursive rather than
    equi-recursive or implicitly unfolded; and
  - constraint representation must remain structurally acyclic and single
    subject.
- Current planning read: the acceptance criteria should say that a future
  exact target bind is admissible only if it can answer YES to all of the
  following:
  - the proposed target is exactly one bounded slice inside the selected
    `boundVarTarget` route and does not reopen the earlier `baseTarget` lane
    or the repaired queue as live work;
  - the target states a concrete retained-child ownership / binding-structure
    story tied to explicit source/test anchors;
  - the target states concrete fail-closed conditions for nested-`forall` and
    nested-owner crossings rather than assuming them away;
  - the target explains the role of `schemeBodyTarget` as neighboring boundary
    context rather than promoting it to the selected subject;
  - the target preserves the inherited boundary unchanged; and
  - the target states explicit out-of-scope exclusions for every unchanged
    blocked route and does not reinterpret predecessor evidence as fresh
    implementation or verification clearance.

### Task 6 - Keep non-authorization and blocked routes explicit

- State explicitly that `N10` does not authorize:
  - exact target binding;
  - implementation;
  - verification;
  - merge action;
  - roadmap edits;
  - controller-state edits;
  - bug-tracker edits; or
  - predecessor-history rewrites.
- State explicitly that `N10` keeps blocked:
  - the earlier `baseTarget` lane as live work;
  - the exact accepted non-local `baseTarget -> baseC` packet as live work;
  - automatic carry-forward of the earlier `N3` contract;
  - automatic carry-forward of the accepted repaired-queue retained-child
    packet;
  - replay reopen;
  - `MLF.Elab.Inst` or `InstBot`;
  - `boundTarget`;
  - `schemeBodyTarget`;
  - `src/MLF/Elab/Run/ResultType/View.hs`;
  - every other `ResultType.Fallback` family;
  - every different solver or pipeline subject;
  - cross-family search;
  - equi-recursive reasoning;
  - implicit unfolding;
  - cyclic structural graph encoding;
  - multi-SCC support;
  - a second executable interface; and
  - any compatibility, convenience, or default-path fallback widening.
- State explicitly that the selected contract is not exact-target readiness,
  not implementation readiness, and not authority to skip the later gate
  sequence.

### Task 7 - Run the docs-only continuity and contract checks required for `N10`

All commands run in:
`.worktrees/round-077`

Baseline docs/state checks:

- `git branch --show-current`
  - Expect: `codex/round-077-n10-safety-acceptance-contract`
- `git status --short --untracked-files=all`
  - Expect: bounded docs-only round payload only
- `git diff --check`
  - Expect: pass
- `python3 -m json.tool orchestrator/rounds/round-077/state-snapshot.json >/dev/null`
  - Expect: pass
- `rg -n '"contract_version": 2|"retry": null|"retry": \{' orchestrator/rounds/round-077/state-snapshot.json`
  - Expect: `contract_version: 2` and `retry: null`
- `rg -n '^\d+\. \[(pending|in-progress|done)\]' orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-010/roadmap.md`
  - Expect: parseable roadmap with item `10` pending
- Required artifact presence:
  - `test -f docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  - `test -f docs/plans/2026-03-14-unannotated-iso-recursive-r5-research-stop-decision.md`
  - `test -f docs/plans/2026-03-17-uri-r2-c1-r4-repair-decision-gate.md`
  - `test -f docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md`
  - `test -f docs/plans/2026-03-21-uri-r2-c1-l2-post-l1-fail-closed-successor-decision-gate.md`
  - `test -f docs/plans/2026-03-23-automatic-iso-recursive-bound-var-target-next-live-subject-selection.md`
  - `test -f tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
  - `test -f orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-010/retry-subloop.md`
  - `test -f orchestrator/rounds/round-076/review-record.json`

`N10`-specific continuity and contract checks:

- `rg -n '"stage_id": "N9"|"attempt_verdict": "accepted"|"stage_action": "finalize"|"status": "authoritative"|"final_outcome": "boundVarTarget-planning-subject-selected"' orchestrator/rounds/round-076/review-record.json`
  - Expect: accepted `N9` finalization fields present
- `rg -n 'boundVarTarget-planning-subject-selected|predecessor evidence only|does not bind an exact target|does not authorize implementation|does not authorize verification' docs/plans/2026-03-23-automatic-iso-recursive-bound-var-target-next-live-subject-selection.md`
  - Expect: accepted `N9` still preserves planning-only authority and still
    blocks exact binding / implementation / verification
- `rg -n 'earlier reopened lane already has predecessor-only N3 / N4 / N5 / N6 evidence|fresh post-N8 successor lane now selected by accepted N9 still requires its own accepted contract|selected boundVarTarget subject' tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
  - Expect: mechanism-table continuity still requires a fresh lane-specific
    contract
- `rg -n 'boundVarTargetRoot =|boundHasForallFrom start0 =|boundVarTarget =|sameLocalTypeLane child =|keepTargetFinal =|Just v -> v|Nothing -> schemeBodyTarget targetPresolutionView rootC' src/MLF/Elab/Run/ResultType/Fallback.hs`
  - Expect: current retained-child route anchors present
- `rg -n 'schemeBodyTarget :: PresolutionView -> NodeId -> NodeId|isSchemeRoot|schemeRootByBody' src/MLF/Elab/Run/Scope.hs`
  - Expect: `schemeBodyTarget` remains neighboring owner-local context
- `rg -n 'keeps retained-child fallback recursive through a same-lane local TypeRef root|keeps retained-child lookup bounded to the same local TypeRef lane|keeps retained-child fallback fail-closed when the same wrapper crosses a nested forall boundary' test/PipelineSpec.hs`
  - Expect: focused retained-child evidence still present
- `rg -n '3\\.3\\.3\\.1 Grafting|5\\.1 Why rigid quantification|8\\.3\\.1 Inlining bounds|usedtwice|reconstruct the binder and the binding flag|17\\.3 Perspectives|binding structure|cyclic term-graphs' papers/these-finale-english.txt`
  - Expect: thesis anchors cited by the artifact remain present
- `rg -n 'Attempt: `attempt-1`|Retry state: `null`|Invariant audit|Acceptance criteria|No-go|boundVarTargetRoot|boundHasForallFrom|boundVarTarget|keepTargetFinal|schemeBodyTarget|predecessor evidence only|does not bind an exact target|does not authorize implementation|does not authorize verification|boundVarTarget-safety-acceptance-contract-established' docs/plans/2026-03-23-automatic-iso-recursive-bound-var-target-safety-acceptance-contract.md`
  - Expect: the canonical `N10` artifact contains the required contract
    sections and bounded outcome markers

- `cabal build all && cabal test` is intentionally out of scope for this
  round if the diff remains docs-only and does not touch `src/`,
  `src-public/`, `app/`, `test/`, or `mlf2.cabal`. The implementer artifact
  and reviewer record must state that skip reason explicitly rather than
  implying future code clearance.

## Reviewer Handoff

Reviewer should record the following concrete checks for this round:

- `N10-CONTRACT`: the artifact is explicitly framed as `round-077` / `N10` /
  `attempt-1` / `retry: null` and is docs-only contract work only.
- `N10-L1-L2-N1-N9-CONTINUITY`: the artifact treats accepted `L1` / `L2` /
  `N1` through `N9` as binding predecessor continuity.
- `N10-PREDECESSOR-EVIDENCE-ONLY`: the earlier `N2`, earlier `N3`, exact
  accepted non-local `baseTarget -> baseC` packet, and repaired-queue
  retained-child packet are all preserved as predecessor evidence only.
- `N10-THESIS-BASIS`: the artifact grounds its contract axes in
  `3.3.3.1 Grafting`, `5.1`, `8.3.1 Inlining bounds`, and
  `17.3 Perspectives`.
- `N10-CODE-ANCHOR-CONTINUITY`: the artifact identifies the selected subject
  through the current `Fallback.hs`, `Scope.hs`, and `PipelineSpec.hs`
  anchors rather than by vague prose.
- `N10-INVARIANT-AUDIT-AXES`: the artifact freezes reviewer-checkable axes for
  retained-child ownership, nested-`forall` / nested-owner fail-closed
  behavior, target-selection confinement, recursive-boundary preservation, and
  single-family confinement.
- `N10-ACCEPTANCE-CRITERIA`: the artifact defines verifier-checkable YES/NO
  criteria for a later exact target bind without binding that target now.
- `N10-NO-EXACT-BIND`: the artifact does not bind the next exact target and
  does not authorize implementation or verification.
- `N10-NO-IMPLICIT-CLEARANCE`: the artifact does not reinterpret predecessor
  docs, code, or accepted review records as fresh execution clearance.
- `N10-BLOCKED-ROUTES`: replay reopen, `InstBot`, `boundTarget`,
  `schemeBodyTarget`, other fallback families, other subjects, equi-recursive
  reasoning, implicit unfolding, cyclic encoding, multi-SCC, second-interface
  work, and fallback widening remain explicitly blocked.
- `N10-DIFF-BOUNDARY`: the diff stays docs-only and bounded to the canonical
  `N10` artifact plus round-local orchestration inputs.
- `N10-SKIP-NOTE`: the reviewer records why the full Cabal gate stayed out of
  scope for this docs-only round.
- `N10-IMMUTABILITY`: prior attempts and reviewer-owned history remain
  unchanged.
- `N10-RETRY-SCHEMA`: the review output follows the retry-subloop schema even
  though this is `attempt-1` with `retry: null`.
- `N10-SCOPE-ALIGNMENT`: the implemented artifact matches the one intended
  contract outcome only:
  `boundVarTarget-safety-acceptance-contract-established`.
