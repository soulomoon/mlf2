# Round 098 Plan (`item-5` Bounded Current-Architecture Vs `non-cyclic-graph` Decision For The Frozen Same-Lane Retained-Child Pocket)

## Objective

Execute only roadmap item `5` and produce one docs-only aggregate artifact
at:
`docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md`.

This is the initial `item-5` plan for `attempt-1` with `retry: null`. The
round must consume only the accepted refreshed item-4 classification for the
exact frozen pocket and record exactly one authoritative item-5 outcome:

- `blocker debt remains within the current architecture`; or
- `reopen the non-cyclic-graph revision question`.

The exact frozen pocket remains:

- family: same-lane retained-child;
- recursive-shape anchor: `boundVarTargetRoot`;
- owner / binder frame: one owner-local retained-child frame;
- route:
  `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC`; and
- quantified-boundary status: clear-boundary only.

The exact frozen packet remains:

```haskell
ELet "k" (ELamAnn "x" recursiveAnn (EVar "x"))
  (ELet "u" (EApp (ELam "y" (EVar "y")) (EVar "k")) (EVar "u"))
```

where
`recursiveAnn = STMu "a" (STArrow (STVar "a") (STBase "Int"))`.

Current planning read: the accepted refreshed evidence is still stronger for
`blocker debt remains within the current architecture`, because the exact
pocket still survives solver admission, exact elaboration clearance,
reconstruction, helper-visible internal recursive visibility, and refreshed
end-to-end revalidation inside the inherited acyclic model, while the
accepted blocker is still the authoritative public-output collapse to
`TForall "a" Nothing (TVar "a")`. Item `5` must still evaluate both
branches explicitly, but it may reopen `non-cyclic-graph` only if the
accepted exact-pocket record now specifically forces that stronger read.

This round is docs-only, aggregate-only, and decision-only. It must not edit
production code, tests, public surfaces, executables, `mlf2.cabal`,
`orchestrator/state.json`, roadmap files, retry / verification contracts,
`Bugs.md`, or completed predecessor history.

This round must not silently widen into:

- another freeze, path audit, blocker-proof slice, or end-to-end rerun;
- the non-local alias-bound family;
- neighboring consumer routes;
- nested-`forall`, nested owner, or nested scheme-root positive success;
- replay / `InstBot` repair or `BUG-2026-03-16-001`;
- broad automatic recursive inference, equi-recursive reasoning, cyclic
  structural graphs, multi-SCC search, second-interface work, or fallback
  widening; or
- a broader architecture redesign beyond the one explicit
  `current architecture` versus `non-cyclic-graph` decision for this exact
  pressure point.

## Locked Round Context

- Round id: `round-098`
- Roadmap item: `item-5`
- Stage: `plan`
- Active attempt: `attempt-1`
- Retry state: `null`
- Active branch: `codex/round-098`
- Active worktree:
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-098`
- Fixed live subject: one exact same-lane retained-child pocket already
  classified as
  `admitted but not reconstruction-visible / blocker debt`, now awaiting the
  bounded architecture-pressure decision only
- Stage mode: one bounded decision slice only
- Current round review feedback: none yet; no `review.md`,
  `reviews/attempt-<n>.md`, or `review-record.json` exists for `round-098`,
  so this is a full `attempt-1` plan rather than a retry delta

Current worktree state is already non-pristine. Respect existing edits and do
not revert unrelated work:

- `M orchestrator/state.json` (controller-owned; must remain untouched)
- `?? orchestrator/rounds/round-098/selection.md`

Item `5` is aggregate-only under the live retry contract. Review may reject
and send the same round back to `plan`, but review may not emit
`accepted + retry` for this item.

## Accepted Continuity That Remains Binding

- `orchestrator/rounds/round-098/selection.md`
  already fixes this round to roadmap item `5` only, preserves the exact
  frozen tuple and packet, and limits the lawful decision to:
  keep blocker debt inside the current architecture or reopen only the
  `non-cyclic-graph` revision question.
- `orchestrator/state.json`
  fixes the live controller state at `active_round_id = round-098`,
  `stage = plan`, `current_task = item-5`, `retry = null`,
  `roadmap_id = 2026-03-26-00-same-lane-retained-child-public-output-vs-non-cyclic-graph-successor-orchestrator-roadmap`,
  `roadmap_revision = rev-001`, and the authoritative
  `roadmap_dir = orchestrator/roadmaps/2026-03-26-00-same-lane-retained-child-public-output-vs-non-cyclic-graph-successor-orchestrator-roadmap/rev-001`.
- `orchestrator/roadmaps/2026-03-26-00-same-lane-retained-child-public-output-vs-non-cyclic-graph-successor-orchestrator-roadmap/rev-001/roadmap.md`
  makes item `5` the only unfinished dependency-satisfied item and defines
  its completion notes as this exact bounded architecture-pressure decision
  only.
- `orchestrator/roadmaps/2026-03-26-00-same-lane-retained-child-public-output-vs-non-cyclic-graph-successor-orchestrator-roadmap/rev-001/retry-subloop.md`
  keeps item `5` aggregate-only and forbids `accepted + retry`.
- `orchestrator/roadmaps/2026-03-26-00-same-lane-retained-child-public-output-vs-non-cyclic-graph-successor-orchestrator-roadmap/rev-001/verification.md`
  requires item-5-specific proof that the round consumes only the accepted
  exact-pocket item-4 result and records exactly one bounded architecture
  outcome for this pressure point.
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  preserves the inherited boundary:
  explicit-only recursive behavior, iso-recursive meaning, non-equi-recursive
  semantics, structurally acyclic encoding, and no fallback widening.
- `docs/plans/2026-03-24-automatic-iso-recursive-bound-var-target-same-lane-retained-child-next-cycle-decision-gate.md`
  preserves accepted `N14` as bounded predecessor evidence only for one exact
  same-lane retained-child packet. It does not pre-clear broad capability and
  does not itself force architecture revision.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md`
  is the accepted item-2 architecture audit. Its binding classification set
  remains:
  `iso-recursive = keep`,
  `non-equi-recursive = keep`,
  `non-cyclic-graph = unknown`,
  and `no-fallback = keep`.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md`
  defines the persistence tuple, the phase-and-surface ledger, and the
  lawful blocker-debt vocabulary that item `4` reused for this same pocket.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`
  keeps the same-lane retained-child family as bounded blocker debt rather
  than accepted reconstruction-visible success, and does not upgrade it into
  proof that the current architecture is already a dead end.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architecture-decision-and-successor-plan-choice.md`
  is the accepted item-7 strategic decision. Its authoritative result remains
  `continue within the current architecture`, and it does not already reopen
  `non-cyclic-graph`.
- `docs/plans/2026-03-25-same-lane-retained-child-stable-visible-persistence-successor-decision-gate.md`
  and `orchestrator/rounds/round-093/review-record.json`
  remain the immediate predecessor item-5 decision for the same exact pocket:
  blocker debt previously remained within the current architecture on weaker
  accepted evidence.
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md`
  freezes the refreshed exact tuple and six-row ledger that later rounds must
  preserve.
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-path-audit.md`
  fixes the unchanged exact public-output collapse anchor at
  `checkedAuthoritative`, with `termClosed` and `typeCheck termClosed` as the
  same-pocket dependencies.
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-collapse-clear-or-confirm.md`
  confirms that the bounded `Pipeline.hs` / `TermClosure.hs` root-handoff
  slice contains no alternate recursive whole-packet authoritative result,
  so the exact public-output collapse remains blocker debt within the
  unchanged current architecture.
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md`
  and `orchestrator/rounds/round-097/review-record.json`
  finalize the refreshed exact item-4 read for this pocket:
  helper-visible internal recursion still carries `TMu ...` and
  `containsMu True`, but both authoritative public outputs still collapse to
  `TForall "a" Nothing (TVar "a")`, so the pocket remains
  `admitted but not reconstruction-visible / blocker debt`.
- `orchestrator/rounds/round-094/review-record.json`,
  `orchestrator/rounds/round-095/review-record.json`,
  and `orchestrator/rounds/round-096/review-record.json`
  keep the refreshed item-1 freeze, refreshed item-2 unchanged-anchor audit,
  and refreshed item-3 confirm-only blocker proof authoritative.
- `Bugs.md`
  still lists open `BUG-2026-03-16-001`, but that replay / `InstBot` defect
  remains predecessor context only and does not authorize replay repair,
  subject widening, or a detour away from this aggregate decision.

## File Map

### Create

- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md`
  - Responsibility: canonical docs-only item-5 decision artifact that
    consumes the accepted refreshed item-4 blocker-debt classification for
    the exact frozen pocket, evaluates only the two lawful item-5 outcomes,
    records exactly one authoritative outcome, and explains why the
    non-selected branch is weaker on current evidence.

- `orchestrator/rounds/round-098/implementation-notes.md`
  - Responsibility: concise round-local notes mirroring the same exact
    frozen tuple, the exact accepted item-4 blocker-debt input, the selected
    item-5 outcome, and the explicit note that no scope widening or
    architecture redesign was accepted here.

### Read-Only Evidence

- `orchestrator/rounds/round-098/selection.md`
- `orchestrator/state.json`
- `orchestrator/roadmaps/2026-03-26-00-same-lane-retained-child-public-output-vs-non-cyclic-graph-successor-orchestrator-roadmap/rev-001/roadmap.md`
- `orchestrator/roadmaps/2026-03-26-00-same-lane-retained-child-public-output-vs-non-cyclic-graph-successor-orchestrator-roadmap/rev-001/retry-subloop.md`
- `orchestrator/roadmaps/2026-03-26-00-same-lane-retained-child-public-output-vs-non-cyclic-graph-successor-orchestrator-roadmap/rev-001/verification.md`
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
- `docs/plans/2026-03-24-automatic-iso-recursive-bound-var-target-same-lane-retained-child-next-cycle-decision-gate.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architecture-decision-and-successor-plan-choice.md`
- `docs/plans/2026-03-25-same-lane-retained-child-stable-visible-persistence-successor-decision-gate.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-path-audit.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-collapse-clear-or-confirm.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md`
- `orchestrator/rounds/round-093/review-record.json`
- `orchestrator/rounds/round-094/review-record.json`
- `orchestrator/rounds/round-095/review-record.json`
- `orchestrator/rounds/round-096/review-record.json`
- `orchestrator/rounds/round-097/review-record.json`
- `Bugs.md`

### Preserve Unchanged

- `orchestrator/state.json`
- `orchestrator/roadmap.md`
- `orchestrator/retry-subloop.md`
- `orchestrator/verification.md`
- `Bugs.md`
- `src/`
- `test/`
- `src-public/`
- `app/`
- `mlf2.cabal`
- the accepted round-093 through round-097 artifacts and review records
- reviewer-owned history under earlier round directories

## Exact Selected Slice (Exactly One)

The only selected slice is:

record the bounded architecture-pressure decision for the exact frozen
same-lane retained-child pocket, and nothing else.

This slice is allowed to:

- restate the accepted decision inputs from the inherited boundary docs, the
  accepted strategic item-2 / item-5 / item-6 / item-7 record, the earlier
  accepted item-5 predecessor decision, and the refreshed item-1 through
  item-4 exact-pocket chain;
- evaluate only the two lawful item-5 branches named by the roadmap:
  `blocker debt remains within the current architecture` or
  `reopen the non-cyclic-graph revision question`;
- choose exactly one of those two outcomes; and
- explain why the non-selected outcome is weaker on the accepted evidence.

This slice is not allowed to:

- relitigate the refreshed case freeze, refreshed path audit, refreshed
  blocker-proof slice, or refreshed end-to-end classification;
- upgrade the pocket to `stable visible persistence`;
- widen into the alias-bound family, neighboring routes, nested-`forall`
  success, replay repair, or a general automatic-recursive-inference claim;
- authorize code changes, test expansion, a second interface, fallback
  behavior, cyclic structural graphs, or multi-SCC search; or
- reopen any architectural boundary other than the one explicit
  `non-cyclic-graph` question.

If the decision starts depending on fresh runtime evidence, cross-family
widening, or speculative architecture theory that the accepted record does
not already contain, the artifact must fail closed by keeping blocker debt
inside the current architecture rather than reopening `non-cyclic-graph` on
guesswork.

## Sequential Tasks

### Task 1 - Freeze the item-5 aggregate decision frame

- Create the canonical item-5 artifact at the path above.
- State explicitly that item `5` is docs-only, aggregate-only, and
  `attempt-1` with `retry: null`.
- Reassert the exact frozen tuple and exact frozen packet unchanged.
- State explicitly that the accepted refreshed item-4 outcome for this
  pocket is already fixed:
  `admitted but not reconstruction-visible / blocker debt`.
- State explicitly that item `5` now decides only what that blocker-debt
  read means for the bounded architecture-pressure posture:
  remain blocker debt within the current architecture, or reopen only the
  `non-cyclic-graph` revision question.
- State explicitly that item `5` does not authorize a new runtime repair,
  another validation rerun, or a broader architecture redesign.

### Task 2 - Assemble one accepted decision-input ledger for the exact pocket

- Pull the decisive accepted inputs into one compact evidence ledger. It must
  include at least:
  - the inherited baseline boundary;
  - the item-2 architectural classification set
    (`keep`, `keep`, `unknown`, `keep`);
  - the item-7 authoritative outcome
    `continue within the current architecture`;
  - the earlier round-093 exact-pocket successor decision that kept blocker
    debt within the current architecture on the weaker predecessor record;
  - the refreshed round-094 freeze of the exact pocket and six-row ledger;
  - the refreshed round-095 unchanged-anchor audit at
    `checkedAuthoritative`;
  - the refreshed round-096 confirm-only blocker proof that the bounded root
    handoff contains no alternate recursive whole-packet authoritative
    result; and
  - the refreshed round-097 accepted blocker-debt classification:
    helper-visible internal recursion persists (`TMu ...`, `containsMu True`)
    while both authoritative public outputs collapse to
    `TForall "a" Nothing (TVar "a")`.
- Keep this ledger evidence-only. It must not yet choose the item-5 outcome
  or invent stronger claims than the accepted record supports.
- Keep `BUG-2026-03-16-001` in the ledger only as predecessor context. It is
  not a decision input that can flip item `5` into replay repair or a
  different live subject.

### Task 3 - Evaluate the two lawful item-5 branches explicitly

- Require the artifact to evaluate both candidate outcomes in one
  reviewer-visible schema such as:
  outcome token, supporting evidence, blocking evidence, and lawful-status
  read.
- For `blocker debt remains within the current architecture`, test only
  whether the accepted evidence supports all of the following:
  - the exact pocket still survives solver admission, exact elaboration
    clearance, reconstruction, helper-visible internal recursive visibility,
    and refreshed end-to-end revalidation inside the inherited acyclic
    model;
  - no accepted artifact currently classifies `non-cyclic-graph` as
    `revise` or proves that cyclic / multi-SCC structure is required for
    this exact pocket; and
  - the accepted public-output collapse is frozen as a bounded public-surface
    blocker for this exact pocket, not yet as accepted proof that the
    acyclic representation itself is impossible.
- For `reopen the non-cyclic-graph revision question`, allow selection only
  if the accepted exact-pocket record can specifically prove all of the
  following:
  - this exact public-output collapse is forced by the inherited acyclic
    representation itself rather than by bounded blocker debt inside the
    current architecture;
  - that stronger read follows from the exact frozen pocket only, not from
    unresolved general `P2`-`P6` pressure or from a different family; and
  - the reopen stays limited to `non-cyclic-graph` only, without broad
    redesign, equi-recursive semantics, fallback behavior, or multi-SCC
    search.
- Reject any branch that depends on fresh code-path experiments, cross-family
  widening, or speculation beyond the accepted record.

### Task 4 - Record exactly one authoritative item-5 outcome

- Choose exactly one of the two item-5 outcomes and state it as the sole
  authoritative round result.
- Tie that choice directly to the decision ledger from Task 2 and the branch
  evaluation from Task 3.
- Current preferred lawful read unless Task 3 proves otherwise:
  `blocker debt remains within the current architecture`.
- If the selected outcome is
  `blocker debt remains within the current architecture`, require the
  artifact to say all of the following:
  - the exact pocket stays
    `admitted but not reconstruction-visible / blocker debt`;
  - the accepted strategic posture
    `continue within the current architecture`
    remains intact for this exact pocket; and
  - `non-cyclic-graph = unknown` remains architecture-pressure context only
    and is not reopened by this exact public-output collapse.
- If the selected outcome is
  `reopen the non-cyclic-graph revision question`, require the artifact to
  say all of the following:
  - this exact pocket specifically forces reopening that one boundary;
  - the previously accepted continuation posture is no longer the strongest
    lawful read for this exact pocket; and
  - the reopen is bounded to `non-cyclic-graph` only and does not silently
    widen into a broader redesign.
- In either branch, require the artifact to explain why the non-selected
  outcome is weaker on current accepted evidence.
- Do not let the artifact invent a second successor lane. This round closes
  the current bounded architecture-pressure decision only.

### Task 5 - Verification and docs-only gate

Run the baseline docs-first checks:

- `git diff --check`
- `python3 -m json.tool orchestrator/state.json >/dev/null`
- `rg -n '"contract_version": 2|"roadmap_id":|"roadmap_revision":|"roadmap_dir":|"retry": null|"retry": \{' orchestrator/state.json`
- `roadmap_dir="$(jq -r '.roadmap_dir' orchestrator/state.json)" && test -f "$roadmap_dir/roadmap.md" && test -f "$roadmap_dir/retry-subloop.md" && test -f "$roadmap_dir/verification.md"`
- `roadmap_dir="$(jq -r '.roadmap_dir' orchestrator/state.json)" && rg -n '^\d+\. \[(pending|in-progress|done)\]' "$roadmap_dir/roadmap.md"`
- `test -f docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
- `test -f docs/plans/2026-03-24-automatic-iso-recursive-bound-var-target-same-lane-retained-child-next-cycle-decision-gate.md`
- `test -f docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md`
- `test -f docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md`
- `test -f docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`
- `test -f docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architecture-decision-and-successor-plan-choice.md`
- `test -f docs/plans/2026-03-25-same-lane-retained-child-stable-visible-persistence-successor-decision-gate.md`
- `test -f docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md`
- `test -f docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-path-audit.md`
- `test -f docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-collapse-clear-or-confirm.md`
- `test -f docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md`
- `test -f orchestrator/rounds/round-093/review-record.json`
- `test -f orchestrator/rounds/round-094/review-record.json`
- `test -f orchestrator/rounds/round-095/review-record.json`
- `test -f orchestrator/rounds/round-096/review-record.json`
- `test -f orchestrator/rounds/round-097/review-record.json`

Confirm the round stayed docs-only:

- `git diff --name-only -- src test src-public app mlf2.cabal`
  and confirm it is empty
- `git diff --name-only -- orchestrator/state.json orchestrator/roadmap.md orchestrator/retry-subloop.md orchestrator/verification.md Bugs.md`
  and confirm it reports only the pre-existing controller-owned
  `orchestrator/state.json` drift, with no round-owned edits to roadmap,
  retry contract, verification contract, or `Bugs.md`

Confirm the canonical item-5 artifact records the exact bounded decision
shape:

- `rg -n 'Stage Contract Freeze|Accepted Decision-Input Ledger|Outcome Evaluation Schema|blocker debt remains within the current architecture|reopen the non-cyclic-graph revision question|boundVarTargetRoot|sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC|containsMu True|TForall "a" Nothing \(TVar "a"\)|continue within the current architecture|non-cyclic-graph = unknown|review may reject and send the same round back to `plan`|accepted \+ retry' docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md`

This round is expected to change only documentation. No focused or full
`cabal` gate is triggered unless the diff escapes that authorized docs-only
surface. If any non-doc drift appears under `src/`, `test/`, `src-public/`,
`app/`, or `mlf2.cabal`, stop and re-plan instead of silently widening the
round.
