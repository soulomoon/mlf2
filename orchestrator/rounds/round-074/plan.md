# Round 074 Plan (`N7` Closure / Next-Cycle Decision Gate For The Accepted `N6` Non-Local `baseTarget -> baseC` Evidence Chain)

## Objective

Execute only roadmap item `N7` and produce one accepted aggregate-only
docs-only decision artifact at:
`docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-next-cycle-decision-gate.md`.

This is the initial `N7` plan for `attempt-1` with `retry: null`. The round
must aggregate the accepted `N6` evidence chain for the exact accepted
non-local generic scheme-root alias-bound / base-like `baseTarget -> baseC`
packet into exactly one authoritative reopened-loop outcome:

- `continue-bounded`
- `stop-blocked`
- `completed`

`N7` is aggregate-only under
`orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-007/retry-subloop.md`.
Reviewer may emit only:

- `accepted + finalize`
- `rejected + retry`

`accepted + retry` is forbidden for `N7`.

This round is docs-only and decision-only. It must not edit production code,
tests, public surfaces, executables, `mlf2.cabal`,
`orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-007/roadmap.md`,
`orchestrator/rounds/round-074/state-snapshot.json`,
`Bugs.md`, or any predecessor-history
artifact.

Current planning read: absent a contradiction in the accepted continuity chain
or a new blocker intersecting the exact accepted packet, the expected `N7`
result is `continue-bounded`, because accepted `N6` already shows the exact
packet green and bounded while the long-horizon automatic iso-recursive
inference goal remains unresolved. `stop-blocked` and `completed` remain
lawful only if read-only continuity evidence forces them.

## Locked Round Context

- Round id: `round-074`
- Roadmap item: `N7`
- Stage: `plan`
- Active attempt: `attempt-1`
- Retry state: `null`
- Fixed live subject: aggregate accepted `N6` evidence for the exact accepted
  non-local generic scheme-root alias-bound / base-like
  `baseTarget -> baseC` packet only
- Active branch: `codex/round-074-n7-closure-decision`
- Active worktree:
  `.worktrees/round-074`
- Stage mode: one aggregate-only docs-only closure / next-cycle decision gate
- Current round review feedback: none yet; this is a full `attempt-1` plan,
  not a retry delta

Current round worktree state is already non-pristine. Respect existing edits
and do not revert unrelated work:

- `?? orchestrator/rounds/round-074/selection.md`

## Accepted Continuity That Remains Binding

- `orchestrator/rounds/round-074/selection.md`
  already fixes this round to `N7` only, makes the round aggregate-only,
  forbids `accepted + retry`, and keeps replay reopen, new implementation,
  widening, roadmap/state edits, and bug-tracker edits out of scope.
- `orchestrator/rounds/round-074/state-snapshot.json` fixes the
  live controller state at `active_round_id: "round-074"`, `stage: "plan"`,
  `current_task: "N7"`, branch
  `codex/round-074-n7-closure-decision`, and `retry: null`.
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-007/roadmap.md` makes
  `N7` the first pending item after accepted `N6`. This round must therefore
  close the current reopened-loop packet rather than reopen earlier items or
  invent a new target.
- `tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
  still records `N7 = NO`, still treats `N7` as the long-horizon closure row,
  and still says it advances only through verified progress on `N1` through
  `N6`.
- `docs/plans/2026-03-22-automatic-iso-recursive-post-l2-roadmap-amendment-authority-gate.md`
  remains the accepted `N1` record with one bounded outcome,
  `reopen-planning-only`.
- `docs/plans/2026-03-22-automatic-iso-recursive-next-live-subject-selection.md`
  remains the accepted `N2` record selecting exactly one planning subject:
  the preserved generic scheme-alias / base-like `baseTarget` route only.
- `docs/plans/2026-03-22-automatic-iso-recursive-base-target-safety-acceptance-contract.md`
  remains the accepted `N3` contract and still binds the reopened loop:
  alias-bound ownership, inverse-translation-safe bound inlining,
  structural/variance binding-flag reconstruction, explicit-only recursive
  meaning, and confinement to one preserved non-local `baseTarget` packet.
- `docs/plans/2026-03-22-automatic-iso-recursive-base-target-exact-target-bind.md`
  remains the accepted `N4` bind freezing exactly one packet: the preserved
  non-local generic scheme-root alias-bound / base-like
  `baseTarget -> baseC` packet plus its same-lane downstream `targetC`
  consumer.
- `docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-bounded-implementation-slice.md`
  remains the accepted `N5` implementation record for exactly one explicit
  non-local proof slice.
- `docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-bounded-verification-gate.md`
  remains the accepted `N6` evidence gate for that exact `N5` slice, including
  fresh focused/full green verification and explicit non-authorization of
  `N7` widening.
- `orchestrator/rounds/round-072/review-record.json`
  and
  `orchestrator/rounds/round-073/review-record.json`
  remain the authoritative acceptance proof that `N5` and `N6` finalized as
  accepted authoritative artifacts on attempt `1`.
- `orchestrator/rounds/round-072/review.md`
  and
  `orchestrator/rounds/round-073/review.md`
  remain the reviewer-owned evidence sources for the bounded slice,
  verification commands, and continuity checks.
- `Bugs.md` still carries open
  `BUG-2026-03-16-001`, but that replay / `InstBot` defect remains read-only
  predecessor context only and does not itself authorize replay reopen,
  `MLF.Elab.Inst`, or any widened subject here.
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-007/retry-subloop.md`
  keeps prior attempts immutable and forbids `accepted + retry` for `N7`.

## File Map

### Modify

- `docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-next-cycle-decision-gate.md`
  - Responsibility: canonical `N7` decision artifact recording exactly one
    authoritative reopened-loop outcome from the accepted `N6` evidence chain,
    plus why the other two outcomes are not lawful.

### Optional Modify

- `orchestrator/rounds/round-074/implementation-notes.md`
  - Responsibility: bounded command transcript / continuity notes only if the
    aggregate decision needs a round-local home for long command output.

### Read-Only Evidence

- `orchestrator/rounds/round-074/selection.md`
- `orchestrator/rounds/round-074/state-snapshot.json`
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-007/roadmap.md`
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-007/verification.md`
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-007/retry-subloop.md`
- `tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
- `Bugs.md`
- `docs/plans/2026-03-22-automatic-iso-recursive-post-l2-roadmap-amendment-authority-gate.md`
- `docs/plans/2026-03-22-automatic-iso-recursive-next-live-subject-selection.md`
- `docs/plans/2026-03-22-automatic-iso-recursive-base-target-safety-acceptance-contract.md`
- `docs/plans/2026-03-22-automatic-iso-recursive-base-target-exact-target-bind.md`
- `docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-bounded-implementation-slice.md`
- `docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-bounded-verification-gate.md`
- `orchestrator/rounds/round-072/selection.md`
- `orchestrator/rounds/round-072/plan.md`
- `orchestrator/rounds/round-072/review.md`
- `orchestrator/rounds/round-072/review-record.json`
- `orchestrator/rounds/round-072/merge.md`
- `orchestrator/rounds/round-073/selection.md`
- `orchestrator/rounds/round-073/plan.md`
- `orchestrator/rounds/round-073/review.md`
- `orchestrator/rounds/round-073/review-record.json`
- `orchestrator/rounds/round-073/merge.md`

### Preserve Unchanged

- `src/`
- `test/`
- `src-public/`
- `app/`
- `mlf2.cabal`
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-007/roadmap.md`
- `orchestrator/rounds/round-074/state-snapshot.json`
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-007/retry-subloop.md`
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-007/verification.md`
- `Bugs.md`
- `orchestrator/rounds/round-074/selection.md`
- reviewer-owned history under
  `orchestrator/rounds/round-001/`
  through
  `orchestrator/rounds/round-073/`

## Exact Selected `N7` Slice (Exactly One)

The only selected `N7` slice is:

aggregate the accepted `N6` evidence chain for the exact accepted non-local
generic scheme-root alias-bound / base-like `baseTarget -> baseC` packet into
exactly one authoritative reopened-loop result token only, without selecting a
new target, reopening code work, rerunning code-path verification, or widening
the live subject.

Required interpretation of that one bounded slice:

- `N7` is not another implementation slice and not another verification gate.
- `N7` must treat accepted `N6` as the current bounded verification baseline
  for the exact accepted `N5` slice.
- `N7` may run docs-only continuity checks to confirm that the accepted
  `N1` through `N6` chain is still authoritative and that no code/public/exe/
  Cabal drift has invalidated the accepted `N6` verification baseline.
- `N7` must not rerun `cabal build all && cabal test` or the focused
  `ARI-C1` command. Fresh code-path verification already belongs to accepted
  `N6`, and no code-path edits are authorized in this round.
- `N7` must record exactly one token and explicitly explain why the other two
  tokens are not lawful on the same evidence packet.
- If the recorded token is not `completed`, the artifact must say exactly what
  remains blocked and whether a separate future roadmap amendment / update is
  required before more work can begin.

Everything else remains out of scope, including:

- repaired `URI-R2-C1` as live work;
- replay reopen;
- `MLF.Elab.Inst` and `InstBot`;
- `boundVarTarget`, `boundTarget`, and `schemeBodyTarget`;
- `src/MLF/Elab/Run/ResultType/View.hs`;
- every other fallback family;
- every different solver/pipeline subject;
- cross-family search;
- equi-recursive reasoning or implicit unfolding;
- cyclic structural graph encoding, graph-cycle exceptions, or multi-SCC
  support; and
- any second interface, compatibility shim, convenience fallback, or
  default-path widening.

## Required Evidence And Verification Inputs

All commands run in:
`.worktrees/round-074`

### Baseline docs/state checks

- `git branch --show-current`
  - Expect: `codex/round-074-n7-closure-decision`
- `git status --short --untracked-files=all`
  - Expect: bounded docs-only round payload only
- `git diff --check`
  - Expect: pass
- `python3 -m json.tool orchestrator/rounds/round-074/state-snapshot.json >/dev/null`
  - Expect: pass
- `rg -n '"contract_version": 2|"retry": null|"retry": \{' orchestrator/rounds/round-074/state-snapshot.json`
  - Expect: `contract_version: 2` and `retry: null`
- `rg -n '^\d+\. \[(pending|in-progress|done)\]' orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-007/roadmap.md`
  - Expect: parseable roadmap with `N7` pending
- Required artifact presence:
  - `test -f docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  - `test -f docs/plans/2026-03-14-unannotated-iso-recursive-r5-research-stop-decision.md`
  - `test -f docs/plans/2026-03-17-uri-r2-c1-r4-repair-decision-gate.md`
  - `test -f docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md`
  - `test -f docs/plans/2026-03-21-uri-r2-c1-l2-post-l1-fail-closed-successor-decision-gate.md`
  - `test -f docs/plans/2026-03-22-automatic-iso-recursive-post-l2-roadmap-amendment-authority-gate.md`
  - `test -f docs/plans/2026-03-22-automatic-iso-recursive-next-live-subject-selection.md`
  - `test -f docs/plans/2026-03-22-automatic-iso-recursive-base-target-safety-acceptance-contract.md`
  - `test -f docs/plans/2026-03-22-automatic-iso-recursive-base-target-exact-target-bind.md`
  - `test -f docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-bounded-implementation-slice.md`
  - `test -f docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-bounded-verification-gate.md`
  - `test -f tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
  - `test -f orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-007/retry-subloop.md`

### Accepted-chain continuity checks

- `python3 -m json.tool orchestrator/rounds/round-072/review-record.json >/dev/null`
- `python3 -m json.tool orchestrator/rounds/round-073/review-record.json >/dev/null`
- Short `python3` continuity assertion over accepted review records for
  `round-068` through `round-073`
  - Expect: `N1`, `N2`, `N3`, `N4`, `N5`, and `N6` all
    `accepted finalize authoritative` with the expected canonical artifact
    paths
- `rg -n '20 examples, 0 failures|1141 examples, 0 failures|does not itself decide N7|Explicit Non-Authorization' docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-bounded-verification-gate.md orchestrator/rounds/round-073/review.md`
  - Expect: accepted `N6` evidence still records the fresh focused/full green
    runs and the explicit statement that `N6` does not itself decide `N7`
- `rg -n 'baseTarget-non-local-proof-slice-established|rootNonLocalSchemeAliasBaseLike|20 examples, 0 failures|1141 examples, 0 failures' docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-bounded-implementation-slice.md orchestrator/rounds/round-072/review.md orchestrator/rounds/round-072/review-record.json`
  - Expect: accepted `N5` implementation slice still matches the bounded
    packet under review

### Current no-drift checks for the accepted `N6` baseline

- `git diff --name-only -- src test src-public app mlf2.cabal`
  - Expect: no output
- `git diff --name-only -- . ':(exclude)docs/**' ':(exclude)orchestrator/**'`
  - Expect: no output
- `rg -n 'BUG-2026-03-16-001|Status: Open' Bugs.md`
  - Expect: open replay / `InstBot` bug still present as predecessor context
    only

No fresh code-path gate is required for `N7`. The accepted `N6` artifact is
the current bounded verification baseline, and `N7` must remain docs-only.
The implementer should record that gate skip explicitly in the canonical
artifact so reviewer can confirm the skip was deliberate and lawful.

## Decision Criteria

Record exactly one token. Use the matrix below without widening it.

### `continue-bounded`

Record `continue-bounded` only if all of the following hold:

1. the accepted `N1` through `N6` artifacts and review records are all present,
   authoritative, and internally consistent;
2. accepted `N6` still records the exact accepted `N5` slice as green and
   bounded, including focused `ARI-C1` green evidence and a green full repo
   gate;
3. current `round-074` continuity shows no tracked drift under `src/`, `test/`,
   `src-public/`, `app/`, or `mlf2.cabal` that would stale the accepted `N6`
   verification baseline;
4. the open replay / `InstBot` bug and every other blocked route remain outside
   the exact accepted `N5` / `N6` packet rather than invalidating it; and
5. the long-horizon `N7` goal is still not fully achieved, so `completed` is
   not proven by the accepted evidence.

If those conditions hold, the canonical artifact must conclude:

- the reopened loop has produced one successful bounded `baseTarget -> baseC`
  packet plus accepted bounded verification evidence;
- the long-horizon automatic iso-recursive inference goal remains unresolved
  and therefore is not `completed`;
- the lawful aggregate result is `continue-bounded`, not `stop-blocked`;
- future work still requires a separate future roadmap amendment / update that
  appends the next bounded cycle before any new target, implementation slice,
  or verification slice can begin; and
- replay reopen, `MLF.Elab.Inst`, `InstBot`, accepted local lanes,
  `boundVarTarget`, `boundTarget`, `schemeBodyTarget`, `ResultType.View`,
  every other fallback family, every different solver/pipeline subject,
  cross-family search, equi-recursive reasoning, cyclic encoding,
  multi-SCC support, second-interface work, and no-fallback widening remain
  blocked.

### `stop-blocked`

Record `stop-blocked` only if at least one blocker below is actually found in
the accepted evidence or current continuity:

1. a required accepted artifact or authoritative review record is missing;
2. the accepted `N1` through `N6` chain is internally contradictory;
3. current no-drift checks show code/public/exe/Cabal changes that make the
   accepted `N6` verification baseline stale for the exact packet; or
4. a blocker now intersects the exact accepted packet so directly that no
   lawful next bounded cycle can begin inside the inherited boundary without
   reopening an out-of-scope route or amending the roadmap first.

If `stop-blocked` is recorded, the canonical artifact must:

- name the exact blocker and the exact artifact/continuity check that proves it;
- state why that blocker invalidates `continue-bounded` for this packet;
- state why `completed` is still not lawful; and
- state whether future work would require a separate future roadmap amendment /
  update, plus any prerequisite blocker resolution, before more work can
  begin.

### `completed`

Record `completed` only if the accepted `N1` through `N6` evidence already
proves, without any further implementation, verification, roadmap amendment,
or widening, that the long-horizon `N7` goal is satisfied thesis-faithfully
inside the current solver/pipeline boundary.

That means the evidence must already prove all of the following:

1. automatic iso-recursive type synthesis is now achieved, not merely one
   bounded packet;
2. no further bounded cycle is needed inside the current active subject;
3. no still-blocked route stands between current state and the long-horizon
   goal; and
4. the inherited explicit-only / non-equi-recursive / non-cyclic-graph /
   no-second-interface / no-fallback boundary is preserved.

Current planning read does not support `completed`. The implementer may record
it only if the read-only accepted evidence unexpectedly proves all of the above
without inference stretch.

## Sequential Tasks

### Task 1 - Freeze the `N7` `attempt-1` aggregate-only contract

- Create the canonical `N7` artifact at the path named above.
- Frame it explicitly as `round-074` / `N7` / `attempt-1` / `retry: null`.
- State that `N7` is aggregate-only, docs-only, and decision-only.
- State that reviewer may finalize or reject+retry, but may not use
  `accepted + retry`.

### Task 2 - Reconfirm the accepted `N1` through `N6` authority chain

- Reconfirm the canonical `N1` through `N6` docs in order.
- Reconfirm authoritative review records for `round-068` through `round-073`.
- Reconfirm that `round-072` and `round-073` reviewer-owned artifacts remain
  the accepted evidence sources for the exact bounded slice and its fresh green
  verification.

### Task 3 - Reconfirm current docs-level continuity without reopening code work

- Run the baseline docs/state checks and required artifact-presence checks.
- Run the no-drift checks proving `round-074` has not changed the code/public/
  exe/Cabal surfaces since accepted `N6`.
- Reconfirm `BUG-2026-03-16-001` remains replay-only predecessor context rather
  than a blocker that directly invalidates the exact accepted packet.
- Record the lawful skip note for fresh code-path verification:
  accepted `N6` remains the current bounded verification baseline and this
  round is docs-only.

### Task 4 - Record exactly one decision token using the matrix above

- Apply the decision criteria exactly as written.
- Record one token only.
- Explain why the chosen token is lawful.
- Explain why the other two tokens are not lawful on the same evidence packet.

### Task 5 - State preserved continuity, remaining blockers, and future-gate needs

- Preserve accepted `L1`, `L2`, `N1`, `N2`, `N3`, `N4`, `N5`, and `N6` as
  predecessor evidence only.
- Preserve the inherited explicit-only / non-equi-recursive /
  non-cyclic-graph / no-second-interface / no-fallback boundary unchanged.
- If the token is not `completed`, state exactly what remains blocked and
  whether a separate future roadmap amendment / update is required before
  more work can begin.
- Do not name, bind, or authorize the next target.

## Expected Outcome Shape

Unless the accepted chain or current no-drift continuity disproves it, the
expected `N7` token is `continue-bounded`.

That expected result must still say:

- the current reopened loop is not `completed`;
- the accepted bounded `baseTarget -> baseC` packet remains the only verified
  packet from this loop;
- all out-of-scope routes stay blocked; and
- a separate future roadmap amendment / update is required before another
  bounded cycle may begin.
