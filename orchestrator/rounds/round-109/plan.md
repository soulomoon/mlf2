# Round 109 Plan (`item-1` Same-Pocket Architecture-Amendment Contract And Writable Slice Freeze)

## Objective

Execute only roadmap item `1` and prepare one docs-only aggregate artifact
at:
`docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-authoritative-handoff-architecture-amendment-contract-and-writable-slice-freeze.md`.

This is the initial `item-1` plan for `attempt-1` with `retry: null`. The
round must consume accepted `round-108` / rev-002 item `5` as authoritative
predecessor truth, remain rev-003 planning-only and architecture-only, and
freeze exactly this much, and no more:

- the same selected rows `C2`, `C5`, and `C7`;
- the same exact same-pocket boundary, exact packet, and exact tuple;
- the same exact same-family lane opened by accepted `round-108`;
- one exact architecture-amendment slice centered on
  `runPipelineElabWith` / `checkedAuthoritative` /
  `typeCheck termClosed`; and
- one exact future writable implementation boundary limited to
  `src/MLF/Elab/Run/Pipeline.hs`,
  `src/MLF/Elab/TermClosure.hs`, and
  `test/PipelineSpec.hs`,
  with the rev-003 read-only audit anchors frozen as read-only.

Current planning read: accepted `round-108` already recorded exactly one
lawful successor outcome,
`open one bounded same-family architecture-amendment lane`, and it narrowed
that successor lane to one exact same-pocket authoritative-handoff problem
only. Item `1` must freeze that contract and writable slice for later item
`2`; it must not perform implementation, validation, helper-route redesign,
fallback widening, or public-interface redesign. The accepted blocker-proof
still says the bounded `Pipeline.hs` / `TermClosure.hs` root-handoff slice
exposes no alternate whole-packet authoritative recursive result inside the
inherited surface set, so the freeze must stay exact-pocket-only and
fail-closed if it starts widening beyond that one handoff.

This round is docs-only and aggregate-only. It must preserve
`iso-recursive = keep`,
`non-equi-recursive = keep`, and
`no-fallback = keep`;
keep accepted rev-002 item `5` authoritative;
keep accepted rev-002 items `1` through `4` authoritative;
keep rev-001 items `6` through `8` blocked;
and keep multi-SCC search, second interfaces, fallback widening,
production rollout, hardening, and broad capability claims blocked. It must
not edit source code, tests, Cabal, or `orchestrator/state.json` in this
round. No commit is authorized.

## Locked Round Context

- Round id: `round-109`
- Roadmap item: `item-1`
- Stage: `plan`
- Active attempt: `attempt-1`
- Retry state: `null`
- Active branch: `codex/round-109`
- Active worktree:
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-109`
- Fixed live subject: one docs-only rev-003 item-1 aggregate freeze for the
  exact same-pocket architecture-amendment contract and one exact future
  writable slice inside the bounded same-family lane opened by accepted
  `round-108`
- Current round review feedback: none yet; only
  `orchestrator/rounds/round-109/selection.md` exists for this round, so
  this is a full `attempt-1` plan rather than a retry delta

Current worktree state is already non-pristine. Respect existing edits and do
not revert unrelated work:

- controller-owned `orchestrator/state.json` is already modified and must
  remain untouched; and
- existing `orchestrator/rounds/round-109/selection.md` is a round input and
  must remain untouched.

Under the live retry contract, item `1` is aggregate-only. Review may reject
and send the same round back to `plan`, but `accepted + retry` is not lawful
for this item.

## Accepted Continuity That Remains Binding

- `orchestrator/rounds/round-109/selection.md`
  fixes this round to roadmap item `1` only, requires a docs-only aggregate
  freeze of the exact same-pocket architecture-amendment contract and
  writable slice, preserves accepted `round-108`, keeps rev-001 items `6`
  through `8` blocked, and explicitly blocks code work, validation work, and
  scope widening.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-003/roadmap.md`
  makes item `1` the only dependency-free unfinished item in rev-003 and
  defines completion as one accepted docs-only artifact freezing the exact
  selected rows, packet, tuple, authoritative-handoff slice, exact writable
  boundary, and exact read-only anchors for the already-opened same-family
  lane.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-003/retry-subloop.md`
  keeps item `1` aggregate-only, forbids `accepted + retry`, and preserves
  prior-attempt immutability if review rejects the round back to `plan`.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-003/verification.md`
  requires lane-freeze checks proving that item `1` narrows only to the same
  exact `C2` / `C5` / `C7` pocket, the same exact packet / anchor / owner-
  local frame / route / clear-boundary-only status, one exact
  `runPipelineElabWith` / `checkedAuthoritative` /
  `typeCheck termClosed` slice, and one exact writable boundary with no
  silent broadening.
- `orchestrator/rounds/round-108/review-record.json`
  is the authoritative acceptance record for predecessor item `5`. Its
  accepted checks make rev-002 item `5` authoritative with final outcome
  `global-non-cyclic-graph-selected-pocket-opens-one-bounded-same-family-architecture-amendment-lane`.
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-architecture-amendment-lane-open-or-stop-decision.md`
  is the authoritative accepted rev-002 item-5 artifact. It records exactly
  one lawful outcome,
  `open one bounded same-family architecture-amendment lane`,
  and narrows any successor revision to the same selected rows, same exact
  packet, same `boundVarTargetRoot` anchor, same owner-local retained-child
  frame, same route
  `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC`,
  and same clear-boundary-only status.
- `docs/plans/2026-03-26-global-non-cyclic-graph-reopened-revision-authority-and-candidate-boundary-freeze.md`
  is the authoritative rev-002 item-1 freeze. It kept the reopened work
  planning-only and architecture-only, froze exactly one bounded candidate
  boundary, and kept rev-001 items `6` through `8` blocked.
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-reopened-subject-selection.md`
  is the authoritative rev-002 item-2 selection. It chose exactly one live
  same-pocket subject:
  the same exact `C2` / `C5` / `C7` pocket, with family, anchor, owner-local
  frame, route, clear-boundary-only status, and exact packet identity all
  frozen, while keeping non-local `C1` contrast-only.
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-safety-and-acceptance-contract.md`
  is the authoritative rev-002 item-3 contract. It preserves the same exact
  subject, defines exactly three lawful bars for that subject only, preserves
  `iso-recursive = keep`,
  `non-equi-recursive = keep`, and
  `no-fallback = keep`,
  and explicitly defers exact audit binding and implementation clearance.
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-architecture-pressure-audit-target-and-evaluation-surface-bind.md`
  is the authoritative rev-002 item-4 bind. It freezes the exact selected
  rows `C2`, `C5`, and `C7`; the exact packet; the exact tuple; the exact
  module / command set; and the exact six review-visible surfaces for the
  selected pocket only.
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-path-audit.md`
  fixes `checkedAuthoritative` as the first exact public-output break for the
  selected pocket, with `termClosed` and `typeCheck termClosed` as the same-
  pocket dependencies that feed that break.
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-collapse-clear-or-confirm.md`
  remains the accepted blocker-proof context showing that the bounded
  `Pipeline.hs` / `TermClosure.hs` root-handoff slice exposes no alternate
  whole-packet authoritative recursive result for this same exact pocket
  inside the inherited surface set.
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  remains the inherited boundary contract:
  explicit-only production baseline,
  iso-recursive only,
  non-equi-recursive,
  no fallback widening,
  no second interface,
  and no equi-recursive or implicit-unfolding authorization.

## File Map

### Create

- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-authoritative-handoff-architecture-amendment-contract-and-writable-slice-freeze.md`
  - Responsibility: canonical docs-only rev-003 item-1 aggregate freeze for
    the exact same-pocket architecture-amendment contract and one exact
    future writable slice. It must consume accepted `round-108` plus
    accepted rev-002 items `1` through `4`, freeze the same exact pocket and
    authoritative-handoff slice, and forbid any broader edit boundary.

### Read-Only Evidence

- `orchestrator/rounds/round-109/selection.md`
- `orchestrator/rounds/round-108/review-record.json`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-003/roadmap.md`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-003/retry-subloop.md`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-003/verification.md`
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-architecture-amendment-lane-open-or-stop-decision.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-reopened-revision-authority-and-candidate-boundary-freeze.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-reopened-subject-selection.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-safety-and-acceptance-contract.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-architecture-pressure-audit-target-and-evaluation-surface-bind.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-path-audit.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-collapse-clear-or-confirm.md`

### Future Writable Slice (Must Remain Unchanged In This Round)

- `src/MLF/Elab/Run/Pipeline.hs`
- `src/MLF/Elab/TermClosure.hs`
- `test/PipelineSpec.hs`

### Read-Only Audit Anchors

- `src/MLF/Elab/Run/ResultType/Fallback.hs`
- `src/MLF/Elab/Run/ResultType.hs`
- `src/MLF/Elab/Run.hs`
- `src/MLF/Elab/Pipeline.hs`
- `src-public/MLF/Pipeline.hs`

### Preserve Unchanged

- `orchestrator/state.json`
- `orchestrator/rounds/round-109/selection.md`
- roadmap files under
  `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-003/`
- `Bugs.md`
- `src/`
- `src-public/`
- `app/`
- `test/`
- `mlf2.cabal`
- every `docs/plans/` artifact other than the one exact item-1 output path
  named above

No round-local `implementation-notes.md`, side ledger, alternate contract
memo, or supplemental architecture note is authorized. The docs artifact
above is the only intended implementation output path. The implementer must
not create or edit any other file for item `1`.

## Exact Selected Slice (Exactly One)

The only selected slice is:

freeze one rev-003 same-pocket architecture-amendment contract and one exact
future writable slice for the already-selected same-lane retained-child /
public-output continuity pocket carried by accepted rows `C2`, `C5`, and
`C7`, centered only on the
`runPipelineElabWith` / `checkedAuthoritative` /
`typeCheck termClosed` authoritative-handoff path.

### Exact Same-Pocket Boundary

The selected pocket remains exactly:

`the exact same-lane retained-child / public-output continuity pocket already carried by accepted rows C2, C5, and C7, with non-local C1 retained only as bounded contrast context`

| Row | Exact same-pocket role that remains binding | What item `1` must freeze for later work | Bound that must remain closed |
| --- | --- | --- | --- |
| `C2` | primary same-lane retained-child row for the selected pocket | same family / anchor / owner-local frame / route identity and same helper-visible retained-child continuity on the exact packet | not a second packet, not a broader same-family success claim, not a family-wide implementation target |
| `C5` | same exact `C2` pocket through the owner / binder-sensitive placement lens only | same local `TypeRef` lane, same owner-local frame, and same nested-`forall` fail-closed guard context | not a second owner-local packet, not a second route, not a widened writable boundary |
| `C7` | same exact `C2` / `C5` pocket through the public-output continuity lens only | exact authoritative public-output split and exact authoritative-handoff slice for the same packet | not a second interface, not a second public path, not a broad repo-level capability claim |

`C1` may appear only as bounded contrast context. It must not become a
second selected subject, a second packet, a second route, a second
interface, or a reason to widen the writable boundary.

### Exact Packet

The selected packet remains exactly this packet and no variant:

```haskell
ELet "k" (ELamAnn "x" recursiveAnn (EVar "x"))
  (ELet "u" (EApp (ELam "y" (EVar "y")) (EVar "k")) (EVar "u"))
```

where
`recursiveAnn = STMu "a" (STArrow (STVar "a") (STBase "Int"))`.

### Exact Tuple

The same frozen tuple remains binding:

- family: same-lane retained-child
- recursive-shape anchor: `boundVarTargetRoot`
- owner / binder frame: one owner-local retained-child frame
- route: `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC`
- quantified-boundary status: clear-boundary only:
  `boundHasForallFrom` is false and `not hasForall` holds

`C5` and `C7` remain rereads of this same exact packet and tuple only. They
are not second packets, second routes, second interfaces, or second same-
family lanes.

### Exact Architecture-Amendment Slice

The contract freeze must center on exactly one authoritative-handoff slice
and no other:

- `runPipelineElabWith`
- `checkedAuthoritative`
- `typeCheck termClosed`

This slice is exact-pocket-only. It exists because accepted predecessor
evidence already localizes the public-output continuity break to
`checkedAuthoritative`, with `termClosed` and `typeCheck termClosed` as the
same-pocket dependencies, while the accepted blocker-proof still says the
bounded `Pipeline.hs` / `TermClosure.hs` root-handoff slice exposes no
alternate whole-packet authoritative recursive result inside the inherited
surface set.

Item `1` may freeze only the contract and future edit boundary around this
slice. It must not:

- alter helper-route ownership;
- reopen the accepted route / anchor / frame identity;
- authorize a second public interface;
- authorize fallback widening or a new fallback path; or
- imply that item `2` may edit anything outside the exact writable slice
  below.

### Exact Future Writable Slice

The one exact future writable slice for later item `2` is:

| Path | Why it is the only future writable path | What remains forbidden even there |
| --- | --- | --- |
| `src/MLF/Elab/Run/Pipeline.hs` | owns `runPipelineElabWith`, `termClosed`, and `checkedAuthoritative`, so it is part of the exact authoritative-handoff implementation site | no second interface, no route reselection, no broader family widening, no fallback redesign |
| `src/MLF/Elab/TermClosure.hs` | remains inside the accepted bounded root-handoff slice that must stay exact-pocket-only | no general term-closure rewrite, no broader recursive-closure search, no multi-SCC behavior |
| `test/PipelineSpec.hs` | is the exact-pocket regression surface for the same selected packet and same same-pocket continuity faces only | no broad new corpus, no second packet harness, no family-wide success narration |

No other source file, test file, Cabal file, executable path, public entry
module, or helper module is part of the writable boundary.

### Exact Read-Only Audit Anchors

The following paths must be frozen as read-only audit anchors for this lane:

| Path | Why it remains read-only in rev-003 item `1` |
| --- | --- |
| `src/MLF/Elab/Run/ResultType/Fallback.hs` | owns the accepted same-lane route / guard / owner-local lookup anchors and must stay audit-only rather than becoming part of the writable slice |
| `src/MLF/Elab/Run/ResultType.hs` | owns dispatch into fallback reconstruction and remains audit-only so item `1` does not widen into helper-route redesign |
| `src/MLF/Elab/Run.hs` | preserves the one public-entrypoint re-export chain and keeps second-interface work blocked |
| `src/MLF/Elab/Pipeline.hs` | preserves the same one-interface chain and remains read-only unless a later accepted successor revision explicitly changes that boundary |
| `src-public/MLF/Pipeline.hs` | preserves the public exposed interface and keeps second-interface and rollout widening blocked |

If later narration tries to turn any read-only anchor into part of the
writable slice without a later accepted revision explicitly reopening that
boundary, it has already widened beyond rev-003 item `1`.

## Explicit Disallowed Scope

The following remain explicitly blocked in this round and in the item-1
implementation it plans:

- any change to accepted rev-002 item-5 truth or accepted rev-002 items `1`
  through `4` truth;
- any reopening of the selected subject boundary or any promotion of `C1`
  into a coequal subject, packet, route, or interface;
- any treatment of `C5` or `C7` as second packets, second routes, second
  interfaces, or separate architecture-amendment lanes;
- any widening beyond the exact selected packet, tuple, or authoritative-
  handoff slice;
- any widening of the writable boundary beyond
  `src/MLF/Elab/Run/Pipeline.hs`,
  `src/MLF/Elab/TermClosure.hs`, and
  `test/PipelineSpec.hs`;
- any conversion of the read-only audit anchors into writable paths;
- any multi-SCC search, second interfaces, fallback widening,
  equi-recursive semantics, implicit unfolding, production rollout,
  hardening, or broad capability claims;
- any claim that rev-001 items `6` through `8` are unblocked by implication;
- any creation or edit outside the one exact item-1 output path named above
  during the future item-1 implementation;
- any touch to source code, tests, Cabal, or `orchestrator/state.json` in
  this planning round; and
- any commit.

If later implementation starts widening into helper-route redesign, public
interface redesign, a second packet, a second same-family lane, or any
source/test edit during item `1`, it has already exceeded the accepted
rev-003 boundary and must fail closed.

## Required Verification

This planning round remains docs-only and aggregate-only. Required
verification for the plan artifact and later item-1 implement work must stay
on docs / control-plane boundaries unless a later diff escapes that surface.

### Plan-Round Verification

- `git diff --check`
- `git diff --name-only`
- `git status --short --branch`

The plan reviewer should confirm from the resulting diff and file contents
that:

- only `orchestrator/rounds/round-109/plan.md` was created or edited by this
  round work;
- the plan freezes exactly one intended implementation output path and
  forbids every other round-owned edit;
- the plan freezes the exact selected rows, exact same-pocket boundary,
  exact packet, exact tuple, exact authoritative-handoff slice, exact future
  writable slice, and exact read-only audit anchors;
- the plan preserves
  `iso-recursive = keep`,
  `non-equi-recursive = keep`,
  and `no-fallback = keep`;
- the plan keeps rev-001 items `6` through `8`, multi-SCC search, second
  interfaces, fallback widening, production rollout, hardening, and broad
  capability claims blocked; and
- the plan stays item-1-only, planning-only, architecture-only, docs-only,
  and aggregate-only.

### Later Item-1 Verification Contract To Preserve

- baseline rev-003 verification commands from
  `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-003/verification.md`
  remain binding;
- confirm the final artifact exists only at
  `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-authoritative-handoff-architecture-amendment-contract-and-writable-slice-freeze.md`;
- confirm the artifact freezes the same exact `C2` / `C5` / `C7` pocket, the
  same exact packet, the same exact tuple, and the same exact same-pocket
  boundary inherited from accepted `round-108` and accepted rev-002 items
  `1` through `4`;
- confirm the artifact freezes exactly one future writable slice limited to
  `src/MLF/Elab/Run/Pipeline.hs`,
  `src/MLF/Elab/TermClosure.hs`, and
  `test/PipelineSpec.hs`;
- confirm the artifact freezes exactly the named read-only anchors
  `src/MLF/Elab/Run/ResultType/Fallback.hs`,
  `src/MLF/Elab/Run/ResultType.hs`,
  `src/MLF/Elab/Run.hs`,
  `src/MLF/Elab/Pipeline.hs`, and
  `src-public/MLF/Pipeline.hs`
  as read-only;
- confirm the artifact keeps
  `iso-recursive = keep`,
  `non-equi-recursive = keep`,
  and `no-fallback = keep`
  and keeps rev-001 items `6` through `8` blocked;
- confirm the artifact continues to block multi-SCC search, second
  interfaces, fallback widening, production rollout, hardening, and broad
  capability claims;
- confirm the artifact does not authorize code edits, test edits, Cabal
  edits, or `orchestrator/state.json` edits in item `1`;
- docs-diff review proving the future item-1 implementation changed only the
  one exact output path above and did not touch `src/`, `src-public/`,
  `app/`, `test/`, `mlf2.cabal`, or `orchestrator/state.json`;
- explicit skip note that `cabal build all && cabal test` remains out of
  scope for item `1` because the item is docs-only and any code-changing work
  belongs no earlier than item `2`; and
- `git status --short --branch` before handoff so the round record captures
  the exact docs-only diff and any preexisting controller-owned state edit.

The reviewer-facing acceptance record for item `1` must still show:

- one implemented stage result;
- one attempt verdict;
- one stage action;
- one retry reason;
- one fix hypothesis; and
- one bounded summary proving the freeze stayed exact-pocket-only and did not
  widen beyond the selected authoritative-handoff slice.
