# Round 088 Plan (`item-7` Architecture Decision)

## Objective

Execute only roadmap item `7` and produce one docs-first artifact at:
`/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architecture-decision-and-successor-plan-choice.md`.

This is the initial `item-7` plan for `attempt-1` with `retry: null`. The
round must turn the accepted item-2 architectural audit and the accepted
item-6 `bounded subset only` coverage result into exactly one strategic
outcome for the refreshed control plane. The artifact must:

- evaluate only the three lawful outcome tokens:
  `continue within the current architecture`,
  `pursue targeted boundary revision`, or
  `stop`;
- ground that choice in the accepted evidence only, especially:
  zero `stable visible persistence` rows, blocker debt in both admitted
  families, nested-`forall` remaining reject-side only, and the still-live
  `non-cyclic-graph = unknown` risk;
- preserve bounded predecessor truth without upgrading bounded evidence into
  `broad generality` or into an `architectural dead end` unless the accepted
  record actually justifies that stronger claim;
- if the outcome is `continue`, name exactly one concrete bounded successor
  cycle and why it is the next lawful evidence gate;
- if the outcome is `pursue targeted boundary revision`, name exactly one
  first revision gate and the boundary under review; and
- if the outcome is `stop`, preserve predecessor truth and state why broader
  work is not currently justified.

This round is docs-only, aggregate-only, and decision-only. It must not edit
production code, tests, public surfaces, executables, `mlf2.cabal`,
`orchestrator/state.json`, `orchestrator/roadmap.md`,
`orchestrator/verification.md`, `orchestrator/retry-subloop.md`, `Bugs.md`,
or any review / merge / predecessor-history artifact.

This round must not silently widen into:

- a reopened capability definition, mechanism map, search model,
  reconstruction contract, or coverage campaign;
- code-path experiments, implementation slices, or test expansion;
- equi-recursive reasoning or implicit unfolding;
- cyclic structural graphs, multi-SCC search, or cross-family reopening;
- a second interface or fallback behavior;
- a silent architecture revision without naming the reviewed boundary; or
- multiple strategic outcomes blurred together.

Current planning read: item `7` is the explicit architecture-fork gate that
must consume the accepted `bounded subset only` read without drifting into
another design round. If the accepted record does not justify a broader claim,
the artifact must fail closed by choosing exactly one bounded strategic
outcome and naming the next lawful gate rather than inventing new evidence.

## Locked Round Context

- Round id: `round-088`
- Roadmap item: `item-7`
- Stage: `plan`
- Active attempt: `attempt-1`
- Retry state: `null`
- Fixed live subject: one docs-only aggregate architecture decision and
  successor-plan choice from the accepted bounded-subset-only evidence chain
- Active branch: `codex/round-088-item-7-architecture-decision`
- Active worktree:
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088`
- Current round review feedback: none yet; no `review.md`,
  `reviews/attempt-<n>.md`, or `review-record.json` exists for `round-088`,
  so this is a full `attempt-1` plan rather than a retry delta

Current round worktree state is already non-pristine. Respect existing edits
and do not revert unrelated work:

- `M orchestrator/state.json` (controller-owned; must remain untouched)
- `?? orchestrator/rounds/round-088/selection.md`

## Accepted Continuity That Remains Binding

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/orchestrator/rounds/round-088/selection.md`
  already fixes this round to roadmap item `7` only, requires exactly one
  explicit strategic outcome, and forbids silent widening beyond the accepted
  item-2 and item-6 evidence.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/orchestrator/roadmap.md`
  makes item `7` the next dependency-satisfied item and defines its
  completion notes as one architecture decision plus one successor-plan choice
  only.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-strategic-roadmap.md`
  is the human source document for this gate. It places the architecture fork
  after the representative coverage campaign and requires an explicit choice
  between continuing, revising, or stopping.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md`
  is the accepted item-2 audit. Its binding classification set remains:
  `iso-recursive = keep`, `non-equi-recursive = keep`,
  `non-cyclic-graph = unknown`, and `no-fallback = keep`.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`
  is the accepted item-6 coverage result. Its binding aggregate read remains:
  zero `stable visible persistence` rows, four
  `admitted but not reconstruction-visible / blocker debt` rows
  (`C1`, `C2`, `C5`, `C7`), three `fail-closed rejection` rows
  (`C3`, `C4`, `C6`), and the strongest lawful feasibility read
  `bounded subset only`.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/orchestrator/rounds/round-087/review-record.json`
  authoritatively finalized item `6` with
  `final_outcome = representative-coverage-and-feasibility-campaign-classified-as-bounded-subset-only`.
  Item `7` must consume that accepted result instead of rerunning the
  coverage judgment.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  preserves the inherited baseline:
  explicit-only recursive behavior, iso-recursive meaning,
  non-equi-recursive semantics, structurally acyclic encoding, and no
  fallback widening.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/docs/plans/2026-03-24-automatic-iso-recursive-bound-var-target-same-lane-retained-child-next-cycle-decision-gate.md`
  preserves accepted `N14` as bounded predecessor evidence only. It does not
  pre-clear broad general automatic iso-recursive inference and does not force
  an architecture revision by itself.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/orchestrator/verification.md`
  requires item-7-specific review checks proving that the round records
  exactly one explicit outcome and does not blur together
  `continue`, `revise`, and `stop`.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/orchestrator/retry-subloop.md`
  makes item `7` aggregate-only: review may reject and return the same round
  to `plan`, but review may not emit `accepted + retry` for this item.
  Because `retry: null` right now, this plan is the full `attempt-1` plan.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/Bugs.md`
  still lists open `BUG-2026-03-16-001`, but that replay / `InstBot` defect
  remains predecessor implementation context only. It does not authorize a
  code repair, replay detour, or roadmap reorder inside item `7`.

## File Map

### Create

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architecture-decision-and-successor-plan-choice.md`
  - Responsibility: canonical docs-only item-7 artifact that consumes the
    accepted item-2 and item-6 evidence, evaluates the three lawful strategic
    outcomes, records exactly one outcome token, and names exactly one bounded
    successor choice consistent with that outcome.

### Read-Only Evidence

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/orchestrator/rounds/round-088/selection.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/orchestrator/roadmap.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/orchestrator/verification.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/orchestrator/retry-subloop.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-strategic-roadmap.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/orchestrator/rounds/round-087/review-record.json`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/docs/plans/2026-03-24-automatic-iso-recursive-bound-var-target-same-lane-retained-child-next-cycle-decision-gate.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/Bugs.md`

### Preserve Unchanged

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/orchestrator/state.json`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/orchestrator/roadmap.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/orchestrator/verification.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/orchestrator/retry-subloop.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/Bugs.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/src/`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/test/`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/src-public/`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/app/`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/mlf2.cabal`
- reviewer-owned history under earlier round directories

## Exact Selected Slice (Exactly One)

The only selected slice is:

record the aggregate architecture decision and successor-plan choice from the
accepted `bounded subset only` evidence.

This slice is allowed to:

- restate the binding decision inputs from accepted item `2` and accepted
  item `6`;
- evaluate exactly three strategic outcomes against that evidence:
  continue within the current architecture, pursue targeted boundary revision,
  or stop;
- reject the two non-selected outcomes with explicit evidence-based reasons
  instead of leaving them implicitly live;
- record exactly one authoritative outcome token; and
- if needed by the selected token, name exactly one bounded successor cycle
  or exactly one first revision gate.

This slice is not allowed to:

- reopen items `1` through `6` or redesign their artifacts;
- create a new capability corpus, mechanism family, search rule,
  reconstruction rubric, or coverage matrix;
- authorize implementation work, code experiments, tests, second interfaces,
  fallback behavior, equi-recursive reasoning, cyclic structural graphs, or
  multi-SCC search;
- revise `orchestrator/roadmap.md`, `orchestrator/state.json`, the retry
  contract, or completed roadmap history; or
- blur together `continue`, `revise`, and `stop` as if more than one outcome
  were simultaneously accepted.

If the decision starts depending on fresh code-path evidence, replay repair,
silent boundary widening, or a new coverage campaign to justify itself, the
artifact must fail closed by choosing `pursue targeted boundary revision` or
`stop` as appropriate rather than solving those gaps inside item `7`.

## Sequential Tasks

### Task 1 - Freeze the item-7 aggregate decision frame

- Create the canonical item-7 artifact at the path above.
- State explicitly that item `7` is docs-only, aggregate-only, and
  `attempt-1` with `retry: null`.
- Reassert the inherited boundary unchanged:
  - explicit recursive annotations remain the current production baseline;
  - recursive meaning remains iso-recursive only;
  - no equi-recursive equality or implicit unfolding is authorized;
  - no cyclic structural graph encoding or multi-SCC search is authorized;
  - no second interface is authorized; and
  - no compatibility / convenience / default-path fallback widening is
    authorized.
- State explicitly that accepted items `1` through `6` plus accepted `N14`
  contribute bounded evidence only. They do not by themselves prove broad
  generality, force an architecture revision, or settle product posture.

### Task 2 - Assemble one bounded decision-input ledger

- Pull the decisive accepted inputs into one compact decision ledger. It must
  include at least:
  - the item-2 constraint classification set
    (`keep`, `keep`, `unknown`, `keep`);
  - the item-6 matrix tally:
    zero `stable visible persistence`,
    four blocker-debt rows, and
    three fail-closed rows;
  - the fact that both admitted families remain below accepted
    reconstruction-visible success;
  - the fact that nested-`forall` remains reject-side only;
  - the fact that ambiguity and termination are bounded only as fail-closed
    discipline; and
  - the fact that two bounded admitted pockets still exist inside the current
    acyclic model.
- Keep this ledger evidence-only. It must not yet choose the outcome or invent
  stronger claims than the accepted artifacts support.

### Task 3 - Evaluate the three lawful outcome branches explicitly

- Require the artifact to evaluate all three candidate outcomes in a single
  review-visible schema such as:
  outcome token, supporting evidence, blocking evidence, and lawful-status
  read.
- For `continue within the current architecture`, test only whether the
  accepted bounded pockets plus bounded fail-closed search discipline justify
  one more bounded evidence gate inside the unchanged architecture.
- For `pursue targeted boundary revision`, test only whether the accepted
  evidence now points to one concrete inherited boundary as the next lawful
  review target. The default candidate under current evidence is
  `non-cyclic-graph`, because it is the only inherited boundary still marked
  `unknown`.
- For `stop`, test only whether the accepted record now rules out both
  bounded continuation and targeted revision strongly enough that broader work
  is not presently justified.
- Reject any outcome branch that depends on silent widening, speculative code
  work, or evidence that the accepted record does not already contain.

### Task 4 - Record exactly one authoritative item-7 outcome

- Choose exactly one of the three outcome tokens and state it as the sole
  authoritative item-7 result.
- Tie that choice directly to the accepted ledger from Task 2 and the branch
  evaluation from Task 3.
- Require the artifact to explain why the two non-selected outcomes are not
  the strongest lawful read on current evidence.
- Keep the reasoning honest:
  zero `stable visible persistence` rows and blocker debt in both admitted
  families prevent any claim of broad general automatic iso-recursive
  inference;
  two bounded admitted pockets plus bounded fail-closed ambiguity /
  termination discipline prevent any claim that the current architecture is
  already proved impossible unless the artifact can justify that stronger
  read from accepted evidence alone.

### Task 5 - Name exactly one bounded successor choice implied by the selected outcome

- If the selected outcome is `continue within the current architecture`,
  require the artifact to name exactly one bounded successor cycle only.
  That cycle must test the most decision-critical unresolved pressure without
  reopening the whole program. The preferred lawful shape is:
  one bounded gate on whether the strongest admitted same-lane
  retained-child pocket can achieve accepted `stable visible persistence`
  inside the inherited acyclic model.
- If the selected outcome is `pursue targeted boundary revision`, require the
  artifact to name exactly one first revision gate and one boundary under
  review. Under current accepted evidence, that boundary should remain
  `non-cyclic-graph` unless the artifact can point to stronger accepted
  pressure elsewhere.
- If the selected outcome is `stop`, require the artifact to preserve
  predecessor truth and name why neither a bounded continuation cycle nor a
  targeted revision gate is yet justified.
- Do not allow more than one successor path. The artifact must leave exactly
  one next lawful move on the table.

### Task 6 - End with the reviewer-facing decision note and docs-only gate

- End the artifact with one bounded item-7 result only:
  the refreshed control plane now has one explicit architecture decision and
  one successor-plan choice grounded in accepted item-2 and item-6 evidence.
- Cite the reviewer-facing expectations from
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-088/orchestrator/verification.md`:
  this round must record exactly one explicit outcome and must preserve the
  inherited boundary, predecessor continuity, and the no-silent-widening
  rule.
- State that this round is expected to change only documentation and that no
  full `cabal build all && cabal test` gate is triggered unless the diff
  escapes the authorized docs-only surface.
- Keep item-7 retry discipline visible:
  review may reject and send the same round back to `plan`, but the accepted
  outcome for item `7` cannot be `accepted + retry`.
