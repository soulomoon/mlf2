# Round 204 - Task Selection

**Selected milestone**: `milestone-2`
**Selected direction**:
`direction-2a-build-broader-positive-p5-ledger-under-revised-freeze`
**Selected extraction**:
`build-broader-positive-p5-ledger-under-revised-freeze`
**Current task/title**:
`Build the broader positive P5 planning ledger beyond the one settled retained-child lane under the revised freeze`

**Roadmap identity**:
- roadmap_id: `2026-04-07-00-p5-polymorphism-nested-forall-explicit-boundary-revision-roadmap`
- roadmap_revision: `rev-002`
- roadmap_dir: `orchestrator/roadmaps/2026-04-07-00-p5-polymorphism-nested-forall-explicit-boundary-revision-roadmap/rev-002`
- milestone_id: `milestone-2`
- direction_id:
  `direction-2a-build-broader-positive-p5-ledger-under-revised-freeze`
- extracted_item_id:
  `build-broader-positive-p5-ledger-under-revised-freeze`

## Why now

Accepted `round-203` completed `milestone-1` under `rev-002` and replaced the
old controlling round-151 semantic read with the narrower classification
`narrowed live semantic pressure`. That refreeze is now the active family
baseline, so the next lawful move is the lowest-numbered unfinished roadmap
item that works directly from that accepted freeze.

That means `milestone-2` must start with
`direction-2a-build-broader-positive-p5-ledger-under-revised-freeze`, not the
later `direction-2b` comparison and not the `milestone-3` handoff. The family
still lacks one exact broader positive `P5 polymorphism-nested-forall`
frontier beyond the one settled retained-child lane, and without that ledger
no current-boundary-versus-explicit-revision comparison can be honest.

## Current baseline

- Round-204 branch/worktree `HEAD`: `938efe6`
- Controller-prepared round branch remains:
  `orchestrator/round-204-build-revised-p5-ledger`
- Controller-prepared round worktree remains: `orchestrator/worktrees/round-204`
- Active roadmap family remains
  `2026-04-07-00-p5-polymorphism-nested-forall-explicit-boundary-revision-roadmap`
  at `rev-002`
- `milestone-1` is now complete through accepted `round-203`
- `milestone-2` and `milestone-3` remain pending
- No retry state is active (`retry: null`, no `resume_error`)
- Accepted `round-203` published the controlling milestone-1 refreeze artifact
  at
  `docs/plans/2026-04-07-p5-polymorphism-nested-forall-explicit-boundary-revision-family-round-151-polymorphic-mediation-mu-preservation-reclassification-and-inherited-boundary-refreeze.md`
  and recorded approval in
  `orchestrator/rounds/round-203/review-record.json`
- That accepted refreeze preserves accepted `round-202` as historical
  predecessor evidence only, reclassifies the round-151
  polymorphic-mediation `mu`-absorption story as
  `narrowed live semantic pressure`, and fixes the live broader subject to
  nested-forall / quantified-crossing support beyond the one settled
  retained-child clear-boundary lane
- Accepted `round-197` still keeps
  `sameLaneAliasFrameClearBoundaryExpr`
  as one bounded retained-child clear-boundary success on
  `runPipelineElab` / `runPipelineElabChecked`, while
  `nestedForallContrastExpr` remains fail-closed with
  `PhiTranslatabilityError`
- Accepted `round-191` / `round-181` still keep `P2` packet-bounded to the
  exact `C1` packet only, and accepted `round-192` still keeps
  `N1 ambiguity-reject`,
  `N2 unsoundness-guard`, and
  `N6 termination-pressure`
  closed as predecessor truth only
- Repository status in the canonical round worktree before writing this
  selection was controller-owned `orchestrator/state.json` only

## Scope

- Keep this round docs-only, planning-only, serial, and faithful to exactly
  one milestone-2 / direction-2a extraction:
  build the broader positive `P5` planning ledger under the revised
  milestone-1 freeze
- Publish one canonical ledger artifact under `docs/plans/` plus round
  bookkeeping only; that artifact must identify the exact broader positive
  `P5 polymorphism-nested-forall` frontier beyond the one settled
  retained-child lane and classify each preserved input as predecessor truth,
  excluded material, or still-live broader support pressure
- Ground the ledger in the accepted `round-203` refreeze and the preserved
  predecessor evidence it names, especially:
  `round-203`, `round-202`, `round-201`, `round-200`, `round-197`,
  `round-192`, `round-191`, `round-181`, `round-151`,
  `implementation_notes.md`, and
  `test/Research/P5ClearBoundarySpec.hs`
- Explain specifically how the surviving broader positive pressure bears on
  the exact live inherited-boundary clauses or semantic questions frozen in
  `milestone-1`, while preserving:
  `P2` as unopened,
  the representative negative-family rows as closed predecessor truth,
  the settled retained-child lane as predecessor evidence only,
  `non-equi-recursive = keep`,
  `no-fallback = keep`,
  `no second interface is authorized`, and
  `non-cyclic-graph = unknown`
  as inherited context rather than newly reopened debt
- Keep the family planning-only: do not revise code, tests, Cabal wiring, or
  the inherited boundary itself
- Do not perform the milestone-2 direction-2b comparison yet, do not bind a
  milestone-3 downstream consequence yet, and do not reopen the March 28
  exact packet, `P2`, or the representative negative-family rows as live debt
