# Round 004 Selection

Selected roadmap item: **4. Execute a bounded feasibility spike for the selected subset** (`orchestrator/roadmap.md`, item 4).

Why this item now:
- Items 1 through 3 are complete, and item 4 is the immediate dependency before any final go/no-go decision (item 5).
- The smallest confidence-increasing slice is to execute only the `ARI-C1` feasibility evidence gates already defined in item 3, without widening to broader automatic recursive-type inference.
- Running this bounded spike now can produce concrete feasibility or no-go evidence while preserving the inherited baseline and prior audit obligations.
- Boundary remains mandatory throughout the spike: explicit-only ingress, non-equi-recursive semantics, and non-cyclic graph representation.

Round-004 scope guard:
- Execute only the bounded `ARI-C1` feasibility spike defined by the accepted candidate-subset document.
- Do not widen scope to fully unannotated recursive-type synthesis or multi-candidate exploration.
- Do not update `orchestrator/roadmap.md` in this step and do not edit production code outside what the bounded spike explicitly authorizes.
