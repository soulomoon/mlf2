# Round 011 Selection

Selected roadmap item: **1. Write the `RE1` provenance-authority evidence contract for `URI-R3-O4`** (`orchestrator/roadmap.md`, item 1).

Why this item should run now:
- Item 1 is the first pending stage in the approved `RE1` -> `RE5` re-entry ladder and has no unmet roadmap dependency beyond the already accepted rounds `001` through `010`, the approved re-entry design, and the accepted `R5` `research-stop` result.
- The accepted `R5` stop decision explicitly identifies `URI-R3-O4` as the decisive inherited blocker: there is still no authoritative provenance-stable replay/reification authority for one bounded unannotated root/cluster inside `URI-R2-C1`.
- Writing the `RE1` contract is the smallest evidence slice that sharpens the path from bounded stop to bounded re-entry verdict, because later uniqueness (`RE2`), positive-evidence (`RE3`), and gate (`RE4`) judgments all need one reviewer-checkable statement of what would count as admissible provenance authority and what must remain rejected.
- Choosing item 1 keeps the subject fixed to `URI-R2-C1` and preserves the roadmap's mandatory single-SCC, single-binder-family, non-equi-recursive, and non-cyclic-graph boundaries while staying prototype-free and fail-closed.

Round-011 scope guard:
- Produce only the `RE1` provenance-authority evidence contract for `URI-R3-O4`; do not pre-empt `RE2`, `RE3`, `RE4`, or `RE5`.
- Keep the active subject fixed to `URI-R2-C1` only, with `single-SCC` obligation-level recursion, `single-binder-family` ownership, non-equi-recursive semantics, and non-cyclic structural-graph constraints explicit throughout.
- Treat completed rounds `001` through `010`, the accepted `R5` stop decision, and the predecessor recursive-types packet as inherited evidence only; do not reopen or rewrite them.
- Keep the round research-first and prototype-free: if the contract cannot be stated without prototype-backed or experiment-backed evidence, fail closed and preserve the stop instead of authorizing such work.
- Do not update `orchestrator/roadmap.md` or `orchestrator/state.json`, do not write `plan.md`, `review.md`, `merge.md`, or `implementation-notes.md`, and do not edit production code, tests, `TODO.md`, `CHANGELOG.md`, `implementation_notes.md`, or task packets.
