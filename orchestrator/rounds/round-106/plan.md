# Round 106 Plan (`item-3` Safety And Acceptance Contract For The Selected Reopened Subject)

## Objective

Execute only roadmap item `3` and prepare one docs-only contract artifact at:
`docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-safety-and-acceptance-contract.md`.

This is the initial `item-3` plan for `attempt-1` with `retry: null`. The
round must consume accepted `round-104` item `1` and accepted `round-105`
item `2` as authoritative predecessor truth, remain planning-only and
architecture-only, and define the reviewer-auditable safety / acceptance
contract for exactly one already-selected reopened subject only.

The planned artifact must define exactly three lawful outcome bars for that
one selected subject, and nothing broader:

- `acyclic still sufficient`
- `single-component cyclic-structure successor lane justified`
- `stop without opening an architecture-amendment lane`

Current planning read: item `3` must define what evidence would count as each
of those three outcomes for the already-selected same-lane retained-child /
public-output continuity pocket, but item `3` must not choose among them.
Outcome selection remains later work only. The contract must preserve
`iso-recursive = keep`, `non-equi-recursive = keep`, and
`no-fallback = keep`; keep rev-001 items `6` through `8` blocked; keep
multi-SCC search, second interfaces, fallback widening, and production
implementation blocked; and fail closed if the artifact starts reopening
subject selection, binding the exact item-4 audit target / surfaces, or
deciding the item-5 open-vs-stop result.

This round is docs-only. It must not edit controller-owned machine state,
roadmap contracts, `Bugs.md`, source code, tests, or Cabal files. No commit
is authorized.

## Locked Round Context

- Round id: `round-106`
- Roadmap item: `item-3`
- Stage: `plan`
- Active attempt: `attempt-1`
- Retry state: `null`
- Active branch: `codex/round-106`
- Active worktree:
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-106`
- Fixed live subject: one docs-only rev-002 safety / acceptance contract for
  the exact same-lane retained-child / public-output continuity pocket
  selected in accepted item `2`
- Current round review feedback: none yet; no `review.md`,
  `reviews/attempt-<n>.md`, or `review-record.json` exists for `round-106`,
  so this is a full `attempt-1` plan rather than a retry delta

Current worktree state is already non-pristine. Respect existing edits and do
not revert unrelated work:

- `M orchestrator/state.json` (controller-owned; must remain untouched)
- `?? orchestrator/rounds/round-106/selection.md` (preexisting round input;
  must remain untouched)

Under the live retry contract, item `3` is retry-capable, but this plan is
for the initial `retry: null` attempt only.

## Accepted Continuity That Remains Binding

- `orchestrator/rounds/round-106/selection.md`
  fixes this round to roadmap item `3` only, requires a docs-only
  contract-only output, preserves accepted item-1 and item-2 truth, and
  explicitly blocks implementation work, subject reselection, item-4 target
  binding, and item-5 open-vs-stop choice.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/roadmap.md`
  makes item `3` the next unfinished item after accepted items `1` and `2`
  and defines completion as one accepted docs-only artifact that establishes
  the safety / acceptance contract for the selected reopened subject only.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/retry-subloop.md`
  keeps item `3` retry-capable inside the same round while preserving prior
  attempts as immutable history.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/verification.md`
  requires reopened-safety-contract checks proving that item `3` defines a
  reviewer-auditable acceptance bar for the three lawful outcomes while
  preserving the inherited boundary and blocked work.
- `orchestrator/rounds/round-104/review-record.json`
  is the authoritative acceptance record for predecessor item `1`. Its
  accepted checks make the rev-002 authority freeze and single candidate
  boundary binding.
- `orchestrator/rounds/round-105/review-record.json`
  is the authoritative acceptance record for predecessor item `2`. Its
  accepted checks make the exact selected reopened subject binding and keep
  `C1` contrast-only, `C5` same-pocket-only, and later-item ownership
  intact.
- `docs/plans/2026-03-26-global-non-cyclic-graph-reopened-revision-authority-and-candidate-boundary-freeze.md`
  is the authoritative item-1 freeze. It preserved rev-002 as planning-only
  and architecture-only, froze exactly one candidate boundary, kept rev-001
  items `6` through `8` blocked, and deferred safety-contract ownership to
  item `3`.
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-reopened-subject-selection.md`
  is the authoritative item-2 selection. It chose exactly one live reopened
  subject: the same exact `C2` / `C5` / `C7` pocket, with family, anchor,
  owner-local frame, route, clear-boundary-only status, and exact packet
  identity all frozen.
- `docs/plans/2026-03-26-global-non-cyclic-graph-keep-vs-reopen-decision-gate.md`
  is the authoritative rev-001 item-5 reopen record. It preserved
  `iso-recursive = keep`, `non-equi-recursive = keep`, and
  `no-fallback = keep`, reopened only the `non-cyclic-graph` question, and
  explicitly kept rev-001 items `6` through `8` blocked pending later
  revision work.
- `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md`
  binds `C1`, `C2`, and `C5` as blocker debt, preserves `C2` as the primary
  same-lane admitted pocket, and preserves `C5` as the same exact pocket
  through the owner / binder-sensitive placement lens only.
- `docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md`
  binds `C3` as reject-side pressure only and binds `C7` as the same exact
  `C2` / `C5` pocket through the public-output continuity lens, still below
  `stable visible persistence`.
- `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md`
  is the accepted representative matrix. It keeps zero
  `stable visible persistence` rows, keeps `C1`, `C2`, `C5`, and `C7` as
  blocker debt, keeps `N4` as architecture-pressure context only at this
  stage, and does not itself justify any cyclic successor lane.
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md`
  freezes the exact same-lane pocket, frozen tuple, exact packet, and the
  helper-visible/internal versus authoritative-public split carried forward
  here.
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md`
  fixes the exact-pocket classification as
  `admitted but not reconstruction-visible / blocker debt` and keeps the
  internal/public split reviewer-visible without reopening broader
  architecture.
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md`
  remains bounded predecessor evidence only. It previously kept this same
  exact pocket as blocker debt within the current architecture, but that
  earlier bounded same-pocket decision does not override the later accepted
  rev-001 global reopen result and does not settle item `3` by itself.
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  remains the inherited boundary contract:
  explicit-only baseline,
  iso-recursive only,
  non-equi-recursive,
  inherited `non-cyclic-graph` boundary,
  no second interface,
  and no fallback widening.

## File Map

### Create

- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-safety-and-acceptance-contract.md`
  - Responsibility: canonical docs-only rev-002 item-3 artifact defining the
    reviewer-auditable safety / acceptance contract for exactly one selected
    reopened subject and exactly three lawful outcome bars, without choosing
    an outcome and without binding later audit surfaces.

### Read-Only Evidence

- `orchestrator/rounds/round-106/selection.md`
- `orchestrator/rounds/round-105/review-record.json`
- `orchestrator/rounds/round-104/review-record.json`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/roadmap.md`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/retry-subloop.md`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/verification.md`
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-keep-vs-reopen-decision-gate.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-reopened-revision-authority-and-candidate-boundary-freeze.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-reopened-subject-selection.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md`

### Preserve Unchanged

- `orchestrator/state.json`
- `orchestrator/rounds/round-106/selection.md`
- roadmap files under
  `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/`
- `Bugs.md`
- `src/`
- `src-public/`
- `app/`
- `test/`
- `mlf2.cabal`

No round-local `implementation-notes.md` is authorized for this item-3 plan.
The docs artifact above is the only intended implementation output path. The
implementer must not create or edit any other file for item `3`.

## Exact Selected Slice (Exactly One)

The only selected slice is:

define the safety and acceptance contract for the exact same-lane
retained-child / public-output continuity pocket already selected in
accepted item `2`, and only for that one selected subject.

This slice is allowed to:

- restate the accepted item-1 boundary freeze and accepted item-2 subject
  selection as binding predecessor authority;
- identify the one exact selected subject by its already-frozen family,
  anchor, owner-local frame, route, clear-boundary-only status, and exact
  packet identity;
- define exactly what evidence would count, for that one selected subject
  only, as
  `acyclic still sufficient`,
  `single-component cyclic-structure successor lane justified`, or
  `stop without opening an architecture-amendment lane`;
- make the contract reviewer-auditable by stating the preserved invariants
  that must hold under every lawful outcome:
  `iso-recursive = keep`,
  `non-equi-recursive = keep`,
  `no-fallback = keep`,
  no multi-SCC search,
  no second interface,
  no fallback widening,
  and no production implementation; and
- hand off exact audit-target binding to item `4` and open-vs-stop choice to
  item `5` without deciding either of them here.

This slice is not allowed to:

- reopen subject selection, introduce a second subject, or treat `C1` as a
  coequal live reopened subject;
- treat `C5` or `C7` as second packets, second routes, or second
  interfaces;
- widen into broad family search, general cyclic search, multi-SCC search,
  second interfaces, fallback widening, or a broad capability claim;
- bind the exact accepted rows, packets, modules, commands, or review-visible
  output surfaces that item `4` will later audit;
- decide whether rev-002 opens one bounded architecture-amendment lane or
  stops;
- choose one of the three lawful outcome tokens as the current round result;
  or
- authorize source edits, tests, `mlf2.cabal` edits, `Bugs.md` edits,
  controller-state edits, roadmap edits, or commits.

If the artifact starts selecting an outcome, naming the exact item-4 audit
surface, turning `C1` into a second lane, or implying that one architecture
lane is already opened, it has already widened beyond item `3` and must fail
closed.

## Explicit Disallowed Scope

The following remain explicitly disallowed in this round:

- any change to accepted item-1 truth, accepted item-2 truth, accepted
  rev-001 truth, or the rule that rev-001 items `6` through `8` stay
  blocked;
- any widening into multiple subjects, multiple packets, unrelated routes,
  broad family search, or multiple round outputs;
- any re-interpretation of the selected same-lane pocket into multi-SCC
  search, second-interface work, fallback widening, or production behavior;
- any attempt to freeze the exact later audit bind across rows, packets,
  modules, commands, or output surfaces;
- any attempt to decide item `5` open-one-lane versus stop;
- any equi-recursive semantics, implicit unfolding, multi-SCC search,
  second interfaces, fallback widening, production implementation,
  hardening, or repo-level capability claim;
- any edit to `orchestrator/state.json`, roadmap files, retry files,
  verification files, `Bugs.md`, source code, tests, or Cabal files; and
- any commit.

## Sequential Tasks

### Task 1 - Re-establish item-3 contract-only ownership

- Write the future canonical item-3 artifact as `attempt-1` with
  `retry: null`.
- State explicitly that item `3` is docs-only contract work on the already
  selected reopened subject only.
- State explicitly that item `3` defines the evidence bars only; it does not
  bind the item-4 audit target or decide the item-5 open-vs-stop result.
- Preserve accepted rev-001 truth, accepted round-104 item-1 truth, accepted
  round-105 item-2 truth, and the inherited explicit-only / iso-recursive /
  non-equi-recursive / non-cyclic-graph / no-second-interface /
  no-fallback boundary unchanged.

### Task 2 - Restate the one lawful selected-subject frame

- Use `selection.md`, the rev-002 roadmap bundle, accepted round-104 review,
  accepted round-105 review, the item-1 freeze, the item-2 selection
  artifact, the accepted representative matrix, and the accepted same-lane
  predecessor docs to restate the exact one-subject frame item `3` is
  allowed to govern.
- Carry forward these fixed inputs:
  - the selected subject is one exact same-lane retained-child /
    public-output continuity pocket only;
  - `C2`, `C5`, and `C7` are continuity faces of that same exact pocket, not
    separate reopened subjects;
  - non-local `C1` remains contrast context only; and
  - rev-002 remains planning-only and architecture-only while rev-001 items
    `6` through `8` stay blocked.
- Do not relitigate whether the candidate boundary exists or whether the
  subject was selected. Accepted items `1` and `2` already froze those
  truths.

### Task 3 - Define exactly three lawful outcome bars without selecting one

- Create the canonical item-3 artifact at the path above.
- Define exactly three reviewer-auditable outcome bars for the one selected
  subject only:
  - `acyclic still sufficient`
  - `single-component cyclic-structure successor lane justified`
  - `stop without opening an architecture-amendment lane`
- Make the contract fail closed:
  weaker evidence, mixed outcomes, broadened scope, or subject drift must not
  count as satisfying any lawful outcome bar.
- Preserve these hard invariants under every lawful outcome bar:
  `iso-recursive = keep`,
  `non-equi-recursive = keep`,
  `no-fallback = keep`,
  no multi-SCC search,
  no second interfaces,
  no fallback widening,
  and no production implementation.
- State explicitly that the contract is only an evidence-bar definition for
  later review and later decision making. Item `3` does not record which
  outcome is true.

### Task 4 - Freeze later-item ownership without binding later surfaces

- State explicitly that item `4` still owns the exact audit bind across the
  selected subject's accepted rows, packets, modules, commands, and
  review-visible output surfaces.
- State explicitly that item `5` still owns the one lawful decision on
  whether to open one bounded architecture-amendment lane or stop.
- Reassert that this round remains docs-only, planning-only, and
  architecture-only. The contract is not implementation clearance, not audit
  completion, and not a broad architecture claim.
- Reassert that no file outside the one frozen output path is authorized for
  item `3`.

## Required Verification After The Plan Lands

- `git diff --check`
- `git status --short --branch`
- `python3 -m json.tool orchestrator/state.json >/dev/null`
- `rg -n '"contract_version": 2|"roadmap_id":|"roadmap_revision":|"roadmap_dir":|"retry": null|"retry": \{' orchestrator/state.json`
- `roadmap_dir="$(jq -r '.roadmap_dir' orchestrator/state.json)" && test -f "$roadmap_dir/roadmap.md" && test -f "$roadmap_dir/retry-subloop.md" && test -f "$roadmap_dir/verification.md"`
- `roadmap_dir="$(jq -r '.roadmap_dir' orchestrator/state.json)" && rg -n '^\d+\. \[(pending|in-progress|done)\]' "$roadmap_dir/roadmap.md"`
- `rg -n 'safety / acceptance contract|acyclic still sufficient|single-component cyclic-structure successor lane justified|stop without opening an architecture-amendment lane|iso-recursive|non-equi-recursive|no-fallback|rev-001 items `6` through `8`|item `4`|item `5`|multi-SCC|second interfaces|fallback widening|production implementation|commit' orchestrator/rounds/round-106/plan.md`
- `git diff --name-only -- orchestrator/state.json Bugs.md src src-public app test mlf2.cabal orchestrator/roadmaps`

Review must also confirm the active item-3-specific checks from the live
verification contract:

- exactly one selected reopened subject remains in scope;
- the contract defines exactly three lawful outcome bars and does not select
  one of them;
- the contract preserves `iso-recursive = keep`,
  `non-equi-recursive = keep`, `no-fallback = keep`, and the no-multi-SCC
  boundary;
- the round does not reopen subject selection, bind the exact item-4 audit
  target / surfaces, or decide item `5`;
- accepted item-1 truth, accepted item-2 truth, and blocked rev-001 items
  `6` through `8` remain binding; and
- the round remains docs-only, so the full `cabal build all && cabal test`
  gate is explicitly skipped as out of scope unless the diff escapes the
  authorized docs / round-artifact surface.
