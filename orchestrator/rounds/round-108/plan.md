# Round 108 Plan (`item-5` Open-One-Bounded-Lane Or Stop Decision)

## Objective

Execute only roadmap item `5` and prepare one docs-only aggregate decision
artifact at:
`docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-architecture-amendment-lane-open-or-stop-decision.md`.

This is the initial `item-5` plan for `attempt-1` with `retry: null`. The
round must consume accepted items `1` through `4` as authoritative
predecessor truth, remain rev-002 planning-only and architecture-only, and
record exactly one lawful outcome for the one selected same-lane
`C2` / `C5` / `C7` pocket only:

- `open one bounded same-family architecture-amendment lane`; or
- `stop without opening that lane`.

Current planning read: the accepted evidence chain now most strongly supports
`open one bounded same-family architecture-amendment lane`, not `stop`,
because accepted `round-103` already reopened the inherited
`non-cyclic-graph` question, accepted rev-002 item `1` froze exactly one
bounded single-component same-family candidate boundary, accepted item `2`
selected exactly one live same-lane `C2` / `C5` / `C7` pocket inside that
boundary, accepted item `3` preserved
`single-component cyclic-structure successor lane justified` as a lawful
bounded bar for that one pocket, and accepted predecessor evidence still
keeps the selected pocket below authoritative public recursive continuity
while the bounded `Pipeline.hs` / `TermClosure.hs` slice still exposes no
alternate whole-packet authoritative recursive result inside the inherited
surface set. The final artifact must still be the aggregate decision record
only, must fail closed to `stop without opening that lane` if the accepted
item-1-through-item-4 evidence does not cleanly justify the bounded
open-lane outcome, and must not become a new contract or a redefinition of
the item-3 bars.

This round is docs-only and aggregate-only. It must preserve
`iso-recursive = keep`, `non-equi-recursive = keep`, and `no-fallback = keep`;
keep accepted items `1` through `4` authoritative; keep rev-001 items
`6` through `8` blocked unless a later accepted successor revision changes
that status explicitly; and keep multi-SCC search, second interfaces,
fallback widening, production implementation, hardening, and broad
capability claims blocked. It must not edit controller-owned machine state,
roadmap contracts, retry contracts, verification contracts, `Bugs.md`,
source code, tests, or Cabal files. No commit is authorized.

## Locked Round Context

- Round id: `round-108`
- Roadmap item: `item-5`
- Stage: `plan`
- Active attempt: `attempt-1`
- Retry state: `null`
- Active branch: `codex/round-108`
- Active worktree:
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-108`
- Fixed live subject: one docs-only rev-002 item-5 aggregate decision for
  the exact same-lane retained-child / public-output continuity pocket
  already selected in accepted item `2`, contract-governed in accepted
  item `3`, and audit-bound in accepted item `4`
- Current round review feedback: none yet; only
  `orchestrator/rounds/round-108/selection.md` exists for this round, so
  this is a full `attempt-1` plan rather than a retry delta

Current worktree state is already non-pristine. Respect existing edits and do
not revert unrelated work:

- controller-owned `orchestrator/state.json` may already be modified and
  must remain untouched; and
- existing `orchestrator/rounds/round-108/selection.md` is a round input and
  must remain untouched.

Under the live retry contract, item `5` is aggregate-only. Review may reject
and send the same round back to `plan`, but `accepted + retry` is not lawful
for this item.

## Accepted Continuity That Remains Binding

- `orchestrator/rounds/round-108/selection.md`
  fixes this round to roadmap item `5` only, requires a docs-only
  aggregate lane-open-or-stop decision for the one selected same-lane
  pocket, preserves accepted item-1 through item-4 truth, keeps rev-001
  items `6` through `8` blocked, and explicitly blocks implementation work,
  subject reselection, item-3 bar redefinition, and scope widening.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/roadmap.md`
  makes item `5` the only remaining pending item in rev-002 and defines
  completion as one accepted aggregate-only docs artifact that records
  exactly one outcome:
  open one bounded same-family architecture-amendment lane for the selected
  subject, or stop without opening that lane.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/retry-subloop.md`
  keeps item `5` aggregate-only, forbids `accepted + retry` for this item,
  and preserves prior-attempt immutability if review sends the round back to
  `plan`.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/verification.md`
  requires reopened-lane-decision checks proving that item `5` records
  exactly one open-versus-stop outcome and does not silently authorize
  multi-SCC search, second interfaces, fallback behavior, production
  implementation, or repo-level capability claims.
- `orchestrator/rounds/round-107/review-record.json`
  is the authoritative acceptance record for predecessor item `4`. Its
  accepted checks make the exact row / packet / module / command / surface
  bind authoritative for later aggregate decision use.
- `docs/plans/2026-03-26-global-non-cyclic-graph-keep-vs-reopen-decision-gate.md`
  is the authoritative accepted rev-001 item-5 reopen record. It preserved
  `iso-recursive = keep`, `non-equi-recursive = keep`, and
  `no-fallback = keep`, rejected `non-cyclic-graph = keep` on the accepted
  global aggregate record, reopened only the `non-cyclic-graph` question,
  and kept rev-001 items `6` through `8` blocked.
- `docs/plans/2026-03-26-global-non-cyclic-graph-reopened-revision-authority-and-candidate-boundary-freeze.md`
  is the authoritative item-1 freeze. It kept rev-002 planning-only and
  architecture-only, froze exactly one candidate boundary, and kept
  rev-001 items `6` through `8` blocked.
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-reopened-subject-selection.md`
  is the authoritative item-2 selection. It chose exactly one live reopened
  subject:
  the same exact `C2` / `C5` / `C7` pocket, with family, anchor,
  owner-local frame, route, clear-boundary-only status, and exact packet
  identity all frozen.
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-safety-and-acceptance-contract.md`
  is the authoritative item-3 contract. It defines exactly three lawful
  bars for that same exact pocket, preserves
  `iso-recursive = keep`, `non-equi-recursive = keep`, and
  `no-fallback = keep`, and explicitly defers the final open-lane-or-stop
  decision to item `5`.
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-architecture-pressure-audit-target-and-evaluation-surface-bind.md`
  is the authoritative item-4 bind. It freezes the exact accepted rows
  `C2`, `C5`, and `C7`, the exact packet, the exact source/test module set,
  the exact command set, and the exact six review-visible surfaces for the
  selected pocket only, while keeping `C1` contrast-only and explicitly
  deferring the item-5 decision.
- `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md`
  keeps `C2` and `C5` as one same exact blocker-debt pocket only, not two
  packets, not two routes, and not two successor lanes.
- `docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md`
  keeps `C7` as that same exact `C2` / `C5` pocket through the
  public-output continuity lens only, with recursive structure still
  helper-visible/internal and authoritative public output still collapsed to
  `forall identity`.
- `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md`
  remains the accepted global matrix read: zero
  `stable visible persistence` rows, same-family blocker debt concentrated
  on the selected same-lane pocket, and no accepted widening into
  multi-SCC search, second interfaces, or fallback behavior.
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md`
  remains bounded predecessor context only. It preserved the earlier exact
  same-pocket `blocker debt remains within the current architecture` read,
  but it no longer settles the reopened rev-002 gate by itself.
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-collapse-clear-or-confirm.md`
  remains bounded predecessor context only. It keeps the accepted fact that
  the bounded `Pipeline.hs` / `TermClosure.hs` root-handoff slice exposes no
  alternate whole-packet authoritative recursive result for this pocket
  inside the inherited surface set.
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  remains the inherited boundary contract:
  explicit-only baseline,
  iso-recursive only,
  non-equi-recursive,
  inherited `non-cyclic-graph` boundary,
  no second interface,
  and no fallback widening.

## File Map

### Create

- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-architecture-amendment-lane-open-or-stop-decision.md`
  - Responsibility: canonical docs-only rev-002 item-5 aggregate decision
    record for the one selected same-lane `C2` / `C5` / `C7` pocket only.
    It must consume accepted items `1` through `4`, preserve inherited keep
    axes and blocked work, and record exactly one lawful outcome:
    open one bounded same-family architecture-amendment lane, or stop
    without opening that lane.

### Read-Only Evidence

- `orchestrator/rounds/round-108/selection.md`
- `orchestrator/rounds/round-107/review-record.json`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/roadmap.md`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/retry-subloop.md`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/verification.md`
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-keep-vs-reopen-decision-gate.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-reopened-revision-authority-and-candidate-boundary-freeze.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-reopened-subject-selection.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-safety-and-acceptance-contract.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-architecture-pressure-audit-target-and-evaluation-surface-bind.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-collapse-clear-or-confirm.md`

### Preserve Unchanged

- `orchestrator/state.json`
- `orchestrator/rounds/round-108/selection.md`
- roadmap files under
  `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/`
- `Bugs.md`
- `src/`
- `src-public/`
- `app/`
- `test/`
- `mlf2.cabal`
- every `docs/plans/` artifact other than the one exact output path above

No round-local `implementation-notes.md`, side ledger, alternate decision
memo, or supplemental contract is authorized. The docs artifact above is the
only intended implementation output path. The implementer must not create or
edit any other file for item `5`.

## Exact Selected Slice (Exactly One)

The only selected slice is:

record one aggregate item-5 decision for the one already-selected same-lane
retained-child / public-output continuity pocket carried by accepted rows
`C2`, `C5`, and `C7`, and only that one selected pocket.

### Exact Selected Pocket

The selected pocket remains exactly:

`the exact same-lane retained-child / public-output continuity pocket already carried by accepted rows C2, C5, and C7, with non-local C1 retained only as bounded contrast context`

The same frozen tuple remains binding:

- family: same-lane retained-child
- recursive-shape anchor: `boundVarTargetRoot`
- owner / binder frame: one owner-local retained-child frame
- route: `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC`
- quantified-boundary status: clear-boundary only:
  `boundHasForallFrom` is false and `not hasForall` holds

The final artifact must continue to treat:

- `C2` as the primary same-lane retained-child face of this one pocket;
- `C5` as the owner-local / binder-sensitive face of this same exact pocket
  only, not a second packet or second route; and
- `C7` as the public-output continuity face of this same exact pocket only,
  not a second interface or second public path.

`C1` may appear only as bounded contrast context. It must not become a
second selected subject, a second audit target, a second packet, or a
replacement route.

### Exact Packet

The aggregate decision remains bound to exactly this packet and no variant:

```haskell
ELet "k" (ELamAnn "x" recursiveAnn (EVar "x"))
  (ELet "u" (EApp (ELam "y" (EVar "y")) (EVar "k")) (EVar "u"))
```

where
`recursiveAnn = STMu "a" (STArrow (STVar "a") (STBase "Int"))`.

### Exact Item-5 Decision Set

The final artifact must record exactly one item-5 outcome token and no
other:

| Lawful item-5 outcome | What it may mean | What it must not mean |
| --- | --- | --- |
| `open one bounded same-family architecture-amendment lane` | one later same-family successor lane may be considered for this same exact pocket only, still bounded narrower than the full reopened family and still preserving inherited keep axes and blocked work | not multi-SCC search, not a second interface, not fallback widening, not production implementation, not hardening, not repo-level capability clearance |
| `stop without opening that lane` | this rev-002 pocket-level decision ends without opening a same-family architecture-amendment lane for the selected pocket | not a reopened subject-selection pass, not a new contract, not a keep-bar redefinition, not implied clearance for rev-001 items `6` through `8` |

The final artifact may cite the authoritative item-3 bars as predecessor
decision inputs only. It must not rename them, restate them, or invent a
fourth bar.

### Current Planning Read To Carry Into The Aggregate Decision

The plan-level read remains:
`open one bounded same-family architecture-amendment lane` is currently the
stronger outcome on accepted evidence.

That read is bounded by the accepted record only:

- the accepted global reopen already withdrew the stronger
  `non-cyclic-graph = keep` aggregate result;
- the accepted rev-002 freeze already narrowed the live question to one
  bounded same-family single-component candidate boundary only;
- the accepted item-2 subject selection already says there is exactly one
  lawful live same-lane pocket to evaluate;
- the accepted item-4 bind already freezes exact rows, packet, modules,
  commands, and six review-visible surfaces for that one pocket only; and
- the accepted predecessor evidence still shows helper-visible/internal
  recursive continuity without authoritative public recursive persistence,
  while the bounded accepted collapse audit still exposes no alternate
  whole-packet authoritative recursive result on the inherited surface set.

The implementation must still write the final artifact as the aggregate
decision record, not as a new proof contract. If the accepted item-1-through-
item-4 evidence cannot justify the bounded open-lane result without widening
scope or weakening inherited invariants, the artifact must fail closed to
`stop without opening that lane`.

## Explicit Disallowed Scope

The following remain explicitly blocked in this round and in the item-5
implementation it plans:

- any change to accepted item-1 truth, accepted item-2 truth, accepted
  item-3 truth, accepted item-4 truth, or accepted rev-001 truth;
- any reopening of subject selection, any introduction of a second selected
  subject, or any promotion of `C1` into a coequal live subject;
- any redefinition of the item-3 bars, any replacement contract, or any
  alteration of the item-4 exact bind across rows, packet, modules,
  commands, or review-visible surfaces;
- any widening into multiple packets, multiple routes, broader family
  search, multi-SCC search, second interfaces, fallback widening,
  equi-recursive semantics, implicit unfolding, production implementation,
  hardening, or broad capability claims;
- any claim that rev-001 items `6` through `8` are unblocked by implication;
- any creation or edit outside the one exact item-5 output path named above;
- any touch to `orchestrator/state.json`, roadmap files, retry files,
  verification files, `Bugs.md`, source code, tests, or Cabal files;
- any fresh mechanism experiment, new replay campaign, or new source-level
  claim outside accepted evidence aggregation; and
- any commit.

If later implementation starts reopening the subject boundary, treating
`C5` or `C7` as second packets or second interfaces, weakening
`iso-recursive = keep`, `non-equi-recursive = keep`, or `no-fallback = keep`,
or implying that code work is now authorized, it has already widened beyond
item `5` and must fail closed.

## Required Verification

Required baseline checks for the future item-5 implementation:

- `git diff --check`
- `python3 -m json.tool orchestrator/state.json >/dev/null`
- `rg -n '"contract_version": 2|"roadmap_id":|"roadmap_revision":|"roadmap_dir":|"retry": null|"retry": \{' orchestrator/state.json`
- `roadmap_dir="$(jq -r '.roadmap_dir' orchestrator/state.json)" && test -f "$roadmap_dir/roadmap.md" && test -f "$roadmap_dir/retry-subloop.md" && test -f "$roadmap_dir/verification.md"`
- `roadmap_dir="$(jq -r '.roadmap_dir' orchestrator/state.json)" && rg -n '^\d+\. \[(pending|in-progress|done)\]' "$roadmap_dir/roadmap.md"`
- predecessor-presence checks for:
  - `orchestrator/rounds/round-107/review-record.json`
  - `docs/plans/2026-03-26-global-non-cyclic-graph-keep-vs-reopen-decision-gate.md`
  - `docs/plans/2026-03-26-global-non-cyclic-graph-reopened-revision-authority-and-candidate-boundary-freeze.md`
  - `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-reopened-subject-selection.md`
  - `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-safety-and-acceptance-contract.md`
  - `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-architecture-pressure-audit-target-and-evaluation-surface-bind.md`

Required item-5 decision checks for the future implementation:

- confirm the final artifact exists only at
  `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-architecture-amendment-lane-open-or-stop-decision.md`
- confirm the artifact records exactly one lawful item-5 outcome for the
  selected same-lane `C2` / `C5` / `C7` pocket:
  `open one bounded same-family architecture-amendment lane` or
  `stop without opening that lane`
- confirm the artifact preserves
  `iso-recursive = keep`,
  `non-equi-recursive = keep`, and
  `no-fallback = keep`
- confirm the artifact keeps rev-001 items `6` through `8` blocked
- confirm the artifact keeps multi-SCC search, second interfaces, fallback
  widening, production implementation, hardening, and broad capability
  claims blocked
- confirm the artifact treats `C1` as contrast-only and keeps `C5` / `C7`
  as rereads of the same exact pocket only
- confirm the artifact does not redefine the item-3 bars and does not alter
  the item-4 bind

Required scope checks for the future implementation:

- docs-diff review proving the implementation changed only the one exact
  output path above and did not touch `orchestrator/state.json`
- explicit skip note that `cabal build all && cabal test` remains out of
  scope because the round is docs-only and the diff stayed outside `src/`,
  `src-public/`, `app/`, `test/`, and `mlf2.cabal`
- `git status --short --branch` before handoff so the round record captures
  the exact docs-only diff and any preexisting controller-owned state edit

The reviewer-facing acceptance record for item `5` must still show:

- one implemented stage result;
- one attempt verdict;
- one stage action;
- one retry reason;
- one fix hypothesis; and
- one bounded decision summary tied to the accepted item-1-through-item-4
  evidence chain only.
