# Round 066 Plan (`L1` Continue-Bounded Next-Target Bind)

## Objective

Execute only roadmap item `L1` and prepare one docs-only bind artifact at:
`/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-066/docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md`.

This is the initial `L1` plan for `attempt-1` with `retry: null`. The round
must carry forward accepted `K4 = continue-bounded`, preserve repaired
`URI-R2-C1` plus the inherited
`explicit-only / non-equi-recursive / non-cyclic-graph / no-second-interface / no-fallback`
boundary, and decide from the current accepted continuity packet whether one
fresh exact bounded successor slice is actually supported.

Current planning read: accepted evidence does **not** presently support a fresh
local `baseTarget` / same-lane `targetC` successor slice. The accepted `I`,
`J`, and `K` chains already own the local `baseTarget` families, and the
remaining generic `Just baseC | rootIsSchemeAlias && rootBoundIsBaseLike -> baseC`
`targetC` arm appears to be preserved broader continuity only rather than a new
lawful local sublane. `attempt-1` must verify that case analysis directly from
the read-only anchors and fail closed if it holds. Do not invent a residual
local slice merely because the generic scheme-alias / base-like arm still
exists in the source.

If the direct case analysis unexpectedly proves exactly one fresh lawful local
slice after all accepted exclusions are applied, the canonical `L1` artifact
may freeze that one slice only. Otherwise the canonical `L1` artifact must
record that the accepted evidence does not currently support a fresh exact bind
without reopening accepted `I` / `J` / `K` work or widening into preserved
broader continuity.

`L1` is docs-only bind / selection work. It does not implement a future `L2`
slice, does not verify a future `L2` slice, does not amend the roadmap, does
not edit controller state, and does not authorize replay reopen, `MLF.Elab.Inst`,
`InstBot`, `boundVarTarget`, `boundTarget`, `schemeBodyTarget`,
`src/MLF/Elab/Run/ResultType/View.hs`, non-local widening, or any broader
trigger-family widening.

## Locked Round Context

- Round id: `round-066`
- Roadmap item: `L1`
- Stage: `plan`
- Active attempt: `attempt-1`
- Retry state: `null`
- Fixed live subject: repaired `URI-R2-C1`
- Active branch: `codex/round-066-l1-next-target-bind`
- Active worktree:
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-066`
- Fixed inherited boundary:
  `explicit-only / non-equi-recursive / non-cyclic-graph / no-second-interface / no-fallback`
- Stage mode: docs-only bind / selection only
- Current round review feedback: none yet; this is a full `attempt-1` plan,
  not a retry delta

Current repository state is already non-pristine. Respect existing work and do
not revert or clean up unrelated changes:

- `M orchestrator/state.json`
- `?? orchestrator/rounds/round-066/selection.md`
- `?? orchestrator/rounds/round-066/plan.md`

## Accepted Continuity That Remains Binding

- `orchestrator/rounds/round-065/review-record.json` finalized `K4` as
  authoritative `attempt: 1`, `attempt_verdict: accepted`,
  `stage_action: finalize`, `status: authoritative`, with canonical artifact
  path `docs/plans/2026-03-21-uri-r2-c1-k4-next-cycle-decision-gate.md`.
- The accepted `K4` artifact is the controlling predecessor decision. It
  records `continue-bounded`, requires a fresh exact target bind before any
  successor implementation, and does not authorize replay reopen,
  `MLF.Elab.Inst`, `InstBot`, `boundVarTarget`, `boundTarget`,
  `schemeBodyTarget`, `View.hs`, non-local widening, or any broader
  trigger-family widening.
- The accepted `I1` / `I2` / `I3` / `I4` chain already consumed the repaired
  local single-base `rootLocalSingleBase` /
  `baseTarget -> baseC` / same-lane `targetC` lane.
- The accepted `J1` / `J2` / `J3` / `J4` chain already consumed the repaired
  local inst-arg-only singleton-base `rootLocalInstArgSingleBase` /
  `baseTarget -> baseC` / same-lane `targetC` lane.
- The accepted `K1` / `K2` / `K3` / `K4` chain already consumed the repaired
  local empty-candidate / no-inst-arg scheme-alias / base-like
  `rootLocalEmptyCandidateSchemeAliasBaseLike` /
  `baseTarget -> baseC` / same-lane `targetC` lane.
- The accepted `F1` / `F2` / `F3` / `F4` chain already consumed the local
  `rootLocalSchemeAliasBaseLike` `keepTargetFinal` / `targetC -> rootFinal`
  lane. It remains accepted predecessor continuity only and may not be
  reopened as a new `L1` family.
- The accepted `C`, `E`, `G`, and `H` cycles already consumed the local
  `schemeBodyTarget` baseline, retained-child `boundVarTarget`, local
  `rootLocalMultiInst`, and local `rootLocalInstArgMultiBase` families.
  None of those families may be reopened in `L1`.
- `/Volumes/src/mlf4/Bugs.md` remains continuity context only. Its `## Open`
  section is empty, so current bug state does not authorize replay reopen,
  `InstBot`, or any broader successor family here.
- Accepted negative findings remain binding:
  `U2 = authority-narrowed`,
  `U3 = uniqueness-owner-stable-refuted`,
  `U4 = constructor-acyclic-termination-refuted`.

## File Map

### Modify

- `docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md`
  - Responsibility: record the `L1` docs-only bind attempt, reconstruct the
    accepted `K4` continuity packet, and either freeze one exact lawful fresh
    slice or fail closed by recording why the current accepted evidence does
    not support one.

### Read-Only Evidence

- `AGENTS.md`
- `orchestrator/roles/planner.md`
- `orchestrator/state.json`
- `orchestrator/roadmap.md`
- `orchestrator/retry-subloop.md`
- `orchestrator/verification.md`
- `orchestrator/rounds/round-066/selection.md`
- `docs/superpowers/specs/2026-03-18-unannotated-iso-recursive-continue-bounded-cycle-design.md`
- `docs/plans/2026-03-21-uri-r2-c1-k4-next-cycle-decision-gate.md`
- `docs/plans/2026-03-21-uri-r2-c1-k1-next-target-bind.md`
- `docs/plans/2026-03-21-uri-r2-c1-k2-bounded-implementation-slice.md`
- `docs/plans/2026-03-21-uri-r2-c1-k3-bounded-verification-gate.md`
- `docs/plans/2026-03-20-uri-r2-c1-i1-next-target-bind.md`
- `docs/plans/2026-03-20-uri-r2-c1-j1-next-target-bind.md`
- `docs/plans/2026-03-19-uri-r2-c1-f1-next-target-bind.md`
- `docs/plans/2026-03-19-uri-r2-c1-f2-bounded-implementation-slice.md`
- `/Volumes/src/mlf4/Bugs.md`
- `src/MLF/Elab/Run/ResultType/Fallback.hs`
- `test/PipelineSpec.hs`

### Preserve Unchanged

- `orchestrator/state.json`
- `orchestrator/roadmap.md`
- `orchestrator/rounds/round-066/selection.md`
- `/Volumes/src/mlf4/Bugs.md`
- `src/`
- `src-public/`
- `app/`
- `test/`
- `mlf2.cabal`

## Sequential Tasks

### Task 1 - Re-establish exact `L1` authority and fail-closed round boundary

- Write the plan and future canonical `L1` artifact as `attempt-1` with
  `retry: null`.
- State explicitly that `L1` is docs-only bind / selection work under accepted
  `K4 = continue-bounded`; it does not implement a future slice, does not
  verify a future slice, does not pre-decide a later decision gate, and does
  not update the roadmap.
- Preserve repaired `URI-R2-C1` as the only live subject and preserve the
  inherited explicit-only / non-equi-recursive / non-cyclic-graph /
  no-second-interface / no-fallback boundary unchanged.
- Treat accepted `J4` / `K1` / `K2` / `K3` / `K4` and the accepted `F` / `I`
  / `J` predecessor binds as immutable continuity evidence only.
- State explicitly that `L1` must not fabricate a new residual local
  scheme-alias / base-like `baseTarget` family unless the current accepted
  anchors prove one exists after all accepted exclusions are applied.

### Task 2 - Prove or refute the existence of one fresh lawful local `baseTarget` slice

- Reconstruct the current `Fallback.hs` `baseTarget` and `targetC` packet from
  read-only anchors only:
  - `Fallback.hs:367-406` for the `baseTarget` source-of-truth conditions;
  - `Fallback.hs:521-553` for the named local proof cluster; and
  - `Fallback.hs:693-710` for the downstream `targetC` ordering.
- Perform the case analysis that matters for `L1`:
  - if a local `TypeRef` root reaches `baseTarget` through a singleton
    `rootBoundCandidates` path, that lane is already owned by accepted
    `rootLocalSingleBase`;
  - if a local `TypeRef` root reaches `baseTarget` through
    `IntSet.null rootBaseBounds && IntSet.size instArgBaseBounds == 1`, that
    lane is already owned by accepted `rootLocalInstArgSingleBase`;
  - if a local `TypeRef` root reaches `baseTarget` through empty
    `rootBoundCandidates` plus empty `instArgBaseBounds` together with
    scheme-alias / base-like authority, that lane is already owned by
    accepted `rootLocalEmptyCandidateSchemeAliasBaseLike`; and
  - the generic singleton fallback inside `baseTarget` still collapses back
    into the accepted `rootLocalSingleBase` family when the root remains on
    the local `TypeRef` lane.
- Use that case analysis to answer the exact `L1` question:
  - either there really is one fresh lawful local slice after the accepted
    `I` / `J` / `K` exclusions; or
  - the remaining generic `Just baseC | rootIsSchemeAlias && rootBoundIsBaseLike -> baseC`
    arm is only preserved broader continuity and cannot lawfully be rebound
    into a fresh local slice without reopening accepted `I` / `J` / `K` work
    or selecting non-local behavior.
- Current planning read is the second case. The implementer must verify that
  read from the anchors and write the canonical artifact accordingly.

### Task 3 - Write the canonical `L1` artifact according to the verified case analysis

- Create
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-066/docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md`
  for `attempt-1`.
- The artifact must include:
  - the accepted `K4` authority and fresh-bind requirement;
  - the accepted ownership of the local `I`, `J`, and `K` `baseTarget` lanes;
  - the accepted `F2` / `F3` `rootLocalSchemeAliasBaseLike` /
    `targetC -> rootFinal` lane as preserved continuity only;
  - the current `Fallback.hs` case analysis over `baseTarget` and `targetC`;
  - the explicit conclusion as to whether one fresh lawful exact slice exists.
- If the case analysis confirms the current planning read, the artifact must
  fail closed:
  - record that the accepted evidence does not currently support a fresh exact
    `L2` slice inside the allowed boundary;
  - record that the remaining generic scheme-alias / base-like `targetC` arm
    is preserved broader continuity only;
  - record that selecting it as a fresh local slice would reopen accepted
    `I` / `J` / `K` work or widen into behavior the current selection and
    roadmap explicitly keep out of scope; and
  - stop there rather than inventing a target.
- If the case analysis unexpectedly proves one fresh lawful local slice, the
  artifact may freeze that one slice only. In that branch, it must also record
  the exact future ownership boundary (`Fallback.hs` plus `PipelineSpec.hs`)
  and the exact focused verification intent for that one slice only.

### Task 4 - Keep the non-selection and exclusion list explicit

- State explicitly that `L1` does not authorize implementation, verification,
  merge action, roadmap mutation, controller-state edits, bug-tracker edits,
  replay reopen, or subject widening.
- State explicitly that `L1` does not reopen or select:
  - accepted `F1` / `F2` / `F3` / `F4`;
  - accepted `I1` / `I2` / `I3` / `I4`;
  - accepted `J1` / `J2` / `J3` / `J4`;
  - accepted `K1` / `K2` / `K3` / `K4`;
  - `boundVarTarget`;
  - `boundTarget`;
  - `schemeBodyTarget`;
  - `src/MLF/Elab/Run/ResultType/View.hs`;
  - non-local widening; or
  - any broader trigger family.
- State explicitly that the broader scheme-alias / base-like `baseTarget`
  route remains inherited continuity only unless the accepted case analysis
  proves a narrower lawful local slice without reopening accepted work.

### Task 5 - Run the docs-only verification required for the bind round

Run the baseline docs/state checks required by `orchestrator/verification.md`:

- `git diff --check`
- `python3 -m json.tool orchestrator/state.json >/dev/null`
- `rg -n '"contract_version": 2|"retry": null|"retry": \{' orchestrator/state.json`
- `rg -n '^\d+\. \[(pending|in-progress|done)\]' orchestrator/roadmap.md`
- `test -f docs/superpowers/specs/2026-03-20-unannotated-iso-recursive-continue-bounded-h-cycle-design.md`
- `test -f docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
- `test -f docs/plans/2026-03-14-unannotated-iso-recursive-r5-research-stop-decision.md`
- `test -f docs/plans/2026-03-17-uri-r2-c1-r4-repair-decision-gate.md`
- `test -f docs/plans/2026-03-17-uri-r2-c1-u6-next-widening-decision-gate.md`
- `test -f orchestrator/retry-subloop.md`

Run `L1`-specific continuity and anchor checks:

- `test -f docs/plans/2026-03-21-uri-r2-c1-k4-next-cycle-decision-gate.md`
- `python3 -m json.tool orchestrator/rounds/round-065/review-record.json >/dev/null`
- short `python3` assertion over `orchestrator/rounds/round-065/review-record.json`
  confirming:
  - `stage_id == "K4"`
  - `attempt == 1`
  - `attempt_verdict == "accepted"`
  - `stage_action == "finalize"`
  - `status == "authoritative"`
  - `artifact_path == "docs/plans/2026-03-21-uri-r2-c1-k4-next-cycle-decision-gate.md"`
- `nl -ba src/MLF/Elab/Run/ResultType/Fallback.hs | sed -n '367,406p'`
  -> capture the current `baseTarget` case tree.
- `nl -ba src/MLF/Elab/Run/ResultType/Fallback.hs | sed -n '521,553p'`
  -> capture the current local proof cluster.
- `nl -ba src/MLF/Elab/Run/ResultType/Fallback.hs | sed -n '693,710p'`
  -> capture the current `targetC` ordering, including the preserved generic
  scheme-alias / base-like arm.
- `nl -ba test/PipelineSpec.hs | sed -n '1244,1303p'`
  -> capture the current scheme-alias / base-like helper packet.
- `nl -ba test/PipelineSpec.hs | sed -n '1594,1620p'`
  -> capture the accepted local empty-candidate example plus preserved local
  continuity.
- `nl -ba test/PipelineSpec.hs | sed -n '1698,1758p'`
  -> capture the source-guard packet naming the explicit local proofs and the
  preserved generic arm.
- short check over `/Volumes/src/mlf4/Bugs.md` confirming `## Open` remains
  empty.

Run docs-only diff-scope checks:

- `git status --short --untracked-files=all`
- `git diff --name-only`

The round must explicitly skip `cabal build all && cabal test` because `L1`
is docs-only and must not touch `src/`, `src-public/`, `app/`, `test/`, or
`mlf2.cabal`. Record that skip reason in the canonical `L1` artifact.

## Review Handoff

Reviewer must confirm all of the following before approving `attempt-1`:

- the canonical `L1` artifact stays bounded to the exact bind question and
  does not drift into implementation, verification, or roadmap mutation;
- the accepted `I`, `J`, and `K` local `baseTarget` lanes remain immutable
  predecessor evidence only;
- the accepted `F2` / `F3` local `rootFinal` lane remains immutable
  predecessor evidence only;
- the artifact does not relabel the preserved generic
  `rootIsSchemeAlias && rootBoundIsBaseLike` arm as a fresh local slice
  unless the current accepted anchors actually prove that narrower claim;
- if no exact fresh slice is proven, the artifact records that contradiction
  explicitly and stops rather than inventing a target; and
- replay reopen, `MLF.Elab.Inst`, `InstBot`, `boundVarTarget`,
  `boundTarget`, `schemeBodyTarget`, `View.hs`, non-local widening, and any
  broader trigger-family widening remain explicitly excluded.

If the round cannot prove one exact lawful fresh slice from the accepted
anchors, review should treat that as a surfaced bind-stage contradiction, not
as permission to silently widen or reopen accepted families.
