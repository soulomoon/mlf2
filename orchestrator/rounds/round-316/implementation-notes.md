### Changes Made
- `test/ProgramParserParitySpec.hs`: added the public round-316 matcher for GADT-style result constructors and existential constructors, committed-source canonical projection checks, shared parser fixture checks, malformed constructor-local forall evidence, and shortcut guards for rejected fixture-key branches.
- `test/conformance/mlfp/parser-parity/gadt-result-constructor-spans/`: added the canonical source and expected projection for a `Main` module with `Nat`, parameterized `Expr a`, GADT-style constructor result heads, recursive constructor fields, and a constructor-pattern case expression.
- `test/conformance/mlfp/parser-parity/existential-constructor-forall/`: added the canonical source and expected projection for `SomeExpr : ∀ a. Expr a -> SomeExpr` plus the nested case expression over the existential package.
- `test/programs/compiler-parser-parity/gadt-result-constructor-spans/` and `test/programs/compiler-parser-parity/existential-constructor-forall/`: added thin fixture roots with only `Main.mlfp` and `ParserParityFixture.mlfp`, exposing `sourceFile` and `sourceText` before calling `renderParserParityProjectionFromSourceText`.
- `test/programs/compiler-parser-parity/parser-library/`: extended the shared lexer, diagnostic ADT, parser-combinator diagnostic propagation, shared parser grammar, span constants, and projection renderer for the bounded recursive-ADT syntax slice. The new parser uses internal projection keys (`recursive-gadt-projection`, `existential-forall-projection`) instead of fixture-key success shortcuts.
- `CHANGELOG.md`, `implementation_notes.md`, `docs/mlfp-self-boot-readiness.md`, and `test/conformance/mlfp/README.md`: recorded bounded parser-parity progress without claiming full parser parity, checker/resolver/backend, driver, platform, proof, or self-boot completion.

### Tests
- `test/ProgramParserParitySpec.hs`: verifies canonical Haskell parser projections match committed projections for the two new fixtures, verifies the shared `.mlfp` parser-library projections match the same outputs through public `run-program`, verifies malformed constructor-local forall syntax reports `expected-constructor-forall-dot`, and guards against round-316 fixture-key parser shortcuts.
- Focused RED evidence: `timeout 900 cabal test mlf2-test --test-options='--match "/MLF.Program parser parity/shared parser-owned .mlfp parser extends source-text grammar to GADT-style results and existential constructors/"'` failed before implementation with the shared parser fixture returning `Right "parser-error\n"` for `gadt-result-constructor-spans`.
- Focused GREEN evidence: the same command passed after implementation with `1 example, 0 failures` in `596.4105 seconds`.
- Parser-parity group: `timeout 3600 cabal test mlf2-test --test-options='--match "/MLF.Program parser parity/"'` timed out with status 124 after confirming the carried shared parser extension matchers and the new round-316 matcher passed; the later full `cabal test` completed the parser-parity group successfully.
- Direct run-program smokes: `timeout 600 dist-newstyle/build/aarch64-osx/ghc-9.14.1/mlf2-0.2.0.0/x/mlf2/build/mlf2/mlf2 run-program test/programs/compiler-parser-parity/gadt-result-constructor-spans --search-path test/programs/compiler-parser-parity/parser-library` passed and printed the committed projection. A `/bin/sh` loop using the same executable and `run-program ... --search-path test/programs/compiler-parser-parity/parser-library` passed for `basic-module-def-bool`, `import-exposing-def-bool`, `value-def-list-int-ref`, `let-lambda-application`, `typed-annotation-types`, `data-declaration-constructor-spans`, `case-expression-constructor-patterns`, `case-expression-nested-patterns`, `typeclass-deriving-method`, `typeclass-instance-nullary-method`, `higher-kinded-class-data-params`, `multiparam-superclass-fundep`, `type-family-kind-lambda`, `type-family-apply-annotation`, and `existential-constructor-forall`.
- Static audits: thin fixture audit, exact-source/static-token audit, rejected-shape audit, and shared parser architecture audit produced no matches. `git diff --check` passed.
- Full gates: `timeout 7200 cabal build all` passed. `timeout 10800 cabal test` passed with `2638 examples, 0 failures` in `7309.0625 seconds`. `timeout 7200 ./scripts/thesis-conformance-gate.sh` passed with `[thesis-gate] PASS: thesis conformance anchors are green`.

### Notes
The canonical project parser rejects retired ASCII `forall`; the positive and negative round-316 source fixtures therefore use canonical Unicode `∀`, which the shared parser-library lexer carries as the internal `forall` token. Temporary debug fixtures created under `dist-newstyle/parser-parity-debug-gadt/` during minimization were removed before handoff. Validation-generated churn in `runtime/mlfp_io/target/release/libmlfp_io.d` was restored after full gates.
