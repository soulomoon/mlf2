# Round 113 Plan (`item-1` Post-Amendment Settlement Surface And Successor Boundary Freeze)

## Objective

Execute only roadmap item `1` and prepare one docs-only aggregate artifact
at:
`docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-post-amendment-settlement-surface-and-successor-boundary-freeze.md`.

This is the initial `item-1` plan for `attempt-1` with `retry: null`. The
round must consume accepted rev-003 item `3` and accepted rev-003 item `4`
as authoritative predecessor truth, remain rev-004 planning/docs-first, and
freeze exactly this much, and no more:

- the same selected rows `C2`, `C5`, and `C7`;
- the same exact same-pocket boundary, exact packet, and exact tuple;
- one exact current-result surface anchored in accepted `round-111` /
  rev-003 item `3`;
- one exact future writable docs boundary limited to new rev-004 settlement
  artifacts only:
  `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-post-amendment-settlement-ledger.md`
  and
  `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-post-amendment-settlement-validation.md`;
  and
- the exact predecessor-artifact immutability rule for the still-live
  pre-amendment same-family docs and ledgers.

Current planning read: accepted `round-112` already recorded exactly one
lawful successor outcome,
`publish one later same-family successor revision for bounded post-amendment settlement`,
and it narrowed that successor revision to the same exact pocket only.
Item `1` must freeze the exact accepted current-result anchor from
`round-111` and the exact new-docs-only boundary for later rev-004 items
`2` and `3`; it must not settle those docs yet, validate those docs yet,
rewrite predecessor ledgers, or narrate broader same-family or repo-level
success.

This round is docs-only and aggregate-only. It must preserve
`iso-recursive = keep`,
`non-equi-recursive = keep`, and
`no-fallback = keep`;
keep the accepted rev-003 item-3 validation authoritative as the one exact
current-result anchor;
keep the accepted rev-003 item-4 handoff decision authoritative;
keep rev-001 items `6` through `8` blocked;
and keep code changes, second interfaces, multi-SCC search, fallback
widening, production rollout, hardening, and broad capability claims
blocked. It must not edit source code, tests, Cabal, or
`orchestrator/state.json` in this round. No commit is authorized inside the
implement step itself.

## Locked Round Context

- Round id: `round-113`
- Roadmap item: `item-1`
- Stage: `plan`
- Active attempt: `attempt-1`
- Retry state: `null`
- Active branch: `codex/round-113`
- Active worktree:
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-113`
- Fixed live subject: one docs-only rev-004 item-1 aggregate freeze for the
  exact same-pocket post-amendment current-result surface and one exact
  future writable docs boundary inside the bounded same-family successor
  revision published by accepted `round-112`
- Current round review feedback: none yet; only
  `orchestrator/rounds/round-113/selection.md` exists for this round, so
  this is a full `attempt-1` plan rather than a retry delta

Current worktree state is already non-pristine. Respect existing edits and do
not revert unrelated work:

- controller-owned `orchestrator/state.json` is already modified and must
  remain untouched; and
- existing `orchestrator/rounds/round-113/selection.md` is a round input and
  must remain untouched.

Under the live retry contract, item `1` is aggregate-only. Review may reject
and send the same round back to `plan`, but `accepted + retry` is not lawful
for this item.

## Accepted Continuity That Remains Binding

- `orchestrator/rounds/round-113/selection.md`
  fixes this round to roadmap item `1` only, requires a docs-only aggregate
  freeze of one exact current-result surface plus one exact future writable
  docs boundary, preserves accepted `round-111` and `round-112`, and blocks
  settlement writing, validation, and scope widening.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-004/roadmap.md`
  makes item `1` the only dependency-free unfinished item in rev-004 and
  defines completion as one accepted docs-only artifact freezing the exact
  selected rows, packet, tuple, current-result surface, future writable docs
  boundary, and predecessor immutability for the already-published bounded
  successor revision.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-004/retry-subloop.md`
  keeps item `1` aggregate-only, forbids `accepted + retry`, and preserves
  prior-attempt immutability if review rejects the round back to `plan`.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-004/verification.md`
  requires post-amendment-freeze checks proving that item `1` narrows only
  to the same exact `C2` / `C5` / `C7` pocket, the same exact packet and
  tuple, one exact current-result surface anchored in accepted rev-003 item
  `3`, one exact future writable docs boundary, and explicit predecessor
  immutability.
- `orchestrator/rounds/round-112/review-record.json`
  is the authoritative acceptance record for predecessor item `4`. Its
  accepted checks make the rev-004 successor revision authoritative with the
  exact outcome
  `publish one later same-family successor revision for bounded post-amendment settlement`.
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-post-amendment-handoff-decision.md`
  is the authoritative accepted rev-003 item-4 artifact. It records the
  reason rev-004 exists at all: accepted `round-111` changed the exact
  current same-pocket public-output read while still-live same-family docs
  remained pre-amendment and should be settled through new bounded artifacts
  rather than silent predecessor rewrites.
- `orchestrator/rounds/round-111/review-record.json`
  is the authoritative acceptance record for predecessor item `3`. It
  finalized the exact same-pocket post-amendment validation result that this
  round must now freeze as the one current-result surface.
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-authoritative-handoff-bounded-amendment-frozen-same-pocket-evidence-surface-validation.md`
  is the authoritative accepted rev-003 item-3 validation artifact. It fixes
  the exact current-result surface: helper-visible/internal recursive
  continuity remains present and both authoritative public entrypoints now
  carry bounded recursive structure on the same exact packet.
- still-live same-family docs that record the pre-amendment public-collapse
  read remain historical evidence only and must stay immutable in rev-004
  item `1`:
  `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md`,
  `docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md`,
  `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md`,
  `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md`,
  and
  `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md`.
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  remains the inherited boundary contract:
  explicit-only production baseline,
  iso-recursive only,
  non-equi-recursive,
  no fallback widening,
  no second interface,
  and no equi-recursive or implicit-unfolding authorization.

## File Map

### Create

- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-post-amendment-settlement-surface-and-successor-boundary-freeze.md`
  - Responsibility: canonical docs-only rev-004 item-1 aggregate freeze for
    the exact same-pocket post-amendment current-result surface and the exact
    future writable docs boundary. It must consume accepted `round-111` and
    `round-112`, freeze the same exact pocket and exact current-result
    anchor, and forbid silent predecessor rewrites.

### Read-Only Evidence

- `orchestrator/rounds/round-113/selection.md`
- `orchestrator/rounds/round-112/review-record.json`
- `orchestrator/rounds/round-111/review-record.json`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-004/roadmap.md`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-004/retry-subloop.md`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-004/verification.md`
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-post-amendment-handoff-decision.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-authoritative-handoff-bounded-amendment-frozen-same-pocket-evidence-surface-validation.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md`

### Future Writable Docs Boundary (Must Remain Absent Or Unchanged In This Round)

- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-post-amendment-settlement-ledger.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-post-amendment-settlement-validation.md`

### Preserve Unchanged

- `orchestrator/state.json`
- `orchestrator/rounds/round-113/selection.md`
- roadmap files under
  `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-004/`
- `Bugs.md`
- `TODO.md`
- `implementation_notes.md`
- `CHANGELOG.md`
- `src/`
- `src-public/`
- `app/`
- `test/`
- `mlf2.cabal`
- every historical `docs/plans/` settlement artifact other than the one exact
  item-1 output path named above

No round-local `implementation-notes.md`, side ledger, alternate settlement
memo, or supplemental roadmap note is authorized. The docs artifact above is
the only intended implementation output path. The implementer must not
create or edit any other file for item `1`.

## Exact Selected Slice (Exactly One)

The only selected slice is:

freeze one rev-004 same-pocket post-amendment settlement-surface and
successor boundary for the already-selected same-lane retained-child /
public-output continuity pocket carried by accepted rows `C2`, `C5`, and
`C7`, centered only on the accepted rev-003 item-3 current-result surface
and the future writable new-docs-only settlement boundary.

### Exact Same-Pocket Boundary

The selected pocket remains exactly:

`the exact same-lane retained-child / public-output continuity pocket already carried by accepted rows C2, C5, and C7, with non-local C1 retained only as bounded contrast context`

| Row | Exact same-pocket role that remains binding | What item `1` must freeze for later work | Bound that must remain closed |
| --- | --- | --- | --- |
| `C2` | primary same-lane retained-child row for the selected pocket | same family / anchor / owner-local frame / route identity and the same exact current-result read now anchored in accepted rev-003 item `3` | not a second packet, not a broader same-family success claim, not a family-wide settlement target |
| `C5` | same exact `C2` pocket through the owner / binder-sensitive placement lens only | same local `TypeRef` lane, same owner-local frame, and same nested-`forall` fail-closed guard context | not a second owner-local packet, not a second route, not a widened settlement boundary |
| `C7` | same exact `C2` / `C5` pocket through the public-output continuity lens only | exact current-result public-output surface and exact successor docs boundary for the same packet | not a second interface, not a second public path, not a broad repo-level capability claim |

`C1` may appear only as bounded contrast context. It must not become a
second selected subject, a second packet, a second route, a second
interface, or a reason to widen the future docs boundary.

### Exact Packet

The selected packet remains exactly this packet and no variant:

```haskell
ELet "k" (ELamAnn "x" recursiveAnn (EVar "x"))
  (ELet "u" (EApp (ELam "y" (EVar "y")) (EVar "k")) (EVar "u"))
```

where
`recursiveAnn = STMu "a" (STArrow (STVar "a") (STBase "Int"))`.

### Exact Tuple

The same frozen tuple remains binding:

| Continuity field | Exact selected value |
| --- | --- |
| Family | same-lane retained-child |
| Recursive-shape anchor | `boundVarTargetRoot` |
| Owner / binder frame | one owner-local retained-child frame |
| Route | `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC` |
| Quantified-boundary status | clear-boundary only: `boundHasForallFrom` is false and `not hasForall` holds |

### Exact Current-Result Surface

The one exact rev-004 current-result surface that item `1` must freeze is:

- authoritative anchor artifact:
  `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-authoritative-handoff-bounded-amendment-frozen-same-pocket-evidence-surface-validation.md`;
- authoritative acceptance record:
  `orchestrator/rounds/round-111/review-record.json`; and
- no coequal replacement surface, no broader same-family synthesis, and no
  rewritten predecessor evidence surface.

### Exact Future Writable Docs Boundary

Later rev-004 docs settlement may write only the two new docs paths named in
this plan, and only in later items `2` and `3`.

No older settlement artifact, no predecessor ledger, no representative
campaign dossier, no case ledger, and no matrix dossier is inside that
future writable boundary.

## Review Checklist

- `git diff --check`
- `python3 -m json.tool orchestrator/state.json >/dev/null`
- prove the round-owned diff is docs/orchestrator-only apart from the
  controller-owned `orchestrator/state.json`
- prove the canonical artifact freezes exactly one current-result surface and
  exactly one future writable docs boundary
- prove predecessor immutability is explicit and names the still-live
  pre-amendment docs as historical evidence
- prove no round-local `implementation-notes.md` was created
- because this round is aggregate-only and docs-only, record the broader
  Cabal gate as intentionally out of scope

## Finish Condition

The round is ready for review when:

- the canonical item-1 freeze artifact exists at the exact path above;
- it freezes the exact accepted rev-003 item-3 current-result surface as the
  only current-result anchor for rev-004;
- it freezes the exact future writable docs boundary for later rev-004 items
  `2` and `3`;
- it preserves predecessor immutability explicitly; and
- no file outside the allowed docs/orchestrator packet has changed.
