# Round 201 Plan

- Round: `round-201`
- Roadmap: `2026-04-05-00-general-automatic-iso-recursive-p5-and-p2-follow-on-roadmap` / `rev-001`
- Milestone: `milestone-4`
- Direction: `direction-4b-bind-final-enablement-or-next-family`
- Extracted item: `bind-final-enablement-or-next-family`
- Retry: `null`
- Execution shape: serial, docs-only, handoff-only, non-widening, one implementation-owned docs artifact only, no worker fan-out, and no production/test/Cabal/roadmap/controller-state edits

## Objective

Publish one canonical milestone-4 final handoff artifact only:
`docs/plans/2026-04-07-post-item-7-explicit-boundary-revision-candidate-final-handoff-to-one-planning-only-p5-polymorphism-nested-forall-boundary-revision-family.md`.

That artifact must carry forward the accepted `round-200` selected refreshed
end-state token exactly:
`explicit boundary-revision candidate`

and bind exactly one downstream consequence only:
`open one planning-only explicit boundary-revision family for broader positive P5 polymorphism-nested-forall support beyond the one settled retained-child clear-boundary lane`

Current planning read: milestone-4 should complete by naming that one exact
next family, not by inserting a separate enablement step. Accepted
`round-200` already concentrated the surviving pressure on broader positive
`P5 polymorphism-nested-forall` support beyond the one settled retained-child
clear-boundary lane; accepted `round-199` / `round-198` still keep `P5` above
`P2`; and accepted `round-197` still keeps the only settled `P5` success
narrow. A mere preparatory enablement step would therefore under-bind the
milestone, while any concrete revision, roadmap amendment, or implementation
move would overreach the accepted evidence. The handoff must stop at one
planning-only next family only.

This round must stay docs-only, serial, handoff-only, and non-widening. The
artifact must not relitigate the refreshed end-state matrix, re-evaluate
`continue-bounded`, weaken or rename
`explicit boundary-revision candidate`, reopen `P2`, reopen preserved
negative-family settlements, authorize a concrete boundary revision, or smuggle
in execution / hardening / roadmap-amendment work.

## Authorized Write Scope

Implementation-owned milestone content in this round is limited to exactly one
new docs artifact:

- `docs/plans/2026-04-07-post-item-7-explicit-boundary-revision-candidate-final-handoff-to-one-planning-only-p5-polymorphism-nested-forall-boundary-revision-family.md`

Round-owned artifacts that may appear later in the packet and must not be
misclassified as extra milestone docs are:

- `orchestrator/rounds/round-201/implementation-notes.md`
- `orchestrator/rounds/round-201/review.md`
- `orchestrator/rounds/round-201/review-record.json`
- `orchestrator/rounds/round-201/merge.md`

Do not create or modify:

- `orchestrator/state.json`
- `orchestrator/roadmaps/**`
- `TODO.md`
- `implementation_notes.md`
- `Bugs.md`
- `README.md`
- any accepted predecessor `docs/plans/**` artifact
- any file under `src/`, `src-public/`, `app/`, or `test/`
- `mlf2.cabal`
- any second `docs/plans/**` artifact, helper scratch file, enablement addendum,
  roadmap-scaffold draft, roadmap-amendment draft, or concrete revision draft

## Locked Round Context

- Accepted `round-200` is the binding immediate predecessor for this round.
  Its canonical artifact
  `docs/plans/2026-04-07-post-item-7-refreshed-repo-level-readiness-and-architecture-decision-from-the-updated-p5-vs-p2-ledger-and-preserved-negative-family-settlements.md`,
  approved `orchestrator/rounds/round-200/review-record.json`, and merge
  summary `orchestrator/rounds/round-200/merge.md` already select the
  refreshed end-state token `explicit boundary-revision candidate` and defer
  any concrete consequence to this direction only.
- Accepted `round-199` is the controlling routing predecessor beneath that
  selected token. Its canonical routing note
  `docs/plans/2026-04-07-post-item-7-p5-dominant-boundary-pressure-routing-note-keeping-p2-unopened-on-the-current-ledger.md`
  plus approved `orchestrator/rounds/round-199/review-record.json` and
  `orchestrator/rounds/round-199/merge.md` keep
  `P5 remains the stronger blocker / pressure source`, keep
  `P2 stays unopened on the current ledger`, and route the family only into
  milestone-4.
- Accepted `round-198` remains the supporting remaining-frontier ledger under
  `round-199`: it already separates refreshed `P5` evidence from preserved
  `P2` evidence and ranks `P5` above `P2`.
- Accepted `round-197` still narrows the refreshed `P5` success surface to one
  retained-child clear-boundary lane only:
  `sameLaneAliasFrameClearBoundaryExpr` succeeds on
  `runPipelineElab` / `runPipelineElabChecked`, while
  `nestedForallContrastExpr` remains fail-closed with
  `PhiTranslatabilityError`.
- Accepted `round-192` still keeps the representative negative-family rows
  `N1 ambiguity-reject`, `N2 unsoundness-guard`, and
  `N6 termination-pressure` bounded at `fail-closed rejection`.
- Accepted `round-191` and accepted `round-181` still keep `P2` packet-bounded:
  `P2` remains `packet-specific folklore`, one exact `C1` packet is the only
  accepted non-local anchor, and `baseTarget -> baseC` remains the packet
  boundary rather than family closure.
- Accepted `round-151` plus the current top-level `implementation_notes.md`
  keep nested-forall `mu` absorption under polymorphic mediation closed as
  known correct behavior. That predecessor truth remains closed context only,
  not fresh blocker evidence.
- The surviving pressure named by accepted `round-200` is broader positive
  `P5 polymorphism-nested-forall` support beyond the one settled retained-child
  clear-boundary lane. This round may bind exactly one planning-only next
  family for that pressure, but it may not freeze the exact inherited boundary
  under pressure, revise the boundary, or start roadmap-scaffold work itself.
- `milestone-4` completes only after one exact downstream consequence is bound.
  This round must therefore stop at one bounded next-family handoff only.

The inherited boundary remains explicit-only / iso-recursive /
non-equi-recursive / non-cyclic-graph / no-fallback unless a later accepted
revision changes it explicitly.

## Sequential Plan

1. Freeze the accepted final-handoff ledger before drafting the new artifact;
   modify no files in this step.
   - Read-only inputs:
     `orchestrator/rounds/round-201/selection.md`,
     `orchestrator/state.json`,
     `orchestrator/roadmaps/2026-04-05-00-general-automatic-iso-recursive-p5-and-p2-follow-on-roadmap/rev-001/roadmap.md`,
     `orchestrator/roadmaps/2026-04-05-00-general-automatic-iso-recursive-p5-and-p2-follow-on-roadmap/rev-001/verification.md`,
     `orchestrator/roadmaps/2026-04-05-00-general-automatic-iso-recursive-p5-and-p2-follow-on-roadmap/rev-001/retry-subloop.md`,
     `docs/plans/2026-04-07-post-item-7-refreshed-repo-level-readiness-and-architecture-decision-from-the-updated-p5-vs-p2-ledger-and-preserved-negative-family-settlements.md`,
     `orchestrator/rounds/round-200/review-record.json`,
     `orchestrator/rounds/round-200/merge.md`,
     `orchestrator/rounds/round-200/implementation-notes.md`,
     `docs/plans/2026-04-07-post-item-7-p5-dominant-boundary-pressure-routing-note-keeping-p2-unopened-on-the-current-ledger.md`,
     `orchestrator/rounds/round-199/review-record.json`,
     `orchestrator/rounds/round-199/merge.md`,
     `docs/plans/2026-04-06-post-item-7-p5-vs-p2-remaining-frontier-ledger.md`,
     `orchestrator/rounds/round-198/review-record.json`,
     `docs/plans/2026-04-06-post-item-7-p5-post-implementation-settlement-surface-and-exact-repo-impact-read.md`,
     `orchestrator/rounds/round-197/review-record.json`,
     `docs/plans/2026-04-05-general-automatic-iso-recursive-full-inference-negative-family-and-termination-pressure-aggregate-classification.md`,
     `orchestrator/rounds/round-192/review-record.json`,
     `docs/plans/2026-04-05-general-automatic-iso-recursive-full-inference-positive-family-aggregate-classification.md`,
     `orchestrator/rounds/round-191/review-record.json`,
     `orchestrator/rounds/round-181/review-record.json`,
     `orchestrator/rounds/round-181/implementation-notes.md`,
     `orchestrator/rounds/round-151/review-record.json`,
     `orchestrator/rounds/round-151/review.md`,
     and `implementation_notes.md`.
   - End this step with one fixed handoff ledger only:
     accepted `round-200` has already chosen
     `explicit boundary-revision candidate`;
     accepted `round-199` / `round-198` already keep `P5` above `P2`;
     accepted `round-197` already limits positive `P5` success to one settled
     retained-child clear-boundary lane while keeping
     `nestedForallContrastExpr` fail-closed;
     accepted `round-192` already keeps the representative negative-family rows
     closed; accepted `round-191` / `round-181` already keep `P2` packet-only;
     and accepted `round-151` remains predecessor truth only.
   - The step-1 exit question is narrow:
     what one exact downstream bind completes milestone-4 while carrying
     `explicit boundary-revision candidate` forward unchanged, keeping `P2`
     unopened, preserving the negative-family closures, and still stopping
     short of concrete boundary revision or roadmap-amendment work?
   - The step-1 answer must stay exact:
     one planning-only explicit boundary-revision family for broader positive
     `P5 polymorphism-nested-forall` support beyond the one settled
     retained-child clear-boundary lane.
   - Verification:
     `python3 -m json.tool orchestrator/state.json >/dev/null`
     `roadmap_dir="$(jq -r '.roadmap_dir' orchestrator/state.json)" && test -f "$roadmap_dir/roadmap.md" && test -f "$roadmap_dir/verification.md" && test -f "$roadmap_dir/retry-subloop.md"`
     `rg -n 'direction-4b-bind-final-enablement-or-next-family|exactly one next handoff or enablement step|explicit boundary-revision candidate' orchestrator/roadmaps/2026-04-05-00-general-automatic-iso-recursive-p5-and-p2-follow-on-roadmap/rev-001/roadmap.md orchestrator/roadmaps/2026-04-05-00-general-automatic-iso-recursive-p5-and-p2-follow-on-roadmap/rev-001/verification.md`
     `rg -n 'explicit boundary-revision candidate|direction-4b-bind-final-enablement-or-next-family|broader positive P5 polymorphism-nested-forall|nestedForallContrastExpr|PhiTranslatabilityError|concrete consequence deferred|deferred to direction-4b' docs/plans/2026-04-07-post-item-7-refreshed-repo-level-readiness-and-architecture-decision-from-the-updated-p5-vs-p2-ledger-and-preserved-negative-family-settlements.md orchestrator/rounds/round-200/review-record.json orchestrator/rounds/round-200/merge.md orchestrator/rounds/round-200/implementation-notes.md`
     `rg -n 'P5 remains the stronger blocker / pressure source|P2 stays unopened on the current ledger|milestone-4|direction-4a-publish-refreshed-readiness-decision' docs/plans/2026-04-07-post-item-7-p5-dominant-boundary-pressure-routing-note-keeping-p2-unopened-on-the-current-ledger.md docs/plans/2026-04-06-post-item-7-p5-vs-p2-remaining-frontier-ledger.md orchestrator/rounds/round-199/review-record.json orchestrator/rounds/round-199/merge.md orchestrator/rounds/round-198/review-record.json`
     `rg -n 'sameLaneAliasFrameClearBoundaryExpr|nestedForallContrastExpr|PhiTranslatabilityError|runPipelineElab|runPipelineElabChecked' docs/plans/2026-04-06-post-item-7-p5-post-implementation-settlement-surface-and-exact-repo-impact-read.md orchestrator/rounds/round-197/review-record.json`
     `rg -n 'N1 ambiguity-reject|N2 unsoundness-guard|N6 termination-pressure|fail-closed rejection' docs/plans/2026-04-05-general-automatic-iso-recursive-full-inference-negative-family-and-termination-pressure-aggregate-classification.md orchestrator/rounds/round-192/review-record.json`
     `rg -n 'packet-specific folklore|C1|baseTarget -> baseC|runPipelineElab|runPipelineElabChecked' docs/plans/2026-04-05-general-automatic-iso-recursive-full-inference-positive-family-aggregate-classification.md orchestrator/rounds/round-191/review-record.json orchestrator/rounds/round-181/review-record.json orchestrator/rounds/round-181/implementation-notes.md`
     `rg -n 'Known correct behavior under polymorphic mediation|Nested-forall-mediated recursive types|round-151|correct behavior' implementation_notes.md orchestrator/rounds/round-151/review.md orchestrator/rounds/round-151/review-record.json`

2. Draft the one milestone-4 final handoff artifact and keep it strictly
   consequence-binding-only.
   - Create
     `docs/plans/2026-04-07-post-item-7-explicit-boundary-revision-candidate-final-handoff-to-one-planning-only-p5-polymorphism-nested-forall-boundary-revision-family.md`.
   - Use a stage-contract header that records `round-201`,
     `milestone-4`,
     `direction-4b-bind-final-enablement-or-next-family`,
     `bind-final-enablement-or-next-family`,
     `attempt-1`,
     `retry: null`,
     and the live subject as one docs-only milestone-4 final handoff artifact
     only.
   - The document must include, in order:
     `## Stage Contract Freeze`,
     `## Accepted Final-Handoff Ledger`,
     `## Carried-Forward Refreshed End-State`,
     `## One Exact Downstream Consequence`,
     `## Why This Opens One Next Family Rather Than An Enablement Step`,
     and `## Non-Claims`.
   - In `## Accepted Final-Handoff Ledger`, cite the exact settled sources
     above with accepted `round-200` as the binding immediate predecessor and
     the earlier `round-199` / `round-198` / `round-197` / `round-192` /
     `round-191` / `round-181` / `round-151` material carried only as the
     accepted lineage beneath that selected token.
   - In `## Carried-Forward Refreshed End-State`, record this exact line only:
     `Carried-forward refreshed end-state token: ` followed by
     `` `explicit boundary-revision candidate` ``.
     Then state explicitly that the token is fixed by accepted `round-200`
     and is not re-evaluated, renamed, weakened, or compared again against
     `continue-bounded` or repo-level readiness in this round.
   - In `## One Exact Downstream Consequence`, record this exact line only:
     `Selected downstream consequence: ` followed by
     `` `open one planning-only explicit boundary-revision family for broader positive P5 polymorphism-nested-forall support beyond the one settled retained-child clear-boundary lane` ``.
   - In that same section, bind the family narrowly:
     it is for broader positive `P5 polymorphism-nested-forall` pressure only;
     it preserves `P2` as unopened on the current ledger;
     it preserves the negative-family rows as closed predecessor truth;
     it carries forward the one settled retained-child clear-boundary `P5`
     lane as predecessor evidence only; and it defers any exact inherited
     boundary freeze, roadmap scaffold, roadmap amendment, implementation
     slice, or concrete revision enactment to that later family rather than
     this round.
   - In `## Why This Opens One Next Family Rather Than An Enablement Step`,
     explain exactly why a separate enablement step would under-bind the
     accepted `round-200` token:
     the surviving pressure is already localized to broader positive
     `P5 polymorphism-nested-forall` support beyond one settled lane, so the
     honest completion signal is one exact next family. Also explain why this
     still stops short of overreach:
     selecting the next family does not authorize concrete boundary revision,
     roadmap amendment, or implementation.
   - In `## Non-Claims`, make the boundaries explicit:
     no relitigation of the refreshed end-state matrix;
     no second downstream consequence;
     no separate enablement step;
     no reopened `P2` lane;
     no reopened negative-family debt;
     no exact inherited boundary selection;
     no roadmap revision or roadmap scaffold in this round;
     and no concrete boundary-revision implementation.
   - Do not author:
     a refreshed-decision restatement,
     a consequence-evaluation matrix,
     a second next family,
     a separate enablement addendum,
     a roadmap-scaffold draft,
     or any execution / hardening / amendment plan.
   - Verification:
     `test -f docs/plans/2026-04-07-post-item-7-explicit-boundary-revision-candidate-final-handoff-to-one-planning-only-p5-polymorphism-nested-forall-boundary-revision-family.md`
     `rg -n 'round-201|milestone-4|direction-4b-bind-final-enablement-or-next-family|bind-final-enablement-or-next-family|round-200|round-199|round-198|round-197|round-192|round-191|round-181|round-151|explicit boundary-revision candidate|Selected downstream consequence:|P5 polymorphism-nested-forall|sameLaneAliasFrameClearBoundaryExpr|nestedForallContrastExpr|PhiTranslatabilityError|P5 remains the stronger blocker / pressure source|P2 stays unopened on the current ledger|packet-specific folklore|fail-closed rejection' docs/plans/2026-04-07-post-item-7-explicit-boundary-revision-candidate-final-handoff-to-one-planning-only-p5-polymorphism-nested-forall-boundary-revision-family.md`
     `python3 - <<'PY'\nimport pathlib, sys\nartifact = pathlib.Path('docs/plans/2026-04-07-post-item-7-explicit-boundary-revision-candidate-final-handoff-to-one-planning-only-p5-polymorphism-nested-forall-boundary-revision-family.md').read_text()\nrequired_sections = [\n    '## Stage Contract Freeze',\n    '## Accepted Final-Handoff Ledger',\n    '## Carried-Forward Refreshed End-State',\n    '## One Exact Downstream Consequence',\n    '## Why This Opens One Next Family Rather Than An Enablement Step',\n    '## Non-Claims',\n]\nrequired_tokens = [\n    'round-201',\n    'milestone-4',\n    'direction-4b-bind-final-enablement-or-next-family',\n    'bind-final-enablement-or-next-family',\n    'round-200',\n    'round-199',\n    'round-198',\n    'round-197',\n    'round-192',\n    'round-191',\n    'round-181',\n    'round-151',\n    'explicit boundary-revision candidate',\n    'P5 polymorphism-nested-forall',\n    'sameLaneAliasFrameClearBoundaryExpr',\n    'nestedForallContrastExpr',\n    'PhiTranslatabilityError',\n    'P5 remains the stronger blocker / pressure source',\n    'P2 stays unopened on the current ledger',\n    'packet-specific folklore',\n    'fail-closed rejection',\n]\nfor token in required_sections + required_tokens:\n    if token not in artifact:\n        print(f'missing token: {token}')\n        sys.exit(1)\ncarry_lines = [\n    line.strip() for line in artifact.splitlines()\n    if line.strip().startswith('Carried-forward refreshed end-state token:')\n]\nif carry_lines != ['Carried-forward refreshed end-state token: `explicit boundary-revision candidate`']:\n    print('artifact must contain exactly the carried-forward token line')\n    sys.exit(1)\nconsequence_lines = [\n    line.strip() for line in artifact.splitlines()\n    if line.strip().startswith('Selected downstream consequence:')\n]\nexpected_consequence = 'Selected downstream consequence: `open one planning-only explicit boundary-revision family for broader positive P5 polymorphism-nested-forall support beyond the one settled retained-child clear-boundary lane`'\nif consequence_lines != [expected_consequence]:\n    print('artifact must contain exactly the selected downstream consequence line')\n    sys.exit(1)\nfor forbidden in [\n    'Selected refreshed end-state token:',\n    'Selected successor move:',\n    'Selected enablement step:',\n    '## Refreshed End-State Evaluation Matrix',\n    '## One Next Lawful Move',\n    '## Immediate Handoff',\n    '## Routing Consequence',\n]:\n    if forbidden in artifact:\n        print(f'forbidden token present: {forbidden}')\n        sys.exit(1)\nprint('ROUND201_FINAL_HANDOFF_OK')\nPY`

3. Verify docs-only scope, one-artifact closure, and honest round-local hygiene.
   - Expected implementation-stage result:
     the one new final handoff artifact above plus the round-owned
     `selection.md`, `plan.md`, and `implementation-notes.md` only.
   - Expected later round-owned artifacts after review / merge:
     `review.md`,
     `review-record.json`,
     and `merge.md`
     under `orchestrator/rounds/round-201/`.
   - Workflow:
     run diff hygiene;
     confirm no second `docs/plans/**` artifact exists;
     confirm no code, test, Cabal, roadmap, top-level-doc, or controller-state
     path is pulled into the round;
     and keep the allowlist explicit about round-owned notes/review/merge
     artifacts so later packet files do not look like milestone-scope drift.
   - Verification:
     `git diff --check`
     `python3 - <<'PY'\nimport subprocess, sys\nartifact = 'docs/plans/2026-04-07-post-item-7-explicit-boundary-revision-candidate-final-handoff-to-one-planning-only-p5-polymorphism-nested-forall-boundary-revision-family.md'\nallowed = {\n    artifact,\n    'orchestrator/rounds/round-201/selection.md',\n    'orchestrator/rounds/round-201/plan.md',\n    'orchestrator/rounds/round-201/implementation-notes.md',\n    'orchestrator/rounds/round-201/review.md',\n    'orchestrator/rounds/round-201/review-record.json',\n    'orchestrator/rounds/round-201/merge.md',\n}\ntracked = subprocess.check_output(['git', 'diff', '--name-only', 'HEAD'], text=True).splitlines()\nuntracked = subprocess.check_output(['git', 'ls-files', '--others', '--exclude-standard'], text=True).splitlines()\npaths = sorted({p for p in tracked + untracked if p})\nextra = [p for p in paths if p not in allowed and p != 'orchestrator/state.json']\nforbidden = [\n    p for p in paths\n    if p == 'mlf2.cabal'\n    or p.startswith('src/')\n    or p.startswith('src-public/')\n    or p.startswith('app/')\n    or p.startswith('test/')\n    or p in {'TODO.md', 'implementation_notes.md', 'Bugs.md', 'README.md'}\n    or (p.startswith('docs/') and p != artifact)\n    or p.startswith('orchestrator/roadmaps/')\n]\nif forbidden or extra:\n    if forbidden:\n        print('FORBIDDEN_PATHS:')\n        print('\\n'.join(forbidden))\n    if extra:\n        print('OUT_OF_SCOPE_PATHS:')\n        print('\\n'.join(extra))\n    sys.exit(1)\nprint('ROUND201_DOCS_ONLY_SCOPE_OK')\nPY`

## Review Focus

- The artifact carries forward accepted `round-200` token
  `explicit boundary-revision candidate` exactly and does not relitigate the
  refreshed end-state matrix.
- The artifact binds exactly one downstream consequence only:
  one planning-only explicit boundary-revision family for broader positive
  `P5 polymorphism-nested-forall` support beyond the one settled retained-child
  clear-boundary lane.
- The handoff keeps `P2` unopened, keeps the representative negative-family
  rows and accepted `round-151` reclassification closed as predecessor truth,
  and does not convert any of that context into fresh live debt.
- The artifact does not smuggle in a separate enablement step, a second next
  family, an exact inherited boundary selection, a roadmap scaffold/amendment,
  or any concrete boundary-revision implementation.
- The round remains docs-only and limited to the authorized single milestone
  artifact plus round-local packet files.
