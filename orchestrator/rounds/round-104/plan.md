# Round 104 Plan (`item-1` Reopened Non-Cyclic-Graph Revision Authority And Candidate Boundary Freeze)

## Objective

Execute only roadmap item `1` and produce one docs-only artifact at:
`docs/plans/2026-03-26-global-non-cyclic-graph-reopened-revision-authority-and-candidate-boundary-freeze.md`.

This is the initial `item-1` plan for `attempt-1` with `retry: null`. The
round must freeze exactly one reopened revision-authority statement and
exactly one planning-only candidate boundary for rev-002 after accepted
`round-103` finalized rev-001 item `5` as
`reopen the non-cyclic-graph revision question`.

The artifact must:

- consume accepted rev-001 item `5` and
  `orchestrator/rounds/round-103/review-record.json` as authoritative
  predecessor truth without relitigating the accepted `keep` versus `reopen`
  result;
- preserve completed rev-001 truth, especially the inherited baseline, the
  accepted rev-001 item `1` through item `5` evidence chain, and the rule
  that rev-001 items `6` through `8` stay blocked;
- freeze rev-002 as planning-only and architecture-only;
- freeze exactly one admissible candidate boundary:
  whether one bounded single-component cyclic-structure successor lane is
  needed for the strongest admitted retained-child / public-output continuity
  pressure, with non-local `C1` continuity available only as contrast
  context; and
- explicitly keep equi-recursive semantics, multi-SCC search, second
  interfaces, fallback widening, production implementation, hardening, and
  repo-level capability claims blocked.

Current planning read: accepted `round-103` already established that the
inherited acyclic model no longer satisfies the global `keep` bar, but it did
not authorize any specific architecture amendment. The strongest admitted
positive-family pressure remains the same-family `C2` / `C5` / `C7`
retained-child continuity chain; `C1` remains bounded contrast only. Item `1`
must freeze that authority and boundary without selecting the exact live
subject, without defining the later acceptance contract, and without implying
that cyclic structure is already justified.

This round is docs-only. It must not edit production code, tests,
`mlf2.cabal`, `Bugs.md`, controller-owned machine state, or roadmap bundle
contracts.

## Locked Round Context

- Round id: `round-104`
- Roadmap item: `item-1`
- Stage: `plan`
- Active attempt: `attempt-1`
- Retry state: `null`
- Active branch: `codex/round-104`
- Active worktree:
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-104`
- Fixed live subject: one docs-only rev-002 authority freeze and
  planning-only candidate-boundary freeze for the reopened global
  `non-cyclic-graph` question
- Current round review feedback: none yet; no `review.md`,
  `reviews/attempt-<n>.md`, or `review-record.json` exists for `round-104`,
  so this is a full `attempt-1` plan rather than a retry delta

Current worktree state is already non-pristine. Respect existing edits and do
not revert unrelated work:

- `M orchestrator/state.json` (controller-owned; must remain untouched)
- `?? orchestrator/rounds/round-104/selection.md`

Item `1` is aggregate-only under the live retry contract. Review may reject
and send the same round back to `plan`, but review may not emit
`accepted + retry` for this item.

## Accepted Continuity That Remains Binding

- `orchestrator/rounds/round-104/selection.md`
  fixes this round to roadmap item `1` only, binds the output to one
  planning-only authority freeze and one planning-only candidate boundary,
  and explicitly blocks implementation work and broad architecture widening.
- `orchestrator/state.json`
  fixes the live controller state at `active_round_id = round-104`,
  `stage = plan`, `current_task = item-1`, `retry = null`,
  `roadmap_id = 2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap`,
  `roadmap_revision = rev-002`, and the authoritative
  `roadmap_dir = orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002`.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/roadmap.md`
  makes item `1` the only dependency-free unfinished item and defines its
  completion notes as one accepted docs-only artifact that freezes the
  reopened authority to exactly one planning-only candidate boundary.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/retry-subloop.md`
  keeps item `1` aggregate-only and forbids `accepted + retry`.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/verification.md`
  requires reopened-authority-freeze checks proving that item `1` preserves
  accepted rev-001 truth, keeps rev-001 items `6` through `8` blocked, and
  narrows the reopened work to one planning-only candidate boundary without
  silently authorizing code work or broad architecture widening.
- `orchestrator/rounds/round-103/review-record.json`
  is the authoritative acceptance record for the predecessor handoff:
  `final_outcome =
  global-non-cyclic-graph-decision-gate-reopens-non-cyclic-graph-revision-question`.
- `docs/plans/2026-03-26-global-non-cyclic-graph-keep-vs-reopen-decision-gate.md`
  is the accepted rev-001 item `5` artifact. It preserves
  `iso-recursive = keep`, `non-equi-recursive = keep`, and
  `no-fallback = keep`, but reopens the inherited `non-cyclic-graph`
  boundary and withdraws the direct handoff into rev-001 items `6` through
  `8`.
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  preserves the inherited boundary:
  explicit recursive annotations remain the production baseline;
  recursive meaning remains iso-recursive only; and no equi-recursive,
  cyclic-graph, second-interface, or fallback widening is authorized.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-capability-contract-and-evaluation-corpus.md`
  preserves the representative family matrix and the later claim bar for
  `P1` through `P6` and `N1` through `N6`, including `N4` as the boundary
  family that would require cyclic structure or multi-SCC search.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md`
  keeps the strategic classification set at:
  `iso-recursive = keep`,
  `non-equi-recursive = keep`,
  `non-cyclic-graph = unknown`,
  `no-fallback = keep`.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md`
  keeps `stable visible persistence` as the only lawful positive `P6`
  success token and therefore remains part of the authority for why rev-001
  item `5` could no longer honestly select `non-cyclic-graph = keep`.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`
  remains the accepted predecessor family-matrix read of `bounded subset
  only`, with zero `stable visible persistence` rows and blocker-debt rows
  `C1`, `C2`, `C5`, and `C7`.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architecture-decision-and-successor-plan-choice.md`
  keeps `continue within the current architecture` as the strongest accepted
  strategic posture before the refreshed global settlement gate, while still
  preserving `non-cyclic-graph` as the only inherited boundary classified
  `unknown`.
- `docs/plans/2026-03-26-global-non-cyclic-graph-settlement-contract-and-unresolved-family-evidence-ledger.md`
  is the accepted rev-001 item `1` freeze. It still binds the exact
  `keep` versus `reopen` bar and explicitly says that one bounded pocket or
  one blocker-debt row is insufficient by itself to settle the global gate.
- `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md`
  is the accepted rev-001 item `2` slice. It keeps `C1`, `C2`, and `C5` at
  `admitted but not reconstruction-visible / blocker debt` and preserves
  `C2` / `C5` as one same exact pocket, not two separate successor lanes.
- `docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md`
  is the accepted rev-001 item `3` slice. It keeps `C3` fail closed and
  keeps `C7` below `stable visible persistence` because recursive structure
  remains helper-visible/internal only.
- `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md`
  is the accepted rev-001 item `4` matrix refresh. It binds the aggregate
  tally:
  zero `stable visible persistence` rows,
  blocker-debt rows `P1-row`, `C1`, `C2`, `C5`, and `C7`,
  fail-closed rows `C3`, `C4`, and `C6`,
  `P5` reject-side only, and
  `N4` as pressure context only until a later accepted round says more.
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md`,
  `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-path-audit.md`,
  `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-collapse-clear-or-confirm.md`,
  `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md`,
  and
  `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md`
  remain bounded predecessor evidence only. Together they make the
  `C2` / `C5` / `C7` same-lane retained-child route the strongest admitted
  positive-family pressure, but they do not by themselves authorize a global
  or implementation-level architecture amendment.
- `Bugs.md`
  still lists open `BUG-2026-03-16-001`, but that replay / `InstBot` defect
  remains predecessor context only. It does not authorize roadmap
  reordering, replay repair, fallback widening, or architecture widening in
  this round.

## File Map

### Create

- `docs/plans/2026-03-26-global-non-cyclic-graph-reopened-revision-authority-and-candidate-boundary-freeze.md`
  - Responsibility: canonical docs-only rev-002 item `1` artifact freezing
    the reopened authority, preserving accepted rev-001 truth, and narrowing
    the reopened work to one planning-only candidate boundary.

### Read-Only Evidence

- `orchestrator/rounds/round-104/selection.md`
- `orchestrator/state.json`
- `orchestrator/rounds/round-103/review-record.json`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/roadmap.md`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/retry-subloop.md`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/verification.md`
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-capability-contract-and-evaluation-corpus.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architecture-decision-and-successor-plan-choice.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-settlement-contract-and-unresolved-family-evidence-ledger.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-keep-vs-reopen-decision-gate.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-path-audit.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-collapse-clear-or-confirm.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md`
- `Bugs.md`

### Preserve Unchanged

- `orchestrator/state.json`
- `orchestrator/roadmap.md`
- `orchestrator/retry-subloop.md`
- `orchestrator/verification.md`
- `Bugs.md`
- `src/`
- `src-public/`
- `app/`
- `test/`
- `mlf2.cabal`
- accepted rev-001 artifacts and review records
- completed roadmap history

No round-local `implementation-notes.md` is authorized for this item-1 plan.
The docs artifact above is the only planned implementation output.

## Exact Selected Slice (Exactly One)

The only selected slice is:

freeze the reopened global `non-cyclic-graph` question to one planning-only
authority ledger and one planning-only candidate boundary.

This slice is allowed to:

- restate the accepted rev-001 reopen outcome as authoritative predecessor
  truth;
- consolidate the accepted March 25 and rev-001 evidence chain into one
  rev-002 authority ledger explaining why the reopened question exists;
- freeze one candidate boundary centered on the strongest admitted
  retained-child / public-output continuity pressure;
- keep non-local `C1` continuity available only as contrast context; and
- state exactly which later items own subject selection, safety contract,
  target binding, and lane-open-or-stop decisions.

This slice is not allowed to:

- select the exact live reopened subject inside the candidate boundary;
- define the safety / acceptance contract for the reopened subject;
- bind the exact audit target, modules, rows, or output surfaces for later
  inspection;
- decide whether to open the architecture-amendment lane or stop;
- authorize equi-recursive reasoning, cyclic structural graphs, multi-SCC
  search, second interfaces, fallback widening, production implementation,
  hardening, or repo-level capability claims;
- relitigate accepted rev-001 item `5` or rewrite completed roadmap history;
- resume rev-001 items `6` through `8`; or
- edit `src/`, `src-public/`, `app/`, `test/`, `mlf2.cabal`,
  `orchestrator/state.json`, roadmap bundle contracts, retry / verification
  contracts, or `Bugs.md`.

If the artifact starts selecting the exact packet, naming implementation
owners, proving cyclic structure is required, or opening multiple candidate
lanes, it has already widened beyond item `1` and must fail closed.

## Sequential Tasks

### Task 1 - Freeze the docs-only item-1 frame and predecessor authority

- Create the canonical item-1 artifact at the path above.
- State explicitly that rev-002 item `1` is docs-only, planning-only,
  `attempt-1`, and `retry: null`.
- Reassert the inherited baseline unchanged and say explicitly that this
  round does not authorize code edits, test edits, controller-owned file
  edits, or roadmap-contract edits.
- Record accepted `round-103` / rev-001 item `5` as the authoritative
  predecessor handoff, using the accepted artifact and review record rather
  than restaging the decision.
- State explicitly that rev-001 items `6` through `8` remain blocked and
  that rev-002 begins by defining what the reopen may mean, not by resuming
  those blocked items under a new name.

### Task 2 - Assemble the accepted authority ledger for why rev-002 exists

- Write one decision-input ledger that consumes only accepted predecessor
  evidence:
  baseline contract,
  capability-family matrix,
  architecture audit,
  full-pipeline visibility contract,
  representative campaign,
  March 25 strategy,
  rev-001 item `1` through item `4` artifacts,
  rev-001 item `5` decision gate,
  and the bounded same-lane predecessor chain.
- Make explicit that the accepted global failure of the `keep` bar came from
  representative evidence still staying below repo-level success while
  `iso-recursive`, `non-equi-recursive`, and `no-fallback` all remained
  accepted `keep` axes.
- Preserve the accepted read that `N4` became the only honest architecture
  pressure axis at global scope, but do not convert that pressure into a
  direct amendment decision.
- Preserve the accepted read that the strongest admitted positive-family
  pressure now comes from the same-family `C2` / `C5` / `C7`
  retained-child / public-output continuity chain, with `C1` available only
  as bounded non-local contrast context.
- State explicitly that `BUG-2026-03-16-001` remains predecessor context
  only and is not part of the reopened authority.

### Task 3 - Freeze exactly one planning-only candidate boundary

- Define the candidate boundary in one sentence that matches the roadmap and
  selection contract:
  whether one bounded single-component cyclic-structure successor lane is
  needed for the strongest admitted retained-child / public-output
  continuity pressure.
- Bind that candidate boundary to the same-family retained-child continuity
  family already carried by `C2`, `C5`, and `C7`, while keeping `C1`
  available only as contrast context rather than as a coequal live subject.
- Keep the boundary at family / pressure level only:
  do not select the exact packet, exact module set, exact review surface, or
  exact future evidence target. Those belong to later items.
- Reject broader alternatives explicitly:
  no multi-SCC search,
  no general cyclic-family search,
  no second-interface or fallback path,
  no equi-recursive or implicit-unfolding semantics,
  no multi-subject reopen,
  and no claim that cyclic structure is already justified.

### Task 4 - Freeze later-item ownership and blocked next steps

- State explicitly that rev-002 item `2` owns exact live-subject selection
  inside the frozen candidate boundary.
- State explicitly that rev-002 item `3` owns the reviewer-auditable safety /
  acceptance contract for
  `acyclic still sufficient`,
  `single-component cyclic-structure successor lane justified`, or
  `stop`.
- State explicitly that rev-002 item `4` owns the exact row / packet /
  module / output-surface bind and that rev-002 item `5` owns the one
  lawful lane-open-or-stop decision.
- Reassert that production implementation, hardening, and capability-claim
  work remain blocked throughout this rev-002 item-1 round and are not
  implicitly reopened by the candidate-boundary freeze.

## Required Verification After The Artifact Lands

- `git diff --check`
- `python3 -m json.tool orchestrator/state.json >/dev/null`
- `rg -n '"contract_version": 2|"roadmap_id":|"roadmap_revision":|"roadmap_dir":|"retry": null|"retry": \{' orchestrator/state.json`
- `roadmap_dir="$(jq -r '.roadmap_dir' orchestrator/state.json)" && test -f "$roadmap_dir/roadmap.md" && test -f "$roadmap_dir/retry-subloop.md" && test -f "$roadmap_dir/verification.md"`
- `roadmap_dir="$(jq -r '.roadmap_dir' orchestrator/state.json)" && rg -n '^\d+\. \[(pending|in-progress|done)\]' "$roadmap_dir/roadmap.md"`
- `rg -n 'round-103|rev-001 items `6` through `8`|single-component cyclic-structure successor lane|C2|C5|C7|C1|multi-SCC|second interface|fallback|production implementation' docs/plans/2026-03-26-global-non-cyclic-graph-reopened-revision-authority-and-candidate-boundary-freeze.md`
- `git diff --name-only -- src test src-public app mlf2.cabal Bugs.md orchestrator/state.json`

Review must also confirm the rev-002 item-1-specific checks from the active
verification contract:

- accepted rev-001 truth is preserved;
- rev-001 items `6` through `8` stay blocked;
- the reopened work narrows to one planning-only candidate boundary only;
- no silent authorization of implementation, broad cyclic search, multi-SCC
  search, second interfaces, or fallback widening appears; and
- the round remains docs-only, so the full `cabal build all && cabal test`
  gate is explicitly skipped as out of scope unless the diff escapes the
  authorized docs / round-artifact surface.
