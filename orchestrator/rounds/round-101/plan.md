# Round 101 Plan (`item-3` Bounded Production-Surface Settlement Evidence For `C3` Nested-`forall` Pressure And `C7` Output-Surface Continuity)

## Objective

Execute only roadmap item `3` and produce one canonical settlement-evidence
artifact at:
`docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md`.

This is the initial `item-3` plan for `attempt-1` with `retry: null`. The
round must produce one bounded production-surface settlement-evidence slice for
the two remaining unresolved rows carried forward by item `1` and intentionally
left open by accepted item `2`:

- `C3`: nested-`forall` / quantified-crossing pressure that starts from the
  same owner-local retained-child search but must fail closed once the
  quantified-boundary guard no longer stays clear; and
- `C7`: reconstruction-heavy output-surface continuity for the exact same-lane
  retained-child packet on
  `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC`.

The output must sharpen the frozen repo-level reads for
`P5 polymorphism-nested-forall` and
`P6 reconstruction-visible-output` only. It may use internal/public output
facts as evidence, but it must not promote either row into representative-
campaign success, repo-level `non-cyclic-graph` settlement, or production
implementation.

Current planning read: the accepted item-1 ledger, accepted representative
campaign, accepted item-2 slice, and accepted same-lane predecessor chain all
still point to the same strongest lawful read:

- `C3` remains `fail-closed rejection`; and
- `C7` remains `admitted but not reconstruction-visible / blocker debt`.

The expected strongest lawful item-3 result is therefore to preserve those
classifications unless fresh read-only reruns contradict the accepted record.
If a rerun does contradict that record, fail closed inside the same frozen rows
and record the contradiction honestly rather than widening into repair work,
representative campaigning, item `5`, or production implementation.

This round is docs-first and evidence-first. It may consume existing focused
tests, exact-pocket replay harnesses, accepted predecessor artifacts, and
read-only source anchors as evidence, but it must not edit production code,
test code, `mlf2.cabal`, controller-owned machine state, roadmap machine
metadata, retry contracts, verification contracts, or `Bugs.md`.

## Locked Round Context

- Round id: `round-101`
- Roadmap item: `item-3`
- Stage: `plan`
- Active attempt: `attempt-1`
- Retry state: `null`
- Active branch: `codex/round-101`
- Active worktree:
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-101`
- Fixed live subject: one bounded production-surface settlement-evidence slice
  for `C3` and `C7` only
- Current round review feedback: none yet; no `review.md`,
  `reviews/attempt-<n>.md`, or `review-record.json` exists for `round-101`, so
  this is a full first-attempt plan rather than a retry delta

Current round worktree state is already non-pristine. Respect existing edits
and do not revert unrelated work:

- `M orchestrator/state.json`
- `?? orchestrator/rounds/round-101/selection.md`

## Accepted Continuity That Remains Binding

- `orchestrator/rounds/round-101/selection.md`
  fixes this round to roadmap item `3` only, binds the output to the bounded
  `C3` / `C7` settlement-evidence slice, and forbids widening into item `4`,
  item `5`, production implementation, hardening, cyclic search, multi-SCC
  search, second interfaces, fallback paths, or a broad capability claim.
- `orchestrator/state.json`
  fixes the live controller state at `active_round_id = round-101`,
  `stage = plan`, `current_task = item-3`, `retry = null`,
  `roadmap_id = 2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap`,
  `roadmap_revision = rev-001`, and the authoritative
  `roadmap_dir = orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-001`.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-001/roadmap.md`
  makes item `3` the lowest-numbered unfinished dependency-satisfied item and
  defines its completion notes as reviewable production-surface evidence for
  `C3` and `C7` with honest classification against the frozen ledger.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-001/retry-subloop.md`
  confirms that item `3` may retry in principle, but `retry: null` means this
  plan is the full first-attempt plan.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-001/verification.md`
  requires polymorphism-and-output proof-slice checks proving that item `3`
  produces representative production-surface evidence for `P5` and `P6` on the
  existing pipeline and public-output surfaces rather than on helper-only paths
  or mechanism-only reasoning.
- `docs/plans/2026-03-26-global-non-cyclic-graph-settlement-contract-and-unresolved-family-evidence-ledger.md`
  is the accepted item-1 freeze. It already binds the exact item-5 settlement
  bar, the unresolved family ledger, the outcome vocabulary
  `stable visible persistence`,
  `admitted but not reconstruction-visible / blocker debt`, and
  `fail-closed rejection`, and the current repo-level reads for `C3` and `C7`.
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  preserves the inherited boundary:
  explicit recursive annotations remain the production baseline;
  recursive meaning remains iso-recursive only; and non-equi-recursive,
  non-cyclic-graph, no-second-interface, and no-fallback boundaries remain
  binding.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-capability-contract-and-evaluation-corpus.md`
  preserves the family-matrix contract. Item `3` may sharpen only the
  current `P5` / `P6` read and must not silently settle the full matrix.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md`
  keeps `non-cyclic-graph = unknown` at repo scope. Item `3` may gather
  bounded evidence under the unchanged acyclic model, but it may not consume
  the later architecture decision.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md`
  fixes the current surface vocabulary for fail-closed quantified crossing and
  reconstruction-visible output continuity, including the rule that positive
  `P6` requires the same recursive structure to remain review-visible on both
  internal and public output surfaces.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`
  preserves the accepted matrix tally:
  zero `stable visible persistence` rows;
  blocker-debt rows `C1`, `C2`, `C5`, and `C7`;
  fail-closed rows `C3`, `C4`, and `C6`.
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architecture-decision-and-successor-plan-choice.md`
  keeps `continue within the current architecture` as the strongest accepted
  aggregate strategic read before this refreshed roadmap family. Item `3`
  may not reinterpret that predecessor read into a present-tense item-5
  settlement.
- `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md`
  is the accepted item-2 artifact. It already sharpened `C1`, `C2`, and `C5`
  while intentionally leaving `C3` and `C7` as the remaining unsettled rows
  before the representative campaign.
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md`,
  `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-path-audit.md`,
  `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-collapse-clear-or-confirm.md`,
  `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md`,
  and
  `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md`
  remain the accepted predecessor chain for the exact `C7` same-lane retained-
  child pocket only. They establish one exact pocket and one blocker-debt
  classification under the unchanged architecture, not a family-level or
  repo-level success claim.
- Accepted rounds `round-094` through `round-098` remain bounded predecessor
  evidence only. They do not authorize widening beyond the exact same-lane
  retained-child pocket.
- `Bugs.md`
  still lists `BUG-2026-03-16-001` as predecessor replay context only. It does
  not authorize item-3 repair work, replay reopen, fallback widening, or
  roadmap reordering.

## File Map

### Create

- `docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md`
  - Responsibility: canonical item-3 artifact recording the exact bounded
    `C3` / `C7` cases, the focused read-only evidence rerun for each row, and
    one honest classification per row against the frozen item-1 ledger for
    `P5` and `P6`.

- `orchestrator/rounds/round-101/implementation-notes.md`
  - Responsibility: concise round-local mirror of the same two-case
    settlement slice, the exact commands rerun, the observed bounded surface
    facts, and the final case-by-case classifications.

### Read-Only Evidence

- `orchestrator/rounds/round-101/selection.md`
- `orchestrator/state.json`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-001/roadmap.md`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-001/verification.md`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-001/retry-subloop.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-settlement-contract-and-unresolved-family-evidence-ledger.md`
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-capability-contract-and-evaluation-corpus.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`
- `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architecture-decision-and-successor-plan-choice.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-path-audit.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-collapse-clear-or-confirm.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md`
- `orchestrator/rounds/round-099/review-record.json`
- `orchestrator/rounds/round-100/review-record.json`
- `orchestrator/rounds/round-094/review-record.json`
- `orchestrator/rounds/round-095/review-record.json`
- `orchestrator/rounds/round-096/review-record.json`
- `orchestrator/rounds/round-097/review-record.json`
- `orchestrator/rounds/round-098/review-record.json`
- `src/MLF/Elab/Run/Pipeline.hs`
- `src/MLF/Elab/Run/ResultType/Fallback.hs`
- `src/MLF/Elab/TermClosure.hs`
- `src/MLF/Elab/Run.hs`
- `src/MLF/Elab/Pipeline.hs`
- `src-public/MLF/Pipeline.hs`
- `test/PipelineSpec.hs`
- `Bugs.md`

### Preserve Unchanged

- `orchestrator/state.json`
- `orchestrator/roadmap.md`
- `orchestrator/retry-subloop.md`
- `orchestrator/verification.md`
- `Bugs.md`
- `src/`
- `src-public/`
- `app/`
- `test/`
- `mlf2.cabal`
- accepted predecessor docs and review records
- reviewer-owned history under earlier round directories

## Exact Selected Slice (Exactly One)

The only selected slice is:

produce one bounded two-row production-surface settlement-evidence dossier for
`C3` and `C7` under the frozen item-1 ledger.

This slice is allowed to:

- create one canonical item-3 artifact and one round-local
  `implementation-notes.md`;
- reuse the already-accepted same-lane retained-child pocket as the read-only
  continuity subject for `C7` and as the read-only quantified-crossing contrast
  frame for `C3`;
- rerun existing focused tests, existing exact-pocket replay harnesses, and
  read-only source-anchor inspections to make the accepted surface facts
  reviewer-visible again; and
- record exactly one honest classification for each of `C3` and `C7` from the
  frozen vocabulary
  `stable visible persistence`,
  `admitted but not reconstruction-visible / blocker debt`, or
  `fail-closed rejection`.

This slice is not allowed to:

- land or plan production implementation, hardening, representative-campaign
  work, or the item-5 global settlement gate;
- edit `src/`, `src-public/`, `app/`, `test/`, `mlf2.cabal`,
  `orchestrator/state.json`, roadmap contracts, retry contracts, verification
  contracts, or `Bugs.md`;
- invent a new packet, neighboring route, or second retained-child family for
  `C7`; it must reuse the exact same-lane pocket already accepted for `C2` and
  `C5`;
- invent a positive polymorphism packet for `C3`; it must stay tied to the
  same quantified-boundary guard and the same fail-closed retained-child search
  context already in the accepted record;
- relitigate accepted predecessor truth from item `2` or the same-lane exact-
  pocket chain;
- promote either row into repo-level family-matrix success,
  `non-cyclic-graph = keep`, or
  `reopen the non-cyclic-graph revision question`; or
- authorize equi-recursive reasoning, cyclic graphs, multi-SCC search, second
  interfaces, or fallback widening.

## Sequential Tasks

### Task 1 - Freeze the exact two-case subject and family mapping

- Treat this as a fresh item-3 settlement-evidence round for two already-
  frozen unresolved rows, not as mechanism work, repair work, or an
  architecture-decision round.
- Carry forward unchanged:
  - `C3` as nested-`forall` / quantified-crossing pressure observed on the
    same owner-local retained-child search, where the clear-boundary route
    would have been
    `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC`
    but no longer survives once `boundHasForallFrom` detects a crossing or
    `not hasForall` fails;
  - `C7` as the exact same-lane retained-child packet on
    `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC`, reread
    specifically through solver -> elaboration -> reconstruction -> internal
    output -> public output continuity.
- Freeze the family-to-row responsibilities explicitly:
  - `C3` is the primary bounded evidence row for
    `P5 polymorphism-nested-forall` and still carries `N2`
    unsoundness-guard context;
  - `C7` is the primary bounded evidence row for
    `P6 reconstruction-visible-output`; and
  - `C7` reuses the same exact same-lane pocket already accepted for `C2` and
    `C5`; it is not a new family or a second public interface.
- State explicitly in both artifact surfaces that item `3` sharpens only the
  current `P5` / `P6` settlement read and leaves item `4`, item `5`, and any
  code/test changes for later rounds.

### Task 2 - Gather fresh read-only evidence for `C3` and `C7`

- Reuse existing read-only anchors and accepted predecessor docs only. Do not
  create new helper functions, new tests, or new code paths to make the
  evidence easier to collect.
- For `C3`, rerun and capture reviewer-visible evidence from the exact
  quantified-boundary guard and its same-lane retained-child contrast only:
  - the `Fallback.hs` source anchors that keep
    `boundVarTargetRoot`, `boundHasForallFrom`,
    `sameLaneLocalRetainedChildTarget`, `keepTargetFinal`, and the
    `targetC` handoff explicit inside the same owner-local retained-child
    search;
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "keeps retained-child lookup bounded to the same local TypeRef lane"'`;
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "keeps retained-child fallback recursive through a same-lane local TypeRef root"'`; and
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "keeps retained-child fallback fail-closed when the same wrapper crosses a nested forall boundary"'`.
- For `C3`, do not invent a new positive polymorphism replay harness if the
  accepted record is still reject-side only. Instead, cite the accepted item-1
  ledger, representative campaign, and current source/test anchors that the
  quantified-crossing contrast remains non-recursive (`containsMu False`) and
  that no lawful recursive route survives after the crossing appears.
- For `C7`, rerun and capture reviewer-visible evidence from the exact same-
  lane retained-child packet only:
  - the read-only exact-pocket replay already used in the accepted same-lane
    predecessor chain via `cabal repl mlf2-test` with
    `:module + *PipelineSpec`, so the round records the helper-visible
    internal result, `containsMu`, and both authoritative public outputs for
    the exact frozen packet;
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "keeps retained-child fallback recursive through a same-lane local TypeRef root"'`;
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "same-lane retained-child exact packet clears Phase 6 elaboration"'`;
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "same-lane retained-child exact packet authoritative public output stays forall identity"'`;
  - `cabal test mlf2-test --test-show-details=direct --test-options='--match "same-lane retained-child exact edge 3 authoritative instantiation"'`; and
  - the same focused
    `ARI-C1 feasibility characterization (bounded prototype-only)` block if it
    remains the smallest accepted shared suite carrying the exact same-lane
    pocket and its bounded contrasts.
- For `C7`, reuse the accepted same-lane predecessor docs and read-only source
  anchors only:
  - `Fallback.hs` for the exact same-lane retained-child route;
  - `Pipeline.hs` for `checkedAuthoritative = typeCheck termClosed` plus the
    diagnostics-only fallback reconstruction call; and
  - the already-accepted same-lane path-audit / collapse / classification
    docs for the unchanged public-output breakpoint.
- Treat every rerun as evidence only. Even if a rerun still ends in blocker
  debt or reject-side classification, do not patch runtime behavior or tests
  in this round.

### Task 3 - Write one two-row settlement matrix for `P5` and `P6`

- Write the canonical item-3 artifact so it contains one bounded settlement
  matrix with exactly two case rows: `C3` and `C7`.
- For each row, record at least these fields:
  - exact frozen packet / route or exact fail-closed route boundary;
  - controlling positive-family target (`P5` or `P6`);
  - read-only evidence sources rerun this round;
  - current surface facts that remain binding;
  - one lawful classification from the frozen item-1 vocabulary; and
  - why the case remains bounded rather than promotable to repo-level
    settlement.
- Apply the accepted predecessor evidence narrowly:
  - `C3` should record the same owner-local retained-child search and the
    same-lane route as the clear-boundary candidate, then show that quantified
    crossing invalidates that candidate because `boundHasForallFrom` detects
    the crossing and `not hasForall` no longer holds; record that the accepted
    visible-output fact remains non-recursive (`containsMu False`) and classify
    the row as `fail-closed rejection`;
  - `C7` should record the exact same-lane retained-child route, the preserved
    helper-visible recursive read (`TMu ...`, `containsMu True`), the
    authoritative public collapse to `TForall "a" Nothing (TVar "a")`, the
    unchanged `checkedAuthoritative` breakpoint, and the reason that the row
    remains `admitted but not reconstruction-visible / blocker debt`.
- State explicitly that:
  - `C3` is reject-side pressure only, not a third admitted family and not
    positive `P5` success;
  - `C7` reuses the exact same same-lane retained-child pocket already
    accepted for `C2` / `C5`, not a new packet or alternate public path;
  - `C7` does not reach `stable visible persistence` unless the fresh rerun
    actually proves matching recursive visibility on both internal and public
    output surfaces; and
  - neither row by itself forces
    `reopen the non-cyclic-graph revision question`.

### Task 4 - Record the bounded settlement implications without widening scope

- Add one short synthesis section that says exactly what item `3` now sharpens
  and exactly what it still does not settle.
- The synthesis must say, in substance:
  - `P5` remains reject-side only unless the fresh exact row now proves a
    stronger bounded result, because quantified crossing still invalidates the
    same-lane retained-child candidate before a lawful recursive output read
    exists; and
  - `P6` still has one strongest bounded same-lane candidate, but recursive
    structure is not yet lawfully visible on both internal and public output
    surfaces for repo-scope success.
- The synthesis must also say what remains later work only:
  - item `4` representative family-matrix campaign;
  - item `5` the global `non-cyclic-graph` settlement gate; and
  - any code or test edits.

### Task 5 - Mirror the bounded result in round-local notes only

- Write `orchestrator/rounds/round-101/implementation-notes.md` as a concise
  round-local mirror of the canonical artifact.
- The notes must explicitly restate:
  - the exact two frozen rows and their exact routes / boundaries;
  - the exact commands rerun this round;
  - the observed bounded surface facts for `C3` and `C7`; and
  - the final case-by-case classifications.
- The notes must stay round-local only. Do not create extra settlement ledgers,
  side packets, or alternate decision documents in this round.

## Verification Handoff

The implementer should leave reviewer-visible evidence for all of the
following:

- `git diff --check`
- `python3 -m json.tool orchestrator/state.json >/dev/null`
- `rg -n '"contract_version": 2|"roadmap_id":|"roadmap_revision":|"roadmap_dir":|"retry": null|"retry": \{' orchestrator/state.json`
- `roadmap_dir="$(jq -r '.roadmap_dir' orchestrator/state.json)" && test -f "$roadmap_dir/roadmap.md" && test -f "$roadmap_dir/retry-subloop.md" && test -f "$roadmap_dir/verification.md"`
- `roadmap_dir="$(jq -r '.roadmap_dir' orchestrator/state.json)" && rg -n '^\d+\. \[(pending|in-progress|done)\]' "$roadmap_dir/roadmap.md"`
- predecessor-presence checks for the item-1 artifact, the accepted item-2
  artifact, the same-lane predecessor docs, and the accepted same-lane review
  records
- `rg -n 'boundVarTargetRoot =|boundHasForallFrom start0 =|sameLaneLocalRetainedChildTarget =|keepTargetFinal =|case sameLaneLocalRetainedChildTarget of|not hasForall' src/MLF/Elab/Run/ResultType/Fallback.hs`
- `rg -n 'checkedAuthoritative =|typeCheck termClosed|computeResultTypeFallback resultTypeInputs' src/MLF/Elab/Run/Pipeline.hs`
- the exact same-lane retained-child replay command used for this round, with
  reviewer-visible output for the helper-visible internal result,
  `containsMu`, and both authoritative public outputs
- `cabal test mlf2-test --test-show-details=direct --test-options='--match "keeps retained-child lookup bounded to the same local TypeRef lane"'`
- `cabal test mlf2-test --test-show-details=direct --test-options='--match "keeps retained-child fallback recursive through a same-lane local TypeRef root"'`
- `cabal test mlf2-test --test-show-details=direct --test-options='--match "keeps retained-child fallback fail-closed when the same wrapper crosses a nested forall boundary"'`
- `cabal test mlf2-test --test-show-details=direct --test-options='--match "same-lane retained-child exact packet clears Phase 6 elaboration"'`
- `cabal test mlf2-test --test-show-details=direct --test-options='--match "same-lane retained-child exact packet authoritative public output stays forall identity"'`
- `cabal test mlf2-test --test-show-details=direct --test-options='--match "same-lane retained-child exact edge 3 authoritative instantiation"'`
- `cabal test mlf2-test --test-show-details=direct --test-options='--match "ARI-C1 feasibility characterization (bounded prototype-only)"'`
- `rg -n 'C3|C7|P5|P6|stable visible persistence|admitted but not reconstruction-visible / blocker debt|fail-closed rejection' docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md orchestrator/rounds/round-101/implementation-notes.md`
- `git diff --name-only -- src test src-public app mlf2.cabal Bugs.md orchestrator/state.json`
  - expected result: no output; this round is docs-only unless the plan is
    violated

Because this plan preserves `src/`, `src-public/`, `app/`, `test/`,
`mlf2.cabal`, `Bugs.md`, and `orchestrator/state.json` unchanged,
`cabal build all && cabal test` is out of scope for this round unless the diff
escapes the authorized docs / orchestrator artifact surface.

## Failure Discipline

If fresh read-only evidence does not support the expected `C3` / `C7` reads,
fail closed inside the same frozen rows:

- record the exact contradiction in the canonical artifact and the round-local
  notes;
- choose the lawful row classification that the actual rerun now supports, but
  keep the interpretation bounded to the same exact row and pocket;
- keep the contradiction bounded to `C3` or `C7` rather than improvising a
  broader theory;
- do not patch code, add tests, or widen into repair work, representative
  campaigning, or the global settlement gate; and
- do not reinterpret one contradicted row into repo-level
  `non-cyclic-graph = keep` or `reopen`.

## Completion Criteria

This plan is complete when the implementer can land one docs-first canonical
artifact and one round-local notes file that:

- preserve the frozen item-1 settlement vocabulary and inherited boundary
  unchanged;
- classify exactly `C3` and `C7`, with no extra rows and no missing row;
- keep `C3` tied to the same quantified-boundary guard and reject-side
  retained-child search context rather than inventing a new positive-family
  packet;
- keep `C7` tied to the same exact same-lane retained-child pocket rather than
  inventing a new family, neighboring route, or second interface;
- sharpen only the current `P5` / `P6` settlement read without consuming item
  `4` or item `5`;
- leave code, tests, controller state, roadmap machine metadata, and
  `Bugs.md` untouched; and
- hand off only to later bounded roadmap work, with item `4` as the next
  immediate consumer, without silently widening into production
  implementation or global settlement.
