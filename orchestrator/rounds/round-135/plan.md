# Round 135 Plan

- Round: `round-135`
- Roadmap: `2026-03-28-04-same-lane-retained-child-representative-gap-successor-roadmap` / `rev-001`
- Item: `item-1`
- Retry: `null`
- Execution shape: aggregate-only; no lane-parallel split is authorized in this round

## Objective

Freeze one docs-only successor-boundary artifact for the remaining same-lane
retained-child representative gap. This round must bind the predecessor
authority chain, one exact second-packet subject, the exact item-2 decision
bar, and the writable slice for one bounded current-architecture attempt while
keeping the settled same-lane pocket, exact `C1`, exact `P1`, and exact `P5`
packets closed.

## Sequential Plan

1. Create the canonical item-1 freeze artifact at
   `docs/plans/2026-03-28-same-lane-retained-child-representative-gap-successor-authority-exact-subject-success-bar-and-writable-slice-freeze.md`
   and keep the round docs-only.
   - Mark it as item `1`, `attempt-1`, `retry: null`, docs-only, freeze-only,
     and pre-implementation.
   - State explicitly that this round does not implement code, rerun focused
     domain evidence as new authority, reopen predecessor packets, authorize
     cyclic search, multi-SCC search, equi-recursive reasoning, fallback
     widening, a second interface, hardening, or repo-level capability claims.
   - Limit the implementer-owned writes for this round to the canonical freeze
     doc and `orchestrator/rounds/round-135/implementation-notes.md`.

2. Populate the authority ledger from the exact predecessor chain this family
   inherits.
   - Carry forward
     `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
     as the explicit-only / iso-recursive / non-equi-recursive /
     non-cyclic / no-fallback baseline.
   - Carry forward
     `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-capability-contract-and-evaluation-corpus.md`
     as the binding representative-family success contract.
   - Cite the accepted post-`P5` repo-scope predecessor trio as the direct
     authority that opened this family:
     `docs/plans/2026-03-28-post-p5-repo-scope-successor-authority-evidence-inputs-and-non-widening-boundary-freeze.md`,
     `docs/plans/2026-03-28-post-p5-repo-scope-refreshed-representative-family-matrix-readiness-surface-and-provenance-validation.md`,
     and
     `docs/plans/2026-03-28-post-p5-repo-scope-readiness-successor-gate-and-immediate-handoff-decision.md`.
   - Carry the accepted same-lane exact-pocket chain only as closed predecessor
     truth, with
     `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md`
     as the named lead gate for that settled packet.
   - Classify `Bugs.md` only as non-authoritative predecessor bug context; it
     may not reopen the older `P2` replay defect or any settled packet.

3. Freeze the exact live subject as one second same-lane retained-child packet,
   not just the family label.
   - Bind the new subject against the settled first pocket for contrast:
     the first pocket remains the exact `boundVarTargetRoot` /
     `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC` clear-
     boundary packet carried by `C2` / `C5` / `C7`.
   - Require the new freeze artifact to name the second packet's exact source
     expression or harness anchor, its route, its owner / binder frame, and
     the authoritative surface whose recursive visibility is under test.
   - Require the selected packet to be distinct from the settled clear-boundary
     `C2` / `C5` / `C7` pocket and distinct from the settled exact `P5`
     packet, while still being the strongest bounded representative of the
     unresolved `P3` / `P4` / `P6` gap under the current architecture.
   - Record explicitly that `C5` and `C7` remain rereads of the first pocket,
     not second packets, and that this round does not reopen `C1`, `P1`, or
     `P5`.

4. Freeze the exact item-2 decision bar for that packet.
   - Item `2` may operate only on the frozen second packet and only inside the
     inherited current architecture.
   - A positive clear requires review-visible recursive structure on the
     current authoritative pipeline surfaces, not helper-only, replay-only, or
     fallback-only evidence.
   - If that positive clear does not happen, item `2` must still end in one
     honest exact-packet outcome only:
     `fail-closed` or `narrower current-architecture blocker`.
   - Record that even a positive exact-packet result remains one bounded packet
     only and does not settle general `P3`, `P4`, or `P6`, and does not permit
     repo-level capability claims.

5. Freeze the writable slice for the next bounded current-architecture
   attempt.
   - Permit only the same-lane retained-child routing / authoritative-output
     continuity paths:
     `src/MLF/Elab/Run/ResultType/Fallback.hs`,
     `src/MLF/Elab/Run/Scope.hs`,
     `src/MLF/Elab/Run/Pipeline.hs`,
     `src/MLF/Elab/Pipeline.hs`,
     `src-public/MLF/Pipeline.hs`,
     and `src/MLF/Elab/TermClosure.hs`.
   - Permit only the exact supporting test and round-owned surfaces:
     `test/PipelineSpec.hs`,
     `test/Research/SameLaneRetainedChildRepresentativeGapSpec.hs`,
     `test/Main.hs`,
     `mlf2.cabal`,
     the canonical freeze doc,
     and `orchestrator/rounds/round-135/*`.
   - Block all `src/MLF/Constraint/**`, cyclic or multi-SCC machinery,
     fallback widening, second-interface work, second-family openings, and any
     edits outside this frozen slice.
   - Record that if item `2` touches `src/`, `src-public/`, `app/`, `test/`,
     or `mlf2.cabal`, the full gate `cabal build all && cabal test` becomes
     mandatory under the live verification contract.

6. Finish the item-1 docs-only round with explicit review expectations.
   - Reviewer focus must stay on authority continuity, exact-subject freeze,
     exact item-2 bar, exact writable-slice freeze, and non-widening scope.
   - The round remains aggregate-only and stops at the freeze; it does not
     authorize implementation inside the same round.
