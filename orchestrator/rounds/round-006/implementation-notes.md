# Round 006 Implementation Notes

Artifact summary:

- Added `docs/plans/2026-03-14-unannotated-iso-recursive-r1-gap-map.md` as the `R1` gap-map artifact from accepted `R0` / `ARI-C1` to the bounded unannotated single-SCC, single-binder-family target.
- The artifact names the finite deltas that explicit anchors still cover, separates what may be locally recoverable from what remains blocked, maps each gap to the inherited invariant classes, and assigns each unresolved decision to a later stage.

Why this satisfies successor roadmap item 1:

- The roadmap-design `R1` gate requires one concrete, finite gap list between accepted `ARI-C1` behavior and the unannotated single-SCC target.
- This round delivers that list without selecting a subset, defining the obligation contract, making a feasibility decision, or drafting an implementation handoff.

Authority and continuity:

- The inherited item-2 invariant audit in `docs/plans/2026-03-14-automatic-recursive-inference-invariant-audit.md` remains authoritative and was not reopened.
- Completed rounds `001` through `005` and the predecessor recursive-types packet remain reference-only historical evidence.

Scope statement:

- This round is docs-only.
- No production behavior changed.
- `R2` through `R5` remain future stages and were not executed here.
