# Round 203 - Task Selection

**Selected milestone**: `milestone-1`
**Selected direction**:
`direction-1a-refreeze-inherited-boundary-after-reopening-polymorphic-mediation-semantics`
**Selected extraction**:
`refreeze-inherited-boundary-after-reopening-polymorphic-mediation-semantics`
**Current task/title**:
`Re-freeze the inherited boundary after reopening nested-forall mu-preservation semantics`

**Roadmap identity**:
- roadmap_id: `2026-04-07-00-p5-polymorphism-nested-forall-explicit-boundary-revision-roadmap`
- roadmap_revision: `rev-002`
- roadmap_dir: `orchestrator/roadmaps/2026-04-07-00-p5-polymorphism-nested-forall-explicit-boundary-revision-roadmap/rev-002`
- milestone_id: `milestone-1`
- direction_id:
  `direction-1a-refreeze-inherited-boundary-after-reopening-polymorphic-mediation-semantics`
- extracted_item_id:
  `refreeze-inherited-boundary-after-reopening-polymorphic-mediation-semantics`

## Why now

The active roadmap revision changed. `rev-001` and accepted `round-202`
remain historical predecessor evidence, but they are no longer the
controlling family freeze because they treated the docs-only `round-151`
polymorphic-mediation `mu`-absorption reclassification as settled correct
predecessor truth.

`rev-002` supersedes that controlling read without rewriting the historical
artifacts. The family now has to re-freeze its exact inherited boundary and
decision surface before any milestone-2 broader positive `P5` ledger can be
honest again. Because round-203 was still only at `plan`, the lawful next
move is to reseat the same round onto the new first milestone instead of
continuing milestone-2 work against a superseded premise.

The immediate question is therefore not yet the broader ledger beyond the one
settled retained-child clear-boundary lane. It is the prior question that
became live again: whether nested-forall / quantified-crossing
polymorphic mediation lawfully absorbs `mu` in
`nestedForallContrastExpr`-class broader positive `P5` cases, or whether
recursive structure should survive on the authoritative pipeline outputs under
the inherited iso-recursive boundary.

## Current baseline

- Round-203 branch/worktree `HEAD`: `2a08e83`
- Controller-prepared round branch remains:
  `orchestrator/round-203-build-broader-positive-p5-ledger`
- Controller-prepared round worktree remains: `orchestrator/worktrees/round-203`
- Active roadmap family remains
  `2026-04-07-00-p5-polymorphism-nested-forall-explicit-boundary-revision-roadmap`,
  but the active revision is now `rev-002`
- `milestone-1` is pending again under `rev-002`; `milestone-2` and
  `milestone-3` remain pending behind that refreeze
- No retry state is active (`retry: null`, no `resume_error`)
- Accepted `round-202` still published the historical milestone-1 freeze
  artifact at
  `docs/plans/2026-04-07-p5-polymorphism-nested-forall-explicit-boundary-revision-family-inherited-boundary-pressure-and-planning-only-decision-surface-freeze.md`,
  but `rev-002` no longer treats its round-151 classification as controlling
- Accepted `round-197` still keeps
  `sameLaneAliasFrameClearBoundaryExpr`
  as one settled retained-child clear-boundary success on
  `runPipelineElab` / `runPipelineElabChecked`, while
  `nestedForallContrastExpr` remains fail-closed with
  `PhiTranslatabilityError`; neither result is broader family closure by
  itself
- Accepted `round-151` remains a docs-only historical reclassification only;
  it did not change assertions or production behavior, so `rev-002` reopens
  its semantic status rather than treating it as binding proof
- Accepted `round-191` / `round-181` still keep `P2` bounded to the exact
  `C1` packet only, and accepted `round-192` still keeps
  `N1 ambiguity-reject`,
  `N2 unsoundness-guard`, and
  `N6 termination-pressure`
  closed as predecessor truth only
- Repository status in the canonical round worktree before writing this
  selection was controller-owned `orchestrator/state.json` plus round-owned
  `orchestrator/rounds/round-203/*` edits

## Scope

- Keep this round docs-only, planning-only, serial, and faithful to exactly
  one milestone-1 / direction-1a extraction:
  re-freeze the inherited boundary after reopening nested-forall
  polymorphic-mediation `mu`-preservation semantics
- Publish one canonical milestone-1 refreeze artifact under `docs/plans/`
  plus round bookkeeping only; that artifact must explicitly name the exact
  reopened semantic question, the exact inherited boundary clauses now under
  pressure, the exact preserved predecessor truth that stays closed, the exact
  live broader positive `P5` subject, and the exact lawful later outcomes for
  `rev-002`
- Ground the refreeze only in the accepted lineage already fixed by the active
  roadmap plus the concrete term-level pressure around
  `nestedForallContrastExpr`, including:
  `round-202`, `round-201`, `round-200`, `round-197`, `round-192`,
  `round-191`, `round-181`, `round-151`, `implementation_notes.md`, and
  `test/Research/P5ClearBoundarySpec.hs`
- Keep preserved predecessor truth closed: do not reopen the settled
  retained-child clear-boundary lane, do not reopen `P2` or the exact `C1`
  packet, and do not reopen the representative negative-family rows as live
  debt
- Keep the family planning-only: do not revise code, tests, Cabal wiring, or
  enact a concrete boundary revision
- Do not build the milestone-2 broader positive `P5` ledger yet, do not
  perform the milestone-2 comparison yet, and do not bind a milestone-3
  downstream consequence yet
