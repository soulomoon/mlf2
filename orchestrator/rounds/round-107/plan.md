# Round 107 Plan (`item-4` Audit-Target And Evaluation-Surface Bind)

## Objective

Execute only roadmap item `4` and prepare one docs-only bind artifact at:
`docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-architecture-pressure-audit-target-and-evaluation-surface-bind.md`.

This is the initial `item-4` plan for `attempt-1` with `retry: null`. The
round must consume accepted items `1` through `3` as authoritative
predecessor truth, remain planning-only and architecture-only, and freeze the
exact architecture-pressure audit target and evaluation surface for exactly
one already-selected reopened subject only.

Current planning read: item `4` must bind what later work may inspect for the
same exact `C2` / `C5` / `C7` pocket, but it must not decide which lawful
item-3 outcome bar is true and must not decide whether item `5` opens one
bounded architecture-amendment lane or stops. The bind must preserve
`iso-recursive = keep`, `non-equi-recursive = keep`, and
`no-fallback = keep`; keep rev-001 items `6` through `8` blocked; keep
multi-SCC search, second interfaces, fallback widening, and production
implementation blocked; and fail closed if the artifact starts reopening
subject selection, broadening beyond the one same-lane pocket, or implying
that implementation is now authorized.

This round is docs-only. It must not edit controller-owned machine state,
roadmap contracts, `Bugs.md`, source code, tests, or Cabal files. No commit
is authorized.

## Locked Round Context

- Round id: `round-107`
- Roadmap item: `item-4`
- Stage: `plan`
- Active attempt: `attempt-1`
- Retry state: `null`
- Active branch: `codex/round-107`
- Active worktree:
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-107`
- Fixed live subject: one docs-only rev-002 item-4 bind for the exact
  same-lane retained-child / public-output continuity pocket selected in
  accepted item `2` and contract-governed in accepted item `3`
- Current round review feedback: none yet; no `review.md`,
  `reviews/attempt-<n>.md`, or `review-record.json` exists for `round-107`,
  so this is a full `attempt-1` plan rather than a retry delta

Current worktree state is already non-pristine. Respect existing edits and do
not revert unrelated work:

- controller-owned `orchestrator/state.json` may already be modified and must
  remain untouched; and
- existing `orchestrator/rounds/round-107/selection.md` is a round input and
  must remain untouched.

Under the live retry contract, item `4` is retry-capable, but this plan is
for the initial `retry: null` attempt only.

## Accepted Continuity That Remains Binding

- `orchestrator/rounds/round-107/selection.md`
  fixes this round to roadmap item `4` only, requires a docs-only
  architecture bind, preserves accepted item-1 through item-3 truth, keeps
  rev-001 items `6` through `8` blocked, and explicitly blocks
  implementation work, subject reselection, item-3 outcome choice, and
  item-5 open-vs-stop choice.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/roadmap.md`
  makes item `4` the next unfinished item after accepted items `1` through
  `3` and defines completion as one accepted docs-only artifact freezing the
  exact rows, packets, modules, and review-visible output surfaces for the
  selected reopened subject.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/retry-subloop.md`
  keeps item `4` retry-capable inside the same round while preserving prior
  attempts as immutable history.
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/verification.md`
  requires reopened-target-bind checks proving that item `4` freezes exact
  accepted rows, packets, modules, and output surfaces for the selected
  subject without authorizing implementation by implication.
- `orchestrator/rounds/round-106/review-record.json`
  is the authoritative acceptance record for predecessor item `3`. Its
  accepted checks make the three lawful outcome bars binding while preserving
  later-item ownership and docs-only scope.
- `docs/plans/2026-03-26-global-non-cyclic-graph-reopened-revision-authority-and-candidate-boundary-freeze.md`
  is the authoritative item-1 freeze. It preserved rev-002 as planning-only
  and architecture-only, froze exactly one candidate boundary, and kept
  rev-001 items `6` through `8` blocked.
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-reopened-subject-selection.md`
  is the authoritative item-2 selection. It chose exactly one live reopened
  subject: the same exact `C2` / `C5` / `C7` pocket, with family, anchor,
  owner-local frame, route, clear-boundary-only status, and exact packet
  identity all frozen.
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-safety-and-acceptance-contract.md`
  is the authoritative item-3 contract. It defines exactly three lawful
  outcome bars for that same exact selected pocket, preserves
  `iso-recursive = keep`, `non-equi-recursive = keep`, and
  `no-fallback = keep`, and explicitly defers exact audit binding to
  item `4`.
- `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md`
  binds `C2` as the primary admitted same-lane pocket and binds `C5` as that
  same exact pocket through the owner / binder-sensitive placement lens only,
  not as a second packet.
- `docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md`
  binds `C7` as that same exact `C2` / `C5` pocket through the
  public-output continuity lens only, with the helper-visible/internal
  versus authoritative-public split still below `stable visible persistence`.
- `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md`
  is the accepted representative matrix. It keeps zero
  `stable visible persistence` rows, keeps `C2`, `C5`, and `C7` as
  blocker debt, and keeps non-local `C1` as bounded contrast context rather
  than a second selected subject.
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md`
  freezes the six review-visible surfaces, the exact tuple, the exact packet,
  and the accepted helper-visible/internal versus authoritative-public split
  for this same pocket.
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-path-audit.md`
  fixes the same-pocket authoritative path map, including
  `checkedAuthoritative` as the first exact public-output break and
  `termClosed` / `typeCheck termClosed` as same-pocket dependencies.
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-collapse-clear-or-confirm.md`
  confirms that the bounded `Pipeline.hs` / `TermClosure.hs` root-handoff
  slice contains no alternate recursive whole-packet authoritative result for
  this pocket.
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md`
  keeps the exact-pocket end-to-end ledger reviewer-visible and fixes the
  current selected-pocket classification below success without widening the
  subject.
- `docs/plans/2026-03-26-global-non-cyclic-graph-keep-vs-reopen-decision-gate.md`
  remains the authoritative rev-001 reopen record. It preserved
  `iso-recursive = keep`, `non-equi-recursive = keep`, and
  `no-fallback = keep`, reopened only the `non-cyclic-graph` question, and
  explicitly kept rev-001 items `6` through `8` blocked.
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  remains the inherited boundary contract:
  explicit-only baseline,
  iso-recursive only,
  non-equi-recursive,
  inherited `non-cyclic-graph` boundary,
  no second interface,
  and no fallback widening.

## File Map

### Create

- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-architecture-pressure-audit-target-and-evaluation-surface-bind.md`
  - Responsibility: canonical docs-only rev-002 item-4 artifact freezing the
    exact same-pocket audit target, exact source/test module set, exact
    reviewable command set, and exact evaluation surfaces for later
    inspection, without choosing an outcome and without authorizing
    implementation.

### Read-Only Evidence

- `orchestrator/rounds/round-107/selection.md`
- `orchestrator/rounds/round-106/review-record.json`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/roadmap.md`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/retry-subloop.md`
- `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/verification.md`
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-keep-vs-reopen-decision-gate.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-reopened-revision-authority-and-candidate-boundary-freeze.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-reopened-subject-selection.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-safety-and-acceptance-contract.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md`
- `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-path-audit.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-collapse-clear-or-confirm.md`
- `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md`

### Read-Only Source/Test Anchors

- `src/MLF/Elab/Run/ResultType.hs`
- `src/MLF/Elab/Run/ResultType/Fallback.hs`
- `src/MLF/Elab/Run/Pipeline.hs`
- `src/MLF/Elab/Run.hs`
- `src/MLF/Elab/Pipeline.hs`
- `src/MLF/Elab/TermClosure.hs`
- `src-public/MLF/Pipeline.hs`
- `test/PipelineSpec.hs`

### Preserve Unchanged

- `orchestrator/state.json`
- `orchestrator/rounds/round-107/selection.md`
- roadmap files under
  `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/`
- `Bugs.md`
- `src/`
- `src-public/`
- `app/`
- `test/`
- `mlf2.cabal`

No round-local `implementation-notes.md` is authorized for this item-4 plan.
The docs artifact above is the only intended implementation output path. The
implementer must not create or edit any other file for item `4`.

## Exact Selected Slice (Exactly One)

The only selected slice is:

bind the exact architecture-pressure audit target and evaluation surface for
the one already-selected same-lane retained-child / public-output continuity
pocket carried by accepted rows `C2`, `C5`, and `C7`, and only that one
selected pocket.

### Exact Accepted Rows

| Row | Exact same-pocket role that remains binding | What item `4` may freeze for later inspection | Bound that must remain closed |
| --- | --- | --- | --- |
| `C2` | primary same-lane retained-child row for the selected pocket | family / anchor / owner-local frame / route identity plus helper-visible same-lane reconstruction continuity on the exact packet | not a second packet, not a broader `P3` claim, not a family-wide success story |
| `C5` | same exact `C2` pocket through the owner / binder-sensitive placement lens only | same local `TypeRef` lane, same owner-local frame, and same nested-`forall` fail-closed guard context | not a second owner-local packet, not a second route, not a broad `P4` theory |
| `C7` | same exact `C2` / `C5` pocket through the public-output continuity lens only | authoritative public-output split, exact public entrypoints, and reviewer-visible continuity-loss surface for the same packet | not a second interface, not a second public path, not a broad `P6` success claim |

`C1` is not part of the live item-4 audit target. If mentioned at all, it may
appear only as bounded contrast context proving that non-local continuity is
not being promoted into a second selected subject, second command surface, or
second accepted packet.

### Exact Packet

The artifact must freeze exactly this packet and no variant:

```haskell
ELet "k" (ELamAnn "x" recursiveAnn (EVar "x"))
  (ELet "u" (EApp (ELam "y" (EVar "y")) (EVar "k")) (EVar "u"))
```

where
`recursiveAnn = STMu "a" (STArrow (STVar "a") (STBase "Int"))`.

The same frozen tuple remains binding:

- family: same-lane retained-child
- recursive-shape anchor: `boundVarTargetRoot`
- owner / binder frame: one owner-local retained-child frame
- route: `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC`
- quantified-boundary status: clear-boundary only:
  `boundHasForallFrom` is false and `not hasForall` holds

### Exact Module Set

Later item-4 work may inspect only this exact source/test module set for the
selected pocket:

| Module | Why it is in-scope for the bind | Pocket face it serves |
| --- | --- | --- |
| `src/MLF/Elab/Run/ResultType/Fallback.hs` | owns `boundVarTargetRoot`, `boundHasForallFrom`, `sameLaneLocalRetainedChildTarget`, `keepTargetFinal`, and the same-lane retained-child route preservation | `C2` primary route identity and `C5` guard / placement continuity |
| `src/MLF/Elab/Run/ResultType.hs` | owns dispatch into `computeResultTypeFallback` for the same-pocket helper-visible route | `C2` helper-visible reconstruction continuity |
| `src/MLF/Elab/Run/Pipeline.hs` | owns `runPipelineElabWith`, `termClosed`, `checkedAuthoritative`, and the exact authoritative public-output commitment | `C7` public-output continuity surface |
| `src/MLF/Elab/Run.hs`, `src/MLF/Elab/Pipeline.hs`, `src-public/MLF/Pipeline.hs` | fix the single public-entrypoint re-export chain and keep a second interface blocked | `C7` no-second-interface continuity |
| `src/MLF/Elab/TermClosure.hs` | remains in-scope only because accepted item `3` proved the bounded `Pipeline.hs` / `TermClosure.hs` root-handoff slice contains no alternate recursive whole-packet authoritative result | `C7` accepted blocker-proof contrast only |
| `test/PipelineSpec.hs` | owns the exact same-lane harness and the focused exact-pocket tests already accepted as review-visible evidence | `C2` / `C5` / `C7` exact-pocket replay and test anchors |

No other module, interface, executable path, or search space is part of the
item-4 bind.

### Exact Commands

The artifact must freeze only the following command surfaces for later
inspection of the selected pocket:

| Command | Exact purpose bound to the selected pocket |
| --- | --- |
| `cabal repl mlf2-test --repl-options=-ignore-dot-ghci` | launch the exact-pocket replay environment already accepted for the same packet only |
| `:module + *PipelineSpec` | inside that REPL, reuse the exact same-lane harness anchored in `test/PipelineSpec.hs` for the frozen packet only; no alternate harness and no second packet |
| `cabal test mlf2-test --test-show-details=direct --test-options='--match "keeps retained-child fallback recursive through a same-lane local TypeRef root"'` | `C2` same-lane retained-child helper-visible recursion continuity |
| `cabal test mlf2-test --test-show-details=direct --test-options='--match "keeps retained-child lookup bounded to the same local TypeRef lane"'` | `C5` owner-local / binder-sensitive same-lane lookup bound |
| `cabal test mlf2-test --test-show-details=direct --test-options='--match "keeps retained-child fallback fail-closed when the same wrapper crosses a nested forall boundary"'` | `C5` fail-closed quantified-boundary guard for the same pocket |
| `cabal test mlf2-test --test-show-details=direct --test-options='--match "same-lane retained-child exact packet clears Phase 6 elaboration"'` | exact same-pocket elaboration continuity before the public-output break |
| `cabal test mlf2-test --test-show-details=direct --test-options='--match "same-lane retained-child exact packet authoritative public output stays forall identity"'` | `C7` authoritative public-output continuity fact for the same packet |
| `cabal test mlf2-test --test-show-details=direct --test-options='--match "same-lane retained-child exact edge 3 authoritative instantiation"'` | same-pocket authoritative instantiation continuity already accepted in the exact-pocket chain |
| `rg -n 'boundVarTargetRoot =|boundHasForallFrom start0 =|sameLaneLocalRetainedChildTarget =|keepTargetFinal =|case sameLaneLocalRetainedChildTarget of|not hasForall' src/MLF/Elab/Run/ResultType/Fallback.hs` | source-anchor inspection for the same-lane route, guard, and owner-local lookup rules tied to `C2` / `C5` |
| `rg -n 'computeResultTypeFallback|runPipelineElab =|runPipelineElabChecked =|runPipelineElabWith|termClosed|checkedAuthoritative =|typeCheck termClosed' src/MLF/Elab/Run/ResultType.hs src/MLF/Elab/Run/Pipeline.hs` | source-anchor inspection for helper-visible reconstruction and authoritative public-output commitment |
| `rg -n 'runPipelineElab|runPipelineElabChecked|runPipelineElabWith' src/MLF/Elab/Run.hs src/MLF/Elab/Pipeline.hs src-public/MLF/Pipeline.hs` | prove that the selected pocket still has one public-entrypoint chain and no second interface |
| `rg -n 'same-lane retained-child exact packet|same-lane local TypeRef root|same local TypeRef lane|nested forall boundary|edge 3 authoritative instantiation' test/PipelineSpec.hs` | prove the replay/test anchors are the already-accepted exact-pocket anchors only |

These commands are review surfaces only. They do not authorize code changes,
new harnesses, a second packet, broader search, or production claims.

### Exact Review-Visible Output Surfaces

The artifact must freeze exactly these six review-visible surfaces, matching
the accepted same-lane ledger vocabulary and no broader surface set:

| Surface | Exact same-pocket read later work may inspect | Primary accepted anchors |
| --- | --- | --- |
| Solver admission state | same family, same `boundVarTargetRoot` anchor, same owner-local frame, same route, same clear-boundary-only status | accepted item `2`; `Fallback.hs` same-lane route anchors |
| Elaboration handoff / result state | the exact packet still clears elaboration before the public-output break | accepted predecessor same-lane Phase-6 clearance; focused exact-pocket elaboration test |
| Reification / reconstruction state | the helper-visible same-lane route still reconstructs a `TMu`-bearing internal type term for the same packet | `test/PipelineSpec.hs`; `computeResultTypeFallback`; accepted path audit |
| Internal output surface | helper-visible/internal output remains recursive and review-visible as `TMu ...` with `containsMu True` | same-lane replay output and accepted same-lane ledger |
| Public output surface | both authoritative public entrypoints remain `Right (TForall "a" Nothing (TVar "a"))` for the same packet | `runPipelineElab`; `runPipelineElabChecked`; `checkedAuthoritative` anchor |
| Reviewer-visible evidence trail | reviewers can point to one exact tuple and one exact internal/public split without witness-only rescue, replay folklore, or alternate interfaces | accepted same-lane case ledger, path audit, blocker proof, and end-to-end revalidation |

The item-4 bind must keep these surfaces explicit, review-visible, and
same-pocket-only. It must not add a seventh surface, a second interface, a
second packet, or a family-wide evidence trail.

## Explicit Disallowed Scope

The following remain explicitly disallowed in this round:

- any change to accepted item-1 truth, accepted item-2 truth, accepted
  item-3 truth, accepted rev-001 truth, or the rule that rev-001 items
  `6` through `8` stay blocked;
- any reopening of subject selection, any introduction of a second subject,
  or any promotion of `C1` into a coequal audit target;
- any treatment of `C5` or `C7` as second packets, second routes, second
  interfaces, or separate successor lanes;
- any choice of one item-3 outcome bar as the current result;
- any decision about whether item `5` opens one bounded architecture-amendment
  lane or stops;
- any widening into broad family search, multi-SCC search, second-interface
  work, fallback widening, equi-recursive semantics, implicit unfolding,
  production implementation, hardening, or repo-level capability claims;
- any creation or edit outside the one intended implementation output path
  named above;
- any edit to `orchestrator/state.json`, roadmap files, retry files,
  verification files, `Bugs.md`, source code, tests, or Cabal files; and
- any commit.

If the artifact starts selecting an item-3 outcome, reopening subject
selection, naming a second packet, implying that code work is now authorized,
or narrating a lane-open-or-stop decision, it has already widened beyond item
`4` and must fail closed.

## Required Verification

This planning round remains docs-only. Required verification for the plan
artifact and later item-4 implement work must therefore stay on docs/control
plane boundaries unless a later diff escapes that surface.

### Plan-Round Verification

- `git diff --check`
- `git diff --name-only`
- `git status --short --branch`

The plan reviewer should confirm from the resulting diff and file contents
that:

- only `orchestrator/rounds/round-107/plan.md` was created or edited by this
  round work;
- the plan freezes exactly one intended implementation output path and
  forbids all other edits;
- the plan binds rows `C2`, `C5`, and `C7` only, with any `C1` mention
  remaining contrast-only;
- the plan names the exact packet, exact module set, exact command set, and
  exact six review-visible surfaces; and
- the plan stays planning-only, architecture-only, docs-only, and item-4-only.

### Later Item-4 Verification Contract To Preserve

- baseline rev-002 verification commands from
  `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-002/verification.md`
  remain binding;
- docs-diff review remains mandatory because item `4` is docs-only;
- the full `cabal build all && cabal test` gate remains out of scope unless a
  later diff escapes the authorized docs / orchestrator surface; and
- later item-4 review must explicitly prove that the accepted rows, packet,
  modules, commands, and review-visible surfaces stayed bound to the one
  selected same-lane pocket only, without implied implementation clearance.
