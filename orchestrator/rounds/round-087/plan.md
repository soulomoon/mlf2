# Round 087 Plan (`item-6` Coverage Campaign)

## Objective

Execute only roadmap item `6` and produce one docs-first artifact at:
`/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`.

This is the initial `item-6` plan for `attempt-1` with `retry: null`. The
round must run one bounded representative coverage and feasibility campaign
for general automatic iso-recursive inference inside the inherited acyclic
architecture. The artifact must:

- apply the accepted item-5 persistence tuple, phase-and-surface ledger, and
  bounded outcome vocabulary to representative capability-corpus pressure;
- cover both admitted item-4 families plus representative nested-`forall`
  reject-side, ambiguity-reject, binder-sensitive, termination-pressure, and
  reconstruction-heavy pressure rather than a convenience subset;
- classify each representative row as `stable visible persistence`,
  `admitted but not reconstruction-visible / blocker debt`, or
  `fail-closed rejection`;
- separate bounded positive evidence from blocker debt and explicit
  fail-closed territory without silently promoting predecessor packets into a
  repo-level generality claim; and
- end with one bounded feasibility read only:
  whether the accumulated evidence supports `broad generality`,
  `bounded subset only`, or `architectural dead end`.

This round is docs-only and coverage-campaign-only. It must not edit
production code, tests, public surfaces, executables, `mlf2.cabal`,
`orchestrator/state.json`, `orchestrator/roadmap.md`,
`orchestrator/verification.md`, `orchestrator/retry-subloop.md`, `Bugs.md`,
or any review / merge / predecessor-history artifact.

This round must not silently widen into:

- a new mechanism-map or search-model redesign;
- a new reconstruction contract;
- code-path experiments, implementation slices, or test expansion;
- equi-recursive reasoning or implicit unfolding;
- cyclic structural graphs, multi-SCC search, or cross-family reopening;
- second interfaces or fallback behavior;
- a revision of the inherited baseline boundary; or
- the item-7 architecture decision itself.

Current planning read: item `6` is the evidence-classification and bounded
feasibility gate between the accepted item-5 persistence contract and the
item-7 architecture decision. If the accepted record does not justify a
stronger success read for a representative row, the artifact must record
blocker debt or fail-closed rejection rather than inventing new evidence,
rerunning code paths, or broadening the architecture.

## Locked Round Context

- Round id: `round-087`
- Roadmap item: `item-6`
- Stage: `plan`
- Active attempt: `attempt-1`
- Retry state: `null`
- Fixed live subject: one docs-only representative coverage and feasibility
  campaign for the accepted bounded automatic iso-recursive evidence chain
- Active branch: `codex/round-087-item-6-coverage-campaign`
- Active worktree:
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087`
- Current round review feedback: none yet; no `review.md`,
  `reviews/attempt-<n>.md`, or `review-record.json` exists for `round-087`,
  so this is a full `attempt-1` plan rather than a retry delta

Current round worktree state is already non-pristine. Respect existing edits
and do not revert unrelated work:

- `M orchestrator/state.json` (controller-owned; must remain untouched)
- `?? orchestrator/rounds/round-087/selection.md`

## Accepted Continuity That Remains Binding

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/orchestrator/rounds/round-087/selection.md`
  already fixes this round to roadmap item `6` only, limits the scope to one
  representative coverage and feasibility campaign, and requires explicit use
  of the accepted item-5 persistence tuple, ledger, and outcome vocabulary.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/orchestrator/roadmap.md`
  makes item `6` the next dependency-satisfied item and defines its
  completion notes as one representative classification campaign plus one
  bounded feasibility read only.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-strategic-roadmap.md`
  supplies the human strategic gate: representative local / non-local,
  retained-child / alias-bound, nested-`forall`, ambiguity, termination, and
  reconstruction-heavy coverage must be assessed before any architecture fork
  decision is lawful.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-capability-contract-and-evaluation-corpus.md`
  is the accepted item-1 capability contract. Its binding pressure for this
  round is the family matrix:
  positive `P2`, `P3`, `P4`, `P5`, `P6` and negative / fail-closed
  `N1`, `N2`, `N4`, `N5`, `N6`.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md`
  is the accepted item-2 audit. Its binding architectural read remains:
  `iso-recursive = keep`, `non-equi-recursive = keep`,
  `non-cyclic-graph = unknown`, and `no-fallback = keep`.
  Item `6` must use that pressure honestly, especially for
  `N4` / `N6`, without consuming the item-7 decision.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-mechanism-map.md`
  is the accepted item-3 mechanism map. It preserves exactly two bounded
  mechanism families:
  non-local alias-bound / base-like and same-lane retained-child, while
  leaving positive nested-`forall`, generalized ambiguity handling, and broad
  reconstruction proof unresolved.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-search-model.md`
  is the accepted item-4 search model. Its binding read remains:
  anchor-first discovery, one lawful owner / binder frame per admitted
  family, fixed target / consumer alignment, reject-side quantified-boundary
  handling, fail-closed ambiguity policy, and bounded acyclic termination
  discipline.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md`
  is the accepted item-5 contract. It freezes the persistence tuple, the
  phase-and-surface ledger, and the only lawful row-level outcome vocabulary:
  `stable visible persistence`,
  `admitted but not reconstruction-visible / blocker debt`, and
  `fail-closed rejection`.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  and
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/docs/plans/2026-03-24-automatic-iso-recursive-bound-var-target-same-lane-retained-child-next-cycle-decision-gate.md`
  keep the inherited explicit-only / iso-recursive / non-equi-recursive /
  non-cyclic-graph / no-second-interface / no-fallback boundary binding and
  preserve accepted predecessor packets as bounded evidence only.
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/Bugs.md`
  still carries open `BUG-2026-03-16-001`, but that replay / `InstBot` defect
  remains predecessor implementation context only. It does not create a retry
  obligation, does not reorder the roadmap, and does not authorize a code or
  replay repair inside item `6`.

## Files In Scope

### Create

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`

### Preserve Unchanged

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/orchestrator/state.json`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/orchestrator/roadmap.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/orchestrator/verification.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/orchestrator/retry-subloop.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/Bugs.md`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/src/`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/test/`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/src-public/`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/app/`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/mlf2.cabal`
- reviewer-owned history under earlier round directories

## Exact Selected Slice (Exactly One)

The only selected slice is:

run one bounded representative coverage and feasibility campaign for the
accepted automatic iso-recursive family matrix inside the inherited
architecture.

This slice is allowed to:

- build one representative coverage matrix from the accepted item-1 family
  contract, the item-3 mechanism map, the item-4 search model, and the item-5
  persistence contract;
- classify representative local / non-local, alias-bound / retained-child,
  nested-`forall`, ambiguity, binder-sensitive, termination-pressure, and
  reconstruction-heavy rows using accepted evidence only;
- state where the evidence currently supports positive persistence, blocker
  debt, or fail-closed rejection without inventing new families or new search
  behavior; and
- end with exactly one feasibility read:
  `broad generality`, `bounded subset only`, or
  `architectural dead end`.

This slice is not allowed to:

- redesign the mechanism map, search model, or reconstruction contract;
- authorize a new implementation slice, new executable path, or test campaign;
- revise the inherited explicit-only / iso-recursive / non-equi-recursive /
  non-cyclic-graph / no-fallback boundary;
- authorize equi-recursive reasoning, implicit unfolding, cyclic structural
  graphs, multi-SCC search, second interfaces, fallback behavior, or
  cross-family reopening; or
- make the item-7 architecture decision or treat the item-6 feasibility read
  as if it already decided the successor architecture.

If the coverage campaign starts depending on unaccepted replay repair, fresh
code-path evidence, hidden candidate ranking, or boundary revision to justify
its conclusion, record that dependence explicitly as blocker debt or later-item
ownership instead of solving it here.

## Sequential Tasks

### Task 1 - Freeze the item-6 docs-only coverage-campaign frame

- Create the canonical item-6 artifact at the path above.
- State explicitly that item `6` is docs-only, coverage-campaign-only, and
  `attempt-1` with `retry: null`.
- Reassert the inherited boundary unchanged:
  - explicit recursive annotations remain the current production baseline;
  - recursive meaning remains iso-recursive only;
  - no equi-recursive equality or implicit unfolding is authorized;
  - no cyclic structural graph encoding or multi-SCC search is authorized;
  - no second interface is authorized; and
  - no compatibility / convenience / default-path fallback widening is
    authorized.
- State explicitly that accepted items `1` through `5` plus accepted `N14`
  contribute bounded evidence only. They define the lawful family matrix,
  audit posture, mechanism vocabulary, search discipline, and persistence
  rubric, but they do not by themselves prove broad generality or pre-clear
  the final architecture choice.

### Task 2 - Build the representative coverage matrix from the controlling docs

- Pull the minimum representative rows directly from the accepted family
  matrix and controlling artifacts. The matrix must cover at least:
  - non-local alias-bound / base-like admitted family;
  - same-lane retained-child admitted family;
  - nested-`forall` / quantified-crossing pressure;
  - ambiguity / competing-candidate pressure;
  - binder-sensitive / owner-sensitive placement pressure;
  - termination-pressure under the inherited acyclic model; and
  - reconstruction-heavy output-surface pressure.
- For each row, require the artifact to name the controlling evidence source:
  item-1 family label, item-3 mechanism family, item-4 search rule or reject
  boundary, and item-5 persistence-ledger expectation.
- Keep the matrix representative rather than exhaustive. The campaign should
  not invent a second corpus, a second interface, or new packet folklore.
- Make the relationship between row and family explicit:
  if one row is representative of multiple pressures
  (for example binder-sensitive and reconstruction-heavy pressure inside the
  same-lane retained-child evidence), the artifact must say so rather than
  pretending it discovered a new independent family.

### Task 3 - Define the per-row classification rubric

- Require one consistent row schema for the campaign table or equivalent
  rubric. Each row must record:
  - representative family or pressure label;
  - recursive-shape anchor;
  - owner / binder frame;
  - target / consumer route;
  - quantified-boundary state;
  - the relevant item-5 phase-and-surface ledger read;
  - the accepted visible-output fact, if any;
  - the lawful item-5 outcome classification; and
  - the reason the current evidence supports that classification.
- Require the row rubric to distinguish carefully between:
  - evidence that the same lawful candidate survives the full ledger;
  - evidence that a candidate is admitted but loses recursive visibility; and
  - evidence that the case must fail closed because it violates ambiguity,
    ownership, quantified-boundary, or bounded-search rules.
- Prohibit row-level manual reinterpretation. If a row needs witness-only
  reasoning, replay-local rescue, or packet folklore that the accepted record
  did not already lift into the current contracts, the row must not be scored
  as positive persistence.

### Task 4 - Apply the item-5 outcome vocabulary to each representative row

- Require the artifact to classify the non-local alias-bound / base-like row
  honestly against the accepted visible read:
  `TBase (BaseTy "Int")` with `containsMu False` remains blocker debt unless
  the accepted record already justifies a stronger item-5-visible read.
- Require the artifact to classify the same-lane retained-child row against
  the full item-5 persistence contract, not merely against predecessor packet
  enthusiasm. It may count as `stable visible persistence` only if the
  accepted evidence actually preserves the same family, owner / binder frame,
  route, and recursive visibility across the whole ledger; otherwise the row
  must remain blocker debt.
- Keep nested-`forall` pressure honest:
  absent new accepted positive evidence, quantified crossing remains
  reject-side only and cannot be upgraded into positive `P5` success.
- Keep ambiguity pressure honest:
  competing-candidate situations remain `fail-closed rejection` under `N1`,
  not partial positive evidence.
- Keep binder-sensitive pressure honest:
  the artifact may credit accepted owner-sensitive placement evidence only to
  the extent the current row still preserves one lawful owner / binder story
  without route hopping or frame drift.
- Keep termination-pressure honest:
  classify whether the current accepted search model provides bounded success,
  bounded rejection, or unresolved blocker debt under `N6`, especially where
  `non-cyclic-graph = unknown` still limits how strong the current read may
  be.
- Keep reconstruction-heavy pressure honest:
  internal and public output visibility must agree, or the row cannot be
  counted as positive persistence.

### Task 5 - Derive one bounded item-6 feasibility read from the matrix

- End the artifact with exactly one aggregate feasibility read:
  `broad generality`, `bounded subset only`, or
  `architectural dead end`.
- Tie that aggregate read directly to the matrix results:
  - whether positive persistence extends beyond one narrow retained-child
    pocket;
  - whether non-local coverage remains blocker debt;
  - whether nested-`forall` remains reject-side only;
  - whether ambiguity and bounded-search pressure remain well-contained; and
  - whether reconstruction-visible output survives broadly enough to make
    `P6` credible beyond one bounded family.
- Do not blur this feasibility read with item `7`.
  The artifact may say why the matrix leaves architecture pressure open, but
  it may not itself choose the successor architecture or revise the live
  baseline.
- If the matrix shows mixed evidence with one bounded positive pocket and
  multiple blocker-debt / fail-closed rows, require the artifact to stop at
  `bounded subset only` rather than overselling generality.

### Task 6 - End with one bounded round result and the docs-only verification note

- End the artifact with one bounded item-6 result only:
  the representative coverage matrix is now classified under the inherited
  boundary, and the accumulated evidence supports one explicit feasibility
  read without consuming the item-7 decision.
- Cite the reviewer-facing expectations from
  `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-087/orchestrator/verification.md`:
  the round must span representative local / non-local, retained-child /
  alias-bound, nested-`forall`, ambiguity, termination, binder-sensitive, and
  reconstruction-heavy pressure rather than a convenience subset.
- State that this round is expected to change only docs and that no full
  `cabal build all && cabal test` gate is triggered unless the work escapes
  the authorized docs-only surface.
- Wherever the accepted record does not justify a stronger claim, require the
  artifact to fail closed by naming blocker debt or later-item ownership
  rather than broadening the round.
