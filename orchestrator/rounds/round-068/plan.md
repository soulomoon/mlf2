# Round 068 Plan (`N1` Post-`L2` Roadmap-Amendment Authority Gate)

## Objective

Execute only roadmap item `N1` and prepare one docs-only authority artifact at:
`docs/plans/2026-03-22-automatic-iso-recursive-post-l2-roadmap-amendment-authority-gate.md`.

This is the initial `N1` plan for `attempt-1` with `retry: null`. The round
must treat accepted `L1` and `L2` closure as binding continuity, keep the
repaired `URI-R2-C1` queue closed as predecessor evidence only, and execute
one bounded post-`L2` roadmap-amendment authority gate without silently
starting `N2` through `N7`, implementation, or verification for any future
slice.

Current planning read: `N1` should land a docs-only authority record that
reopens successor work only at planning scope, preserves the inherited
`explicit-only / non-equi-recursive / non-cyclic-graph / no-second-interface / no-fallback`
boundary unchanged, keeps the exhausted repaired `URI-R2-C1` queue closed, and
makes the preserved generic scheme-alias / base-like `baseTarget` route
admissible for later `N2` selection only. The artifact must not treat that
route as already selected, implementation-ready, or verification-ready, and it
must keep every broader route or alternate family blocked unless a later
accepted roadmap item explicitly reopens it.

`N1` is docs-only roadmap-authority work. It does not edit
`orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-001/roadmap.md`, does not edit `orchestrator/rounds/round-068/state-snapshot.json`, does not
edit `Bugs.md`, does not rewrite prior review history, and does not authorize
code changes.

## Locked Round Context

- Round id: `round-068`
- Roadmap item: `N1`
- Stage: `plan`
- Active attempt: `attempt-1`
- Retry state: `null`
- Fixed live subject: post-`L2` successor control plane for automatic
  iso-recursive inference
- Active branch: `codex/round-068-n1-post-l2-roadmap-amendment`
- Active worktree:
  `.worktrees/round-068`
- Stage mode: docs-only roadmap-amendment authority gate only
- Current round review feedback: none yet; this is a full `attempt-1` plan,
  not a retry delta

Current round worktree state is already non-pristine. Respect existing edits
and do not revert unrelated work:

- `?? orchestrator/rounds/round-068/selection.md`

## Accepted Continuity That Remains Binding

- `orchestrator/rounds/round-068/selection.md` already fixed this round to
  roadmap item `N1` only and explicitly forbids using this round to authorize
  `N2` through `N7`, implementation, verification, review-history rewrites,
  or bug-tracker changes.
- `orchestrator/rounds/round-068/state-snapshot.json` in the controller root fixes the live controller
  state at `active_round_id: "round-068"`, `stage: "plan"`,
  `current_task: "N1"`, `branch:
  "codex/round-068-n1-post-l2-roadmap-amendment"`, and `retry: null`.
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-001/roadmap.md` makes `N1` the first pending item in the refreshed
  successor control plane; every later item depends on authority that `N1`
  creates and therefore cannot run first.
- `tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
  preserves `N0 = YES` and `N1 = NO`: the repaired `URI-R2-C1` queue is
  closed, and no accepted roadmap amendment yet authorizes post-`L2` successor
  work.
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  remains the inherited baseline contract: explicit recursive annotations are
  supported; automatic recursive-type inference remains unresolved and
  disabled; the explicit-only / non-equi-recursive / non-cyclic-graph boundary
  remains mandatory.
- `docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md` finalized the
  repaired queue as fail-closed inside the inherited boundary: no fresh lawful
  exact successor slice remained after accepted predecessor ownership.
- `docs/plans/2026-03-21-uri-r2-c1-l2-post-l1-fail-closed-successor-decision-gate.md`
  finalized the only lawful bounded `L2` decision as `stop-blocked` and
  carried forward the preserved generic scheme-alias / base-like `baseTarget`
  route as blocked future context pending a separate accepted roadmap amendment
  plus a fresh later selection.
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-001/retry-subloop.md` allows retries for `N1`, but it also makes
  prior attempts immutable. This `attempt-1` plan must therefore create a new
  bounded authority artifact without rewriting any prior attempt or review
  artifact.
- `Bugs.md` currently carries
  `BUG-2026-03-16-001` as open continuity context, but that replay /
  `InstBot` defect does not widen `N1` into replay reopen, `MLF.Elab.Inst`,
  or any implementation slice. The bug tracker stays read-only in this round.

## File Map

### Modify

- `docs/plans/2026-03-22-automatic-iso-recursive-post-l2-roadmap-amendment-authority-gate.md`
  - Responsibility: record the `N1` docs-only authority attempt, name the
    authoritative post-`L2` reopening decision, preserve or explicitly revise
    the inherited boundary, state whether the preserved generic scheme-alias /
    base-like `baseTarget` route is blocked or admissible for later selection,
    and forbid implementation or verification from beginning until later
    accepted roadmap items explicitly authorize them.

### Read-Only Evidence

- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-001/roadmap.md`
- `orchestrator/rounds/round-068/state-snapshot.json`
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-001/verification.md`
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-001/retry-subloop.md`
- `tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
- `docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md`
- `docs/plans/2026-03-21-uri-r2-c1-l2-post-l1-fail-closed-successor-decision-gate.md`
- `Bugs.md`
- `orchestrator/rounds/round-068/selection.md`

### Preserve Unchanged

- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-001/roadmap.md`
- `orchestrator/rounds/round-068/state-snapshot.json`
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-001/retry-subloop.md`
- `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-001/verification.md`
- `orchestrator/rounds/round-068/selection.md`
- `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
- `docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md`
- `docs/plans/2026-03-21-uri-r2-c1-l2-post-l1-fail-closed-successor-decision-gate.md`
- `Bugs.md`
- `src/`
- `src-public/`
- `app/`
- `test/`
- `mlf2.cabal`

## Sequential Tasks

### Task 1 - Re-establish exact `N1` authority and stage boundary

- Write the future canonical `N1` artifact as `attempt-1` with `retry: null`.
- State explicitly that `N1` is a docs-only roadmap-amendment authority gate.
  It may reopen successor planning authority only; it may not execute `N2`,
  `N3`, `N4`, `N5`, `N6`, or `N7`.
- Preserve accepted `L1` / `L2` closure as binding predecessor continuity.
  The repaired `URI-R2-C1` queue remains closed predecessor evidence only and
  may not be silently reopened as a live subject.
- Preserve the inherited explicit-only / non-equi-recursive /
  non-cyclic-graph / no-second-interface / no-fallback boundary unchanged in
  the current planning read. If the artifact proposes any revision, it must be
  explicit, thesis-backed, and limited to roadmap authority only; it may not
  smuggle in implementation behavior.

### Task 2 - Consolidate the predecessor packet that constrains the amendment

- Use `selection.md`, `roadmap.md`, `state.json`, `retry-subloop.md`, the
  mechanism table, the baseline contract, the accepted `L1` / `L2` artifacts,
  and `Bugs.md` to reconstruct the exact post-`L2` continuity packet.
- Carry forward these fixed inputs:
  - the refreshed successor control plane is blocked until `N1` lands;
  - accepted `L1` already proved the repaired queue has no fresh lawful exact
    successor slice inside the inherited boundary;
  - accepted `L2` already finalized the repaired queue as `stop-blocked`;
  - the preserved generic scheme-alias / base-like `baseTarget` route is
    currently blocked pending a separate accepted roadmap amendment plus fresh
    selection; and
  - the open replay / `InstBot` bug remains out of scope for this docs-only
    authority round.
- Do not reinterpret accepted negative findings, replay history, or preserved
  broader continuity as if `N1` were allowed to resume the exhausted repaired
  queue.

### Task 3 - Write the canonical `N1` artifact as one bounded authority outcome

- Create
  `docs/plans/2026-03-22-automatic-iso-recursive-post-l2-roadmap-amendment-authority-gate.md`.
- The artifact must include:
  - the stage contract freeze for `round-068` / `N1` / `attempt-1` /
    `retry: null`;
  - the accepted `L1` / `L2` closure as binding continuity;
  - the inherited baseline boundary and whether it is preserved or explicitly
    revised;
  - the authoritative post-`L2` reopening decision; and
  - the precise status of the preserved generic scheme-alias / base-like
    `baseTarget` route.
- Current planning read: record exactly one authority outcome:
  - reopen the control plane only for later docs-first successor selection and
    safety work;
  - keep the repaired `URI-R2-C1` queue closed and non-reopenable under this
    artifact;
  - preserve the inherited boundary unchanged;
  - make the preserved generic scheme-alias / base-like `baseTarget` route
    admissible for later `N2` selection only; and
  - keep all other replay paths, fallback families, solver-wide routes, and
    code-changing slices blocked unless a later accepted roadmap item
    explicitly reopens them.
- State explicitly that `N2` must still choose exactly one thesis-backed next
  live subject inside the authority created by `N1`. `N1` does not perform
  that selection itself.

### Task 4 - Keep the non-authorization and immutability rules explicit

- State explicitly that `N1` does not authorize implementation, verification
  of any future slice, merge action, controller-state edits, roadmap edits,
  bug-tracker edits, or predecessor-history rewrites.
- State explicitly that `N1` does not reopen or select:
  - the exhausted repaired `URI-R2-C1` queue;
  - accepted `L1`;
  - accepted `L2`;
  - replay reopen;
  - `MLF.Elab.Inst` or `InstBot`;
  - `boundVarTarget`;
  - `boundTarget`;
  - `schemeBodyTarget`;
  - `src/MLF/Elab/Run/ResultType/View.hs`;
  - equi-recursive reasoning;
  - implicit unfolding;
  - cyclic structural graph encoding;
  - multi-SCC support;
  - cross-family search;
  - a second executable interface; or
  - any compatibility, convenience, or default-path fallback.
- State explicitly that the roadmap-update rule from
  `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-001/retry-subloop.md` applies only after an accepted round
  finalizes and merges. This `N1` attempt records authority in a docs artifact;
  it does not rewrite completed roadmap history.

### Task 5 - Run the docs-only verification required for the authority gate

Run the baseline docs/state checks required by `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-001/verification.md`:

- `git diff --check`
- `python3 -m json.tool orchestrator/rounds/round-068/state-snapshot.json >/dev/null`
- `rg -n '"contract_version": 2|"retry": null|"retry": \{' orchestrator/rounds/round-068/state-snapshot.json`
- `rg -n '^\d+\. \[(pending|in-progress|done)\]' orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-001/roadmap.md`
- `test -f docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
- `test -f docs/plans/2026-03-14-unannotated-iso-recursive-r5-research-stop-decision.md`
- `test -f docs/plans/2026-03-17-uri-r2-c1-r4-repair-decision-gate.md`
- `test -f docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md`
- `test -f docs/plans/2026-03-21-uri-r2-c1-l2-post-l1-fail-closed-successor-decision-gate.md`
- `test -f tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
- `test -f orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-001/retry-subloop.md`

Run `N1`-specific continuity checks:

- `rg -n '^1\\. \\[pending\\] Execute the `N1` post-`L2` roadmap-amendment authority gate for automatic iso-recursive inference' orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-001/roadmap.md`
- `rg -n 'N1 — Post-`L2` roadmap amendment authority|Draft a thesis-backed roadmap-amendment design note' tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
- `rg -n 'stop-blocked|separate roadmap amendment|fresh selection' docs/plans/2026-03-21-uri-r2-c1-l2-post-l1-fail-closed-successor-decision-gate.md`
- `rg -n 'Attempt: `attempt-1`|Retry state: `null`|admissible for later `N2` selection|does not authorize implementation or verification' docs/plans/2026-03-22-automatic-iso-recursive-post-l2-roadmap-amendment-authority-gate.md`

- `cabal build all && cabal test` is intentionally out of scope for this round
  if the diff remains docs-only and does not touch `src/`, `src-public/`,
  `app/`, `test/`, or `mlf2.cabal`. The implementation notes and reviewer
  record must state that skip reason explicitly rather than implying future
  code clearance.

## Reviewer Handoff

Reviewer should confirm that the implemented `N1` artifact:

- is explicitly framed as `round-068` / `N1` / `attempt-1` / `retry: null`;
- treats accepted `L1` / `L2` closure as binding predecessor continuity;
- records exactly one bounded post-`L2` authority decision;
- preserves the inherited boundary unchanged unless an explicit thesis-backed
  revision is stated;
- states whether the preserved generic scheme-alias / base-like `baseTarget`
  route is blocked or admissible for later selection;
- keeps implementation and verification blocked until later accepted roadmap
  items explicitly authorize them; and
- leaves `orchestrator/roadmaps/2026-03-22-00-automatic-iso-recursive-inference-post-l2-successor-roadmap/rev-001/roadmap.md`, `orchestrator/rounds/round-068/state-snapshot.json`, `Bugs.md`, and
  prior attempt/review history unchanged.
