# Round 124 Plan (`item-1` Bounded `P1` Freeze)

## Objective

Execute roadmap item `1` only:
freeze the bounded `P1` successor authority, exact packet, success bar, and
writable slice.

This round is `attempt-1` with `retry: null`.

## Writable Slice

This round may write only:

- `docs/plans/2026-03-28-p1-local-recursive-shape-successor-authority-success-bar-and-writable-slice-freeze.md`
- `orchestrator/rounds/round-124/*`

## Task List

1. Bind the direct predecessor authority chain from the baseline, capability
   contract, refreshed matrix, and accepted `C1` / `P2` successor gate.
2. Freeze one exact `P1` live packet and current read.
3. Freeze the exact item-2 success bar and writable slice.
4. Keep `C1`, the same-lane pocket, and `P5` out of scope.
