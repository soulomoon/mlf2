# P5 Polymorphism-Nested-Forall Explicit Boundary-Revision Roadmap

Roadmap family: `2026-04-07-00-p5-polymorphism-nested-forall-explicit-boundary-revision-roadmap`
Revision: `rev-002`
Base branch: `codex/automatic-recursive-type-inference`
Created: 2026-04-07

## Goal

Carry forward the same planning-only explicit boundary-revision family after
reopening the semantic status of nested-forall `mu` absorption, so the family
can continue honestly toward broader positive
`P5 polymorphism-nested-forall` support by:

- superseding the `rev-001` milestone-1 freeze only where it treated the
  accepted `round-151` reclassification of polymorphic-mediation `mu`
  absorption as settled correct predecessor truth;
- re-freezing the exact inherited boundary clauses and semantic questions now
  under live pressure before any broader `P5` ledger, comparison, or handoff;
- preserving historical predecessor artifacts unchanged while updating the
  controlling planning read for this family; and
- keeping `rev-002` planning-only while it decides whether broader positive
  `P5` support still fits the inherited boundary honestly or sustains one
  exact explicit boundary-revision consequence.

## Outcome Boundaries

- Preserve the inherited boundary unless a later accepted artifact revises it
  explicitly: explicit recursive annotations remain the production baseline,
  recursive meaning remains iso-recursive only,
  `non-equi-recursive = keep`,
  `non-cyclic-graph = unknown`,
  `no-fallback = keep`,
  no second interface is authorized,
  and no cyclic or multi-SCC widening is authorized.
- Carry forward `explicit boundary-revision candidate` as the accepted
  family-entry pressure classification only. It does not pre-decide which
  exact inherited boundary clauses are under pressure, whether the pressure is
  still best read as a candidate after fuller planning, or what later
  downstream enactment would look like.
- Preserve the accepted retained-child clear-boundary settlement from
  `round-197` as predecessor evidence only:
  `sameLaneAliasFrameClearBoundaryExpr` remains one bounded positive `P5`
  success on `runPipelineElab` / `runPipelineElabChecked`,
  while `nestedForallContrastExpr` remains fail-closed with
  `PhiTranslatabilityError`.
- Preserve the historical `round-151` artifact unchanged, but do not treat
  its docs-only reclassification of nested-forall `mu` absorption under
  polymorphic mediation as controlling settled truth in `rev-002`.
  The question whether `nestedForallContrastExpr`-class broader positive `P5`
  cases lawfully absorb `mu` or should preserve recursive structure across
  polymorphic mediation is reopened as live planning pressure that must be
  re-frozen explicitly.
- Keep `P2 non-local-propagation` unopened on the current ledger and preserve
  the exact `C1` packet as packet-specific predecessor truth only unless a
  later accepted planning artifact explicitly changes that scope.
- Keep the representative negative-family rows from accepted `round-192`
  closed as predecessor truth only:
  `N1 ambiguity-reject`,
  `N2 unsoundness-guard`, and
  `N6 termination-pressure` remain `fail-closed rejection`.
- `rev-002` is planning-only and docs/control-plane-only:
  no `src/`, `src-public/`, `app/`, `test/`, or `mlf2.cabal` changes are
  authorized in this revision.
- Treat `runPipelineElab`, `runPipelineElabChecked`, and the matching
  internal / public pipeline facades as the authoritative evidence surfaces
  for interpreting preserved and live support, even though this revision does
  not yet implement or re-run new broader support packets.

## Global Sequencing Rules

- `milestone-1` must complete before any broader positive `P5` ledger,
  current-boundary-versus-revision comparison, or downstream handoff. The
  first round of `rev-002` must extract from `milestone-1`.
- `rev-002` stays serial and planning-only. No parallel round extraction is
  pre-authorized in this revision.
- No round in `rev-002` may implement code, widen tests, or enact a concrete
  boundary revision. Any such work requires a later accepted roadmap revision
  or successor family after this planning family binds its result.
- Any candidate boundary-revision read in this family must distinguish
  preserved predecessor truth from reopened semantic pressure and from still-
  live broader positive `P5` support pressure.
- Do not reopen `P2`, the retained-child clear-boundary settlement, the
  accepted March 28 exact packet, or the representative negative-family rows
  as live debt unless a later accepted artifact explicitly reclassifies them.
- Historical `rev-001`, accepted `round-202`, and accepted `round-151` remain
  immutable predecessor evidence only. `rev-002` may supersede their
  controlling classification for this family, but it must not rewrite them in
  place.
- The family closes only when one exact downstream consequence is bound and
  every non-selected route is explicitly kept closed.

## Parallel Lanes

- `lane-main`: default serial lane for every `rev-002` milestone. This family
  remains intentionally single-threaded while the reopened semantic pressure
  is being re-frozen.

## Milestones

### 1. [done] Re-freeze the inherited boundary after reopening nested-forall polymorphic-mediation `mu` preservation semantics

- Milestone id: `milestone-1`
- Depends on: none
- Intent: publish one canonical docs-only authority artifact that supersedes
  the `rev-001` milestone-1 freeze only where it closed round-151
  polymorphic-mediation `mu` absorption as settled predecessor truth, and that
  re-freezes the exact inherited boundary clauses now under pressure, the
  exact reopened semantic question, the exact preserved predecessor truth that
  stays closed, the exact live broader positive `P5` subject, and the exact
  lawful family outcomes for the revised planning surface.
- Completion signal: an accepted docs-only authority artifact names the exact
  reopened semantic question, the exact inherited boundary clauses under
  pressure, the exact preserved predecessor truths, the exact live broader
  positive `P5` subject, the exact lawful family outcomes, and the exact
  no-go claims that keep `rev-002` non-implementing.
- Completion notes: accepted `round-203` finalized this milestone through
  `docs/plans/2026-04-07-p5-polymorphism-nested-forall-explicit-boundary-revision-family-round-151-polymorphic-mediation-mu-preservation-reclassification-and-inherited-boundary-refreeze.md`
  with authoritative review in
  `orchestrator/rounds/round-203/review-record.json`. That docs-only refreeze
  keeps accepted `round-202` as historical predecessor evidence only, but
  supersedes its controlling round-151 semantic classification by
  reclassifying the round-151 polymorphic-mediation `mu`-absorption story as
  `narrowed live semantic pressure` rather than settled predecessor truth.
  The accepted artifact freezes the live inherited-boundary surface as:
  `explicit recursive annotations remain the production baseline = live pressure`,
  `recursive meaning remains iso-recursive only = live interpretive pressure inside a preserved clause`,
  `non-equi-recursive = keep = preserved closed`,
  `non-cyclic-graph = unknown = preserved background`,
  `no-fallback = keep = preserved closed`, and
  `no second interface is authorized = preserved closed`.
  It preserves accepted `round-197` as one settled retained-child
  clear-boundary lane only, preserves `nestedForallContrastExpr` as current
  fail-closed predecessor evidence rather than whole-family closure, keeps
  `P2` unopened, keeps `N1 ambiguity-reject`, `N2 unsoundness-guard`, and
  `N6 termination-pressure` closed as predecessor truth only, fixes the live
  broader positive subject to nested-forall / quantified-crossing support
  beyond the one settled retained-child lane where polymorphic mediation may
  preserve or absorb recursive structure, and hands off only to
  `milestone-2` / `direction-2a-build-broader-positive-p5-ledger-under-revised-freeze`.
- Parallel lane: `lane-main`
- Coordination notes: keep accepted `round-202` as historical predecessor
  freeze rather than the controlling classification on round-151; keep
  `round-197` as one settled retained-child clear-boundary lane only; keep
  `round-151` historically intact but semantically reopened; and keep `P2`
  plus the representative negative-family rows unopened.

Accepted direction lineage:

- Direction id: `direction-1a-refreeze-inherited-boundary-after-reopening-polymorphic-mediation-semantics`
  Status: accepted in `round-203`.
  Outcome: published the canonical docs-only `rev-002` milestone-1 refreeze
  artifact, preserved all historical artifacts unchanged, reclassified the
  round-151 absorption story as narrowed live semantic pressure, froze the
  exact inherited-boundary clauses and preserved truths under that revised
  read, and handed the family forward only to the broader positive `P5`
  ledger milestone under the revised freeze.

### 2. [done] Build the broader positive `P5` planning ledger beyond the one settled retained-child lane under the revised freeze

- Milestone id: `milestone-2`
- Depends on: `milestone-1`
- Intent: use the accepted `rev-002` milestone-1 refreeze to identify the
  exact broader positive `P5 polymorphism-nested-forall` frontier beyond the
  one settled retained-child clear-boundary lane, distinguish preserved
  evidence from live pressure, and freeze the exact planning surface that the
  final downstream handoff must consume without reopening excluded neighbor
  families.
- Completion signal: an accepted docs-only planning ledger identifies one
  exact broader positive `P5` frontier, classifies what remains predecessor
  truth versus live pressure under the revised freeze, and fixes the exact
  surviving decision surface that the final downstream handoff must bind.
- Parallel lane: `lane-main`
- Coordination notes: treat accepted `round-203` and its milestone-1
  refreeze artifact as controlling. Only the exact live inherited-boundary
  clauses and semantic questions named there may remain open in this
  milestone. Keep the family planning-only; do not introduce new code or new
  representative tests, do not promote `nestedForallContrastExpr` into whole-
  family closure by itself, and do not reopen `P2` or the representative
  negative-family rows.
- Completion notes: accepted `round-204`, merged as base commit `9ee680c`
  (`Publish the milestone-2 broader-positive P5 ledger under the revised freeze`),
  finalized
  `direction-2a-build-broader-positive-p5-ledger-under-revised-freeze`
  through
  `docs/plans/2026-04-07-p5-polymorphism-nested-forall-explicit-boundary-revision-family-broader-positive-p5-ledger-under-the-revised-freeze.md`
  with authoritative review in
  `orchestrator/rounds/round-204/review-record.json`. That accepted broader
  ledger fixes the exact live frontier to broader positive nested-forall /
  quantified-crossing support beyond the one settled retained-child lane,
  preserves accepted `round-203` as the controlling milestone-1 refreeze,
  keeps `P2`, the March 28 exact packet, and the representative negative-
  family rows closed as predecessor truth or excluded material, and preserves
  `explicit boundary-revision candidate` as family-entry pressure only rather
  than a bound downstream consequence. `milestone-2` is therefore complete,
  and `rev-002` now moves directly to `milestone-3` for the one remaining
  downstream bind without opening a separate `direction-2b` extraction.

Accepted direction lineage:

- Direction id: `direction-2a-build-broader-positive-p5-ledger-under-revised-freeze`
  Status: accepted in `round-204`, merged as `9ee680c`.
  Outcome: published the canonical milestone-2 broader-ledger artifact, froze
  the exact broader positive `P5` frontier under the accepted `round-203`
  refreeze, classified preserved evidence versus still-live pressure
  honestly, and routed the family forward only to
  `milestone-3` / `direction-3a-bind-one-downstream-consequence`.

### 3. [pending] Bind one exact downstream consequence from the revised planning ledger

- Milestone id: `milestone-3`
- Depends on: `milestone-2`
- Intent: close the planning family by binding one exact downstream
  consequence from the accepted `milestone-1` / `milestone-2` planning record
  while keeping `rev-002` itself non-implementing.
- Completion signal: an accepted docs-only handoff names exactly one
  downstream consequence and records why the non-selected routes remain
  closed.
- Parallel lane: `lane-main`
- Coordination notes: the downstream consequence may be one later enactment /
  implementation family, one later bounded current-architecture continuation
  family, or an explicit no-further-action close if the accepted planning
  record truly collapses the pressure. It may not bind more than one next
  move, and it may not enact that move inside `rev-002`. Accepted
  `round-204` now fixes the broader frontier and preserved/live-pressure split
  for this family, so this milestone must consume that frozen ledger directly
  instead of reopening milestone-2 classification work.

Candidate directions:

- Direction id: `direction-3a-bind-one-downstream-consequence`
  Summary: publish the canonical planning-family final handoff that consumes
  the accepted `round-203` / `round-204` planning record, names one exact
  downstream consequence only, and keeps all non-selected routes closed.
  Why it matters now: `round-204` has already frozen the broader frontier
  under the revised milestone-1 surface, so the family now needs one exact
  bind on whether that surviving pressure routes to later enactment, bounded
  continuation, or explicit closure instead of another milestone-2 reread.
  Preconditions: `milestone-2`.
  Parallel hints: serial finalization only.
  Boundary notes: handoff-only; do not revise the inherited boundary or enact
  any downstream code, tests, or contract changes in this revision.
  Extraction notes: the final artifact must explain why every non-selected
  route stays closed, including any attempt to reopen `P2`, to treat the one
  settled retained-child lane as broad family closure, or to resurrect a
  separate `direction-2b` comparison packet after the accepted broader ledger.
