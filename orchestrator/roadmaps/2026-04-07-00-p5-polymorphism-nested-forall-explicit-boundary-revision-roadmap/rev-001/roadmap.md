# P5 Polymorphism-Nested-Forall Explicit Boundary-Revision Roadmap

Roadmap family: `2026-04-07-00-p5-polymorphism-nested-forall-explicit-boundary-revision-roadmap`
Revision: `rev-001`
Base branch: `codex/automatic-recursive-type-inference`
Created: 2026-04-07

## Goal

Carry forward the accepted `round-200` / `round-201` end-state
`explicit boundary-revision candidate` into one fresh planning-only family for
broader positive `P5 polymorphism-nested-forall` support beyond the one
settled retained-child clear-boundary lane by:

- freezing the exact inherited boundary clauses now under pressure rather than
  treating "boundary revision" as a vague family label;
- freezing the exact decision surface this family must use before any
  implementation, hardening, or concrete revision enactment;
- building an honest broader positive `P5` planning ledger that separates
  preserved predecessor truth from still-live broader support pressure; and
- ending `rev-001` with one exact downstream handoff only, while keeping this
  revision planning-only.

## Outcome Boundaries

- Preserve the inherited boundary unless a later accepted artifact revises it
  explicitly: explicit recursive annotations remain the production baseline,
  recursive meaning remains iso-recursive only,
  `non-equi-recursive = keep`,
  `non-cyclic-graph = unknown`,
  `no-fallback = keep`,
  no second interface is authorized,
  and no cyclic or multi-SCC widening is authorized.
- Carry forward `explicit boundary-revision candidate` as the accepted
  family-entry pressure classification only. It does not pre-decide which
  exact inherited boundary clauses are under pressure, whether the pressure is
  still best read as a candidate after fuller planning, or what later
  downstream enactment would look like.
- Preserve the accepted retained-child clear-boundary settlement from
  `round-197` as predecessor evidence only:
  `sameLaneAliasFrameClearBoundaryExpr` remains one bounded positive `P5`
  success on `runPipelineElab` / `runPipelineElabChecked`,
  while `nestedForallContrastExpr` remains fail-closed with
  `PhiTranslatabilityError`.
- Preserve the accepted `round-151` reclassification of nested-forall `mu`
  absorption under polymorphic mediation as known correct behavior, not as the
  live blocker to relitigate.
- Keep `P2 non-local-propagation` unopened on the current ledger and preserve
  the exact `C1` packet as packet-specific predecessor truth only unless a
  later accepted planning artifact explicitly changes that scope.
- Keep the representative negative-family rows from accepted `round-192`
  closed as predecessor truth only:
  `N1 ambiguity-reject`,
  `N2 unsoundness-guard`, and
  `N6 termination-pressure` remain `fail-closed rejection`.
- `rev-001` is planning-only and docs-only:
  no `src/`, `src-public/`, `app/`, `test/`, or `mlf2.cabal` changes are
  authorized in this revision.
- Treat `runPipelineElab`, `runPipelineElabChecked`, and the matching
  internal / public pipeline facades as the authoritative evidence surfaces
  for interpreting preserved and live support, even though this revision does
  not yet implement or re-run new broader support packets.

## Global Sequencing Rules

- `milestone-1` must complete before any broader positive `P5` ledger,
  current-boundary-versus-revision comparison, or downstream handoff. The
  first round of this family must extract from `milestone-1`.
- `rev-001` stays serial and planning-only. No parallel round extraction is
  pre-authorized in this revision.
- No round in `rev-001` may implement code, widen tests, or enact a concrete
  boundary revision. Any such work requires a later accepted roadmap revision
  or successor family after this planning family binds its result.
- Any candidate boundary-revision read in this family must distinguish exact
  inherited boundary clauses under pressure from the preserved predecessor
  truth that remains closed.
- Do not reopen `P2`, the retained-child clear-boundary settlement, the
  accepted March 28 exact packet, the accepted `round-151` reclassification,
  or the representative negative-family rows as live debt unless a later
  accepted artifact explicitly reclassifies them.
- The family closes only when one exact downstream consequence is bound and
  every non-selected route is explicitly kept closed.

## Parallel Lanes

- `lane-main`: default serial lane for every `rev-001` milestone. This family
  is planning-only and intentionally single-threaded while the exact inherited
  boundary under pressure is being frozen.

## Milestones

### 1. [done] Freeze the exact inherited boundary under pressure and the exact decision surface for this family

- Milestone id: `milestone-1`
- Depends on: none
- Intent: turn the accepted `round-200` / `round-201` handoff into one
  canonical planning surface that freezes the exact inherited boundary clauses
  now under pressure, the preserved predecessor evidence that remains closed,
  the exact broader positive `P5` subject for this family, and the exact
  decision outcomes this family may lawfully reach.
- Completion signal: an accepted docs-only authority artifact names the exact
  inherited boundary clauses under pressure, the exact preserved predecessor
  truths, the exact live broader positive `P5` subject, the exact lawful
  family outcomes, and the no-go claims that keep implementation and revision
  enactment blocked in `rev-001`.
- Completion notes: accepted `round-202` finalized this milestone through
  `docs/plans/2026-04-07-p5-polymorphism-nested-forall-explicit-boundary-revision-family-inherited-boundary-pressure-and-planning-only-decision-surface-freeze.md`
  with authoritative review in
  `orchestrator/rounds/round-202/review-record.json`. That docs-only freeze
  artifact carried `explicit boundary-revision candidate` forward as
  planning-only family-entry pressure, froze
  `explicit recursive annotations remain the production baseline` as the only
  inherited clause under live pressure, kept
  `recursive meaning remains iso-recursive only`,
  `non-equi-recursive = keep`,
  `no-fallback = keep`, and
  `no second interface is authorized` preserved closed, kept
  `non-cyclic-graph = unknown` as preserved background rather than reopened
  debt, preserved `sameLaneAliasFrameClearBoundaryExpr`,
  `nestedForallContrastExpr`,
  `P2` / exact `C1`,
  `round-151` polymorphic-mediation absorption, and
  `N1 ambiguity-reject` / `N2 unsoundness-guard` / `N6 termination-pressure`
  as closed predecessor truth, fixed the live subject to
  `broader positive P5 polymorphism-nested-forall support beyond the one settled retained-child clear-boundary lane`,
  and froze the only lawful later outcomes to the two milestone-2 planning
  reads plus the three milestone-3 downstream-consequence options. The
  accepted result hands off only to `milestone-2` /
  `direction-2a-build-broader-positive-p5-ledger`.
- Parallel lane: `lane-main`
- Coordination notes: keep `round-200` / `round-201` as binding predecessor
  authority, keep `round-197` as one settled retained-child clear-boundary
  lane only, keep `round-151` closed as known-correct predecessor truth, and
  keep `P2` plus the representative negative-family rows unopened.

Candidate directions:

- Direction id: `direction-1a-freeze-inherited-boundary-and-decision-surface`
  Summary: publish the first-round docs-only family authority artifact that
  freezes the exact inherited boundary under pressure, the exact decision
  surface, and the exact planning-only writable slice for this family.
  Why it matters now: the user-specified first move is not implementation but
  one exact freeze of the inherited boundary under pressure and the family
  decision surface; nothing later in the family is honest without that freeze.
  Preconditions: none.
  Parallel hints: serial only; extract this first and do not co-schedule it
  with any other direction.
  Boundary notes: docs-only; do not select a concrete revision, do not name an
  implementation slice, and do not reopen preserved predecessor evidence as
  live debt.
  Extraction notes: the first round should produce one canonical artifact under
  `docs/plans/` plus round bookkeeping only, and it should explicitly freeze
  the exact inherited boundary clauses under pressure and the exact decision
  outcomes this family may later choose among.

### 2. [pending] Build the broader positive `P5` planning ledger beyond the one settled retained-child lane

- Milestone id: `milestone-2`
- Depends on: `milestone-1`
- Intent: use the accepted milestone-1 freeze to identify the exact broader
  positive `P5 polymorphism-nested-forall` frontier beyond the one settled
  retained-child clear-boundary lane, but only as pressure on the explicit
  recursive-annotation production baseline; distinguish preserved evidence
  from live pressure; and compare whether that broader frontier still admits
  an honest bounded current-architecture continuation or instead sustains one
  exact explicit boundary-revision candidate.
- Completion signal: an accepted docs-only planning ledger identifies one
  exact broader positive `P5` frontier, classifies what remains predecessor
  truth versus live pressure, and records one honest planning-level read on
  current-boundary continuation versus explicit revision candidacy.
- Parallel lane: `lane-main`
- Coordination notes: treat the accepted `round-202` freeze as controlling:
  only `explicit recursive annotations remain the production baseline` is live
  inherited-boundary pressure in this milestone;
  `recursive meaning remains iso-recursive only`,
  `non-equi-recursive = keep`,
  `no-fallback = keep`, and
  `no second interface is authorized` stay preserved closed; and
  `non-cyclic-graph = unknown` stays preserved background rather than reopened
  debt. Keep the family planning-only; do not introduce new code or new
  representative tests, do not promote `nestedForallContrastExpr` into whole-
  family closure by itself, and do not reopen `P2` or the representative
  negative-family rows.

Candidate directions:

- Direction id: `direction-2a-build-broader-positive-p5-ledger`
  Summary: identify the exact broader positive `P5` frontier beyond the one
  settled retained-child clear-boundary lane that still pressures the explicit
  recursive-annotation baseline, and classify each preserved input as
  predecessor truth, excluded material, or still-live broader support
  pressure.
  Why it matters now: the family needs an exact live frontier instead of an
  abstract "broader support" label before any current-boundary-versus-
  revision comparison can be honest.
  Preconditions: `milestone-1`.
  Parallel hints: serial only.
  Boundary notes: docs-only inventory and classification; do not revise code,
  tests, or the inherited boundary itself.
  Extraction notes: keep the focus on broader positive nested-forall /
  quantified-crossing support beyond the settled retained-child lane; explain
  specifically why any surviving pressure bears on the explicit-recursive-
  annotation baseline only; and state explicitly why preserved `P2`,
  negative-family, absorbed-`mu`, iso-recursive, non-equi-recursive,
  no-fallback, no-second-interface, and `non-cyclic-graph = unknown`
  predecessor context do not become live debt again here.

- Direction id: `direction-2b-compare-current-boundary-vs-explicit-revision`
  Summary: use the milestone-2 planning ledger to decide whether one exact
  bounded current-architecture continuation still survives honestly or whether
  the broader planning record instead supports one exact explicit
  boundary-revision candidate.
  Why it matters now: the family cannot bind a downstream consequence until it
  compares the broader frontier against the frozen decision surface from
  `milestone-1`.
  Preconditions: `milestone-1`; a stable broader positive `P5` ledger from
  `direction-2a` or an equivalent accepted artifact.
  Parallel hints: serial only; do not extract until the broader frontier is
  frozen.
  Boundary notes: remain planning-only and decision-surface-only; do not enact
  a revision or authorize implementation.
  Extraction notes: the outcome must name one surviving planning-level read
  only rather than leaving a menu of equal options open.

### 3. [pending] Bind one exact downstream consequence from the planning ledger

- Milestone id: `milestone-3`
- Depends on: `milestone-2`
- Intent: close the planning family by binding one exact downstream
  consequence from the accepted milestone-1 / milestone-2 planning record
  while keeping `rev-001` itself non-implementing.
- Completion signal: an accepted docs-only handoff names exactly one
  downstream consequence and records why the non-selected routes remain closed.
- Parallel lane: `lane-main`
- Coordination notes: the downstream consequence may be one later enactment /
  implementation family, one later bounded current-architecture continuation
  family, or an explicit no-further-action close if the accepted planning
  record truly collapses the pressure. It may not bind more than one next move,
  and it may not enact that move inside `rev-001`.

Candidate directions:

- Direction id: `direction-3a-bind-one-downstream-consequence`
  Summary: publish the canonical planning-family final handoff that names one
  exact downstream consequence only and keeps all non-selected routes closed.
  Why it matters now: without one exact handoff, this planning-only family
  would end in ambiguity about whether the next move is enactment, bounded
  continuation, or closure.
  Preconditions: `milestone-2`.
  Parallel hints: serial finalization only.
  Boundary notes: handoff-only; do not revise the inherited boundary or enact
  any downstream code, tests, or contract changes in this revision.
  Extraction notes: the final artifact must explain why every non-selected
  route stays closed, including any attempt to reopen `P2` or to treat the one
  settled retained-child lane as broad family closure.
