# Global Non-Cyclic-Graph Settlement And Automatic Iso-Recursive Inference Roadmap

## Context

- This roadmap revision succeeds accepted revision `rev-003`, whose
  authoritative execution record now extends through
  `orchestrator/rounds/round-112`.
- Accepted rev-003 item `4` published this successor revision only for
  bounded same-pocket post-amendment settlement after accepted rev-003 item
  `3` validated bounded recursive carry-through on the authoritative public
  surface for the exact same selected packet.
- The live controller resolves the active roadmap revision through
  `orchestrator/state.json` fields `roadmap_id`, `roadmap_revision`, and
  `roadmap_dir`; the top-level `orchestrator/roadmap.md`,
  `orchestrator/retry-subloop.md`, and `orchestrator/verification.md` files
  are pointer stubs only.
- Accepted rounds `round-109` through `round-112` remain authoritative
  predecessor evidence only. Their accepted truth must not be reinterpreted
  into broad rollout or repo-level capability.
- The inherited baseline remains unchanged unless a later accepted item in
  this revision explicitly changes it: explicit recursive annotations remain
  the production baseline, recursive meaning remains iso-recursive only,
  `non-equi-recursive = keep` remains binding, `no-fallback = keep`
  remains binding, no second interface is authorized, and no multi-SCC
  search is authorized.
- Rev-004 is narrower than rev-003. It does not reopen the handoff
  implementation question. It exists only because accepted rev-003 item `3`
  now records a new exact-pocket public-output result while still-live
  same-family evidence surfaces remain pre-amendment and should be settled
  through new bounded artifacts rather than silent predecessor rewrites.
- This revision therefore remains planning/docs-first and exact-pocket-only.
  No code-changing round is authorized by default in rev-004.

## Status Legend

- `pending`
- `in-progress`
- `done`

## Items

1. [done] Freeze the exact rev-004 post-amendment settlement surface and successor boundary
   Depends on:
   Completion notes: complete when an accepted docs-only artifact freezes the
   exact same-pocket post-amendment settlement scope for rev-004 only:
   the same selected rows `C2` / `C5` / `C7`, the same exact packet and
   tuple, one exact current-result surface anchored in accepted rev-003
   item `3`, one exact future writable docs boundary for new settlement
   artifacts only, and explicit preservation of predecessor-artifact
   immutability. This item may not authorize code changes, second packets,
   second interfaces, or broad capability narration.

2. [done] Publish one bounded same-pocket post-amendment settlement ledger
   Depends on: item 1
   Completion notes: complete when an accepted docs-only artifact records the
   current exact-pocket post-amendment read on new bounded settlement
   surfaces only, without silently rewriting accepted predecessor artifacts.
   The artifact must preserve the exact-pocket-only boundary and must not
   widen into broad same-family or repo-level success claims.

3. [done] Validate the bounded post-amendment settlement on the same exact pocket surface
   Depends on: item 2
   Completion notes: complete when an accepted validation artifact proves the
   new rev-004 settlement surfaces stay aligned with the accepted rev-003
   item-3 result, stay exact-pocket-only, preserve predecessor immutability,
   and do not silently widen into broader family or rollout claims.

4. [pending] Decide the post-settlement same-family handoff
   Depends on: items 1, 2, 3
   Completion notes: complete when an accepted aggregate artifact records
   exactly one lawful follow-on outcome for rev-004:
   either stop after the bounded post-amendment settlement, or publish one
   later same-family successor revision for further bounded same-pocket
   settlement. This gate must remain exact-pocket-only and must preserve
   `iso-recursive = keep`, `non-equi-recursive = keep`, and
   `no-fallback = keep`.
