# Global `non-cyclic-graph` Same-Lane Retained-Child Public-Output Continuity Architecture-Pressure Audit Target And Evaluation Surface Bind

Date: 2026-03-26
Round: `round-107`
Roadmap item: `item-4`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: one docs-only rev-002 item-4 bind for the exact same-lane
retained-child / public-output continuity pocket already selected in
accepted item `2` and contract-governed in accepted item `3`
Artifact kind: canonical docs-only rev-002 item-4 audit-target /
evaluation-surface bind

## Stage Contract Freeze

This artifact implements only roadmap item `4` for `attempt-1` with
`retry: null`.

It consumes accepted item `1`, item `2`, and item `3` truth as authoritative
predecessor authority and freezes exactly this much, and no more:

- rev-002 remains planning-only and architecture-only;
- accepted items `1` through `3` remain binding and are not relitigated;
- rev-001 items `6` through `8` remain blocked;
- exactly one already-selected reopened subject remains in scope;
- only accepted rows `C2`, `C5`, and `C7` are bound as the live audit
  target for that one selected pocket;
- the exact packet, exact module set, exact command set, and exact six
  review-visible output surfaces are frozen for later inspection only; and
- later work may inspect this bind only within one bounded same-family
  successor-lane question.

This artifact does not:

- reopen subject selection or introduce a second selected subject;
- promote non-local `C1` into a coequal audit target;
- treat `C5` or `C7` as second packets, second routes, second interfaces, or
  separate successor lanes;
- choose which lawful item-3 outcome bar is true;
- decide whether item `5` opens one bounded architecture-amendment lane or
  stops without opening it;
- authorize edits to `orchestrator/state.json`, roadmap files, retry files,
  verification files, `Bugs.md`, source code, tests, or Cabal files; or
- authorize multi-SCC search, second interfaces, fallback widening,
  equi-recursive semantics, implicit unfolding, production implementation,
  hardening, or broad capability claims.

The inherited boundary remains unchanged:

- explicit recursive annotations remain the production baseline;
- recursive meaning remains iso-recursive only;
- `non-equi-recursive = keep` remains binding;
- `no-fallback = keep` remains binding;
- equi-recursive semantics and implicit unfolding remain blocked; and
- no live cyclic-structure implementation is authorized by this bind.

## Accepted Predecessor Authority

| Source | Binding truth carried forward |
| --- | --- |
| `orchestrator/rounds/round-107/selection.md` and `orchestrator/rounds/round-107/plan.md` | This round is item-4-only, docs-only, planning-only, and architecture-only. The artifact must bind the exact rows, packet, modules, commands, and six review-visible output surfaces for one selected pocket only. |
| `orchestrator/rounds/round-106/review-record.json` | Accepted item `3` is authoritative. Exactly three lawful item-3 outcome bars remain binding, but item `4` still owns the exact audit bind and item `5` still owns the later open-versus-stop decision. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-reopened-revision-authority-and-candidate-boundary-freeze.md` | Rev-002 remains planning-only and architecture-only, exactly one candidate boundary was frozen, and rev-001 items `6` through `8` remain blocked. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-reopened-subject-selection.md` | The only selected subject is the exact same-lane retained-child / public-output continuity pocket already carried by accepted rows `C2`, `C5`, and `C7`, with non-local `C1` retained only as contrast context. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-safety-and-acceptance-contract.md` | Item `3` defined the three lawful outcome bars and explicitly deferred the exact audit-target / evaluation-surface bind to item `4` and the lane-open-or-stop decision to item `5`. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md` and `docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md` | `C2`, `C5`, and `C7` remain continuity faces of one exact same-lane pocket only; `C5` is not a second packet and `C7` is not a second public path. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md` | The accepted matrix still has zero `stable visible persistence` rows, keeps the strongest admitted pressure on the exact `C2` / `C5` / `C7` pocket only, and keeps non-local `C1` contrast bounded rather than selected. |
| `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md` | The accepted same-lane ledger froze the exact tuple, the exact helper-visible/internal versus authoritative-public split, and the six review-visible output surfaces that item `4` must preserve without widening. |
| `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-path-audit.md` | The accepted path audit fixed `checkedAuthoritative` as the first exact public-output break for this same pocket, with `termClosed` and `typeCheck termClosed` as same-pocket dependencies. |
| `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-collapse-clear-or-confirm.md` | The bounded `Pipeline.hs` / `TermClosure.hs` root-handoff slice contains no alternate recursive whole-packet authoritative result for this pocket. |
| `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md` | The exact-pocket end-to-end ledger remains reviewer-visible and still classifies the selected pocket below success without widening the subject. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-keep-vs-reopen-decision-gate.md` and `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md` | The inherited global boundary remains explicit-only in production, iso-recursive only, non-equi-recursive, no-fallback, no second interface, and not yet an accepted cyclic-structure implementation lane. |

These accepted artifacts are predecessor authority only. None of them
already chooses an item-3 outcome, opens an item-5 lane, or authorizes code
changes.

## Exact Selected Pocket Bound Here

This artifact binds exactly one pocket and no other:

`the exact same-lane retained-child / public-output continuity pocket already carried by accepted rows C2, C5, and C7, with non-local C1 retained only as bounded contrast context`

### Exact Accepted Rows

| Row | Exact same-pocket role that remains binding | What item `4` freezes for later inspection | Bound that must remain closed |
| --- | --- | --- | --- |
| `C2` | primary same-lane retained-child row for the selected pocket | family / anchor / owner-local frame / route identity plus helper-visible same-lane reconstruction continuity on the exact packet | not a second packet, not a broader `P3` claim, not a family-wide success story |
| `C5` | same exact `C2` pocket through the owner / binder-sensitive placement lens only | same local `TypeRef` lane, same owner-local frame, and same nested-`forall` fail-closed guard context | not a second owner-local packet, not a second route, not a broad `P4` theory |
| `C7` | same exact `C2` / `C5` pocket through the public-output continuity lens only | authoritative public-output split, exact public entrypoints, and reviewer-visible continuity-loss surface for the same packet | not a second interface, not a second public path, not a broad `P6` success claim |

Non-local `C1` is not part of the live item-4 audit target. If mentioned at
all, it remains contrast-only and may not become a second selected subject,
second packet, second command surface, or second accepted packet.

### Exact Packet

The artifact freezes exactly this packet and no variant:

```haskell
ELet "k" (ELamAnn "x" recursiveAnn (EVar "x"))
  (ELet "u" (EApp (ELam "y" (EVar "y")) (EVar "k")) (EVar "u"))
```

where
`recursiveAnn = STMu "a" (STArrow (STVar "a") (STBase "Int"))`.

The same frozen tuple remains binding:

| Continuity field | Exact selected value |
| --- | --- |
| Family | same-lane retained-child |
| Recursive-shape anchor | `boundVarTargetRoot` |
| Owner / binder frame | one owner-local retained-child frame |
| Route | `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC` |
| Quantified-boundary status | clear-boundary only: `boundHasForallFrom` is false and `not hasForall` holds |

`C5` and `C7` remain rereads of this same exact packet only. They are not
second packets, second routes, or second interfaces.

## Exact Module Set

Later item-4 inspection may use only this exact source/test module set for
the selected pocket:

| Module | Why it is in-scope for the bind | Pocket face it serves |
| --- | --- | --- |
| `src/MLF/Elab/Run/ResultType/Fallback.hs` | owns `boundVarTargetRoot`, `boundHasForallFrom`, `sameLaneLocalRetainedChildTarget`, `keepTargetFinal`, and the same-lane retained-child route preservation | `C2` primary route identity and `C5` guard / placement continuity |
| `src/MLF/Elab/Run/ResultType.hs` | owns dispatch into `computeResultTypeFallback` for the same-pocket helper-visible route | `C2` helper-visible reconstruction continuity |
| `src/MLF/Elab/Run/Pipeline.hs` | owns `runPipelineElabWith`, `termClosed`, `checkedAuthoritative`, and the exact authoritative public-output commitment | `C7` public-output continuity surface |
| `src/MLF/Elab/Run.hs`, `src/MLF/Elab/Pipeline.hs`, `src-public/MLF/Pipeline.hs` | fix the single public-entrypoint re-export chain and keep a second interface blocked | `C7` no-second-interface continuity |
| `src/MLF/Elab/TermClosure.hs` | remains in-scope only because accepted item `3` proved the bounded `Pipeline.hs` / `TermClosure.hs` root-handoff slice contains no alternate recursive whole-packet authoritative result | `C7` accepted blocker-proof contrast only |
| `test/PipelineSpec.hs` | owns the exact same-lane harness and the focused exact-pocket tests already accepted as review-visible evidence | `C2` / `C5` / `C7` exact-pocket replay and test anchors |

No other module, interface, executable path, or search space is part of the
item-4 bind.

## Exact Command Set

This artifact freezes only the following command surfaces for later
inspection of the selected pocket:

| Command | Exact purpose bound to the selected pocket |
| --- | --- |
| `cabal repl mlf2-test --repl-options=-ignore-dot-ghci` | launch the exact-pocket replay environment already accepted for the same packet only |
| `:module + *PipelineSpec` | inside that REPL, reuse the exact same-lane harness anchored in `test/PipelineSpec.hs` for the frozen packet only; no alternate harness and no second packet |
| `cabal test mlf2-test --test-show-details=direct --test-options='--match "keeps retained-child fallback recursive through a same-lane local TypeRef root"'` | `C2` same-lane retained-child helper-visible recursion continuity |
| `cabal test mlf2-test --test-show-details=direct --test-options='--match "keeps retained-child lookup bounded to the same local TypeRef lane"'` | `C5` owner-local / binder-sensitive same-lane lookup bound |
| `cabal test mlf2-test --test-show-details=direct --test-options='--match "keeps retained-child fallback fail-closed when the same wrapper crosses a nested forall boundary"'` | `C5` fail-closed quantified-boundary guard for the same pocket |
| `cabal test mlf2-test --test-show-details=direct --test-options='--match "same-lane retained-child exact packet clears Phase 6 elaboration"'` | exact same-pocket elaboration continuity before the public-output break |
| `cabal test mlf2-test --test-show-details=direct --test-options='--match "same-lane retained-child exact packet authoritative public output stays forall identity"'` | `C7` authoritative public-output continuity fact for the same packet |
| `cabal test mlf2-test --test-show-details=direct --test-options='--match "same-lane retained-child exact edge 3 authoritative instantiation"'` | same-pocket authoritative instantiation continuity already accepted in the exact-pocket chain |
| `rg -n 'boundVarTargetRoot =|boundHasForallFrom start0 =|sameLaneLocalRetainedChildTarget =|keepTargetFinal =|case sameLaneLocalRetainedChildTarget of|not hasForall' src/MLF/Elab/Run/ResultType/Fallback.hs` | source-anchor inspection for the same-lane route, guard, and owner-local lookup rules tied to `C2` / `C5` |
| `rg -n 'computeResultTypeFallback|runPipelineElab =|runPipelineElabChecked =|runPipelineElabWith|termClosed|checkedAuthoritative =|typeCheck termClosed' src/MLF/Elab/Run/ResultType.hs src/MLF/Elab/Run/Pipeline.hs` | source-anchor inspection for helper-visible reconstruction and authoritative public-output commitment |
| `rg -n 'runPipelineElab|runPipelineElabChecked|runPipelineElabWith' src/MLF/Elab/Run.hs src/MLF/Elab/Pipeline.hs src-public/MLF/Pipeline.hs` | prove that the selected pocket still has one public-entrypoint chain and no second interface |
| `rg -n 'same-lane retained-child exact packet|same-lane local TypeRef root|same local TypeRef lane|nested forall boundary|edge 3 authoritative instantiation' test/PipelineSpec.hs` | prove the replay/test anchors are the already-accepted exact-pocket anchors only |

These commands are review surfaces only. They do not authorize code changes,
new harnesses, a second packet, broader search, or production claims.

## Exact Six Review-Visible Output Surfaces

This artifact freezes exactly these six review-visible surfaces, matching the
accepted same-lane ledger vocabulary and no broader surface set:

| Surface | Exact same-pocket read later work may inspect | Primary accepted anchors |
| --- | --- | --- |
| Solver admission state | same family, same `boundVarTargetRoot` anchor, same owner-local frame, same route, same clear-boundary-only status | accepted item `2`; `Fallback.hs` same-lane route anchors |
| Elaboration handoff / result state | the exact packet still clears elaboration before the public-output break | accepted predecessor same-lane Phase-6 clearance; focused exact-pocket elaboration test |
| Reification / reconstruction state | the helper-visible same-lane route still reconstructs a `TMu`-bearing internal type term for the same packet | `test/PipelineSpec.hs`; `computeResultTypeFallback`; accepted path audit |
| Internal output surface | helper-visible/internal output remains recursive and review-visible as `TMu ...` with `containsMu True` | same-lane replay output and accepted same-lane ledger |
| Public output surface | both authoritative public entrypoints remain `Right (TForall "a" Nothing (TVar "a"))` for the same packet | `runPipelineElab`; `runPipelineElabChecked`; `checkedAuthoritative` anchor |
| Reviewer-visible evidence trail | reviewers can point to one exact tuple and one exact internal/public split without witness-only rescue, replay folklore, or alternate interfaces | accepted same-lane case ledger, path audit, blocker proof, and end-to-end revalidation |

The item-4 bind keeps these surfaces explicit, review-visible, and
same-pocket-only. It does not add a seventh surface, a second interface, a
second packet, or a family-wide evidence trail.

## Explicitly Blocked Scope

The following remain explicitly blocked in this round:

- any change to accepted item-1 truth, accepted item-2 truth, accepted
  item-3 truth, accepted rev-001 truth, or the rule that rev-001 items
  `6` through `8` stay blocked;
- any reopening of subject selection, any introduction of a second subject,
  or any promotion of `C1` into a coequal audit target;
- any treatment of `C5` or `C7` as second packets, second routes, second
  interfaces, or separate successor lanes;
- any choice of one item-3 outcome bar as the current result;
- any decision about whether item `5` opens one bounded
  architecture-amendment lane or stops;
- any widening into broad family search, multi-SCC search, second-interface
  work, fallback widening, equi-recursive semantics, implicit unfolding,
  production implementation, hardening, or repo-level capability claims;
- any creation or edit outside this artifact path; and
- any commit.

If later narration starts selecting an item-3 outcome, reopening subject
selection, naming a second packet, implying that code work is now authorized,
or narrating a lane-open-or-stop decision, it has already widened beyond
item `4` and must fail closed.

## Later-Item Handoff Ownership

Later rev-002 work must treat this artifact as bind authority only:

- item `3` remains the only authority for the three lawful outcome bars, and
  this artifact does not choose among them;
- item `4` owns only the exact audit-target / evaluation-surface bind
  frozen here for the one selected pocket;
- item `5` still owns the one lawful decision on whether to open one
  bounded architecture-amendment lane or stop without opening it; and
- no file outside this artifact path is authorized output for this round's
  item-4 implementation.

This round therefore remains docs-only, planning-only, and
architecture-only. The bind defined here is not implementation clearance,
not a selected item-3 outcome, and not an item-5 architecture-amendment
decision.
