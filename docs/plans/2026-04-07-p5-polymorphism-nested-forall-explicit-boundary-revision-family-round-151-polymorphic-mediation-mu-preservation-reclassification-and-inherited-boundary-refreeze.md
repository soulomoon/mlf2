# P5 Polymorphism-Nested-Forall Explicit Boundary-Revision Family Round-151 Polymorphic-Mediation Mu-Preservation Reclassification And Inherited Boundary Refreeze

Date: 2026-04-07
Round: `round-203`
Milestone: `milestone-1`
Direction: `direction-1a-refreeze-inherited-boundary-after-reopening-polymorphic-mediation-semantics`
Extracted item: `refreeze-inherited-boundary-after-reopening-polymorphic-mediation-semantics`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: one docs-only milestone-1 inherited-boundary refreeze after reopening the round-151 polymorphic-mediation semantic read
Artifact kind: canonical milestone-1 round-151 semantic reclassification and inherited-boundary refreeze

## Stage Contract Freeze

This artifact implements only `round-203` / `milestone-1` /
`direction-1a-refreeze-inherited-boundary-after-reopening-polymorphic-mediation-semantics` /
`refreeze-inherited-boundary-after-reopening-polymorphic-mediation-semantics`
for `attempt-1` with `retry: null`.

`rev-002` remains docs-only, serial, planning-only, and milestone-1-only. The
job of this artifact is to supersede the `rev-001` / `round-202` controlling
freeze only where that predecessor freeze treated the accepted `round-151`
polymorphic-mediation `mu`-absorption wording as settled predecessor truth.
Everything else from the accepted predecessor chain stays historical evidence
unless this artifact states otherwise explicitly.

The inherited boundary remains the controlling production baseline in this
round:
explicit recursive annotations remain the production baseline,
recursive meaning remains iso-recursive only,
`non-equi-recursive = keep`,
`non-cyclic-graph = unknown`,
`no-fallback = keep`,
and no second interface is authorized.

This artifact does not build the broader milestone-2 ledger, does not compare
current-boundary continuation against explicit revision, does not bind a
milestone-3 handoff, and does not authorize code, tests, Cabal changes, or a
concrete boundary revision.

## Accepted Predecessor Authority Ledger

| Source | Binding read carried into this refreeze |
| --- | --- |
| `orchestrator/rounds/round-203/selection.md`; `orchestrator/state.json`; `orchestrator/roadmaps/2026-04-07-00-p5-polymorphism-nested-forall-explicit-boundary-revision-roadmap/rev-002/{roadmap,verification,retry-subloop}.md` | The active control plane fixes `round-203` as the first `rev-002` milestone-1 move, keeps the round docs-only and planning-only, and requires one canonical refreeze before any broader positive `P5` ledger or downstream handoff can continue honestly. |
| `docs/plans/2026-04-07-p5-polymorphism-nested-forall-explicit-boundary-revision-family-inherited-boundary-pressure-and-planning-only-decision-surface-freeze.md`; `orchestrator/rounds/round-202/review-record.json` | Accepted `round-202` remains the immediate historical predecessor freeze for `rev-001`, but `rev-002` supersedes it on one exact point only: `round-151` no longer counts as settled controlling predecessor truth for polymorphic-mediation `mu` absorption. The `round-202` artifact otherwise remains preserved historical evidence and is not rewritten in place. |
| `docs/plans/2026-04-07-post-item-7-explicit-boundary-revision-candidate-final-handoff-to-one-planning-only-p5-polymorphism-nested-forall-boundary-revision-family.md`; `orchestrator/rounds/round-201/review-record.json`; `docs/plans/2026-04-07-post-item-7-refreshed-repo-level-readiness-and-architecture-decision-from-the-updated-p5-vs-p2-ledger-and-preserved-negative-family-settlements.md`; `orchestrator/rounds/round-200/review-record.json` | Accepted `round-201` and `round-200` still carry the family-entry pressure read `explicit boundary-revision candidate` only. They open exactly one planning-only family for broader positive `P5 polymorphism-nested-forall` pressure beyond the one settled retained-child lane, but they do not select a concrete revision, an implementation slice, or a downstream enactment. |
| `docs/plans/2026-04-06-post-item-7-p5-post-implementation-settlement-surface-and-exact-repo-impact-read.md`; `orchestrator/rounds/round-197/review-record.json`; `test/Research/P5ClearBoundarySpec.hs` | Accepted `round-197` remains the settled retained-child clear-boundary predecessor lane: `sameLaneAliasFrameClearBoundaryExpr` stays a bounded positive success on `runPipelineElab` / `runPipelineElabChecked`, while `nestedForallContrastExpr` still fails closed at those entrypoints with `PhiTranslatabilityError`. That pair remains authoritative evidence about the current surface, not a broad family closure and not a concrete revision. |
| `docs/plans/2026-04-05-general-automatic-iso-recursive-full-inference-positive-family-aggregate-classification.md`; `orchestrator/rounds/round-191/review-record.json`; `orchestrator/rounds/round-181/review-record.json`; `docs/plans/2026-04-05-general-automatic-iso-recursive-full-inference-negative-family-and-termination-pressure-aggregate-classification.md`; `orchestrator/rounds/round-192/review-record.json` | Accepted `round-191` / `round-181` keep `P2` packet-bounded to the exact `C1` packet only, and accepted `round-192` keeps `N1 ambiguity-reject`, `N2 unsoundness-guard`, and `N6 termination-pressure` closed as fail-closed predecessor truth only. None of that material is reopened as live debt by this refreeze. |
| `orchestrator/rounds/round-151/review.md`; `orchestrator/rounds/round-151/review-record.json`; `implementation_notes.md` | Accepted `round-151` remains a docs-only historical reclassification that changed wording rather than behavior. Its text and approval record stay preserved unchanged, but `rev-002` no longer treats the wording "Nested-forall-mediated recursive types: mu is absorbed during constraint solving through polymorphic mediation" as controlling settled family truth. Instead, that wording becomes historical evidence for one narrowed live milestone-1 semantic question. |

## Exact Reopened Semantic Question

`rev-002` reclassifies the round-151 polymorphic-mediation read as:

`narrowed live semantic pressure`, not `settled predecessor truth`.

The exact reopened question is:

For broader positive `P5 polymorphism-nested-forall` cases beyond the one
settled retained-child clear-boundary lane, when recursive structure crosses a
nested-`forall` boundary through polymorphic mediation
(witnessed concretely by `nestedForallContrastExpr`-class pressure and
motivated historically by `let rec f = id f` in `implementation_notes.md`),
should the inherited iso-recursive boundary preserve recursive structure on the
authoritative pipeline-facing interpretation, or is round-151-style `mu`
absorption still the honest semantic read?

This refreeze does not answer that question. It freezes only the status of the
question itself:

- the round-151 absorption story is not restored as settled predecessor truth;
- the round-151 absorption story is not rejected outright either;
- the live read is narrower than "all nested-forall recursion" because it is
  limited to broader positive nested-forall / quantified-crossing pressure
  beyond the one settled retained-child clear-boundary lane.

## Exact Inherited Boundary Clauses Under Pressure

| Inherited boundary clause | Milestone-1 classification | Why this exact classification is frozen now |
| --- | --- | --- |
| `explicit recursive annotations remain the production baseline` | `live pressure` | The active family still asks whether broader positive `P5` support beyond the one settled retained-child lane can stay honest under the inherited explicit baseline. The clause is preserved, but it is now the exact baseline that later milestones must compare against the reopened semantic pressure. |
| `recursive meaning remains iso-recursive only` | `live interpretive pressure inside a preserved clause` | `rev-002` does not reopen equi-recursive reasoning, but it does reopen how the preserved iso-recursive boundary should read recursive structure when polymorphic mediation crosses a nested-`forall` boundary. The clause remains inherited truth while its application to the live broader positive subject is now under exact planning pressure. |
| `non-equi-recursive = keep` | `preserved closed` | Nothing in the accepted `rev-002` setup authorizes equi-recursive reinterpretation, implicit unfolding, or semantic equality widening as the way to read the reopened question. |
| `non-cyclic-graph = unknown` | `preserved unknown but not reopened` | The clause stays inherited background context only. `rev-002` is about the reopened polymorphic-mediation semantic read, not a new non-cyclic-graph decision. |
| `no-fallback = keep` | `preserved closed` | The refreeze does not authorize fallback widening to force a broader positive read. Fail-closed behavior remains binding while the planning family studies the reopened question honestly. |
| `no second interface is authorized` | `preserved closed` | The authoritative evidence surfaces remain `runPipelineElab`, `runPipelineElabChecked`, and the matching internal/public pipeline facades only. This family does not reopen interface proliferation. |

## Preserved Predecessor Truth That Stays Closed

- Accepted `round-202` remains the historical predecessor freeze for `rev-001`; only its controlling semantic treatment of round-151 is superseded here.
- Accepted `round-197` remains one settled retained-child clear-boundary lane only; `sameLaneAliasFrameClearBoundaryExpr` is still bounded predecessor success, not broader family closure.
- `nestedForallContrastExpr` remains current fail-closed predecessor evidence at the authoritative pipeline entrypoints with `PhiTranslatabilityError`; that observed fail-closed state is evidence for the live subject, not by itself a final closure of the broader family question.
- Accepted `round-191` / `round-181` keep `P2` packet-bounded to the exact `C1` packet only and do not become live debt again here.
- Accepted `round-192` keeps `N1 ambiguity-reject`, `N2 unsoundness-guard`, and `N6 termination-pressure` closed as fail-closed predecessor truth only.
- The historical text of accepted `round-151` and the current `implementation_notes.md` stays preserved unchanged as predecessor wording only; `rev-002` changes the controlling family read, not the historical artifacts themselves.

## Exact Live Broader Positive P5 Subject

The exact live broader positive subject is:

`broader positive nested-forall / quantified-crossing support beyond the one settled retained-child clear-boundary lane, specifically where polymorphic mediation may decide whether recursive structure survives or disappears on the authoritative pipeline-facing read`

This subject is broader than the retained-child lane because it names pressure
beyond `sameLaneAliasFrameClearBoundaryExpr`.

This subject is narrower than reopening the whole repository because it does
not reopen `P2`, the representative negative-family rows, the March 28 exact
packet, cyclic widening, fallback widening, or a second interface. It keeps
the family on one exact inherited-boundary pressure point only: the semantic
status of recursive-structure preservation versus `mu` absorption across
nested-`forall` polymorphic mediation.

## Exact Planning-Only Decision Surface

Milestone-1 binds only this planning-level freeze:

- controlling status of the round-151 absorption story:
  `narrowed live semantic pressure`
- controlling preserved truths:
  retained-child clear-boundary success stays settled,
  `P2` stays unopened,
  representative negative-family rows stay closed,
  and the inherited explicit-only / iso-recursive / non-equi-recursive /
  non-cyclic-graph / no-fallback / no-second-interface boundary remains the
  active baseline

Later milestones remain limited to the following lawful outcomes:

| Later milestone | Exact lawful outcomes frozen here | Milestone-1 read |
| --- | --- | --- |
| `milestone-2` | `bounded current-boundary continuation still survives honestly`; `exact explicit boundary-revision candidate remains strongest` | Milestone-2 may compare only against the revised milestone-1 freeze from this artifact. It may not quietly revert to the round-202 read that treated round-151 as settled truth. |
| `milestone-3` | `one later bounded current-architecture continuation family`; `one later enactment / implementation family`; `one explicit no-further-action close` | Milestone-3 may bind exactly one downstream consequence only after milestone-2 completes under this revised freeze. |

This artifact therefore re-freezes the family decision surface without
choosing a continuation, a revision, or a closeout.

## Rev-002 No-Go Claims

- No `src/`, `src-public/`, `app/`, `test/`, or `mlf2.cabal` changes are authorized in `rev-002`.
- No new representative tests or code-bearing validation slices are authorized in this round.
- No concrete boundary revision, implementation slice, or verification rerun is authorized here.
- No milestone-2 broader ledger, milestone-2 comparison result, or milestone-3 handoff is authored here.
- No silent restoration of the round-151 `mu`-absorption story to settled predecessor truth is authorized.
- No silent rejection of the round-151 historical artifact is authorized; the artifact stays preserved while the controlling read changes.
- No reopening of `P2`, the representative negative-family rows, the retained-child clear-boundary lane, or the March 28 exact packet is authorized.
- No cyclic widening, multi-SCC widening, equi-recursive reinterpretation, fallback widening, or second-interface widening is authorized.
