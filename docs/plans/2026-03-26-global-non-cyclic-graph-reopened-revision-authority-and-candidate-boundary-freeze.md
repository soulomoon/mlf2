# Global `non-cyclic-graph` Reopened Revision Authority And Candidate Boundary Freeze

Date: 2026-03-26
Round: `round-104`
Roadmap item: `item-1`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: one docs-only rev-002 authority freeze and planning-only
candidate-boundary freeze for the reopened global `non-cyclic-graph`
question
Artifact kind: canonical docs-only rev-002 item-1 freeze

## Stage Contract Freeze

This artifact implements only roadmap item `1` for `attempt-1` with
`retry: null`.

It consumes accepted `round-103` as authoritative predecessor truth and
freezes exactly this much, and no more:

- rev-002 is planning-only and architecture-only;
- the accepted rev-001 reopen result remains binding and is not relitigated;
- completed rev-001 item truth remains binding;
- rev-001 items `6` through `8` remain blocked; and
- exactly one planning-only candidate boundary is frozen for later rev-002
  items.

This artifact does not:

- select the exact live reopened subject inside that candidate boundary;
- define the safety or acceptance contract for the reopened subject;
- bind the exact audit target, rows, packets, modules, or output surfaces;
- decide whether one bounded successor lane opens or whether the revision
  stops;
- authorize code changes, test changes, `mlf2.cabal` changes, or edits to
  `orchestrator/state.json`, roadmap contracts, retry contracts,
  verification contracts, or `Bugs.md`; or
- authorize production implementation, hardening, or repo-level capability
  claims.

The inherited production boundary also remains unchanged in this item:

- explicit recursive annotations remain the production baseline;
- recursive meaning remains iso-recursive only;
- equi-recursive semantics and implicit unfolding remain blocked;
- cyclic structural graphs are not authorized as live implementation work;
- multi-SCC search remains blocked;
- second interfaces remain blocked; and
- fallback widening remains blocked.

## Authoritative Predecessor Truth

The accepted predecessor chain now binding on rev-002 item `1` is:

| Source | Binding truth carried forward |
| --- | --- |
| `orchestrator/rounds/round-103/review-record.json` | The accepted rev-001 item-5 final outcome is `global-non-cyclic-graph-decision-gate-reopens-non-cyclic-graph-revision-question`. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-keep-vs-reopen-decision-gate.md` | The accepted global gate preserved `iso-recursive = keep`, `non-equi-recursive = keep`, and `no-fallback = keep`, but withdrew the direct handoff from rev-001 item `5` into rev-001 items `6` through `8`. |
| `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md` | The inherited baseline remains explicit-only, iso-recursive, and structurally acyclic, with no equi-recursive semantics, no cyclic graph encoding, and no fallback widening. |
| `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-capability-contract-and-evaluation-corpus.md` and `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md` | Later repo-level success still requires representative coverage across `P1` through `P6` and bounded or fail-closed handling for `N1` through `N6`, while only `stable visible persistence` counts as positive `P6` success. |
| `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md` | The inherited architecture classification set remains `iso-recursive = keep`, `non-equi-recursive = keep`, `non-cyclic-graph = unknown`, and `no-fallback = keep`. |
| `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`, `docs/plans/2026-03-26-global-non-cyclic-graph-settlement-contract-and-unresolved-family-evidence-ledger.md`, and `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md` | The accepted representative read remains `bounded subset only`, the matrix still has zero `stable visible persistence` rows, `C1`, `C2`, `C5`, and `C7` remain blocker debt, and `N4` remains boundary pressure only rather than an already accepted reopen result. |
| `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architecture-decision-and-successor-plan-choice.md` | Before the later global reopen, the strongest accepted bounded continuation inside the unchanged architecture was the same-lane retained-child continuity route, while `non-cyclic-graph` stayed the only inherited boundary classified `unknown`. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md` and `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md` | The strongest admitted retained-child / public-output continuity pressure still comes from one exact same-lane pocket carried by `C2`, `C5`, and `C7`, while the exact-pocket decision still concluded only `blocker debt remains within the current architecture` for that bounded pocket. Non-local `C1` continuity remains admitted contrast only. |

These accepted artifacts are predecessor evidence only. They do not already
authorize cyclic structure, multi-SCC search, second interfaces, fallback
widening, implementation, or a repo-level capability claim.

## Frozen Rev-002 Authority

The authoritative meaning of the rev-002 reopen is now frozen as follows:

1. Accepted rev-001 item `5` reopened only the inherited
   `non-cyclic-graph` revision question.
2. That accepted reopen did not itself select a replacement architecture,
   prove that cyclic structure is required, or re-enable the blocked rev-001
   implementation chain.
3. Rev-002 therefore begins from one bounded planning question only:
   whether the strongest admitted retained-child / public-output continuity
   pressure warrants evaluating one bounded same-family successor lane beyond
   the inherited acyclic boundary.
4. The completed rev-001 chain remains binding evidence for why that
   planning question exists, but it remains bounded evidence only.

This freeze preserves completed rev-001 truth and explicitly keeps rev-001
items `6` through `8` blocked. Nothing in this artifact resumes that
withdrawn handoff.

## One Frozen Candidate Boundary

Exactly one planning-only candidate boundary is frozen for later rev-002
items:

`whether one bounded single-component cyclic-structure successor lane is
needed for the strongest admitted retained-child / public-output continuity
pressure, with non-local C1 continuity available only as contrast context`

This boundary is intentionally narrow:

- it is one candidate boundary, not multiple candidate lanes;
- it is bounded to the strongest admitted same-family retained-child /
  public-output continuity pressure already carried by the accepted
  `C2` / `C5` / `C7` chain;
- `C5` remains the same exact `C2` pocket rather than a second packet;
- `C1` remains contrast context only and does not become a parallel reopened
  subject; and
- `single-component` is contrast against broader cyclic or multi-component
  search, not an authorization of live multi-SCC work.

This boundary does not decide:

- that the bounded successor lane is actually required;
- that the bounded successor lane is actually sufficient;
- that any live cyclic-structure implementation is now lawful; or
- that the reopened revision will end by opening the lane rather than
  stopping.

## Explicit Blocked Work And Non-Goals

The following remain explicitly blocked after this item-1 freeze:

- equi-recursive semantics;
- implicit unfolding;
- multi-SCC search;
- second interfaces;
- fallback widening;
- production implementation;
- hardening;
- repo-level capability claims; and
- any rewrite of completed rev-001 truth or accepted predecessor history.

The following decisions also remain owned by later rev-002 items, not by this
artifact:

- item `2`: select the exact live reopened subject inside the frozen
  candidate boundary;
- item `3`: define the safety and acceptance contract for that selected
  subject;
- item `4`: bind the exact audit target and evaluation surface; and
- item `5`: decide whether to open one bounded successor lane or stop.

## Later-Item Handoff Ownership

Later rev-002 items must treat this artifact as the boundary contract for the
reopened revision:

- item `2` may narrow only within the single frozen candidate boundary above;
- item `3` may define only the safety bar for that later-selected subject;
- item `4` may bind only the exact audit target for that later-selected
  subject; and
- item `5` may record only one outcome: open one bounded same-family
  successor lane, or stop without opening it.

No later item may reinterpret this freeze into clearance for broader cyclic
search, multi-SCC search, second interfaces, fallback behavior, production
implementation, hardening, or repo-level success claims unless a later
accepted artifact explicitly changes the live boundary.
