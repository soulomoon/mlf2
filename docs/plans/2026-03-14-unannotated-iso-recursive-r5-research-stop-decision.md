# Unannotated Iso-Recursive `R5` Research-Stop Decision

Date: 2026-03-14  
Round: 010  
Roadmap item: 5. Final decision for `URI-R2-C1`  
Subject: `URI-R2-C1`  
Decision outcome: research-stop

## Metadata And Inherited Inputs

This item-5 document records the final bounded direction for `URI-R2-C1` after the accepted `R4` result `not-yet-go`.

Authoritative inherited evidence inputs cited directly for this decision:

- accepted `R0` / `ARI-C1` baseline from the completed automatic-recursive-inference chain, including `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md` and `docs/plans/2026-03-14-automatic-recursive-inference-item5-handoff-decision.md`;
- `docs/superpowers/specs/2026-03-14-unannotated-iso-recursive-roadmap-design.md`;
- `docs/plans/2026-03-14-unannotated-iso-recursive-r1-gap-map.md`;
- `docs/plans/2026-03-14-unannotated-iso-recursive-r2-candidate-subset-selection.md`;
- `docs/plans/2026-03-14-unannotated-iso-recursive-r3-inference-obligation-contract.md`;
- `docs/plans/2026-03-14-unannotated-iso-recursive-r4-feasibility-decision.md`;
- `docs/plans/2026-03-14-automatic-recursive-inference-invariant-audit.md` as the authoritative inherited invariant audit;
- `orchestrator/rounds/round-010/selection.md` as the current round-selection authority.

## Decision Statement

Roadmap item 5 resolves to **research-stop** for `URI-R2-C1`.

Reason:

- accepted `R4` concluded `not-yet-go` for `URI-R2-C1`;
- no inherited contradictory evidence inside the fixed boundary overrules that accepted result; and
- the absence of such contradictory evidence is itself the reason to keep the stop decision rather than convert the track into implementation clearance.

This decision is final for the current bounded item-5 stage and remains fail-closed.

## Evidence Chain (`R0` -> `R5`)

1. `R0` / `ARI-C1` from the completed automatic-recursive-inference chain fixed the accepted bounded baseline: annotation-anchored recursive-shape handling only, with explicit-only / non-equi-recursive / non-cyclic-graph constraints still mandatory.
2. `R1` (`docs/plans/2026-03-14-unannotated-iso-recursive-r1-gap-map.md`) named the finite delta from `ARI-C1` to bounded unannotated inference and made the missing anchor-supplied information explicit instead of treating the target as a vague extension.
3. `R2` (`docs/plans/2026-03-14-unannotated-iso-recursive-r2-candidate-subset-selection.md`) selected exactly one bounded subset, `URI-R2-C1`, and deferred or rejected broader alternatives rather than widening the active subject.
4. `R3` (`docs/plans/2026-03-14-unannotated-iso-recursive-r3-inference-obligation-contract.md`) fixed the obligation contract `URI-R3-O1` through `URI-R3-O5` for that one chosen subset and preserved the authoritative inherited invariant audit as the controlling invariant source.
5. `R4` (`docs/plans/2026-03-14-unannotated-iso-recursive-r4-feasibility-decision.md`) judged the inherited docs-only evidence for `URI-R2-C1` and recorded `not-yet-go`, not `feasible-continue`.
6. `R5` therefore executes the terminal item-5 decision only: because `R4` did not clear the bounded subject, this stage records explicit research-stop rather than collapsing the approved `R1` -> `R5` ladder into an implementation-handoff reinterpretation.

## Unresolved Obligations That Force The Stop

The controlling reason for research-stop is that the inherited `URI-R3-O1` through `URI-R3-O5` contract was not positively cleared inside the fixed boundary for `URI-R2-C1`.

- `URI-R3-O4` remains the decisive inherited blocker: accepted evidence still lacks provenance-stable unannotated replay authority for one root/cluster that survives generalization, reconstruction, reification, and witness replay without manufactured authority or late repair.
- `URI-R3-O5` remains unresolved because there is still no docs-only uniqueness clearance showing one uniquely admissible unannotated local root/cluster without heuristic ranking, competing roots, or widening-dependent choice.
- `URI-R3-O1`, `URI-R3-O2`, and `URI-R3-O3` remain blocked by the absence of prototype-free positive evidence that bounded unannotated admission can satisfy structural acyclicity, single-family owner stability, and constructor-directed occurs-check/termination obligations without leaning on explicit anchors.

These gaps remain bounded to `URI-R2-C1` only. They do not authorize widening to broader unannotated recursive inference, multi-SCC search, cross-family ownership exploration, implicit unfolding, cyclic graphs, or any other redesign outside the accepted boundary.

## Fixed Boundary (Unchanged And Mandatory)

This decision preserves the fixed boundary without weakening it:

- `single-SCC` obligation-level recursion only;
- `single-binder-family` ownership only;
- no cross-family SCC linking;
- non-equi-recursive semantics only;
- non-cyclic structural graph only;
- no default-on widening;
- no prototype-backed reinterpretation of the accepted evidence chain.

The authoritative inherited invariant audit remains authoritative for acyclicity, binding-tree discipline, occurs-check/termination, reconstruction/reification/witness replay, and principality-risk boundaries.

## Explicit Non-Handoff

This round does **not** authorize:

- implementation planning for the unannotated target;
- production implementation work;
- first-touch module selection;
- test-authoring work;
- any conversion of `not-yet-go` into implementation-handoff clearance.

The document records a stop decision only. It is not an implementation-handoff spec.

## Future Research Re-Entry Requirements

Any later roadmap that wants to revisit handoff must first provide bounded evidence, not an implementation plan.

Minimum re-entry evidence requirements:

- authoritative prototype-free evidence for one provenance-stable unannotated root/cluster inside `URI-R2-C1`;
- bounded uniqueness evidence that clears `URI-R3-O5` without heuristic ranking;
- bounded positive evidence that `URI-R3-O1` through `URI-R3-O3` can be satisfied without implicit unfolding, cyclic graphs, or widened ownership/search.

Additional re-entry rule:

- any future revisit must remain bounded to `URI-R2-C1` unless a separate reviewed roadmap explicitly selects a different subject and boundary.

## Continuity

Completed rounds `001` through `009`, the approved successor design, and the predecessor recursive-types packet are cited as inherited evidence only and are not rewritten.

Continuity statement:

- the completed automatic-recursive-inference chain remains authoritative for accepted `R0` / `ARI-C1`;
- the approved successor design remains the staging authority for `R1` through `R5`;
- predecessor packet history and prior round artifacts remain historical authority and are not reopened or rewritten here.

## Stage Discipline

- this document executes the item-5 final decision only;
- this document does not reopen `R1`, reselect `R2`, rewrite `R3`, or rerun `R4`;
- this document does not convert `not-yet-go` into implementation clearance;
- this document does not authorize production implementation work.
