# `N11` Exact Bounded Target Bind For The Selected `boundVarTarget` Planning Subject

Date: 2026-03-24
Round: `round-078`
Roadmap item: `N11`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: accepted `N9 = boundVarTarget-planning-subject-selected` plus
accepted `N10 = boundVarTarget-safety-acceptance-contract-established`
Artifact kind: canonical docs-only exact-target bind

## Stage Contract Freeze

This artifact implements only roadmap item `N11` for `attempt-1` with
`retry: null`.

`N11` is docs-only exact-target-bind work inside accepted
`N9 = boundVarTarget-planning-subject-selected` and accepted
`N10 = boundVarTarget-safety-acceptance-contract-established`. It binds
exactly one bounded target and exactly one downstream same-lane selection use
inside the selected retained-child / nested-`forall` / binding-structure
`boundVarTarget` lane.

This artifact does not authorize `N12`, `N13`, or `N14`. It does not
authorize implementation, verification, merge action, roadmap edits,
`orchestrator/state.json` edits, bug-tracker edits, or
predecessor-history rewrites.

The inherited boundary remains fixed and unchanged:

- explicit-only recursive baseline;
- non-equi-recursive semantics;
- non-cyclic-graph structural encoding;
- no second executable interface; and
- no compatibility, convenience, or default-path fallback widening.

## Accepted `L1` / `L2` / `N1` Through `N10` Continuity And Exact Packet

Only accepted predecessor continuity is carried forward here:

1. `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
   remains the inherited baseline contract: explicit recursive annotations are
   supported, automatic recursive-type inference remains unresolved and
   disabled, and the explicit-only / non-equi-recursive /
   non-cyclic-graph / no-second-interface / no-fallback boundary remains
   mandatory.
2. `docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md` remains the
   authoritative `L1` fail-closed record. Inside the repaired `URI-R2-C1`
   queue and the inherited boundary, it proved that no fresh lawful exact
   successor slice remained there.
3. `docs/plans/2026-03-21-uri-r2-c1-l2-post-l1-fail-closed-successor-decision-gate.md`
   remains the authoritative `L2` closeout. It finalized that repaired queue
   as `stop-blocked` and required separate later authority before any broader
   preserved route could become live work.
4. `docs/plans/2026-03-22-automatic-iso-recursive-post-l2-roadmap-amendment-authority-gate.md`
   remains the authoritative `N1` record. Its one bounded outcome,
   `reopen-planning-only`, reopened only a docs-first planning lane and did
   not authorize implementation or verification.
5. `docs/plans/2026-03-22-automatic-iso-recursive-next-live-subject-selection.md`
   remains the earlier `N2` selection record for the completed first reopened
   lane only. The preserved generic scheme-alias / base-like `baseTarget`
   subject is predecessor evidence only here and may not be silently carried
   forward as fresh authority for the selected `boundVarTarget` lane.
6. `docs/plans/2026-03-22-automatic-iso-recursive-base-target-safety-acceptance-contract.md`
   remains the earlier `N3` contract for the completed first reopened
   `baseTarget` lane only. It is predecessor evidence only here and does not
   replace the fresh subject-specific contract required after accepted `N9`.
7. `docs/plans/2026-03-22-automatic-iso-recursive-base-target-exact-target-bind.md`,
   `docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-bounded-implementation-slice.md`,
   `docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-bounded-verification-gate.md`,
   and
   `docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-next-cycle-decision-gate.md`
   remain the accepted `N4` / `N5` / `N6` / `N7` packet for the exact non-local
   `baseTarget -> baseC` lane only. That exact accepted packet is predecessor
   evidence only here and does not authorize exact-target, implementation, or
   verification carry-forward for `boundVarTarget`.
8. `docs/plans/2026-03-22-automatic-iso-recursive-post-n7-roadmap-amendment-authority-gate.md`
   remains the authoritative `N8` record. Its one bounded outcome,
   `reopen-planning-only-successor-lane`, reopened exactly one fresh
   planning-only successor lane and still did not choose a subject, bind a
   target, authorize implementation, or authorize verification.
9. `docs/plans/2026-03-18-uri-r2-c1-e1-next-target-bind.md`,
   `docs/plans/2026-03-18-uri-r2-c1-e2-bounded-implementation-slice.md`,
   `docs/plans/2026-03-18-uri-r2-c1-e3-bounded-verification-gate.md`, and
   `docs/plans/2026-03-18-uri-r2-c1-e4-next-cycle-decision-gate.md`
   remain the accepted repaired-queue retained-child packet only. They are
   predecessor evidence only here and do not automatically authorize this fresh
   post-`N8` lane.
10. `tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
    still records that the earlier reopened lane already has predecessor-only
    `N4` / `N5` / `N6` evidence, while the fresh post-`N8` successor lane
    still has no accepted exact target after accepted `N10`.
11. `docs/plans/2026-03-23-automatic-iso-recursive-bound-var-target-next-live-subject-selection.md`
    remains the authoritative `N9` record. It selects exactly one planning
    subject only: the retained-child / nested-`forall` / binding-structure
    `boundVarTarget` route. It explicitly leaves exact-bind, implementation,
    and verification work blocked.
12. `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/rounds/round-076/review-record.json`
    remains the acceptance proof that `N9` finalized as `accepted + finalize`
    with `final_outcome = "boundVarTarget-planning-subject-selected"`.
13. `docs/plans/2026-03-23-automatic-iso-recursive-bound-var-target-safety-acceptance-contract.md`
    remains the authoritative `N10` record. Any lawful `N11` bind must satisfy
    its reviewer-visible acceptance criteria: name the concrete retained-child
    ownership and target-selection anchors (`boundVarTargetRoot`,
    `boundHasForallFrom`, `boundVarTarget`, `keepTargetFinal`, `targetC`, and
    the retained-child `PipelineSpec` evidence), state fail-closed
    nested-`forall` / nested-owner / nested scheme-root conditions, keep
    `schemeBodyTarget` as neighboring boundary context only, and explicitly
    exclude every unchanged blocked route.
14. `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/rounds/round-077/review-record.json`
    remains the acceptance proof that `N10` finalized as `accepted + finalize`
    with `final_outcome =
    "boundVarTarget-safety-acceptance-contract-established"`.
15. `/Users/ares/.codex/worktrees/d432/mlf4/Bugs.md` remains read-only
    predecessor context only. Open `BUG-2026-03-16-001` keeps replay /
    `MLF.Elab.Inst` / `InstBot` pressure visible, but it does not reopen replay
    or authorize a different live subject in this round.

The exact packet constrained by this artifact is therefore narrow:

- the repaired `URI-R2-C1` queue stays closed predecessor evidence only;
- the earlier `baseTarget` lane, including the earlier `N2` selection and
  earlier `N3` contract, stays predecessor evidence only;
- the exact accepted non-local `baseTarget -> baseC` packet stays predecessor
  evidence only;
- the accepted repaired-queue retained-child packet stays predecessor evidence
  only;
- accepted `N10` supplies exact-target criteria only, not implementation or
  verification clearance; and
- the only live packet frozen here is the same-lane local `TypeRef`
  retained-child `boundVarTarget -> targetC` bind.

## Thesis Basis For This Exact Bind

The thesis-backed narrowing for this exact bind comes from four section anchors
in `papers/these-finale-english.txt`:

1. `3.3.3.1 Grafting` says that graphic-type transformations must account for
   binding structure, including how free and bound variables move inside a
   larger owner. Repo-local implication: the exact bind must stay centered on
   one retained-child ownership packet inside one bounded owner frame.
2. `5.1 Why rigid quantification?` says the `MLF` instance relation is adapted
   to the richer binding structure of graphic types. Repo-local implication:
   the exact bind must remain inside one retained-child / binding-structure
   family rather than broadening into cross-family search.
3. `8.3.1 Inlining bounds` says inverse translation is lawful only when the
   bound can be rebuilt unambiguously, the binder is a structural ancestor, and
   the binding flag can be reconstructed. Repo-local implication:
   nested-`forall`, nested-owner, and nested scheme-root crossings must stay
   fail-closed rather than being accepted implicitly.
4. `17.3 Perspectives` says recursive types together with second-order
   polymorphism are already difficult and that the main difficulty likely lies
   in recursion in the binding structure, while cyclic term-graphs were not the
   explored route. Repo-local implication: this exact bind remains
   explicit-only, iso-recursive, structurally acyclic, and single-family.

These anchors justify only this one exact bind. They do not authorize code,
implementation, or verification.

## Read-Only Anchor Reconstruction

The current read-only source/test anchors identify the selected exact target
precisely:

1. `src/MLF/Elab/Run/ResultType/Fallback.hs:558-730` computes
   `boundVarTargetRoot` from
   `canonicalFinal (schemeBodyTarget targetPresolutionView rootC)`, defines
   `boundHasForallFrom`, constrains retained-child lookup through
   `sameLocalTypeLane child`, filters candidates by
   `bndRoot == boundVarTargetRoot` and `not hasForall`, lifts the packet
   through `keepTargetFinal`, and then lets `targetC` choose either the
   retained child through `Just v -> v` or fall back through
   `schemeBodyTarget targetPresolutionView rootC` / `rootFinal`.
2. `src/MLF/Elab/Run/Scope.hs:127-160` defines `schemeBodyTarget` as the
   owner-local `S'`-style selector for scheme-body translation. That makes
   `schemeBodyTarget` neighboring boundary context only, not the selected live
   target.
3. `test/PipelineSpec.hs:1495-1592` preserves the bounded retained-child
   evidence packet:
   the same-lane local `TypeRef` positive case,
   the same-lane source guard proving retained-child lookup stays on that same
   local lane, and the nested-`forall` fail-closed contrast.

Those anchors prove one exact narrowing only: the selected target is the
same-lane local `TypeRef` retained-child candidate whose bound root matches
`boundVarTargetRoot`, and the exact downstream selection use is the `targetC`
path that returns that retained child through `Just v -> v`. Any route that
would instead land on `rootFinal`, `boundTarget`, `schemeBodyTarget`, another
fallback family, or a nested-`forall` / nested-owner crossing is outside this
bind.

## The Only Frozen `N11` Target

The only frozen `N11` target is:

the same-lane local `TypeRef` retained-child `boundVarTarget -> targetC`
packet in `src/MLF/Elab/Run/ResultType/Fallback.hs`, limited to the route
where:

- `boundVarTargetRoot` stays anchored at
  `canonicalFinal (schemeBodyTarget targetPresolutionView rootC)`;
- `boundHasForallFrom` keeps nested-`forall`, nested-owner, and nested
  scheme-root crossings fail-closed;
- `boundVarTarget` selects exactly one retained child only from the same local
  `TypeRef` lane;
- `keepTargetFinal` becomes true only because that retained-child packet is
  present inside the already-local root; and
- the downstream `targetC` choice resolves through `Just v -> v`, returning
  the retained child rather than
  `schemeBodyTarget targetPresolutionView rootC`, `rootFinal`, `boundTarget`,
  or another fallback family.

This one frozen target is exercised by the retained-child `PipelineSpec`
evidence packet in `test/PipelineSpec.hs:1495-1592`:

- `wireSameLaneLocalRoot` rewires the positive case so the retained child is
  reparented under the retained root, the retained root loses its parent, and
  both root and child share the retained bound;
- the same-lane local `TypeRef` positive case proves the packet can stay on the
  retained-child route without reopening a different family;
- the same-lane source guard proves the packet is bounded by
  `sameLocalTypeLane child`; and
- the nested-`forall` fail-closed contrast proves that a wrapper crossing a
  nested `forall` boundary is outside the exact target and must stay rejected.

Required interpretation of that one bounded target:

- owner-boundary anchor: `boundVarTargetRoot` is the canonicalized
  `schemeBodyTarget` root for the selected local owner lane; it identifies the
  one bounded scheme-body/root ownership frame in which the retained child may
  be kept;
- retained-child ownership packet: the only admissible child is the
  `boundVarTarget` candidate whose `bindingScopeRefCanonical` result stays on
  the same local `TypeRef` lane and whose bound root canonically matches
  `boundVarTargetRoot`;
- fail-closed bound condition: that retained child is admissible only when
  `boundHasForallFrom bnd` is false, meaning no nested-`forall`, nested-owner,
  or nested scheme-root crossing is being smuggled in as success;
- exact target-selection step under study: `keepTargetFinal` becomes true for
  this same-lane retained-child packet and the downstream `targetC` choice
  resolves through `Just v -> v`, selecting the retained child itself;
- neighboring boundary context only:
  `schemeBodyTarget targetPresolutionView rootC` remains the neighboring
  owner-boundary reference used to describe the packet; it is not the selected
  exact target; and
- explicit non-selection inside the same local root: the frozen packet is not
  `rootFinal`, not `boundTarget`, not the earlier `baseTarget -> baseC`
  packet, and not the repaired-queue retained-child packet reused as automatic
  authority.

## One-Packet Admissibility Story

### Retained-child ownership and owner boundary

The exact bind is admissible only for one owner/child/bound packet.
`boundVarTargetRoot` anchors the owner-local scheme-body frame, and the only
candidate child is the one kept on the same local `TypeRef` lane by
`sameLocalTypeLane child`.

The positive `PipelineSpec` helper `wireSameLaneLocalRoot` makes that packet
review-visible without widening it:

- the retained root loses its parent;
- the retained child is reparented under that retained root; and
- both retained root and retained child share the same retained bound.

This is evidence for one bounded owner/child/bound packet only. It is not a
license to search other owners, other retained children, or other fallback
families.

### Fail-closed nested-`forall` / nested-owner / nested scheme-root conditions

The exact bind is also admissible only when `boundHasForallFrom` remains false
for the retained bound.

That means:

- no nested `TyForall` is crossed as success;
- no nested-owner or nested scheme-root crossing is accepted as success; and
- no retained-child candidate whose bound root leaves the selected
  `boundVarTargetRoot` frame can become the exact target.

If any of those conditions fails, the packet must stay fail-closed and this
exact bind does not authorize a different target.

### Exact target-selection step under study

The exact target-selection step frozen here is narrow:

1. `boundVarTarget` survives only because the child stays on the same local
   `TypeRef` lane, the candidate bound root equals `boundVarTargetRoot`, and
   `not hasForall` holds.
2. `keepTargetFinal` becomes true only because that retained-child packet is
   present inside the already-local root.
3. Inside `targetC`, the selected packet resolves through `Just v -> v`.

This artifact therefore freezes the retained child itself as the exact target.
It does not freeze `rootFinal`, `boundTarget`, or
`schemeBodyTarget targetPresolutionView rootC` as the exact target for this
lane.

### Neighboring boundary context only

`schemeBodyTarget` remains neighboring boundary context only.
`src/MLF/Elab/Run/Scope.hs` still defines it as the owner-local `S'`-style
scheme-body selector, and `Fallback.hs` uses it here only to anchor the owner
frame and to describe the fail-closed fallback boundary.

This artifact does not promote `schemeBodyTarget` to the selected live
subject, the selected exact target, or a fresh implementation authority.

## Explicit Out-Of-Scope Record

This exact-target bind keeps all other routes blocked or predecessor-only:

- the earlier `baseTarget` lane as live work;
- the earlier `N2` selection as still-live subject authority;
- the earlier `N3` contract as automatic fresh-lane clearance;
- the exact accepted non-local `baseTarget -> baseC` packet as live work;
- the accepted repaired-queue retained-child packet as automatic fresh-lane
  clearance;
- `boundTarget`;
- `schemeBodyTarget`;
- `rootFinal` as the fresh exact target for this lane;
- replay reopen;
- `MLF.Elab.Inst` or `InstBot`;
- `src/MLF/Elab/Run/ResultType/View.hs`;
- any other `ResultType.Fallback` family;
- any different solver or pipeline subject;
- cross-family search;
- implicit unfolding;
- equi-recursive reasoning or equality;
- cyclic structural graph encoding;
- graph-cycle exceptions;
- multi-SCC support;
- a second executable interface; and
- any compatibility, convenience, or default-path fallback widening.

If any future slice needs one of those routes, this bind does not authorize it.

## Lawful Verification Skip Note

`cabal build all && cabal test` remains intentionally out of scope for this
round because `N11` is docs-only by contract and this round does not touch
`src/`, `src-public/`, `app/`, `test/`, or `mlf2.cabal`.

This skip does not authorize a code path. It records only that implementation
and verification remain blocked until a later accepted roadmap item authorizes
code or test changes.

## One Authoritative `N11` Outcome

Authoritative `N11` outcome:
`boundVarTarget-exact-target-bind-established`

This is the only lawful outcome recorded here.

Its exact meaning is fixed:

- accepted `L1` / `L2` / `N1` through `N10` remain binding predecessor
  continuity;
- the selected retained-child / nested-`forall` / binding-structure
  `boundVarTarget` lane now has one frozen exact target:
  the same-lane local `TypeRef` retained-child packet whose downstream
  `targetC` step returns the retained child through `Just v -> v`;
- future work still requires a later accepted bounded implementation slice and
  a later verifier-owned result before any broader claim can be made; and
- this bind is not implementation readiness and not verification readiness.

It is not lawful to treat the earlier `baseTarget` selection, the earlier `N3`
contract, the exact accepted non-local `baseTarget -> baseC` packet, or the
accepted repaired-queue retained-child packet as automatic carry-forward
authority on this fresh lane. Each remains predecessor evidence only.

## Non-Authorization And Immutability

This artifact does not authorize:

- `N12`, `N13`, or `N14`;
- implementation or verification;
- merge action;
- roadmap edits;
- controller-state edits;
- bug-tracker edits; or
- predecessor-history rewrites.

Under `orchestrator/retry-subloop.md`, prior attempts and reviewer-owned
history remain immutable. This document is the canonical `attempt-1`
exact-target-bind artifact for `round-078` / `N11`.
