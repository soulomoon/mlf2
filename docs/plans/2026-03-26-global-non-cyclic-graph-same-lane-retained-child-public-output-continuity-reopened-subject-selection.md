# Global `non-cyclic-graph` Same-Lane Retained-Child Public-Output Continuity Reopened Subject Selection

Date: 2026-03-26
Round: `round-105`
Roadmap item: `item-2`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: one docs-only rev-002 item-2 selection of the exact
same-lane retained-child / public-output continuity pocket inside the
accepted candidate boundary
Artifact kind: canonical docs-only rev-002 item-2 live reopened subject
selection

## Stage Contract Freeze

This artifact implements only roadmap item `2` for `attempt-1` with
`retry: null`.

It consumes accepted `round-104` item-1 truth and freezes exactly this much,
and no more:

- rev-002 remains planning-only and architecture-only;
- accepted rev-001 truth remains binding;
- rev-001 items `6` through `8` remain blocked;
- the accepted item-1 candidate boundary remains the only lawful narrowing
  domain; and
- exactly one live reopened subject is selected inside that boundary.

This artifact does not:

- relitigate whether the frozen candidate boundary exists;
- define the item-3 safety / acceptance contract;
- bind the item-4 audit target across rows, packets, modules, commands, or
  output surfaces;
- decide the item-5 open-one-lane-versus-stop outcome;
- authorize edits to `orchestrator/state.json`, roadmap files, retry files,
  verification files, `Bugs.md`, source code, tests, or Cabal files; or
- authorize multi-SCC search, second interfaces, fallback widening,
  production implementation, hardening, or broad capability claims.

The inherited baseline also remains unchanged:

- explicit recursive annotations remain the production baseline;
- recursive meaning remains iso-recursive only;
- equi-recursive semantics and implicit unfolding remain blocked;
- cyclic structural graphs are not authorized as live implementation work;
- multi-SCC search remains blocked;
- second interfaces remain blocked; and
- fallback widening remains blocked.

## Accepted Predecessor Authority

| Source | Binding truth carried forward |
| --- | --- |
| `orchestrator/rounds/round-104/review-record.json` | Accepted `round-104` made the item-1 authority freeze authoritative. Item `2` may narrow only within that accepted freeze. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-reopened-revision-authority-and-candidate-boundary-freeze.md` | Rev-002 is planning-only and architecture-only; exactly one candidate boundary is frozen; `C1` remains contrast-only; and the strongest admitted same-family pressure remains the one exact `C2` / `C5` / `C7` pocket. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-keep-vs-reopen-decision-gate.md` | Accepted rev-001 item `5` reopened only the `non-cyclic-graph` revision question, preserved `iso-recursive = keep`, `non-equi-recursive = keep`, and `no-fallback = keep`, and did not re-enable rev-001 items `6` through `8`. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md` | The accepted representative matrix still has zero `stable visible persistence` rows and still makes the same exact `C2` / `C5` / `C7` pocket the strongest admitted same-family route below repo-level success, with `N4` as architecture-pressure context only. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md` | `C2` is the primary admitted same-lane retained-child pocket and `C5` is the owner-local / binder-sensitive placement face of that same exact pocket, not a second packet. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md` | `C7` is the public-output continuity face of that same exact `C2` / `C5` pocket, not a second packet, not a second route, and not a second public interface. |
| `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md`, `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md`, and `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md` | The accepted same-lane predecessor chain already froze one exact family / anchor / owner-local frame / route / clear-boundary / packet tuple and kept that same exact pocket at bounded blocker-debt or bounded architecture-pressure status only. |

These accepted artifacts are predecessor authority only. They do not already
define item `3`, item `4`, or item `5`, and they do not authorize broader
cyclic work.

## Exact Selected Live Reopened Subject

Item `2` selects exactly one live reopened subject inside the frozen rev-002
boundary:

`the exact same-lane retained-child / public-output continuity pocket already carried by accepted representative rows C2, C5, and C7, with non-local C1 retained only as contrast context`

The selection is bounded by one family, one anchor, one owner-local frame,
one route, one clear-boundary status, and one exact packet:

| Continuity field | Exact selected value |
| --- | --- |
| Family | same-lane retained-child |
| Recursive-shape anchor | `boundVarTargetRoot` |
| Owner / binder frame | one owner-local retained-child frame |
| Route | `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC` |
| Quantified-boundary status | clear-boundary only: `boundHasForallFrom` is false and `not hasForall` holds |
| Exact packet | the exact packet shown below and no packet variant |

Exact packet:

```haskell
ELet "k" (ELamAnn "x" recursiveAnn (EVar "x"))
  (ELet "u" (EApp (ELam "y" (EVar "y")) (EVar "k")) (EVar "u"))
```

where
`recursiveAnn = STMu "a" (STArrow (STVar "a") (STBase "Int"))`.

`C2` names the admitted same-lane retained-child pocket on this exact route.
`C5` is the owner-local / binder-sensitive placement face of this same exact
pocket and does not open a second packet. `C7` is the public-output
continuity face of this same exact pocket. It does not introduce a second
packet, a second interface, or a second route. `C7` is the same exact
`C2` / `C5` pocket reread through the public-output continuity lens, and
that public-output pressure is what makes this one exact pocket the live
architecture-pressure subject inside rev-002 item `2`.

This artifact selects the pressure subject only. It does not say whether one
bounded single-component cyclic-structure successor lane is required,
sufficient, or opened for that subject.

## Why This Subject And Not Broader Alternatives

The accepted item-1 freeze already narrowed rev-002 to one candidate
boundary: whether one bounded single-component cyclic-structure successor
lane is needed for the strongest admitted retained-child / public-output
continuity pressure. Item `2` therefore must choose the exact pressure
subject inside that frozen boundary, not reopen the boundary itself.

The one lawful choice is this same exact `C2` / `C5` / `C7` pocket:

- the accepted representative matrix still identifies that pocket as the
  strongest admitted same-family route below success;
- `C2` carries the admitted same-lane retained-child continuity on the exact
  route under review;
- `C5` keeps the same pocket in the owner-local / binder-sensitive
  placement frame without turning it into a second packet;
- `C7` exposes the public-output continuity face of that same pocket and is
  therefore the part of the same route that keeps live architecture pressure
  visible in rev-002; and
- non-local `C1` remains admitted contrast only, not a coequal reopened
  subject.

Broader alternatives fail closed:

- selecting `C1` as a second live reopened subject would widen beyond the
  accepted item-1 boundary;
- selecting `C5` or `C7` as a separate packet would contradict accepted
  predecessor truth that they reuse the same exact `C2` pocket;
- selecting multiple routes or multiple same-family pockets would widen into
  broader family search; and
- selecting multi-SCC search, second interfaces, fallback widening, or
  production behavior would step outside item `2` entirely.

## Blocked Scope And Non-Goals

The following remain explicitly blocked after this subject selection:

- any change to accepted item-1 truth or accepted rev-001 truth;
- any reopening of rev-001 items `6` through `8`;
- any widening into multiple subjects, multiple packets, multiple routes, or
  broader family search;
- any attempt to define item-3 safety rules or acceptance rules;
- any attempt to bind the item-4 audit surface across rows, packets,
  modules, commands, or outputs;
- any attempt to decide item-5 open-one-lane versus stop;
- any multi-SCC search, second-interface work, fallback widening, or
  production implementation; and
- any broad repo-level capability claim.

## Later-Item Handoff Ownership

Later rev-002 items must treat this artifact as subject-selection authority
only:

- item `3` still owns the safety / acceptance contract for this selected
  subject;
- item `4` still owns the exact audit bind across rows, packets, modules,
  commands, and output surfaces for this selected subject; and
- item `5` still owns the one lawful decision on whether to open one
  bounded architecture-amendment lane for this selected subject or stop.

This round therefore remains docs-only, planning-only, and architecture-only.
The selected subject is not implementation clearance, not verification
clearance, and not a broad architecture claim.
