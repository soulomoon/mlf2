# `N4` Exact Bounded Target Bind For The Preserved Generic Scheme-Alias / Base-Like `baseTarget` Planning Subject

Date: 2026-03-22
Round: `round-071`
Roadmap item: `N4`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: preserved generic scheme-alias / base-like `baseTarget` planning subject
Artifact kind: canonical docs-only exact-target bind

## Stage Contract Freeze

This artifact implements only roadmap item `N4` for `attempt-1` with
`retry: null`.

`N4` is docs-only exact-target-bind work inside accepted
`N1 = reopen-planning-only`, accepted
`N2 = baseTarget-planning-subject-selected`, and the accepted `N3` safety /
acceptance contract. It binds exactly one bounded target and exactly one
same-lane selection use inside that already-selected planning subject.

This artifact does not authorize `N5`, `N6`, or `N7`. It does not authorize
implementation, verification, merge action, roadmap edits,
`orchestrator/state.json` edits, bug-tracker edits, or predecessor-history
rewrites.

The inherited boundary remains fixed and unchanged:

- explicit-only recursive baseline;
- non-equi-recursive semantics;
- non-cyclic-graph structural encoding;
- no second executable interface; and
- no compatibility, convenience, or default-path fallback widening.

## Accepted `L1` / `L2` / `N1` / `N2` / `N3` Continuity And Exact Packet

Only accepted predecessor continuity is carried forward here:

1. `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md`
   remains the authoritative `L1` fail-closed record. Inside repaired
   `URI-R2-C1` and the inherited boundary, it proved that there was no fresh
   lawful exact successor slice in the repaired queue and preserved the broader
   generic scheme-alias / base-like `baseTarget` route only as future context.
2. `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-21-uri-r2-c1-l2-post-l1-fail-closed-successor-decision-gate.md`
   remains the authoritative `L2` closeout. It finalized the repaired queue as
   `stop-blocked` and required any broader successor work to pass through a
   separate roadmap amendment plus a fresh selection.
3. `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-22-automatic-iso-recursive-post-l2-roadmap-amendment-authority-gate.md`
   remains the authoritative `N1` record. Its one bounded outcome,
   `reopen-planning-only`, reopened only a docs-first planning lane, kept
   repaired `URI-R2-C1` closed as predecessor evidence only, and preserved the
   inherited boundary unchanged.
4. `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-22-automatic-iso-recursive-next-live-subject-selection.md`
   remains the authoritative `N2` record. It selected exactly one live subject:
   the preserved generic scheme-alias / base-like `baseTarget` route only.
5. `/Users/ares/.codex/worktrees/d432/mlf4/docs/plans/2026-03-22-automatic-iso-recursive-base-target-safety-acceptance-contract.md`
   remains the authoritative `N3` record. It requires any lawful `N4` bind to
   name one owner binder, one owned bound, one exact translation step, one
   inverse-translation-safe bound-inlining story, one structural / variance
   account for binding-flag reconstruction, and one explicit out-of-scope
   record.
6. `/Users/ares/.codex/worktrees/d432/mlf4/tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
   still records `N4 = NO` because the reopened loop has not yet frozen one
   exact bounded slice inside the selected `baseTarget` subject.
7. `/Users/ares/.codex/worktrees/d432/mlf4/Bugs.md` remains read-only
   predecessor context only. Open `BUG-2026-03-16-001` keeps replay /
   `InstBot` pressure visible, but it does not reopen replay, `MLF.Elab.Inst`,
   or any non-selected route.

The exact admissible packet is therefore narrow: the preserved non-local
scheme-root alias-bound / base-like `baseTarget -> baseC` packet only, not any
already-accepted local `TypeRef` lane and not any repaired-queue reopen.

## Read-Only Anchor Reconstruction

The exact narrowing comes from these unchanged code and test anchors in the
round worktree:

1. `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/src/MLF/Elab/Run/ResultType/Fallback.hs:367-408`
   computes `baseTarget` before any local-lane retention decision. The packet
   under study remains the existing generic `baseTarget` computation for a
   scheme-root alias-bound / base-like root.
2. `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/src/MLF/Elab/Run/ResultType/Fallback.hs:521-548`
   names the accepted local proof cluster. `rootLocalEmptyCandidateSchemeAliasBaseLike`
   and `rootLocalSchemeAliasBaseLike` are both gated by
   `rootBindingIsLocalType`, so they already own the local lanes and cannot be
   rebound here.
3. `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/src/MLF/Elab/Run/ResultType/Fallback.hs:693-728`
   keeps `keepTargetFinal` local-only and then consumes the preserved packet via
   the downstream generic `Just baseC | rootIsSchemeAlias && rootBoundIsBaseLike -> baseC`
   `targetC` arm. That same-lane use is the only consumer frozen here.
4. `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/test/PipelineSpec.hs:1244-1269`
   defines `schemeAliasBaseLikeFallback`. It extracts the `AVar` body root,
   optionally localizes that root with `makeLocalTypeRoot`, and then reattaches
   the canonical `Int` base node to `rootNid` with `rebindRootTo`.
5. `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/test/PipelineSpec.hs:1594-1606`
   preserves the already-accepted local contrasts:
   `localEmptyCandidateSchemeAliasBaseLikeFallback True` owns the local
   empty-candidate `baseTarget -> baseC` lane, while
   `schemeAliasBaseLikeFallback True` owns the local retained-final
   `targetC -> rootFinal` continuity lane.
6. `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/test/PipelineSpec.hs:1622-1625`
   preserves the remaining non-local packet. `schemeAliasBaseLikeFallback False`
   returns `TBase (BaseTy "Int")` and `containsMu False`, which distinguishes
   the preserved non-local packet from the accepted local `rootFinal` lane.
7. `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/test/PipelineSpec.hs:1698-1762`
   keeps the source guard ordering explicit: the accepted local predicates stay
   ahead of the generic `rootIsSchemeAlias && rootBoundIsBaseLike` arm, and the
   local `keepTargetFinal` cluster remains separate.

Those anchors prove one exact narrowing only: the selected packet is the
preserved non-local generic scheme-root alias-bound / base-like
`baseTarget -> baseC` packet exercised by `schemeAliasBaseLikeFallback False`,
and its same-lane selection use is the downstream generic `targetC` arm that
consumes `baseC`.

## The Only Frozen `N4` Target

The only frozen `N4` target is:

the preserved non-local generic scheme-root alias-bound / base-like
`baseTarget -> baseC` packet in
`/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/src/MLF/Elab/Run/ResultType/Fallback.hs`,
limited to the existing generic `baseTarget` computation at
`Fallback.hs:367-408` and the downstream same-lane `targetC` consumption at
`Fallback.hs:700-710`, as exercised by
`schemeAliasBaseLikeFallback False` in
`/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/test/PipelineSpec.hs:1244-1269`
and
`/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/test/PipelineSpec.hs:1622-1625`.

Required interpretation of that one bounded target:

- owner binder: the scheme-root alias binder behind the extracted `AVar` body
  in `schemeAliasBaseLikeFallback`, i.e. the binder that still owns `rootNid`
  after `bodyRoot` extracts the canonical root and before any local
  `TypeRef`-lane retargeting is introduced;
- owned bound: the canonical base-like bound reattached to `rootNid` via
  `rebindRootTo`, currently realized by the focused `Int` base node;
- exact translation step under study: the existing generic scheme-alias /
  base-like `baseTarget` computation and the same-lane `targetC` use that
  selects `baseC`;
- same-lane selection use: only the downstream generic
  `Just baseC | rootIsSchemeAlias && rootBoundIsBaseLike -> baseC` arm, not
  `keepTargetFinal`, not `rootFinal`, and not any `schemeBodyTarget` fallback;
- inverse-translation-safe bound-inlining story: this one packet only, using
  thesis-backed alias / inert-bound inlining language rather than a broader
  solver-wide claim; and
- binding-flag reconstruction: this one packet only, from structural and
  variance evidence tied to the same owner binder / owned bound pair, not from
  heuristic repair or replay fallback.

## One-Packet Admissibility Story

### Owner binder / owned bound

The owner binder and owned bound stay explicit in the selected helper packet.
`schemeAliasBaseLikeFallback False` extracts the body variable, canonicalizes
its root as `rootNid`, and then rebinds that exact root to the canonical `Int`
base node. This keeps the preserved packet binder-centric:

- the owner binder is the scheme-root alias binder that owns the extracted
  `AVar` body root;
- the owned bound is the canonical base-like `Int` node reattached to that same
  root; and
- the selected `baseTarget -> baseC` / `targetC` lane reads that owned bound
  directly without first reopening any different owner or family.

### Inverse-translation-safe bound-inlining story

The accepted `N3` contract allows an `N4` packet only if the bound-inlining
story stays inverse-translation-safe for one owner-binder / owned-bound pair.
That bounded story is satisfied only in this narrow sense:

1. `8.3.1 Inlining bounds` allows inlining only when inverse translation can
   rebuild the binder and bound unambiguously, the node is not `usedtwice`, and
   the binder is the structural ancestor.
2. `15.6.2 Expressivity of alias bounds` says alias bounds need special care,
   and the graphic presentation does not directly express them. So alias-bound
   handling must stay explicit and local rather than turning into a broader
   fallback or search rule.
3. `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/src/MLF/Frontend/Normalize.hs:20-35`
   already keeps alias-bound normalization explicit and structural: variable
   bounds are inlined only through explicit substitution, with self-bound and
   non-structural cases rejected.

For this exact packet, the admissible inlining narrative is therefore limited
to the one reattached scheme-root alias bound owned by `rootNid` and currently
realized by `Int`. This artifact does not claim that every alias bound is now
safe to inline, and it does not authorize any broader alias-bound rewrite.

### Structural / variance evidence for binding-flag reconstruction

The selected packet also stays inside the accepted `N3` binding-flag
reconstruction axis:

1. `8.3.1 Inlining bounds` requires the binder to be the structural ancestor
   and the binding flag to be recoverable from variance.
2. The selected packet never enters the accepted local retained-final lane,
   because `keepTargetFinal` is gated by `rootBindingIsLocalType` while the
   preserved packet is the non-local `schemeAliasBaseLikeFallback False` case.
3. The selected packet also stops before any
   `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/src/MLF/Elab/Run/Scope.hs:127-160`
   `schemeBodyTarget` escape. The frozen same-lane consumer is still the direct
   `targetC -> baseC` arm.

So the only lawful future proof for this packet is: the same scheme-root owner
remains the structural ancestor of the same base-like bound, and any
binding-flag reconstruction must be narrated from that existing structure and
its variance only. If the packet needs heuristic repair, replay guidance,
non-ancestor ownership, or a hidden body-target detour, it is outside this
bind.

## Explicit Out-Of-Scope Record

This exact-target bind keeps all other routes out-of-scope:

- the accepted local `rootLocalSchemeAliasBaseLike`
  `keepTargetFinal` / `targetC -> rootFinal` lane exercised by
  `schemeAliasBaseLikeFallback True`;
- the accepted local `rootLocalEmptyCandidateSchemeAliasBaseLike`
  `baseTarget -> baseC` lane exercised by
  `localEmptyCandidateSchemeAliasBaseLikeFallback True`;
- the accepted local `rootLocalSingleBase` and
  `rootLocalInstArgSingleBase` singleton-base lanes;
- `rootLocalMultiInst`, `rootLocalInstArgMultiBase`, `boundVarTarget`,
  `boundTarget`, and `schemeBodyTarget`;
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-071/src/MLF/Elab/Run/ResultType/View.hs`;
- repaired `URI-R2-C1` as live work;
- replay reopen;
- `MLF.Elab.Inst` and `InstBot`;
- any other `ResultType.Fallback` family;
- any different solver or pipeline subject;
- cross-family search;
- implicit unfolding;
- equi-recursive reasoning or equality;
- cyclic structural graph encoding;
- graph-cycle exceptions;
- multi-SCC reasoning;
- a second executable interface; and
- compatibility, convenience, or default-path fallback widening.

If any future slice needs one of those routes, this bind does not authorize it.

## Non-Authorization And Immutability

This artifact does not authorize:

- `N5`, `N6`, or `N7`;
- implementation or verification;
- merge action;
- roadmap edits;
- controller-state edits;
- bug-tracker edits; or
- predecessor-history rewrites.

This artifact is the immutable `attempt-1` bind record for `round-071` / `N4`.
Per `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/retry-subloop.md`,
future retries must be additive and must not rewrite earlier attempts or
reviewer-owned history.
