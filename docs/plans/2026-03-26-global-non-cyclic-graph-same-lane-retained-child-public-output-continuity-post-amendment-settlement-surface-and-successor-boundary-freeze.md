# Global `non-cyclic-graph` Same-Lane Retained-Child Public-Output Continuity Post-Amendment Settlement Surface And Successor Boundary Freeze

Date: 2026-03-26
Round: `round-113`
Roadmap item: `item-1`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: one docs-only rev-004 item-1 aggregate freeze for the exact
same-pocket post-amendment current-result surface and one exact future
writable settlement-docs boundary inside the bounded same-family successor
revision published by accepted `round-112`
Artifact kind: canonical docs-only rev-004 item-1 aggregate contract-freeze
record

## Stage Contract Freeze

This artifact implements only roadmap item `1` for `attempt-1` with
`retry: null`.

It consumes the live round-113 control-plane inputs plus accepted rev-003
item `3` / item `4` predecessor truth and freezes exactly this much, and no
more:

- rev-004 item `1` remains docs-only, planning-only, and aggregate-only;
- accepted `round-111` remains authoritative for the exact current-result
  surface;
- accepted `round-112` remains authoritative for why rev-004 exists and for
  why predecessor docs must be settled through new bounded artifacts rather
  than silent rewrites;
- exactly the same selected rows `C2`, `C5`, and `C7` remain in scope;
- exactly the same exact packet and same exact tuple remain in scope;
- exactly one current-result surface is frozen for rev-004 only:
  the accepted `round-111` validation artifact and its exact same-pocket
  post-amendment read;
- exactly one future writable docs boundary is frozen for later rev-004
  settlement artifacts only:
  `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-post-amendment-settlement-ledger.md`
  and
  `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-post-amendment-settlement-validation.md`;
  and
- exactly five still-live same-family pre-amendment docs remain frozen as
  immutable historical evidence rather than becoming part of the writable
  settlement boundary.

This artifact does not:

- reopen subject selection or reinterpret the accepted same-pocket lane;
- relitigate the accepted rev-003 item-2 implementation or accepted
  rev-003 item-3 validation result;
- authorize edits under `src/`, `src-public/`, `app/`, `test/`, or
  `mlf2.cabal`;
- authorize edits to `orchestrator/state.json`, the rev-004 roadmap bundle,
  `Bugs.md`, `TODO.md`, `implementation_notes.md`, or `CHANGELOG.md`;
- convert any historical predecessor settlement artifact into part of the
  future writable docs boundary;
- authorize a second packet, a second route, a second interface, or a broad
  same-family or repo-level success narrative; or
- authorize multi-SCC search, second interfaces, fallback widening,
  production rollout, hardening, or broad capability claims.

The inherited baseline also remains unchanged:

- `iso-recursive = keep`;
- `non-equi-recursive = keep`;
- `no-fallback = keep`;
- explicit recursive annotations remain the production baseline;
- equi-recursive semantics and implicit unfolding remain blocked; and
- rev-001 items `6` through `8` remain blocked.

## Round-113 Control-Plane Inputs

| Control-plane input | Binding read carried forward |
| --- | --- |
| `orchestrator/rounds/round-113/selection.md` | This round is item-1-only, docs-only, aggregate-only, and limited to freezing one exact same-pocket current-result surface plus one exact future writable docs boundary. |
| `orchestrator/rounds/round-113/plan.md` | The output must preserve accepted `round-111` and `round-112`, freeze the same exact pocket, freeze the exact current-result anchor, freeze the new-docs-only writable boundary, and keep predecessor immutability explicit. |
| `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-004/roadmap.md` | Item `1` is the only dependency-free unfinished item, and completion requires one accepted docs-only artifact freezing the exact selected rows, packet, tuple, current-result surface, future writable docs boundary, and predecessor immutability. |
| `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-004/retry-subloop.md` | Item `1` is aggregate-only, may not use `accepted + retry`, and must finalize or reject without rewriting prior accepted truth. |
| `orchestrator/roadmaps/2026-03-26-01-global-non-cyclic-graph-settlement-and-automatic-iso-recursive-inference-roadmap/rev-004/verification.md` | Post-amendment-freeze verification must prove the artifact narrows only to the same exact `C2` / `C5` / `C7` pocket, the same exact packet and tuple, one exact current-result surface, one exact writable docs boundary, and explicit predecessor immutability. |

These inputs define the live item-1 contract only. They do not themselves
replace accepted predecessor truth.

## Accepted Predecessor Authority

| Accepted predecessor artifact | Binding truth carried forward |
| --- | --- |
| `orchestrator/rounds/round-112/review-record.json` | Accepted `round-112` made rev-004 authoritative with final outcome `publish one later same-family successor revision for bounded post-amendment settlement`. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-post-amendment-handoff-decision.md` | Accepted rev-003 item `4` fixed the reason rev-004 exists: accepted item `3` changed the exact current same-pocket read while still-live same-family docs remained pre-amendment and therefore should be settled through new bounded artifacts rather than silent predecessor rewrites. |
| `orchestrator/rounds/round-111/review-record.json` | Accepted `round-111` finalized the exact same-pocket post-amendment validation result. That acceptance record is the controller-level authority for the one current-result surface frozen here. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-authoritative-handoff-bounded-amendment-frozen-same-pocket-evidence-surface-validation.md` | Accepted rev-003 item `3` fixed the exact current-result read: helper-visible/internal recursive continuity remained present, and both authoritative public entrypoints now carried bounded recursive structure on the same exact packet. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md` | Still-live same-family settlement dossier that currently records the pre-amendment public-collapse read for `C2` / `C5` and must now remain immutable historical evidence rather than a writable settlement target. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md` | Still-live same-family settlement dossier that currently records the pre-amendment public-collapse read for `C7` and must now remain immutable historical evidence rather than a writable settlement target. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md` | Still-live matrix dossier that currently classifies `C2` / `C5` / `C7` with the pre-amendment blocker-debt public-output read and must remain predecessor evidence only. |
| `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md` and `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md` | Accepted predecessor exact-pocket ledgers remain authoritative historical evidence of the pre-amendment state and must remain immutable rather than being silently rewritten here. |
| `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md` | The inherited repo baseline remains explicit-only in production, iso-recursive only, non-equi-recursive, no-fallback, and structurally non-cyclic by default unless a later accepted revision explicitly changes that status. |

These accepted artifacts are predecessor authority only. None of them
already lands rev-004 item `2`, lands rev-004 item `3`, or decides rev-004
item `4`.

## Exact Same-Pocket Boundary

This freeze binds exactly one same-pocket subject and no other:

`the exact same-lane retained-child / public-output continuity pocket already carried by accepted rows C2, C5, and C7, with non-local C1 retained only as bounded contrast context`

### Exact Same-Pocket Rows

| Row | Exact same-pocket role that remains binding | What item `1` freezes for later work | Bound that must remain closed |
| --- | --- | --- | --- |
| `C2` | primary same-lane retained-child row for the selected pocket | same family / anchor / owner-local frame / route identity and the same accepted post-amendment current-result read on the exact packet | not a second packet, not a broader same-family success claim, not a family-wide settlement target |
| `C5` | same exact `C2` pocket through the owner / binder-sensitive placement lens only | same local `TypeRef` lane, same owner-local frame, and same nested-`forall` fail-closed guard context | not a second owner-local packet, not a second route, not a widened settlement boundary |
| `C7` | same exact `C2` / `C5` pocket through the public-output continuity lens only | exact authoritative public-output post-amendment result and exact successor docs boundary for the same packet | not a second interface, not a second public path, not a broad repo-level capability claim |

`C1` may appear only as bounded contrast context. It must not become a
second selected subject, a second packet, a second route, a second
interface, or a reason to widen the settlement boundary.

### Exact Packet

The selected packet remains exactly this packet and no variant:

```haskell
ELet "k" (ELamAnn "x" recursiveAnn (EVar "x"))
  (ELet "u" (EApp (ELam "y" (EVar "y")) (EVar "k")) (EVar "u"))
```

where
`recursiveAnn = STMu "a" (STArrow (STVar "a") (STBase "Int"))`.

### Exact Tuple

The same frozen tuple remains binding:

| Continuity field | Exact selected value |
| --- | --- |
| Family | same-lane retained-child |
| Recursive-shape anchor | `boundVarTargetRoot` |
| Owner / binder frame | one owner-local retained-child frame |
| Route | `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC` |
| Quantified-boundary status | clear-boundary only: `boundHasForallFrom` is false and `not hasForall` holds |

`C5` and `C7` remain rereads of this same exact packet only. They are not
second packets, second routes, second interfaces, or second same-family
lanes.

## Exact Current-Result Surface

The one exact current-result surface for rev-004 is the accepted rev-003
item-3 validation surface only:

- canonical anchor artifact:
  `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-authoritative-handoff-bounded-amendment-frozen-same-pocket-evidence-surface-validation.md`;
- acceptance record:
  `orchestrator/rounds/round-111/review-record.json`; and
- no coequal replacement surface, no broader same-family synthesis, and no
  rewritten predecessor dossier.

### Exact Accepted Read Frozen For Rev-004

The accepted current same-pocket read carried forward into rev-004 is:

| Surface | Exact accepted read |
| --- | --- |
| Helper-visible/internal fallback result | `TArrow (TVar "t32") (TMu "t38" (TArrow (TVar "t38") (TBase (BaseTy {getBaseName = "Int"}))))` |
| Helper-visible/internal recursive witness | `containsMu True` |
| Authoritative public output: `runPipelineElab` | `Right (TForall "a" Nothing (TArrow (TVar "t31") (TMu "t38" (TArrow (TVar "t38") (TBase (BaseTy {getBaseName = "Int"}))))))` |
| Authoritative public output: `runPipelineElabChecked` | `Right (TForall "a" Nothing (TArrow (TVar "t31") (TMu "t38" (TArrow (TVar "t38") (TBase (BaseTy {getBaseName = "Int"}))))))` |

This exact read is what rev-004 means by `post-amendment`. No broader
family-wide summary is frozen here. No older pre-amendment ledger becomes a
coequal current-result surface.

## Exact Future Writable Docs Boundary

The one exact future writable docs boundary for later rev-004 settlement is:

| Path | Why it is the only future writable docs path | What remains forbidden even there |
| --- | --- | --- |
| `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-post-amendment-settlement-ledger.md` | owns the later item-2 job: record the current exact-pocket post-amendment read on new bounded settlement surfaces only | no rewriting of predecessor artifacts in place, no second packet, no second interface, no broad same-family or repo-level success narration |
| `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-post-amendment-settlement-validation.md` | owns the later item-3 job: validate the new bounded settlement surfaces against the accepted current-result anchor only | no code/test changes, no second surface family, no broader rollout or capability claim, no reinterpretation of predecessor evidence as already-settled |

No other docs artifact, no predecessor dossier, no case ledger, no
representative matrix, and no roadmap file is part of the writable
settlement boundary.

Item `1` itself does not create either future writable settlement artifact.
It freezes the boundary only for later items `2` and `3`.

## Exact Immutable Predecessor Settlement Surfaces

The following still-live same-family artifacts remain frozen as immutable
historical evidence:

| Path | Why it remains immutable in rev-004 item `1` |
| --- | --- |
| `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md` | records the older pre-amendment public-collapse read for the same same-lane pocket family context and must not be silently rewritten |
| `docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md` | records the older pre-amendment `C7` public-collapse read and must remain predecessor evidence only |
| `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md` | records the pre-amendment representative matrix classification and must not be back-edited into post-amendment success |
| `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md` | records the earlier exact-pocket case freeze and public-output split before the accepted amendment landed |
| `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md` | records the earlier exact-pocket blocker-debt classification before the accepted amendment changed the authoritative public result |

If any later rev-004 narration edits one of those predecessor artifacts in
place instead of writing the bounded new settlement artifacts frozen above,
it has already widened beyond this accepted boundary.

## Blocked Scope Preserved

All of the following remain explicitly blocked by this item-1 freeze:

- any change to accepted `round-111` / rev-003 item-3 truth or accepted
  `round-112` / rev-003 item-4 truth;
- any reopening of the selected subject boundary or any promotion of `C1`
  into a coequal subject, packet, route, or interface;
- any treatment of `C5` or `C7` as second packets, second routes, second
  interfaces, or separate same-family settlement lanes;
- any widening beyond the exact selected packet, exact tuple, exact
  current-result surface, or exact future writable docs boundary;
- any conversion of the historical predecessor settlement artifacts into
  writable targets;
- any code change, test change, Cabal change, or roadmap-bundle rewrite in
  this round;
- any multi-SCC search, second interfaces, fallback widening,
  equi-recursive semantics, implicit unfolding, production rollout,
  hardening, or broad capability claims; and
- any implication that rev-001 items `6` through `8` are unblocked by
  implication.

If later settlement starts widening into broad same-family success,
predecessor rewrites, code implementation, a second packet, or a second
public interface, it has already exceeded the accepted rev-004 item-1
boundary and must fail closed.

## Later-Item Handoff Ownership

This artifact is the rev-004 item-1 boundary contract for later work only.

Later ownership remains exactly:

- item `2` may create only the frozen bounded settlement ledger at
  `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-post-amendment-settlement-ledger.md`,
  and only to record the current exact-pocket post-amendment read on new
  bounded surfaces without rewriting predecessor evidence;
- item `3` may create only the frozen bounded settlement validation artifact
  at
  `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-post-amendment-settlement-validation.md`,
  and only to validate the new bounded settlement surfaces against the exact
  accepted current-result anchor frozen here;
- item `4` may record only one exact-pocket post-settlement outcome:
  stop after the bounded settlement, or publish one later same-family
  successor revision for further bounded same-pocket settlement; and
- no later item may reinterpret this freeze into authorization for code
  changes, second interfaces, multi-SCC search, fallback widening,
  production rollout, hardening, or broad capability claims unless a later
  accepted revision explicitly changes the live boundary.

## Docs-Only Verification Note

This round is expected to change only documentation artifacts:

- this canonical item-1 settlement-surface / successor-boundary freeze
  artifact; and
- the standard round packet under `orchestrator/rounds/round-113/`.

Because the round does not touch `src/`, `src-public/`, `app/`, `test/`, or
`mlf2.cabal`, no full `cabal build all && cabal test` gate is triggered at
implement time unless the diff escapes that authorized docs-only surface.
