# `G1` Next-Target Bind For Repaired `URI-R2-C1`

Date: 2026-03-19
Round: `round-046`
Roadmap item: `G1`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: repaired `URI-R2-C1`
Artifact kind: docs-only bind/selection freeze

## Stage Contract Freeze

This artifact implements only roadmap item `G1` for `attempt-1` with `retry: null`.

It carries forward the accepted `F4 = continue-bounded` result and freezes one
more bounded next slice only. `G1` is a bind/selection stage only. It does not
perform production edits, test edits, roadmap mutation, controller-state edits,
bug-tracker updates, replay reopen, or subject widening.

The inherited boundary remains fixed and unchanged:

- explicit-only recursive baseline;
- non-equi-recursive semantics;
- non-cyclic structural graph encoding;
- no second executable interface; and
- no compatibility, convenience, or default-path widening.

This artifact therefore does not clear authority, uniqueness, owner stability,
or constructor admissibility, and it does not authorize broad automatic
recursive inference beyond repaired `URI-R2-C1`.

## Accepted `F2` / `F3` / `F4` Evidence Chain Carried Forward Without Reopening

Only accepted bounded predecessor facts are carried forward here:

1. `F2` authoritative `attempt-1` landed only the bounded local-binding
   scheme-alias/base-like `rootLocalSchemeAliasBaseLike` / `targetC -> rootFinal`
   fail-closed slice in
   `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-046/src/MLF/Elab/Run/ResultType/Fallback.hs`
   and
   `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-046/test/PipelineSpec.hs`.
   It did not reopen replay repair, `MLF.Elab.Inst`, `InstBot`, or any broader
   trigger family.
2. `F3` authoritative `attempt-1` reverified that exact `F2` local-binding
   scheme-alias/base-like slice under read-only anchor inspection, a fresh
   focused `ARI-C1 feasibility characterization (bounded prototype-only)` rerun,
   a fresh full repo gate, predecessor continuity checks, and docs-only diff
   review. The accepted baseline therefore remains that `F2` lane only.
3. `F4` authoritative `attempt-1` recorded `continue-bounded`, not
   `widen-approved` and not `stop-blocked`. The only lawful successor action is
   one more bounded non-widening cycle from the accepted `F2` / `F3` baseline.
4. Accepted `E2` / `E3` remain inherited same-lane retained-child context only.
   They are baseline history, not reopened target-selection authority for `G1`.
5. Accepted negative findings remain binding: `U2` stays `authority-narrowed`,
   `U3` stays `uniqueness-owner-stable-refuted`, and `U4` stays
   `constructor-acyclic-termination-refuted`. `G1` does not reinterpret any of
   them as implementation clearance.
6. `/Volumes/src/mlf4/Bugs.md` remains repaired-lane continuity context only.
   Resolved replay-path bug `BUG-2026-03-16-001` does not authorize replay
   reopen, `MLF.Elab.Inst`, `MLF.Elab.Inst.applyInstantiation`, `InstBot`, or
   broader recursive-inference work in this round.

`G1` starts from the accepted `F2` / `F3`
`rootLocalSchemeAliasBaseLike` baseline unchanged. It does not reopen replay
repair, `MLF.Elab.Inst.applyInstantiation`, `InstBot`, or the completed
`E2` / `E3` / `F1` / `F2` / `F3` / `F4` chain.

## Exact Selected `G2` Target (Exactly One)

The only frozen future `G2` target is:

local-binding `rootHasMultiInst` fail-closed hardening in
`/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-046/src/MLF/Elab/Run/ResultType/Fallback.hs`
limited to the already-computed per-root multiplicity trigger at
`Fallback.hs:240-248` and the downstream local `keepTargetFinal` / `targetC`
selection in `Fallback.hs:668-693`.

Required interpretation of that one bounded slice:

- start from the accepted `F2` / `F3` baseline unchanged:
  `rootLocalSchemeAliasBaseLike`, `targetC -> rootFinal`, and the accepted
  scheme-alias/base-like `ARI-C1` examples remain inherited starting
  conditions, not reopened target families;
- keep the selected work confined to the adjacent local-binding case where
  `keepTargetFinal` is carried solely by local `rootHasMultiInst` proof
  itself;
- treat `instArgRootMultiBase`, `rootLocalSchemeAliasBaseLike`, and
  `boundVarTarget` as absent or non-authoritative for this selected slice, so
  the repaired lane is not widened into mixed trigger-family selection work;
- keep the repaired `URI-R2-C1` lane fail-closed whenever the selected case
  would require `instArgRootMultiBase`, `boundVarTarget`, non-local binding,
  replay reopen, `MLF.Elab.Inst`, `InstBot`, equi-recursive reasoning, cyclic
  structural encoding, or a second interface as authority; and
- no other `keepTargetFinal` trigger family is selected by this artifact.

### Why `rootHasMultiInst` Is The Narrower Remaining Family

`rootHasMultiInst` is the narrower remaining unopened trigger family because it
is a single per-root multiplicity gate computed directly from `rootInstRoots`
and `edgeTraceCounts` in `Fallback.hs:240-248`.

`instArgRootMultiBase` is not selected in this cycle because its current
computation aggregates multiple base-bound sources across witnesses,
binder-argument copies, and edge/root bound maps in `Fallback.hs:289-359`. That
family would widen the next slice beyond the smallest reviewer-auditable step.

### Future File Ownership

Future `G2` ownership is frozen to exactly these files only:

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-046/src/MLF/Elab/Run/ResultType/Fallback.hs`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-046/test/PipelineSpec.hs`

### Future Focused Coverage Intent

Future `G2` coverage remains one bounded extension of the existing
`ARI-C1 feasibility characterization (bounded prototype-only)` block in
`/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-046/test/PipelineSpec.hs`
only:

- add one local-binding multi-inst case that directly exercises the selected
  `rootHasMultiInst` lane without relying on `instArgRootMultiBase`,
  `rootLocalSchemeAliasBaseLike`, or `boundVarTarget`; and
- add one matched fail-closed contrast where the same-looking wrapper leaves the
  local `TypeRef` lane and therefore must still reject or fall back
  non-recursively.

No second implementation family is selected in this artifact.

## Explicit Non-Selection And Non-Authorization

This `G1` bind does not authorize:

- edits to `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-046/src/MLF/Elab/Inst.hs`;
- replay-repair reopen, including `MLF.Elab.Inst.applyInstantiation` /
  `InstBot`, as active `G2` work;
- reopening the completed `E2` / `E3` / `F1` / `F2` / `F3` / `F4` chain;
- `instArgRootMultiBase` as a separate `G2` target family in this cycle;
- `boundVarTarget` widening or non-local binding widening;
- prototype/research entrypoints or a second executable interface;
- equi-recursive reasoning or implicit unfolding;
- cyclic structural graph encoding;
- multi-SCC or cross-family widening;
- heuristic ownership ranking or manufactured authority;
- compatibility shims, convenience fallbacks, or default-path widening; or
- roadmap, controller-state, or bug-tracker edits as part of this bind.

Any broader target family requires a separate accepted roadmap change before
implementation.

## Retry-Aware Reviewer Handoff

This artifact is the immutable `attempt-1` bind/selection record for `G1`.
If `round-046` retries later, future attempts must be additive and must not
rewrite this artifact.

Per `orchestrator/retry-subloop.md`, reviewer outcomes supported by this
artifact remain:

- `accepted + finalize`
- `accepted + retry`
- `rejected + retry`

Reviewer record fields expected for this round remain:

- `Implemented stage result`
- `Attempt verdict`
- `Stage action`
- `Retry reason`
- `Fix hypothesis`

## Verification Notes

Commands executed in:
`/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-046`

### Baseline Commands

- `git diff --check` -> pass (no output)
- `python3 -m json.tool orchestrator/state.json >/dev/null` -> pass
- `rg -n '"contract_version": 2|"retry": null|"retry": \{' orchestrator/state.json`
  -> pass:
  - `2:  "contract_version": 2,`
  - `13:  "retry": null`
- `rg -n '^\d+\. \[(pending|in-progress|done)\]' orchestrator/roadmap.md` -> pass:
  - items `1` through `16` remain parseable;
  - item `13` (`G1`) remains pending for this pre-merge implement attempt;
  - items `14` through `16` remain pending exactly as expected for pre-merge
    `G1`.
- `test -f docs/superpowers/specs/2026-03-18-unannotated-iso-recursive-continue-bounded-cycle-design.md`
  -> pass
- `test -f docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  -> pass
- `test -f docs/plans/2026-03-14-unannotated-iso-recursive-r5-research-stop-decision.md`
  -> pass
- `test -f docs/plans/2026-03-17-uri-r2-c1-r4-repair-decision-gate.md` -> pass
- `test -f docs/plans/2026-03-17-uri-r2-c1-u6-next-widening-decision-gate.md`
  -> pass
- `test -f orchestrator/retry-subloop.md` -> pass

### `G1`-Specific Checks

- `test -f docs/plans/2026-03-19-uri-r2-c1-f4-next-cycle-decision-gate.md`
  -> pass
- `python3 -m json.tool orchestrator/rounds/round-045/review-record.json >/dev/null`
  -> pass
- short `python3` assertion over `orchestrator/rounds/round-045/review-record.json`
  -> pass:
  - confirmed `F4` / `attempt-1` / `accepted` / `finalize` / `authoritative`
    metadata;
  - confirmed canonical artifact path
    `docs/plans/2026-03-19-uri-r2-c1-f4-next-cycle-decision-gate.md`;
  - confirmed required `F4-*` pass keys remained present.
- `rg -c '^The only frozen future `G2` target is:$' docs/plans/2026-03-19-uri-r2-c1-g1-next-target-bind.md`
  -> pass:
  - `1`
- `awk 'NR<30 {print}' docs/plans/2026-03-19-uri-r2-c1-g1-next-target-bind.md | rg -n 'Round: `round-046`|Roadmap item: `G1`|Stage: `implement`|Attempt: `attempt-1`|Retry state: `null`|Live subject: repaired `URI-R2-C1`|explicit-only recursive baseline|non-equi-recursive semantics|non-cyclic structural graph encoding|no second executable interface|no compatibility, convenience, or default-path widening'`
  -> pass:
  - metadata and inherited-boundary lines present in the artifact header and
    stage-contract section.
- `sed -n '70,145p' docs/plans/2026-03-19-uri-r2-c1-g1-next-target-bind.md | rg -n 'The only frozen future `G2` target is:|rootHasMultiInst|Fallback\\.hs:240-248|Fallback\\.hs:668-693|instArgRootMultiBase|rootLocalSchemeAliasBaseLike|boundVarTarget|Future `G2` ownership is frozen|/src/MLF/Elab/Run/ResultType/Fallback.hs|/test/PipelineSpec.hs|No second implementation family is selected'`
  -> pass:
  - selected target, frozen ownership, explicit non-selection of
    `instArgRootMultiBase`, and focused coverage intent are all recorded.
- `sed -n '30,165p' docs/plans/2026-03-19-uri-r2-c1-g1-next-target-bind.md | rg -n '`authority-narrowed`|`uniqueness-owner-stable-refuted`|`constructor-acyclic-termination-refuted`|does not reinterpret any of them as implementation clearance|replay reopen|MLF\\.Elab\\.Inst|InstBot|E2|E3|F1|F2|F3|F4|Bugs\\.md remains repaired-lane continuity context only'`
  -> pass:
  - accepted negative findings, predecessor continuity-only status, and
    non-reopen boundaries are all recorded.

### Docs-Only Diff Evidence

This round remains docs-only. No production, test, public API, executable,
Cabal, roadmap, bug-tracker, or controller-state files were edited.

- `git status --short --untracked-files=all` -> docs/orchestrator-only change set:
  - `?? docs/plans/2026-03-19-uri-r2-c1-g1-next-target-bind.md`
  - `?? orchestrator/rounds/round-046/implementation-notes.md`
  - `?? orchestrator/rounds/round-046/plan.md`
  - `?? orchestrator/rounds/round-046/selection.md`
- `git diff --name-only` -> no tracked diffs
- `git diff --name-only -- . ':(exclude)docs/**' ':(exclude)orchestrator/**'` ->
  no output

### Full-Gate Skip Note

`cabal build all && cabal test` was intentionally not run in `G1` `attempt-1`
because this round is docs-only and does not edit `src/`, `src-public/`,
`app/`, `test/`, or `mlf2.cabal`.
