# `RE4` Bounded Re-Entry Gate For `URI-R2-C1`

Date: 2026-03-15
Round: 014
Roadmap item: 4
Stage: `RE4` bounded re-entry gate
Active subject: `URI-R2-C1`
Artifact kind: bounded gate decision only
Decision labels permitted: `reopen-handoff-track` or `not-yet-reopen`

## Purpose

This document executes roadmap item 4 by evaluating inherited evidence for `URI-R2-C1` against the accepted `RE1`, `RE2`, and `RE3` prerequisites and recording one explicit bounded re-entry outcome.

This document is a gate decision over inherited evidence only. It is not a new evidence-contract stage, not a prototype request, not a handoff-track design, not an `RE5` recommendation, and not implementation clearance.

## Inherited Authority Chain

This round is controlled by inherited accepted artifacts rather than by new prototype or implementation evidence.

Direct authority inputs for this gate:

- `docs/superpowers/specs/2026-03-14-uri-r2-c1-reentry-roadmap-design.md` defines `RE4` as the bounded re-entry gate in the approved `RE1` -> `RE5` ladder.
- `docs/plans/2026-03-14-unannotated-iso-recursive-r5-research-stop-decision.md` preserves the accepted bounded `research-stop` and the exact re-entry requirements that later stages must judge.
- `docs/plans/2026-03-14-uri-r2-c1-re1-provenance-authority-evidence-contract.md` defines the provenance-authority gate for `URI-R3-O4`.
- `docs/plans/2026-03-14-uri-r2-c1-re2-uniqueness-evidence-contract.md` defines the uniqueness gate for `URI-R3-O5`.
- `docs/plans/2026-03-14-uri-r2-c1-re3-positive-evidence-contract.md` defines the prototype-free positive-evidence gate for `URI-R3-O1` through `URI-R3-O3`.
- `docs/plans/2026-03-14-automatic-recursive-inference-invariant-audit.md` remains the authoritative inherited audit for acyclicity, binding-tree discipline, occurs-check or termination boundaries, reconstruction or reification or witness replay, and principality-risk boundaries.
- `orchestrator/rounds/round-014/selection.md` is the current round-selection authority for this gate only.

Interpretation:

- `RE4` may record only one explicit gate result: `reopen-handoff-track` or `not-yet-reopen`.
- `RE4` carries forward the accepted `R5` stop, the accepted `RE1` / `RE2` / `RE3` contracts, and the inherited invariant audit instead of inventing new replay, uniqueness, ownership, search, acyclicity, or termination semantics.
- The existence of `RE1`, `RE2`, and `RE3` documents is not itself affirmative evidence that their gates are satisfied.

## Fixed Boundary

The active subject and boundary remain unchanged and mandatory:

- `URI-R2-C1` only;
- `single-SCC` obligation-level recursion only;
- `single-binder-family` ownership only;
- no cross-family SCC linking;
- non-equi-recursive semantics only;
- non-cyclic structural graph only;
- no default-on widening;
- prototype-free evidence only.

This gate may not broaden provenance roots, comparison sets, ownership families, SCC scope, search scope, or graph semantics. If a reopening argument needs any such broadening, the gate remains closed.

## Gate Inputs Under Review

The exact prerequisites under review are:

- provenance authority for `URI-R3-O4` per `RE1`;
- uniqueness for `URI-R3-O5` per `RE2`;
- positive evidence for `URI-R3-O1`, `URI-R3-O2`, and `URI-R3-O3` per `RE3`;
- continuity with the inherited invariant audit and the accepted `R5` bounded stop.

## Evaluation Checklist

### 1. `RE1` provenance authority for `URI-R3-O4`

Inherited evidence actually present:

- `RE1` defines a reviewer-checkable contract for one finite local provenance root or equivalent local cluster inside `URI-R2-C1`.
- `RE1` explicitly rejects manufactured provenance authority, late repair, replay-domain widening, surrogate-root substitution, unstable binder naming, binder-family drift, competing roots, competing clusters, heuristic ranking, widened provenance search, prototype-backed claims, experiment-backed claims, and implementation drift.
- `R5` records `URI-R3-O4` as still unresolved and names the missing requirement as authoritative prototype-free evidence for one provenance-stable unannotated root or cluster.

Gate status:

- Unsatisfied.

Reason:

- The inherited repository evidence supplies a contract that says what would count as provenance authority, but it does not supply affirmative inherited evidence that one bounded provenance-stable root or equivalent local cluster already survives `generalizeWithPlan -> schemeToType -> reifyTypeWithNamedSetNoFallback` and witness replay without manufactured authority or late repair.

### 2. `RE2` uniqueness for `URI-R3-O5`

Inherited evidence actually present:

- `RE2` defines a reviewer-checkable contract for uniqueness inside one already-bounded local subject admitted by `RE1`.
- `RE2` explicitly rejects heuristic ranking, tie-breaking, competing roots, competing clusters, widened comparison, widened search, non-local propagation, replay-domain widening, owner repair, provenance repair, multi-cluster salvage, multi-SCC salvage, prototype-backed claims, experiment-backed claims, and implementation drift.
- `R5` records `URI-R3-O5` as still unresolved and names the missing requirement as bounded uniqueness evidence without heuristic ranking.

Gate status:

- Unsatisfied.

Reason:

- The inherited repository evidence supplies the uniqueness contract only. It does not supply affirmative inherited evidence that exactly one admissible bounded interpretation survives inside the fixed `URI-R2-C1` subject without heuristic ranking, tie-breaking, competing local roots or clusters, widened comparison, or repair.

### 3. `RE3` positive evidence for `URI-R3-O1` through `URI-R3-O3`

Inherited evidence actually present:

- `RE3` defines reviewer-checkable positive-evidence requirements for one local obligation SCC, one acyclic binder-mediated structural slice, one closed binder family with deterministic parent-chain ownership, and one constructor-directed local reasoning path.
- `RE3` explicitly rejects prototype-backed claims, experiment-backed claims, implementation drift, explicit-anchor replay substitution used to manufacture evidence, widened ownership, owner repair, parent-chain repair, widened search, non-local propagation, multi-cluster salvage, multi-SCC salvage, cyclic structural encoding, structural back-edge dependence, implicit unfolding, equi-recursive reasoning, and termination weakening.
- `R5` records `URI-R3-O1`, `URI-R3-O2`, and `URI-R3-O3` as still unresolved and names the missing requirement as bounded prototype-free positive evidence without implicit unfolding, cyclic graphs, or widened ownership or search.

Gate status:

- Unsatisfied.

Reason:

- The inherited repository evidence supplies the positive-evidence contract only. It does not supply affirmative inherited evidence that the same bounded subject already satisfies structural acyclicity, deterministic single-family ownership, and constructor-directed occurs-check or termination clearance within the fixed boundary and without any rejected evidence source.

### 4. Continuity with the inherited invariant audit and accepted `R5` stop

Inherited evidence actually present:

- The invariant audit remains authoritative for non-cyclic structural-graph requirements, binding-tree discipline, occurs-check or termination boundaries, reconstruction or reification or witness replay, and principality-risk boundaries.
- `R5` remains the accepted bounded `research-stop` and states that any later revisit must first provide bounded evidence rather than implementation planning.
- The approved re-entry design requires `RE4` to evaluate whether the bounded evidence gathered against `RE1`, `RE2`, and `RE3` is sufficient to reopen a later handoff-track design.

Gate status:

- Satisfied as a continuity constraint, but fail-closed for reopening.

Reason:

- Continuity is preserved, but that continuity does not override the unsatisfied prerequisite gates above. The inherited audit and accepted stop remain controlling until affirmative inherited evidence exists.

## Fail-Closed Triggers

This gate must remain closed if any reopening argument depends on:

- prototype-backed or experiment-backed evidence;
- implementation drift;
- manufactured provenance authority, late repair, or widened replay domains;
- heuristic ranking, tie-breaking, or widened comparison;
- widened ownership, owner repair, parent-chain repair, or cross-family reasoning;
- widened search, non-local propagation, multi-cluster salvage, or multi-SCC salvage;
- cyclic structural encoding, implicit unfolding, equi-recursive reasoning, or termination weakening.

Any one of these triggers is enough to block reopening because this round is docs-only, prototype-free, and fail-closed.

## Decision

Decision outcome: `not-yet-reopen`

Justification:

- `RE1` is unsatisfied because the inherited materials define provenance-authority requirements but do not provide affirmative inherited evidence that one bounded provenance-stable unannotated root or equivalent local cluster already carries replay and reification authority without manufactured authority or late repair.
- `RE2` is unsatisfied because the inherited materials define bounded uniqueness requirements but do not provide affirmative inherited evidence that exactly one admissible bounded interpretation survives without heuristic ranking, tie-breaking, competing roots or clusters, widened comparison, or repair.
- `RE3` is unsatisfied because the inherited materials define bounded prototype-free positive-evidence requirements but do not provide affirmative inherited evidence that `URI-R3-O1` through `URI-R3-O3` are already cleared for the same bounded subject without widened ownership or search, cyclic structural encoding, implicit unfolding, equi-recursive reasoning, or termination weakening.

Because at least one prerequisite remains unsatisfied and clearing those gaps would require evidence not already present in inherited accepted artifacts, the bounded gate must stay closed. The explicit `RE4` result is `not-yet-reopen`.

## Unsatisfied Conditions

The exact bounded conditions still missing for `URI-R2-C1` are:

- affirmative inherited evidence for one finite local provenance root or one equivalent local cluster that remains the same bounded subject across generalization, reconstruction, reification, and witness replay without manufactured authority, late repair, replay-domain widening, or binder-family drift;
- affirmative inherited evidence that this same bounded subject is uniquely admissible inside `URI-R2-C1` without competing local roots or clusters, heuristic ranking, tie-breaking, widened comparison, or repair;
- affirmative inherited evidence that this same bounded subject already satisfies:
  - one local obligation SCC only with one acyclic binder-mediated structural slice;
  - one closed binder family only with one deterministic parent-chain owner for every obligation; and
  - one constructor-directed local reasoning path with no implicit unfolding, no equi-recursive reasoning, and no termination weakening.

These missing conditions remain bounded to `URI-R2-C1` only. They do not authorize widening the subject, importing new comparison classes, or broadening ownership or search.

## Successor Boundary

Because the result is `not-yet-reopen`, the accepted bounded stop remains in force until later bounded evidence exists inside the same `URI-R2-C1` boundary.

This document does not itself write `RE5`, does not draft a handoff-track, and does not authorize implementation work. If a later gate ever records `reopen-handoff-track`, the only authorized consequence would be that a later roadmap may design a bounded handoff-track for `URI-R2-C1`.

## Continuity

Completed rounds `001` through `013` and the predecessor recursive-types packet remain inherited evidence only and are not rewritten here.

Continuity statement:

- inherited accepted rounds remain historical authority;
- the accepted `R5` stop remains authoritative until later bounded evidence exists;
- the approved re-entry design remains the staging authority for `RE1` through `RE5`;
- the inherited invariant audit remains controlling for safety boundaries; and
- this round does not reopen or rewrite predecessor packet logs or prior round artifacts.
