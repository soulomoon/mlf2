# General Automatic Iso-Recursive Full-Inference Negative-Family And Termination-Pressure Aggregate Classification

Date: 2026-04-05
Round: `round-192`
Roadmap item: `item-6`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: one docs-only aggregate classification for the representative
negative-family and termination-pressure evidence only
Artifact kind: canonical docs-only item-6 negative-family and
termination-pressure aggregate classification record

## Stage Contract Freeze

This artifact implements only roadmap item `6` for `attempt-1` with
`retry: null`.

This round is docs-only, aggregate-only, evidence-only,
current-architecture-only, and non-widening. Its job is to classify the
already-authoritative representative negative / pressure ledger into one
honest current-architecture read for:

- `N1 ambiguity-reject`
- `N2 unsoundness-guard`
- `N6 termination-pressure`

The admissible evidence surface is fixed to the already-frozen item-4
authoritative rows only:

- `test/Research/P5ClearBoundarySpec.hs` for the clear-boundary control and
  the reject-side `nestedForallContrastExpr` contrast;
- `test/PipelineSpec.hs` for the authoritative current-entrypoint fail-closed
  local-vs-non-local contrasts, the no-ranking / no-neighboring-route source
  guards, and the bounded same-lane lookup checks; and
- `src/MLF/Elab/Run/ResultType/Fallback/Core.hs` as the read-only route /
  guard anchor for `rootNonLocalSchemeAliasBaseLike`,
  `sameLaneLocalRetainedChildTarget`, `boundHasForallFrom`,
  `keepTargetFinal`, and `targetC`.

No test or production edit was needed in this round. The focused reruns make
all three representative rows reviewer-visible on the authoritative surfaces
as they already stand, so step `2` of the round plan stays a no-op.

This artifact does not authorize:

- any `src/`, `src-public/`, `app/`, `test/`, or `mlf2.cabal` change;
- any new harness, helper, corpus row, or widened representative family;
- any reopening of the item-3 route / guard contract;
- any cyclic search, multi-SCC search, equi-recursive reinterpretation,
  fallback widening, or second-interface work;
- any reopening of `non-cyclic-graph`;
- any move into `item-7`; or
- any repo-level readiness claim.

The inherited boundary therefore remains controlling here:
explicit-only, iso-recursive, non-equi-recursive, `non-cyclic-graph = unknown`,
and no-fallback.

## Authority Ledger

| Source | Binding read carried forward here |
| --- | --- |
| `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md` | Keeps the explicit-only / iso-recursive / non-equi-recursive / non-cyclic-graph baseline live and forbids silent widening. |
| `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-capability-contract-and-evaluation-corpus.md` | Preserves `N1`, `N2`, and `N6` as representative family obligations rather than incidental test folklore. |
| `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md` | Keeps `non-cyclic-graph = unknown`, keeps no-fallback, and preserves the inherited current-architecture boundary while representative negative pressure is still being read. |
| `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md` | Fixes the negative-side vocabulary used here, including `fail-closed rejection`, and keeps ambiguity, quantified crossing, and unsoundness pressure on the reject side unless later evidence lawfully upgrades them. |
| `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md` | Preserves the representative matrix posture in which negative rows remain evidence gates rather than positive support. |
| `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architecture-decision-and-successor-plan-choice.md` | Keeps the March strategic posture bounded and below any premature architecture-decision or readiness claim. |
| `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md` | Preserves the refreshed same-lane predecessor tuple and review ledger as predecessor context only. |
| `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-path-audit.md` | Keeps the same-lane authoritative-path account visible as predecessor context, including the bounded same-lane `boundHasForallFrom` / `sameLaneLocalRetainedChildTarget` / `keepTargetFinal` / `targetC` vocabulary. |
| `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-collapse-clear-or-confirm.md` | Preserves that same-pocket authoritative collapse did not itself justify a broader architecture claim. |
| `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md` | Keeps the predecessor same-lane revalidation bounded and below general automatic capability. |
| `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md` | Preserves that predecessor pressure did not itself reopen `non-cyclic-graph`. |
| `docs/plans/2026-04-02-general-automatic-iso-recursive-full-inference-predecessor-authority-unresolved-semantic-matrix-family-success-bar-and-first-concrete-deliverable-freeze.md` | Freezes `N1`, `N2`, and `N6` as still-live negative obligations inside the inherited current architecture. |
| `docs/plans/2026-04-02-general-automatic-iso-recursive-full-inference-current-architecture-semantic-mechanism-map.md` | Names `rootNonLocalSchemeAliasBaseLike` and `sameLaneLocalRetainedChildTarget` as current route families and keeps nested-`forall` interaction, ambiguity, and boundedness as still-live pressure rather than settled positive success. |
| `docs/plans/2026-04-03-general-automatic-iso-recursive-full-inference-fail-closed-candidate-generation-ambiguity-rejection-and-bounded-termination-discipline.md` | Freezes admissibility to the named route arms plus the `boundHasForallFrom` / `keepTargetFinal` / `targetC` guard cluster only, rejects ambiguous or unsafe cases, and explains why `N6` must stay bounded. |
| `docs/plans/2026-04-03-general-automatic-iso-recursive-full-inference-reconstruction-visible-readiness-contract-and-authoritative-evaluation-surfaces.md` | Makes `runPipelineElab` and `runPipelineElabChecked` the authoritative surfaces for representative `N1`, `N2`, and `N6` reruns. |
| `docs/plans/2026-04-05-general-automatic-iso-recursive-full-inference-positive-family-aggregate-classification.md` | Keeps item `5` closed and leaves this negative-family / termination-pressure read, `non-cyclic-graph`, and repo-level readiness unresolved. |
| `orchestrator/rounds/round-179/review-record.json` | Accepted item `3` authority: ambiguity, unsoundness, and boundedness are frozen to the current named route / guard cluster only. |
| `orchestrator/rounds/round-180/review-record.json` | Accepted item `4` authority: the authoritative evaluation surfaces and representative corpus obligations are fixed to the current test surfaces. |
| `orchestrator/rounds/round-191/review-record.json` | Accepted item `5` authority: positive-family closure did not settle this negative-family campaign and explicitly left item `6` open. |

## Representative Evidence Ledger

| Representative row | Evidence anchors used in this round | Reviewer-visible read on the authoritative current surfaces | Step-2 status |
| --- | --- | --- | --- |
| `N1 ambiguity-reject` | `test/PipelineSpec.hs`: `keeps the same single-base wrapper fail-closed once it leaves the local TypeRef lane`, `keeps the same multi-inst wrapper fail-closed once it leaves the local TypeRef lane`, `keeps the same inst-arg multi-base wrapper fail-closed once it leaves the local TypeRef lane`, `keeps the same inst-arg-only singleton-base wrapper fail-closed once it leaves the local TypeRef lane`, and `keeps the explicit non-local scheme-alias/base-like proof separate from the preserved local lanes`; `src/MLF/Elab/Run/ResultType/Fallback/Core.hs`: `rootNonLocalSchemeAliasBaseLike`, `sameLaneLocalRetainedChildTarget`, `keepTargetFinal`, `targetC` | Once the wrapper leaves the owner-local `TypeRef` lane, the current authoritative read stays on the quantified fail-closed shell instead of ranking candidates, guessing a wildcard route, or collapsing the local lanes into the explicit non-local scheme-alias / base-like arm. | `no-op`: existing tests already make the fail-closed contrast explicit. |
| `N2 unsoundness-guard` | `test/Research/P5ClearBoundarySpec.hs`: `keeps the clear-boundary control recursive on both current pipeline entrypoints` and `reports PhiTranslatabilityError at pipeline entrypoints as a downstream consequence of correct non-recursive nested-forall outcome`; `src/MLF/Elab/Run/ResultType/Fallback/Core.hs`: `boundHasForallFrom`, `sameLaneLocalRetainedChildTarget` | The clear-boundary control remains recursive, but the nested-`forall` contrast remains non-recursive and reject-side on both authoritative entrypoints, surfacing `PhiTranslatabilityError` rather than silently admitting a quantified crossing. | `no-op`: existing tests already make the reject-side boundary explicit. |
| `N6 termination-pressure` | `test/PipelineSpec.hs`: `keeps retained-child lookup bounded to the same local TypeRef lane`, the four `fail-closed once it leaves the local TypeRef lane` contrast tests, and `keeps the explicit non-local scheme-alias/base-like proof separate from the preserved local lanes`; `src/MLF/Elab/Run/ResultType/Fallback/Core.hs`: `sameLaneLocalRetainedChildTarget`, `keepTargetFinal`, `targetC`, `rootNonLocalSchemeAliasBaseLike` | The same-lane lookup stays bounded to the owner-local `TypeRef` lane, the explicit non-local arm stays separate, and leaving the local lane does not reopen neighboring routes or trigger ranking rescue. The negative pressure therefore remains bounded because forbidden widened search never enters the lawful ledger. | `no-op`: existing tests already make the boundedness read explicit. |

These three rows are the full admissible evidence ledger for this artifact.
Everything else remains authority or boundary context only.

## Row Classification Matrix

| Negative / pressure row | Evidence anchors used | Classification | Strongest lawful read |
| --- | --- | --- | --- |
| `N1 ambiguity-reject` | `test/PipelineSpec.hs` fail-closed local-vs-non-local contrast tests and explicit separation guard; `src/MLF/Elab/Run/ResultType/Fallback/Core.hs` named route split | `fail-closed rejection` | The current architecture still refuses to treat leaving the local `TypeRef` lane as permission to guess among competing survivors. The preserved local lanes stay separate from `rootNonLocalSchemeAliasBaseLike`, and the non-local contrasts stay on the quantified fail-closed shell instead of ranking or widening among candidates. |
| `N2 unsoundness-guard` | `test/Research/P5ClearBoundarySpec.hs` clear-boundary control and `nestedForallContrastExpr` entrypoint checks; `src/MLF/Elab/Run/ResultType/Fallback/Core.hs` `boundHasForallFrom` filter | `fail-closed rejection` | The current architecture still treats quantified-boundary crossing as reject-side pressure, not as positive recursive success. The clear-boundary control stays recursive, but the nested-`forall` contrast remains non-recursive and downstream-failing on both authoritative entrypoints, which is the intended soundness guard. |
| `N6 termination-pressure` | `test/PipelineSpec.hs` bounded same-lane lookup check, fail-closed local-vs-non-local contrasts, and explicit non-local separation guard; `src/MLF/Elab/Run/ResultType/Fallback/Core.hs` `sameLaneLocalRetainedChildTarget`, `keepTargetFinal`, `targetC`, `rootNonLocalSchemeAliasBaseLike` | `fail-closed rejection` | The current architecture keeps this pressure row bounded by preventing forbidden widened search from entering the lawful route ledger at all. Same-lane lookup stays owner-local, the explicit non-local arm stays explicit, and local wrappers that leave the lane fail closed rather than reopening neighboring routes, adding wildcard rescue, or growing beyond the fixed item-3 anchor set. |

## Bounded Conclusion

The strongest lawful item-6 aggregate read from the representative current
negative-family / termination-pressure evidence is:

- `N1 ambiguity-reject` = `fail-closed rejection`
- `N2 unsoundness-guard` = `fail-closed rejection`
- `N6 termination-pressure` = `fail-closed rejection`

This is still meaningful progress for item `6`: the current authoritative
surfaces now have one frozen aggregate read showing that the representative
ambiguity, quantified-boundary, and bounded-termination pressure rows remain
reject-side and bounded under the unchanged current architecture.

But that conclusion is intentionally narrow.

It does not claim a broader negative-family closure beyond `N1`, `N2`, and
`N6`.
It does not reinterpret reject-side nested-`forall` behavior as positive
`P5` support.
It does not reopen `non-cyclic-graph`.
It does not move into `item-7`.
It does not claim repo-level readiness.

## Non-Claims

This artifact does not claim any of the following:

- that `N3`, `N4`, or `N5` have now been revalidated or classified;
- that any ambiguous, owner-crossing, or quantified-crossing case has become
  positive success;
- that the item-3 route / guard contract needs revision;
- that boundedness here came from cyclic search, multi-SCC search, fallback,
  or any second interface;
- that solver-only, helper-only, or source-only observations would be enough
  without the authoritative-surface reruns used above; or
- that the repository is ready for a general automatic iso-recursive
  inference claim.
