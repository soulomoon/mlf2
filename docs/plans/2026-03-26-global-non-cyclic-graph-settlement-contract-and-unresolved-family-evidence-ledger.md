# Global Non-Cyclic-Graph Settlement Contract And Unresolved Family Evidence Ledger

Date: 2026-03-26
Status: canonical item-1 artifact for `round-099` implement stage,
`attempt-1`, `retry: null`.

## Scope And Inherited Boundary

This artifact executes roadmap item `1` only. It is docs-only and
contract-freeze-only. It freezes the exact later acceptance bar for
`non-cyclic-graph = keep` versus
`reopen the non-cyclic-graph revision question`, freezes the current
representative-family ledger, and preserves the accepted predecessor
evidence chain without selecting either outcome now.

The inherited boundary remains unchanged:

- explicit recursive annotations remain the production baseline;
- recursive meaning remains iso-recursive only;
- no equi-recursive equality or implicit unfolding is authorized;
- no cyclic structural graph encoding or multi-SCC search is authorized;
- no second interface is authorized; and
- no compatibility, convenience, or default-path fallback widening is
  authorized.

This round does not authorize production implementation, hardening, or final
capability-claim planning. No production implementation work is lawful before
accepted item `5` records `non-cyclic-graph = keep`.

Accepted rounds `round-094` through `round-098` remain
bounded exact-pocket predecessor evidence only. They preserve one exact
same-lane retained-child pressure chain and do not settle the repo-level
`non-cyclic-graph` question by themselves.

## Current Strategic Posture

The accepted strategic posture carried into this item-1 freeze is:

- `iso-recursive = keep`
- `non-equi-recursive = keep`
- `non-cyclic-graph = unknown`
- `no-fallback = keep`

The strongest accepted aggregate read remains
`continue within the current architecture`, while repo-level
`non-cyclic-graph` settlement remains unresolved. The accepted record contains
bounded admitted pockets and fail-closed discipline, but it does not yet
justify either `non-cyclic-graph = keep` or
`reopen the non-cyclic-graph revision question`.

## Exact Item-5 Settlement Gate Contract

Item `5` may record exactly one later outcome.

| Lawful later outcome | Acceptance bar | Why weaker evidence fails closed |
| --- | --- | --- |
| `non-cyclic-graph = keep` | Accepted production-surface evidence must make representative settlement credible inside the inherited acyclic model across positive families `P1` through `P6` and required negative or bounded families `N1` through `N6`. The evidence must stay on the existing solver -> elaboration -> reconstruction -> internal/public output surfaces and must satisfy the accepted output vocabulary, including `stable visible persistence` where positive `P6` success is claimed. | Solver-only wins, helper-only visibility, witness-only inspection, one bounded admitted pocket, one blocker-debt pocket, one strategic read of `continue within the current architecture`, or one accepted exact-pocket chain are all insufficient by themselves. |
| `reopen the non-cyclic-graph revision question` | Accepted evidence must show that at least one required positive family cannot be carried inside the inherited acyclic model without crossing the current architecture boundary into cyclic structure, multi-SCC search, second interfaces, fallback behavior, or another explicitly reopened boundary. | One unresolved `unknown` audit axis, one bounded same-lane retained-child blocker, or one bounded non-local blocker does not prove that the inherited acyclic model is impossible at repo scope. |

Item `1` selects neither outcome. It freezes the bar only.

The following are explicitly insufficient by themselves to settle item `5`:

- one bounded admitted pocket;
- one `admitted but not reconstruction-visible / blocker debt` pocket;
- one accepted `continue within the current architecture` strategic read; or
- one unresolved `non-cyclic-graph = unknown` pressure axis in the audit.

## Unresolved Family Evidence Ledger

### Positive families

| Family | Frozen repo-level read | Accepted predecessor evidence now binding | Why the family is not settled yet |
| --- | --- | --- | --- |
| `P1 local-recursive-shape` | bounded predecessor support exists inside the inherited acyclic model | the baseline contract keeps explicit recursive transport complete, and the architectural audit says `non-cyclic-graph` directly supports `P1` inside the current representation | one local-shape family alone does not settle the repo-level gate or prove representative matrix sufficiency |
| `P2 non-local-propagation` | unresolved at repo scope | row `C1` records one admitted non-local alias-bound pocket as `admitted but not reconstruction-visible / blocker debt`; the architectural audit keeps `P2` unresolved | accepted evidence does not yet show reviewable production-surface continuity for non-local propagation beyond one bounded pocket |
| `P3 retained-child-owner-sensitive` | unresolved at repo scope | rows `C1`, `C2`, and `C5` preserve bounded owner-sensitive pressure; accepted rounds `round-094` through `round-098` feed bounded same-lane retained-child evidence only | retained-child and alias-bound pressure remain partially admitted, not generally cleared across owner-sensitive placements |
| `P4 binder-sensitive-placement` | unresolved at repo scope | rows `C2` and `C5` preserve one same-lane retained-child / owner-local frame as bounded pressure evidence | the accepted record contains one exact same-lane pocket only, not general binder-sensitive placement success |
| `P5 polymorphism-nested-forall` | unresolved and reject-side only on accepted evidence | row `C3` records nested-`forall` / quantified-crossing pressure as `fail-closed rejection`; the accepted campaign expressly keeps positive `P5` success unearned | no accepted production-surface evidence yet shows nested-`forall` pressure succeeding inside the unchanged baseline |
| `P6 reconstruction-visible-output` | unresolved at repo scope | rows `C1`, `C2`, and `C7` all remain below `stable visible persistence`; the full-pipeline contract requires the same recursive structure to remain review-visible on both internal and public output surfaces | zero accepted rows currently reach `stable visible persistence`, so solver admission or internal-only visibility cannot be promoted into repo-level success |

### Negative and bounded families

| Family | Frozen repo-level read | Accepted predecessor evidence now binding | Why the read stays bounded |
| --- | --- | --- | --- |
| `N1 ambiguity-reject` | fail-closed ambiguity rejection remains required and has accepted bounded support | row `C4` is accepted `fail-closed rejection`, and the full-pipeline contract preserves ambiguity as a fail-closed invalidation rule | the current record proves rejection discipline, not broad positive success |
| `N2 unsoundness-guard` | fail-closed soundness and binder-discipline rejection remains required and has accepted bounded support | row `C3` preserves quantified-crossing rejection, row `C5` preserves owner/binder-sensitive pressure, and the full-pipeline contract keeps owner / binder / scope violations reject-side | accepted evidence preserves the guard only; it does not upgrade the positive hard cases that press against it |
| `N3 equi-recursive-required` | out of scope under the unchanged iso-recursive baseline | the baseline contract, capability contract, and architectural audit all preserve iso-recursive-only meaning and block equi-recursive equality | no accepted item in this family reopens semantic widening |
| `N4 cyclic-or-multi-scc-required` | out of scope under the unchanged non-cyclic-graph / no-multi-SCC baseline unless a later accepted decision reopens it | the baseline contract and architectural audit keep cyclic structure and multi-SCC search outside the inherited model | this item does not settle whether reopening is needed later; it only preserves the current out-of-scope boundary |
| `N5 second-interface-or-fallback-required` | out of scope under the unchanged no-second-interface / no-fallback baseline | the baseline contract and architectural audit keep second-interface and fallback paths forbidden under the live production boundary | this item preserves the boundary and does not authorize escape-hatch success claims |
| `N6 termination-pressure` | bounded or fail-closed handling remains required | row `C6` is accepted `fail-closed rejection`, and the architectural audit keeps `N6` materially unresolved under the inherited acyclic model | bounded rejection exists, but that does not clear representative positive-family pressure or prove global sufficiency |

## Accepted Matrix Tally

The accepted representative campaign vocabulary remains:

- `stable visible persistence`: none;
- `admitted but not reconstruction-visible / blocker debt`:
  `C1`, `C2`, `C5`, and `C7`; and
- `fail-closed rejection`: `C3`, `C4`, and `C6`.

The current repo-level feasibility read therefore remains
`bounded subset only`.

The same-lane retained-child chain from rounds `round-094` through
`round-098` feeds only the bounded reads for `P3`, `P4`, and `P6`. It does
not clear those families globally and does not authorize promotion from
`admitted but not reconstruction-visible / blocker debt` to
`stable visible persistence`.

## Accepted Predecessor Evidence Chain

Later rounds must preserve the following accepted chain without widening it:

- the inherited baseline contract in
  `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`;
- the accepted capability contract and representative family matrix in
  `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-capability-contract-and-evaluation-corpus.md`;
- the accepted architectural audit in
  `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md`,
  which keeps `non-cyclic-graph = unknown`;
- the accepted full-pipeline production-surface contract in
  `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md`;
- the accepted representative campaign in
  `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`,
  which keeps the repo-level read at `bounded subset only`;
- the accepted strategic architecture decision in
  `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architecture-decision-and-successor-plan-choice.md`,
  which keeps `continue within the current architecture` as the strongest
  lawful aggregate read before this refreshed roadmap family; and
- the accepted exact-pocket chain in
  `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md`,
  `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-path-audit.md`,
  `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-collapse-clear-or-confirm.md`,
  `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md`,
  and
  `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md`,
  together with accepted rounds `round-094`, `round-095`, `round-096`,
  `round-097`, and `round-098`.

Open `BUG-2026-03-16-001` remains predecessor replay context only. It does
not authorize roadmap reordering, production implementation, fallback
widening, or a stronger architecture claim in this item-1 freeze.

## Honest Starting Posture And Handoff

The one honest repo-level starting posture for this refreshed roadmap family
is:

- repo-level `non-cyclic-graph` settlement remains unresolved;
- the current record contains bounded admitted pockets plus fail-closed
  rejection discipline; and
- the current record does not yet justify
  `non-cyclic-graph = keep` or
  `reopen the non-cyclic-graph revision question`.

This artifact therefore fails closed by freezing predecessor-established
truth and naming the next missing proof work only.

The next immediate consumer is roadmap item `2`. Its job is bounded proof
work inside the same inherited architecture: produce representative
production-surface evidence for unresolved propagation and placement
families without widening into cyclic search, multi-SCC search, second
interfaces, fallback paths, global settlement, or production implementation.
