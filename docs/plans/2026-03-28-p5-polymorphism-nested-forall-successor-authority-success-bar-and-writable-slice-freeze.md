# P5 Polymorphism-Nested-Forall Successor Authority, Success Bar, And Writable-Slice Freeze

Date: 2026-03-28
Round: `round-128`
Roadmap item: `item-1`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: one bounded current-architecture successor freeze for the
remaining `P5 polymorphism-nested-forall authoritative-surface` family
Artifact kind: canonical docs-only successor-boundary freeze

## Stage Contract Freeze

This artifact implements only roadmap item `1` for `attempt-1` with
`retry: null`.

This round is docs-only, freeze-only, and pre-implementation. Its job is to
bind the direct predecessor authority chain, the exact live `P5` control and
contrast packet, the exact success bar for this family, and the exact writable
slice for the next bounded current-architecture attempt.

This round does not:

- implement a `P5` fix;
- rerun focused tests or the full Cabal gate as domain evidence;
- reopen the settled same-lane `C2` / `C5` / `C7` pocket;
- reopen the settled exact `C1` / `P2` packet;
- reopen the settled exact `P1` packet;
- decide a new repo-scope posture;
- authorize cyclic search, multi-SCC search, equi-recursive reasoning,
  fallback widening, a second interface, hardening, rollout, or a broad
  capability claim.

## Item-1 Authority Ledger

| Input class | Source | Binding read carried forward here |
| --- | --- | --- |
| Production baseline authority | `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md` | The repo still ships explicit-only recursive support, iso-recursive meaning only, no equi-recursive semantics, inherited non-cyclic structure, and no fallback widening. |
| Repo-scope capability authority | `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-capability-contract-and-evaluation-corpus.md` | `P5 polymorphism-nested-forall` remains a required positive family for any eventual repo-level readiness claim. |
| Accepted repo-scope refreshed matrix | `docs/plans/2026-03-27-post-rev-004-repo-scope-refreshed-representative-family-matrix-settlement-surface-and-provenance-validation.md` | `C3` remains the live bounded row for `P5`: clear-boundary control stays recursive on the fallback surface, while the nested-`forall` contrast remains fail-closed and positive `P5` success is still unearned. |
| Accepted immediate handoff authority | `docs/plans/2026-03-28-post-p1-local-recursive-shape-successor-gate-and-immediate-handoff-decision.md` | The only live continuation after the settled exact `P1` packet is a new bounded `P5 polymorphism-nested-forall authoritative-surface` family. |
| Focused bounded provenance context | `test/Research/P5ClearBoundarySpec.hs`; `orchestrator/rounds/round-118/lanes/p5-provenance-summary.md` | The current bounded contrast is already expressed as one clear-boundary control packet and one quantified-crossing contrast packet with isolated rerun provenance. |

## Exact Live Subject

The live subject for this roadmap family is fixed to one bounded `P5` contrast
only:

- family row: `C3`
- controlling family: `P5 polymorphism-nested-forall`
- clear-boundary control:
  `sameLaneClearBoundaryExpr`
- quantified-crossing contrast:
  `nestedForallContrastExpr`
- source of exact packet names:
  `test/Research/P5ClearBoundarySpec.hs`

This item binds no second positive family, no reopened `C1` packet, and no
reopened `P1` packet.

## Exact Current Read Carried Forward

The exact current accepted read for this live subject is frozen as:

- the clear-boundary control remains recursive on the fallback surface with
  `containsMu True`;
- the quantified-crossing contrast currently fails closed on the fallback
  surface with `containsMu False`; and
- current lawful classification:
  `reject-side bounded contrast / positive P5 success unearned`

This means the family debt is narrower than broad recursive polymorphism:
can one exact quantified-crossing packet surface lawful recursive structure on
the current authoritative outputs inside the inherited architecture?

## Exact Success Bar For Item `2`

Item `2` may claim exact-packet success only if it satisfies all of the
following together:

1. The live subject remains the exact quantified-crossing contrast packet
   `nestedForallContrastExpr`, with `sameLaneClearBoundaryExpr` retained only
   as a bounded control.
2. The behavior remains inside the inherited current architecture:
   no cyclic search,
   no multi-SCC search,
   no equi-recursive reasoning,
   no second interface,
   and no fallback widening.
3. Recursive structure for that exact quantified-crossing packet becomes
   review-visible on the current authoritative surfaces rather than
   helper-only or diagnostic-only surfaces.
4. Fresh evidence is recorded through focused `P5` coverage and any needed
   production-path tests on the canonical round-owned surfaces.
5. Any touched code path passes the repo verification bar required by the live
   verification contract.

Even if item `2` clears this exact success bar, that would still mean:

- one exact-packet `P5` improvement only;
- not general `P5` family settlement by itself; and
- not repo-level readiness by itself.

## Writable Slice Freeze For Item `2`

The next bounded implementation round may write only inside this frozen slice:

| Path or class | Why it is included |
| --- | --- |
| `src/MLF/Elab/Run/ResultType/Fallback.hs` | The current quantified-boundary guard and retained-child candidate logic live here. |
| `src/MLF/Elab/Run/Pipeline.hs` | Current authoritative production entrypoints live here. |
| `src/MLF/Elab/Pipeline.hs` | Re-export continuity may need synchronization if production entrypoint behavior changes. |
| `src-public/MLF/Pipeline.hs` | Public entrypoint continuity may need synchronization if production entrypoint behavior changes. |
| `test/Research/P5ClearBoundarySpec.hs` | The exact control / contrast harness already lives here. |
| `test/PipelineSpec.hs` | Authoritative-surface regression coverage may need one exact-packet production-path anchor. |
| `test/Main.hs` | Test wiring only if new focused spec modules are introduced. |
| `mlf2.cabal` | Cabal wiring only if new focused modules are introduced. |
| this freeze doc and round-owned round-128 artifacts | Bound documentation and round evidence for this family. |

This writable slice is permissive, not mandatory. Item `2` may narrow below
this list, but it may not widen beyond it without a later accepted change.

## Next Lawful Move

The next lawful move after this freeze is roadmap item `2` only:

`Implement and validate one bounded current-architecture P5 authoritative-surface continuation slice`
