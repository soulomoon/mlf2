# General Automatic Iso-Recursive Full-Inference Predecessor Authority, Unresolved Semantic Matrix, Family Success Bar, And First Concrete Deliverable Freeze

Date: 2026-04-02
Round: `round-177`
Roadmap item: `item-1`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: one docs-only full-inference family freeze for predecessor
authority, unresolved semantic matrix, family success bar, and first concrete
deliverable only
Artifact kind: canonical docs-only item-1 freeze artifact

## Stage Contract Freeze

This artifact implements only roadmap item `1` for `attempt-1` with
`retry: null`.

This round is docs-only, item-1-only, aggregate-only, freeze-only,
non-widening, and pre-mechanism. Its job is to bind the predecessor
authority chain for the new full-inference family, restate the exact family
question, freeze the still-live semantic matrix, freeze the family success
bar, and freeze the first concrete deliverable plus the fail-closed
docs-only writable slice.

This round does not:

- publish the semantic mechanism map itself;
- define recursive-shape candidate-generation or search rules;
- authorize any source, test, or Cabal slice;
- upgrade `sameLaneAliasFrameClearBoundaryExpr` or
  `sameLaneDoubleAliasFrameClearBoundaryExpr` into a repo-level readiness
  claim;
- reopen an accepted predecessor artifact as live implementation debt; or
- rewrite `orchestrator/state.json`, any roadmap file, `TODO.md`,
  `implementation_notes.md`, `Bugs.md`, or any accepted predecessor freeze /
  settlement / handoff artifact.

Implementer-owned writes for this round are limited to:

- this canonical item-1 freeze artifact; and
- `orchestrator/rounds/round-177/implementation-notes.md`.

The inherited current-architecture boundary remains controlling here:

- explicit recursive annotations remain the production baseline;
- recursive meaning remains iso-recursive only;
- `non-equi-recursive = keep`;
- the family remains inside the inherited explicit-only / iso-recursive /
  non-equi-recursive / non-cyclic-graph / no-fallback current architecture;
- no second interface is authorized;
- no cyclic search is authorized;
- no multi-SCC search is authorized;
- no equi-recursive widening is authorized; and
- no fallback widening is authorized.

This artifact preserves that inherited boundary as the controlling family
contract only. It does not relitigate the March 25 architectural audit inside
item `1`; it freezes the family under the inherited current architecture
until a later accepted decision explicitly says otherwise.

## Item-1 Authority Ledger

| Input class | Source | Binding read carried forward here |
| --- | --- | --- |
| Production baseline authority | `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md` | The repo still ships explicit-only recursive support, iso-recursive meaning only, no equi-recursive semantics, inherited non-cyclic structure, and no fallback widening. |
| Repo-level capability contract | `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-capability-contract-and-evaluation-corpus.md` | The eventual repo-level question is a representative-family question rather than a packet question, and the still-live family obligations are the capability-contract obligations `P2` through `P6` plus `N1`, `N2`, and `N6` under the inherited boundary. |
| Architectural constraint audit | `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md` | The inherited target still stays inside iso-recursive / non-equi-recursive / no-fallback constraints, `N3` / `N4` / `N5` stay boundary territory rather than silently reopened work, and the live pressure remains whether the inherited current architecture can explain representative `P2`-`P6` plus bounded `N6` honestly. |
| Full-pipeline reconstruction and validation contract | `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md` | `P6 reconstruction-visible-output` remains a review-visible output-surface obligation on authoritative surfaces; solver-only, helper-only, witness-only, or fallback-like rescue is not lawful success. |
| March 27 refreshed matrix settlement surface | `docs/plans/2026-03-27-post-rev-004-repo-scope-refreshed-representative-family-matrix-settlement-surface-and-provenance-validation.md` | The March 27 refreshed matrix preserved the same-lane `C2` / `C5` / `C7` pocket as settled predecessor truth only, kept `C1` visibly non-recursive on authoritative surfaces, and kept positive `P5` success unresolved. |
| March 27 narrowed successor gate | `docs/plans/2026-03-27-post-rev-004-repo-scope-narrowed-successor-gate-and-immediate-handoff-decision.md` | The strongest accepted repo-scope read remained `narrowed unresolved / continue within the current architecture`, so the successor chain stays bounded current-architecture work rather than an implicit boundary revision. |
| March 28 representative-gap freeze | `docs/plans/2026-03-28-same-lane-retained-child-representative-gap-successor-authority-exact-subject-success-bar-and-writable-slice-freeze.md` | The predecessor family lawfully froze `sameLaneAliasFrameClearBoundaryExpr` as one bounded same-lane retained-child representative-gap packet across `P3` / `P4` / `P6`, not as a family-wide settlement. |
| March 29 representative-gap settlement surface | `docs/plans/2026-03-29-same-lane-alias-frame-representative-gap-post-item-2-settlement-surface-and-repo-impact-read.md` | The March 29 checkpoint kept `sameLaneAliasFrameClearBoundaryExpr` as a `narrower current-architecture blocker` and explicitly preserved the broader `P3` / `P4` / `P6` family as unresolved. |
| April 1 successor authority freeze | `docs/plans/2026-04-01-general-automatic-iso-recursive-successor-authority-exact-inherited-blocker-lane-and-writable-slice-freeze.md` | The April family reopened the one-alias packet only as one inherited blocker lane under the same explicit-only / iso-recursive / non-equi-recursive / non-cyclic-graph / no-fallback boundary. |
| April 2 alias-frame settlement surface | `docs/plans/2026-04-02-general-automatic-iso-recursive-post-item-2-settlement-surface-and-exact-repo-impact-read.md` | `sameLaneAliasFrameClearBoundaryExpr` is now one settled `narrow success` packet on `runPipelineElab` and `runPipelineElabChecked`, but the exact repo-impact read stayed packet-bounded only. |
| April 2 alias-frame successor decision | `docs/plans/2026-04-02-general-automatic-iso-recursive-post-item-3-successor-decision-and-immediate-handoff-after-bounded-lane.md` | The accepted decision is `continue-bounded`, the accepted immediate handoff is `open one bounded current-architecture family`, and broader `P3` / `P4` / `P6` plus repo-level readiness remained unresolved. |
| April 2 follow-on successor authority freeze | `docs/plans/2026-04-02-general-automatic-iso-recursive-current-architecture-follow-on-successor-authority-next-exact-representative-gap-packet-current-live-read-success-bar-and-writable-slice-freeze.md` | The follow-on family lawfully froze `sameLaneDoubleAliasFrameClearBoundaryExpr` as the next exact representative-gap packet under the same inherited boundary and bounded packet discipline. |
| April 2 follow-on settlement surface | `docs/plans/2026-04-02-general-automatic-iso-recursive-current-architecture-follow-on-post-item-2-settlement-surface-and-exact-repo-impact-read.md` | `sameLaneDoubleAliasFrameClearBoundaryExpr` is now one settled `narrow success` packet on `runPipelineElab` and `runPipelineElabChecked`, but the exact repo-impact read stayed packet-bounded only. |
| April 2 follow-on successor decision | `docs/plans/2026-04-02-general-automatic-iso-recursive-current-architecture-follow-on-post-item-3-successor-decision-and-immediate-handoff-after-bounded-lane.md` | The accepted outcome again remained `continue-bounded` with `open one bounded current-architecture family`, while broader `P3` / `P4` / `P6` and repo-level readiness remained unresolved after the second packet win. |
| Accepted bounded packet approval | `orchestrator/rounds/round-174/review-record.json` | Accepted round-174 closed `sameLaneDoubleAliasFrameClearBoundaryExpr` as bounded packet evidence only, with the evidence summary explicitly tied to one bounded double-alias retained-child packet and a green full gate rather than a broader readiness claim. |
| Accepted bounded-family handoff approval | `orchestrator/rounds/round-176/review-record.json` | Accepted round-176 confirmed the docs-only item-4 result as `continue-bounded` plus `open one bounded current-architecture family`, preserving broader readiness and boundary-revision questions as unresolved. |

## Settled Predecessor Fragments Only

The accepted predecessor packet wins carried into this family are:

- `sameLaneAliasFrameClearBoundaryExpr` as one settled `narrow success`
  packet on `runPipelineElab` and `runPipelineElabChecked`; and
- `sameLaneDoubleAliasFrameClearBoundaryExpr` as one settled `narrow success`
  packet on `runPipelineElab` and `runPipelineElabChecked`.

Those wins remain bounded predecessor truth only. They do not by themselves
settle non-local propagation, owner-sensitive placement, binder-sensitive
placement, polymorphism / nested-`forall`, reconstruction-visible output, or
bounded fail-closed generality. They are not themselves a repo-level
full-inference readiness answer.

## Exact Family Question

This family owns one question only:

> Under the inherited explicit-only / iso-recursive / non-equi-recursive /
> non-cyclic-graph / no-fallback current architecture, can the repo earn an
> honest repo-level full-inference readiness answer for general automatic
> iso-recursive inference without widening semantics, interfaces, or search
> shape?

Item `1` freezes that question only. It does not yet answer it, and it does
not authorize any code campaign by implication.

## Unresolved Semantic Matrix

The semantic matrix frozen here starts with the still-live repo-level family
obligations rather than with already-carried predecessor fragments. The
following rows remain live:

| Token | Exact meaning frozen here | Status carried by item `1` |
| --- | --- | --- |
| `P2 non-local-propagation` | Programs where recursive evidence is discovered in one location but must reach a different consumer or target lane without becoming packet-specific folklore. | still live positive obligation; predecessor packet wins do not yet explain the general propagation rule |
| `P3 retained-child-owner-sensitive` | Retained-child, alias-bound, or otherwise owner-sensitive cases where recursive placement depends on who owns the retained structure and where the target sits. | still live positive obligation; the two settled same-lane packets remain bounded fragments only |
| `P4 binder-sensitive-placement` | Programs where binder placement, retained ancestry, or nested binding structure matters to whether the inferred recursive type is lawful and reviewable. | still live positive obligation; no general binder-placement rule is frozen yet |
| `P5 polymorphism-nested-forall` | Recursive inference cases that interact with polymorphism or nested `forall` structure while still staying inside the inherited baseline. | still live positive obligation; reject-side contrast exists, but general positive settlement does not |
| `P6 reconstruction-visible-output` | Programs where solver-side recursive success only counts if elaborated or reconstructed output still exposes the inferred recursive structure in a reviewable form. | still live positive obligation; authoritative review-visible output remains part of the family success bar |
| `N1 ambiguity-reject` | Cases with competing recursive candidates or placements where the implementation cannot justify one answer without guesswork. | still live negative obligation; later work must fail closed rather than rank or guess |
| `N2 unsoundness-guard` | Cases where recursive inference would violate ownership, binder discipline, scope discipline, soundness assumptions, or other thesis-backed invariants. | still live negative obligation; later work must preserve fail-closed unsoundness guards |
| `N6 termination-pressure` | Cases that place heavy pressure on search or recursive-candidate growth; later work must show either bounded success or bounded rejection without divergence. | still live negative obligation; later work must stay bounded or fail closed inside the inherited boundary |

The following negative rows remain explicitly out of scope under the inherited
boundary unless a later accepted family reopens that boundary on purpose:

| Token | Exact meaning frozen here | Status carried by item `1` |
| --- | --- | --- |
| `N3 equi-recursive-required` | Cases that would need equi-recursive equality, implicit unfolding, or other semantics outside the inherited iso-recursive boundary. | preserved as out of scope under the inherited boundary |
| `N4 cyclic-or-multi-scc-required` | Cases whose only plausible route would require cyclic structural graphs, multi-SCC search, or a richer recursion representation than the inherited baseline permits. | preserved as out of scope under the inherited boundary |
| `N5 second-interface-or-fallback-required` | Cases that would succeed only by adding a new executable path, alternate interface, or default-path fallback behavior. | preserved as out of scope under the inherited boundary |

This item `1` freezes the semantic matrix only; it does not publish the
semantic mechanism map, it does not define candidate-generation or search
rules, and it does not authorize any code slice.

## Family Success Bar

The family success bar frozen here is deliberately repo-level rather than
packet-level:

1. No later round may claim repo-level full-inference readiness from one or
   two bounded packet wins, including
   `sameLaneAliasFrameClearBoundaryExpr` and
   `sameLaneDoubleAliasFrameClearBoundaryExpr`.
2. The family needs an honest repo-level answer across the representative
   positive obligations `P2` through `P6`.
3. The family also needs fail-closed or bounded handling for `N1`, `N2`, and
   `N6` inside the inherited current architecture.
4. That answer must stay inside the inherited explicit-only / iso-recursive /
   non-equi-recursive / non-cyclic-graph / no-fallback boundary, with no
   second interface, no cyclic search, no multi-SCC search, no
   equi-recursive widening, and no fallback widening.
5. If later work cannot keep the answer honest inside that boundary, the work
   must stop or move only through a later accepted explicit boundary decision;
   it may not widen by implication.

## First Concrete Deliverable And Fail-Closed Writable Slice

The first concrete deliverable path is fixed exactly to:

`docs/plans/2026-04-02-general-automatic-iso-recursive-full-inference-current-architecture-semantic-mechanism-map.md`

That future docs-first mechanism artifact must explain, at minimum:

- recursive-shape discovery;
- non-local propagation;
- owner-sensitive placement;
- binder-sensitive placement;
- polymorphism / nested-`forall` interaction; and
- reconstruction visibility.

That future artifact must also keep settled predecessor fragments separate
from still-missing general rules.

The writable-slice freeze for this family is fail-closed:

- the only new non-round-local file authorized by this item-1 freeze is the
  future mechanism-map path above;
- this item-1 freeze does not authorize writes to `src/`, `src-public/`,
  `app/`, `test/`, `mlf2.cabal`, `orchestrator/state.json`,
  `orchestrator/roadmaps/**`, `TODO.md`, `implementation_notes.md`,
  `Bugs.md`, any accepted predecessor artifact, or any second docs artifact;
  and
- if a later round cannot keep the work inside that docs-only writable slice,
  it must stop or be rejected rather than widening into code, a second
  interface, or a broader deliverable by implication.

## Non-Claims

This item-1 freeze does not claim:

- that the semantic mechanism map already exists;
- that general recursive-shape discovery or search discipline has already been
  explained;
- that the inherited current architecture has already earned repo-level
  full-inference readiness;
- that `P2`, `P3`, `P4`, `P5`, or `P6` are generally settled; or
- that `N1`, `N2`, or `N6` are already discharged beyond bounded predecessor
  fragments.
