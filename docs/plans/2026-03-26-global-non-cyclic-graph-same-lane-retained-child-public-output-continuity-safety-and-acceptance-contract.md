# Global `non-cyclic-graph` Same-Lane Retained-Child Public-Output Continuity Safety And Acceptance Contract

Date: 2026-03-26
Round: `round-106`
Roadmap item: `item-3`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: one docs-only rev-002 item-3 safety / acceptance contract for
the exact same-lane retained-child / public-output continuity pocket already
selected in accepted item `2`
Artifact kind: canonical docs-only rev-002 item-3 safety / acceptance
contract

## Stage Contract Freeze

This artifact implements only roadmap item `3` for `attempt-1` with
`retry: null`.

It consumes accepted `round-104` item-1 truth and accepted `round-105`
item-2 truth as authoritative predecessor authority and freezes exactly this
much, and no more:

- rev-002 remains planning-only and architecture-only;
- accepted items `1` and `2` remain binding and are not relitigated;
- rev-001 items `6` through `8` remain blocked;
- exactly one already-selected reopened subject remains in scope; and
- exactly three lawful outcome bars are defined for that one subject only.

This artifact does not:

- reopen the rev-002 candidate boundary or the accepted item-2 subject
  selection;
- bind the exact item-4 audit target across rows, packets, modules,
  commands, or review-visible output surfaces;
- choose which item-3 outcome is true;
- decide whether item `5` opens one bounded architecture-amendment lane or
  stops without opening it;
- authorize edits to `orchestrator/state.json`, roadmap files, retry files,
  verification files, `Bugs.md`, source code, tests, or Cabal files; or
- authorize multi-SCC search, second interfaces, fallback widening,
  production implementation, hardening, or broad capability claims.

The inherited boundary also remains unchanged:

- explicit recursive annotations remain the production baseline;
- recursive meaning remains iso-recursive only;
- equi-recursive semantics and implicit unfolding remain blocked;
- `non-equi-recursive = keep` remains binding;
- `no-fallback = keep` remains binding; and
- no live cyclic-structure implementation is authorized by this contract.

## Accepted Predecessor Authority

| Source | Binding truth carried forward |
| --- | --- |
| `orchestrator/rounds/round-104/review-record.json` | Accepted `round-104` made the item-1 authority freeze authoritative: rev-002 is planning-only and architecture-only, exactly one candidate boundary is frozen, and rev-001 items `6` through `8` remain blocked. |
| `orchestrator/rounds/round-105/review-record.json` | Accepted `round-105` made the item-2 subject selection authoritative: exactly one live reopened subject exists, and `C2`, `C5`, and `C7` are continuity faces of the same exact pocket while non-local `C1` remains contrast only. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-reopened-revision-authority-and-candidate-boundary-freeze.md` | The only frozen rev-002 candidate boundary is whether one bounded single-component cyclic-structure successor lane is needed for the strongest admitted retained-child / public-output continuity pressure, with no multi-SCC widening and no second interface. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-reopened-subject-selection.md` | The exact selected subject is the same exact same-lane retained-child / public-output continuity pocket already carried by accepted rows `C2`, `C5`, and `C7`, with non-local `C1` retained only as contrast context. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-keep-vs-reopen-decision-gate.md` and `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md` | The inherited global boundary remains `iso-recursive = keep`, `non-equi-recursive = keep`, `no-fallback = keep`, explicit-only in production, and non-equi-recursive / non-cyclic-graph by default. The rev-001 reopen did not re-enable rev-001 items `6` through `8`. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md`, `docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md`, and `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md` | The strongest admitted same-family pressure still comes from one exact `C2` / `C5` / `C7` pocket only; `C7` remains below `stable visible persistence`; `C5` is not a second packet; `C3` remains reject-side pressure only; and the accepted representative matrix does not itself justify broad cyclic work. |
| `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-case-and-review-ledger.md`, `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-end-to-end-revalidation-and-classification.md`, and `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md` | The exact same-lane pocket, exact packet, and helper-visible/internal versus authoritative-public split remain frozen predecessor truth. Those earlier same-pocket artifacts remain bounded predecessor evidence only and do not settle this global item-3 contract by themselves. |

These accepted artifacts are predecessor authority only. None of them already
defines which item-3 outcome is true, and none of them authorizes broader
cyclic search, broader family search, a second interface, fallback widening,
production implementation, or a repo-level capability claim.

## Exact Selected Subject Governed By This Contract

This contract governs exactly one subject and no other:

`the exact same-lane retained-child / public-output continuity pocket already carried by accepted representative rows C2, C5, and C7, with non-local C1 retained only as contrast context`

That one selected subject remains frozen as follows:

| Continuity field | Exact selected value |
| --- | --- |
| Family | same-lane retained-child |
| Recursive-shape anchor | `boundVarTargetRoot` |
| Owner / binder frame | one owner-local retained-child frame |
| Route | `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC` |
| Quantified-boundary status | clear-boundary only: `boundHasForallFrom` is false and `not hasForall` holds |
| Same-pocket continuity faces | `C2` same-lane retained-child, `C5` owner-local / binder-sensitive placement for the same exact pocket, `C7` public-output continuity for the same exact pocket |
| Contrast-only context | non-local `C1` continuity remains contrast only and never becomes a coequal reopened subject |

Exact packet:

```haskell
ELet "k" (ELamAnn "x" recursiveAnn (EVar "x"))
  (ELet "u" (EApp (ELam "y" (EVar "y")) (EVar "k")) (EVar "u"))
```

where
`recursiveAnn = STMu "a" (STArrow (STVar "a") (STBase "Int"))`.

`C5` and `C7` remain rereads of this same exact packet only. They are not
second packets, second routes, second interfaces, or second selected
subjects.

## Exactly Three Lawful Outcome Bars

Item `3` defines evidence bars only. It does not select an outcome. Later
review and later rev-002 items may treat only the following three tokens as
lawful item-3 outcomes for this one exact selected subject:

| Lawful outcome bar | Counts as satisfied only if | Fail-closed boundary that still applies |
| --- | --- | --- |
| `acyclic still sufficient` | Later exact item-4 audit on this same exact selected subject can show that the inherited acyclic architecture remains sufficient for the subject's same-pocket retained-child / public-output continuity burden without needing any architecture-amendment lane. That later evidence must stay on the same exact family, anchor, owner-local frame, route, clear-boundary-only status, and exact packet already frozen by accepted item `2`. | This bar fails closed if sufficiency depends on a second subject, a second packet, a second route, multi-SCC search, a second interface, fallback widening, production implementation, or any weakening of `iso-recursive = keep`, `non-equi-recursive = keep`, or `no-fallback = keep`. |
| `single-component cyclic-structure successor lane justified` | Later exact item-4 audit on this same exact selected subject can show that the subject cannot honestly satisfy `acyclic still sufficient`, while the unresolved pressure is still bounded to one same-family, same-lane, same-packet, single-component cyclic-structure successor-lane candidate only. The justification must remain about one selected subject only and must preserve the inherited baseline rather than replacing it. | This bar fails closed if justification widens into multi-SCC search, second interfaces, fallback widening, equi-recursive semantics, implicit unfolding, production implementation, or broad capability claims. This bar records only that one bounded successor-lane candidate would be justified for later decision review; it does not open that lane here. |
| `stop without opening an architecture-amendment lane` | Later exact item-4 audit on this same exact selected subject can show that neither `acyclic still sufficient` nor `single-component cyclic-structure successor lane justified` can be lawfully satisfied without weakening the inherited invariants or widening beyond the accepted subject boundary. In that case the only lawful item-3 bar is to stop for this selected subject rather than opening an architecture-amendment lane. | This bar fails closed if the stop rationale depends on reopening subject selection, introducing a replacement subject, widening the family, or treating blocked work as implicitly authorized. This bar stops without opening a lane; it does not authorize a second attempt to widen the same round into a different architecture theory. |

No fourth bar, mixed bar, provisional bar, or renewed `unknown` token is
lawful for item `3`.

## Fail-Closed Rules

The item-3 contract is intentionally fail-closed. Evidence does not satisfy
any lawful outcome bar if any of the following is true:

- the evidence drifts away from the exact item-2-selected subject;
- the evidence turns `C1` into a second live reopened subject;
- the evidence turns `C5` or `C7` into second packets, second routes, or
  second interfaces;
- the evidence mixes two outcome bars instead of satisfying one outcome bar
  cleanly;
- the evidence depends on broad family search, multi-SCC search, fallback
  widening, or production behavior;
- the evidence weakens the inherited `iso-recursive = keep`,
  `non-equi-recursive = keep`, or `no-fallback = keep` boundary; or
- the evidence tries to use this contract to choose the item-5 result or to
  bind the exact item-4 audit surface.

This artifact therefore defines the bars only. It does not record which bar,
if any, later exact item-4 audit work will satisfy.

## Preserved Invariants And Explicitly Blocked Scope

Under every lawful item-3 outcome bar, all of the following remain binding:

- accepted item-1 truth remains binding;
- accepted item-2 truth remains binding;
- rev-001 items `6` through `8` remain blocked;
- rev-002 remains planning-only and architecture-only;
- `iso-recursive = keep`;
- `non-equi-recursive = keep`;
- `no-fallback = keep`;
- no equi-recursive semantics or implicit unfolding;
- no multi-SCC search;
- no second interfaces;
- no fallback widening;
- no production implementation; and
- no broad capability claim.

The following remain explicitly blocked in this round:

- reopening subject selection or introducing a second selected subject;
- treating `C5` or `C7` as anything other than the same exact `C2` pocket;
- widening into multiple packets, multiple routes, or broad family search;
- freezing the exact later audit bind across rows, packets, modules,
  commands, or review-visible output surfaces;
- deciding whether item `5` opens one bounded architecture-amendment lane or
  stops;
- authorizing source edits, tests, `mlf2.cabal` edits, `Bugs.md` edits,
  roadmap edits, or controller-state edits; and
- making a commit.

## Later-Item Handoff Ownership

Later rev-002 items must treat this artifact as contract authority only:

- item `4` still owns the exact audit bind across the selected subject's
  accepted rows, packets, modules, commands, and review-visible output
  surfaces;
- item `5` still owns the one lawful decision on whether to open one
  bounded architecture-amendment lane or stop without opening it; and
- no file outside this artifact path is authorized output for item `3`.

This round therefore remains docs-only, planning-only, and
architecture-only. The contract defined here is not implementation
clearance, not exact audit binding, and not an architecture-amendment
decision.
