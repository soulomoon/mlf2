# Same-Lane Retained-Child Stable-Visible-Persistence Case And Review Ledger

Date: 2026-03-25
Round: `round-089`
Roadmap item: `item-1`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: docs-only freeze of the exact same-lane retained-child
stable-visible-persistence case and review ledger
Artifact kind: canonical docs-only persistence-case freeze / review ledger

## Stage Contract Freeze

This artifact implements only roadmap item `1` for `attempt-1` with
`retry: null`.

Item `1` is docs-only and contract-freeze-only. Its job is to freeze exactly
one admitted pocket from the accepted predecessor chain, bind the exact item-5
persistence tuple to that pocket only, and define one review-visible phase /
surface ledger that later rounds must preserve when they audit, implement,
validate, or decide the same bounded subject.

This artifact does not authorize:

- edits under `src/`, `src-public/`, `app/`, `test/`, or `mlf2.cabal`;
- edits to `orchestrator/state.json`, `orchestrator/roadmap.md`,
  `orchestrator/verification.md`, or `orchestrator/retry-subloop.md`;
- edits to `Bugs.md`;
- the item-2 breakpoint audit;
- the item-3 minimum bounded implementation / proof slice;
- the item-4 end-to-end validation / outcome classification slice;
- the item-5 bounded successor decision slice;
- the non-local alias-bound / base-like family;
- neighboring consumer routes;
- nested-`forall`, nested owner, or nested scheme-root positive success;
- broad automatic recursive inference claims; or
- a reopened `non-cyclic-graph` revision argument.

The inherited boundary remains fixed and unchanged:

- explicit recursive annotations remain the current production baseline;
- recursive meaning remains iso-recursive only;
- no equi-recursive equality or implicit unfolding is authorized;
- no cyclic structural graph encoding or multi-SCC search is authorized;
- no second interface is authorized; and
- no compatibility, convenience, or default-path fallback widening is
  authorized.

Accepted `N14`, accepted item `5`, accepted item `6`, and accepted item `7`
contribute bounded predecessor evidence only. They preserve real bounded
continuity for one same-lane retained-child packet, but they do not by
themselves prove accepted `stable visible persistence`. Accepted item `2`
still leaves `non-cyclic-graph = unknown` as architecture-pressure context
only; item `1` does not reopen that question.

## Fixed Bounded Subject

The only lawful subject for this refreshed successor loop is the strongest
bounded admitted pocket already named by the accepted item-5 / item-6 / item-7
chain: the same-lane retained-child family anchored at `boundVarTargetRoot`
inside one owner-local retained-child frame, with route
`sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC`, clear
quantified-boundary status only, and the current bounded recursive output fact
`containsMu True`.

Nothing in this artifact upgrades that bounded evidence into a general
capability claim. The subject remains one pocket only.

## Immutable Case Table

| Frozen field | Exact value for the selected pocket | Explicit exclusion boundary |
| --- | --- | --- |
| Family | same-lane retained-child | non-local alias-bound / base-like family excluded |
| Recursive-shape anchor | `boundVarTargetRoot` | any anchor swap or scheme-root alias-bound reinterpretation excluded |
| Owner / binder frame | one owner-local retained-child frame | nested owner, nested scheme-root, or different frame excluded |
| Target / consumer route | `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC` | neighboring consumer routes excluded |
| Quantified-boundary state | clear-boundary only: `boundHasForallFrom` is false and `not hasForall` holds | nested-`forall` or any quantified crossing excluded |
| Current bounded output-side fact | `containsMu True` | witness-only, packet-history-only, or non-recursive surface reinterpretation excluded |

Any route that changes family, frame, route, or quantified status is outside
continuity for this frozen case.

## Frozen Persistence Tuple

Item `1` reuses the item-5 persistence tuple vocabulary without alteration and
binds it to the one selected pocket only.

| Tuple field | Frozen interpretation for this case | Drift rule |
| --- | --- | --- |
| admitted family identity | The candidate must remain the same-lane retained-child family and may not swap into any non-local or neighboring family story. | A family swap is not continuity of this case. |
| recursive-shape anchor interpretation | The recursive-shape anchor remains `boundVarTargetRoot` inside the same owner-local retained-child story. | Any anchor swap or anchor reinterpretation breaks continuity. |
| owner / binder frame interpretation | The owner / binder frame remains one owner-local retained-child frame. | Any nested-owner or different-frame read breaks continuity. |
| target / consumer route interpretation | The route remains `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC`. | Any route hop, neighboring consumer read, or consumer replacement breaks continuity. |
| quantified-boundary-clear status | The case remains lawful only while `boundHasForallFrom` is false and `not hasForall` holds. | Any quantified crossing breaks continuity and stays reject-side. |
| output-surface recursive-visibility obligation | Once a reviewable output surface exists, recursive structure must remain visibly present for this same family / anchor / frame / route on both internal and public output surfaces. | If recursion survives only as witness-only, packet-history-only, or hidden-phase folklore, the result is not continuity of this case. |

If a later round changes any tuple field, hops to a neighboring route,
introduces quantified crossing, or keeps recursion only through witness-only
or packet-history-only reinterpretation, that later read is not continuity of
the frozen case.

## Frozen Review Ledger

This ledger is contractual rather than evidentiary. Item `1` names what later
rounds must preserve for the one selected pocket; it does not yet decide which
rows the current implementation already satisfies.

| Phase / surface | Continuity obligation later rounds must preserve | Invalidation read |
| --- | --- | --- |
| Solver admission state | The same family, anchor, owner / binder frame, route, and quantified-boundary-clear status must remain the one admitted pocket under review. | Family swap, anchor swap, owner / binder change, route switch, quantified crossing, or ambiguity that would need ranking invalidates continuity. |
| Elaboration handoff / result state | Elaboration inputs and outputs must still refer to the same frozen tuple rather than replacing it with a different owner, route, or recursive story. | Erasing family identity, changing owner / binder meaning, changing route, or preserving recursion only as a hidden historical note invalidates continuity. |
| Reification / reconstruction state | Reification / reconstruction must preserve the same admitted family and route interpretation without silent unfolding or witness-only rescue. | Mechanism-family drift, silent unfolding, replay-specific reinterpretation, or proof that exists only by witness inspection invalidates continuity. |
| Internal output surface | The internal output surface must still expose recursive structure review-visibly for the same family / anchor / frame / route. | A downgraded non-recursive surface read, route drift, or claimed recursion that no longer appears on the internal surface invalidates continuity. |
| Public output surface | The public output surface must expose the same recursive structure as the internal surface for the same frozen pocket, without a second interface or fallback story. | Public omission of recursion, disagreement with the internal surface, or any family / route drift invalidates continuity. |
| Reviewer-visible evidence trail | Reviewers must be able to point to one stable persistence tuple across every earlier row without inventing a new route or a new packet-local story. | Manual reinterpretation, replay-only rescue, witness-only proof, packet-history-only rescue, or fallback-like reasoning invalidates continuity. |

## Honest Starting Posture

The one honest starting classification for the selected pocket remains:
`admitted but not reconstruction-visible / blocker debt`.

That read is still binding because:

- zero accepted `stable visible persistence` rows exist in the current record;
- the same-lane retained-child pocket is still the strongest bounded
  positive-output candidate because `containsMu True` and the
  `keepTargetFinal -> targetC` route are already preserved; but
- accepted full solver -> elaboration -> reconstruction -> internal /
  public-output continuity is still not in the record.

Nested-`forall` pressure remains reject-side only. It does not upgrade into
positive `stable visible persistence`, and it does not widen the lawful
subject beyond the clear-boundary same-lane retained-child case frozen above.

## Item-2 Handoff

The next round must audit the live pipeline against this frozen tuple and
ledger only. Its job is to determine which rows are already satisfied and
where the first actual continuity breakpoint occurs for this same pocket,
without widening into the alias-bound family, neighboring routes,
nested-`forall` success, or a fresh architecture argument.

## Docs-Only Verification Note

This round is expected to change only documentation:

- this canonical item-1 case-freeze / review-ledger artifact; and
- `orchestrator/rounds/round-089/implementation-notes.md`.

Because the round does not touch `src/`, `src-public/`, `app/`, `test/`, or
`mlf2.cabal`, no full `cabal build all && cabal test` gate is triggered
unless the diff escapes that authorized docs-only surface.
