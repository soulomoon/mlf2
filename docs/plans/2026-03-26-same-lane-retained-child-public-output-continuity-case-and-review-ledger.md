# Same-Lane Retained-Child Public-Output Continuity Case And Review Ledger

Date: 2026-03-26
Round: `round-094`
Roadmap item: `item-1`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: docs-only freeze of the exact same-lane retained-child
public-output continuity case and review ledger
Artifact kind: canonical docs-only item-1 case freeze / review ledger

## Stage Contract Freeze

This artifact implements only roadmap item `1` for `attempt-1` with
`retry: null`.

Item `1` is docs-only and contract-freeze-only. Its job is to freeze exactly
one same-lane retained-child pocket, freeze the exact helper-visible/internal
versus authoritative public-output split now under review for that same
pocket, and bind one refreshed review ledger that later rounds must preserve
when they audit, repair, revalidate, or decide the same bounded subject.

This artifact does not authorize:

- edits under `src/`, `src-public/`, `app/`, `test/`, or `mlf2.cabal`;
- edits to `orchestrator/state.json`, `orchestrator/roadmap.md`,
  `orchestrator/verification.md`, or `orchestrator/retry-subloop.md`;
- edits to `Bugs.md`;
- the item-2 authoritative public-output path audit;
- the item-3 minimum bounded implementation / proof slice;
- the item-4 end-to-end revalidation / classification slice;
- the item-5 bounded architecture-pressure decision slice;
- the non-local alias-bound family;
- neighboring consumer routes;
- nested-`forall`, nested owner, or nested scheme-root positive success;
- replay / `InstBot` repair;
- broad automatic-recursive-inference claims; or
- a reopened `non-cyclic-graph` revision argument.

The inherited boundary remains fixed and unchanged:

- explicit recursive annotations remain the current production baseline;
- recursive meaning remains iso-recursive only;
- no equi-recursive equality or implicit unfolding is authorized;
- no cyclic structural graph encoding or multi-SCC search is authorized;
- no second interface is authorized; and
- no compatibility, convenience, or default-path fallback widening is
  authorized.

Accepted `N14`, accepted strategic items `2`, `5`, `6`, and `7`, plus
accepted rounds `089` through `093`, contribute bounded predecessor evidence
only. They do not by themselves prove broad automatic iso-recursive
inference, do not clear the non-local alias-bound family, and do not reopen
`non-cyclic-graph`.

## Fixed Bounded Subject

The only lawful subject for this refreshed successor loop is one exact
same-lane retained-child pocket already fixed by the accepted predecessor
chain:

- family: same-lane retained-child;
- recursive-shape anchor: `boundVarTargetRoot`;
- owner / binder frame: one owner-local retained-child frame;
- route:
  `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC`;
- quantified-boundary status: clear-boundary only; and
- exact packet:

  ```haskell
  ELet "k" (ELamAnn "x" recursiveAnn (EVar "x"))
    (ELet "u" (EApp (ELam "y" (EVar "y")) (EVar "k")) (EVar "u"))
  ```

  where
  `recursiveAnn = STMu "a" (STArrow (STVar "a") (STBase "Int"))`.

Nothing in this artifact upgrades that exact pocket into a family-level or
repo-level capability claim. The live subject remains one pocket only.

## Immutable Case Table

| Frozen field | Exact value for the selected pocket | Explicit exclusion boundary |
| --- | --- | --- |
| Family | same-lane retained-child | non-local alias-bound family excluded |
| Recursive-shape anchor | `boundVarTargetRoot` | any anchor swap or scheme-root alias reinterpretation excluded |
| Owner / binder frame | one owner-local retained-child frame | nested owner, nested scheme-root, or different frame excluded |
| Target / consumer route | `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC` | neighboring consumer routes excluded |
| Quantified-boundary state | clear-boundary only: `boundHasForallFrom` is false and `not hasForall` holds | nested-`forall` or any quantified crossing excluded |
| Exact packet | `ELet "k" (ELamAnn "x" recursiveAnn (EVar "x")) (ELet "u" (EApp (ELam "y" (EVar "y")) (EVar "k")) (EVar "u"))` | packet variants that change family, frame, route, or quantified status excluded |

Any route that changes family, frame, route, packet identity, or quantified
status is outside continuity for this frozen case.

## Frozen Public-Output Continuity Split

This refreshed family freezes one exact continuity split for the same pocket
only.

| Surface | Frozen read for this pocket | Drift rule |
| --- | --- | --- |
| Helper-visible / internal output | recursive structure remains visible as `TMu ...` and `containsMu True` on the accepted helper-visible internal path | if later evidence removes recursive structure from this internal surface, continuity has changed |
| Authoritative public output | both authoritative public entrypoints currently return `TForall "a" Nothing (TVar "a")` for the same pocket | if later evidence changes the authoritative public output, that must be recorded as a later-round result rather than silently rewritten here |

Item `1` freezes this split without explaining why it occurs. Explanation,
path localization, repair, or architecture-causality arguments remain later
round work only.

## Refreshed Review Ledger

This ledger is both contractual and anchored to accepted predecessor truth.
It defines the one review-visible continuity story that later rounds must
preserve for the same pocket only.

| Phase / surface | Frozen read for this case | Later-round drift rule |
| --- | --- | --- |
| Solver admission state | The admitted pocket remains the same family, anchor, owner-local frame, route, and clear-boundary status already frozen by accepted predecessor evidence. | Family swap, anchor swap, owner / frame drift, route switch, or quantified crossing is not continuity of this case. |
| Elaboration handoff / result state | Accepted predecessor evidence already clears the earlier exact `Phase 6 (elaboration)` breakpoint for this same pocket. | Later rounds may not relitigate or erase that accepted clearance while claiming continuity of this case. |
| Reification / reconstruction state | The same exact route still reconstructs a `TMu`-bearing internal type on the helper-visible path. | Family drift, route drift, or witness-only rescue is not continuity of this case. |
| Internal output surface | The helper-visible internal surface still exposes recursive structure review-visibly as `TMu ...` plus `containsMu True`. | A non-recursive internal surface read is not continuity of this case. |
| Public output surface | The authoritative public surface still collapses to `TForall "a" Nothing (TVar "a")` on both public entrypoints for the same pocket. | Silent reinterpretation into visible recursive success or architecture proof is not continuity of this case. |
| Reviewer-visible evidence trail | Reviewers must be able to point to one exact pocket and one exact internal/public split across the accepted predecessor chain and later rounds. | Cross-family rescue, replay-only rescue, witness-only rescue, or broader capability narration is not continuity of this case. |

## Accepted Predecessor Chain

The refreshed family starts from the following accepted predecessor chain,
which later rounds must preserve without widening:

- accepted `N14` in
  `docs/plans/2026-03-24-automatic-iso-recursive-bound-var-target-same-lane-retained-child-next-cycle-decision-gate.md`
  preserves one exact same-lane retained-child packet as bounded predecessor
  evidence only;
- accepted strategic item `2` in
  `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md`
  keeps `non-cyclic-graph = unknown` as architecture-pressure context only;
- accepted strategic item `5` in
  `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md`
  fixes the phase-and-surface ledger vocabulary used here;
- accepted strategic item `6` in
  `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`
  keeps the repo-level posture at bounded-subset-only feasibility rather than
  broad accepted success;
- accepted strategic item `7` in
  `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architecture-decision-and-successor-plan-choice.md`
  keeps `continue within the current architecture` as the strongest lawful
  strategic read before this refreshed loop;
- accepted round `089` froze the exact pocket and predecessor ledger in
  `docs/plans/2026-03-25-same-lane-retained-child-stable-visible-persistence-case-and-review-ledger.md`;
- accepted round `090` localized the first earlier breakpoint to
  `Phase 6 (elaboration)` in
  `docs/plans/2026-03-25-same-lane-retained-child-stable-visible-persistence-breakpoint-audit.md`;
- accepted round `091` cleared that exact Phase-6 breakpoint in
  `docs/plans/2026-03-25-same-lane-retained-child-stable-visible-persistence-phase-6-elaboration-resolution.md`;
- accepted round `092` froze the current internal/public split in
  `docs/plans/2026-03-25-same-lane-retained-child-stable-visible-persistence-end-to-end-revalidation-and-classification.md`:
  helper-visible/internal `TMu ...` plus `containsMu True`, versus
  authoritative public `TForall "a" Nothing (TVar "a")`; and
- accepted round `093` consumed that exact read in
  `docs/plans/2026-03-25-same-lane-retained-child-stable-visible-persistence-successor-decision-gate.md`
  and fixed the bounded successor posture
  `blocker debt remains within the current architecture`, explicitly
  refusing to reopen `non-cyclic-graph` on weaker evidence.

Later rounds must not reinterpret this predecessor chain into broad automatic
recursive inference, alias-bound success, or an already-proved architecture
revision.

## Honest Starting Posture

The one honest starting posture for this refreshed family remains:
the exact pocket is still
`admitted but not reconstruction-visible / blocker debt`
inside the current architecture.

That posture is still binding because:

- the exact pocket still preserves recursive structure on the
  helper-visible/internal path;
- the authoritative public surface still collapses to
  `TForall "a" Nothing (TVar "a")`;
- accepted round `093` already kept that blocker debt inside the current
  architecture rather than reopening `non-cyclic-graph`; and
- no accepted artifact yet proves that the acyclic representation itself is
  impossible for this pocket.

This refreshed item-1 freeze therefore starts from blocker debt, not broad
success, not fail-closed rejection, and not an already-accepted architecture
reopen.

## Item-2 Handoff

The next round must consume this exact frozen case and review ledger only.

Its job is roadmap item `2`:
audit the exact authoritative public-output path for the same frozen pocket,
localize the first exact owner-local continuity-loss site between the
accepted helper-visible reconstruction path and the authoritative public
output path, and do so without widening into repair, family expansion, or
architecture revision.

Item `1` itself does not reopen `non-cyclic-graph`.

## Docs-Only Verification Note

This round is expected to change only documentation artifacts:

- this canonical item-1 public-output continuity case / review-ledger
  artifact; and
- `orchestrator/rounds/round-094/implementation-notes.md`.

Because the round does not touch `src/`, `src-public/`, `app/`, `test/`, or
`mlf2.cabal`, no full `cabal build all && cabal test` gate is triggered at
implement time unless the diff escapes that authorized docs-only surface.
