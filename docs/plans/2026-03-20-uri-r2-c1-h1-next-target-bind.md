# `H1` Next-Target Bind For Repaired `URI-R2-C1`

Date: 2026-03-20
Round: `round-050`
Roadmap item: `H1`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: repaired `URI-R2-C1`
Artifact kind: docs-only bind/selection freeze

## Stage Contract Freeze

This artifact implements only roadmap item `H1` for `attempt-1` with
`retry: null`.

It carries forward the accepted `G4 = continue-bounded` result and freezes one
more bounded next slice only. `H1` is a bind/selection stage only. It does not
perform production edits, test edits, replay reopen, roadmap mutation,
controller-state edits, bug-tracker updates, or subject widening.

The inherited boundary remains fixed and unchanged:

- explicit-only recursive baseline;
- non-equi-recursive semantics;
- non-cyclic structural graph encoding;
- no second executable interface; and
- no compatibility, convenience, or default-path widening.

This artifact therefore does not clear authority, uniqueness, owner stability,
constructor admissibility, replay-lane repair, or broad automatic recursive
inference beyond repaired `URI-R2-C1`.

## Accepted `G1` / `G2` / `G3` / `G4` Evidence Chain Carried Forward Without Reopening

Only accepted bounded predecessor facts are carried forward here:

1. `G1` finalized as the authoritative bind that froze exactly one bounded
   `G2` target under repaired `URI-R2-C1`: the local-binding
   `rootHasMultiInst` `keepTargetFinal` / `targetC` lane, while leaving
   `instArgRootMultiBase` explicitly unselected as the remaining adjacent
   family.
2. `G2` finalized as the authoritative implementation of only that bounded
   lane, introducing the reviewer-auditable proof
   `rootLocalMultiInst = rootBindingIsLocalType && rootHasMultiInst`, with
   `targetC -> rootFinal` admitted only for that local lane plus one focused
   positive example and one matched non-local fail-closed contrast.
3. `G3` finalized as the authoritative verification artifact for exactly that
   `G2` lane under read-only anchor checks, a fresh focused
   `ARI-C1 feasibility characterization (bounded prototype-only)` rerun, a
   fresh full repo gate, and predecessor continuity checks.
4. `G4` finalized authoritatively on `attempt-1` with result token
   `continue-bounded`, not `widen-approved` and not `stop-blocked`, so one more
   bounded non-widening cycle is the only lawful successor action.
5. Accepted negative findings remain binding:
   `U2 = authority-narrowed`,
   `U3 = uniqueness-owner-stable-refuted`,
   `U4 = constructor-acyclic-termination-refuted`.
   `H1` does not reinterpret any of them as implementation clearance.
6. `/Volumes/src/mlf4/Bugs.md` remains repaired-lane continuity context only.
   The resolved replay-path bug does not authorize replay reopen,
   `MLF.Elab.Inst`, `InstBot`, `boundVarTarget` widening, non-local widening,
   or broader recursive-inference work in this round.

`H1` starts from the accepted `G2` / `G3` local `rootLocalMultiInst` /
`targetC -> rootFinal` baseline unchanged. It does not reopen `G1`, `G2`, `G3`,
or `G4` as live work, and it does not treat accepted negative evidence as new
clearance for replay repair, `MLF.Elab.Inst`, or `InstBot`.

## Exact Selected `H2` Target (Exactly One)

The only frozen future `H2` target is:

local-binding `instArgRootMultiBase` fail-closed hardening in
`/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-050/src/MLF/Elab/Run/ResultType/Fallback.hs`
limited to the already-computed inst-argument multi-base aggregation at
`Fallback.hs:289-359` and the downstream local `keepTargetFinal` / `targetC`
selection in `Fallback.hs:671-697`.

Required interpretation of that one bounded slice:

- start from the accepted `G2` / `G3` baseline unchanged:
  `rootLocalMultiInst`, `targetC -> rootFinal`, and the accepted local
  multi-inst `ARI-C1` examples remain inherited starting conditions, not
  reopened target families;
- keep the selected work confined to the adjacent local-binding case where
  reviewer-auditable proof of the shape
  `rootBindingIsLocalType && instArgRootMultiBase` is the only new authority
  carrying the retained final-target selection through the chosen
  `keepTargetFinal` / `targetC` lane;
- treat `rootHasMultiInst`, `rootLocalSchemeAliasBaseLike`, and
  `boundVarTarget` as inherited context only rather than selected authority for
  this cycle, so `H2` does not widen into mixed trigger-family work; and
- keep the repaired `URI-R2-C1` lane fail-closed whenever the selected case
  would require non-local binding, replay reopen, `MLF.Elab.Inst`, `InstBot`,
  equi-recursive reasoning, cyclic structural encoding, a second interface, or
  any broader widening family.

### Why `instArgRootMultiBase` Is The Remaining Bounded Family

`instArgRootMultiBase` is the remaining bounded family because `G1` through `G4`
already selected, implemented, verified, and decision-gated the narrower local
`rootHasMultiInst` lane while explicitly leaving this adjacent family
unselected.

The current bounded evidence anchors show the remaining family already lives in
the same local target-selection block rather than in a different subsystem:

- `Fallback.hs:289-359` already computes the inst-argument multi-base summary
  that feeds `instArgRootMultiBase`; and
- `Fallback.hs:671-697` already threads `instArgRootMultiBase` through the
  local `keepTargetFinal` / `targetC` decision path.

That makes `instArgRootMultiBase` the next reviewer-auditable bounded step
without reopening the accepted `G` chain or widening into a different module
family.

### Future File Ownership

Future `H2` ownership is frozen to exactly these files only:

- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-050/src/MLF/Elab/Run/ResultType/Fallback.hs`
- `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-050/test/PipelineSpec.hs`

### Future Focused Coverage Intent

Future `H2` coverage remains one bounded extension of the existing
`ARI-C1 feasibility characterization (bounded prototype-only)` block in
`/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-050/test/PipelineSpec.hs`
only:

- add one local-binding multi-base success example that exercises the selected
  `instArgRootMultiBase` lane while the root stays on the accepted local
  `TypeRef` lane;
- add one matched fail-closed non-local contrast where the same-looking wrapper
  leaves the local lane and therefore must still reject or fall back
  non-recursively; and
- refresh the source-guard assertions only as needed to name the selected
  `instArgRootMultiBase` authority without reauthorizing `rootHasMultiInst`,
  `rootLocalSchemeAliasBaseLike`, or `boundVarTarget` as separate trigger
  families.

No second implementation family is selected in this artifact.

## Explicit Non-Selection And Non-Authorization

This `H1` bind does not authorize:

- edits to `/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-050/src/MLF/Elab/Inst.hs`;
- replay-repair reopen, including `MLF.Elab.Inst` / `InstBot`, as active `H2`
  work;
- reopening the completed `G1` / `G2` / `G3` / `G4` chain;
- `rootHasMultiInst`, `rootLocalSchemeAliasBaseLike`, or `boundVarTarget` as
  separate `H2` target families in this cycle;
- non-local binding widening, replay-path widening, or any broader
  cross-family widening;
- prototype or research entrypoints, or a second executable interface;
- equi-recursive reasoning or implicit unfolding;
- cyclic structural graph encoding;
- multi-SCC widening, heuristic ownership ranking, or manufactured authority;
- compatibility shims, convenience fallbacks, or default-path widening; or
- roadmap, controller-state, or bug-tracker edits as part of this bind.

Any broader target family requires a separate accepted roadmap change before
implementation.

## Retry-Aware Reviewer Handoff

This artifact is the immutable `attempt-1` bind/selection record for `H1`.
If `round-050` retries later, future attempts must be additive and must not
rewrite this artifact.

Per `orchestrator/retry-subloop.md`, reviewer outcomes supported by this
artifact remain:

- `accepted + finalize`
- `accepted + retry`
- `rejected + retry`

Reviewer record fields expected for this round remain:

- `Implemented stage result`
- `Attempt verdict`
- `Stage action`
- `Retry reason`
- `Fix hypothesis`

## Verification Notes

Commands executed in:
`/Users/ares/.codex/worktrees/d432/mlf4/.worktrees/round-050`

### Baseline Commands

- `git diff --check` -> pass (no output)
- `python3 -m json.tool orchestrator/state.json >/dev/null` -> pass
- `rg -n '"contract_version": 2|"retry": null|"retry": \{' orchestrator/state.json`
  -> pass
- `rg -n '^\d+\. \[(pending|in-progress|done)\]' orchestrator/roadmap.md`
  -> pass
- `test -f docs/superpowers/specs/2026-03-20-unannotated-iso-recursive-continue-bounded-h-cycle-design.md`
  -> pass
- `test -f docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
  -> pass
- `test -f docs/plans/2026-03-14-unannotated-iso-recursive-r5-research-stop-decision.md`
  -> pass
- `test -f docs/plans/2026-03-17-uri-r2-c1-r4-repair-decision-gate.md` -> pass
- `test -f docs/plans/2026-03-17-uri-r2-c1-u6-next-widening-decision-gate.md`
  -> pass
- `test -f orchestrator/retry-subloop.md` -> pass

### `H1`-Specific Checks

- `test -f docs/plans/2026-03-19-uri-r2-c1-g4-next-cycle-decision-gate.md`
  -> pass
- `python3 -m json.tool orchestrator/rounds/round-049/review-record.json >/dev/null`
  -> pass
- short `python3` assertion over `orchestrator/rounds/round-049/review-record.json`
  -> pass:
  - confirmed `G4` / `attempt-1` / `accepted` / `finalize` / `authoritative`
    metadata; and
  - confirmed canonical artifact path
    `docs/plans/2026-03-19-uri-r2-c1-g4-next-cycle-decision-gate.md`.
- `rg -c '^The only frozen future `H2` target is:$' docs/plans/2026-03-20-uri-r2-c1-h1-next-target-bind.md`
  -> pass:
  - `1`
- `awk 'NR<30 {print}' docs/plans/2026-03-20-uri-r2-c1-h1-next-target-bind.md | rg -n 'Round: `round-050`|Roadmap item: `H1`|Stage: `implement`|Attempt: `attempt-1`|Retry state: `null`|Live subject: repaired `URI-R2-C1`|explicit-only recursive baseline|non-equi-recursive semantics|non-cyclic structural graph encoding|no second executable interface|no compatibility, convenience, or default-path widening'`
  -> pass:
  - metadata and inherited-boundary lines present in the artifact header and
    stage-contract section.
- `sed -n '70,170p' docs/plans/2026-03-20-uri-r2-c1-h1-next-target-bind.md | rg -n 'The only frozen future `H2` target is:|instArgRootMultiBase|Fallback\\.hs:289-359|Fallback\\.hs:671-697|rootHasMultiInst|rootLocalSchemeAliasBaseLike|boundVarTarget|Future `H2` ownership is frozen|/src/MLF/Elab/Run/ResultType/Fallback.hs|/test/PipelineSpec.hs|No second implementation family is selected'`
  -> pass:
  - selected target, frozen ownership, and explicit non-selection of the other
    trigger families are all recorded.
- `sed -n '20,165p' docs/plans/2026-03-20-uri-r2-c1-h1-next-target-bind.md | rg -n '`continue-bounded`|authority-narrowed|uniqueness-owner-stable-refuted|constructor-acyclic-termination-refuted|does not reinterpret any of them as implementation clearance|does not reopen `G1`|replay reopen|MLF\\.Elab\\.Inst|InstBot|Bugs\\.md remains repaired-lane continuity context only'`
  -> pass:
  - accepted decision continuity, still-binding negative findings, and
    non-reopen boundaries are all recorded.

### Docs-Only Diff Evidence

This round remains docs-only. No production, test, public API, executable,
Cabal, roadmap, bug-tracker, or controller-state files were edited.

- `git status --short --untracked-files=all` -> docs/orchestrator-only change set:
  - `?? docs/plans/2026-03-20-uri-r2-c1-h1-next-target-bind.md`
  - `?? orchestrator/rounds/round-050/implementation-notes.md`
  - existing controller packet files remain present:
    - ` M orchestrator/state.json`
    - `?? orchestrator/rounds/round-050/plan.md`
    - `?? orchestrator/rounds/round-050/selection.md`
- `git diff --name-only` -> existing tracked controller diff only:
  - `orchestrator/state.json`
- `git diff --name-only -- . ':(exclude)docs/**' ':(exclude)orchestrator/**'` ->
  no output

### Full-Gate Skip Note

`cabal build all && cabal test` was intentionally not run in `H1` `attempt-1`
because this round is docs-only and does not edit `src/`, `src-public/`,
`app/`, `test/`, or `mlf2.cabal`.
