# `N10` Reopened-Loop Safety And Acceptance Contract For The Selected `boundVarTarget` Planning Subject

Date: 2026-03-23
Round: `round-077`
Roadmap item: `N10`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: accepted `N9 = boundVarTarget-planning-subject-selected`
Artifact kind: canonical docs-only invariant audit / acceptance contract

## Stage Contract Freeze

This artifact implements only roadmap item `N10` for `attempt-1` with
`retry: null`.

`N10` is docs-only safety/acceptance-contract work inside accepted
`N9 = boundVarTarget-planning-subject-selected`. It records the
verifier-checkable invariant audit, threatened-property boundaries, Acceptance
criteria, and No-go conditions that any later exact target bind for the
selected retained-child / nested-`forall` / binding-structure
`boundVarTarget` lane must satisfy before that later bind can even be proposed.

This artifact does not bind an exact target.
This artifact does not authorize implementation.
This artifact does not authorize verification.

This artifact also does not authorize merge action, roadmap edits,
`orchestrator/state.json` edits, bug-tracker edits, or predecessor-history
rewrites.

The inherited boundary remains fixed and unchanged:

- explicit-only recursive baseline;
- non-equi-recursive semantics;
- non-cyclic structural graph encoding;
- no second executable interface; and
- no compatibility, convenience, or default-path fallback widening
  (`no-fallback`).

## Accepted `L1` / `L2` / `N1` Through `N9` Continuity And Exact Packet Ownership

Only accepted predecessor continuity is carried forward here:

1. `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
   remains the inherited baseline contract: explicit recursive annotations are
   supported, automatic recursive-type inference remains unresolved and
   disabled, and the explicit-only / non-equi-recursive /
   non-cyclic-graph / no-second-interface / no-fallback boundary remains
   mandatory.
2. `docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md` remains the
   authoritative `L1` fail-closed record. Inside the repaired `URI-R2-C1`
   queue and the inherited boundary, it proved that no fresh lawful exact
   successor slice remained there.
3. `docs/plans/2026-03-21-uri-r2-c1-l2-post-l1-fail-closed-successor-decision-gate.md`
   remains the authoritative `L2` closeout. It finalized the repaired queue as
   `stop-blocked` and required separate later authority before any broader
   preserved route could become live work.
4. `docs/plans/2026-03-22-automatic-iso-recursive-post-l2-roadmap-amendment-authority-gate.md`
   remains the authoritative `N1` record. Its one bounded outcome,
   `reopen-planning-only`, reopened only a docs-first planning lane and did
   not authorize implementation or verification.
5. `docs/plans/2026-03-22-automatic-iso-recursive-next-live-subject-selection.md`
   remains the earlier `N2` selection record for the completed first reopened
   lane only. The preserved generic scheme-alias / base-like `baseTarget`
   subject is predecessor evidence only here and may not be silently carried
   forward as fresh authority for the selected `boundVarTarget` lane.
6. `docs/plans/2026-03-22-automatic-iso-recursive-base-target-safety-acceptance-contract.md`
   remains the earlier `N3` contract for the completed first reopened
   `baseTarget` lane only. It is predecessor evidence only here and does not
   replace the fresh subject-specific contract required after accepted `N9`.
7. `docs/plans/2026-03-22-automatic-iso-recursive-base-target-exact-target-bind.md`,
   `docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-bounded-implementation-slice.md`,
   `docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-bounded-verification-gate.md`,
   and
   `docs/plans/2026-03-22-automatic-iso-recursive-base-target-non-local-next-cycle-decision-gate.md`
   remain the accepted `N4` / `N5` / `N6` / `N7` packet for the exact non-local
   `baseTarget -> baseC` lane only. That exact accepted packet is predecessor
   evidence only here and does not authorize exact-target, implementation, or
   verification carry-forward for `boundVarTarget`.
8. `docs/plans/2026-03-22-automatic-iso-recursive-post-n7-roadmap-amendment-authority-gate.md`
   remains the authoritative `N8` record. Its one bounded outcome,
   `reopen-planning-only-successor-lane`, reopened exactly one fresh
   planning-only successor lane and still did not choose a subject, bind a
   target, authorize implementation, or authorize verification.
9. `docs/plans/2026-03-18-uri-r2-c1-e1-next-target-bind.md`,
   `docs/plans/2026-03-18-uri-r2-c1-e2-bounded-implementation-slice.md`,
   `docs/plans/2026-03-18-uri-r2-c1-e3-bounded-verification-gate.md`, and
   `docs/plans/2026-03-18-uri-r2-c1-e4-next-cycle-decision-gate.md`
   remain the accepted repaired-queue retained-child packet only. They are
   predecessor evidence only here and do not automatically authorize this fresh
   post-`N8` lane.
10. `tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
    still records that the earlier reopened lane already has predecessor-only
    `N3` / `N4` / `N5` / `N6` evidence, while the fresh post-`N8` successor
    lane selected by accepted `N9` still requires its own accepted contract for
    the selected `boundVarTarget` subject before any exact target can be bound.
11. `docs/plans/2026-03-23-automatic-iso-recursive-bound-var-target-next-live-subject-selection.md`
    remains the authoritative `N9` record. It selects exactly one planning
    subject only: the retained-child / nested-`forall` / binding-structure
    `boundVarTarget` route. It explicitly leaves later safety-contract,
    exact-bind, implementation, and verification work blocked.
12. `/Users/ares/.codex/worktrees/d432/mlf4/orchestrator/rounds/round-076/review-record.json`
    remains the acceptance proof that `N9` finalized as `accepted + finalize`
    with `final_outcome = "boundVarTarget-planning-subject-selected"`.
13. `/Users/ares/.codex/worktrees/d432/mlf4/Bugs.md` remains read-only
    predecessor context only. Open `BUG-2026-03-16-001` keeps replay /
    `MLF.Elab.Inst` / `InstBot` pressure visible, but it does not reopen replay
    or authorize a different live subject in this round.

The exact packet constrained by this artifact is therefore narrow:

- the repaired `URI-R2-C1` queue stays closed predecessor evidence only;
- the earlier `baseTarget` lane, including the earlier `N2` selection and
  earlier `N3` contract, stays predecessor evidence only;
- the exact accepted non-local `baseTarget -> baseC` packet stays predecessor
  evidence only;
- the accepted repaired-queue retained-child packet stays predecessor evidence
  only;
- accepted `N9` selects exactly one fresh planning subject only, the
  `boundVarTarget` lane; and
- replay reopen, `MLF.Elab.Inst`, `InstBot`, `boundTarget`,
  `schemeBodyTarget` as a live subject,
  `ResultType.View` (`src/MLF/Elab/Run/ResultType/View.hs`), every other
  fallback family, every different solver/pipeline subject, cross-family
  search, equi-recursive reasoning, implicit unfolding, cyclic structural graph
  encoding, multi-SCC support, second-interface work, and fallback widening
  remain blocked unless a later accepted roadmap item explicitly reopens them.

## Thesis Basis For The Selected Contract Axes

The thesis-backed narrowing for this contract comes from four section anchors
in `papers/these-finale-english.txt`:

1. `3.3.3.1 Grafting` says that grafting on graphic types must account for the
   binding structure of the graph, including how free and bound variables move
   under a larger owner. Repo-local implication: any later exact target bind
   must explain retained-child ownership and binder movement inside one bounded
   binding-structure route.
2. `5.1 Why rigid quantification?` says the `MLF` instance relation is adapted
   to the richer binding structure of graphic types. Repo-local implication:
   the selected subject must remain binding-structure-led and may not widen
   into a flatter or cross-family fallback search.
3. `8.3.1 Inlining bounds` says inverse translation is lawful only when the
   bound can be rebuilt unambiguously, the node is not `usedtwice`, the binder
   is a structural ancestor, and the binding flag can be reconstructed.
   Repo-local implication: nested-`forall` / nested-owner crossings and
   retained-child ownership must stay fail-closed rather than being assumed
   away.
4. `17.3 Perspectives` says recursive types together with second-order
   polymorphism are already difficult and that the main difficulty likely lies
   in recursion in the binding structure, while cyclic term-graphs were not the
   explored route. Repo-local implication: this contract stays explicit-only,
   iso-recursive, structurally acyclic, and single-family.

These anchors justify only contract obligations. They do not authorize code,
an exact target bind, implementation, or verification.

## Current Code/Test Anchor Packet For The Selected Subject

The current read-only source/test anchors identify the selected planning
subject precisely:

1. `/Users/ares/.codex/worktrees/d432/mlf4/src/MLF/Elab/Run/ResultType/Fallback.hs`
   still computes `boundVarTargetRoot` from
   `schemeBodyTarget targetPresolutionView rootC`, uses
   `boundHasForallFrom` to reject nested-`forall` and nested-owner crossings,
   computes `boundVarTarget` from retained-child candidates, keeps
   `keepTargetFinal` gated by the local-binding lane, and lets `targetC`
   choose either the retained child or `schemeBodyTarget targetPresolutionView rootC`
   without silently widening to a different family.
2. `/Users/ares/.codex/worktrees/d432/mlf4/src/MLF/Elab/Run/Scope.hs`
   still defines `schemeBodyTarget` as the owner-local `S'`-style selector for
   scheme-body translation. That makes `schemeBodyTarget` neighboring boundary
   context, not the selected `boundVarTarget` subject itself.
3. `/Users/ares/.codex/worktrees/d432/mlf4/test/PipelineSpec.hs`
   still carries the bounded retained-child evidence:
   the same-lane local `TypeRef` recursive success case, the same-lane source
   guard, and the nested-`forall` fail-closed contrast.

This packet identifies one planning subject only: the retained-child /
nested-`forall` / binding-structure route centered on `boundVarTargetRoot`,
`boundHasForallFrom`, `boundVarTarget`, `keepTargetFinal`, and the final
`targetC` choice. It is not `boundTarget`, not `schemeBodyTarget`, and not the
already-completed non-local `baseTarget -> baseC` packet.

## Threatened Properties For The Selected Subject

The selected `boundVarTarget` planning subject is admissible only because these
properties remain review-visible and fail-closed:

1. retained-child ownership must remain local, explicit, and traceable to one
   bounded binding-structure route rather than becoming hidden or search-wide.
2. nested-`forall` and nested-owner crossings must remain explicit rejection
   conditions, not alternate success paths.
3. target selection must remain confined to the bounded interaction among the
   retained child, `schemeBodyTarget`, and `rootFinal`.
4. recursive meaning must remain explicit-only and iso-recursive rather than
   equi-recursive or implicitly unfolded.
5. constraint representation must remain structurally acyclic and single-family;
   the selected route may not widen into cyclic encoding, multi-SCC reasoning,
   replay reopen, or cross-family search.

## Invariant audit

The thesis-backed invariant audit for the selected subject is frozen below.

## Thesis-Backed Invariant Audit

### Section `3.3.3.1 Grafting`

Repo-local audit obligations for the selected `boundVarTarget` subject:

- any later exact target bind must identify which retained child is being kept,
  which owner path binds it, and why the selected route stays inside one
  bounded binding-structure lane;
- any proposal that requires detached or ambiguous rebinding across owners is
  immediate no-go; and
- the admissibility story must describe how binding structure is preserved
  across the retained-child route, not how a broader search might recover it
  later.

### Section `5.1 Why rigid quantification?`

Repo-local audit obligations for the selected `boundVarTarget` subject:

- the selected subject must remain one adaptation to richer binding structure,
  not a license to widen into other fallback families or solver-wide search;
- any later exact target bind must explain why the chosen route is still
  bounded to the retained-child / nested-`forall` lane named by accepted `N9`;
  and
- if the target requires cross-family search to justify itself, it is outside
  this contract.

### Section `8.3.1 Inlining bounds`

Repo-local audit obligations for the selected `boundVarTarget` subject:

- retained-child ownership must remain reconstructible from one bounded parent /
  child / bound relation tied to current source/test anchors;
- nested-`forall` and nested scheme-root crossings must stay fail-closed via
  `boundHasForallFrom` rather than being reinterpreted as admissible;
- any later exact target bind must keep `schemeBodyTarget` as neighboring
  boundary context only and must explain when `targetC` chooses a retained
  child versus staying on `schemeBodyTarget` or `rootFinal`; and
- a target that depends on ambiguous binder or binding-flag reconstruction is
  immediate no-go.

### Section `17.3 Perspectives`

Repo-local audit obligations for the selected `boundVarTarget` subject:

- recursive planning must remain binder-centric, explicit-only, and
  iso-recursive;
- the selected subject must not rely on equi-recursive equality, implicit
  unfolding, or cyclic term-graph assumptions; and
- any later exact target bind must keep reasoning structurally acyclic and
  narrowly tied to one binding-structure family rather than solver-wide
  recursive inference.

## Invariant Audit Axes

`N10` accepts the selected subject only under these invariant axes:

1. retained-child ownership axis
   - Every future exact target bind must identify the kept child, the owner or
     parent relation that keeps it in-lane, the relevant bound, and why that
     ownership remains local to the selected `boundVarTarget` packet.
2. nested-`forall` / nested-owner fail-closed axis
   - Any future exact target bind must state the concrete fail-closed condition
     for `boundHasForallFrom`, nested scheme-root crossings, and nested-owner
     boundaries.
   - A proof story that assumes those crossings away, or that silently accepts
     them, does not satisfy this contract.
3. target-selection confinement axis
   - Any future exact target bind must explain the exact confinement among
     `boundVarTarget`, `schemeBodyTarget`, `keepTargetFinal`, and `targetC`.
   - `schemeBodyTarget` remains neighboring boundary context only; promoting it
     to the live subject is outside this contract.
4. recursive-boundary axis
   - The selected subject stays explicit-only, iso-recursive,
     non-equi-recursive, and non-cyclic-graph.
   - Implicit unfolding, equi-recursive equality, cyclic graph encoding,
     graph-cycle exceptions, or multi-SCC reasoning remain out of scope.
5. single-family confinement axis
   - The selected subject remains one retained-child / nested-`forall` /
     binding-structure planning lane.
   - Replay reopen, the earlier `baseTarget` lane, the exact accepted non-local
     `baseTarget -> baseC` packet, the repaired-queue retained-child packet,
     every other fallback family, every other solver/pipeline subject, and
     every cross-family search path remain blocked.

## Acceptance criteria for any later exact target bind

A later exact target bind is admissible only if reviewer-visible evidence can
answer YES to all of the following:

1. The proposed target is exactly one bounded slice inside the selected
   `boundVarTarget` route and does not reopen the earlier `baseTarget` lane,
   the exact accepted non-local `baseTarget -> baseC` packet, or the repaired
   queue as live work.
2. The target states a concrete retained-child ownership and
   binding-structure story tied to current source/test anchors:
   `boundVarTargetRoot`, `boundHasForallFrom`, `boundVarTarget`,
   `keepTargetFinal`, `targetC`, and the retained-child `PipelineSpec`
   evidence.
3. The target states concrete fail-closed conditions for nested-`forall`,
   nested-owner, and nested scheme-root crossings rather than treating those
   crossings as implicitly admissible.
4. The target explains the role of `schemeBodyTarget` as neighboring boundary
   context only and does not promote `schemeBodyTarget` to the selected live
   subject.
5. The target keeps the inherited boundary unchanged:
   explicit-only, iso-recursive, non-equi-recursive, non-cyclic-graph, no
   second interface, and no-fallback widening.
6. The target stays single-family and bounded:
   it does not depend on replay reopen, `MLF.Elab.Inst`, `InstBot`,
   `boundTarget`, `ResultType.View`, another fallback family, another
   solver/pipeline subject, cross-family search, implicit unfolding,
   equi-recursive equality, cyclic encoding, or multi-SCC reasoning.
7. The target names explicit out-of-scope exclusions for every unchanged
   blocked route and does not reinterpret accepted predecessor evidence as
   fresh implementation or verification clearance.

If any criterion above is missing, ambiguous, or satisfied only by heuristic,
search, or fallback reasoning, the exact target bind may not be proposed.

## No-go And Blocked-Boundary Record

This `N10` artifact keeps all of the following blocked:

- the earlier `baseTarget` lane as live work;
- the earlier `N2` selection as still-live subject authority;
- the earlier `N3` contract as automatic fresh-lane clearance;
- the exact accepted non-local `baseTarget -> baseC` packet as live work;
- the accepted repaired-queue retained-child packet as automatic fresh-lane
  clearance;
- replay reopen;
- `MLF.Elab.Inst` or `InstBot`;
- `boundTarget`;
- `schemeBodyTarget`;
- `ResultType.View`
  (`/Users/ares/.codex/worktrees/d432/mlf4/src/MLF/Elab/Run/ResultType/View.hs`);
- any other `ResultType.Fallback` family;
- any different solver or pipeline subject;
- cross-family search;
- implicit unfolding;
- equi-recursive reasoning or equality;
- cyclic structural graph encoding;
- graph-cycle exceptions;
- multi-SCC support;
- a second executable interface; and
- any compatibility, convenience, or default-path fallback widening.

Any future slice that requires one of those routes is outside this contract and
must remain no-go unless a later accepted roadmap item explicitly reopens that
route first.

## Non-Authorization And Immutability

This artifact does not bind:

- a later exact target;
- any implementation slice;
- any verification slice; or
- any successor decision beyond this contract.

This artifact also does not authorize:

- implementation or verification;
- merge action;
- roadmap edits;
- controller-state edits;
- bug-tracker edits; or
- predecessor-history rewrites.

Under `orchestrator/retry-subloop.md`, prior attempts and reviewer-owned
history remain immutable. This document is the canonical `attempt-1`
safety-contract artifact for `round-077` / `N10`.

## Lawful Verification Skip Note

`cabal build all && cabal test` remains intentionally out of scope for this
round because `N10` is docs-only by contract and this round does not touch
`src/`, `src-public/`, `app/`, `test/`, or `mlf2.cabal`.

This skip does not authorize a code path. It records only that the full Cabal
gate remains blocked until a later accepted roadmap item authorizes code or
test changes.

## One Authoritative `N10` Outcome

Authoritative `N10` outcome:
`boundVarTarget-safety-acceptance-contract-established`

This is the only lawful outcome recorded here.

Its exact meaning is fixed:

- accepted `L1` / `L2` / `N1` through `N9` remain binding predecessor
  continuity;
- the selected retained-child / nested-`forall` / binding-structure
  `boundVarTarget` lane now has one subject-specific safety/acceptance
  contract;
- any later exact target bind for this lane remains blocked unless it can
  satisfy every YES/NO acceptance criterion above; and
- this contract is not exact-target readiness, not implementation readiness,
  and not verification readiness.

It is not lawful to treat the earlier `N2` selection, the earlier `N3`
contract, the exact accepted non-local `baseTarget -> baseC` packet, or the
accepted repaired-queue retained-child packet as automatic carry-forward
authority on this fresh lane. Each remains predecessor evidence only.

## Reviewer Handoff

Reviewer should confirm that this artifact:

- is explicitly framed as `round-077` / `N10` / `attempt-1` / `retry: null`;
- treats accepted `L1` / `L2` / `N1` through `N9` as binding predecessor
  continuity;
- preserves the earlier `N2`, the earlier `N3`, the exact accepted non-local
  `baseTarget -> baseC` packet, and the repaired-queue retained-child packet as
  predecessor evidence only;
- grounds its contract axes in `3.3.3.1 Grafting`,
  `5.1 Why rigid quantification?`, `8.3.1 Inlining bounds`, and
  `17.3 Perspectives`;
- identifies the selected subject through the current `Fallback.hs`,
  `Scope.hs`, and `PipelineSpec.hs` anchors;
- defines an Invariant audit and Acceptance criteria for any later exact
  target bind without binding that target now; and
- keeps replay reopen, `MLF.Elab.Inst`, `InstBot`, `boundTarget`,
  `schemeBodyTarget`, `ResultType.View`, other fallback families, other
  subjects, equi-recursive reasoning, implicit unfolding, cyclic encoding,
  multi-SCC, second-interface work, and fallback widening blocked.
