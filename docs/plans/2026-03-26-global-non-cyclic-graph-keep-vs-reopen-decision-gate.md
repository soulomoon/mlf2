# Global `non-cyclic-graph` Keep Vs Reopen Decision Gate

Date: 2026-03-26
Round: `round-103`
Roadmap item: `item-5`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: one docs-only aggregate settlement gate for the inherited
`non-cyclic-graph` boundary after the accepted representative family-matrix
refresh
Artifact kind: canonical docs-only item-5 decision gate

## Stage Contract Freeze

This artifact implements only roadmap item `5` for `attempt-1` with
`retry: null`.

Item `5` is docs-only, aggregate-only, and decision-only. It consumes only:

- the accepted item-1 settlement freeze;
- the accepted item-2 and item-3 bounded production-surface slices;
- the accepted item-4 representative family-matrix refresh; and
- bounded predecessor strategic context needed to interpret those accepted
  artifacts.

This round does not authorize:

- new evidence reruns, new source-level claims, or fresh mechanism
  experiments;
- edits under `src/`, `src-public/`, `app/`, `test/`, or `mlf2.cabal`;
- edits to `orchestrator/state.json`, roadmap contracts, retry contracts,
  verification contracts, or `Bugs.md`;
- production implementation, hardening, or repo-level capability claims; or
- silent widening into equi-recursive reasoning, cyclic structural graphs,
  multi-SCC search, second interfaces, or fallback behavior.

The inherited boundary remains unchanged:

- explicit recursive annotations remain the production baseline;
- recursive meaning remains iso-recursive only;
- no equi-recursive equality or implicit unfolding is authorized;
- no cyclic structural graph encoding or multi-SCC search is authorized; and
- no second interface or fallback widening is authorized.

The exact item-1 settlement bar remains binding:

| Lawful outcome | Binding bar |
| --- | --- |
| `non-cyclic-graph = keep` | Accepted production-surface evidence must make representative settlement credible inside the inherited acyclic model across positive families `P1` through `P6` and required negative or bounded families `N1` through `N6`, including `stable visible persistence` wherever positive `P6` success is claimed. |
| `reopen the non-cyclic-graph revision question` | Accepted aggregate evidence must show that at least one required positive family cannot be carried credibly inside that inherited acyclic model without revisiting the current `non-cyclic-graph` boundary. |

Weaker evidence still fails closed on both sides. One bounded admitted pocket,
one blocker-debt pocket, one inherited `unknown` classification, one bounded
exact-pocket predecessor decision, or one strategic read of
`continue within the current architecture` cannot settle this gate by itself.

## Accepted Decision-Input Ledger

| Decision input | Accepted read |
| --- | --- |
| Inherited baseline boundary | `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md` still keeps the explicit-only / iso-recursive / non-equi-recursive / non-cyclic-graph / no-second-interface / no-fallback baseline live. |
| Accepted item-1 settlement bar | `docs/plans/2026-03-26-global-non-cyclic-graph-settlement-contract-and-unresolved-family-evidence-ledger.md` freezes the exact later bar for `non-cyclic-graph = keep` versus `reopen the non-cyclic-graph revision question`, and still says the current repo-level read before this gate is `bounded subset only`. |
| March 25 strategic posture | `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architecture-decision-and-successor-plan-choice.md` keeps `continue within the current architecture` as the strongest accepted predecessor strategic posture before the refreshed global matrix family. |
| March 25 architecture audit | `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-architectural-constraint-audit.md` still classifies `iso-recursive = keep`, `non-equi-recursive = keep`, `non-cyclic-graph = unknown`, and `no-fallback = keep`. |
| Full-pipeline visibility bar | `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md` still makes `stable visible persistence` the only lawful positive `P6 reconstruction-visible-output` success token. Solver-only, helper-only, or internal-only visibility is non-success. |
| Accepted item-2 slice | `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md` keeps `C1`, `C2`, and `C5` at `admitted but not reconstruction-visible / blocker debt`. `C1` still ends in visible non-recursive output, while `C2` / `C5` still reuse one exact same-lane pocket whose authoritative public output collapses. |
| Accepted item-3 slice | `docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md` keeps `C3` at `fail-closed rejection`, keeps `P5` reject-side only, and keeps `C7` below `stable visible persistence` because helper-visible/internal recursion still does not survive to authoritative public output. |
| Accepted item-4 matrix refresh | `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md` binds the current aggregate tally: zero `stable visible persistence` rows; blocker-debt rows `P1-row`, `C1`, `C2`, `C5`, and `C7`; fail-closed rows `C3`, `C4`, and `C6`; `P5` reject-side only; `P6` below visible persistence; and `N4` pressure still context only until this gate decides otherwise. |
| Positive-family ledger | The accepted item-4 ledger leaves `P1` as bounded local support only, `P2` admitted but visibly non-recursive, `P3` and `P4` dependent on one same exact blocker-debt pocket, `P5` reject-side only, and `P6` without any representative row at `stable visible persistence`. |
| Negative and bounded-family ledger | The accepted item-4 ledger keeps `N1`, `N2`, and `N6` fail closed or bounded; `N3` and `N5` stay out of scope under the unchanged baseline; and `N4` remains the only live architecture-pressure axis. |
| Same-lane exact-pocket predecessor chain | `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md` and its predecessor chain remain bounded predecessor context only. They strengthen one exact same-lane pressure point but do not globally settle the matrix. |
| Bug context | `BUG-2026-03-16-001` remains predecessor replay context only. It is not lawful aggregate evidence for either branch. |

This ledger is evidence-only. It preserves accepted predecessor truth without
choosing the item-5 outcome yet.

## Outcome Evaluation Schema

| Outcome token | Supporting evidence | Blocking evidence | Lawful-status read |
| --- | --- | --- | --- |
| `non-cyclic-graph = keep` | Accepted predecessor strategy still keeps `continue within the current architecture` as the strongest pre-refresh aggregate posture. `P1-row` still shows bounded local support inside the inherited acyclic model. `C1`, `C2`, `C5`, and `C7` still show admitted pockets inside that same model, and `C3`, `C4`, and `C6` still fail closed rather than drifting into unsoundness or uncontrolled search. | The item-1 keep bar is stricter than bounded feasibility. The accepted item-4 matrix still has zero `stable visible persistence` rows, `P5` remains reject-side only, `P6` remains below visible persistence on every representative row, and `P2` through `P4` still depend on bounded blocker-debt packets rather than representative production-surface success. The accepted aggregate read therefore remains `bounded subset only`, not credible representative settlement across `P1` through `P6` and `N1` through `N6` inside the inherited acyclic model. | `not selected`: the accepted aggregate matrix no longer satisfies the item-1 keep bar without weakening that bar. |
| `reopen the non-cyclic-graph revision question` | Inference from the accepted item-1 through item-4 chain: the refreshed representative matrix now fails the keep bar globally, not pocket-locally. `P6` is a required positive family, and the accepted full-pipeline contract says it needs `stable visible persistence`. Yet the accepted matrix shows none anywhere. The two admitted family routes that are supposed to carry recursive evidence through the current architecture both fail that burden on accepted production surfaces: `C1` stays visibly non-recursive, while `C7` still preserves recursive structure only on the helper-visible/internal side and collapses to authoritative public `forall identity`. `P3` and `P4` still reuse that same exact blocker-debt pocket, and `P5` remains reject-side only. Because the other inherited architecture axes stay `keep`, the only live boundary left for this global failure is `non-cyclic-graph`; the accepted aggregate evidence now upgrades `N4` from pressure context to the honest next architecture question. | Earlier predecessor posture favored staying within the current architecture, and the same-lane exact-pocket chain still proves that one bounded pocket survives deeply inside the acyclic model before authoritative collapse. That bounded predecessor truth remains real, but item-1 already said one exact pocket or one blocker-debt pocket cannot settle the global gate by itself. The refreshed representative matrix is stronger than that bounded predecessor context and now overrides it for repo-level settlement. | `selected`: the accepted aggregate record now supports reopening the `non-cyclic-graph` question and no longer supports keeping the inherited acyclic boundary as the repo-level settlement result. |

No third outcome, mixed verdict, or renewed `unknown` token is lawful here.
This gate must record exactly one of the two item-5 outcomes.

## One Authoritative Item-5 Outcome

Authoritative item-5 outcome token:
`reopen the non-cyclic-graph revision question`

This is the strongest lawful read on the accepted aggregate evidence.

Why this outcome is selected:

1. The accepted item-1 keep bar is no longer met. The refreshed
   representative matrix still records zero `stable visible persistence`
   rows and still leaves the repo-level aggregate read at `bounded subset
   only`.
2. The accepted positive-family ledger no longer leaves a credible
   representative path to repo-level success inside the inherited acyclic
   model. `P1` remains bounded local support only; `P2`, `P3`, and `P4`
   remain blocker-debt only; `P5` remains reject-side only; and `P6`
   remains below the full-pipeline visibility bar everywhere.
3. `P6` is the decisive required family for the global gate because the
   accepted full-pipeline contract makes visible end-to-end persistence a
   mandatory repo-level success condition. The accepted matrix now shows
   that neither admitted family route can carry that burden on the existing
   authoritative surfaces:
   `C1` ends in visible non-recursive output, and `C7` still collapses from
   helper-visible/internal recursion to authoritative public `forall
   identity`.
4. Inference from the accepted aggregate ledger: once every representative
   positive family is still below success, while `iso-recursive`,
   `non-equi-recursive`, and `no-fallback` remain accepted `keep` axes, the
   only remaining architecture question left to revisit is the inherited
   `non-cyclic-graph` boundary itself. At this global stage, the deficit is
   no longer honest to describe as mere bounded implementation debt.
5. The lawful next move is therefore a same-family roadmap revision focused
   on the `non-cyclic-graph` question only. `item `6` remains blocked` until
   that revision settles whether and how the architecture boundary changes.

This reopen is bounded:

- it reopens only the `non-cyclic-graph` question;
- it does not silently authorize equi-recursive semantics;
- it does not silently authorize cyclic search, multi-SCC search, second
  interfaces, or fallback behavior; and
- it does not claim current implementation completeness or a repo-level
  capability win.

## Why The Non-Selected Outcome Is Weaker

`non-cyclic-graph = keep` is weaker on the accepted evidence because it would
require treating `bounded subset only` as if it were already representative
settlement.

The accepted record still preserves real bounded predecessor truth:

- the strongest same-lane pocket still survives solver admission, bounded
  elaboration, and helper-visible/internal recursive output inside the
  inherited acyclic model;
- `C1` still preserves one admitted non-local packet inside that same model;
  and
- the accepted strategic posture before the refreshed global matrix still
  said `continue within the current architecture`.

But the refreshed global matrix now withholds the stronger keep conclusion:

- zero representative rows reach `stable visible persistence`;
- every positive family remains below repo-level success, with `P5`
  reject-side only and `P6` unsatisfied on every representative route;
- the current aggregate read is still `bounded subset only`, which item-1
  expressly said is insufficient for `non-cyclic-graph = keep`; and
- the same-lane predecessor chain is globally bounded context only, not a
  lawful substitute for representative settlement across the full family
  matrix.

Keeping the inherited acyclic boundary at this stage would therefore weaken
the item-1 acceptance bar and silently continue past accepted aggregate
failure that the refreshed matrix now makes reviewer-visible.

## Round Closure Note

This artifact records exactly one aggregate item-5 outcome and closes only
the current global settlement gate.

It does not update roadmap contracts, does not authorize implementation,
and does not create any side ledger or implementation-notes artifact for this
round.
