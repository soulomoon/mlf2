# Global `non-cyclic-graph` Same-Lane Retained-Child Public-Output Continuity Architecture-Amendment Lane Open-Or-Stop Decision

Date: 2026-03-26
Round: `round-108`
Roadmap item: `item-5`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: one docs-only rev-002 item-5 aggregate lane-open-or-stop
decision for the exact same-lane retained-child / public-output continuity
pocket already selected in accepted item `2`, contract-governed in accepted
item `3`, and audit-bound in accepted item `4`
Artifact kind: canonical docs-only rev-002 item-5 aggregate decision record

## Stage Contract Freeze

This artifact implements only roadmap item `5` for `attempt-1` with
`retry: null`.

It consumes accepted rev-001 reopen truth plus accepted rev-002 items `1`
through `4` as authoritative predecessor authority and freezes exactly this
much, and no more:

- rev-002 remains planning-only and architecture-only;
- accepted items `1` through `4` remain binding and are not relitigated;
- rev-001 items `6` through `8` remain blocked unless a later accepted
  successor revision explicitly changes that status;
- exactly one already-selected same-lane `C2` / `C5` / `C7` pocket remains
  in scope; and
- exactly one lawful item-5 outcome token is recorded for that one pocket
  only:
  `open one bounded same-family architecture-amendment lane`.

This artifact does not:

- reopen subject selection or introduce a second selected subject;
- redefine, restate, rename, or replace the accepted item-3 bars;
- alter the accepted item-4 bind across rows, packet, modules, commands, or
  review-visible surfaces;
- authorize edits to `orchestrator/state.json`, roadmap files, retry files,
  verification files, `Bugs.md`, source code, tests, or Cabal files; or
- authorize multi-SCC search, second interfaces, fallback widening,
  production implementation, hardening, or broad capability claims.

The inherited boundary also remains unchanged:

- `iso-recursive = keep`;
- `non-equi-recursive = keep`;
- `no-fallback = keep`;
- explicit recursive annotations remain the production baseline;
- equi-recursive semantics and implicit unfolding remain blocked; and
- no live cyclic-structure implementation is authorized by this decision.

The accepted item-3 bars are predecessor decision inputs only. This
artifact cites them only by their exact accepted names and does not create a
new contract.

## Accepted Decision-Input Ledger

| Decision input | Accepted read carried forward |
| --- | --- |
| `orchestrator/rounds/round-107/review-record.json` | Accepted item `4` is authoritative. The exact rows `C2`, `C5`, and `C7`, the exact packet, the exact module/command set, and the exact six review-visible surfaces are frozen for later aggregate decision use. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-keep-vs-reopen-decision-gate.md` | Accepted rev-001 item `5` reopened only the `non-cyclic-graph` question, preserved `iso-recursive = keep`, `non-equi-recursive = keep`, and `no-fallback = keep`, and did not re-enable rev-001 items `6` through `8`. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-reopened-revision-authority-and-candidate-boundary-freeze.md` | Accepted item `1` froze exactly one planning-only candidate boundary: whether one bounded single-component cyclic-structure successor lane is needed for the strongest admitted retained-child / public-output continuity pressure. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-reopened-subject-selection.md` | Accepted item `2` selected exactly one live reopened subject: the exact same-lane retained-child / public-output continuity pocket already carried by `C2`, `C5`, and `C7`, with non-local `C1` retained only as contrast context. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-safety-and-acceptance-contract.md` | Accepted item `3` made exactly three bars lawful for that one selected pocket only and explicitly left the lane-open-or-stop choice to item `5`. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-same-lane-retained-child-public-output-continuity-architecture-pressure-audit-target-and-evaluation-surface-bind.md` | Accepted item `4` froze the exact evidence surface for later decision use only, kept `C1` contrast-only, and kept `C5` / `C7` as rereads of the same exact pocket rather than second packets, routes, or interfaces. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md` | Accepted predecessor evidence still keeps `C2` and `C5` on one same exact same-lane pocket only. Helper-visible/internal recursion remains visible on that pocket, but authoritative public output still collapses. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-c3-c7-production-surface-settlement-evidence-slice.md` | Accepted predecessor evidence still keeps `C7` as the public-output continuity face of that same exact pocket only, still below `stable visible persistence`, with both authoritative public entrypoints at `Right (TForall "a" Nothing (TVar "a"))`. |
| `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md` | Accepted predecessor evidence still concentrates the strongest admitted same-family pressure on the exact `C2` / `C5` / `C7` pocket and does not widen the live question into broader family search, second interfaces, or fallback behavior. |
| `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-current-architecture-vs-non-cyclic-graph-decision-gate.md` | Accepted predecessor context kept the same exact pocket at `blocker debt remains within the current architecture`, but that earlier bounded decision no longer settles the reopened rev-002 lane-open-or-stop gate by itself. |
| `docs/plans/2026-03-26-same-lane-retained-child-public-output-continuity-authoritative-collapse-clear-or-confirm.md` | Accepted predecessor blocker-proof context fixed that the bounded `Pipeline.hs` / `TermClosure.hs` root-handoff slice exposes no alternate whole-packet authoritative recursive result for this same exact pocket inside the inherited surface set. |

These accepted artifacts are predecessor authority only. None of them
already opens the lane, broadens the selected pocket, or authorizes
implementation work.

## Outcome Evaluation Against The Two Lawful Item-5 Tokens

| Lawful item-5 token | Accepted evidence supporting it | Accepted evidence blocking it | Lawful-status read |
| --- | --- | --- | --- |
| `open one bounded same-family architecture-amendment lane` | Accepted rev-001 reopen already withdrew the stronger aggregate `non-cyclic-graph = keep` result while preserving the inherited keep axes. Accepted item `1` froze exactly one bounded same-family single-component candidate boundary. Accepted item `2` froze exactly one live pocket. Accepted item `4` then bound exact rows, packet, modules, commands, and six review-visible surfaces for that same pocket only. Across those frozen surfaces, accepted predecessor evidence still shows helper-visible/internal recursive continuity on the exact same packet while both authoritative public entrypoints remain `Right (TForall "a" Nothing (TVar "a"))`, and the accepted bounded `Pipeline.hs` / `TermClosure.hs` blocker-proof still exposes no alternate whole-packet authoritative recursive result inside the inherited surface set. Inference from that accepted chain: the selected pocket no longer cleanly satisfies the accepted `acyclic still sufficient` bar, while the unresolved pressure still remains bounded to one same-family, same-lane, same-packet pocket only. | Earlier accepted bounded predecessor context for the same pocket still said `blocker debt remains within the current architecture`. That remains real predecessor context, but it blocks the open-lane token only if the accepted rev-002 item-1-through-item-4 chain still leaves `acyclic still sufficient` cleanly available. The accepted reopened chain no longer does: the pocket remains below authoritative public recursive continuity on the frozen surfaces, and the accepted bounded blocker-proof still shows no alternate authoritative whole-packet result inside those same surfaces. | `selected` |
| `stop without opening that lane` | This token would be lawful only if the accepted item-1-through-item-4 chain could not cleanly support either the accepted `acyclic still sufficient` bar or the accepted `single-component cyclic-structure successor lane justified` bar without widening scope or weakening inherited invariants. The earlier same-pocket current-architecture decision and the preserved keep axes therefore remain real cautionary context. | The accepted item-1 freeze, item-2 selection, item-3 contract, item-4 bind, and accepted predecessor blocker-proof chain do cleanly support one bounded same-family successor-lane opening for this exact pocket without widening scope or weakening inherited invariants. The pressure remains one exact `C2` / `C5` / `C7` pocket only, `C1` stays contrast-only, `C5` and `C7` stay rereads of the same packet only, and all inherited keep axes remain intact. Because the accepted record still supports that one bounded open-lane result, fail-closed stop is weaker than the selected token. | `not selected` |

No third token, mixed token, or renewed `unknown` token is lawful here.
This gate records exactly one of the two lawful item-5 outcomes.

## One Authoritative Item-5 Outcome

Authoritative item-5 outcome token:
`open one bounded same-family architecture-amendment lane`

This is the strongest lawful read on the accepted item-1-through-item-4
evidence chain.

Why this outcome is selected:

1. Accepted rev-001 item `5` already reopened the inherited
   `non-cyclic-graph` question while preserving
   `iso-recursive = keep`,
   `non-equi-recursive = keep`, and
   `no-fallback = keep`.
2. Accepted item `1` froze exactly one candidate boundary, and accepted item
   `2` froze exactly one live pocket inside that boundary:
   the same exact `C2` / `C5` / `C7` pocket and no other.
3. Accepted item `4` then froze the exact rows, packet, module set, command
   set, and six review-visible surfaces for this one pocket only. That bind
   keeps the decision narrow enough to avoid broad family search, multi-SCC
   search, second interfaces, fallback widening, or production claims.
4. Across those exact frozen surfaces, accepted predecessor evidence still
   keeps recursive structure helper-visible/internal only, while both
   authoritative public entrypoints still remain
   `Right (TForall "a" Nothing (TVar "a"))`. The accepted bounded
   `Pipeline.hs` / `TermClosure.hs` blocker-proof also still shows no
   alternate whole-packet authoritative recursive result for this same
   pocket inside the inherited surface set.
5. Inference from the accepted record: for this one exact pocket, the
   accepted item-3 bar
   `single-component cyclic-structure successor lane justified`
   is the only bar that still reads cleanly without widening scope or
   weakening inherited invariants. The accepted record therefore supports
   opening one bounded same-family architecture-amendment lane for later
   narrower planning, rather than stopping here.

This outcome is intentionally narrow:

- it opens only one bounded same-family architecture-amendment lane for the
  one already-selected same-lane `C2` / `C5` / `C7` pocket;
- it does not settle a broader family, a broader architecture theory, or a
  repo-level capability claim;
- it does not authorize multi-SCC search, second interfaces, fallback
  widening, production implementation, or hardening; and
- it does not unblock rev-001 items `6` through `8` by implication.

## Blocked Scope Preserved

All of the following remain explicitly blocked after this item-5 decision:

- any change to accepted item-1 truth, accepted item-2 truth, accepted
  item-3 truth, accepted item-4 truth, or accepted rev-001 truth;
- any reopening of subject selection or any promotion of `C1` into a second
  live subject;
- any treatment of `C5` or `C7` as second packets, second routes, second
  interfaces, or separate successor lanes;
- any redefinition of the accepted item-3 bars or any alteration of the
  accepted item-4 bind;
- any widening into broader family search, multi-SCC search, second
  interfaces, fallback widening, equi-recursive semantics, implicit
  unfolding, production implementation, hardening, or broad capability
  claims; and
- any claim that rev-001 items `6` through `8` are unblocked without a later
  accepted successor revision saying so explicitly.

## Successor-Revision Handoff Note

Because the authoritative item-5 outcome is
`open one bounded same-family architecture-amendment lane`, any later
successor revision may narrow only to that one bounded same-family
architecture-amendment lane for this same exact pocket:

- same family: same-lane retained-child;
- same selected rows: `C2`, `C5`, and `C7`;
- same exact packet;
- same anchor: `boundVarTargetRoot`;
- same owner-local retained-child frame;
- same route:
  `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC`; and
- same clear-boundary-only status.

Any later handoff must remain narrower than the current reopened family and
must preserve all inherited keep axes and blocked work unless a later
accepted successor revision explicitly changes the live boundary.

Any such successor revision still remains planning-only and
architecture-only unless a later accepted artifact explicitly changes that
status.

This artifact records exactly one aggregate item-5 outcome and closes only
the current rev-002 lane-open-or-stop gate. It is not implementation
clearance, not a broad architecture rewrite, and not permission to touch any
file outside this artifact path.
