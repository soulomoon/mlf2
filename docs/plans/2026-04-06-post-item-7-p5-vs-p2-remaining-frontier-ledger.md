# Post-Item-7 P5 Vs P2 Remaining-Frontier Ledger

Date: 2026-04-06
Round: `round-198`
Milestone: `milestone-3`
Direction: `direction-3a-refresh-the-p5-vs-p2-gap-ledger`
Extracted item: `refresh-the-p5-vs-p2-gap-ledger`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: one docs-only reread of the post-milestone-2 `P5` versus `P2`
remaining frontier only
Artifact kind: canonical milestone-3 remaining-frontier reread ledger

## Stage Contract Freeze

This artifact implements only `round-198` /
`milestone-3` / `direction-3a-refresh-the-p5-vs-p2-gap-ledger` for
`attempt-1` with `retry: null`.

This round is docs-only, aggregate-only, current-architecture-only, and
non-widening. Its job is to consume only the accepted post-`round-193`
routing baseline, the accepted `round-191` positive-family aggregate read,
the accepted exact `round-181` `C1` packet provenance, and the settled
milestone-2 `round-197` `P5` settlement surface, then name the current
strongest remaining positive-family frontier honestly.

This artifact does not:

- reopen the March 28 exact `P5` packet or the accepted `round-151`
  reclassification;
- relitigate prior same-lane settlements or the accepted exact `C1` packet as
  live family closure;
- create new implementation evidence, new tests, or a new `P2` lane freeze;
- authorize repo-level readiness, cyclic search, multi-SCC search, fallback
  behavior, or a second interface; or
- collapse refreshed `P5` evidence and preserved `P2` folklore into one
  blended claim.

## Authority / Evidence Ledger

| Source | Binding read carried into this reread |
| --- | --- |
| `docs/plans/2026-04-05-general-automatic-iso-recursive-full-inference-repo-level-readiness-and-architecture-decision.md` | Accepted `round-193` selected `continue-bounded`, kept both `P2 non-local-propagation` and `P5 polymorphism-nested-forall` unresolved, and named `P5` as the sharper blocker family to route first. |
| `orchestrator/rounds/round-193/review-record.json` | Approved that item-7 decision as docs-only, kept repo-level readiness blocked by unresolved `P2` and `P5`, and limited the successor move to one planning-only `P5` gate. |
| `docs/plans/2026-04-05-general-automatic-iso-recursive-full-inference-positive-family-aggregate-classification.md` | Accepted `round-191` fixed the aggregate classification that still governs this reread: `P2` is `packet-specific folklore`, while `P5` remains `current-architecture blockers`. |
| `orchestrator/rounds/round-191/review-record.json` | Approved that aggregate artifact as bounded to the accepted `C1` packet plus same-lane chain / frontier ledger and confirmed `P2` stayed packet-specific while `P5` stayed blocked. |
| `orchestrator/rounds/round-181/review-record.json` | Accepted the exact `C1` packet result: the non-local scheme-alias / base-like packet stays recursive on `runPipelineElab` and `runPipelineElabChecked` without widening beyond the bounded item-5 slice. |
| `orchestrator/rounds/round-181/implementation-notes.md` | Preserves the packet boundary underneath that result: the fallback surface stays on the non-recursive `baseTarget -> baseC` read, so the accepted `C1` evidence is one exact authoritative packet rather than a family-wide non-local closure. |
| `docs/plans/2026-04-06-post-item-7-p5-post-implementation-settlement-surface-and-exact-repo-impact-read.md` | Accepted `round-197` settled one retained-child clear-boundary `P5` lane only: `sameLaneAliasFrameClearBoundaryExpr` now has bounded current-architecture support on `runPipelineElab` / `runPipelineElabChecked`, `nestedForallContrastExpr` remains fail-closed with `PhiTranslatabilityError`, and the merged payload stayed `test-only`. |
| `orchestrator/rounds/round-197/review-record.json` | Approved that milestone-2 settlement as a docs-only republication of merged `round-196` evidence and confirmed it stayed lane-bounded rather than promoting general `P5` family closure. |

## Refreshed Two-Row Comparison Ledger

| Remaining-frontier row | Fixed accepted read | Limiting read preserved here | Frontier effect |
| --- | --- | --- | --- |
| `P5 polymorphism-nested-forall` | The settled milestone-2 surface now gives one retained-child clear-boundary success lane: `sameLaneAliasFrameClearBoundaryExpr` has bounded current-architecture support on `runPipelineElab` / `runPipelineElabChecked`, while `nestedForallContrastExpr` remains fail-closed with `PhiTranslatabilityError`, and the merged payload stayed `test-only`. | This is one retained-child clear-boundary lane only. It is not general `P5` family closure, not reusable positive support across the broader nested-`forall` family, and not a repo-level readiness result. | Milestone-2 narrowed `P5`, but it did not remove `P5` as a live family-level blocker because the broader quantified-crossing family still lacks positive support beyond that one lane. |
| `P2 non-local-propagation` | The accepted `C1` non-local scheme-alias / base-like packet remains recursively visible on `runPipelineElab` / `runPipelineElabChecked`, while the fallback `baseTarget -> baseC` surface remains the packet boundary. The accepted aggregate classification therefore remains `packet-specific folklore`. | No second non-local packet, adjacent chain, or family frontier has been added since the accepted `round-181` / `round-191` ledger. | `P2` remains a real but packet-bounded positive signal, not a refreshed family-level blocker that outranks a still-unsettled broader `P5` family. |

## Remaining-Frontier Evaluation Matrix

| Lawful routing outcome under review | Fit against the fixed accepted ledger | Blocking read from the same ledger | Lawful status |
| --- | --- | --- | --- |
| Keep `P5` above `P2` in the remaining-frontier ranking | `round-193` already routed first through `P5` as the sharper blocker. `round-197` settles one retained-child clear-boundary lane, but the accepted reread still preserves fail-closed quantified-crossing contrast at `nestedForallContrastExpr` and still lacks reusable positive support across the broader `P5 polymorphism-nested-forall` family. | None stronger than the preserved family-level `P5` gap. The accepted `P2` record still names one exact `C1` packet only. | `selected` |
| Advance immediately to a bounded `P2` freeze | The accepted `C1` packet proves that one non-local lane exists and remains worth preserving below family closure. | The same fixed ledger does not honestly demote `P5` below that packet-specific `P2` read: milestone-2 narrowed one `P5` lane only, left the broader family unresolved, and preserved fail-closed quantified-crossing contrast. Promoting `P2` next would therefore outrun the accepted blocker ranking. | `not selected` |

## Remaining-Frontier Conclusion

`P5 remains the stronger blocker / pressure source`

The fixed accepted ledger does not honestly demote `P5` below the accepted
packet-specific `P2` read. Milestone-2 settled one retained-child
clear-boundary lane, but the broader `P5 polymorphism-nested-forall` family
still lacks reusable positive support and still preserves fail-closed
quantified-crossing contrast at `nestedForallContrastExpr`. The accepted
`P2` ledger still consists of one exact `C1` packet only.

Next lawful milestone-3 routing consequence: keep
`direction-3b-freeze-one-bounded-p2-follow-on-lane` closed on this ledger and
route only to the later
`direction-3c-record-p5-dominant-boundary-pressure` follow-on.

## Non-Claims

This artifact does not claim:

- general `P5` family closure from `sameLaneAliasFrameClearBoundaryExpr`;
- promotion of the accepted `C1` packet into non-local family closure;
- a new `P2` freeze, new implementation evidence, or new tests;
- repo-level readiness or an immediate architecture revision; or
- any cyclic, multi-SCC, fallback, or second-interface widening.
