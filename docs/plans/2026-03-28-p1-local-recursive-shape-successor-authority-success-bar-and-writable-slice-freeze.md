# P1 Local-Recursive-Shape Successor Authority, Success Bar, And Writable-Slice Freeze

Date: 2026-03-28
Round: `round-124`
Roadmap item: `item-1`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: one bounded current-architecture successor freeze for the
remaining `P1 local-recursive-shape authoritative-surface` family
Artifact kind: canonical docs-only successor-boundary freeze

## Stage Contract Freeze

This artifact implements only roadmap item `1` for `attempt-1` with
`retry: null`.

This round is docs-only, freeze-only, and pre-implementation. Its job is to
bind the direct predecessor authority chain, the exact live `P1` packet, the
exact success bar for this family, and the exact writable slice for the next
bounded current-architecture attempt.

This round does not:

- implement a `P1` fix;
- rerun focused tests or the full Cabal gate as domain evidence;
- reopen the settled same-lane `C2` / `C5` / `C7` pocket;
- reopen the settled exact `C1` / `P2` packet;
- promote `P5` into a second live lane;
- decide a new repo-scope posture;
- authorize cyclic search, multi-SCC search, equi-recursive reasoning,
  fallback widening, a second interface, hardening, rollout, or a broad
  capability claim.

## Item-1 Authority Ledger

| Input class | Source | Binding read carried forward here |
| --- | --- | --- |
| Production baseline authority | `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md` | The repo still ships explicit-only recursive support, iso-recursive meaning only, no equi-recursive semantics, inherited non-cyclic structure, and no fallback widening. |
| Repo-scope capability contract | `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-capability-contract-and-evaluation-corpus.md` | `P1 local-recursive-shape` remains a required positive family for any eventual repo-level readiness claim. |
| Accepted repo-scope refreshed matrix | `docs/plans/2026-03-27-post-rev-004-repo-scope-refreshed-representative-family-matrix-settlement-surface-and-provenance-validation.md` | `P1-row` remains historical carry-forward only: bounded local same-lane support still exists, but the corresponding unannotated variant still does not infer recursive shape on the current pipeline surfaces. |
| Accepted immediate handoff authority | `docs/plans/2026-03-28-post-c1-p2-successor-gate-and-immediate-handoff-decision.md` | The only live continuation after the settled `C1` / `P2` family is a new bounded `P1 local automatic-success authoritative-surface` family. |
| Focused local contrast context | `docs/plans/2026-03-26-global-non-cyclic-graph-representative-family-matrix-end-to-end-settlement-campaign.md`; `test/PipelineSpec.hs` | The current carry-forward contrast is: bounded local recursive support still exists for the same-lane local `TypeRef` control packet, while the corresponding unannotated variant `ELam "x" (EVar "x")` still yields `containsMu False` on current authoritative surfaces. |

## Exact Live Subject

The live subject for this roadmap family is fixed to one exact bounded local
contrast only:

- family row: `P1-row`
- controlling family: `P1 local-recursive-shape`
- positive control context:
  same-lane local retained-child support through the local `TypeRef` lane
- exact live packet:
  the corresponding unannotated variant `ELam "x" (EVar "x")`
- current authoritative surface read:
  `containsMu False`

This item binds no second positive family, no reopened `C1` packet, and no
coequal `P5` lane.

## Exact Current Read Carried Forward

The exact current accepted read for this live subject is frozen as:

- bounded local same-lane recursive support still exists for the control
  packet anchored by the local `TypeRef` lane;
- the corresponding unannotated variant `ELam "x" (EVar "x")` still does not
  infer recursive shape on current pipeline surfaces; and
- current lawful classification:
  `below review-visible unannotated automatic success / blocker debt`

This means the family debt is narrower than general local recursion or general
same-lane settlement:
can one exact unannotated local packet surface recursive shape on the current
authoritative outputs inside the inherited architecture?

## Exact Success Bar For Item `2`

Item `2` may claim exact-packet success only if it satisfies all of the
following together:

1. The live subject remains the exact unannotated local packet
   `ELam "x" (EVar "x")` or an accepted equivalent restatement recorded in the
   round-owned plan without widening the family.
2. The behavior remains inside the inherited current architecture:
   no cyclic search,
   no multi-SCC search,
   no equi-recursive reasoning,
   no second interface,
   and no fallback widening.
3. Recursive structure for that exact unannotated packet becomes review-visible
   on the current authoritative surfaces rather than helper-only or
   diagnostic-only surfaces.
4. Fresh evidence is recorded through focused `P1` coverage and any needed
   production-path tests on the canonical round-owned surfaces.
5. Any touched code path passes the repo verification bar required by the live
   verification contract.

Even if item `2` clears this exact success bar, that would still mean:

- one exact-packet `P1` improvement only;
- not general `P1` family settlement by itself; and
- not repo-level readiness by itself.

## Writable Slice Freeze For Item `2`

The next bounded implementation round may write only inside this frozen slice:

| Path or class | Why it is included |
| --- | --- |
| `src/MLF/Elab/Run/ResultType/Fallback.hs` | The current local-versus-unannotated contrast still references the local `TypeRef` support lane. |
| `src/MLF/Elab/Run/Pipeline.hs` | Current authoritative production entrypoints live here. |
| `src/MLF/Elab/Pipeline.hs` | Re-export continuity may need synchronization if production entrypoint behavior changes. |
| `src-public/MLF/Pipeline.hs` | Public entrypoint continuity may need synchronization if production entrypoint behavior changes. |
| `test/PipelineSpec.hs` | The current local-shape contrast and authoritative-surface checks already live here. |
| `test/Research/P1LocalRecursiveShapeSpec.hs` | Optional focused harness for this exact family if item `2` introduces one. |
| `test/Main.hs` | Test wiring only if a new focused spec module is introduced. |
| `mlf2.cabal` | Cabal wiring only if new focused modules are introduced. |
| `docs/plans/2026-03-28-p1-local-recursive-shape-successor-authority-success-bar-and-writable-slice-freeze.md` and round-owned round-124 artifacts | Bound documentation and round evidence for this family. |

This writable slice is permissive, not mandatory. Item `2` may narrow below
this list, but it may not widen beyond it without a later accepted change.

## Next Lawful Move

The next lawful move after this freeze is roadmap item `2` only:

`Implement and validate one bounded current-architecture P1 authoritative-surface continuation slice`
