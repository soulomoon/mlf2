# Goal Mechanism Table (Current vs Target)

Last updated (UTC): 2026-05-02
Goal: Keep xMLF as the thesis-faithful typed elaboration IR while making `MLF.Backend.IR` the single executable eager backend IR, avoiding a duplicate public `LowerableBackend.IR` unless a later evidence-backed boundary requires it.
Source of truth: `papers/these-finale-english.txt`; `docs/architecture.md`; `src/MLF/Backend/IR.hs`; 2026-05-02 backend IR design discussion.

| Mechanism | Current codebase behavior | Target behavior | Gap summary | Evidence (spec/code/tests) | Gate (YES/NO) | Next action |
|---|---|---|---|---|---|---|
| IR role separation and non-duplication | xMLF remains the thesis-faithful typed elaboration IR; `MLF.Backend.IR` is now frozen as the single executable eager backend IR; and the backend docs/module notes explicitly reject a duplicate public `LowerableBackend.IR` without later accepted evidence. | xMLF remains the typed/theory elaboration representation; `MLF.Backend.IR` is the single executable eager backend IR; any ANF-like normalization stays internal unless a documented boundary earns its own invariants. | The one-backend-IR role-separation contract is now explicit across the durable backend guidance surfaces and guarded against public-surface drift. | `docs/architecture.md`; `src/MLF/Backend/IR.hs`; `src/MLF/Backend/Convert.hs`; `src/MLF/Backend/LLVM/Lower.hs`; `docs/backend-native-pipeline.md`; `test/RepoGuardSpec.hs` (`one-backend-IR contract stays explicit and no public lower IR leaks`). | YES | Advance to milestone-2 / row-2 eager-runtime lowering contract work while keeping the one-backend-IR contract fixed. |
| Eager runtime lowering contract | `MLF.Backend.IR` now owns the eager executable representation consumed by the rest of the backend, while LLVM/native lowering owns only downstream private closure ABI, layout/runtime support, and executable rendering concerns. | `Backend.IR` states the eager runtime/codegen contract explicitly without importing lazy STG machinery such as thunks, update frames, CAF update semantics, or graph reduction. | The eager-runtime ownership split is now explicit across the durable backend guidance surfaces, and the lazy STG exclusions are guarded against wording drift. | `docs/architecture.md`; `src/MLF/Backend/IR.hs`; `src/MLF/Backend/Convert.hs`; `src/MLF/Backend/LLVM/Lower.hs`; `docs/backend-native-pipeline.md`; `test/RepoGuardSpec.hs` (`eager-runtime lowering contract stays explicit and lazy STG machinery stays out of scope`). | YES | Advance to milestone-3 / row-3 callable-shape work while keeping the row-2 eager-runtime contract fixed. |
| Direct calls, closure values, and callable shapes | `BackendApp` is now the backend-owned direct first-order call node, while `BackendClosureCall` is the explicit indirect closure path for closure-valued aliases, captured closures, constructor-field projections, and case/let-selected closure values. Conversion and lowering consume the same callable-head classifier, and malformed confused-call heads fail with named backend validation errors instead of lowerer recovery. | Callable shape should be explicit enough that direct calls, local function calls, and closure calls are hard to confuse and diagnostics identify the violated backend invariant. | The callable-shape contract is now synchronized across the durable backend guidance, validator, conversion, lowering, and focused rejection coverage. | `docs/architecture.md`; `src/MLF/Backend/IR.hs`; `src/MLF/Backend/Convert.hs`; `src/MLF/Backend/LLVM/Lower.hs`; `test/BackendIRSpec.hs` (`validates explicit closure construction and indirect closure calls`, `rejects malformed closure IR`); `test/BackendConvertSpec.hs` (`keeps direct first-order local calls on the direct application path`, `classifies function-valued case pattern fields as closure locals`); `test/BackendLLVMSpec.hs` (`lowers case-selected closure callees through the explicit closure ABI`, `lowers let-selected closure callees through the explicit closure ABI`, `rejects BackendApp heads that select closure values through let or case`); `test/RepoGuardSpec.hs` (`callable-shape contract stays explicit and direct-vs-closure call heads stay unambiguous`). | YES | Advance to milestone-4 / row-4 ADT/case semantics versus layout work while keeping the callable-shape contract fixed. |
| ADT/case semantics versus layout | `BackendData`, `BackendConstructor`, `BackendConstruct`, and `BackendCase` preserve semantic ADT/case structure in `MLF.Backend.IR`; declaration-order tags, tag slot 0, field slots after the tag, closure-record storage for function-like constructor fields, and nullary tag-only representation are private `MLF.Backend.LLVM.Lower` policy. | Semantic constructor/case structure stays explicit, while representation decisions such as tag values, field layout, boxed/unboxed field treatment, and nullary strategy are made in a documented backend-owned place. | The semantic/layout split is now explicit across the durable backend surfaces, and the current lowerer-owned private layout policy is frozen by focused LLVM and repository-guard evidence without authorizing a second executable IR or public lowering boundary. | `docs/architecture.md`; `docs/backend-native-pipeline.md`; `src/MLF/Backend/IR.hs`; `src/MLF/Backend/Convert.hs`; `src/MLF/Backend/LLVM/Lower.hs`; `test/BackendLLVMSpec.hs` (`lowers Nat construction and case analysis to heap tags and switch`, `loads only constructor fields used by a case branch`, `lowers source closure-valued constructor fields through the explicit closure ABI`, `lowers nullary and recursive-list constructors through case`); `test/RepoGuardSpec.hs` (`ADT and case semantic boundary stays explicit while lowerer-owned layout policy stays private and frozen`). | YES | Advance to milestone-5 / row-5 primitive-operation and eager-evaluation-order work while keeping the row-4 semantic/layout ownership split fixed. |
| Primitive operations and eager evaluation order | Many operations can appear as ordinary applications or backend-supported patterns; expression evaluation order is mostly lowerer-owned. | Primitive operations and effect-sensitive evaluation order are explicit enough for eager LLVM lowering, either in `Backend.IR` itself or in a private normalizer inside the LLVM backend. | As supported programs grow, implicit evaluation order and primitive lowering rules may become fragile. | Current expression-shaped `BackendExpr`; LLVM lowering diagnostics for unsupported expressions/calls. | NO | Identify current primitive/evaluation-order assumptions and decide the narrowest explicit representation needed before adding more effects, FFI, or primitive ops. |
| Polymorphism erasure and lowerability | `BackendTyAbs` and `BackendTyApp` may remain in `Backend.IR`; lowerers reject unsupported polymorphic shapes or specialize selected paths. | The backend contract clearly says which polymorphic nodes are still allowed at `Backend.IR` and what must be erased, specialized, or rejected before LLVM emission. | Lowerability is partly a property of the LLVM path rather than an explicit named backend invariant. | `BackendTyAbs`, `BackendTyApp`; backend conversion and LLVM unsupported-node diagnostics. | NO | Define and test the supported polymorphism-erasure/specialization boundary for LLVM-lowerable backend programs. |
| Validation, evidence, and guidance synchronization | `validateBackendProgram` checks local invariants; docs describe current backend behavior; tests cover selected conversion/lowering cases. | Every target backend invariant above has focused tests or documentation evidence, and guidance stays synchronized across `AGENTS.md`, `docs/architecture.md`, and module notes when contracts change. | Some goals are currently design intent rather than verified mechanism. | `validateBackendProgram`; `docs/architecture.md`; backend tests referenced by cabal/test stanzas. | NO | Add or update focused tests and docs with each mechanism improvement; run the narrowest relevant test slice before the full gate for behavior changes. |

Rules:
- Keep mechanism order fixed.
- Use only `YES` or `NO`.
- Flip `NO -> YES` only with passing verification evidence.
