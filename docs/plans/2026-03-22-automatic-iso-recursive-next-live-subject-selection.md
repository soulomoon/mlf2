# `N2` Thesis-Backed Next Live-Subject Selection For Automatic Iso-Recursive Inference

Date: 2026-03-22
Round: `round-069`
Roadmap item: `N2`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: post-`L2` automatic iso-recursive successor planning lane
Artifact kind: canonical docs-only next-live-subject selection record

## Stage Contract Freeze

This artifact implements only roadmap item `N2` for `attempt-1` with
`retry: null`.

`N2` is docs-only next-live-subject selection work inside accepted
`N1 = reopen-planning-only`. It may choose exactly one thesis-backed subject
at planning scope only. It does not execute or authorize `N3`, `N4`, `N5`,
`N6`, or `N7`.

The inherited boundary remains fixed and unchanged:

- explicit-only recursive baseline;
- non-equi-recursive semantics;
- non-cyclic structural graph encoding;
- no second executable interface; and
- no compatibility, convenience, or default-path widening.

This artifact does not authorize implementation, verification, merge action,
roadmap edits, controller-state edits, bug-tracker edits, or
predecessor-history rewrites.

## Accepted `L1` / `L2` / `N1` Authority And Exact Admissible Selection Domain

Only accepted predecessor continuity is carried forward here:

1. `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md`
   remains the inherited baseline contract: explicit recursive annotations are
   supported, automatic recursive-type inference remains unresolved and
   disabled, and the explicit-only / non-equi-recursive /
   non-cyclic-graph / no-second-interface / no-fallback boundary remains
   mandatory.
2. `docs/plans/2026-03-21-uri-r2-c1-l1-next-target-bind.md` remains the
   authoritative `L1` fail-closed record. Inside the repaired `URI-R2-C1`
   queue and the inherited boundary, it proved that there is no fresh lawful
   exact successor slice.
3. `docs/plans/2026-03-21-uri-r2-c1-l2-post-l1-fail-closed-successor-decision-gate.md`
   remains the authoritative `L2` closeout. It finalized the repaired queue as
   `stop-blocked` and preserved the broader generic scheme-alias / base-like
   `baseTarget` route only as blocked future context requiring a separate
   roadmap amendment plus a fresh selection.
4. `docs/plans/2026-03-22-automatic-iso-recursive-post-l2-roadmap-amendment-authority-gate.md`
   remains the authoritative `N1` authority record. Its one bounded outcome,
   `reopen-planning-only`, keeps repaired `URI-R2-C1` closed as predecessor
   evidence only and makes the preserved generic scheme-alias / base-like
   `baseTarget` route admissible for later `N2` selection only.
5. `tasks/todo/2026-03-21-automatic-iso-recursive-next-loop/mechanism_table.md`
   still records `N2 = NO` because the next live subject has not yet been
   thesis-backed and fixed. `N2` therefore remains a subject-selection stage,
   not an implementation stage.
6. `/Users/ares/.codex/worktrees/d432/mlf4/Bugs.md` remains read-only
   predecessor context only. Open `BUG-2026-03-16-001` does not reopen replay,
   `MLF.Elab.Inst`, `InstBot`, or any other non-selected route.

The admissible post-`L2` `N2` domain is therefore narrow:

- the repaired `URI-R2-C1` queue stays closed predecessor evidence only;
- accepted `N1` reopened only a planning lane, not an implementation lane;
- the preserved generic scheme-alias / base-like `baseTarget` route is the
  only admissible candidate carried forward for `N2`; and
- every other replay path, `ResultType.Fallback` family, solver-wide route,
  cross-family route, and code-changing slice stays blocked unless a later
  accepted roadmap item reopens it explicitly.

## Thesis Basis For The Only Lawful Live Subject

The thesis-backed narrowing comes from three section anchors in
`papers/these-finale-english.txt`:

1. `8.3.1 Inlining bounds` says bounds should be inlined only when inverse
   translation can rebuild them unambiguously, including both the binder and
   the binding flag. Repo-local implication: any live successor touching the
   preserved scheme-alias / base-like route must stay centered on alias-bound
   ownership, bound inlining, and binding reconstruction before broader
   fallback behavior is even discussable.
2. `15.6.2 Expressivity of alias bounds` says alias bounds need special care in
   xMLF, that inert or alias bounds may need full inlining, and that the
   graphic presentation does not express alias bounds directly. Repo-local
   implication: the preserved generic scheme-alias / base-like `baseTarget`
   route is the only admissible `N2` candidate because it is already the
   surviving alias-bound-adjacent route, while other families would widen away
   from the thesis' alias-bound translation pressure.
3. The recursive-types outlook in `17.3 Perspectives` says recursive types
   combined with second-order polymorphism are already tricky, that cyclic
   term-graphs might be possible but were not explored, and that the likely
   main difficulty lies in recursion inside the binding structure. Repo-local
   implication: the next live subject must remain binder-centric and
   planning-only rather than turning into equi-recursive reasoning,
   implicit-unfolding behavior, or cyclic-graph experimentation.

These anchors do not authorize code directly. They only justify why the next
live subject must remain narrow, alias-bound-aware, and binding-structure-led.

## One Bounded `N2` Selection

Authoritative `N2` selection: the preserved generic scheme-alias / base-like
`baseTarget` route.

This round selects that route only as the next docs-first alias-bound /
bound-inlining / binding-structure planning subject.

Its exact meaning is:

- the next planning artifacts may study how preserved scheme-alias /
  base-like `baseTarget` cases should be described so that binder ownership and
  binding-flag reconstruction remain unambiguous within the inherited
  explicit-only boundary;
- the selection stays at planning scope only and does not bind an
  implementation slice, a verification slice, or any new solver-wide behavior;
- admissibility of the preserved generic scheme-alias / base-like
  `baseTarget` route is not implementation readiness, not verification
  clearance, and not authority to skip later stage gates;
- `N3` must still write the verifier-checkable safety and acceptance contract
  for this selected subject; and
- `N4` must still bind one exact bounded target before any design or code
  slice can begin.

## Explicit Deferred Alternatives And Non-Authorization

This artifact explicitly defers every other alternative as still blocked. In
particular, `N2` does not reopen or select:

- any other `ResultType.Fallback` family;
- any different solver or pipeline subject;
- replay reopen;
- `MLF.Elab.Inst` or `InstBot`;
- `boundVarTarget`;
- `boundTarget`;
- `schemeBodyTarget`;
- `src/MLF/Elab/Run/ResultType/View.hs`;
- equi-recursive reasoning;
- implicit unfolding;
- cyclic structural graph encoding;
- multi-SCC support;
- cross-family search;
- a second executable interface; or
- any compatibility, convenience, or default-path fallback.

This artifact also does not authorize:

- implementation or verification for the preserved generic scheme-alias /
  base-like `baseTarget` route;
- roadmap edits, controller-state edits, bug-tracker edits, or
  predecessor-history rewrites; or
- any interpretation of accepted `L1`, accepted `L2`, or accepted `N1` as
  implicit permission to skip `N3` or `N4`.

The repaired `URI-R2-C1` queue remains closed predecessor evidence only, and
every non-selected route remains deferred until a later accepted roadmap item
explicitly says otherwise.
