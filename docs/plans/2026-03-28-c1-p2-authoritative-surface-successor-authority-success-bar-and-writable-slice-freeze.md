# C1 / P2 Authoritative-Surface Successor Authority, Success Bar, And Writable-Slice Freeze

Date: 2026-03-28
Round: `round-120`
Roadmap item: `item-1`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: one bounded current-architecture successor freeze for the
remaining `C1` / `P2 non-local-propagation authoritative-surface blocker
family`
Artifact kind: canonical docs-only successor-boundary freeze

## Stage Contract Freeze

This artifact implements only roadmap item `1` for `attempt-1` with
`retry: null`.

This round is docs-only, freeze-only, and pre-implementation. Its job is to
bind the direct predecessor authority chain, the exact live `C1` packet, the
exact success bar for this family, and the exact writable slice for the next
bounded current-architecture attempt.

This round does not:

- implement a `C1` fix;
- rerun focused tests or the full Cabal gate as domain evidence;
- republish the March 27 matrix under a new name;
- reopen the settled same-lane `C2` / `C5` / `C7` pocket as live debt;
- promote `P5` into a second live lane;
- decide a new repo-scope posture;
- authorize cyclic search, multi-SCC search, equi-recursive reasoning,
  fallback widening, a second interface, hardening, rollout, or a broad
  capability claim.

The inherited production boundary therefore remains unchanged:

- explicit recursive annotations remain the production baseline;
- recursive meaning remains iso-recursive only;
- `non-equi-recursive = keep`;
- the inherited non-cyclic structural boundary remains binding;
- `no-fallback = keep`; and
- one-interface-only remains binding.

## Item-1 Authority Ledger

| Input class | Source | Binding read carried forward here |
| --- | --- | --- |
| Production baseline authority | `docs/plans/2026-03-14-automatic-recursive-inference-baseline-contract.md` | The repo still ships explicit-only recursive support, iso-recursive meaning only, no equi-recursive semantics, inherited non-cyclic structure, and no fallback widening. |
| Repo-scope capability contract | `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-capability-contract-and-evaluation-corpus.md` | `P2 non-local-propagation` remains a required positive family for any eventual repo-level readiness claim. |
| Full-pipeline visibility contract | `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-full-pipeline-reconstruction-and-validation-contract.md` | Exact-packet success for this family must still be judged on current authoritative output surfaces rather than helper-only or solver-only reads. |
| Accepted exact `C1` family classification | `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-representative-coverage-and-feasibility-campaign.md`; `docs/plans/2026-03-26-global-non-cyclic-graph-c1-c2-c5-production-surface-settlement-evidence-slice.md` | `C1` remains the exact admitted non-local alias-bound / base-like packet on `baseTarget -> baseC -> targetC`, and the accepted visible-output read remains non-recursive blocker debt. |
| Accepted refreshed matrix authority | `docs/plans/2026-03-27-post-rev-004-repo-scope-refreshed-representative-family-matrix-settlement-surface-and-provenance-validation.md` | The current accepted repo-scope `C1` read is: fallback `TBase (BaseTy "Int")` with `containsMu False`, and both authoritative public pipeline entrypoints still return the non-`TMu` `forall`-identity shape. |
| Accepted immediate handoff authority | `docs/plans/2026-03-27-post-rev-004-repo-scope-narrowed-successor-gate-and-immediate-handoff-decision.md` | The only live continuation opened after the refreshed matrix is the remaining bounded `C1` / `P2` authoritative-surface blocker family. |
| Focused harness context | `test/Research/C1AuthoritativeSurfaceSpec.hs` | The focused harness remains the exact bounded research/control read for this family, but it is supporting context only until a later accepted round republishes fresh rerun evidence. |
| Settled same-lane predecessor truth only | the accepted rev-004 same-lane settlement chain | `C2` / `C5` / `C7` remain closed predecessor truth only and may not be reopened by this family. |

This artifact is a new round-owned freeze only. It does not rewrite prior
accepted docs in place.

## Exact Live Subject

The live subject for this roadmap family is fixed to one exact admitted packet
only:

- family row: `C1`
- controlling family: `P2 non-local-propagation`
- packet description: non-local alias-bound / base-like admitted packet
- route: `baseTarget -> baseC -> targetC`
- strategic role: the one remaining live current-architecture blocker family
  after the accepted March 27 narrowed successor gate

This item binds no second packet, no second route, no second family, and no
coequal `P5` lane.

## Exact Current Read Carried Forward

The exact current accepted read for this live subject is frozen as:

- fallback surface:
  `TBase (BaseTy "Int")` with `containsMu False`
- current authoritative public pipeline entrypoints:
  both `runPipelineElab` and `runPipelineElabChecked` return
  `TForall "a" Nothing (TArrow (TVar "a") (TVar "a"))`
  with `containsMu False`
- current lawful classification:
  admitted exact packet only,
  `admitted but not reconstruction-visible / blocker debt`

This means the current family debt is not:

- whether a recursive helper-visible or fallback-internal read exists at all;
- whether the same-lane `C2` / `C5` / `C7` pocket survives visibly; or
- whether repo-scope readiness may already be claimed.

The current family debt is narrower:
can this one exact admitted non-local packet carry recursive structure through
the current authoritative surfaces inside the inherited architecture?

## Exact Success Bar For Item `2`

Item `2` may claim exact-packet success only if it satisfies all of the
following together:

1. The live subject remains the exact admitted `C1` packet and route
   `baseTarget -> baseC -> targetC`.
2. The behavior remains inside the inherited current architecture:
   no cyclic search,
   no multi-SCC search,
   no equi-recursive reasoning,
   no second interface,
   and no fallback widening.
3. Recursive structure for that exact packet becomes review-visible on the
   current authoritative surfaces rather than helper-only or diagnostic-only
   surfaces.
4. Fresh evidence is recorded through the focused `C1` harness and any needed
   production-path tests on the canonical round-owned surfaces.
5. Any touched code path passes the repo verification bar required by the live
   verification contract.

Supporting fallback or internal evidence may be cited, but it does not by
itself clear this family. The decisive burden remains the current
authoritative-surface read.

Even if item `2` clears this exact success bar, that would still mean:

- one exact-packet `C1` improvement only;
- not automatic general `P2` family settlement by itself; and
- not repo-level readiness for automatic iso-recursive inference by itself.

## Writable Slice Freeze For Item `2`

The next bounded implementation round may write only inside this frozen slice:

| Path or class | Why it is included |
| --- | --- |
| `src/MLF/Elab/Run/ResultType/Fallback.hs` | The accepted predecessor packet and route are currently anchored here. |
| `src/MLF/Elab/Run/Pipeline.hs` | Current authoritative production entrypoints live here. |
| `src/MLF/Elab/Pipeline.hs` | Re-export surface may need synchronized continuity if item `2` changes production entrypoint behavior. |
| `src-public/MLF/Pipeline.hs` | Public entrypoint surface may need synchronized continuity if item `2` changes production entrypoint behavior. |
| `test/Research/C1AuthoritativeSurfaceSpec.hs` | Focused exact-packet harness for this family. |
| `test/PipelineSpec.hs` | Production-path regression coverage for this family. |
| `test/Main.hs` | Test wiring only if a new focused spec module requires it. |
| `mlf2.cabal` | Cabal wiring only if new focused test modules or modules require it. |
| `docs/plans/2026-03-28-c1-p2-authoritative-surface-successor-authority-success-bar-and-writable-slice-freeze.md` and round-owned round-121 artifacts | Bound documentation and round evidence for this family. |

This writable slice is permissive, not mandatory. Item `2` may narrow below
this list, but it may not widen beyond it without a later accepted change.

The following remain explicitly blocked for item `2`:

- `src/MLF/Constraint/**`
- `src/MLF/Constraint/Presolution/**`
- any cyclic or multi-SCC machinery
- any second executable or alternate interface
- any convenience or default-path fallback widening
- any same-lane `C2` / `C5` / `C7` reopening
- any `P5` lane opening
- any repo-level capability claim

## Next Lawful Move

The next lawful move after this freeze is roadmap item `2` only:

`Implement and validate one bounded current-architecture C1 authoritative-surface continuation slice`

That next round must preserve:

- this exact predecessor authority chain;
- this exact live subject;
- this exact current read;
- this exact success bar; and
- this exact writable slice.

## Docs-Only Verification Note

This round changes only documentation and round-owned orchestrator artifacts:

- this canonical freeze artifact; and
- `orchestrator/rounds/round-120/implementation-notes.md`.

Because the round does not touch `src/`, `src-public/`, `app/`, `test/`, or
`mlf2.cabal`, the full `cabal build all && cabal test` gate is not required
here. Reviewer verification should therefore focus on docs-diff correctness,
authority continuity, exact-packet scope discipline, and whether item `2`
becomes the next concrete lawful move without silent widening.
