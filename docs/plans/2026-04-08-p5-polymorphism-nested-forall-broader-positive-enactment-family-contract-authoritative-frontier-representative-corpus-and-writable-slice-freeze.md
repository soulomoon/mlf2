# P5 Polymorphism-Nested-Forall Broader Positive Enactment Family Contract, Authoritative Frontier, Representative Corpus, And Writable-Slice Freeze

Date: 2026-04-08
Round: `round-206`
Milestone: `milestone-1`
Direction: `direction-1a-freeze-broader-positive-enactment-contract`
Extracted item: `freeze-broader-positive-enactment-contract`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: one docs-only milestone-1 enactment-contract freeze before any
code-bearing broader-positive round
Artifact kind: canonical milestone-1 enactment-contract freeze

## Stage Contract Freeze

This artifact implements only `round-206` / `milestone-1` /
`direction-1a-freeze-broader-positive-enactment-contract` /
`freeze-broader-positive-enactment-contract` for `attempt-1` with
`retry: null`.

This round remains docs/control-plane-only, serial, and milestone-1-only. Its
job is to consume accepted `round-205` as the binding family-entry authority,
consume accepted `round-204` and accepted `round-203` as the controlling
ledger beneath that handoff, and freeze one exact enactment contract for later
code-bearing work: the broader-positive frontier, the expected behavior shift,
the authoritative success surfaces, the representative corpus, the writable
slice, and the preserved closed guardrails.

This artifact does not enact broader-positive support, does not rewrite any
accepted predecessor artifact, does not authorize code, tests, Cabal, roadmap,
or controller-state edits in this round, and does not upgrade one settled
retained-child lane into whole-family closure.

## Family-Entry Authority Ledger

| Source | Binding read carried into this enactment-contract freeze |
| --- | --- |
| `orchestrator/rounds/round-206/selection.md`; `orchestrator/state.json`; `orchestrator/roadmaps/2026-04-08-00-p5-polymorphism-nested-forall-broader-positive-explicit-boundary-enactment-roadmap/rev-001/{roadmap,verification,retry-subloop}.md` | The active control plane fixes `round-206` as the first and only lawful `milestone-1` move in this family, keeps the round docs/control-plane-only, and requires the enactment contract to be frozen before any code-bearing round may start. |
| `docs/plans/2026-04-08-p5-polymorphism-nested-forall-explicit-boundary-revision-family-final-handoff-binding-one-exact-downstream-consequence-from-the-revised-planning-ledger.md`; `orchestrator/rounds/round-205/review-record.json` | Accepted `round-205` is the binding family-entry authority. It opened exactly one later enactment / implementation family for still-live broader-positive `P5 polymorphism-nested-forall` pressure beyond the one settled retained-child clear-boundary lane, and it explicitly deferred the exact code-bearing contract to this round. |
| `docs/plans/2026-04-07-p5-polymorphism-nested-forall-explicit-boundary-revision-family-broader-positive-p5-ledger-under-the-revised-freeze.md`; `orchestrator/rounds/round-204/review-record.json` | Accepted `round-204` fixes the exact broader-positive frontier beneath this family: support beyond the one settled retained-child clear-boundary lane, with `sameLaneAliasFrameClearBoundaryExpr` preserved as predecessor evidence only and `nestedForallContrastExpr` preserved as fail-closed quantified-crossing evidence rather than closure. |
| `docs/plans/2026-04-07-p5-polymorphism-nested-forall-explicit-boundary-revision-family-round-151-polymorphic-mediation-mu-preservation-reclassification-and-inherited-boundary-refreeze.md`; `orchestrator/rounds/round-203/review-record.json` | Accepted `round-203` keeps the round-151 polymorphic-mediation `mu`-absorption story as narrowed live semantic pressure rather than settled broader-positive truth. That reclassification remains controlling beneath this family. |
| `docs/plans/2026-04-07-post-item-7-explicit-boundary-revision-candidate-final-handoff-to-one-planning-only-p5-polymorphism-nested-forall-boundary-revision-family.md`; `orchestrator/rounds/round-201/review-record.json`; `docs/plans/2026-04-07-post-item-7-refreshed-repo-level-readiness-and-architecture-decision-from-the-updated-p5-vs-p2-ledger-and-preserved-negative-family-settlements.md`; `orchestrator/rounds/round-200/review-record.json` | Accepted `round-201` and accepted `round-200` preserve `explicit boundary-revision candidate` as family-entry pressure only. They do not already freeze a code-bearing slice, a broader-positive implementation diff, or a family-closing answer. |
| `docs/plans/2026-04-06-post-item-7-p5-post-implementation-settlement-surface-and-exact-repo-impact-read.md`; `orchestrator/rounds/round-197/review-record.json`; `test/Research/P5ClearBoundarySpec.hs` | Accepted `round-197` preserves one settled retained-child clear-boundary predecessor lane only: `sameLaneAliasFrameClearBoundaryExpr` stays recursively visible on `runPipelineElab` and `runPipelineElabChecked`, while `nestedForallContrastExpr` stays fail-closed with `PhiTranslatabilityError`. That pair remains preserved authority for the current surface, not broader-positive family closure. |
| `docs/plans/2026-04-05-general-automatic-iso-recursive-full-inference-positive-family-aggregate-classification.md`; `orchestrator/rounds/round-191/review-record.json`; `orchestrator/rounds/round-181/review-record.json` | Accepted `round-191` and accepted `round-181` keep `P2` packet-bounded to the exact `C1` packet only. `P2` is preserved authority beneath this family, not a substitute live frontier. |
| `docs/plans/2026-04-05-general-automatic-iso-recursive-full-inference-negative-family-and-termination-pressure-aggregate-classification.md`; `orchestrator/rounds/round-192/review-record.json` | Accepted `round-192` keeps `N1 ambiguity-reject`, `N2 unsoundness-guard`, and `N6 termination-pressure` closed as fail-closed rejection on the authoritative current surfaces. Those rows remain preserved guardrails beneath this family. |
| `docs/plans/2026-03-25-general-automatic-iso-recursive-inference-capability-contract-and-evaluation-corpus.md`; `docs/plans/2026-03-28-p5-polymorphism-nested-forall-successor-authority-success-bar-and-writable-slice-freeze.md`; `docs/plans/2026-03-28-post-implementation-p5-polymorphism-nested-forall-settlement-surface-and-exact-repo-impact-read.md`; `docs/plans/2026-03-29-same-lane-alias-frame-representative-gap-post-item-2-settlement-surface-and-repo-impact-read.md` | The March 25 / 28 / 29 lineage remains earlier corpus and freeze provenance only. It preserves predecessor vocabulary and older packet history, but it does not control the new family-entry freeze or reopen those earlier packets as live subjects. |
| `src/MLF/Elab/Run/ResultType/Fallback/Core.hs`; `src/MLF/Elab/Run/ResultType/Fallback.hs`; `src/MLF/Elab/TermClosure.hs`; `src/MLF/Elab/Run/Pipeline.hs`; `src/MLF/Elab/Pipeline.hs`; `src-public/MLF/Pipeline.hs`; `test/Research/P5ClearBoundarySpec.hs`; `test/PipelineSpec.hs` | The current repo anchors still expose the retained-child guard cluster and the authoritative pipeline surfaces only. They show where `boundHasForallFrom`, `sameLaneLocalRetainedChildTarget`, `keepTargetFinal`, `targetC`, and `preserveRetainedChildAuthoritativeResult` currently live, but they do not yet enact a broader-positive law beyond the one settled retained-child lane. |

## Exact Broader-Positive Enactment Frontier

The exact broader-positive enactment frontier frozen here is:

`broader positive nested-forall / quantified-crossing support beyond the one settled retained-child clear-boundary lane, where later rounds must preserve recursive structure review-visibly on both authoritative pipeline entrypoints without leaving the accepted explicit-only / iso-recursive / non-equi-recursive / non-cyclic-graph / no-fallback / no-second-interface boundary`

This frontier is narrower than the whole repository and broader than one
already-settled retained-child packet.

- It begins after the one settled retained-child clear-boundary lane preserved
  by `sameLaneAliasFrameClearBoundaryExpr`.
- It includes the still-live quantified-crossing pressure currently witnessed
  by `nestedForallContrastExpr` and the same-wrapper nested-`forall`
  baseline, but it does not collapse the frontier to those two rows alone.
- It stays inside the inherited explicit-only / iso-recursive boundary and
  therefore excludes cyclic search, multi-SCC widening, equi-recursive
  reasoning, fallback rescue, and any second interface.
- It does not reopen `P2`, the representative negative-family rows, or the
  March 28 exact packet as substitute live family closure.

## Expected Behavior Shift

Later code-bearing rounds in this family must stop treating round-151-style
polymorphic-mediation `mu` absorption as the controlling broader-positive read
for this frontier.

`implementation_notes.md` and accepted `round-151` remain preserved
historical evidence for the current production baseline:
`Known correct behavior under polymorphic mediation` still records
`Nested-forall-mediated recursive types` as absorption-driven today.
This milestone does not rewrite those historical artifacts. Instead, it freezes
the exact expectation for later enactment rounds:
broader-positive quantified-crossing support may not be judged complete merely
because the current baseline absorbs `mu` through polymorphic mediation.

This is an expected behavior shift only. The present round makes no claim that
the current code already earns broader-positive recursive-structure
preservation on the frozen frontier.

## Authoritative Success Surfaces

Later broader-positive claims in this family are lawful only when evidence is
review-visible on both authoritative pipeline entrypoints:
`runPipelineElab` and `runPipelineElabChecked`.

| Surface or seam | Frozen role in this family |
| --- | --- |
| `runPipelineElab` | Mandatory authoritative evidence surface. Later success claims must be visible here. |
| `runPipelineElabChecked` | Mandatory authoritative evidence surface. Later success claims must also be visible here; one-entrypoint success is insufficient. |
| `src/MLF/Elab/Run/Pipeline.hs` | Authoritative internal surface owner for `runPipelineElab` and `runPipelineElabChecked`. |
| `src/MLF/Elab/Pipeline.hs` | Authoritative internal continuity seam that must stay aligned with the internal entrypoints rather than creating a second read. |
| `src-public/MLF/Pipeline.hs` | Authoritative public continuity seam that must expose the same behavior without a second interface. |
| `src/MLF/Elab/Run/ResultType/Fallback/Core.hs`; `src/MLF/Elab/Run/ResultType/Fallback.hs` | Supporting but non-authoritative mechanism seams. They currently house `boundHasForallFrom`, `sameLaneLocalRetainedChildTarget`, `keepTargetFinal`, and `targetC`, but helper-side movement here alone cannot earn the broader-positive claim. |
| `src/MLF/Elab/TermClosure.hs` | Supporting but non-authoritative mechanism seam. `preserveRetainedChildAuthoritativeResult` may support continuity, but continuity here alone is not an authoritative success surface. |

The frozen success bar is therefore surface-first: later code-bearing review
must show the broader-positive result honestly on both authoritative
entrypoints, with the three pipeline files as the continuity seams, and may
not substitute helper-only, fallback-only, or one-surface-only evidence.

## Representative Corpus Obligations

The representative corpus is frozen into three classes only:
preserved controls, live enactment targets, and preserved closed negatives.

| Corpus class | Exact rows frozen here | Binding read carried forward |
| --- | --- | --- |
| `preserved controls` | `sameLaneClearBoundaryExpr`; `sameLaneAliasFrameClearBoundaryExpr` in `test/Research/P5ClearBoundarySpec.hs`; the authoritative-entrypoint continuity cluster in `test/PipelineSpec.hs` from `sameLaneAliasFrameClearBoundaryExpr` through `sameLaneNonupleAliasFrameClearBoundaryExpr` | These rows preserve the one settled retained-child clear-boundary control family only. They remain required control evidence for later rounds, but they do not close the broader-positive frontier by themselves. |
| `live enactment targets` | `nestedForallContrastExpr` in `test/Research/P5ClearBoundarySpec.hs`; `keeps retained-child fallback open for recursive types even when the same wrapper crosses a nested forall boundary` in `test/PipelineSpec.hs` | These rows keep the quantified-crossing broader-positive pressure explicit. `nestedForallContrastExpr` preserves the current authoritative fail-closed contrast, and the same-wrapper nested-`forall` fallback baseline preserves the current non-recursive broader reading that later rounds must stop treating as the controlling family answer. |
| `preserved closed negatives` | `keeps the same single-base wrapper fail-closed once it leaves the local TypeRef lane`; `keeps the same multi-inst wrapper fail-closed once it leaves the local TypeRef lane`; `keeps the same inst-arg multi-base wrapper fail-closed once it leaves the local TypeRef lane`; `keeps the same inst-arg-only singleton-base wrapper fail-closed once it leaves the local TypeRef lane`; `does not infer recursive shape for the corresponding unannotated variant` in `test/PipelineSpec.hs`; docs-only preserved `P2`, `N1 ambiguity-reject`, `N2 unsoundness-guard`, and `N6 termination-pressure` from accepted `round-191`, `round-181`, and `round-192` review records | These rows stay fail-closed or packet-bounded. Later broader-positive rounds may not relabel them as positive support, may not use them to widen search or interfaces, and may not reopen them as substitute frontier closure. |

This corpus ledger is exact on purpose:
preserved controls must remain green,
live enactment targets mark the still-unenacted broader-positive frontier, and
preserved closed negatives remain closed.

## Exact Writable Slice

Later code-bearing milestone-2 and milestone-3 work is frozen to this exact
writable slice only:

- `src/MLF/Elab/Run/ResultType/Fallback.hs`
- `src/MLF/Elab/Run/ResultType/Fallback/Core.hs`
- `src/MLF/Elab/TermClosure.hs`
- `src/MLF/Elab/Run/Pipeline.hs`
- `src/MLF/Elab/Pipeline.hs`
- `src-public/MLF/Pipeline.hs`
- `test/Research/P5ClearBoundarySpec.hs`
- `test/PipelineSpec.hs`

Everything outside that slice stays excluded unless a later accepted
milestone or roadmap revision changes the contract explicitly. Excluded paths
and files include at least:
`test/Main.hs`,
`mlf2.cabal`,
`TODO.md`,
`implementation_notes.md`,
`CHANGELOG.md`,
thesis-facing files,
roadmap files,
controller-state files,
and any second docs/plans artifact.

This round itself remains docs-only and does not touch the writable slice.
The slice is frozen here only so later code-bearing review has one exact
boundary to enforce.

## Preserved Closed Guardrails And Exclusions

- The one settled retained-child clear-boundary lane stays predecessor truth
  only. It may not be silently upgraded into broader-positive family closure.
- `P2` stays packet-bounded to the exact `C1` packet only.
- `N1 ambiguity-reject`, `N2 unsoundness-guard`, and
  `N6 termination-pressure` stay closed as preserved fail-closed guardrails.
- Cyclic search, multi-SCC widening, equi-recursive reasoning, fallback
  rescue, and a second interface remain forbidden.
- The inherited explicit-only / iso-recursive / non-equi-recursive /
  non-cyclic-graph / no-fallback boundary remains controlling until later
  accepted evidence says otherwise explicitly.

## Milestone-2 And Milestone-3 Consequences

- `milestone-2` may change production semantics only inside the frozen
  writable slice, and only to enact the broader-positive recursive-structure
  preservation required by this contract without reopening preserved closed
  families.
- `milestone-3` must prove the frozen broader-positive corpus honestly on both
  `runPipelineElab` and `runPipelineElabChecked`. Helper-only success,
  fallback-only success, or one-entrypoint success cannot satisfy the frozen
  success bar.
- `milestone-4` owns repo-facing note updates such as `TODO.md`,
  `implementation_notes.md`, and `CHANGELOG.md` after milestone-3 evidence is
  accepted.

## Non-Claims

- No claim is made that the current code already supports the broader-positive
  frontier.
- No claim is made that one settled retained-child lane closes the whole
  broader-positive family.
- No production, test, Cabal, roadmap, or controller-state change is
  authorized by this round.
- No rewrite of accepted predecessor artifacts, accepted review records, or
  historical `implementation_notes.md` wording is made here.
- No reopening of `P2`, `N1 ambiguity-reject`, `N2 unsoundness-guard`, or
  `N6 termination-pressure` is authorized.
- No cyclic, multi-SCC, equi-recursive, fallback, or second-interface
  widening is authorized.
