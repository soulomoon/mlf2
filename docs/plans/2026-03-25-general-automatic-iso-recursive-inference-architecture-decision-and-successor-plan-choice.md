# General Automatic Iso-Recursive Inference Architecture Decision And Successor Plan Choice

Date: 2026-03-25
Round: `round-088`
Roadmap item: `item-7`
Stage: `implement`
Attempt: `attempt-1`
Retry state: `null` (`retry: null`)
Live subject: one docs-only aggregate architecture decision and successor-plan
choice from the accepted `bounded subset only` evidence chain
Artifact kind: canonical docs-only architecture decision / successor-plan
choice

## Stage Contract Freeze

This artifact implements only roadmap item `7` for `attempt-1` with
`retry: null`.

Item `7` is docs-only, aggregate-only, and decision-only. Its job is to
consume the accepted item-2 architectural audit and the accepted item-6
representative coverage result, evaluate exactly three lawful strategic
outcomes, and record exactly one authoritative item-7 outcome plus exactly one
bounded successor choice.

This artifact does not authorize:

- edits under `src/`, `src-public/`, `app/`, `test/`, or `mlf2.cabal`;
- edits to `orchestrator/state.json`, `orchestrator/roadmap.md`,
  `orchestrator/verification.md`, or `orchestrator/retry-subloop.md`;
- edits to `Bugs.md`;
- a reopened capability definition, mechanism map, search model,
  reconstruction contract, or coverage campaign;
- code-path experiments, implementation slices, or test expansion;
- equi-recursive equality or implicit unfolding;
- cyclic structural graph encoding or multi-SCC search;
- a second executable interface; or
- compatibility, convenience, or default-path fallback widening.

The inherited boundary remains fixed and unchanged:

- explicit recursive annotations remain the current production baseline;
- recursive meaning remains iso-recursive only;
- no equi-recursive equality or implicit unfolding is authorized;
- no cyclic structural graph encoding or multi-SCC search is authorized;
- no second interface is authorized; and
- no compatibility, convenience, or default-path fallback widening is
  authorized.

Accepted items `1` through `6` plus accepted `N14` contribute bounded evidence
only. They do not by themselves prove broad general automatic iso-recursive
inference, force an architecture revision, or settle product posture.

## Accepted Decision-Input Ledger

The accepted decision ledger for item `7` is:

| Decision input | Accepted read |
| --- | --- |
| Inherited boundary | Explicit-only recursive behavior remains the production baseline; recursive meaning remains iso-recursive only; no equi-recursive equality, implicit unfolding, cyclic structural graph encoding, multi-SCC search, second interface, or fallback widening is authorized. |
| Item-2 classification set | `iso-recursive = keep`, `non-equi-recursive = keep`, `non-cyclic-graph = unknown`, `no-fallback = keep`. |
| Item-6 matrix tally | `stable visible persistence`: none. `admitted but not reconstruction-visible / blocker debt`: `C1`, `C2`, `C5`, `C7`. `fail-closed rejection`: `C3`, `C4`, `C6`. |
| Admitted-family posture | Both admitted families remain below accepted reconstruction-visible success. The non-local alias-bound / base-like pocket stays admitted but visible output remains `TBase (BaseTy "Int")` with `containsMu False`. The same-lane retained-child pocket is the strongest bounded positive-output candidate, but it still lacks accepted full solver -> elaboration -> reconstruction -> internal/public persistence. |
| Reject-side posture | Nested-`forall` pressure remains reject-side only; positive `P5` success is not accepted. Ambiguity and termination are bounded only as fail-closed discipline rather than as broad positive success. |
| Architecture-pressure posture | The item-2 audit still marks `non-cyclic-graph` as the only inherited boundary classified `unknown`, so it remains the live architecture-pressure candidate. |
| Bounded feasibility posture | The accepted record still contains two bounded admitted pockets inside the inherited acyclic model, so the current architecture is not yet an accepted dead end even though it does not justify broad generality. |
| Predecessor continuity posture | Accepted `N14` and earlier packets remain bounded predecessor evidence only; they preserve real bounded wins but do not authorize a stronger general-capability or architecture-revision claim. |

This ledger is evidence-only. It records the strongest accepted input posture
without choosing an outcome yet.

## Outcome Evaluation Schema

| Outcome token | Supporting evidence | Blocking evidence | Lawful-status read |
| --- | --- | --- | --- |
| `continue within the current architecture` | The accepted record still contains two bounded admitted pockets inside the inherited acyclic model. The strongest same-lane retained-child pocket already keeps a recursive output fact (`containsMu True`) plus route visibility. Ambiguity and termination remain bounded by fail-closed rules rather than by uncontrolled growth. | Zero rows qualify as `stable visible persistence`. Both admitted families remain blocker debt rather than accepted reconstruction-visible success. Nested-`forall` remains reject-side only, so this branch cannot be read as broad general capability. | `selected`: the accepted record still leaves one bounded evidence gate worth running inside the unchanged architecture, but only as a bounded continuation rather than as a general-capability claim. |
| `pursue targeted boundary revision` | `non-cyclic-graph` remains the only inherited boundary still classified `unknown`, and the strategic roadmap already names it as the highest-risk architecture pressure point for generality. | Item `2` explicitly stopped at `unknown`, not `revise`. Item `6` explicitly says the current record is not an architectural dead end because two bounded admitted pockets still exist inside the acyclic model. No accepted artifact yet shows that boundary revision is already the strongest lawful next move. | `not selected`: the accepted record identifies the likely revision candidate, but it does not yet justify choosing revision before one more bounded persistence gate inside the unchanged architecture. |
| `stop` | Zero `stable visible persistence` rows, blocker debt in both admitted families, and reject-side-only nested-`forall` pressure all block any claim of broad general automatic iso-recursive inference. | The accepted record still supports two bounded admitted pockets plus fail-closed ambiguity / termination discipline, and item `6` explicitly says that this is not yet an architectural dead end. That keeps one bounded next move lawfully open. | `rejected`: stopping now would erase accepted bounded feasibility evidence that still survives inside the inherited architecture. |

## One Authoritative Item-7 Outcome

Authoritative outcome token: `continue within the current architecture`

This is the strongest lawful item-7 read on the accepted evidence chain.

Why this outcome is selected:

1. The accepted ledger does not justify any claim of broad general automatic
   iso-recursive inference. Zero `stable visible persistence` rows, blocker
   debt in both admitted families, and reject-side-only nested-`forall`
   pressure keep the overall claim bounded.
2. The accepted ledger also does not justify treating the current
   architecture as already forced into revision. Item `2` still classifies
   `non-cyclic-graph` as `unknown`, not `revise`, and item `6` explicitly
   preserves two bounded admitted pockets inside the inherited acyclic model.
3. The accepted ledger does not justify `stop`. The current model still has
   one strongest admitted pocket and one admitted non-local pocket, and the
   accepted search posture still bounds ambiguity and termination fail closed.

Why the non-selected outcomes are weaker on current evidence:

- `pursue targeted boundary revision` is weaker because the accepted record
  identifies `non-cyclic-graph` as a live risk but does not yet show that the
  boundary itself, rather than missing persistence evidence, is now the
  strongest blocker.
- `stop` is weaker because it would treat bounded feasibility evidence as if
  it had already collapsed, which the accepted item-6 result expressly does
  not say.

## One Bounded Successor Choice

Selected successor choice: one bounded same-lane retained-child
stable-visible-persistence gate inside the inherited acyclic model.

Why this is the next lawful gate:

- the same-lane retained-child pocket is the strongest accepted positive
  candidate because it already preserves a recursive output fact
  (`containsMu True`) and the route
  `sameLaneLocalRetainedChildTarget -> keepTargetFinal -> targetC`;
- item `6` says the central unresolved pressure is not whether a bounded
  pocket exists, but whether any bounded pocket can cross from blocker debt
  into accepted `stable visible persistence`; and
- testing that one pocket is the narrowest successor that can sharpen the
  architecture decision without reopening the whole coverage campaign,
  silently revising `non-cyclic-graph`, or inventing new mechanism/search
  scope.

This successor choice leaves exactly one lawful next move on the table:
determine whether the strongest admitted same-lane retained-child pocket can
achieve accepted stable visible persistence while the inherited explicit-only /
iso-recursive / non-equi-recursive / non-cyclic-graph / no-fallback boundary
remains unchanged.

## Reviewer-Facing Decision Note And Docs-Only Gate

Bounded item-7 result: the refreshed control plane now has one explicit
architecture decision and one successor-plan choice grounded only in accepted
item-2 and item-6 evidence.

This artifact is written to satisfy the reviewer-facing item-7 expectations in
`orchestrator/verification.md`:

- it records exactly one explicit outcome and does not blur together
  `continue within the current architecture`,
  `pursue targeted boundary revision`, and `stop`;
- it preserves the inherited boundary, predecessor continuity, and the
  no-silent-widening rule; and
- where the accepted record does not justify a stronger claim, it fails
  closed by keeping the decision bounded.

This round is expected to change only documentation:

- this canonical item-7 architecture decision / successor-plan artifact; and
- `orchestrator/rounds/round-088/implementation-notes.md`.

Because the round does not touch `src/`, `src-public/`, `app/`, `test/`, or
`mlf2.cabal`, no full `cabal build all && cabal test` gate is triggered
unless the diff escapes that authorized docs-only surface.

Item-7 retry discipline remains explicit: review may reject and send the same
round back to `plan`, but the accepted outcome for item `7` cannot be
`accepted + retry`.
