# Goal Mechanism Table (Current vs Target)

Last updated (UTC): 2026-03-07
Goal: `Thesis-exact Haskell refactoring and recursion-schemes simplification without violating gMLF/xMLF graph semantics`
Source of truth: `papers/these-finale-english.txt` (primary); `papers/xmlf.txt` only when the thesis is silent

Use this table to track what the codebase does now vs what it must do to satisfy the thesis-exact recursion-refactor campaign.

| Mechanism | Current codebase behavior | Target behavior | Gap summary | Evidence (spec/code/tests) | Gate (YES/NO) | Next action |
|---|---|---|---|---|---|---|
| Surface Preprocessing Exactness | Staged frontend types, normalization, desugaring, and annotation internalization already match the thesis preprocessing boundary: alias bounds are inlined capture-avoidantly, typed lets stay coercion-only sugar, and annotation types become explicit coercion/graphic constraints before elaboration. | Frontend refactors keep alias-bound normalization and annotation/coercion lowering thesis-faithful before downstream elaboration; simplification may remove duplicate tree walks, but it must not change preprocessing semantics. | Fresh verifier sweep found no remaining semantic gap: `MLF.Frontend.Normalize` and `MLF.Frontend.Desugar` are the only preprocessing tree candidates, while `MLF.Frontend.ConstraintGen.Translate` remains the thesis-owned semantic boundary. | Thesis `§12.3.2.2`, `§15.3.8`; `docs/thesis-obligations.yaml` alias-bound pipeline anchors; `src/MLF/Frontend/Syntax.hs`; `src/MLF/Frontend/Normalize.hs`; `src/MLF/Frontend/Desugar.hs`; `src/MLF/Frontend/ConstraintGen/Translate.hs`; `test/FrontendParseSpec.hs`; `test/FrontendNormalizeSpec.hs`; `test/ConstraintGenSpec.hs`; `test/PipelineSpec.hs`; `implementation_notes.md` | YES | Keep the parse/normalize/constraint/pipeline anchors green and only refactor frontend preprocessing inside the audited tree-only boundary. |
| Leftmost-Lowermost Quantifier Ordering | The elaboration path already encodes Σ/ϕ reorder behavior in `sigmaReorder` plus context helpers, and the O15 reorder anchors directly exercise the required and identity cases. | Any cleanup preserves the thesis leftmost-lowermost order from `§15.2.4` and applies `ϕR` exactly when `Typ(a′)` and `Typexp(a′)` differ; `ε` is returned only when binder order already matches. | Fresh verifier sweep found the ordering contract already thesis exact: the code/test anchors freeze the reorder semantics as a non-negotiable elaboration boundary rather than generic simplification fodder. | Thesis `§15.2.4`, `§15.3.4`; `docs/thesis-obligations.yaml` `O15-REORDER-REQUIRED`, `O15-REORDER-IDENTITY`; `src/MLF/Elab/Pipeline.hs`; `src/MLF/Elab/Sigma.hs`; `src/MLF/Elab/Phi/Context.hs`; `test/ElaborationSpec.hs`; `implementation_notes.md` | YES | Preserve the `sigmaReorder` contract and rerun the O15 reorder guards before accepting helper-level cleanup. |
| Let-Scope Translation Discipline | Let-vs-lambda binding distinctions and coercion-only annotation handling already preserve the thesis let translation discipline: typed lets desugar to annotated RHS terms and do not recover declared-scheme behavior or scope leakage. | Refactors preserve the alternative let typing / trivial-scheme identity behavior from `§15.2.6` and do not reintroduce the unusual scope leakage that the thesis avoids by revising let translation. | Fresh verifier sweep found no active gap: the live parser/frontend/constraint path already keeps typed lets as annotation sugar, and the row is now explicitly tracked as a thesis-owned boundary. | Thesis `§15.2.6`, `§15.2.6.1`; `src/MLF/Frontend/Syntax.hs`; `src/MLF/Frontend/Desugar.hs`; `src/MLF/Frontend/ConstraintGen/Translate.hs`; `src/MLF/Elab/Pipeline.hs`; `test/FrontendParseSpec.hs`; `test/ConstraintGenSpec.hs`; `test/ElaborationSpec.hs`; `implementation_notes.md` | YES | Keep typed-let and coercion-only guards green; only simplify helper structure around the existing let-scope translation contract. |
| Translatable Presolution Boundary | Translatability validation and rigidification are already explicit and well-tested in the presolution path, and the campaign now treats this zone as graph/presolution logic first rather than a generic recursion-schemes target. | `Definition 15.2.10` / `Theorem 15.2.11` remain enforced exactly: no inert-locked nodes, rigid non-degenerate scheme roots, rigid arrow nodes, and rigid non-interior gen-node children. | Fresh verifier sweep confirmed the row is thesis exact as a guardrail: live validation/rigidification code plus O15 translatability tests already enforce the required boundary. | Thesis `§9.2.6`, `Definition 15.2.10`, `Theorem 15.2.11`; `docs/thesis-obligations.yaml` `O15-TRANS-*`; `src/MLF/Constraint/Presolution/Validation.hs`; `test/Presolution/EnforcementSpec.hs`; `test/TranslatablePresolutionSpec.hs`; `implementation_notes.md` | YES | Treat validation/rigidification as an explicit non-goal boundary except for tiny pure helper extraction behind the translatability guards. |
| Typing Environment Construction | The production elaboration path now has direct row-owned evidence for `Γ_{a′}` construction: lambda bodies keep the enclosing binder payload on the live path, let bodies see `x : Typ(b)` rather than the let’s generalized surface scheme, and the recovered live-path entries stay well-scoped under the same pipeline that elaborates terms. | Simplification preserves the exact lambda/let/subterm/edge environment discipline from `Definition 15.3.6` and continues to uphold `Property 15.3.7` on the live production path. | The verifier gap is closed: `docs/thesis-obligations.yaml` now carries direct row-owned `O15-ENV-*` anchors, and the live-path elaboration regressions exercise both the lambda-side and let-side environment extensions plus bound well-scoping. | Thesis `Definition 15.3.6`, `Property 15.3.7`; `docs/thesis-obligations.yaml` `O15-ENV-LAMBDA`, `O15-ENV-LET`, `O15-ENV-WF`; `src/MLF/Elab/Run/Pipeline.hs`; `src/MLF/Elab/Elaborate.hs`; `src/MLF/Elab/Run/Scope.hs`; `test/ElaborationSpec.hs`; `test/ScopeSpec.hs`; `implementation_notes.md` | YES | Preserve the row5 production-path environment anchors and keep the `ga′` / `letScopeOverrides` guard slices green through future cleanup. |
| Computation Context Construction | Computation-context search and edge translation are already explicit in `Phi.Context`, `Phi.Omega`, and pipeline wiring, with direct context-find/reject regression tests anchoring binder/order-sensitive insertion points. | Refactors preserve `C^{r→n}` construction, rejection behavior, and binder/order-sensitive insertion points from `§15.3.4` / `§15.3.5`; helper cleanup must not change reachable-target or fallback semantics. | Fresh verifier sweep confirmed the row is already thesis exact: context search remains an explicit elaboration mechanism rather than an ordinary tree fold, and the direct guards match the thesis claims. | Thesis `§15.3.4`, `§15.3.5`; `docs/thesis-obligations.yaml` `O15-CONTEXT-FIND`, `O15-CONTEXT-REJECT`, `O15-EDGE-TRANSLATION`; `src/MLF/Elab/Phi/Context.hs`; `src/MLF/Elab/Phi/Omega.hs`; `src/MLF/Elab/Pipeline.hs`; `test/ElaborationSpec.hs`; `implementation_notes.md` | YES | Preserve explicit `Phi.Context` / `Phi.Omega` logic and require the context-find/reject guard slice before accepting helper cleanup. |
| Binder-Safe Tree Recursion Coverage | The codebase already uses recursion-schemes selectively on genuinely tree-shaped structures, and the active campaign now carries an explicit exhaustive traversal inventory that classifies the remaining hotspots as `safe fold`, `already recursion-schemes-backed`, `keep explicit`, or `graph-boundary`. | Add recursion-schemes only where an explicit per-traversal audit shows the data is truly tree-shaped and binder/capture semantics are unchanged; keep binder-sensitive traversals explicit. | The verifier gap is closed: the campaign inventory now names the active traversal hotspots and freezes which ones are safe fold targets versus explicit semantic boundaries. | Thesis `§15.3.3`–`§15.3.6`, `§14.4.2`, `§14.4.3`; `src/MLF/Util/RecursionSchemes.hs`; `src/Util/IndexedRecursion.hs`; `src/MLF/Frontend/Desugar.hs`; `src/MLF/Frontend/Normalize.hs`; `src/MLF/Elab/TermClosure.hs`; `src/MLF/Elab/Reduce.hs`; `src/MLF/Elab/Elaborate.hs`; `src/MLF/Constraint/Presolution/WitnessCanon.hs`; `implementation_notes.md` | YES | Use the inventory in `implementation_notes.md` as the authoritative scope boundary for future recursion-refactor work. |
| Graph-Phase Explicitness Guardrail | Reify, presolution finalization, witness normalization/canonicalization, and related graph phases remain explicit and graph-aware in the live codebase, with targeted semantic tests backing them. | Preserve explicit graph-aware algorithms wherever thesis semantics depend on interiors/frontiers, canonicalization, permissions, copy maps, ordered operations, or witness normalization; success may be preserving explicit code rather than introducing abstractions. | The verifier gap is closed: the campaign now has an explicit negative guardrail that keeps graph-sensitive phases out of broad recursion-schemes pressure unless a later row-specific thesis audit widens scope. | Thesis `§4.4`, `§5.3.2`, `§5.4.3`, `§6.2`, `§9.2.6`, `§10.4.1`, `§15.2.7`, `§15.3.4`; `src/MLF/Constraint/Presolution/Driver.hs`; `src/MLF/Constraint/Presolution/Validation.hs`; `src/MLF/Constraint/Presolution/WitnessCanon.hs`; `src/MLF/Reify/Core.hs`; `test/TranslatablePresolutionSpec.hs`; `test/Presolution/EnforcementSpec.hs`; `implementation_notes.md` | YES | Treat the graph-phase guardrail in `implementation_notes.md` as an explicit campaign non-goal unless new row-specific thesis evidence says otherwise. |

## Gate Rules

- A row is `YES` only when behavior is objectively aligned with the thesis, the active code/test anchors still match the claim, and the required verification passes.
- Use only `YES` or `NO` (never `PARTIAL`, `N/A`, or prose gates).
- Keep mechanism order fixed; improving-loop rounds always anchor on the first `NO` row.

## Verification Cadence

- Row-focused verification must use the thesis/code/test anchors named in the row.
- Safety/regression verification should prefer the narrowest relevant guard suite first.
- Any code-changing attempt must end with the full gate: `cabal build all && cabal test`.

## Notes

- This table intentionally mixes positive refactor rows and explicit guardrail rows so the campaign stays thesis exact.
- Guardrail rows may close by preserving explicit graph-aware code plus tests/docs; they do not require a recursion-schemes rewrite.
